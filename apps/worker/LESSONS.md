<!--
  TEMPLATE: area LESSONS.md → write to <code-area>/LESSONS.md (one per code area).
  This file is EMPTY by design at bootstrap. Do NOT invent lessons. The header,
  the format block, and the "lessons start at §1" note are all that ship.
  Lessons accrete through /tdd Step 9 → orchestrator hot-routing. Delete this comment.
-->

# LESSONS.md — System of Work Assistant (control-plane worker (storage · Temporal · API))

> Full prose for every lesson logged during work in `apps/worker/`. The compact index lives in `apps/worker/CLAUDE.md` "Lessons logged" table.
>
> **Lesson numbers are stable IDs.** New lessons get the next sequential number. Numbers may be referenced from code comments, commit messages, and cross-references between lessons. **Don't reorder; don't reuse a deleted number's slot.**
>
> **Lessons start at §1.** Each code area has its own lesson sequence — lessons don't carry across code areas.

---

## Lesson format

```markdown
## <a id="N"></a>N. <Short topic> — <one-line rule>

**Date:** YYYY-MM-DD.
**Source slice:** <slice-id or commit hash>.

<2-5 paragraphs explaining: what was discovered, why it matters, how to
apply the rule, what edge cases are still open. Cite file:line references
where applicable.>

**Rule:** <one-sentence summary, same as the heading subtitle>.
```

---

## <a id="1"></a>1. On-request Copilot synthesis skills reuse a single-sourced governed core — never re-implement the gate

**Date:** 2026-07-12.
**Source slice:** C6 (b)-1 `b1048c3` (brief 040) + confirmed by (b)-2 `048d13e` (brief 041).

An on-request Copilot synthesis SKILL (Q&A, briefing, concept-synthesis, …) is a tRPC `.query()` procedure that **supplies its own retrieval** and then routes through the **one shared governed core** — never a re-implemented egress/gate. In `apps/worker/src/api/procedures/copilot.ts` that core is `runGovernedCopilotSynthesis(deps, workspaceId, question, scopedContext)` over `GovernedCopilotSynthesisDeps {synthesis, workspacePosture, routeSelector}`: WS-8 re-guard (`enforceRetrievalScope`) → authoritative server-side posture → route select → egress veto BEFORE synthesis → synthesize on the **veto-cleared route** (`decision.value.route`, never re-selected — the P1.2b binding) → `toUiSafeCopilotAnswer` candidate/UI-safe gate. `CopilotDeps` is just `GovernedCopilotSynthesisDeps` + `retrieval`.

What VARIES per skill is only the **retrieval source** and the **directive**; the safety machinery does not. Briefing (`copilotBriefing.ts`) supplies a `CopilotBriefingRetrievalPort` that assembles the §9.4 Today read-model into a `RetrievedContext` (already-UiSafe items + counts only — no-raw by construction) and a server-fixed `BRIEFING_DIRECTIVE`. Concept (`copilotConcept.ts`) reuses `CopilotRetrievalPort` (workspace knowledge) + a `CONCEPT_DIRECTIVE` framing — reusing the core so verbatim that `copilot.ts` was byte-unchanged (both Step-8 reviewers confirmed). Extracting the core once means the egress veto + candidate gate + WS-8 re-guard **cannot drift** between skills; adding a skill is a retrieval + a directive + a mount, reviewed against the same invariants each time.

Two corollaries worth keeping: (1) a skill with a **client-supplied** term (concept) carries the Q&A injection posture — bound it at the transport parser (`parseConceptInput`, ≤200 + single-line via a code-point guard, LESSONS Unicode-in-regex) — while a **server-fixed** directive (briefing) has a smaller surface. (2) When a retrieval reads a read-model whose **port** returns raw (e.g. `approvalInbox`→raw `Approval`), redact at the retrieval (read `.length` only), so no raw content enters the egressed `blocks` — no-raw *by construction* beats trusting a downstream projection.

**Rule:** an on-request Copilot synthesis skill supplies its own retrieval + reuses the single-sourced governed core (`runGovernedCopilotSynthesis`) — never a re-implemented gate; the retrieval source varies, the safety machinery does not.

## <a id="2"></a>2. Activate a built-but-dormant boot capability with a pure fail-safe gate helper (default-OFF)

**Date:** 2026-07-12.
**Source slice:** auto-ingest slice 1 `727ab76` (brief 042).

To make the shipped app run a capability whose machinery is already built but flag-gated, add a PURE `gate…(opts) → wiring | undefined` helper (mirroring `gateCopilotVaultReadDeps`, `boot.ts:441-463`): it returns the wiring object ONLY when the owner opt-in AND every precondition are present, else `undefined`. The worker-host reads owner config (env/IPC), calls the helper, and spreads the result into `bootWorker` — so with the opt-in unset the boot is **byte-equivalent to the prior degraded boot** (no new construction, no behavior change). Two properties make it safe: (1) any expensive/side-effectful dep is built inside a **thunk** the helper invokes only on the ON path, so the OFF path constructs nothing (unit-test that the builder is never called when OFF); (2) owner-configurability lives in the Electron `main` process (where `process.env` is already read) and threads through the IPC config the same way an existing field does — never hardcode a path (`gateAutoIngest` reads `SOW_INGEST_WATCH`/`SOW_VAULT_ROOT`/…, default OFF).

The security dividend: default-OFF + owner-opt-in IS the activation authorization. Nothing real happens until the owner opts in, so the review surface shrinks to "is the OFF path byte-unchanged, and is every ON-path dep behind the gate." A subtlety worth flagging at review: activating one capability can incidentally satisfy a DORMANT sibling's precondition (auto-ingest-ON defines `proofSpineParams`, which the propose/semantic-dispatch also needs) — call it out; the sibling stays off by its OWN designed locks, not by the incidental barrier you just removed.

**Rule:** activating a built-but-dormant boot capability = a pure fail-safe gate helper (default-OFF, thunk'd deps, owner-config not hardcoded) that augments `bootWorker` only when the opt-in AND its precondition are both present; the shipped default stays byte-equivalent to the prior boot.

## <a id="3"></a>3. A durable KnowledgeWriter-idempotency store is an operational-store repo, fail-closed both directions

**Date:** 2026-07-12.
**Source slice:** auto-ingest slice 2a `bbabd5f` (brief 043).

The `KnowledgeRevisionStore` that backs KnowledgeWriter exactly-once (`getByIdempotencyKey`/`record`) must be a real `@sow/db` operational-store repo, NOT an in-memory `Map` — the Map loses exactly-once across a worker restart (a fresh instance sees an empty store → re-commit). Build it as a Drizzle table keyed by `idempotencyKey` (PRIMARY KEY) with `INSERT … ON CONFLICT DO NOTHING` (first-write-wins: `record` returns void, so exactly-once == idempotent no-op, keeping the FIRST — this deliberately DIVERGES from sibling repos that signal a typed conflict, because the interface has no error channel). It passes the ONE `repository-contract.test.ts` suite on BOTH dialects (no dialect-specific SQL; Postgres never a stub). The durability property is inherently a FILE test (temp-file SQLite, close→reopen→fresh repo sees the row) — the in-memory contract suite structurally can't test restart.

The load-bearing safety property is **fail-closed in BOTH directions**: a real `DbError` on `getByIdempotencyKey` OR on `record` must PROPAGATE (reject), never be masked as `undefined`/swallowed — the only `undefined` is a genuine `not_found`. A masked lookup error reads to the writer as "no prior commit" → re-commit (duplicate); a swallowed `record` error means the writer believes it persisted when it didn't → replay re-commits. The consumer (the KnowledgeWriter `applyPlan`, via `createCommitActivity`'s try/catch) must honor the rejection as `commit_failed`, no re-commit. Adapter lives at the composition root (`apps/worker/src/composition/`) because `packages/knowledge` must not import `packages/db` (layer ban). Audit content persists summaries only (REQ-S-003 — no secret/raw column).

**Rule:** a durable KnowledgeWriter-idempotency store is an operational-store repo keyed by `idempotencyKey` (UNIQUE, first-write-wins), passing the one repo-contract suite on both dialects, fail-closed on BOTH `getByIdempotencyKey` AND `record` (reject, never mask); the in-memory Map is an honest interim that loses exactly-once across restart.

## <a id="4"></a>4. Activate a real sole-writer commit by swapping the fake for the existing adapter — never a new writer

**Date:** 2026-07-12.
**Source slice:** auto-ingest slice 2b `a6cf0ec` (brief 044).

To make a deterministic-fake commit leaf real, DON'T write a new writer — swap the fake `CommitKnowledgePort` for the EXISTING `createCommitActivity(deps)` (`packages/workflows/src/activities/commitKnowledge.ts`) over the sole-writer `applyPlan` (the meeting flow already uses it — mirror `buildActivities.ts:382-391`). Three details are load-bearing: (1) leave `ownershipCheck`/`secretScan` UNSET so `applyPlan`'s REAL defaults (`enforceHumanOwnership`/`scanForSecrets`) engage — a pass-through would silently disable the sole-writer + secret guards; (2) `expectedBaseRevision` must be a RESOLVER (`() => readVaultHeadRevision(vault)`) not a fixed value, because the live vault head moves between commits; (3) construct the real writer deps + durable store LAZILY behind the same default-OFF gate (rebind the shared `proofSpineParams.revisions` in `bootWorker` after backends are built, gated on `proofSpineParams !== undefined`) so nothing is constructed and nothing persists on the OFF path.

The fail-closed carry-forward from the durable store (lesson §3) is honored WITHOUT touching the sole writer: `createCommitActivity` already wraps `applyPlan` in a try/catch → `commit_failed`, and its doc-comment names the revision store as the throwing substrate — so a store rejection fails closed at the activity boundary. Verify (don't assume) the consumer catches it before concluding "no writer change." A post-commit `record`-reject is the writer's pre-existing `record-fault → System-Health` design: Temporal-retry → `not_found` → re-project onto the advanced head → empty diff → no-op (no duplicate). Watch for a DIFFERENT pre-existing gap the real commit EXPOSES: a fixed output path (`sources/<ws>/ingested.md`) that was harmless while the commit was fake becomes a collision once writes are real (→ a named per-source-path slice).

**Rule:** activate a real sole-writer commit by swapping the fake for `createCommitActivity` over `applyPlan` (real ownership/secret defaults + a live `readVaultHeadRevision` resolver + the durable store), behind the same default-OFF gate — never a new writer; verify the existing adapter honors the store's fail-closed.

## <a id="5"></a>5. A derivation must receive the per-item identity; a derived path must be traversal-safe by construction — and fork a shared port rather than dishonor it

**Date:** 2026-07-13.
**Source slice:** multi-file ingestion fix `ac78327` (brief 045).

Three durable lessons from making multi-file ingestion real:

**(1) A derivation collapses silently if it can't see what distinguishes its inputs.** The ingestion `build` derived a note path + planId from a STATIC boot binding (`sourceRef:"autoingest-src"` for every file) — so every dropped file produced the same planId → the durable store replayed the 2nd → ONE note, no error, "applied" each time. The real per-file identity (`context.source` = `file:<ws>:<relPath>` + `contentHash`) was in scope at the driver but never threaded into the `build` port. The build stage MUST receive the per-item identity; passing only `(validated, ws)` was the actual bug. Symptom to watch: a "successful" pipeline that silently produces one output for many distinct inputs (a replay-away, not a crash).

**(2) A derived filesystem path is an injection boundary — make the WHOLE path traversal-safe by construction.** Derive the variable segment as a `sha256` hex digest of the identity (a hash can't contain `..`/separators/NUL — safe by construction, deterministic, collision-free) rather than sanitizing a raw ref. AND guard every OTHER interpolated segment: `WorkspaceId` is a bare branded string (`ids.ts` rejects only empty/whitespace — NO charset validation), yet it's interpolated into `sources/${ws}/…`; an unsafe `ws` would break cross-workspace confinement. Guard it `^[A-Za-z0-9_-]+$` (anchored, ASCII — the JS `$`-without-`m` trailing-newline bypass must be empirically ruled out), fail-closed. Note the vault-root guard (`createFsVault.abs`) backstops only WHOLE-vault escape, not cross-workspace-within-vault — so the derivation-side ws-guard is the PRIMARY WS-8 cross-workspace defense. Narrow the derivation's INPUT TYPE too (`{sourceId, contentHash}`, not the full envelope) so attacker-influenceable fields (`origin`/`routingHints`) are structurally excluded from the path surface (least privilege).

**(3) When a shared port's consumers diverge, FORK it — don't dishonor it.** The `BuildOutputsPort` was shared by meeting-closeout, source-ingestion, AND hermes (cron/kanban — no `contentHash`). Adding a required per-file `source` to the SHARED port would have forced hermes to fabricate a fake contentHash (a dishonest param). The right move is a DEDICATED `SourceBuildOutputsPort` — the shared port stays byte-unchanged (zero regression by construction for the other consumers), and it matches the existing precedent (crossCalendar/projectSync already fork their own build-outputs ports). A port is shared only while its consumers' shapes coincide; when one genuinely diverges, forking is more honest + more conservative than widening.

**Rule:** thread the per-item identity into a derivation (a static seed collapses many inputs to one output silently); derive filesystem paths traversal-safe by construction (hash the identity + guard every interpolated segment incl. the workspace id, narrow the input type); and fork a shared port when a consumer diverges rather than forcing a dishonest param.

## <a id="6"></a>6. A trust label must be co-located with the bytes it vouches for — verify the consumer's pairing before compacting a positionally-paired array

**Date:** 2026-07-13.
**Source slice:** C5.4b slice 1 `4650ad4` (brief 046).

The Copilot content-trust decision stamps `sources[].provenance = knowledge_writer`, but the model synthesizes over `RetrievedContext.blocks` — a **separate array** from `sources`. So stamping a source trusted while leaving `blocks` untouched leaves a trust label sitting over bytes the gate never proved. The fix carries each admitted citation's rehydrated `AdmittedFact.content`+`mdContentSha` on the serving verdict and REBUILDS `blocks` from those proven bytes — a trust label and the bytes it vouches for must be co-located, never on two arrays that can disagree.

The load-bearing subtlety surfaced at Step 7.5: `buildCopilotUserPrompt` pairs `blocks[i]`↔`sources[i]` **positionally**. A first design "compacted" `blocks` to just the admitted subset (a shorter array) — which SHIFTS the alignment, so a proven excerpt renders under the wrong (untrusted) citation. The correct rebuild is POSITIONAL: `blocks = sources.map(s => admitted.get(s.citationId)?.content ?? "")`, preserving `blocks.length === sources.length` while still dropping unverified bytes (unadmitted → `""`). The general rule: before you compact/rebuild ONE of two positionally-paired arrays, read the CONSUMER and confirm its pairing contract — an index-keyed pairing turns "drop an element" into "misattribute every element after it."

Fail-closed axes to preserve on such a rebuild: a malformed verdict value (non-string content), a foreign/subset key, a degraded/empty verdict — each strips to all-untrusted with `blocks` untouched (never blank a read-only answer). Carry the proven `mdContentSha` even if unused today (honest verdict + enables a later content↔sha integrity re-check); the gate already proved integrity, so the decorator does NOT re-hash.

**Rule:** stamping a source trusted requires rebuilding the synthesized-over `blocks` from the SAME proven bytes (a trust label never sits on a separate array the model reads); and before compacting one of two positionally-paired arrays, verify the consumer's index-pairing contract — rebuild positionally (`""` at dropped indices), never compact.

## <a id="7"></a>7. A serve-time coverage/health reader with no persisted source degrades HONESTLY — a fail-closed constant + an in-code note naming the missing store, never a green stub

**Date:** 2026-07-13.
**Source slice:** C5.4b slice 2 `286910f` (brief 047).

Building the serving oracle's boot-side readers surfaced that there is **no serve-time `ParityReport` store** — the reconciler produces reports but nothing persists/queries them at serve time (`write-through-flag.ts` takes `latestParityReport` as an INJECTED input, not a stored read). The honest reader (`createServingCoverageReader`) therefore returns `parity = undefined` and `oracleBuildOk = false` UNCONDITIONALLY (both force `isDegradedCoverage`), with only `pinValid` a real signal (via `checkVersionPin`). Crucially it is a REAL adapter that degrades because the source is absent — NOT a stub that fakes green — and an in-code comment NAMES the missing store as the injection point, so a future slice wiring real parity persistence knows exactly where it plugs in (this is the C5.4a honest-interim pattern applied to a coverage/health leg). A green stub would be a silent false-positive on the trust gate; a documented fail-closed constant is safe and self-explaining.

Two reader-design corollaries banked here: (1) the committed-vault reader skips a listed-but-unreadable `.md` and returns the READABLE subset (undefined only when zero remain) — an incomplete allow-set can only make a citation WITHHOLD (fail-safe), never falsely admit, so a race-deleted file shouldn't degrade the whole workspace; a genuine list/read THROW fails the whole read closed. (2) When a read seam is synchronous but its real implementation needs async I/O, widen the seam to the codebase's existing sync-OR-async `MaybeAsyncResult` union (`(x) => T | undefined | Promise<T | undefined>`) + one `await` at the consumer (a no-op on a sync value) — this fits the real async reader with ZERO ripple to existing sync fakes, and reuses an established precedent (`CopilotRetrievalPort.retrieve`) rather than forcing every fake to become async.

**Rule:** a serve-time coverage/health reader with no persisted source degrades honestly (a fail-closed constant + an in-code note naming the missing store as the injection point), never a green stub; incompleteness must fail SAFE (withhold, not admit); widen a sync seam to the `MaybeAsyncResult` union rather than rippling async through every fake.

## <a id="8"></a>8. A go-live seam is AND-composed of independent OFF-locks — each alone keeps the capability OFF, and the shipped default stays byte-equivalent

**Date:** 2026-07-13.
**Source slice:** C5.4b slice 3 `3d56dc6` (brief 048).

Wiring a safety-critical capability into boot so it is CONSTRUCTIBLE + proven-selectable but stays OFF is best done as several INDEPENDENT OFF-locks, each individually sufficient to keep the capability off (defense-in-depth beyond the single flag of Lesson 2). The serving oracle's go-live seam has three: (1) the arming flag `copilotServingOracleGoLive` unset (default) ⇒ `goLiveArmed` false ⇒ the selector returns the interim degraded oracle; (2) `loaderBacked` is constructed ONLY when a signing key is provisioned, so absent a key it is `undefined` and the selector's `goLiveArmed && loaderBacked!==undefined` guard falls back to interim — a STRUCTURAL lock (the Keychain SecretsPort is HITL/unbuilt, so this holds today even if the flag were flipped); (3) the real coverage reader degrades by reality (`parity===undefined`) and is NOT config-defeatable (no `BootConfig` field can inject green coverage — that lives only in tests). Verify the locks are genuinely independent by adversarial pairwise-defeat: defeat any two, the third must still hold the line, and NO config combination admits a non-KW source.

Two properties keep it safe + reviewable: (a) the shipped default (all new config fields absent) is BYTE-EQUIVALENT to pre-seam — pin it (unset ⇒ interim selected, nothing new constructed); (b) extract the go-live logic into PURE helpers (`buildServedVaultResolver`, `buildLoaderBackedServingOracle`) so it is unit-testable without booting the huge async `bootWorker`, and construct nothing eager on the OFF path (the factory defers the fs-backed loader/readers/gate until called). WS-8 on a shared single dev vault: the resolver maps ONLY the single served workspace to the one vault (a second workspace can never resolve to it; unset ⇒ empty map ⇒ all degrade), with the writer's HMAC (binding `workspaceId`) as the real backstop — a mis-resolved foreign vault's notes fail `signature_invalid`.

**Rule:** a go-live seam is AND-composed of independent OFF-locks (arming flag AND a provisioned key AND real coverage) — each individually sufficient to keep the capability OFF, proven by adversarial pairwise-defeat; the shipped default stays byte-equivalent to pre-seam, and the go-live logic lives in pure, unit-testable helpers that construct nothing on the OFF path.

## <a id="9"></a>9. A secrets adapter maps EVERY fault to a typed ref-only error, never throws, holds key bytes only transiently — real I/O behind a mockable backend seam

**Date:** 2026-07-13.
**Source slice:** 11.4 slice 1 `4010155` (brief 049).

The `KeychainSecretsAdapter` implementing `SecretsPort.resolveSigningKey` maps every failure mode to a typed `SecretUnresolved` carrying ONLY the opaque ref + a FIXED class token (`missing|locked|denied|backend_error|invalid_ref`) — never the key bytes, never a backend's raw stderr/`detail` (the seam's optional `.detail` exists for backend-isolation debug and the adapter DROPS it). The adapter never throws (a throwing/rejecting backend, a rogue/prototype `kind`, a malformed ref — all fail-closed to a typed err; `Object.hasOwn`-guard the reason lookup). Key bytes pass straight through as `Uint8Array` (never `.toString()`/stringified/logged), held only in the return binding; a ZERO-LENGTH key is REJECTED (never serve an empty HMAC key). The opaque `keychain://<service>/<account>` ref is parsed fail-closed (exactly 2 non-empty segments, a bounded charset that bars `.`/`..`/leading-`-`/shell-metacharacters, a length cap) with ZERO backend calls on a malformed ref.

Structure real I/O behind a MOCKABLE backend seam (`KeychainBackend.read(service, account)`), so the adapter core + all its tests run against a fake in-memory backend and the BUILD never touches the real Keychain — the real backend is a separate slice, wired only under owner provisioning. A distinct `invalid_ref` reason (config error) vs the Keychain-runtime faults matters: the boot-side degraded controller routes runtime faults to the retryable-on-unlock path but a bad ref to a NON-retryable config surface.

**Rule:** a secrets adapter maps every fault to a typed ref-only error (a fixed class token, never the value or raw backend detail), never throws (fail-closed on rogue kind / malformed ref / backend throw), holds key bytes only transiently (never stringified/logged; reject a zero-length key), fail-closed-parses the opaque ref (0 backend calls on malformed), and puts real I/O behind a mockable backend seam so the build never touches the real store.

## <a id="10"></a>10. A CLI-backed secrets reader: args-array + absolute bin, secret only in the ok Result, robust stderr-pattern faults — and de-alias returned bytes (a Buffer's `.slice` shares memory)

**Date:** 2026-07-13.
**Source slice:** 11.4 slice 2 `4db092e` (brief 050).

The `security find-generic-password` backend runs over an INJECTED, mockable execFile-shaped `exec` seam (so the parse is unit-tested against synthetic `{code, stdout, stderr}` — the real `security` binary runs only at owner-provisioning). Invoke the ABSOLUTE bin `/usr/bin/security` (no PATH lookup ⇒ no binary-hijack) with an args ARRAY (never a shell string); argv-injection is then structurally impossible — no shell, absolute bin, getopt OPTION-VALUE semantics (a leading-`-` value after `-s`/`-a` is consumed literally, never reparsed; a `--` separator is inapplicable here and would only risk enabling the unwanted trailing `[keychain]` positional). Classify faults by STDERR PATTERN (`\blocked\b` word-boundary, "interaction not allowed" ⇒ locked; "denied"/"not authorized"/"auth failed" ⇒ denied; else backend_error) not brittle numeric codes — with an in-code note to verify the real codes/strings against the live binary at provisioning; a fault can NEVER map to code 0 (garbage stdout on spawn-failure/timeout is never mistaken for a success value). Rule 7: the `-w` stdout IS the secret — it reaches ONLY the ok `Uint8Array`; the fault path reads ONLY code+stderr (stdout can't reach `detail` by construction); `detail` bounds ≤200 + scrubs secret-shaped runs.

**The load-bearing gotcha (a review catch):** DE-ALIAS the returned key bytes with `new Uint8Array(bytes.subarray(0, end))`, NEVER `bytes.slice(0, end)` — a Node `Buffer` (exactly what the real execFile yields) OVERRIDES `Uint8Array.prototype.slice` with shared-memory VIEW semantics, so `.slice` would silently alias the ~4KB allocation pool on the ONE path carrying a real secret (a heap-leak vector invisible to a test that uses a plain `Uint8Array`). The `subarray`-into-a-fresh-`Uint8Array` constructor always copies, Buffer or not — pin it with a `Buffer.alloc(...).subarray(...)` input test.

**Rule:** a CLI-backed secrets reader uses an args-array + absolute bin (no shell, no PATH), returns the secret ONLY in the ok Result (fault path never reads stdout), classifies faults by stderr PATTERN with a live-verify note (a fault never maps to code 0), bounds+scrubs any debug detail — and DE-ALIASES returned bytes via `new Uint8Array(subarray)`, NEVER `.slice` (a Node Buffer's `.slice` shares the backing pool).

## <a id="11"></a>11. A real-I/O adapter is boot-wired behind an owner-provisioning gate — default-absent ⇒ inert/byte-equivalent, the real backend built only on the provisioned path

**Date:** 2026-07-13.
**Source slice:** 11.4 slice 3 `536d3b2` (brief 051).

Wiring a real-I/O adapter (the Keychain `SecretsPort`) into `bootWorker` is done behind an owner-provisioning GATE (`BootConfig.keychainSecrets`, absent by default). `buildKeychainSecrets(gate)` constructs the adapter + the real bounded no-shell execFile backend ONLY when the gate is present, returns `undefined` otherwise — so the shipped default spawns NO real process and is byte-equivalent to pre-slice. Pin the inert default with a factory SPY asserting ZERO off-path invocations of the real-exec factory (not just "returns undefined"). Source the one live consumer fail-closed-first: `provenanceServingOracle.secrets = keychainSecrets?.secrets ?? bundle.secrets` (the gate is the real source; the inline bundle stays a test fallback; making the field optional keeps the prior assembly tests green AND makes OFF-lock 2 more fail-closed — omission ⇒ interim). A facade for a NOT-YET-WIRED consumer (the provider `getSecret`) can ship built+tested with an explicit reachability sub-note, but do NOT thread it into a dormant path — defer that (and a degraded-controller routing whose call-site is dormant) to a named follow-up slice rather than wiring dormant-on-dormant with nothing to exercise.

**Rule:** boot-wire a real-I/O adapter behind an owner-provisioning gate (default-absent ⇒ inert, byte-equivalent, the real backend/process built ONLY on the provisioned path — pinned by a factory-spy zero-invocation assertion); source the live consumer fail-closed (`gate?.x ?? fallback`, optional field ⇒ omission degrades); ship a facade for a not-yet-wired consumer only with a reachability sub-note, and defer a routing whose call-site is dormant to a named follow-up rather than wiring dormant-on-dormant.

## <a id="12"></a>12. A serve-time trust-signal store is a fail-closed `@sow/db` operational-store repo behind a narrow fakeable read-port — deterministic latest-ordering (secondary tiebreak) + a read-back identity re-gate

**Date:** 2026-07-13.
**Source slice:** 11.1 serve-time ParityReport store `ca10090` (brief 052, Brick-B slice 1).

A store that feeds a serve-time trust decision (here the C5.4b coverage kill-switch reads the latest `ParityReport`) is a real `@sow/db` operational-store repo, NOT an in-memory cache: a `parity_reports` table on BOTH dialects passing the ONE `repository-contract` suite (no dialect-specific SQL), an additive migration (`0006`, next-after-`0005`), `record` idempotent first-write-wins on the report id (UNIQUE — immutable operational truth, never two rows per id), and `getLatestForRevision(ws, rev)` returning the newest-recorded report or `undefined`. The frozen contract is stored **as-is** (payload as a JSON/text column, re-gated through the frozen `ParityReportSchema.parse` on read — a lossless round-trip); a store-side `recorded_at` (injected-clock) supplies the "latest" ordering the timestamp-free contract lacks (no contract change). Three sharp edges a naive port misses: **(a) fail-closed BOTH directions distinguishing fault-vs-absence** — a real `DbError`, an unparseable stored payload, OR an identity-mismatched payload returns a typed err `Result`, NEVER masked as a silent ok and NEVER collapsed into the `undefined` that means "no row" (the reader must degrade on a fault without treating it as "no report"). **(b) DETERMINISTIC latest-ordering needs a secondary tiebreak** — `desc(recorded_at)` alone lets a same-`recorded_at` collision between two distinct report ids pick an arbitrary row, and SQLite vs Postgres can disagree, so a clean report could shadow a dirty one (a trust-gate defeat, forbidden-pattern §2); add `desc(reportId)` as the deterministic dialect-agreeing tiebreak + tell the write-path caller to supply canonical ISO-8601. **(c) re-gate IDENTITY, not just shape, on read-back** — a directly-tampered ops-DB row whose columns match the query key but whose payload claims a different `(workspaceId, reconciledAtRevision)` would surface cross-workspace (WS-8); assert the parsed payload's ws/rev == the query args, else typed err. Front the repo with a NARROW read-port exposing only the serve-time read (`getLatestForRevision`) that REJECTS on fault — `record` stays on the repo for the write-path slice — so the serve-time reader consumes a minimal fakeable seam.

**Rule:** a serve-time trust-signal store is a fail-closed `@sow/db` operational-store repo (dual-dialect contract suite + additive migration + idempotent-first-write-wins on id) fronted by a narrow fakeable read-port that REJECTS on fault; store the frozen report as-is + re-gate it through its schema on read; fail-closed BOTH directions distinguishing a fault (degrade) from a true no-row (`undefined`); make latest-ordering DETERMINISTIC with a secondary id tiebreak (a same-timestamp collision must not pick a dialect-arbitrary row that shadows a dirty report); and re-gate IDENTITY on read-back (parsed ws/rev == query key, else typed err) so a tampered row cannot surface cross-workspace.

## <a id="13"></a>13. Wiring a db-backed (async) source into a sync serve-time reader: widen the seam to the async sibling's union + keep the new dep OPTIONAL so the unbound seam degrades byte-equivalently

**Date:** 2026-07-13.
**Source slice:** 13.10 B2 serving-coverage reader parity wiring `daf4fa1` (brief 053, Brick-B slice 2).

When a serve-time reader gains a leg backed by an async source (here `createServingCoverageReader`'s parity leg reads the async db-backed `ParityReportStore`), the reader's returned function must become `async` — its **return type changes** from `T` to `Promise<T>`, which is NOT purely additive: every DIRECT caller (incl. the reader's own factory unit tests that read `.pinValid`/`.parity`) must `await`, so convert them in-slice (that's part of making it async, not scope creep). Do NOT ripple `Promise` through every fake of the CONSUMING seam — instead **widen the seam type to the sync-or-async union** (`(…) => Sources | Promise<Sources>`), MIRRORING the async sibling that already exists on that seam (the `CommittedVaultReader` is already `… | Promise<…>` and the loader `await`s it); the union keeps every existing SYNC fake valid (sync is a member, `await` no-ops on it) so the consumer's other tests stay untouched. Keep the new source dep **OPTIONAL** (`store?: …`): unbound ⇒ the leg resolves to its degraded value (`parity: undefined`) exactly as before, so the reader is **byte-equivalent to pre-slice** and boot needs no change — the real adapter binds in a later composition-root slice (the waiver-holder pattern, [20](LESSONS.md#20)). Fail-closed rides through unchanged: the async source REJECTS on fault, the reader's existing bare `catch` degrades ALL legs (a store fault never crosses the boundary, never a false green); a `catch` that also zeroes an independent leg (here `pinValid`) is the safe stricter direction (coverage is AND-composed — parity absence alone already degrades). Ship only the PARITY leg; leave a sibling leg (`oracleBuildOk`) at its degraded constant for its own slice — coverage stays AND-degraded, correct + inert.

**Rule:** to wire an async (db-backed) source into a sync serve-time reader, make the reader's fn `async` and convert its own direct-caller tests to `await` (in-slice — the return type changed); widen the CONSUMING seam to the sync-or-async union mirroring the async sibling already on it (existing sync fakes stay valid, `await` no-ops), don't ripple `Promise` through every fake; keep the new source dep OPTIONAL so the unbound seam degrades byte-equivalently and boot is untouched until a later composition-root slice binds the real adapter; fail-closed rides through the reader's all-legs-degrade `catch` (a source reject never crosses, never a false green); ship one leg at a time (a sibling leg stays at its degraded constant for its own slice).

## <a id="14"></a>14. A worker-side "record-only-on-ok" gate over a knowledge-layer reconcile Result — record the report VERBATIM, a reconcile err is a typed SKIP never a stored clean report, a sink fault REJECTS (fault ≠ skip)

**Date:** 2026-07-13.
**Source slice:** 13.10 B3 parity reconcile→store record path `07d6b0b` (brief 054, Brick-B slice 3).

When persisting a knowledge-layer reconcile output (`reconcileParity`'s `Result<ReconcilerOutcome, ReconcileError>`) into an operational store that feeds a **serve-time trust gate**, the write path is a WORKER-SIDE gate — `apps/worker` sees both `@sow/knowledge` and `@sow/db`, so a **type-only** import of the outcome types gives zero runtime coupling and no knowledge→db edge (the recording never lives in the knowledge package). Record from the RIGHT producer: the full `reconcileParity` (classifies the whole canonical∪db union → real `content_mismatch`), never a narrow primitive that sees one divergence class and would over-claim coverage. Three fail-closed properties, each pinned: **(a) record-only-on-ok, verbatim** — `isErr(outcome)` early-returns BEFORE the sink call (an err never reaches `record`); an `ok` forwards `outcome.value.report` BY REFERENCE, never synthesizing the trust fields (`cleanForServing`/`coverageComplete`) that carry the reconciler's real determination (pin with a `toBe` same-reference test — a missing/partial rebuild-oracle's `coverageComplete=false` must never read as complete-and-clean). **(b) a reconcile err is a typed SKIP** — return a discriminated `{ kind: "skipped_reconcile_error", error }` (the union has NO fault variant); the future caller routes it to health/degrade. **(c) a sink fault REJECTS (fault ≠ skip)** — a `record` `DbError` rejects through the gate (no try/catch swallow) so the caller degrades, distinct from the reconcile-err skip; the recorder port mirrors the read-port's reject-on-fault ([12](LESSONS.md#12)) and REUSES the same fault-rejection helper so rule-7 (drop the opaque cause, keep the code) holds by construction. Record DIRTY reports too (`cleanForServing=false` is operational truth — the serve-time degrade signal; dropping it blinds the gate). Ship the gate DORMANT + reachability-waivered (no trigger/caller); a later slice wires the reconcile trigger + the health routing.

**Rule:** a worker-side record gate over a knowledge reconcile `Result` records the report VERBATIM only on `ok` (`isErr` early-returns before the sink; forward the report BY REFERENCE, never synthesize the trust fields); a reconcile `err` is a typed `skipped` disposition never coerced into a stored clean report; a sink `DbError` REJECTS (fault ≠ skip — both fail-closed + distinguishable, reusing the read-port's fault-rejection helper for rule-7); record dirty reports too (a defect report is the serve-time degrade signal); type-only-import the knowledge output types (no knowledge→db edge); record from the full-coverage producer; ship dormant + waivered until a trigger slice.

## <a id="15"></a>15. A promise-REJECTION test whose assertions live only inside `.catch(cb)` is VACUOUSLY GREEN if the code ever resolves — capture the reason + assert unconditionally (fail loudly on resolve)

**Date:** 2026-07-13.
**Source slice:** 13.10 B3 — a convergent security + code-quality finding (`07d6b0b`).

A test that pins a REJECTION property — here the recorder's fault-rejection must carry `DbError.code` but NOT the opaque driver `cause` (safety rule 7 — no raw content/secret leak) — by attaching `.catch(cb)` and asserting INSIDE `cb` is a SILENT PASS if the code under test ever RESOLVES instead of rejecting: `cb` never runs, no assertion executes, the test is green while the exact property it claims to pin is unverified. Worst on a safety/redaction property — a regression that makes the path resolve (or leak) sails through CI. Force the outcome deterministically instead: `await promise.then(() => { throw new Error("expected rejection") }, (e) => e)` — capture the rejection reason, then assert the property (has-`code` / no-raw-content) UNCONDITIONALLY so a resolve throws loudly; or `await expect(promise).rejects.toThrow(...)` + assert on the captured error. Any test whose assertions live ONLY in a `.catch`/rejection callback is suspect — the assertions must run whether or not the promise rejects.

**Rule:** never let a rejection test's assertions live only inside `.catch(cb)` — it passes vacuously if the code resolves (a silent green on the pinned property, worst on a safety/redaction pin). Capture the rejection reason (`.then(() => { throw … }, e => e)` or `rejects.toThrow`) and assert UNCONDITIONALLY so a resolve fails loudly. `pattern:` grep review target — a test-block `.catch(` whose callback holds the only `expect(`; `accepted: reviewer-caught` (the mandatory dual review verifies rejection tests fail-on-resolve).

## <a id="16"></a>16. The waiver-holder CLOSING slice — bind the real adapter as a NESTED arg INSIDE the existing dormancy gate (byte-equivalent shipped default), distinguish construction from selection, prove the chain with a write→read e2e, and green ONLY the legs it wires (ASSERT the honest degrade)

**Date:** 2026-07-13.
**Source slice:** 13.10 B4 serving-coverage store boot-binding `8aef52d` (brief 055, Brick-B slice 4).

The waiver-holder pattern ([20](LESSONS.md#20)) lands dormant pieces behind reachability waivers, then ONE composition-root ENTRY makes the chain reachable in a single reviewed slice. Closing it correctly: **(a) bind the real adapter as a NESTED arg INSIDE the existing dormancy gate** — construct `createParityReportStoreAdapter(backends.repos.parityReports)` as a nested argument inside the pre-existing `provenanceStamping && bundle` ternary branch, NOT at boot's top level. JS lazy eval means the shipped default (no bundle) evaluates NOTHING nested (no adapter constructed, the repo not even accessed) ⇒ boot byte-equivalent to pre-slice; a top-level always-constructed binding would break that. **(b) construction ≠ selection** — distinguish the gate that CONSTRUCTS the wired capability (`provenanceStamping && bundle`) from the gate that SELECTS it live (`goLiveArmed`, via the selector). An unarmed-but-CONSTRUCTED capability is INERT (selection keeps the interim/degraded path); a review caught a comment saying "inside the triple-lock" that conflated the two (only 2 of 3 OFF-locks gate construction; the 3rd gates selection) — say "construction guard" vs "selection gate" precisely. **(c) prove the chain with a write→read round-trip e2e over a REAL repo** — compose the shipped seams (store adapter + reader + the record path) over a real (better-sqlite3) repo; it is GREEN-on-write (the seams already exist — integration coverage, not RED-first) and is the first test exercising them together. A boot-wiring LINE isn't cleanly unit-RED-able ⇒ cover it by typecheck + `/wired` (the trace proves the waiver closed), not a heavy `bootWorker` RED. **(d) green ONLY the legs it wires; ASSERT the honest degrade** — a deferred sibling leg (`oracleBuildOk`) keeps the AND-composed verdict degraded, so the e2e MUST assert `isDegradedCoverage` true (never fake a full green admission before every leg is real); a degrade-path case must pin the OTHER legs true (e.g. `pinValid===true`) so a degrade is provably its intended cause (a missing revision-scoped row) and not a swallowed fault masquerading as it (fault-vs-absence, [12](LESSONS.md#12)).

**Rule:** close a waiver-holder chain with ONE composition-root entry that binds the real adapter as a NESTED arg INSIDE the existing dormancy gate (lazy eval ⇒ byte-equivalent shipped default); distinguish the CONSTRUCTION guard from the SELECTION gate (an unarmed-but-built capability is inert); prove the chain with a write→read round-trip e2e over a REAL repo (green-on-write integration coverage; the boot line itself via typecheck + `/wired`, not a `bootWorker` RED); green ONLY the legs it wires and ASSERT the AND-verdict still degrades on any deferred leg (never a fake full-green), pinning the other legs true so a degrade is provably its intended cause, not a swallowed fault.

## <a id="17"></a>17. The last coverage-leg seam — make a hardwired-false trust-gate leg BINDABLE via an OPTIONAL fail-closed resolver (unbound ⇒ false ⇒ degrade); the AND-gate becomes green-CAPABLE (≠ armed), still DORMANT; the heavy status PRODUCER (real I/O) stays a deferred owner-gated deliverable

**Date:** 2026-07-13.
**Source slice:** 13.10 B5 rebuild-oracle `oracleBuildOk` coverage leg `57fef4b` (brief 056).

When a multi-leg trust gate (an AND of coverage legs) has ONE leg hardwired-`false` pending its source, make it BINDABLE with an OPTIONAL fail-closed resolver dep — MIRRORING the sibling boot-resolved leg (here `resolveOracleBuild?: () => boolean` mirrors `resolveRunning`/`pinValid`): source `leg = deps.resolve?.() ?? false` INSIDE the reader's existing try/catch, so **unbound ⇒ `false`** (byte-equivalent to the prior hardwired false), a **throwing probe degrades ALL legs** (fail-closed), and `?? false` never defaults true (fail-closed both ways). Because this is the LAST hardwired leg, making it bindable makes the whole AND-gate **green-CAPABLE** (reachable non-degraded) — pin that with a MILESTONE test (all legs bound-true ⇒ the gate greens). But be precise: **green-CAPABLE (all legs bound as fakes in a unit test) is NOT armed.** Production stays DORMANT — boot leaves the resolver unbound (⇒ the leg false ⇒ degrade) and the live-SELECT gate (here `goLiveArmed`, via the selector) stays OFF; the milestone test asserts `isDegradedCoverage` false, NOT an oracle selection/admission. Crucially, separate the no-I/O serve-time SEAM (this slice — reads a boolean status) from the heavy PRODUCER that COMPUTES the status (here the gbrain import-into-scratch = real I/O): the producer is a DEFERRED owner-gated build-time deliverable; the serve path never does the heavy work, it reads a status the producer persists/probes. Folding the producer's real I/O into the serve-time leg would be a hard-line crossing.

**Rule:** make a hardwired-false trust-gate leg BINDABLE with an OPTIONAL fail-closed resolver dep (mirror the sibling boot-resolved leg; `deps.resolve?.() ?? false` inside the reader's try/catch — unbound ⇒ false byte-equivalent, throw ⇒ all-legs-degrade, never defaults true); the LAST such leg makes the AND-gate green-CAPABLE — prove it with a milestone test, but keep green-CAPABLE (fakes bound in a unit test) DISTINCT from ARMED (production leaves the resolver unbound + the live-select gate off); separate the no-I/O serve-time SEAM (this slice) from the heavy status PRODUCER (real I/O — a deferred owner-gated build-time deliverable), never folding the producer's I/O into the serve path.

## <a id="18"></a>18. The producer→gate→sink composition seam — record BEFORE route (a store fault rejects before any routing), a sink fault propagates, own composition NOT input construction or trigger

**Date:** 2026-07-14.
**Source slice:** 13.10 piece A reconcile-pass composition `ca8d835` (brief 057, the reconcile-TRIGGER arc's smallest-safe-first slice).

When composing a multi-stage fail-closed pass — here `runReconcilePass` runs the pure `reconcileParity` producer → the B3 `recordReconcileOutcome` record gate ([14](LESSONS.md#14)) → routes the producer's ready-made `HealthItem`s through an injected sink — the composition helper owns ONLY the wiring: **never the input construction** (a later piece builds `req` from a real `GbrainReadGrant` DB projection + the canonical set) and **never the trigger / scheduling / burst-collapse**. Ordering is load-bearing + fail-closed: **record BEFORE route** — a store `record` fault REJECTS (via the gate) BEFORE any health routing, so a pass that didn't durably land surfaces NOTHING (the caller degrades + raises a store-fault item), kept distinct from a reconcile-error SKIP (records nothing, routes nothing — never coerced into a clean pass); a **health-sink fault PROPAGATES** (a trust-defect signal is never silently dropped). Match the sink port shape EXACTLY to what the producer emits (a `HealthItem`, no lossy decomposition) and DEFER the richer real binding — re-project each item through the OBS-2 `HealthSurface` for dedupe (a defect recurring every pass must bump ONE item's occurrence count, not spawn a fresh id-keyed row, since the reconciler mints a new id per pass) — to the piece where that surface is in scope. Read the outcome's health items via the producer's own `ok`-narrowing (`isOk(outcome)`; a disposition tag alone can't narrow the union) and keep an explicit "route only on a recorded (durably-landed) pass" clause as documentary defense on a trust-gate feeder even when the narrowing already implies it. Ship DORMANT + reachability-waivered (reachable only from its own test; the production caller = the later trigger piece), and drive the tests with the REAL producer over fake recorder/sink so the pass exercises the real gate, not a stubbed outcome.

**Rule:** a composition helper over a producer→gate→sink pass owns COMPOSITION only (never input construction or trigger); record BEFORE route so a store fault REJECTS before any routing (a pass that didn't durably land surfaces nothing — distinct from a reconcile-error skip that records+routes nothing), and a sink fault PROPAGATES (never a silent drop of a trust-defect signal); match the sink port to exactly what the producer emits and defer the richer OBS-2-dedupe binding to where the surface is in scope; narrow via the producer's own `isOk` (a disposition tag can't) + keep an explicit durably-landed clause as documentary defense; ship dormant + waivered, exercised by the REAL producer over fakes.

## <a id="19"></a>19. A read-only-adapter → DB-facts projection builder is fail-closed on COVERAGE — any fault / adapter-rejection / truncation (type-robust) / malformed row / absent-or-non-positive version ⇒ complete=false; conservative trust flags; source the workspace from the grant

**Date:** 2026-07-14.
**Source slice:** 13.10 piece B reconciler-db-projection builder `8a27e78` (brief 058, reconcile-TRIGGER arc).

A builder that projects a read-only gbrain adapter's response into the `ReconcilerDbProjection` the parity reconciler compares against must be fail-closed on the `complete` COVERAGE flag, because `complete` gates the reconciler's `coverageComplete` (and thence the serve-time trust gate) — a **false-complete is a trust-gate defeat.** `complete=true` ONLY on a clean, fully-consumed, well-formed read; degrade on EVERY incomplete signal: a read `err`, an adapter that REJECTS or resolves to a non-Result (a try/catch + non-Result guard preserves never-throws across an untyped injected boundary), a truncation/open-cursor signal that is TYPE-ROBUST (a non-boolean `truncated` / non-string `cursor` must ALSO degrade — a lax `typeof` check is a fail-OPEN on non-conforming pagination), a malformed row or envelope (collect the parseable rows, still mark incomplete), or an absent/non-positive schema version. Never throw (§16). Map trust-bearing flags CONSERVATIVELY — `stamped` is true only on an explicit `===true`, so an unstamped `db_only` fact stays visible for the reconciler to quarantine (a false `stamped=true` hides a hidden-brain defect, safety rule 1). Source the workspace identity from the GRANT-bound adapter, not a caller param — the projection's workspace can't then disagree with the read grant (WS-8), so a reconciler `workspace_mismatch` can only mean a genuinely mis-scoped canonical set. Where a required numeric field needs a degrade sentinel (`schemaVersion=0`), emit it ONLY with `complete=false` so it never reads as a real value on a complete projection. Bound the paging-field coverage to the fields the fake transport models by design ({`truncated`, `cursor`}); the POSITIVE-completeness-token upgrade (default-incomplete unless explicitly signaled; reject unknown "more-results" field names; a row-count-vs-stated-total cross-check) needs the real transport shape → an owner-gated follow-up that lands with the live `GbrainReadGrant` binding.

**Rule:** a read-only-adapter → DB-facts projection builder is fail-closed on the COVERAGE flag — `complete=true` ONLY on a clean, fully-consumed, well-formed read; ANY of {read err, adapter rejection/non-Result, truncation-or-open-cursor (TYPE-ROBUST: a non-boolean/non-string signal still degrades), malformed row/envelope, absent/non-positive version} ⇒ `complete=false`, never a throw, never a false-complete; map trust flags conservatively (`stamped` on explicit `===true` only, so an unstamped `db_only` fact stays quarantine-visible); source the workspace from the grant-bound adapter not a caller param (WS-8); emit a numeric degrade sentinel ONLY with `complete=false`; the positive-completeness-token upgrade needs the real transport shape (owner-gated follow-up).

## <a id="20"></a>20. A committed-vault → fact-set composition returns a 3-way outcome (derived / absent / derive_error), re-gates the read-back workspace, and NAMES the collapsing-reader coverage gap

**Date:** 2026-07-14.
**Source slice:** 13.10 piece C canonical-fact-set reader `85f6b63` (brief 059, reconcile-TRIGGER arc).

A composition that turns a committed-vault snapshot into the `CanonicalFactSet` the parity reconciler compares the DB projection against must distinguish TWO failure meanings a naive `T | undefined` collapse would fuse: a benign **ABSENCE** (no/empty vault, or the reader faulted ⇒ there is simply nothing to reconcile ⇒ skip) versus a real **DERIVE ERROR** (a structurally-broken vault — `duplicate_fact_identity` / `invalid_page_path` ⇒ a defect the caller routes to health). Return a 3-way discriminated outcome (`{kind:"derived";set}` / `{kind:"absent"}` / `{kind:"derive_error";error}`) so the trigger caller can skip-vs-health them differently; collapsing both to `undefined` silently drops the defect signal. Never throw: wrap the injected reader's CALL (not just its `await`) in a belt-and-suspenders try/catch so a SYNC throw and an ASYNC reject BOTH degrade to `absent` — even when the reader's contract is never-throw, don't rely on it across an injected boundary. Re-gate identity on READ-BACK ([12](LESSONS.md#12)): re-verify the returned snapshot's `workspaceId` against the request and degrade to `absent` on a mismatch — strictly-safer defense-in-depth over the reader's confinement (a contract-abiding reader always matches, so it fires only on a reader defect), and it doesn't foreclose the trigger caller adding a distinct ws-mismatch HEALTH signal. When a collapsing dependency creates a real coverage gap (here: an empty vault → `undefined`-with-no-revision means an empty-vault + populated-DB — all `db_only` HARD defects — is skipped-not-caught), NAME it (in-code + a Future TODO with the concrete fix — surface an empty snapshot + head revision from the reader) rather than silently owning it; the fix is owner-gated (reachable once real corpora exist). Drive the tests with the REAL deriver (a two-files-same-slug fixture yields a real `duplicate_fact_identity`), not a stubbed error.

**Rule:** a committed-vault → fact-set composition returns a 3-way outcome (`derived` / `absent` / `derive_error`) so a benign absence and a broken-vault defect route differently (never collapse to `undefined`); wrap the injected reader's CALL in a try/catch so sync-throw + async-reject both degrade to `absent` (don't rely on a never-throw contract across an injected boundary); re-gate the read-back `workspaceId` against the request (WS-8/Lesson 12 read-back), degrading to `absent` on mismatch without foreclosing a caller ws-mismatch health signal; NAME (in-code + Future TODO) any coverage gap a collapsing dependency creates rather than silently owning it; test with the REAL producer, not a stub.

## <a id="21"></a>21. A trigger-agnostic driver composes INJECTED async collaborators so it runs unchanged under either trigger model; it catches only the collaborator with a DESIGNED rejection channel and relies on the never-reject ones

**Date:** 2026-07-14.
**Source slice:** 13.10 piece D reconcile driver `8d07a60` (brief 060, reconcile-TRIGGER arc).

An end-to-end driver that composes several fail-closed collaborators into one pass (here: a canonical-fact-set builder + a db-projection builder + the reconcile pass) should take those collaborators as INJECTED async functions rather than importing the concrete ones — then the SAME driver runs UNCHANGED under either execution model (in a Temporal workflow the collaborators are `proxyActivities`; in a worker-scheduled pass they are direct async calls), so the heavy vs light "workflow-weight" choice DEFERS to the trigger piece where the driver is actually *triggered*, not baked into the driver. Injection over a module import also keeps the arc's test style uniform (a fake collaborator captures the assembled request + controls resolve/reject — no `vi.mock`). Route by cost + safety: SHORT-CIRCUIT a benign/broken upstream (an absent or derive-error canonical reference) BEFORE the expensive downstream read (don't pay the gbrain read when there's nothing to reconcile); SURFACE a defect typed (a `derive_error` rides the outcome — defer materializing a HealthItem to the piece where the health surface binds) rather than the driver minting ids/classes it shouldn't own. For never-throw discipline, catch ONLY the collaborator with a DESIGNED rejection channel (the reconcile pass deliberately rejects on a record/sink fault to make it visible) into a distinct typed outcome (`pass_faulted`), and RELY on the contractually never-reject builders (they already fail-closed to `absent`/`complete=false` — double-wrapping muddies the fault's meaning). The injected `Promise<…>` dep type does NOT encode never-reject, so the trigger/boot piece MUST bind those deps ONLY to the never-reject builders (a raw transport without the fail-closed wrapper would break the guarantee), and the trigger piece owns REDACTING the outcome's `cause`/`error` before any log sink (safety rule 7 — the pass strips its own cause, but the router owns the routing).

**Rule:** compose an end-to-end driver over INJECTED async collaborators (not module imports) so it runs unchanged under either trigger model — deferring the workflow-weight choice to the trigger piece — and keeps the test style injection-uniform (no `vi.mock`); short-circuit a benign/broken upstream before the expensive downstream read; surface a defect TYPED (defer HealthItem materialization to where the surface binds); catch ONLY the collaborator with a designed rejection channel into a distinct typed outcome and rely on the never-reject builders (don't double-wrap); the trigger/boot piece MUST bind the deps only to the never-reject builders (the `Promise<…>` type doesn't encode it) and MUST redact the outcome's `cause`/`error` before any log sink (safety rule 7).

## <a id="22"></a>22. A pure trigger-origin scheduler burst-collapses (LIFE-2), snapshots-and-DELETES the queue before the await, and redacts through a SINGLE sink chokepoint; the workflow-weight is the lighter pass for an idempotent read+record

**Date:** 2026-07-14.
**Source slice:** 13.10 piece E reconcile scheduler `89106e5` (brief 061, reconcile-TRIGGER arc).

The trigger-origin for an idempotent, re-triggerable pass (here the parity reconcile) is a PURE worker-side scheduler — NOT a Temporal workflow: because the pass is idempotent read+record (not an external side effect), Temporal's exactly-once/durability is over-engineering; the burst-collapse is a pure function (`collapseToMaxRevision`, LIFE-2), not a Temporal need; and a crash → degrade → next-trigger-recovers is fail-safe (the serving reader already degrades on a stale/absent report). Build it trigger-SOURCE-agnostic (`enqueue` + an EXTERNALLY-triggered `flush`) so the real fs-watch/schedule/post-commit source + the flush timing bind at the boot gate — the scheduler owns no `setInterval`/real time, stays pure + unit-testable. Two correctness properties are load-bearing: (1) **burst-collapse** — accumulate per-workspace, and on flush fire the driver EXACTLY ONCE at the MAX-revision trigger (a burst of N changes must not multiply into N passes); (2) **snapshot-and-DELETE the queue BEFORE the dispatch await** — so an enqueue arriving DURING the async dispatch lands in a FRESH queue (not lost, not folded into the in-flight burst) and a re-entrant flush is a no-op (no double-dispatch); pin this concurrency with a deferred-blocking-driver test, not just the sync path. Route the driver's outcome through a SINGLE redacted log sink (safety rule 7): map to a log-safe summary (the disposition kind / the `DeriveError` code only, paths dropped; `redactError` → `REDACTED_RAW` + a typed causeCode for a fault) so the RAW cause/error never reaches the log — a single chokepoint means no downstream (health materializer, logger) can leak it, while the safe summary (kind + ws + revision + code) is enough to mint the HealthItem. Isolate per-workspace (one workspace's burst never fires another's).

**Rule:** the trigger-origin for an idempotent re-triggerable pass is a PURE scheduler (enqueue + externally-triggered flush), NOT a Temporal workflow (durability is over-engineering for read+record; the pure `collapseToMaxRevision` is the LIFE-2 burst-collapse; crash→degrade→next-trigger is fail-safe); burst-collapse to ONE max-revision dispatch per flush (never one-per-change); SNAPSHOT-and-DELETE the queue BEFORE the dispatch await so a mid-flight enqueue lands in a fresh queue + a re-flush is a no-op (pin it with a deferred-driver concurrency test); route the outcome through a SINGLE redacted-summary sink so no downstream can leak the raw cause/error (the safe kind+ws+revision+code still mints the HealthItem); isolate per-workspace; leave the real source/timing/health/arming to the boot gate.

## <a id="23"></a>23. An arming SEAM is a default-OFF gate helper — strict `=== true` guard-first + a factory-spy zero-invocation OFF pin; the owner-gated input stays UNBOUND so even the armed path degrades; split the pure helper (byte-equivalent by construction) from the composition-root edit

**Date:** 2026-07-14.
**Source slice:** 13.10 piece F1 reconcile boot gate `e9c6d77` (brief 062, reconcile-TRIGGER arc — the arming seam).

Activating a dormant, safety-critical subsystem (here the parity reconcile that feeds the serve-time trust gate) is a default-OFF gate helper (`gate…(opts, deps) → wiring | undefined`, mirror `gateAutoIngest` / Lesson 2/8): the OFF guard is the FIRST statement and uses STRICT `opts.flag === true` (a truthy-coerce like `!!opts.flag` or `opts.flag !== false` is a false-arming vector — an accidental `"false"`/`1`/`{}` must NOT arm); on OFF it returns `undefined` and invokes NONE of its dep-thunks — pin this with a FACTORY-SPY ZERO-INVOCATION assertion (Lesson 11): the shipped default constructs nothing, byte-equivalent. Keep the owner-gated real INPUT UNBOUND even on the armed path (here the `GbrainReadGrant` transport → `makeDbAdapter` returns `undefined` ⇒ the projection degrades `complete=false` ⇒ the recorded report `coverageComplete=false`), so arming alone (without the owner provisioning the input) records DEGRADED, never a false-green — prove it by driving the ASSEMBLED wiring end-to-end through the REAL downstream (not a stub) and asserting the degraded record. SPLIT the safety-critical arming logic from the composition-root wiring: **F1** = the pure gate helper added to `boot.ts` with NO `bootWorker` edit, so boot is byte-equivalent BY CONSTRUCTION (an unreferenced exported helper cannot change boot behavior — a stronger guarantee than an OFF-path-returns-undefined one, and the diff is provably additive with the entry point outside every hunk); **F2** = the `bootWorker` call site + the real leaf-thunks + the reachability closure, isolated to its own focused-review commit because a byte-equivalence slip at the composition root is hard-line-adjacent. Building the seam crosses NO hard line; the ARMING (flip the flag + provision the input + the governance gates + the owner-confirmed flip) is the owner's.

**Rule:** an arming seam is a default-OFF `gate…(opts, deps) → wiring | undefined` helper — OFF guard FIRST + STRICT `=== true` (never a truthy-coerce), returning `undefined` + ZERO dep-thunk invocations on OFF (factory-spy-pinned ⇒ byte-equivalent shipped default); keep the owner-gated real input UNBOUND so even the armed path DEGRADES (never a false-green — prove it end-to-end through the real downstream); SPLIT the pure gate helper (added byte-equivalent BY CONSTRUCTION, no composition-root edit) from the `bootWorker`/composition-root wiring (its own focused-review commit) so a byte-equivalence slip on the seam is isolated; building the seam crosses no hard line — the arming (flip + provision + govern) is the owner's.

**F2 completion (`dadb167`) — the composition-root binding.** The `bootWorker` `gateReconcile(...)` call binds the REAL leaf-thunks: the LOCAL vault reader; `makeDbAdapter → undefined` (the owner-gated transport UNBOUND ⇒ the armed path degrades, never a false-green); the recorder over the ops-store repo; and a `HealthSurface`-backed sink that materializes the defect HealthItem from the SAFE `detail` code (reusing the reconciler's own `parity_defect` class, subjectRef `ws‖rev` — never a new `FailureClass`, never the raw error) through `surface.record` (OBS-2 mint/dedupe/audit-link, NOT a raw put). Byte-equivalent by DEFAULT: `reconcile` unset ⇒ the gate returns `undefined` ⇒ no machinery AND the wiring field is OMITTED from the returned struct. Two armed-path HEALTH semantics were deliberately DEFERRED to the arming review (dormant + serve-time-safe since record-before-route lands the ParityReport): a health-sink `surface.record` fault must be made to PROPAGATE (per Lesson 18 — a swallow silently drops operator-visibility on a real defect once armed), and a `pass_faulted` should mint a HealthItem (a durable-store fault is health-worthy) — both are HARD arming-gate blockers, not shipped-as-final. The source/timing binding + the transport provisioning + the flag flip are the owner's arming bundle.

## <a id="24"></a>24. A read-capability activation gate offers the tool only when it can actually serve — a fail-safe usability predicate mirroring the reader's filter EXACTLY, gated AFTER the flag/config preconditions, narrowing-only

**Date:** 2026-07-14.
**Source slice:** 13.10d `copilot.vault.read` usable-gate `90679a3` (brief 064, run-path standup).

An activation gate for a read capability should offer the tool ONLY when it can actually serve a result — not merely when a config value (a `vaultRoot`) is present — else the model is handed an always-empty tool (a wasted tool-call + confusion; here the default `<userData>/vault` is auto-created empty, so `copilot.vault.read` was offered but only ever returned `SAFE_EMPTY`). AND a fail-safe usability predicate (`vaultUsable(root)`) into the gate. Three properties make it correct + safe: (1) **gate the cheap flag/config preconditions FIRST and the fs/usability check LAST** (early-return `undefined` before touching the fs), so the shipped-default OFF path never does an fs read (byte-equivalent) and only the armed path pays the check — pin it with a "predicate-not-consulted-on-early-gate-off" test; (2) **the usability predicate must mirror the downstream READER's filter EXACTLY** — here `readdirSync(root, {recursive:true, withFileTypes:true}).some(e => e.isFile() && name.endsWith(".md"))`, matching `createCommittedVaultReader` — so `usable ⟺ the-reader-would-actually-find-a-page`; a LOOSER check is a fail-OPEN that re-offers the exact inert tool the gate exists to prevent (a directory named `notes.md/` without `isFile()`, or a shallow non-recursive scan missing a nested-only note); (3) **fail-safe throughout** — a throw in the predicate OR its factory ⇒ `false` ⇒ inert (double try/catch, never a thrown boot; an empty-string/missing root throws → caught → inert). The change is a STRICT SUBSET of the prior gate — it can only ADD a reason to be inert, never widen — so PROVE no-widening; it is byte-equivalent on a real populated vault and changes only WHEN the tool is offered, never the read scope (the WS-8 handler is untouched). Boot-time one-shot (a vault populated after boot needs a restart — matches the auto-ingest model).

**Rule:** a read-capability activation gate offers the tool only when it can actually serve — AND a fail-safe usability predicate that mirrors the downstream reader's filter EXACTLY (`usable ⟺ the-reader-would-find-a-result`; a looser check is a fail-OPEN re-offering the inert tool) into the gate, AFTER the cheap flag/config preconditions early-return (so the OFF default never touches the fs), fail-safe throw⇒inert (double try/catch); prove it's a STRICT SUBSET (narrows only, never widens), byte-equivalent on a real source, changing only WHEN the tool is offered, never the read scope.

## <a id="25"></a>25. Resolve a dormant-era best-effort swallow on a safety-critical sink to the propagate-contract BEFORE arming — the pass taxonomy catches the now-visible fault; the faulted outcome mints a HealthItem from the SAFE redacted code only; the terminal sink stays unconditionally never-throwing

**Date:** 2026-07-14.
**Source slice:** 13.10 dormant arming-prep Item 7 `8c559f9` (brief 065) — the two F2 armed-path health-semantics deferrals (session 068 #1/#2).

A sink built DORMANT sometimes ships a deliberate "best-effort swallow" as a dormant-era simplification (`createReconcileHealthSink` swallowed a `HealthSurface.record` fault because the transport was unbound ⇒ the sink was inert ⇒ a dropped item was reduced visibility, not a serve-time false-green). That swallow DEVIATES from the composition contract (Lesson 18: a routing `ReconcileHealthSink` fault must PROPAGATE) and must be resolved BEFORE arming — once real defects flow, a swallowing sink silently drops operator visibility on a REAL parity defect. The fix (drop the try/catch) is SAFE because the pass's own outcome taxonomy CATCHES the now-propagating fault: `runReconcilePass` rejects → the driver's designed rejection channel → a typed `pass_faulted` outcome (no unhandled rejection escapes the trigger `flush` — pin this end-to-end with the real driver+scheduler over the real sink). Then the composing faulted outcome is itself made health-worthy: `pass_faulted` (a durable-store / health-sink fault) mints a HealthItem, but ONLY from the SAFE redacted code the single upstream chokepoint already produced (`redactedCause.causeCode` — a typed token — NEVER `redactedCause.message`/`stack`, even redacted; safety rule 7), with a fixed `arch_gap` literal fallback when the code is falsy (guard the empty-code edge — a dangling `tag:` is a bug; RED-first pin) and a stable `ws‖rev` subjectRef, reusing an EXISTING `FailureClass` (no frozen-taxonomy expansion — Lesson 18). Crucially, the two sinks have OPPOSITE fault contracts: the ROUTING health-sink PROPAGATES (make the fault visible as `pass_faulted`); the TERMINAL log/mint sink stays UNCONDITIONALLY never-throwing (a downstream `flush` relies on it — a sync throw OR an async rejection from the recorder is swallowed HERE, a lost observability line is fail-safe because the durable ParityReport already landed). Ships DORMANT + byte-equivalent (the sinks are constructed only on the armed gate path).

**Rule:** resolve a dormant-era best-effort swallow on a safety-critical ROUTING sink to the propagate-contract before arming (a swallow drops operator visibility on a real defect once armed); rely on the pass's outcome taxonomy to CATCH the now-propagating fault (reject → driver's designed channel → a typed faulted outcome, no unhandled rejection escapes the trigger flush — pin end-to-end over the real sink); make the faulted outcome health-worthy by minting a HealthItem from the SAFE redacted code ONLY (a typed causeCode, never the redacted message/stack — safety rule 7), a fixed `arch_gap` literal fallback (guard the falsy-code edge), a stable subjectRef, and an EXISTING FailureClass (no frozen-taxonomy expansion); keep the TERMINAL mint/log sink UNCONDITIONALLY never-throwing (a downstream flush relies on it) — the routing sink propagates, the terminal sink swallows.

## <a id="26"></a>26. A coverage/completeness contract fails closed with a POSITIVE token (default-incomplete), not a NEGATIVE more-results probe — which fail-opens on an unknown/absent pagination field; widen the rejected-signal set type-robustly + cross-check every present stated total; the token + field names stay a documented candidate

**Date:** 2026-07-14.
**Source slice:** 13.10 dormant arming-prep Item 2b `4e5f18f` (brief 067) — hardening piece B's `parseGraphRead` (extends Lesson 19).

A serve-time coverage/completeness flag must fail CLOSED, and a NEGATIVE "complete unless a known more-results signal appears" probe does NOT — it fail-OPENS the moment the response omits the specific fields it probes (a genuinely-truncated read carrying no `truncated`/`cursor`, or a response paginating via a differently-named field, reads as COMPLETE = a false-green trust-gate defeat). Invert to a POSITIVE completeness token: `factsComplete=true` ONLY when an explicit `complete === true` token is present, DEFAULT-INCOMPLETE when absent, STRICT `=== true` (a truthy-non-true value degrades — the completeness axis's dangerous value is a false-complete, mirror the conservative `stamped` posture, Lesson 19). Keep the more-results rejection as an ADDITIONAL degrade lever, WIDENED type-robustly across every known pagination field name (`truncated` / `cursor` / `hasMore` / `nextPageToken` / `nextOffset` / `pageInfo.hasNextPage` — any present-and-truthy ⇒ incomplete; benign falsy values must NOT over-degrade — pin a counter-test), and cross-check any stated total: a mismatch of ANY present finite `total`/`totalCount` vs the RAW returned row count ⇒ incomplete (a self-contradictory `{total,totalCount}` pair can't false-complete — check EVERY present total, not first-finite-wins). The coverage FLOOR stays unconditional: a malformed row / err / absent-schema degrades even WITH the positive token (a token can't rescue a malformed read). CRITICAL RESIDUAL: the positive token does NOT fully close the unknown-field risk — if the real vendor sends `complete:true` loosely AND paginates via a field named OUTSIDE the candidate set, it false-completes; so the token + the exact field-NAME set are a DOCUMENTED CANDIDATE (arch_gap, Lesson 21) that MUST be confirmed against the real vendor surface at the arming binding (a field named outside the set is the dangerous, silently-missed direction).

**Rule:** a serve-time completeness/coverage flag fails closed with a POSITIVE token (default-incomplete, STRICT `===true`), never a NEGATIVE more-results probe (which fail-opens on an unknown/absent pagination field); keep more-results rejection as an additional type-robust WIDENED lever (all known field names; benign-falsy pinned not to over-degrade) + cross-check EVERY present finite stated total against the raw row count (not first-finite-wins, so a conflicting pair degrades); keep the coverage floor unconditional (a token can't rescue a malformed/err/absent-schema read); and treat the token + the exact field-NAME set as a documented candidate (arch_gap, Lesson 21) that MUST be confirmed against the real vendor surface at arming — a real field named OUTSIDE the candidate set is the dangerous, silently-missed direction.

## <a id="27"></a>27. Gate a hardcoded external-effect transport behind a default-OFF AND-composed owner seam — the effect gets HARDER to enable, enabling nothing; shipped default byte-equivalent

**Date:** 2026-07-14.
**Source slice:** 13.10 post-audit hardening Slice 1 `462a7c7` (brief 068) — the external-write `AdapterTransport` owner-gate (extends L8/11/23).

A dormancy audit found the outbound per-target external-write client (`AdapterTransport`) was built by a HARDCODED `createStubAdapterTransport()` at the composition root (`backends.ts:720`) — so swapping in a real vendor write client was a one-line SOURCE EDIT with NO owner gate (unlike propose = `copilotProposeMode` flag, or auto-ingest = `autoIngest` env). Close it by gating the SELECTION through a pure default-OFF helper (`selectAdapterTransport(gate?)`): a non-stub transport is chosen ONLY when an AND of independent OFF-locks holds — a strict `enabled === true` flag AND an owner-injected `make` factory, each TYPE-ROBUST / FAIL-CLOSED (`typeof make === "function"`, `enabled === true` strict) so a malformed JSON/env value degrades to the stub rather than arming or throwing at boot. Ship the real factory UNBOUND ⇒ the shipped default is byte-equivalent (the deterministic stub) + dormant; pin the OFF path with a factory-spy ZERO-invocation assertion (nothing real constructed on OFF). The change makes real external writes HARDER to enable (deliberate owner config, not a source edit) while enabling nothing — the §8 envelope is untouched and the arming (flip + provision) stays the owner's.

**Rule:** when a per-target external-effect client is constructed by a hardcoded call at a composition root (a one-line source edit swaps in the real vendor), gate its SELECTION through a pure default-OFF helper — a non-stub is chosen ONLY on an AND of independent OFF-locks (a strict `enabled === true` flag AND an owner-injected factory), each type-robust / fail-closed so a malformed config degrades to the stub (never arms, never throws at boot); ship the real factory UNBOUND ⇒ shipped default byte-equivalent + dormant, factory-spy zero-invocation OFF pin; the change makes the effect HARDER to enable and enables nothing (envelope + arming untouched). Extends L8/11/23 to the external-write transport.

## <a id="28"></a>28. Every owner-gated arming `=== true` check earns a truthy-not-`true` regression guard; where no runtime seam exists, a key-anchored source-assertion with a RED-on-weaken mutation proof is a valid deterministic pin

**Date:** 2026-07-14.
**Source slice:** 13.10 post-audit hardening Slice 2 `392e7db` (brief 069) — a test-only regression guard over the three propose/serving-oracle arming `=== true` sites.

A strict `=== true` owner-gated arming check can silently weaken to a truthy coercion (`if (config.X)`) under a future refactor — letting a non-boolean truthy config (`1`, the STRING `"false"`, `{}`) arm the go-live path. Pin each such site. Where a runtime seam exists (the pure `selectServingOracleFactory`), use a behavioral guard: parametrize truthy-not-`true` values (`1`/`"true"`/`"false"`/`{}`/`[]`) ⇒ the degraded default, NOT the armed factory, with a CO-LOCATED positive control (`=== true` ⇒ armed) so non-vacuity is self-contained. Where the check is INLINE with no lightweight seam (the two `boot.ts` config mappings) — a `bootWorker` behavioral test is env-gated (`SOW_API`) hence SKIPPED in the default suite ⇒ a non-pin, the pure fn can't cover a leg it truthy-checks internally (`provenanceStampingEnabled` at `:238`), and a refactor-to-a-helper is barred by a zero-production-change mandate — a SOURCE-ASSERTION is the only deterministic always-running pin: make it expression + destructured-key ANCHORED (pin the specific site, e.g. `:1171`, NOT a look-alike guard like the `:1148` construction gate), whitespace/newline-tolerant, and PROVE non-vacuity by SOURCE MUTATION (weaken the `=== true` ⇒ the anchored assertion goes RED, then restore) — the mutation proof is what makes it rigorous rather than a brittle string-match. Record in-code WHY it's a source-assertion (the env-gated non-pin + zero-production-change constraint) so a future reader doesn't "upgrade" it to a broken behavioral test. (Follow-up: extracting the inline mappings into a pure `armFromConfig` helper would make them behaviorally pinnable and retire the source-assertion.)

**Rule:** pin every owner-gated arming `=== true` check with a truthy-not-`true` regression guard (incl. the string `"false"`); use a behavioral guard + co-located positive control where a runtime seam exists; where the check is inline with no lightweight seam (a behavioral test is env-gated/skipped ⇒ a non-pin; the pure fn can't cover a leg it truthy-checks internally) and a refactor is out of scope, a key-anchored source-assertion (expression + destructured-key anchored to the specific site, whitespace-tolerant) with a RED-on-weaken SOURCE-MUTATION proof is a valid deterministic pin — not a brittle string-match; record in-code WHY so no one "upgrades" it to a broken behavioral test.

## <a id="29"></a>29. A serve-time trust-status PRODUCER composes a LOCAL derivation + an owner-gated, prod-UNBOUND injected client; its boot gate is CLIENT-PRESENCE-gated (not a flag) and byte-equivalent by construction; the waiver-closing binding wires the client unbound, routes divergence health safe-fields-only, and proves the AND-verdict still degrades by default

**Date:** 2026-07-15.
**Source slice:** 13.10 rebuild-oracle producer arc — A `210e95e` (brief 070) → B `118135c` (brief 071) → C `c251518` (brief 072); the `oracleBuildOk` serving-coverage producer (runbook Phase-4 "build-first").

The rebuild-oracle producer computes the last hardwired-false serving-coverage leg (`oracleBuildOk`) by rebuilding the gbrain index from committed Markdown and corroborating — WITHOUT crossing the real-I/O hard line, because the heavy rebuild already exists (`rebuildIndexFromMarkdown`) behind an INJECTED `IndexRebuildClient` seam that stays prod-UNBOUND (mirror the reconcile arc's `makeDbAdapter → undefined`). Three moves:

(A) The PRODUCER (`probeRebuildOracle`) composes a LOCAL committed-vault read + the injected client → a fail-closed typed status: ONLY a wholesale-replace with complete recovery corroborates `true`; absence / WS-8 read-back mismatch / divergence / fault all degrade to false. Never throws (§16); a 4-way outcome (corroborated/absent/diverged/faulted) distinguishes a benign absence from a health-worthy divergence. NO revision pin — the producer hands the FROZEN in-memory snapshot to the rebuild (no re-read window inside the producer→rebuild→client chain), so a pin buys nothing here and would spuriously stale-degrade under the current full-vault-vs-md-subset reader.

(B) The BOOT GATE (`gateRebuildOracle`) is CLIENT-PRESENCE-gated — OFF ⟺ `typeof makeRebuildClient !== "function"` — NOT a flag (unlike reconcile's `reconcile` flag with an always-present `makeDbAdapter`). So the correct dormant binding OMITS the factory: a `() => undefined` reads as ON (`typeof === "function"`) AND doesn't typecheck (`⊄ () => IndexRebuildClient`). Provisioning the real client (not arming a flag) enables observe-coverage BEFORE selection is armed. Added byte-equivalent BY CONSTRUCTION (no `bootWorker` edit; Lesson 23 split), it folds per-ws corroboration to a fail-closed boot-global boolean (all served corroborate ⇒ true; empty/any-non-corroborated ⇒ false, strict `=== true`).

(C) The BINDING (piece C) is the waiver-CLOSING slice (Lesson 16): wire the gate with the client UNBOUND (omit `makeRebuildClient`), `await compute()` once, AND-bind `resolveOracleBuild` into `createServingCoverageReader` (never a standalone admit signal), and route only `diverged` health through a SAFE-FIELDS-ONLY reprojection (frozen failureClass + synthesized message + subjectRef — the item's free-form message NEVER forwarded, rule 7; mirror `createReconcileHealthSink`). The health SINK propagates a record fault (Lesson 18); the boot CALLER contains it (§16) so a boot-time health fault never crashes boot, and the containment fault-signal is itself guarded against a throwing logger. Prove the AND-verdict STILL degrades by default (`oracleBuildOk=false` with `pinValid=true` + a clean parity report — no false green).

**Rule:** build a serve-time trust-status producer as a LOCAL-derivation + owner-gated prod-UNBOUND-injected-client composition (fail-closed status, only a full corroboration ⇒ true, never-throws, no pin on a frozen snapshot); make its boot gate CLIENT-PRESENCE-gated (OMIT the factory to stay dormant — a `() => undefined` reads as ON and won't typecheck; provisioning ≠ arming) and byte-equivalent by construction; close the waiver by binding with the client unbound + `await compute()` once + AND-binding the accessor + routing divergence health SAFE-FIELDS-ONLY (sink propagates, boot caller contains, containment signal guarded), and ASSERT the AND-verdict still degrades by default (no false green).

## <a id="30"></a>30. Every workspace-bound operational record carries an IMMUTABLE workspace-binding anchor — a re-registration that changes the binding is rejected (composition-layer, get-before-upsert, get-fault-fails-closed); the only escape hatch is a deliberate owner-gated + audited reclassification

**Date:** 2026-07-15.
**Source slice:** Phase-14 §19.1 — 14.1 `e1e4cfd` (workspace `type`→dataOwner) · 14.6 `21ee906` (project `workspaceId`) · 14.2 `6f639a6` (connector-instance `workspaceId`); dual review caught the same latent WS-2/WS-8 downgrade three times, each fixed-in-slice.

Every operational record that BINDS to a workspace (its `workspaceId`, or a field like a workspace `type` that DERIVES the isolation classification / egress-veto applicability) carries an isolation ANCHOR. An unconditional `upsert` on such a record makes that anchor silently mutable: re-registering the same id with a CHANGED binding (a workspace `type` employer_work→personal_life flipping `dataOwner` employer→user; a project or connector-instance re-created under a different `workspaceId`) moves the resource — and its accumulated identity/content/future durable writes — across the WS-8 isolation boundary. It is fail-SAFE at rest (egress stays CLOSED) but a LATENT downgrade a later spine/arming step exploits (a consumer reading the anchor gets the wrong governance posture / write target). Guard it: BEFORE the upsert, `get(id)` → not_found ⇒ create; existing-with-SAME-binding ⇒ idempotent overwrite (honors registry-union idempotency); existing-with-CHANGED-binding ⇒ a typed `*_immutable` reject (no upsert, no side effect); a get-FAULT fails closed (typed store-fault, no upsert — a transient fault must NOT bypass the guard). The guard is COMPOSITION-LAYER, not structural — a direct `repo.upsert` bypasses it, so EVERY writer of the record must preserve the binding (name this for future direct-upsert callers, e.g. the Phase-16 connector composition). The only sanctioned way to change a binding is a DELIBERATE, owner-gated + audited reclassification path that forces a re-scope/re-index — never a silent flip, never a weakened guard.

**Rule:** a workspace-bound operational record's binding anchor (`workspaceId`, or a field deriving the isolation class) is immutable-by-default — guard the creation/register path with get-before-upsert (same-binding idempotent, changed-binding ⇒ typed `*_immutable`, get-fault fails closed); the guard is composition-layer so every writer must preserve the binding (a direct repo.upsert bypasses it); the only escape hatch is an owner-gated + audited reclassification, never a silent flip and never a weakened guard.

## <a id="31"></a>31. Make a "this config cannot arm a hard line" guarantee STRUCTURAL — type the arming flags as literal `false` (+ runtime `Object.freeze`), not just a runtime test

**Date:** 2026-07-15.
**Source slice:** Phase-14 14.5 `a81040e` — `presetProfiles` (preset → provisioning profile); "no preset arms a hard line."

A config layer that SETS DEFAULTS (a preset → provisioning profile: default connectors/workflows/schedules/policy) must not be able to open an arming hard line (propose / real-write / real-transport / real-spend) regardless of which config is selected. A runtime test ("assert every profile's arming flags are false") is necessary but WEAK — a future edit can add a profile with a `true` flag and the test only catches it if it enumerates that profile. Make the guarantee STRUCTURAL: type the arming flags in the profile's `policyDefaults` as the LITERAL `false` (and the safe posture literal, e.g. `defaultVisibility: "isolated"`) so a profile that sets one `true` FAILS TO TYPECHECK — the config literally cannot arm — and `Object.freeze` the shared closed-defaults constant so the guarantee also holds at RUNTIME (TS `readonly` is compile-time only). Keep the runtime test as the behavioral backstop. Divergence between config tiers lives in the NON-arming fields (connector/workflow NAMES — listing a connector doesn't arm it; the arming surface is the flags, all literal-false).

**Rule:** when a config/defaults layer must be unable to arm a hard line regardless of selection, make it STRUCTURAL — type the arming flags as literal `false` (+ the safe posture literal) so a config that arms won't typecheck, and `Object.freeze` the closed-defaults constant for a runtime backstop; keep a behavioral test too, but the type is the load-bearing guarantee. Tier divergence lives in non-arming fields only.

## <a id="32"></a>32. The cross-workspace read is default-CLOSED — an approved + directional + scoped link is the ONLY input, and even then only the SANITIZED slice crosses (the link authorizes the sanitizer's OUTPUT, never raw bytes)

**Date:** 2026-07-15.
**Source slice:** Phase-14 14.7 `4d36b9b2`+`06ba3d3a` — the CrossWorkspaceLink store + read gate (safety rule 4, the single sanctioned WS-8 cross-read input).

A WS-8 cross-workspace read path defaults CLOSED and opens ONLY through a durable, owner-approved link — and even then never leaks raw content. The read gate (`resolveApprovedCrossWorkspaceSlice(reader)`): absent an approved directional A→B link, ZERO of B crosses (non-vacuous pin — a positive control asserts B HAS content first, then A reads zero); a link crosses ONLY the scoped slice AND only through the existing sanitizer (a raw-content-shaped B row FAILS CLOSED even under an approved link — the link authorizes the sanitizer's OUTPUT, never raw bytes) with a read-back identity re-gate on each crossing row (row.workspaceId === link.toWorkspaceId — extends Lesson 12, excludes a foreign-ws row in B's bucket); revoke closes the path immediately (and a revoked link is TERMINAL — never revived); authorization is one-way (A→B ≠ B→A); a store fault fails closed. The `(fromWorkspaceId, toWorkspaceId, SCOPE)` tuple is the immutable anchor (extends Lesson 30 — scope is in the tuple because a silent scope WIDENING on the same linkId is a bypass); scope columns are NOT NULL so a scopeless "all-of-B" link is unrepresentable; the approval uses the link's OWN status (never the frozen `Approval.subjectKind`). Ship the gate reachability-waivered when its consumers (coordination/global briefs) are dormant (Lesson 11).

**Rule:** a cross-workspace read is default-CLOSED — opened ONLY by an owner-approved, directional, SCOPED link, and even then ONLY the sanitizer's OUTPUT crosses (never raw bytes; re-gate each crossing row's workspaceId, extends L12); scope is part of the immutable anchor (a silent widening is a bypass, extends L30) + NOT NULL (no scopeless all-of-B); revoked is terminal; one-way; fault fails closed; the link carries its own status (frozen Approval untouched).

## <a id="33"></a>33. A connector→ingestion bridge routes EVERY polled record through the candidate-data gate with an idempotent content-versioned dispatch key; skip-on-poison vs HOLD-on-dispatch-failure; scoped fields from the bound instance, payload structurally excluded

**Date:** 2026-07-15.
**Source slice:** Phase-15 15.1 `a6190122` — `createConnectorIngestionBridge` (the onRecords seam, fakes, reachability-waivered).

A connector→ingestion bridge maps each polled record → `RegisterSourceInput` sourcing every scoped field (workspaceId/origin/type/sensitivity/routingHints) from the BOUND connector-instance binding — NEVER from `record.payload` content (WS-8/no-inference, REQ-F-017; payload is STRUCTURALLY excluded from every scoped field, observer, and error — pinned with a hostile `payload.workspaceId` that is ignored), routes THROUGH `registerSource` (the candidate-data gate — never around it, safety rule 2), and dispatches with the content-versioned key `src:${ws}:${contentHash}` as the Temporal `workflowId` (AlreadyStarted→dedupe, Lesson 16). Distinguish TWO failure modes: a record REJECTED by registration SKIPS + is observed (a poison record never infinitely holds the page); a DISPATCH failure HOLDS the page (returns err → the cursor stays, retried — REQ-I-005, never a silent drop; safe because dispatch is idempotent). Fakes only (type-only Temporal import, no network, no tokenRef); ship the bridge reachability-waivered when its production drive path (the poll schedule + binding) is dormant (Lesson 11).

**Rule:** a connector→ingestion bridge routes EVERY record through the candidate-data gate (`registerSource`, never around it) with an idempotent content-versioned dispatch key (=workflowId, L16); source scoped fields from the BOUND instance, structurally exclude `payload` (WS-8/no-inference); SKIP+observe a registration-rejected poison record but HOLD the page (cursor stays, REQ-I-005) on a dispatch failure; fakes keep it dormant, waivered until the drive path binds (L11).

## <a id="34"></a>34. A persisted content-dedupe store is a first-write-wins WS-8-scoped `@sow/db` repo; a `has`-fault PROCEEDs (Temporal is the real exactly-once backstop)

**Date:** 2026-07-15.
**Source slice:** Phase-15 15.4 — the persisted `SeenContentHashRepository` content-dedupe store.

A persisted content dedupe store (`SeenContentHashRepository`) is a dual-dialect `@sow/db` repo keyed by `(workspaceId, contentHash)` (WS-8-scoped — a hash seen in workspace A never dedupes it in B), first-write-wins (`ON CONFLICT DO NOTHING` preserves the original `seenAt`), survives restart, and is fail-closed BOTH directions distinguishing a store fault from a benign not-seen (extends Lesson [3](LESSONS.md#3)). It is a PRE-DISPATCH OPTIMIZATION, not the exactly-once authority: a `has`-fault must PROCEED to `registerSource`+dispatch (the Temporal `workflowId` `src:ws:hash` AlreadyStarted-dedupe is the real exactly-once backstop, Lesson [16](LESSONS.md#16)), NEVER HOLD — a persistent dedupe-store fault under HOLD would block the whole connector. Ship 15.4 with honest typed-errs; defer the caller's HOLD/PROCEED interpretation to the Phase-16 binding.

**Rule:** a persisted content-dedupe store is a dual-dialect `@sow/db` repo keyed WS-8-scoped `(workspaceId, contentHash)`, first-write-wins (`ON CONFLICT DO NOTHING` keeps the original `seenAt`), restart-durable, fail-closed both directions (store fault ≠ benign not-seen, extends L3); it is a pre-dispatch OPTIMIZATION so a `has`-fault PROCEEDs to `registerSource`+dispatch (the Temporal `src:ws:hash` workflowId AlreadyStarted-dedupe is the real exactly-once backstop, L16), never HOLD (a persistent fault would block the connector); ship with honest typed-errs, defer the caller HOLD/PROCEED interpretation to the Phase-16 binding. `pin: seen-content-hash-durability.test.ts fault_is_a_typed_err_both_directions + dedupe_is_workspace_scoped`.

## <a id="35"></a>35. A make-it-real note projection consumes the gate-validated body VERBATIM, degrades absent/empty, keeps the path body-independent, threads via a forked port

**Date:** 2026-07-15.
**Source slice:** Phase-15 — the make-it-real source note-body projection (consuming the 15.2 `SourceEnvelope.body`).

The source note-body projection writes the note body VERBATIM from the gate-validated candidate extraction (`SourceEnvelope.body`) — never raw-around-the-gate, no transform (safety rule 2). It degrades an absent-OR-empty body to a safe minimal real note (`_No extracted content yet._`, Lesson [15](LESSONS.md#15)), never a placeholder / empty / failure. The note PATH stays structurally body-independent — only `SourceNoteIdentity` keys `deriveSourceNotePath`, so a hostile `../../`+multiline body derives an IDENTICAL path AND planId (traversal-safe / WS-8, extends Lesson [5](LESSONS.md#5)). It commits through the unchanged sole KnowledgeWriter `applyPlan` (safety rule 1). The body threads as an OPTIONAL trailing param on the source-specific FORKED `SourceBuildOutputsPort.build` — NOT rippling the shared `ValidatedExtraction` into meeting/hermes (Lesson [5](LESSONS.md#5); additive-optional keeps existing callers valid, Lesson [15](LESSONS.md#15)).

**Rule:** a make-it-real note projection writes the note body VERBATIM from the gate-validated `SourceEnvelope.body` (never raw-around-the-gate, no transform, rule 2), degrades an absent-OR-empty body to a safe minimal real note (`_No extracted content yet._`, L15 — never placeholder/empty/failure), keeps the note PATH structurally body-independent (only `SourceNoteIdentity` keys `deriveSourceNotePath` — a hostile multiline/`../../` body derives an IDENTICAL path AND planId, traversal-safe/WS-8, L5), commits through the unchanged sole KnowledgeWriter `applyPlan` (rule 1), and threads the body as an OPTIONAL trailing param on the FORKED `SourceBuildOutputsPort.build` (not rippling the shared `ValidatedExtraction` into meeting/hermes, L5; additive-optional keeps existing callers valid, L15). `pin: sourceNoteBodyProjection.test.ts note_path_is_body_independent + body_written_is_exactly_the_threaded_gate_validated_value`.

## <a id="36"></a>36. A durable ingestion-disposition store; a server-side-operational-only raw envelope; a replay-safe re-enter through the gate

**Date:** 2026-07-15.
**Source slice:** Phase-15 — the durable `SourceDispositionRepository` + re-enter runner (parks the 15.2 body).

A durable ingestion-disposition store (`SourceDispositionRepository`) is a first-write-wins dual-dialect `@sow/db` repo, exactly-once by the CAS disposition key (a re-record HIT reuses the prior audit ref — inv-A/inv-B), fail-closed both directions distinguishing fault from benign absence (Lesson [3](LESSONS.md#3)). The parked raw `SourceEnvelope` (incl. the 15.2 body) persists as SERVER-SIDE-OPERATIONAL-ONLY state for re-entry — audit is summaries-only, it is never rendered (the UI-safe `ingestionInbox` projection stays the sole render path) nor logged (safety rule 7). The re-enter runner re-drives THROUGH the pipeline candidate gate (rule 2, never raw-around-gate) reusing the original `idempotencyKey` so an already-committed source replays over the `KnowledgeRevisionStore` (no duplicate, rule 3 / inv-D); the rescope's owner workspace override is registry-validated (WS-8, never a raw bind).

**Rule:** a durable ingestion-disposition store (`SourceDispositionRepository`) is a first-write-wins dual-dialect `@sow/db` repo, exactly-once by the CAS disposition key (a re-record HIT reuses the prior audit ref, inv-A/inv-B), fail-closed both directions (fault ≠ benign absence, L3); the parked raw `SourceEnvelope` (incl. the 15.2 body) is SERVER-SIDE-OPERATIONAL-ONLY re-entry state — never rendered (the UI-safe `ingestionInbox` projection stays the sole render path) nor logged, audit summaries-only (rule 7); the re-enter runner re-drives THROUGH the candidate gate (rule 2, never raw-around-gate) reusing the original `idempotencyKey` so an already-committed source replays over the `KnowledgeRevisionStore` (no duplicate, rule 3 / inv-D), and the rescope's owner workspace override is registry-validated (WS-8, never a raw bind). `pin: dispositionDurable.test.ts reenter_redrives_through_the_candidate_gate + reenter_reuses_idempotency_key_replays + rescope_registry_validates`.

## <a id="37"></a>37. An auto-ingest watcher excludes its OWN output subtree (via the producer's shared constant) to break the write→re-ingest feedback loop

**Date:** 2026-07-15.
**Source slice:** Phase-15 15.6 — the auto-ingest vault-watcher output-subtree exclusion.

An auto-ingest vault watcher (default-OFF `SOW_INGEST_WATCH`) must exclude its OWN ingestion-note output subtree so a note it just wrote does not re-fire it — the source → KnowledgeWriter-writes-`note.md` → re-fire → re-ingest infinite loop. The exclusion is root-anchored + segment-safe (`rel === "sources" || rel.startsWith("sources/")` — `sources/x` is excluded, but `mynotes/sources/x` and `sourcesX` are NOT), applied guard-BEFORE-I/O in BOTH entry paths (the direct `computeOutcome` after `isMarkdown` / before realpath, AND the `onEvent` pre-filter so no debounce timer even arms), and surfaces a typed `ignored/output_subtree` (never a silent drop).

It references the SAME `SOURCE_NOTE_SUBTREE` constant the note-path producer (`deriveSourceNotePath`) uses — one literal, so a future output-path change can't silently drift and re-open the loop. It stays narrow: a user `.md` outside `sources/` still ingests normally.

**Rule:** an auto-ingest vault watcher (default-OFF `SOW_INGEST_WATCH`) excludes its OWN ingestion-note output subtree so a written note doesn't re-fire it (source → KW-writes-note.md → re-fire → re-ingest infinite loop); the exclusion is root-anchored + segment-safe (`rel === "sources" || rel.startsWith("sources/")` — `sources/x` excluded, `mynotes/sources/x`+`sourcesX` NOT), applied guard-BEFORE-I/O in BOTH entry paths (direct `computeOutcome` after `isMarkdown`/before realpath AND the `onEvent` pre-filter so no debounce timer arms), surfacing a typed `ignored/output_subtree` (never a silent drop); it references the SAME `SOURCE_NOTE_SUBTREE` constant the producer (`deriveSourceNotePath`) uses (one literal — an output-path change can't silently drift and re-open the loop); stays narrow (a user `.md` outside `sources/` still ingests). `pin: vaultWatcher-live.test.ts output_subtree_onEvent_arms_no_timer_zero_dispatch + exclusion_matches_real_derived_output_path`.

## <a id="38"></a>38. A meeting-dispatch bridge keys idempotency on the meeting's recordId (not contentHash) and deliberately skips the source content-dedupe — meeting vs source use different dedupe axes

**Date:** 2026-07-16.
**Source slice:** Phase-15 15.9 — the production meeting-closeout dispatcher + completed-meeting bridge (G1 flagship).

The completed-meeting bridge discriminates a connector record on the BOUND connector instance's `binding.kind` (a policy-bound 14.2 field — never the record or `record.payload`, WS-8 / no-inference): a `kind:"meeting"` binding routes to `dispatchMeetingCloseout`; a default `kind:"source"` binding takes the 15.1 `registerSource` path (back-compat). BOTH paths flow THROUGH `registerSource`'s candidate gate (rule 2, never around it), but the MEETING path deliberately SKIPS `registerSource`'s contentHash dedupe. A meeting's idempotency axis is its stable identity, NOT its transcript content: `meeting:${workspaceId}:${recordId}` becomes the Temporal `workflowId` → `REJECT_DUPLICATE`. contentHash is the WRONG axis for meetings — two DISTINCT meetings that share an empty/near-empty transcript collide on contentHash, so a contentHash-dedupe would silently drop the second meeting (the exact defect the security review caught). recordId keying also gives ONE closeout per meeting, STABLE across an edited-transcript re-poll (a source is content-versioned; a meeting is identity-versioned).

**Rule:** a meeting-dispatch bridge keys idempotency on the meeting's stable recordId (`meeting:${ws}:${recordId}` = the Temporal `workflowId`, `REJECT_DUPLICATE`) and DELIBERATELY skips `registerSource`'s contentHash dedupe (while still routing THROUGH the candidate gate, rule 2) — contentHash is the wrong axis for meetings (distinct empty-transcript meetings collide ⇒ silent-drop of the 2nd), and recordId gives one closeout per meeting stable across an edited-transcript re-poll (source = content-versioned; meeting = identity-versioned); discrimination is on the BOUND instance's `binding.kind`, never the record/payload (WS-8/no-inference, extends L33); the meeting-path dedupe-skip is intentional — the Phase-16 wiring owner must preserve it. `pin: connectorIngestionBridge.test.ts meeting_path_ignores_contenthash_dedupe + meeting_dispatch_is_idempotent_by_workflowid + meeting_workspace_from_bound_instance_not_content`.

## <a id="39"></a>39. A conditionally-required dependency is validated at composition (fail-fast construction), never deferred to a runtime HOLD

**Date:** 2026-07-16.
**Source slice:** Phase-15 15.9 — the completed-meeting bridge's `dispatchMeeting` dep requirement.

When a bridge branch (`kind:"meeting"`) needs a dependency (`dispatchMeeting`) that other branches (`kind:"source"`) don't, validate that conditional requirement at CONSTRUCTION — fail-fast throw when a meeting-capable binding is wired without its `dispatchMeeting` dep — NOT at runtime. The original draft let a missing dep fall through to a per-record infinite HOLD with no health item: every meeting record HOLDs forever with zero operational signal (a silent black hole). Moving the check to composition-time makes the misconfiguration a loud build/boot failure, surfaced ONCE, before any record flows.

**Rule:** a conditionally-required dependency (a branch needs a dep other branches don't) is validated at COMPOSITION (fail-fast construction throw), never deferred to a runtime HOLD — a missing dep on the live path is a per-record infinite HOLD with no health item (a silent black hole); fail at wiring so the misconfiguration is loud + surfaced once before any record flows. `pin: connectorIngestionBridge.test.ts meeting_binding_without_meeting_dispatch_fails_closed (asserts construction throws)`.

## <a id="40"></a>40. A trust-boundary output-contract hardening — a swappable injected backend's return is re-normalized (de-aliased + bounded) at the port, never trusted to have done it

**Date:** 2026-07-16.
**Source slice:** 17.1 `8ebb55ff` (brief 112) + 17.2 `2737e0df` (brief 113).

The `KeychainSecretsAdapter` (§19.4 `SecretsPort`) and its no-shell `security`-CLI backend were already built + fail-closed (task 11.4); Phase-17 finalized each with the SAME defense-in-depth posture — a value crossing UP from a swappable injected collaborator is re-normalized to the port's OWN output contract, never trusted to already satisfy it. **17.1:** the adapter de-aliases a backend hit into an INDEPENDENT `Uint8Array` (`ok(new Uint8Array(read.value))`) — a backend returning a pooled-`Buffer` view/slice would otherwise leak that aliasing through the `SecretsPort`, and on the secrets surface an aliasing leak IS key exposure (rule 7); the zero-length reject stays BEFORE the copy (never copy an anomalous blob, never serve an empty HMAC key). **17.2:** the backend rejects a code-0 stdout above `MAX_KEY_LEN` (4096) to `backend_error` rather than serving it — the wrapper does not trust the injected exec to have bounded its output (the real `execFile`'s 64KB `maxBuffer` is the coarse bound; this is the tight semantic one), checked on the RAW length BEFORE the de-alias. The real backend's own de-alias/bound stays the PRIMARY defense; the port re-normalizes as the backstop for a future/rogue backend swap.

**Rule:** at a trust boundary a port re-normalizes a swappable collaborator's return to its OWN output contract — de-alias returned key bytes into a FRESH `Uint8Array` (a `Buffer` view/slice shares the pool; an aliasing leak on the secrets surface = key exposure, rule 7, L10) and reject an over-length code-0 stdout to a typed `backend_error` — checking length/validity BEFORE the copy and keeping the real backend's own de-alias/bound as the primary defense; never trust the injected backend to have satisfied the contract. `pin: keychain-adapter.test.ts returned_bytes_are_de_aliased_from_the_backend_buffer + keychain-backend.test.ts a_code_0_stdout_above_the_max_key_length_is_rejected_not_served + de-aliases_a_pooled_Buffer_view_the_real_execFile_yields`.

## <a id="41"></a>41. The shared credential seam is a degraded-by-default lock-routing accessor — a `locked` Keychain mints a HealthItem AND still returns the fail-closed Err, never a plaintext fallback

**Date:** 2026-07-16.
**Source slice:** 17.3 `09e0630e` (brief 114).

`createLockRoutingSecretsAccessor` wraps the `getSecret` facade as the single credential seam Phase-18/19 consume. Degraded-by-default: the gate ABSENT ⇒ fail-closed `missing` (no creds, byte-equivalent, L11 — provisioning ≠ arming). Present ⇒ delegate; a `locked` Keychain routes to `KeychainLockController.onKeychainLocked` (minting a keychain-locked `HealthItem` for operator visibility) AND STILL returns the fail-closed Err — NEVER a plaintext fallback (§16/rule 7). Route ONLY on `locked`: a `missing`-collapsed config error (`invalid_ref`) must not be mislabeled a lock (a lock is retryable-on-unlock, a bad ref a non-retryable config surface); `missing`/`denied` fail closed WITHOUT minting. Throw-safe on BOTH injected seams (L21/L29): a facade throw degrades to `missing`, a router reject still returns the fail-closed `locked` Err (the real Keychain adapter can throw — TCC/spawn/native). The routing INPUT carries no ref + no secret value (rule 7). Boot BINDING is deferred to the consuming phase — the real ModelProvider/Stamper deps don't exist yet, so binding now would be dormant-on-dormant (L11); the accessor is built+tested, the arming slice binds it.

**Rule:** the shared credential seam is a degraded-by-default lock-routing accessor — gate absent ⇒ fail-closed `missing` (byte-equivalent, L11); a `locked` Keychain routes to `onKeychainLocked` (mints a keychain-locked `HealthItem`) AND STILL returns the fail-closed Err, never a plaintext fallback (§16/rule 7); routes ONLY on `locked` (a `missing`-collapsed `invalid_ref` config error is never mislabeled a retryable lock; `missing`/`denied` never mint); throw-safe on BOTH injected seams (facade throw ⇒ `missing`, router reject ⇒ the `locked` Err, never an unhandled reject, L21/L29); the routing input is value-free; defer the boot binding to the consuming phase (no dormant-on-dormant). `pin: keychain-boot.test.ts locked_routes_to_onKeychainLocked_and_fails_closed_no_plaintext + facade_that_throws_fails_closed_to_missing_never_rejects + router_reject_on_locked_still_returns_the_fail_closed_locked_err_never_rejects + routing_input_carries_no_ref_or_secret_value_redaction`.

## <a id="42"></a>42. A single-sourced secret-ref convention over `keychain://<service>/<account>` — traversal-safe by construction via the resolver's own parser, a closed runtime-validated kind set

**Date:** 2026-07-16.
**Source slice:** 17.4 `732be4dc` (brief 115).

`secretRefConvention.ts` adds a typed KIND namespace (`SecretRefSpec`) + build/parse over the existing `keychain://<service>/<account>` format so every downstream seam resolves ONE `getSecret` facade with a stable, single-sourced convention: provider keys `providers/{claude,openai,openrouter}` (the canonical `ProviderId` cloud subset — Phase-18 composes the ref directly from `providerId`, no translation layer), `embeddings/voyage` (a DISTINCT kind — not a ModelProvider), `sow/kw-signing` (HMAC), per-vendor `connector-read/<vendor>` + `connector-write/<vendor>`, `telegram-bot/token`. Traversal-safe BY CONSTRUCTION (L5): `buildSecretRef` composes then round-trip-validates through the adapter's EXPORTED `parseKeychainRef` — the SAME single-sourced charset/segment guard the resolver uses (no duplicated literal that could drift, L37) — so a hostile vendor/account with a separator/traversal/whitespace fails closed to `null`, never smuggling a second segment or a different kind. The build side ALSO runtime-validates the closed provider/embeddings set + a default fail-closed backstop (symmetric with parse — the compile-time type is not a runtime backstop on the credential path). Unknown/malformed ⇒ `null` (0 backend calls, L9); value-free (rule 7).

**Rule:** a single-sourced secret-ref convention over `keychain://<service>/<account>` is traversal-safe BY CONSTRUCTION — `buildSecretRef` round-trips through the resolver's OWN exported `parseKeychainRef` (one charset/segment guard, no drift-prone duplicate literal, L5/L37; a hostile component fails closed to `null`, never a smuggled second segment/kind) — with a closed provider/embeddings kind set runtime-validated on BOTH build and parse (the compile-time type is not a runtime backstop on the credential path), 0 backend calls on a malformed ref (L9), value-free (rule 7); every downstream seam composes the ref from its typed kind, no translation layer. `pin: secretRefConvention.test.ts finalized_provider_and_embeddings_sets_are_exact + a_component_with_an_injected_separator_or_traversal_fails_closed + unknown_kind_or_malformed_ref_fails_closed_zero_backend_calls + build_fails_closed_on_an_offset_member_or_a_rogue_kind_runtime_backstop`.

## <a id="43"></a>43. Bind a real effect-producing run leg behind an AND-composed default-OFF composition-root gate — map every provider fault to a typed `GateDeny`, never throw, resolve the key only through the degraded accessor

**Date:** 2026-07-17.
**Source slice:** 18.1 `99cae521` (brief 116).

18.1 replaces the broker's fixed-candidate run leg (`createStubProviderRunner`) with a real `ProviderRunner` selected through an AND-composed default-OFF gate in `assembleBackends`: `providerTransport` unset/OFF ⇒ the EXACT stub instance (byte-identical shipped default, factory-spy zero-invocation); armed (`enabled === true` && `make`) ⇒ the real runner. `createRealProviderRunner` selects the `ModelProviderPort` adapter for the route's `providerId` (worker-local registry over the five `create*ModelProvider` factories), builds a `ProviderRequest`, calls `.complete()`, and maps `ProviderOutput → AgentResult` / **`ProviderError → GateDeny` — never throwing** (§16): a provider fault is a typed gate denial, not an exception that could escape the run leg; a rogue-collaborator throw is caught into a typed `provider_unavailable` too (totality). The provider key resolves ONLY through the injected 17.3 accessor — a missing/locked key degrades via the never-reject `KeychainLockController` (the job is HELD retryable, never terminal, never a plaintext fallback). Local routes are pinned to the loopback allowlist (rule 5); the egress veto still PRECEDES the run leg (§7 order unchanged). The `AgentRuntimePort` agentic leg is a fail-closed typed `GateDeny(provider_unavailable)` dormant seam. Extends the L27/L11/L23 effect-transport-gate family to a leg whose effect is REAL model spend — building the seam crosses no hard line; the flip + key provisioning is the owner's crossing.

**Rule:** bind a real effect-producing run leg behind an AND-composed default-OFF composition-root gate (strict `enabled === true` && an owner-injected factory ⇒ the real runner; else the EXACT stub — byte-identical + factory-spy zero-invocation, L27/L11/L23); map EVERY provider fault to a typed `GateDeny` and catch a rogue-collaborator throw into a typed `provider_unavailable` — the run leg NEVER throws (§16); resolve the credential ONLY through the degraded lock-routing accessor (missing/locked ⇒ retryable HOLD, never a plaintext fallback); keep the egress veto BEFORE the run leg and local routes on the loopback allowlist (rule 5). `pin: provider-runner.test.ts real_runner_maps_provider_error_to_gate_err_never_throws + runner_is_total_on_a_rogue_collaborator_throw + gate_off_never_invokes_real_factory + assembleBackends_default_run_leg_is_stub_byte_identical + egress_veto_precedes_run_leg`.

## <a id="44"></a>44. Deny-only policing gates are ACTIVE by default (no dormancy knob) — only effect-producing seams arm behind default-OFF; the durable ledger plugs in backward

**Date:** 2026-07-17.
**Source slice:** 18.2 `d4c2a9a0` (brief 117).

18.2 replaces the broker's three stub gates with the real HEALTH/BUDGET/SCHEMA gates from `@sow/providers`. The load-bearing distinction: **a deny-only policing gate produces no spend, no egress, no external write, so it is ACTIVE by default — it gets NO dormancy knob.** Only effect-PRODUCING seams (the run leg, a real transport) arm behind a default-OFF gate; a gate that only REJECTS candidates is a pure safety tightening and ships ON (a dormancy knob on it would be a switch to turn safety OFF). The real SCHEMA gate replaces a permissive stub — a schema-invalid candidate is rejected before emit (rule 2/REQ-S-006), and a `ProposedAction` candidate on an untrusted read-only job is a `tool_policy_violation` (only a KMP passes). The NEW pluggable `BudgetLedgerPort` seam wraps `createBudgetGate` to record each run's spend; single-run in-boot now, the durable cross-run ledger (§19.6) plugs in BACKWARD (no forward dependency); `.record` is try/caught (§16 never-throw — a future durable ledger can't crash the broker post). Accept-path resolution: the `meeting.close` broker candidate is a KMP STAND-IN under the registered `sow:knowledge-mutation-plan` schema (a closeout note IS a KMP; the extraction output-schema is the separate 18.3/18.4 concern) — `mapCandidate` still ignores it, so downstream is untouched; REQ-F-017's `NoInferenceView` binds at 18.3/18.4 with real extraction fields (a no-op here would be a misleading gate).

**Rule:** a deny-only POLICING gate (health/budget/schema — no spend/egress/write) is ACTIVE by default and gets NO dormancy knob — only effect-PRODUCING seams arm behind default-OFF (a knob on a reject-only gate is a switch to disable safety); model the durable upgrade as a pluggable port that plugs in BACKWARD (the in-boot single-run `BudgetLedgerPort` → the §19.6 cross-run ledger, no forward dep; `.record` try/caught, never crashes the post); an accept-path may ride a documented KMP STAND-IN schema until the real output-schema lands, but bind REQ-F-017's no-inference only where real extraction fields exist (a no-op gate over no fields is a misleading green). `pin: broker-gates.test.ts schema_gate_rejects_invalid_candidate_no_side_effect + pa_candidate_on_readonly_untrusted_is_tool_policy_violation + budget_ledger_seam_single_run_accounts + budget_ledger_wired_into_broker + assembleBackends_default_meeting_close_stays_green`.

## <a id="45"></a>45. A confidence-scored routing producer BINDS from the registry entry and PARKS to clarification otherwise — never invents a binding (REQ-F-017); the threshold is single-sourced at the boundary

**Date:** 2026-07-17.
**Source slice:** 18.6+18.5 `3af7e58d` (brief 118).

18.6/18.5 replace two placeholder routing legs (a `confidence:1` boot-ws bind; a fixed `confidence:0.95`) with real registry-backed, fail-to-park producers. `createContentProjectClassify`: a source `routingHints.projectRef` → `ResolveRegistryPort.resolve` → a confident (≥ threshold) match binds `{workspaceId, projectId}` FROM THE REGISTRY ENTRY; no-match / ambiguous / below-threshold → `recordPark` (the Ingestion Inbox clarification surface), NEVER a guessed projectId/workspace. `createCorrelationSignalProducer` over an injected `CorrelationScorerPort`: a confident correlation binds, below-threshold parks. REQ-F-017 (routing surface): below-threshold/no-match parks to clarification, never invents. WS-8: the bound workspace+project come ONLY from the registry entry — a source-supplied `routingHints.projectRef`/workspace is never authoritative (a smuggled caller ws/projectId is ignored, L30/L32). The threshold (0.7) is SINGLE-SOURCED across the classify + the route/correlate activity, pinned to agree at the boundary (`confidence === threshold` ⇒ bind — `>=` not `>`; no split-brain). Byte-equivalent dormant: the SHIPPED boot default (`createBootWorkspaceContentResolver` + `createBootCorrelationScorer`) binds the boot workspace, never parks — byte-equivalent to the old `confidence:1/0.95`, so the flagship meeting→note spine is unchanged; `createRegistryContentResolver` is the DORMANT real resolver reachable-when-armed (mirror of 18.1), the real registry/model boot-wiring is G11's LIVE half (L11).

**Rule:** a confidence-scored routing producer BINDS `{workspaceId, projectId}` ONLY from the resolved REGISTRY ENTRY on a confident (≥ threshold) match and PARKS to the clarification surface on no-match/ambiguous/below-threshold — never a guessed/invented binding (REQ-F-017 routing surface); a source-supplied projectRef/workspace is NEVER authoritative (WS-8, the smuggled caller ws is ignored, L30/L32); single-source the threshold across the classify + the activity so they agree at the boundary (`=== threshold` ⇒ bind, no `>`/`>=` split-brain); a resolver/scorer fault propagates typed, a throwing resolver folds to a totality `route_failed`; ship byte-equivalent (the boot default binds, never parks) with the real registry resolver dormant + reachability-waivered (L11). `pin: content-project-resolver.test.ts classify_no_registry_match_parks_never_guesses + classify_workspace_from_registry_not_caller + classify_binds_at_exact_threshold_boundary + correlation_below_threshold_parks_clarification + boot_defaults_bind_byte_equivalent`.

## <a id="46"></a>46. The extraction leg gates `mapCandidate` on the ACCEPTED broker outcome (never a blind echo of params) behind a real structural schema gate — and the candidate-data gate is a DIVISION OF LABOR (structural gate vs `validateNoInference`)

**Date:** 2026-07-17.
**Source slice:** 18.3 `0bbb281c` (brief 119).

18.3 replaces two placeholders the meeting path carried. `mapCandidate → mapAcceptedMeetingExtraction`: narrows `outcome.ok`, reads the ACCEPTED candidate (confirming the run-leg output passed the broker schema gate), returns the extraction; a NON-accepted outcome → an EMPTY extraction (the downstream gate rejects → no commit), NEVER a blind echo of `params` — today's leg `mapCandidate:(_outcome)=>params.meetingExtraction` IGNORED the broker verdict, so a real model's output would never have been checked. The pass-everything schema-gate stub (`()=>ok(undefined)`) → `createMeetingExtractionSchemaGate()`: a REAL pure structural gate over `extraction.fields` (non-empty; each a well-formed `ExtractionField {value: primitive|TBD, evidenceRef?}`); malformed → `schema_rejected`; never coerces, never throws. The extraction's candidate-data gate (rule 2) = this structural gate + the live `validateNoInference` (REQ-F-017) at the `ValidateExtractionPort` — a DIVISION OF LABOR: the `MeetingSchemaGate` is the STRUCTURAL half (well-formedness), `validateNoInference` the SEMANTIC-EVIDENCE half (a concrete owner/dueDate with NO `evidenceRef` → rejected as inferred; `TBD` → ok; never invented; empty `{}` fields ride the schema gate's non-empty check, not the vacuously-passing no-inference). Fail-closed tightening: an empty extraction passed the OLD stub and was committable; gate-on-outcome + the real gate ATOMICALLY close that. The faithful evidence-bearing reconstruction from a REAL broker outcome is DEFERRED to the crossing (the frozen KMP stand-in discards `evidenceRef`, which REQ-F-017 keys on — GATE-1; do NOT arm the real transport until Option A lands).

**Rule:** an extraction leg gates `mapCandidate` on the ACCEPTED broker outcome — read the accepted candidate on `outcome.ok`, an empty extraction on non-accepted, NEVER a blind echo of `params` (an echo means the broker's schema verdict never gated the model output) — behind a REAL pure structural schema gate (non-empty, well-formed `ExtractionField`s; never coerce/throw); the candidate-data gate is a DIVISION OF LABOR — the structural gate proves well-formedness, `validateNoInference` proves evidence (a concrete owner/date with no `evidenceRef` is inferred ⇒ rejected, `TBD` ⇒ ok), and empty fields must be caught by the STRUCTURAL half (no-inference passes `{}` vacuously); preserve the validator MECHANISM in the dormant build but DEFER the faithful evidence-bearing reconstruction to the crossing (the KMP stand-in discards `evidenceRef` — arming without Option A silently bypasses REQ-F-017). `pin: meeting-extraction.test.ts map_gates_on_accepted_outcome_reads_candidate + validate_rejects_inferred_owner + validate_tbd_owner_and_date_pass + validate_empty_evidenceRef_rejected_by_no_inference + validate_empty_fields_rejected_by_schema_gate`.

## <a id="47"></a>47. Route an untrusted source THROUGH the broker so job-admission (ING-7) runs on it — the bypass was the gap; the ws comes from the routing ctx not content; employer raw content routes loopback-only

**Date:** 2026-07-17.
**Source slice:** 18.4 `2068039f` (brief 120).

18.4 replaces the source-agent bypass (`run:()=>ok(sourceBinding.extraction)`, which never hit admission/gates) with `createSourceAgentBrokerRouting`: it builds a source `AgentJob` from the dropped body (capability `source.process` [worker-internal arch_gap]; `toolPolicy read_only`; `trustLevel untrusted`; `carriesRawContent`; `outputSchemaId` = KMP stand-in; `workspaceId = ctx.workspaceId`), runs `admitJob` (ING-7) BEFORE `broker.runJob`, then gate-on-outcome `mapCandidate` (reuses 18.3's workflow-agnostic `mapAcceptedMeetingExtraction`). The safety win: **routing the source through the broker SUBJECTS it to admission** — an untrusted source job with a mutating toolPolicy is now rejected (`UNTRUSTED_CONTENT_MUTATING_TOOL`); today's bypass meant admission NEVER ran for the source path (the ING-7 gate existed but the source went around it). WS-8: the source job `workspaceId = ctx.workspaceId` (the routing-bound admission ws), NEVER `ctx.source.workspaceId` (content); an unbound ctx ws fails closed BEFORE any admission/dispatch (no job built). Rule-5: source (employer raw content, ack-OFF) routes `source.process` → loopback-local ONLY; a non-local route fails the egress veto closed. Safe-build: local zero-egress route, dormant 18.1 stub run leg, valid-KMP stub candidate — the source path lives behind auto-ingest (default-OFF), so the shipped default is byte-equivalent; 18.4 changes HOW the source is processed WHEN triggered (gated, not bypass). (Also fixed 2 latent config bugs surfaced under Temporal: a `DEFAULT_ROUTE` `egressClass` invalid-enum cast; `buildMatrix`/`buildEgress` scoped to `ctx.workspaceId`.)

**Rule:** route untrusted imported content THROUGH the broker so job-admission (ING-7, rule 6) actually runs on it — a bypass that skipped admission is the gap, not a convenience; the built source `AgentJob` is `read_only` + `untrusted` + `carriesRawContent`, and an untrusted+mutating-toolPolicy job is rejected at admission (`UNTRUSTED_CONTENT_MUTATING_TOOL`); the job `workspaceId` comes from the routing-bound `ctx.workspaceId` NEVER the content's `ctx.source.workspaceId` (WS-8, an unbound ws fails closed pre-admission, L33), and employer raw content routes loopback-local ONLY (rule 5, a non-local route fails the veto); ship byte-equivalent (the path is behind default-OFF auto-ingest — it changes HOW, not WHETHER, the source runs). `pin: source-extraction.test.ts source_routed_through_broker_not_bypass + ing7_source_mutating_tool_rejected_at_admission + source_job_workspaceId_from_ctx_not_content + source_job_unbound_workspace_fails_closed + source_mapCandidate_gates_on_outcome`.

## <a id="48"></a>48. A ProposedAction producer is PURE + takes no gateway dep (so it STRUCTURALLY cannot dispatch — lands PENDING) — and `payloadHash` must digest the PAYLOAD, not the identity key (rule-3 TOCTOU)

**Date:** 2026-07-17.
**Source slice:** 18.7 `8d9c6507` (brief 121).

18.7 populates the KMP's `externalActionProposals` with real ProposedActions that land PENDING a §9 Approval — NO dispatch (Phase 21/22 arms the Tool Gateway). SOURCE leg: `produceProposedActions` is binding-driven, DORMANT by default (optional `externalActionBinding?` on `SourceIngestionParams`; unset in boot ⇒ `[]`). An explicit VALIDATED action-intent field (named by the binding) implies ONE ProposedAction; the `targetSystem` + external-write keys come from the BINDING + a traversal-safe identity (the §8 `buildCanonicalObjectKey`/`buildIdempotencyKey`), NEVER from content (content flows only to `payload.intent`); a non-concrete intent (empty/whitespace/TBD/non-string) ⇒ `[]` (fail-closed, REQ-F-017). MEETING leg: `externalActionProposals` now MIRRORS the existing projection-derived actions (`actions.map(a=>a.action)` — no new producer; the flagship "propose tasks" now reaches the candidate gate). STRUCTURAL no-dispatch: the producer is PURE + takes NO gateway/dispatch dep, so it CANNOT dispatch — the action lands PENDING via the existing propose path (`approvalPolicy != "auto_private"` ⇒ requires approval, fail-closed); the candidate gate validates each action (`ProposedActionSchema` + `ruleExternalWriteKeys`). Review fixed a real rule-3 integrity gap: `payloadHash` digested the IDENTITY key (`canonicalObjectKey`), NOT the payload — a payload swap between propose and dispatch would have been undetectable by the §8 TOCTOU guard; now `payloadHash = payload:<targetSystem>:sha256(payload)` (payload-sensitive; the Tool-Gateway integrity binds at arming).

**Rule:** a ProposedAction producer is a PURE function that takes NO gateway/dispatch dep — it STRUCTURALLY cannot dispatch, so its output lands PENDING a §9 Approval by construction (not by a runtime flag that could flip); the `targetSystem` + external-write keys derive from the BINDING + a traversal-safe identity, never from content (content flows only into `payload.intent`; a smuggled `targetSystem` never overrides the binding, WS-8/no-inference), and a non-concrete intent ⇒ `[]` (fail-closed, REQ-F-017); the rule-3 `payloadHash` must digest the PAYLOAD (`sha256(payload)`) NOT the identity/canonical-object key — a hash over the identity leaves a propose→dispatch payload swap invisible to the §8 TOCTOU guard. `pin: proposed-action-producer.test.ts proposedaction_lands_pending_no_dispatch + payloadhash_digests_the_payload + targetsystem_and_keys_from_binding_not_content + empty_or_tbd_intent_emits_empty + safe_build_no_external_write`.

## <a id="49"></a>49. Source-note frontmatter carries the validated extraction over a FIXED `[owner,dueDate]` convention (can't inject arbitrary frontmatter) — reusing the meeting projection's own helpers, through the sole KnowledgeWriter

**Date:** 2026-07-17.
**Source slice:** 18.8 `f1318db2` (brief 122).

Trace-first: the meeting note (frontmatter/body/workspace) + the source note body (15.3) + workspace were ALREADY real; the one real gap was the SOURCE note frontmatter (identity-only `{source, contentHash}`, ignoring the validated extraction). The source `buildOutputs` note frontmatter now carries the validated extraction's owner/dueDate over a FIXED `["owner","dueDate"]` convention (`frontmatterValue`'s TBD-when-absent sentinel + `neutralizeFrontmatterValue` marker-safety; identity fields `source`/`contentHash` retained) — REUSING the SAME helpers the meeting projection uses (parity, not a re-impl). The note title stays identity-based (`Ingested: <sourceId>`, a stable anchor). Three INDEPENDENT structural WS-8/no-inference defenses, each non-vacuously pinned: the FIXED convention can't inject arbitrary frontmatter (a smuggled `workspaceId`/path field never lands); the note path is identity-derived + traversal-safe; the workspaceId is routing-bound (not content). Rule 1 (one-writer): the frontmatter reaches the note ONLY via the validated KMP → `createCommitActivity → applyPlan` (the sole writer, which re-runs the composed gate); NO direct fs write added. REQ-F-017: an absent owner/dueDate degrades to `TBD` (the `frontmatterValue` sentinel), never an invented value (`validated.fields ?? {}` is a safe fail-closed degrade, the writer gate the backstop). Also pins a previously-UNPINNED safety leg: the source marker-neutralization (deleting it kept all tests green) is now mutation-verified load-bearing.

**Rule:** a note projection that adds validated-extraction frontmatter uses a FIXED key convention (`["owner","dueDate"]`) — a fixed allow-list of keys structurally can't inject arbitrary frontmatter (a smuggled `workspaceId`/path field never lands), degrades an absent value to a `TBD` sentinel (never invented, REQ-F-017), and neutralizes region-markers in values — REUSING the sibling projection's own helpers (parity, not a re-impl) and reaching the note ONLY through the validated KMP → the sole KnowledgeWriter `applyPlan` (rule 1, no direct fs write); pin the marker-neutralization as load-bearing with a source-mutation proof (deleting it kept every test green — an unpinned safety leg). `pin: realSourceCommit.test.ts commit_carries_real_extraction_frontmatter + source_frontmatter_absent_owner_date_is_TBD + frontmatter_ws8_no_smuggled_field + source_frontmatter_marker_neutralized + content_flows_through_validated_kmp_only`.

## <a id="50"></a>50. Pin a safety invariant at the REAL assembled composition root (`assembleBackends`/`boot`), not only at the injected-fake unit level — a worker-side wiring regression escapes fake coverage

**Date:** 2026-07-17.
**Source slice:** 18.9 `55f0a33a` (brief 123) + 18.10 `29555821` (brief 124).

Both are verify+pin slices (NO production change) that drive the REAL assembled seam — coverage that neither the providers/domain unit tests (injected fakes) nor the dormancy tests exercise, catching a worker-side wiring regression. **18.9** drives the worker's REAL assembled broker (`assembleBackends` → the default `vetoJobEgress` + `resolveJobRoute`, not injected fakes) with RAW employer content through the egress veto (rule 5): cloud + employer + raw + ack-OFF ⇒ DENY `EMPLOYER_RAW_EGRESS_UNACKNOWLEDGED` at `egress_veto` with NO cloud fallback; **OpenRouter classified CLOUD (its own processor) ⇒ DENIED, never laundered to a local ALLOW** (rule 5 — OpenRouter is not an OpenAI alias); loopback-local ⇒ ALLOW + runs zero-egress; the veto DENIES even when the processor is allowlisted (veto precedes allowlist); a tunneled-`local` remote ⇒ EGRESS + DENIED (no laundering); non-vacuity L28-proven. **18.10** pins the auto-ingest boot gate: `gateAutoIngest` already guards strict `opts.autoIngest !== true` and is the SOLE arming chokepoint (the desktop worker-host passes `config.autoIngest` RAW — verified), so the strict guard at boot is the only thing between an env/IPC-sourced value and the hard-line-adjacent ingestion loop; the L28 truthy-non-`true` vector (`"true"`/`1`/the L28-named `"false"` string/`{}`) must NOT arm ⇒ `undefined` + the proof-spine builder never invoked, with a co-located `literal true arms` positive control (non-vacuity, not a blanket deny). Both leave the leakage-eval / real-arming as the owner crossing (#13), not a flip.

**Rule:** pin a safety invariant at the REAL assembled composition root (`assembleBackends`/`boot` with the DEFAULT wiring), not only at the injected-fake unit level — a fake-injected unit test can't catch a worker-side wiring regression that swaps or bypasses the real seam; drive raw-employer content through the real egress veto (cloud ⇒ DENY no-fallback, OpenRouter is its OWN cloud processor never laundered local, the veto precedes the allowlist, a tunneled-`local` remote still DENIES) and pin the boot arming gate as the SOLE chokepoint against a truthy-not-`true` vector (reinforcing L28: `"true"`/`1`/`"false"`/`{}` ⇒ no-arm + zero build-invocation, with a co-located `literal true` positive control for non-vacuity). `pin: egress-veto-assembled.test.ts assembled_broker_employer_raw_ack_off_openrouter_cloud_denied + assembled_broker_veto_denies_even_when_processor_allowlisted + assembled_broker_tunneled_local_denied · boot-auto-ingest-gating.test.ts auto_ingest_truthy_not_true_does_not_arm + auto_ingest_literal_true_arms`.

## <a id="51"></a>51. GATE-1: promote the agent extraction to a first-class EVIDENCE-PRESERVING Appendix-A schema so `validateNoInference` runs on the model's real evidence — force a primitive-union to ajv-strict `anyOf` via `.finite()`, blocklist the pollution keys structurally

**Date:** 2026-07-17.
**Source slice:** 18.11 / CP-1 `beb77b6d` (brief 125; contract-impl territory — the DOMAIN round-trip test is worker-adjacent).

The meeting/source legs rode a **KMP stand-in** (`sow:knowledge-mutation-plan`) that DISCARDS the extraction's per-field `evidenceRef` — so arming a REAL model over it would let a concrete invented owner/date (no `evidenceRef`) pass `validateNoInference` silently (GATE-1, the REQ-F-017 hard gate). The fix promotes the extraction to a first-class `AgentExtractionCandidate` + a `sow:agent-extraction` Appendix-A schema that PRESERVES `evidenceRef` end-to-end (`{ fields: Record<name, { value, evidenceRef? }> }`). Three techniques worth banking: (1) the schema is STRUCTURAL only — non-emptiness + the field catalog are the downstream `MeetingSchemaGate`'s job (L46 division of labor; empty `{fields:{}}` is structurally valid), and the value's evidence is `validateNoInference`'s; (2) a primitive union (`string|number|boolean`) renders as a `type`-array which ajv strict-mode REJECTS — emit `anyOf` instead, achieved by `z.number().finite()` (which ALSO rejects NaN/±Inf, a real constraint), so the shared registry's strict mode stays untouched (never add a global `allowUnionTypes` — that loosens strict for ALL schemas); (3) close the ajv↔Zod prototype-smuggling shape (`__proto__`/`constructor`/`prototype`) STRUCTURALLY with a `propertyNames` blocklist — don't rely on a downstream "consume the Zod-parsed object" convention a future consumer could violate; the full field-name charset allowlist stays deferred (§9 catalog = arch_gap). Name the contracts type to avoid a collision with an existing looser same-name type in a sibling package (`AgentExtractionCandidate` vs `@sow/workflows` `AgentExtraction`).

**Rule:** an evidence-bearing model extraction gets a FIRST-CLASS evidence-preserving Appendix-A candidate + schema (a KMP stand-in that discards `evidenceRef` silently defeats REQ-F-017 the moment a real model arms — GATE-1); keep the schema STRUCTURAL (shape only — non-emptiness/catalog are the downstream gate's, evidence is `validateNoInference`'s, L46); force a primitive-union to ajv-strict `anyOf` via `.finite()` rather than a `type`-array or a global `allowUnionTypes` (keep the shared registry byte-pristine); blocklist the prototype-pollution keys with `propertyNames` STRUCTURALLY (not a downstream consume-Zod-parsed convention); name the type to avoid a cross-package collision. `pin: agent-extraction.test.ts preserves_evidenceRef + rejects_dangerous_property_keys + freezes_generated_schema · agent-extraction-gate1.test.ts concrete_value_no_evidenceRef_rejected_inferred`.

## <a id="52"></a>52. A deny-only policing gate's SOURCE AND-locks to the SAME effect-transport arming as the run leg — armed-without-source fails closed to UNAVAILABLE (never stub-green); prefer a runtime fail-closed-with-HealthItem over a boot-throw

**Date:** 2026-07-17.
**Source slice:** 18.14 / CP-4 `f9e5417d` (brief 126).

The always-green `DEFAULT_HEALTH_SOURCES` is correct ONLY while the transport is dormant; once the real transport is ARMED it would false-green a dead/unreachable real provider into the deny-only HEALTH gate. `selectHealthSources` AND-locks the health-source SELECTION to the SAME `providerTransport` arming as `selectProviderRunner` (one owner flip arms both run + health — no split-brain). Two load-bearing points: (1) the REVERSE split-brain — arming the runner but OMITTING `healthSource` — fails closed to a `UNAVAILABLE` (unreachable) source, NEVER the stub-green default (a misconfig can't silently re-open the false-green); (2) that armed-without-source case is a RUNTIME fail-closed DENY that surfaces an operator-visible OBS-2 HealthItem end-to-end (stage `health`, `provider_unavailable`), NOT a boot-throw — for a control-plane worker, degrade-and-surface-health beats crash-at-boot (§16), and L39's silent-black-hole concern is met BY the health item (the misconfig is loud, just not fatal). Byte-equivalent dormant (exact-instance + factory-spy zero-invocation OFF pin, L23; strict `===true` AND-lock, truthy-not-`true` fails closed, L28); both safety-posture health constants `Object.frozen` (L31).

**Rule:** a deny-only POLICING gate's SOURCE must AND-lock to the SAME effect-transport arming as the run leg (one flip arms both — a stub-green source is only safe while the transport is dormant); arming the effect leg while OMITTING the source fails closed to UNAVAILABLE, never stub-green (close the reverse split-brain); prefer a RUNTIME fail-closed deny that surfaces an operator-visible HealthItem over a boot-throw (§16 — a control-plane worker degrades + surfaces health, never crashes; the health item satisfies L39's loud-not-silent requirement without being fatal); ship byte-equivalent dormant (exact-instance + factory-spy OFF pin, L23; strict `===true`, L28). **ARMING CAVEAT:** an explicit `config.healthSources` takes PRECEDENCE over the AND-lock — when arming the transport, do NOT also bind a green explicit source (re-opens the false-green). `pin: health-source-and-lock.test.ts armed_without_healthSource_surfaces_health_item + dormant_default_stays_green_byte_equivalent + armed_transport_unhealthy_provider_denies + health_source_selection_and_locked`.

## <a id="53"></a>53. A fail-closed credential HOLD gets operator observability WITHOUT weakening — a generic `missing` HealthItem, best-effort (never alters the Err), distinct-from-locked via subjectRef over the SAME FailureClass (no frozen-taxonomy expansion)

**Date:** 2026-07-17.
**Source slice:** 18.16 / CP-6 `bfd477cd` (brief 127).

A genuinely-missing (un-provisioned/unresolvable) credential was a SILENT fail-closed HOLD — the operator couldn't see a job stuck pending provisioning. The fix mints a `credential-unavailable` HealthItem on the lock-routing accessor's `missing` resolution (incl. facade-absent + facade-throw), WITHOUT weakening the fail-closed posture. Four bankable points: (1) the mint is BEST-EFFORT — a mint/router fault is swallowed and the accessor STILL returns the fail-closed `missing`/`locked` Err, never rejects (§16, L15-pinned across sync-throw + async-reject); (2) distinct-from-locked (L41 — never mislabel a missing key a retryable lock) is achieved by a distinct `credential:` subjectRef prefix over the SAME existing `worker_down` FailureClass — NO frozen-taxonomy expansion (L25), and the §10.3 `(failureClass, subjectRef)` dedupe identity differs so missing↔locked never collide; (3) value-free (rule 7) — only the safe class + subjectRef (a provider id) + `now` cross into the health path, never the key or its raw ref; (4) where a collapsed taxonomy can't distinguish sub-causes (the facade collapses `invalid_ref`→`missing`), the message stays GENERIC ("credential unavailable — verify provisioning/ref") — don't claim a distinction you can't make; the `SecretUnavailableReason` split is a documented Future-TODO. Part (b): a durable park's `auditRef` is a DETERMINISTIC mint keyed on the record identity (`meeting-park:<sourceId>:<idempotencyKey>`) — never null (§16 operational truth), idempotent (first-write-wins reuse, L36).

**Rule:** give a fail-closed credential HOLD operator observability WITHOUT weakening it — mint a GENERIC redaction-safe HealthItem on the `missing` resolution, BEST-EFFORT (a mint fault never alters the fail-closed Err, §16), distinct-from-locked via a distinct subjectRef prefix over the SAME existing FailureClass (no frozen-taxonomy expansion, L25; the §10.3 identity differs so the two never collide, L41), value-free (safe class + subjectRef + now only, rule 7); where a collapsed taxonomy can't separate sub-causes, keep the message generic + defer the split (don't assert a distinction you can't make); a durable record's audit ref is a deterministic identity-keyed mint — never null, idempotent (L36). `pin: keychain-boot.test.ts missing_routes_credential_unavailable_mint + credential_unavailable_distinct_from_locked + mint_is_best_effort_never_changes_result + missing_mint_input_redaction · dispositionDurable.test.ts recordpark_mints_stable_auditref_never_null`.

## <a id="54"></a>54. COST-1 dollar cap: conservative element-wise-MAX pricing is fail-SAFE (over-count ⇒ the cap fires early, never under-caps); ship the deny-gate ON but GUARD the shipped-behavior (a tokenless dormant job must not be newly-denied)

**Date:** 2026-07-17.
**Source slice:** 18.15b / CP-5b `b24429f5` (brief 130).

`pricingFor` keys pricing by PROVIDER while real pricing is PER-MODEL, so a single provider entry must cover whatever model a route resolves to. Project the per-model table to ONE conservative provider-axis rate via the ELEMENT-WISE MAX (CP-5a `conservativeProviderPricing`) — an over-count is the fail-SAFE direction: the deny-only dollar cap only ever REDUCES spend, so over-denying a cheaper model is safe while under-counting a pricier one would let real spend exceed the cap undetected. Ship the deny-gate ON by default (no dormancy knob, L44) — but shipping a cap ON buys ONE risk: a shipped-behavior regression, so guard it. (1) A tokenless dormant job (the stub run leg emits `{runtimeSeconds:1}`, no tokens) must meter as UNMEASURED cost (`meterUsageCost` → `measured:false`) ⇒ NO cost breach — else turning the cap ON newly-DENIES every current job. (2) An over-cost route DENIES on COST specifically (runtime under cap isolates the previously-dead limb). (3) An under-cost route PASSES (the cap DISCRIMINATES, not deny-all). Absent/empty pricing for a provider ⇒ `pricingFor` undefined ⇒ `measured:false` ⇒ the always-present runtime cap is the stricter-present backstop (never a false cost-cheap, never a false ALLOW). The projection helper throws fail-CLOSED at module load on an empty/NaN/negative table — a composition misconfiguration surfaces at boot, never a silent never-breaching rate.

**Rule:** activate a dollar cap by projecting per-model pricing to the conservative element-wise MAX (over-count = fail-safe, never under-caps) threaded into the composed budget gate default-ON (deny-only ships ON, L44); PIN the shipped-behavior — a tokenless dormant job stays UNMEASURED (no new deny), an over-cost route denies on COST, an under-cost route passes (discriminates); absent pricing degrades to `measured:false` runtime-only (never a false cost-cheap); the projection throws fail-CLOSED at import on a malformed table. `pin: budget-cost-cap.test.ts default_on_gate_passes_tokenless_dormant_usage + composed_gate_denies_over_cost_claude_route + composed_gate_allows_under_cost_claude_route + unpriced_route_degrades_runtime_only_byte_equivalent`.

## <a id="55"></a>55. A compiled-in safety cap CAN'T fail-open; a boot config-loader on a SAFETY cap CAN — prefer the compiled-in constant + a BIDIRECTIONAL transcription drift-guard until a fail-CLOSED-at-arming loader exists

**Date:** 2026-07-17.
**Source slice:** 18.15b / CP-5b `b24429f5` (brief 130).

Sourcing the cost-cap pricing forked: (A) a compiled-in constant transcribed from `config/providers.defaults.json`, vs (B) a boot loader that READS the config at runtime (the ARCHITECTURE §5.4 "must not hardcode" single-source-of-truth end state). Chose A on SPEND-SAFETY grounds, not convenience: option B's loader would fail-OPEN on any repo-root / packaged-path resolution failure ⇒ the dollar cap silently VANISHES ⇒ uncapped real spend — the exact silent-safety-loss class this round hardens (CP-4 always-green, 17.3 silent-hold). A compiled-in constant CANNOT vanish. The transcription-drift risk A carries is bounded three ways: the conservative element-wise MAX (over-count = fail-safe direction, L54), a dated "conservative fail-safe PLACEHOLDER pending flip-time re-verify" citation (NOT current authority — the exact per-model numbers are re-verified against the live source at the crossing, a HARD flip precondition), and a BIDIRECTIONAL drift-guard test (`toEqual` a config-built map, mirroring `default_budget_defaults_match_config`) — an unmirrored config edit OR an ungrounded extra key in the constant both go RED, and it stays non-vacuous on an emptied config. The single-loader unification is an explicit Future-TODO — but one that fails CLOSED at arming, not open.

**Rule:** for a SAFETY cap, prefer a compiled-in constant (can't fail-open) + a BIDIRECTIONAL transcription drift-guard over a boot config-loader that could fail-OPEN on a path miss and silently lose the cap; frame the constant as a conservative fail-safe PLACEHOLDER re-verified against the live source at flip (a hard precondition), not current authority; defer the single-loader unification as a fail-CLOSED-at-arming Future-TODO. `pin: budget-cost-cap.test.ts default_claude_pricing_matches_config (bidirectional toEqual + non-vacuity guard)`.

## <a id="56"></a>56. Ground vendor pricing on the LIVE authoritative source, never cached/memory — and don't falsify a cited table even in the "conservative" direction (opus-4-8 = $5/$25; the "$10/$50" was a Fable-5 conflation)

**Date:** 2026-07-17.
**Source slice:** 18.15b / CP-5b `b24429f5` (brief 130, Finding-E).

The brief + the predecessor slice's commit asserted opus-4-8 was "$5/$25 stale, live $10/$50" and instructed a bump. Grounding on the authoritative live pricing source (claude-api Current Models, cached 2026-06-24 — newer than the config's 2026-06-04) refuted it: opus-4-8 = $5/$25 (current/correct), and $10/$50 is FABLE 5's rate — a conflation carried through memory. Two bankable points: (1) NEVER trust cached/memory vendor pricing — it drifts and conflates model families; ground on the live authoritative source before touching any spend-safety value (the CLAUDE.md "never answer LLM pricing from memory" rule is load-bearing). (2) Even when a value feeds a CONSERVATIVE cap, do NOT over-state a per-model rate in a CITED authoritative table to "add margin" — falsifying a citation is worse than a non-existent staleness, and it's unnecessary: the conservative margin is already delivered correctly by the element-wise MAX (fable-5's $10/$50 dominates), so the cap is maximally conservative WITHOUT falsifying opus. Raised as a Finding, independently verified by the orchestrator, and the instruction was corrected — the original input was the phantom, the grounded evidence won (phantom-message defense).

**Rule:** ground every spend-safety vendor value on the LIVE authoritative source, never cached/memory (which conflates model families and drifts); never over-state a per-model rate in a cited authoritative table even "conservatively" — falsifying a citation is worse than a non-existent staleness, and the conservative margin belongs in the element-wise-MAX projection (L54), not the source data; when grounded evidence contradicts an instruction, raise it as a Finding and defer to the evidence.

## <a id="57"></a>57. Thread the arming SEAM only — the dormant default stays EMPTY (fail-closed preserved); arming supplies the real value + any coupled schema flip. Additive conditional-spread + AND-lock so a supplied input can't arm a disabled gate

**Date:** 2026-07-17.
**Source slice:** 18.13b / CP-3b `052522ae` (brief 129).

The #13 "auto-ingest fails source CLOSED at the schema gate" precondition needed a source `stubExtraction` threading point. Trace correction: the register-hook `stubExtraction?` param already existed and the source reconstruction was already candidate-driven (CP-2b's shared `mapAcceptedMeetingExtraction`) — the real hole was that `AutoIngestWiring`/`gateAutoIngest` didn't CARRY a stubExtraction, so an armed source booted with the `assembleBackends {candidateOutput:{}}` default and failed CLOSED at the schema gate. The fix threads the SEAM ONLY: an additive optional `AutoIngestWiring.stubExtraction?` + an optional param on `gateAutoIngest` that is CONDITIONALLY spread onto the wiring (omitted when absent ⇒ the default wiring shape is byte-identical, pinned by `.toStrictEqual` + an `in`-check). The DORMANT default stays EMPTY: the source still fails closed today (correct), because ARMING is what supplies the real stub AND flips the coupled `outputSchemaId → sow:agent-extraction` (so the stub normalizes to an `agent_extraction` candidate, not the KMP stand-in ⇒ EMPTY ⇒ reject) — the end-to-end is reachability-WAIVERED (L11) until then. The AND-lock is load-bearing: the gate's OFF guard (`opts.autoIngest !== true || vaultRoot === undefined`) runs BEFORE the stub is read, so a supplied stub can NEVER arm a disabled gate (a stub is not an arming knob) — pinned with an off-path-with-stub test.

**Rule:** for an arming precondition, thread the SEAM only — add the additive optional carrier (conditional-spread so the default shape is byte-identical, `.toStrictEqual` + `in`-check pinned) and keep the DORMANT default EMPTY / fail-closed; arming supplies the real value AND any coupled schema/id flip (end-to-end reachability-WAIVERED, L11, until then); keep the OFF guard BEFORE the new input is read so a supplied value can't arm a disabled gate (AND-lock, off-path-with-input pinned). `pin: boot-auto-ingest-gating.test.ts gate_threads_source_stub_when_provided + gate_omits_stub_by_default_byte_equivalent + gate_off_path_ignores_stub`.

## <a id="58"></a>58. Assemble the owner's crossing bundle in ONE tested WORKER helper (the layer rule forbids `providers→worker`); forward the arming gate through the boot reconstruction seam; pin the FORWARD with a NON-gated unit test (the boot integration path is SOW_API-gated/skipped in preflight)

**Date:** 2026-07-17.
**Source slice:** 18.18a / flip-wiring (worker) `aa4b93e1` (brief 132).

The Phase-18 flip-wiring worker leg had two parts, both DORMANT / byte-equivalent until the owner arms. (1) **Forward-only fix.** `bootWorker` reconstructs a `BackendsConfig` from `BootConfig` by forwarding a SUBSET of fields, and it SILENTLY DROPPED `config.providerTransport` — the field is already inherited (`BootConfig extends BackendsConfig`) but was never copied — so an owner-set `ProviderTransportGate` never reached `selectProviderRunner`/`selectHealthSources` (the flip would have been INERT). Extract a pure `buildBackendsConfig(config): BackendsConfig` seam and forward `providerTransport` via CONDITIONAL-spread: unset ⇒ the key is ABSENT (not merely `undefined`) ⇒ `backendsConfig` is byte-equivalent to pre-slice ⇒ `selectProviderRunner(config.providerTransport, stub)` returns the EXACT shipped deterministic stub. (2) **One assembly chokepoint.** `buildRealProviderTransportGate(deps)` composes the owner's crossing bundle `{ enabled: true, make: () => createRealProviderRunner(runnerDeps), healthSource? }` — `make` is a THUNK (the real runner is NOT constructed at build; factory-spy 0×@build, exactly 1×@`make()`), so building the bundle opens no socket. **Territory (the layer rule):** the bundle-assembly is WORKER-side because `{providers,integrations} → policy → {domain,contracts}` forbids a `providers→worker` import; providers owns only the reusable transport (`createRealModelHttpTransport`, providers L7). **L52 reaffirmed:** the real health source rides `gate.healthSource` ONLY — the helper's return type can't produce a `config.healthSources` binding (which takes `??` precedence at `backends.ts:794` and would re-open the always-green false-green under a real transport); omitting the source on an armed gate ⇒ `selectHealthSources` → `UNAVAILABLE_HEALTH_SOURCES`, never the green stub. **Pin discipline (Flag-3):** the forward regression is pinned by a NON-gated `buildBackendsConfig` UNIT test — the `bootWorker` broker-routing integration path is SOW_API-gated and SKIPPED in preflight, so a behavioral-only boot assertion would guard NOTHING in CI. **Finding-F:** the separate agent-sdk `meeting.close` factory (`copilotRealModel`/`copilotAgentMode`) is untouched — `providerTransport` arms raw-model routes only.

**Rule:** when a boot-config reconstruction forwards a SUBSET of fields, adding an arming gate to the config type is not enough — the reconstruction silently DROPS any field it doesn't copy (an owner-set gate stays inert), so extract the reconstruction into a pure `build…Config` seam, forward the gate via conditional-spread (unset ⇒ key ABSENT ⇒ byte-equivalent, `.toStrictEqual({})` + `in`-check pinned), and pin the FORWARD with a NON-gated unit test because the integration boot path is env-gated/skipped-in-CI (a behavioral-only test guards nothing). Assemble the owner's crossing bundle in ONE tested WORKER helper (`{enabled:true, make:thunk, healthSource?}`, `make` factory-spy-zero-at-build) — WORKER-side because the layer rule forbids `providers→worker`; the real health source rides `gate.healthSource` ONLY (never `config.healthSources` — L52; omitted ⇒ UNAVAILABLE, never stub-green). `pin: buildBackendsConfig.test.ts armed_providerTransport_forwards_into_backendsConfig + unset_providerTransport_is_byte_equivalent + forward_coexists_with_sibling_fields · real-provider-transport-gate.test.ts gate_is_enabled_true_literal + make_is_a_thunk_zero_invocation_at_build + healthSource_rides_the_gate_not_config + healthSource_omitted_fails_closed_to_unavailable`.

## <a id="59"></a>59. A subscription/agent-runtime run leg emits candidate data by passing `CompletionOutput.structuredOutput` through UNVALIDATED as `AgentResult.candidateOutput`; inlines content via an injected ContentResolver seam; dollar-caps via the SDK-native `maxBudgetUsd` (the token-priced budget gate can't meter a runtime route); folds every `CompletionError→GateDeny` KIND-only with `auth` ENFORCED terminal

**Date:** 2026-07-18.
**Source slice:** 18.20 / subscription-routing round (Option B) `7ab16dcb` — bind the fail-closed runtime branch of `createRealProviderRunner` to a subscription-extraction runner.

Under Option B, real-model extraction runs on the Claude SUBSCRIPTION (`createClaudeSubscriptionCompletion`, Agent SDK on the local `claude` login; worker `ANTHROPIC_API_KEY` UNSET). The run leg (a `ProviderRunner`) binds the runtime branch (`!("provider" in route)`, previously a fail-closed deny) to a worker-side subscription runner that MIRRORS the `createClaudeCopilotSynthesis` sibling (the layer rule forbids `providers→worker`, so the adapter is worker-side; providers owns only the generic `createClaudeSubscriptionCompletion` client + the extraction-request builders). Invariants:

- **Candidate pass-through, UNVALIDATED (rule 2 / L46).** The runner frames `CompletionOutput.structuredOutput → makeAgentResult({candidateOutput})` VERBATIM — it does NOT parse/normalize the model output. The broker's `bySchemaIdNormalizer` (keyed on `job.outputSchemaId === "sow:agent-extraction"`) + `validateNoInference` (REQ-F-017) are the candidate gate; the run leg is NOT the gate. `evidenceRef` survives because the run leg touches nothing.
- **Content inlines via an injected ContentResolver seam.** The subscription `CompletionRequest.userPrompt` needs INLINE text; the ref path (`contextRefs`/`refsToPromptText`) is deliberately MARKER-only (no `contextRefs→text` resolver exists). A NEW injected `ExtractionContentResolver` resolves it — UNBOUND this round (fake-tested; the real resolver binds at ENABLE, like the fetch transport, L11).
- **Dollar cap ONLY via the SDK-native `maxBudgetUsd`.** The token-priced broker POST budget gate can't meter a runtime route (`pricingFor` returns undefined with no `provider` on the route — Finding-F; `usage.runtimeSeconds=0` unmeasured), so the enforced `budget.maxCostUsd` reaches the model ONLY via `buildExtractionCompletionRequest`→`CompletionRequest.maxCostUsd`→SDK `maxBudgetUsd` (the in-flight cap; a breach returns `error_max_budget_usd`→`budget`). See providers L8.
- **Every `CompletionError → GateDeny` KIND-only (rule 7).** The deny names the KIND only (never the SDK message — it may carry prompt/content). `budget → budget_exceeded/cancelled_budget`; `auth` ENFORCED TERMINAL (a subscription login has NO keychain credential to HOLD — do NOT route it through `KeychainLockController` like the raw-provider `auth_unavailable`; pin an `auth`+`retryable:true`→terminal case so the enum can't drift); `transport/rate_limited/timeout` retryable per `error.retryable`; a content-seam/client throw folds to `provider_unavailable` (§16 totality — `return await` so the outer catch backstops, L15).
- **Defense-in-depth over the egress veto (rule 5).** The runner asserts `route.egressClass === "cloud"` before egress (mirrors the copilot sibling) — a mis-classified `{runtime,local}` route can't smuggle cloud egress. The PRIMARY veto (broker fixed order admission→route→EGRESS_VETO→…→RUN) precedes the run leg regardless of branch and stays primary; the cloud-route veto re-eval for untrusted employer-raw jobs is the owner ENABLE (#13).
- **Dormant / byte-equivalent, 3 OFF-locks.** The subscription deps are OPTIONAL on `RealProviderRunnerDeps` and thread through `buildRealProviderTransportGate` VERBATIM (ZERO composition-root edit — one commit, no L23 split). OFF-locks: `providerTransport` unset · subscription unbound · no `{runtime}` route in shipped `capabilityDefaults`. Reachability-WAIVERED (L11); an unresolved `outputSchemaId` denies fail-closed BEFORE any content resolve/dispatch.

**Rule:** bind a subscription/agent-runtime run leg by MIRRORING the sibling completion adapter WORKER-side (the layer rule forbids `providers→worker`); pass `CompletionOutput.structuredOutput` through as `AgentResult.candidateOutput` UNVALIDATED (the broker normalizer + `validateNoInference` are the gate, not the run leg, L46); inline content via an INJECTED resolver seam left unbound until ENABLE (the ref path is marker-only); dollar-cap via the SDK-native `maxBudgetUsd` since the token-priced budget gate can't meter a runtime route (Finding-F); fold every `CompletionError→GateDeny` KIND-only (rule 7) with `auth` ENFORCED terminal (no keychain to HOLD) + `return await` for the totality backstop; assert `egressClass==="cloud"` as defense-in-depth over the primary veto; keep the subscription deps OPTIONAL so they thread the existing arming bundle with zero composition-root edit (byte-equivalent, 3 OFF-locks, reachability-waivered). `pin: subscription-extraction-runner.test.ts success_frames_structuredoutput_as_candidateoutput + threads_enforced_maxcostusd_zero_by_presence + budget_completion_error_maps_to_cancelled_budget_deny + completion_error_auth_maps_terminal + content_seam_throw_folds_closed + content_resolution_err_folds_closed_no_dispatch + unresolved_output_schema_folds_closed_no_dispatch · provider-runner.test.ts runtime_route_selects_subscription_runner_only_when_bound + off_gate_constructs_no_subscription_client`.

## <a id="60"></a>60. The extraction content resolver derefs the job → parked `sourceId` via an EXACTLY-ONE `refKind:"source"` contextRef, reads `SourceEnvelope.body` via the existing durable reader, WS-8 read-back re-gates, and fails closed code-only — never `ok("")`

**Date:** 2026-07-18.
**Source slice:** 18.21 / staged-ENABLE round (Option B) — `createRealExtractionContentResolver`.

The subscription runner (L59) needs inline `userPrompt` content, but the ref path (`contextRefs`/`refsToPromptText`) is deliberately marker-only. The real `ExtractionContentResolver` derefs the extraction `AgentJob` to its parked `sourceId` via an EXACTLY-ONE `contextRefs` entry with `refKind === "source"` (multiple OR zero matches ⇒ fail-closed `source_ref_unresolved`, NEVER a `.find` first-match guess — WS-8/no-inference), reads the body via the EXISTING `createDurableParkedReader`/`ParkedSourceReader.read(sourceId)` → `SourceEnvelope.body`, and returns it. Uniform over `meeting.close` + `source.process` (both park a `SourceEnvelope` keyed by `sourceId`).

Two load-bearing fail-closed properties: (1) **WS-8 read-back re-gate** — the parked envelope's `workspaceId` MUST equal `job.workspaceId`, else `err(workspace_mismatch)` BEFORE the body is read out (rule 4 / L12/L20/L32); `ParkedSourceReader.read` keys on `sourceId` (not workspace-scoped) and the `sourceId` is externally-populated, so a mis-populated/smuggled ref would otherwise leak a foreign workspace's body to the model → the wrong workspace's notes. (2) **Never `ok("")`** — an absent OR empty body ⇒ `err(no_body)` (an empty prompt is not a valid extraction input). Faults are code-only (rule 7 — the reader's message carries the `sourceId`, dropped); total (§16 — a reader throw folds to a typed err). Reachability-WAIVERED (L11) — the ENABLE caller (step 6) populates the `{refKind:"source", ref:sourceId}` ContextRef + binds the resolver as `subscription.content`.

**Rule:** an extraction content resolver derefs the job → the parked `sourceId` via an EXACTLY-ONE `refKind:"source"` contextRef (multiple/zero ⇒ fail-closed, never a first-match guess — WS-8/no-inference), reads the body via the existing durable `SourceEnvelope.body` reader (not the marker-only ref path, not the vault directly), WS-8 read-back re-gates the parked envelope's workspace against the job BEFORE returning content (a foreign-workspace body never crosses to the model, rule 4), fails closed code-only on unresolved-ref/unavailable/empty (NEVER `ok("")`), and is total (§16). `pin: real-extraction-content-resolver.test.ts workspace_mismatch_fails_closed + empty_or_absent_body_fails_closed + exactly_one_source_ref + reader_throw_folds_closed`.

## <a id="61"></a>61. The staged-ENABLE arming surface is built ENTIRELY as pure default-OFF helpers (boot untouched) — an AND-locked route knob (one flip, no split-brain), a route-shape-agnostic rule-5 proof, an armed-path shadowing-env guard, a deny-only cost cap + a fail-closed health wrap

**Date:** 2026-07-18.
**Source slice:** 18.23 / staged-ENABLE round (Option B) `58ea6100` (A) + `e46160f4` (B) — the owner-GO'd steps 0/2/3b/4/5, all dormant; the gate flip (step 6) + first real spend (step 7) are the owner HARD STOP.

A STAGED go-live (build the arming preconditions, HARD STOP before the flip) is built as pure default-OFF helpers that leave the composition root (`boot.ts`/`backends.ts`) BYTE-UNTOUCHED — every piece is reachability-WAIVERED (L11) and the owner's single step-6 flip wires them all together. The pieces:

- **AND-locked route knob** (`selectExtractionRoute(armed)`): STRICT `=== true` (truthy-not-`true` ⇒ local, L28); `armed` derives from the SAME predicate `selectProviderRunner` reads (`providerTransport.enabled===true && make is fn`), so ONE flip arms transport + route with no split-brain (L52); default ⇒ the byte-identical shipped `{provider:ollama,local}` route, armed ⇒ the cloud `{runtime}` route. Keep it a SEPARATE knob from the transport gate (don't overload `providerTransport`), AND-locked to the same signal.
- **Rule-5 proof** (the load-bearing SAFETY pin): a cloud `{runtime}` route is route-shape-agnostic to the egress veto — `providerOfRoute` is null so `resolveRoute` SKIPS the provider allowlist + the local-endpoint check and RESOLVES → reaches the veto → `processorOfRoute` (keying off `egressClass`/processor, NOT the provider-vs-runtime branch) ⇒ employer-raw + ack-OFF ⇒ DENY `EMPLOYER_RAW_EGRESS_UNACKNOWLEDGED`, run leg unreached. Verify through the REAL `assembleBackends({})` broker + pair with a NON-VACUITY positive control (ack-ON resolves past the veto to the stub — L7).
- **Armed-path shadowing-env guard** (`assertSubscriptionAuthEnv`): a subscription-shadowing env var on the ARMED path ⇒ a fail-closed typed refusal (rule-7 code-only, no value echoed); the shadowing set is an EXPLICIT EXTENSIBLE constant (`SUBSCRIPTION_SHADOWING_ENV_KEYS`), NOT just `ANTHROPIC_API_KEY` — enumerating the FULL set (`ANTHROPIC_AUTH_TOKEN` + base-url overrides) against the live SDK docs is a HARD ENABLE precondition (a missed var = a silent fail-OPEN), so name the seam + defer the full enumeration to the flip rather than guessing var names. Unarmed ⇒ never reads env (byte-equivalent).
- **Deny-only cost cap** (moves the WORKER constant + its `config/*.json` SEED together): the extraction dollar cap rides `DEFAULT_BUDGET_DEFAULTS` AND `config/providers.defaults.json §budgets` — a BIDIRECTIONAL drift-guard (`default_budget_defaults_match_config`) asserts they're byte-identical, so they MOVE TOGETHER or the guard goes red (L55). Deny-only ships ON (L44); a raise never newly-denies a tokenless dormant job (L54); both bounds are re-confirm-at-final-spend placeholders.
- **Fail-closed health wrap** (`createSubscriptionHealthSources`): maps the 18.22 verdict — reachable ⇒ healthy/passing, ANY unhealthy ⇒ `unreachable`/failing (the HEALTH gate denies; no false-green, L52); rides `gate.healthSource` ONLY.

**Rule:** build a STAGED go-live arming surface as pure default-OFF helpers that leave the composition root byte-untouched (reachability-WAIVERED, the owner's single flip wires them) — an arming route knob AND-locked to the SAME predicate the run-leg gate reads (one flip, no split-brain, kept a separate knob), a route-shape-agnostic rule-5 DENY proof through the REAL broker + a non-vacuity positive control (L7), an armed-path shadowing-env guard whose watched set is an EXPLICIT EXTENSIBLE constant with the full enumeration deferred-to-flip-not-guessed, a deny-only cap whose worker-constant + config-seed move together under the drift-guard (L55), and a fail-closed health wrap (no false-green, L52). `pin: extraction-route-gate.test.ts route_knob_truthy_not_true_stays_local · egress-veto-assembled.test.ts assembled_broker_denies_employer_raw_cloud_runtime + assembled_broker_allows_personal_cloud_runtime · subscription-auth-guard.test.ts anthropic_key_set_on_armed_path_refuses + default_path_never_checks_key · subscription-health-sources.test.ts health_wrap_unhealthy_* · budget-cost-cap.test.ts source_process_extraction_cost_cap_is_1_5`.

## <a id="62"></a>62. The step-6 wiring binds a staged go-live behind ONE signal (`config.providerTransport`) — a single AND-lock predicate arms transport + route + auth-guard + ContextRef together; the eager-consumption + reader-ordering constraint defers the gate build + reader-holder to the owner flip

**Date:** 2026-07-18.
**Source slice:** 18.24 / step-6 subscription arming wiring `65dd9e5f` (A pure helpers) + `c9361092` (B composition-root wiring) — dormant; the arm (set the signal) + first run stay the owner HARD STOP.

Wiring a staged arming surface (L61) into the composition root binds every piece behind ONE owner signal so the flip is a single reviewed action — but a helper whose real dep only exists AFTER the composition root is assembled CANNOT be pre-built dormant; that one piece defers to the flip (L11 — don't wire dormant-on-dormant):

- **One AND-lock signal.** Extract the arming predicate to ONE type-guard (`isProviderTransportArmed`: `enabled===true && typeof make==="function"`) that BOTH the run-leg selector (`selectProviderRunner`) AND every armed seam (route swap, auth-guard fire, ContextRef populate) read — so the owner's single `config.providerTransport` flip arms them all together, no split-brain (L52). Don't add a second arming field: two owner inputs that must agree is a fail-OPEN surface.
- **Enumerate the safety set + fire it as a DEGRADE, not a crash.** The shadowing-env guard's watched set goes from the named placeholder to the FULL grounded set (here 8 vars) at the wiring; on the armed path a set var DEGRADES the arm (don't set the transport ⇒ extraction stays local + a HealthItem surfaces) rather than crashing the whole worker (L52 — prefer a fail-closed degrade+surface over a boot-throw; a persisted armed config must not down the worker on a later env change). Full-set re-verify vs live SDK docs stays a HARD flip precondition (a missed var fails OPEN); lowercase `http_proxy`/`https_proxy` + `ANTHROPIC_CUSTOM_HEADERS` are named flip-verify candidates.
- **Co-gate any shipped-shape change off the same signal.** A field that changes the shipped job/route shape (the source ContextRef, the cloud route) is applied by a post-processor over the pre-built params (`withSubscriptionExtractionArming`, mirroring `withDurableRevisions`) gated on the SAME signal ⇒ flag-OFF byte-equivalent; the id it carries comes from the routing-bound identity, NEVER content (WS-8, L33/L45).
- **Single-source the duplicated literal at the bind.** Where the wiring touches a value hand-copied across ≥2 sites (the local route in `boot.ts:1094` + `LOCAL_EXTRACTION_ROUTE` + `DEFAULT_ROUTE`), unify them to ONE constant so an armed-vs-default drift is impossible (L5/L37).
- **Defer the piece whose dep is post-assembly.** The transport gate's `make()` builds a runner from deps that include a resolver bound to `backends.repos.*` — but `assembleBackends` consumes `config.providerTransport` EAGERLY (`backends.ts:809`) and the repos exist only AFTER it returns. So the gate build + inject + the reader-holder late-bind CANNOT be dormant-wired; leave the pure BUILDER (`gateSubscriptionExtraction`, reachability-WAIVERED L11) + an in-code flip TODO naming the ordering, and the owner's step-6 sets `config.providerTransport = gateSubscriptionExtraction(...)` + fills the reader-holder post-assembly.

**Rule:** to wire a staged go-live (L61) into the composition root, bind every seam behind ONE signal read by a single AND-lock type-guard the run-leg selector already reads (transport+route+auth+ContextRef arm on one flip, no split-brain L52); enumerate the safety-env set to its full grounded value + fire it as a fail-closed DEGRADE-the-arm (not a worker crash — L52; full re-verify vs live docs a HARD flip precondition, a missed var fails OPEN); apply any shipped-shape change via a same-signal-gated post-processor (byte-equivalent OFF, id from the bound identity never content — WS-8); single-source any literal the bind duplicates (L5/L37); and DEFER the one piece whose real dep only exists post-`assembleBackends` (eager consumption) to the owner flip as a pure builder + an in-code TODO, never dormant-on-dormant (L11). `pin: subscription-extraction-arming.test.ts arming_gate_off_is_byte_equivalent + arming_signal_truthy_not_true_does_not_arm + resolve_degrades_on_shadowing_env · subscription-auth-guard.test.ts shadowing_env_full_set_enumerated + shadowing_env_full_set_each_var_fails_closed · boot single-source capability_defaults + withSubscriptionExtractionArming exactly-one-ref`.

## <a id="63"></a>63. Resolve the deferred post-assembly arm piece (L62) with a DEDICATED narrow runner (no registry deps) + a single late-bound reader-holder — not a full-registry multi-late-bind

**Date:** 2026-07-18. **Source slice:** 18.25 arm `7e892be3` + detectLogin `dd347b3c`.

When the L62-deferred piece (the gate whose `make()` needs a post-`assembleBackends` dep) comes due at the arm, wiring it via the FULL provider registry (`createRealProviderRunner`) drags in EVERY registry dep (`transport`, `facade`, `KeychainLockController`, `now`) — all also post-assembly — forcing a fragile 3-holder multi-late-bind + a `createLockRoutingSecretsAccessor(controller,…)` that runs at construction ("works because the broken registry is never used" — the exact anti-pattern to avoid on a hard-line crossing). Instead build a DEDICATED narrow runner for the armed capability (`createSubscriptionOnlyProviderRunner` — serves the cloud `{runtime}` route ONLY, FAILS CLOSED on a `provider` route = a bonus structural narrowing that never touches the KeychainLockController/secrets path, correct when no raw-API key is provisioned): it needs NONE of the registry deps, so the ONLY late-bind is ONE reader-holder (`createLateBoundParkedReader` filled with the real `createDurableParkedReader` POST-assembly). All broker gates (egress/health/budget/schema) still run UPSTREAM; the runner's route fail-closed is defense-in-depth. The macOS login-presence detector for the health leg is a spend-free Keychain-presence check (`security find-generic-password -s`, NO `-w` — a no-`-w` presence find is ACL-process-independent; the `-w` value read is the run's one-time "Allow" prompt). `pin: subscription-arm harness — assembly proof (runner denies pre-holder-fill, resolves post-fill) + subscription-only runner denies a provider route`.

## <a id="64"></a>64. Arming a new candidate KIND requires registering its parser in the WORKER schema-gate registry too (not just the contracts schema) + flipping the job outputSchemaId — else the armed run spends-and-produces-nothing; the pre-spend fake-completion dry-run is the go/no-go CP2 can't be

**Date:** 2026-07-18. **Source slice:** 18.27 / #13 Finding C `a20f9e7d` — caught by the maiden-run pre-spend dry-run.

A candidate KIND existing in `@sow/contracts` (schema) + the providers normalizer is NOT enough for the armed run to produce a note: the WORKER broker schema-gate registry (`CANDIDATE_MODEL_SCHEMAS`) must ALSO register the parser AND the job's `outputSchemaId` must be flipped to it (on the armed path, co-gated with the same arm signal — dormant default byte-equivalent). Miss either and the armed run emits the candidate → `schema_rejected` ("no model parser") → `mapCandidate` EMPTY → no commit → NO note: real spend, zero deliverable. **The spend-free CP2 arm-verification (health/route/veto) CANNOT catch this** — the gap is DOWNSTREAM of the arm, in the candidate→note path, which CP2 (no job runs) never exercises. **The go/no-go that catches it is a FAKE-completion dry-run** (real arm + real broker/gate/KnowledgeWriter, fake at the SDK seam ⇒ costUsd 0) proving broker-accepts + note-produced BEFORE the real query. Always dry-run the full candidate→note path with a fake completion before the first real spend. The GATE-1 payoff (REQ-F-017 no-inference on a real model's evidenceRef; an absent datum ⇒ `TBD` not invented) is proven only once this path is reachable end-to-end. `pin: agent-extraction-broker.test.ts assembled_broker_accepts_agent_extraction + inferred_candidate_rejected_no_note (REQ-F-017) + arming_flips_outputschemaid`.

## <a id="65"></a>65. Completing an "explicit-extensible" shadowing/redirect env guard to its full grounded set pre-arming — enumerate BOTH cases of every proxy var, EXCLUDE the bypass-allowlist, over-inclusion is fail-safe

**Date:** 2026-07-18. **Source slice:** 18.28 — the `/phase-exit 18` crossing-gate security-MEDIUM hardening.

The L61/L62 armed-boot shadowing-env guard (`SUBSCRIPTION_SHADOWING_ENV_KEYS` → `assertSubscriptionAuthEnv`) ships an EXPLICIT EXTENSIBLE set with the full enumeration "deferred-to-flip"; completing it (8→13) pre-employer-raw-arming closed a Class-B fail-OPEN. Three reusable rules: **(1) enumerate BOTH cases of every proxy var** (`HTTP_PROXY`+`http_proxy`, `HTTPS_PROXY`+`https_proxy`, `ALL_PROXY`+`all_proxy`) — Node/undici (`EnvHttpProxyAgent`, `proxy-from-env`) honor lowercase, so an uppercase-only guard is a silent fail-OPEN (a lowercase-only proxy redirects egress unwatched); **(2) EXCLUDE the bypass-allowlist** (`NO_PROXY`/`no_proxy`) — it is a redirect-EXEMPTION, not a redirect; watching it would false-positive-DEGRADE a legitimate config; **(3) over-inclusion is FAIL-SAFE** — a watched var the SDK does not actually honor only causes a spurious DEGRADE on the owner-gated ARMED path (safe, re-verified vs live docs at the flip), whereas a MISSED var/case is a silent fail-OPEN (redirected/intercepted egress) — so when unsure, INCLUDE. The `some()` guard body stays UNCHANGED (extensible without a logic change, L61); a set var DEGRADES the arm (`effectiveArmed=false` + typed fault, L52/L62), never crashes boot; the fault is code-only (rule 7). A TLS-interception ENABLER (`NODE_EXTRA_CA_CERTS`) that redirects nothing on its own (it matters only WITH an already-watched proxy) stays a re-verify-at-flip note, NOT a watched var. Context7-ground the set vs the live Agent-SDK docs at the flip (a HARD precondition, never memory — L56). `pin: subscription-auth-guard.test.ts shadowing_env_full_set_enumerated (13-var toStrictEqual) + shadowing_env_full_set_each_var_fails_closed + subscription-extraction-arming.test.ts armed_boot_degrades_on_lowercase_proxy`.

## <a id="66"></a>66. Generalize a FIXED-key frontmatter allow-list to a STRICT PATTERN allow-list that STILL structurally refuses arbitrary keys — reconstruct the key from the capture, visible truncation sentinel, absent→TBD retained

**Date:** 2026-07-19. **Source slice:** 18.29 — the maiden-run note-projection alignment.

The L49 source-note frontmatter convention (a FIXED `["owner","dueDate"]` allow-list) under-populated when the real model emitted a MULTI-TASK task-prefixed extraction (`task1_owner`, `task1_dueDate`, …) — the fixed read missed them, degrading to blanket `TBD`. Generalize the allow-list to a STRICT ANCHORED PATTERN (`^task\d+_(owner|dueDate)$` ∪ the bare keys) WITHOUT re-opening injection: a key lands ONLY on an anchored `^…$` match (never `/g`); **RECONSTRUCT the projected key from the pattern's `\d+` capture** — never reuse the raw input key, so a `\n`-tail / unicode-digit / suffix / reversed variant that partially matches can't smuggle the original bytes; keep every value on the SAME `neutralizeFrontmatterValue(frontmatterValue(...))` path (marker parity, rule 1); keep absent→`TBD` (REQ-F-017, no invention). Preserve the existing bare-key CONTRACT via UNION (a single-task source still emits `owner`/`dueDate`; a pure multi-task source shows a vestigial bare `TBD` pair — honest + contract-stable — plus the populated task keys; conditional-bare would break consumers relying on those keys). Bound the variable-arity key set with a defensive cap + a VISIBLE, UNFORGEABLE truncation sentinel (`tasksTruncated:true`, a projection-added literal NOT sourced from `fields` — no silent cap, L51). The note path stays identity-only (`deriveSourceNotePath` on `SourceNoteIdentity` — a frontmatter shape change can't move the note, WS-8/traversal). `pin: realSourceCommit.test.ts hostile_or_arbitrary_keys_never_land + projection_is_deterministic_ascending_order + many_tasks_capped_with_visible_sentinel + single_task_bare_keys_byte_equivalent`.

## <a id="67"></a>67. Pre-ARM assurance for an already-built-dormant owner-gated trigger — CONFIRM the OFF/arming/exclusion pins + tie the downstream safety gates to the DISPATCH path at the ASSEMBLED ROOT (L50) with a hostile input

**Date:** 2026-07-19. **Source slice:** 18.30 — the auto-ingest dormant-trigger verify-and-pin.

When a to-be-armed capability is discovered ALREADY built + wired dormant end-to-end (the "build dormant" is done; only the owner-gated ARM remains), the useful slice is NOT new wiring but a PRE-ARM ASSURANCE pass so the eventual flip arms a proven-safe seam. Two moves: **(1) CONFIRM-and-CITE** the dormant-safety pins that already exist — the OFF byte-equivalence + the strict `=== true` arming guard (L28) + the feedback-loop / self-output exclusion (L37) — verify they genuinely pin (not vacuous), and add NOTHING where covered (keep it tight; don't re-pin already-covered properties). **(2) FILL only the genuine gap** — tie the DOWNSTREAM safety gates (here ING-7 admission + the rule-5 egress veto) to the trigger's ACTUAL dispatch CAPABILITY (`source.process`, NOT the sibling `meeting.close` that the existing assembled-veto test happened to use) at the ASSEMBLED ROOT (L50 — `assembleBackends` binding the REAL admit/route/veto, never injected fakes) with a HOSTILE input: untrusted+mutating ⇒ admission-reject; employer-raw+ack-OFF+cloud ⇒ `egress_veto` DENY; + an ack-ON non-vacuity control (L7). The assurance is that the pipeline order (admission→route→egress_veto→run) short-circuits BEFORE the dormant run leg AND the production builder hard-codes the untrusted/`carriesRawContent` trust so no path bypasses the gates. **If a hostile input unexpectedly ADMITS/ALLOWS on the assembled path, that is a real dormant-safety hole ⇒ a Finding + STOP, never papered over with a passing test.** `pin: egress-veto-assembled.test.ts (source.process untrusted-mutating admission-reject + employer-raw-cloud egress_veto DENY + ack-ON control)`.

## <a id="68"></a>68. Thread an arming-adjacent egress-processor allowlist through the auto-ingest gate — an INDEPENDENT OFF-lock, populate BOTH raw + non-raw lists, byte-equivalent conditional-thunk, brand at the single worker-side site

**Date:** 2026-07-20. **Source slice:** 18.31 — the auto-ingest egress-allowlist seam (Path-β foundation).

An owner-gated arming path needs an egress allowlist to reach the veto, but the allowlist is an INDEPENDENT OFF-lock from the run-leg arming (Lessons 8/27/52) — arming the transport WITHOUT allowlisting the cloud processor still fails closed `PROCESSOR_NOT_ALLOWED`, which is the intended AND-composed safety. Thread it as an OPTIONAL `egressAllowedProcessors` through the gate (`gateAutoIngest` → `buildAutoIngestProofSpineParams`), passing it to the params-builder thunk ONLY when non-empty (conditional 2nd-arg, mirroring the stub-extraction seam L57) so the OFF/default path invokes the thunk EXACTLY as today — existing call-with-`ws` spy + `.toStrictEqual` pins stay green ⇒ byte-equivalent by construction (L23/L28). Populate BOTH `EgressPolicy.allowedProcessors` AND `rawContentAllowedProcessors` (source ingestion carries raw content, so the cloud processor must be in both). Brand `string→ProcessorId` at the SINGLE worker-side site (`buildAutoIngestProofSpineParams`), never at the caller — so the desktop layer forwards plain IPC-safe strings untouched, no `ProcessorId` concern crossing the layer boundary. Pin the invariant at the ASSEMBLED ROOT (L50): an armed personal-ws cloud route ALLOWs past the veto ONLY when allowlisted, else DENYs (non-vacuous control), and allowlisting NEVER bypasses the employer-raw veto (which precedes the rule-3 allowlist). `pin: egress-veto-assembled.test.ts (allow-only-when-allowlisted + empty-deny + employer-raw-precedence) + boot-auto-ingest-gating.test.ts (both-lists-populated + default-empty byte-equiv)`.

## <a id="69"></a>69. The pre-spend ENABLE go/no-go is a COMMITTED env-gated fake-completion armed dry-run — real arm+broker+all-gates+KnowledgeWriter over the assembled root ($0), non-vacuous deny control; drive the RISK surface (armed activities) not the orchestration

**Date:** 2026-07-20. **Source slice:** 18.33 — the committed L64 armed-auto-ingest dry-run harness.

When an owner-gated ENABLE risks "spend-and-produce-nothing" (a candidate that clears arming but never becomes a note — the L64 schema-registration / `outputSchemaId` gap), the go/no-go must be a COMMITTED, repeatable command, NOT a throwaway (the maiden run's harnesses were deleted, leaving no reproducible gate). Build it as an env-gated `-live` integration test behind a DEDICATED gate (`SOW_L64_DRYRUN` — NOT the `-live`/`SOW_TEMPORAL` gate, since Option B doesn't use Temporal; a dedicated gate keeps the command self-documenting + out of both the default AND `-live` suites): boot ARMED (`subscriptionArm.enabled` + `autoIngest` + `vaultRoot` + `egressAllowedProcessors:[<canonical processor const, not a magic string>]`) over a FAKE `$0` completion + fake content-resolver + fake `checkReachable`, drop a benign source, assert a REAL note through the FULL armed path — ING-7 admission, §5 egress veto, `agent_extraction` schema-gate, `validateNoInference`, KnowledgeWriter — ALL real over the assembled root; the SDK/network are the ONLY fakes. Drive the armed ACTIVITIES directly (bootWorker-direct), NOT a live-Temporal e2e: the L64 risk is DOWNSTREAM of the arm (candidate→note); the Temporal orchestration is already pinned elsewhere, so a live-Temporal rig adds flakiness to a command whose whole value is being a RELIABLE go/no-go. Include a NON-VACUOUS deny control (empty allowlist ⇒ `PROCESSOR_NOT_ALLOWED`, `completionCalls===0` proving the run-leg was never reached — the only diff is the allowlist, so the deny is attributable). Tie the harness's armed shape to the desktop-produced shape via the SHARED worker-owned types (tsc-enforced), keeping the desktop processor-agnostic — no cross-layer runtime fixture (LESSONS-§5). `pin: autoIngest-armed-live.test.ts (SOW_L64_DRYRUN — note-produced + absent-datum-TBD + no-refire L37 + empty-allowlist-deny)`.

## <a id="70"></a>70. Bind a real spend-free arming HEALTH-input behind its OWN independent enable OFF-lock — the arm flag alone stays fail-closed BY DESIGN; production default = the LIVE detector, never the fail-closed placeholder

**Date:** 2026-07-20. **Source slice:** 18.35 — the worker-side checkReachable bind seam (§ARM-18 ENABLE one-step prep).

A real spend-free reachability/health PROBE can be BUILT (18.26 `probeSubscriptionReachability` + 18.25 `detectClaudeKeychainLogin`) yet bound in NO production path — so the boot default stays `FAIL_CLOSED_REACHABILITY` and the owner ENABLE would need a CODE PATCH, not a config flip. Close it with a pure resolver `resolveArmCheckReachable(subscriptionArm, enableSignal, deps?)` behind a NEW **independent enable OFF-lock** (`SOW_SUBSCRIPTION_REACHABILITY_LIVE`, strict `"1"|"true"`, read at the boot root and passed to the resolver as a STRING — never `process.env` inside the pure resolver), so the arm flag ALONE stays HEALTH-denied BY DESIGN (env-only arm = FAIL_CLOSED — an intentional two-OFF-lock posture, L8/27/52/57; the real-probe injection is a DISTINCT owner step, not a side effect of the arm). Precedence order is load-bearing: explicit-injected-`checkReachable`-wins (preserves the existing `?? FAIL_CLOSED` test-injection seam) → **OFF-guard (`enabled !== true`) FIRST** → strict-signal → construct the probe thunk (a supplied signal can NEVER arm a disabled gate — L57 AND-lock). Shipped default byte-equivalent: return the SAME `FAIL_CLOSED_REACHABILITY` instance by IDENTITY (assert `===`, so byte-equivalence is provable, not merely behavioral) + the probe/detector factories invoked 0× on the OFF path (L23/L27 factory-spy); relocate the `FAIL_CLOSED_REACHABILITY` floor INTO the resolver module + import it into boot (single reference site). **THE silent-failure trap:** the production default detector MUST be the LIVE macOS Keychain-presence detector (`detectClaudeKeychainLogin`), NOT 18.26's file-path `detectClaudeLogin` (which fails closed on macOS by design, provider L10) — defaulting wrong makes the armed∧enabled path HEALTH-denied FOREVER, a fail-SAFE bug INVISIBLE to injected-deps tests; pin it by exposing the defaults as a referenceable constant and asserting `ARM_REACHABILITY_DEFAULTS.detectLogin === detectClaudeKeychainLogin` (identity, no I/O). Env inherits to the FORKED worker child (`fork(entry, [], {…})` with no `env` filter), so a shell-exported enable var reaches the in-child `bootWorker` — zero desktop change for the shell-export ENABLE; the `.env`-FILE path additionally needs the var on the desktop `dotenv-allowlist`. `pin: subscription-reachability-arming.test.ts (env_only_arm_stays_fail_closed [=== identity] + enable_signal_truthy_not_true + enable_signal_without_arm [AND-lock] + default_off_is_byte_equivalent_zero_probe_construction + default_detector_is_keychain_not_filepath)`.

## <a id="71"></a>71. A spend-safety shadow guard covers BOTH the `process.env` surface AND the harness settings-file surface (the SDK `query()` honors `settings.json`); presence-only, armed-path-only — and route EVERY arm consumer through ONE combined degrade signal (a missed site = split-brain)

**Date:** 2026-07-20. **Source slice:** 18.36 — the Claude-Code settings-level key-injection guard.

A subscription/agent-runtime run authenticating via an SDK `query()` that honors Claude-Code `settings.json` has a SECOND shadow surface the `process.env` guard (L61/L62/L65) structurally can't see: a **settings-level key injection** (`apiKeyHelper` mints a key; a settings `env` block shadows exactly like `process.env`; Bedrock/Vertex cred scripts + provider-switch flags redirect to a PAID cloud). Guard it as a PURE **armed-path-only** fold over an INJECTED settings-hierarchy reader (managed → CLI → project-local → project → user, honoring `CLAUDE_CONFIG_DIR`): a top-level cred-minting field present-AND-non-empty ⇒ degrade (an empty COMMAND mints nothing ⇒ clean), a settings `env` KEY present ⇒ degrade with **PRESENCE-not-value** semantics (an empty `ANTHROPIC_API_KEY` still wins its precedence slot — parity with the env leg; SINGLE-SOURCE the shared env-key set, L5/L37). PRESENCE ONLY (rule 7 — never execute the command / read the key value); absent settings ⇒ clean, unreadable/throwing settings ⇒ fail-SAFE degrade (L65). EXCLUDE common/ambiguous fields (a `model` pin, `forceLoginMethod:"claudeai"`) EXACTLY as L65 excludes `NO_PROXY` — watching them PERMANENTLY false-degrades a legit armed config; over-inclusion is fail-safe ONLY for RARE cred/redirect fields. Armed-path-only ⇒ byte-equivalent OFF (0 fs reads unless `effectiveArmed`, factory-spy). **THE split-brain trap (both reviewers caught it HIGH/MEDIUM):** when N boot consumers degrade off a combined signal, route ALL N through the combined var. This slice degraded the transport-strip (`armRefused = envRefused ∨ settingsFault`) but LEFT the 3rd consumer (route+ContextRef+schema arming) on the raw `effectiveArmed`, so a settings fault stripped transport while the route/schema stayed ARMED (L52 split-brain — safe today only via the downstream stub, a latent asymmetric-degrade vector). Bind every consumer to `armEffective = effectiveArmed ∧ no-settings-fault` + a derivation comment; the two boot-visible logs keep their DISTINCT reasons. A composition-level coherence pin (all degrade-consumers move together) needs the SOW_API boot-integration harness — the split-brain SLIPPED PAST the unit tests because the boot wiring is SOW_API-gated (L58). `pin: subscription-settings-guard.test.ts (apikeyhelper_present_faults + settings_env_empty_value_still_faults + off_path_reads_no_settings + excludes_model_forcelogin) + boot armEffective/armRefused derivation (typecheck + /wired, L58)`.

## <a id="72"></a>72. Ground a load-bearing security watched-set against the AUTHORITATIVE REPO source, not only public docs (public docs under-covered the provider-switch set TWICE); watch the PROVIDER SWITCH + exclude the downstream COMMON creds — sound ONLY when EVERY switch is watched

**Date:** 2026-07-20. **Source slice:** 18.37 — the subscription-shadow env-set extension to the full provider surface.

The §ARM-18 subscription-shadow guard's COMPLETENESS is load-bearing: a missed provider-switch env var is a silent fail-OPEN — an armed run routes to a paid 3P endpoint = real spend + redirected egress (rule 5). Grounding the watched set against **code.claude.com PUBLIC docs under-covered it TWICE** (18.28's original set → 18.37's public-docs extension → STILL missed the 5th switch `CLAUDE_CODE_USE_ANTHROPIC_AWS`, found only by the reviewer via the `anthropics/claude-code` REPO source `_HAS_3P_PROVIDER_AT_LOAD`). LESSON: for a load-bearing security env-enumeration, ground against the AUTHORITATIVE REPO source (the actual resolver/hook code), not public docs — and make a repo-source re-ground a HARD precondition before the armed run (extends L56 "never trust a load-bearing external value second-hand" — public docs are SECOND-HAND for a security enumeration). The exclusion DESIGN that keeps the set tractable: watch the PROVIDER SWITCH (the by-presence trigger — all 5: BEDROCK/VERTEX/FOUNDRY/MANTLE/ANTHROPIC_AWS) + the by-presence direct tokens/redirects/mTLS, and EXCLUDE the downstream COMMON cloud creds (`AWS_ACCESS_KEY_ID`/`AWS_PROFILE`/`GOOGLE_APPLICATION_CREDENTIALS`/…) + routing IDs + `NO_PROXY` (the L65/model class — watching a common var permanently false-degrades a legit armed config). **The exclusion is SOUND ONLY WHEN EVERY SWITCH IS WATCHED** — a bare common cred can't redirect CC auth without a switch, so excluding it is safe IFF the switch that activates it is caught; a MISSED switch makes the whole generic-cred exclusion fail-OPEN (`CLAUDE_CODE_USE_ANTHROPIC_AWS` was exactly this). Pin with an enumeration MIRROR (`set sorted === the N-key literal` — a silent drop FAILS) + each-var-fails-closed + a switch-soundness test (each cred-activating switch, watched, refuses) + exclusions-stay-excluded; single-source the set across every consuming leg (process.env + settings-file). `pin: subscription-auth-guard.test.ts (shadowing_env_full_set_enumerated 30-key mirror + shadowing_env_full_set_each_var_fails_closed + aws-cred-exclusion-sound-across-switches + shadowing_env_exclusions_stay_excluded)`.

## <a id="73"></a>73. A process.env-scan DENYLIST is structurally UNWINNABLE for a rule-5 egress COMPLETENESS invariant — invert to a spawn-env ALLOWLIST (complete-by-construction, drift-immune); keep the denylist as defense-in-depth belt

**Date:** 2026-07-20. **Source slice:** 18.38→18.40 — the §ARM-18 CHECKPOINT-1 GATE-1 go/no-go pivot (supersedes L71/L72's denylist premise).

The §ARM-18 subscription-shadow guard tried to DENYLIST every env var that could shadow the subscription (redirect egress / inject a raw key). Across repeated re-grounds (13→29→30→60→69→81) it kept under-covering — and the DECISIVE finding: `CLAUDE_ENV_FILE` is a `process.env`-scan BYPASS (claude-code loads a dotenv from that path and MERGES it into `query()`'s spawned-child env, so a key or `ANTHROPIC_BASE_URL` redirect reaches the run WITHOUT ever appearing in the `process.env` the denylist scans). A denylist CANNOT be complete for a rule-5 egress-veto invariant — which must be COMPLETE, not best-effort — because (a) the shadow namespace is unbounded + ambiguous (claude-code cred vars interleaved with bundled 3rd-party-SDK cred strings, drifting every version bump), AND (b) injection can arrive via mechanisms (a dotenv FILE, a settings FILE) the process.env scan structurally can't see. LESSON: for a rule-5 completeness invariant over a THIRD-PARTY SUBPROCESS, INVERT to a POSITIVE ALLOWLIST — spawn the subprocess with a MINIMAL allowlisted env (the Agent SDK `query()`'s `env` option REPLACES the child env ENTIRELY, not merged — sdk.d.ts:1391-1409 / SDK 0.3.201). The child sees ONLY `PATH`/`HOME` + the OS operational set — NO credential vars (the subscription login is AMBIENT via `~/.claude` + macOS Keychain, not env) — so NO shadow var (known / unknown / future-version / `CLAUDE_ENV_FILE`-injected) can reach it. COMPLETE-BY-CONSTRUCTION + drift-immune → it RETIRES the "re-verify the shadow set vs the bundled version at every bump" HARD precondition (L72). Keep the denylist as DEFENSE-IN-DEPTH belt (a loud pre-run degrade when the operator's process.env looks misconfigured) — belt + suspenders — but it is NOT the completeness guarantee. Corollaries: (a) under-allowlisting fails CLOSED (auth/spawn error, never a silent wrong-cred run) — safe; expand the allowlist empirically via the `$0` dry-run + the real run; (b) since `env` replaces entirely, confirm the SDK re-injects its OWN control/IPC vars (`CLAUDE_CODE_SDK_*` stdio handshake) on top, else the spawn breaks (a real-run validation gate, not a unit test); (c) the ORTHOGONAL settings-FILE surface (the managed tier SURVIVES `settingSources:[]`) is SEPARATE — a spawn-env allowlist does NOT cover a `settings.json` `apiKeyHelper`; that surface needs its own bounded completeness (the settings guard + the relocation degrade). A completeness invariant demands a positive allowlist, not a negative denylist; a security env-enumeration that under-covers 5× is the signal to invert, not fold once more. `pin: subscription-child-env-allowlist.test.ts (allowlist_contains_only_operational_keys + allowlist_excludes_unknown_future_shadow + providers_forwards_childenv_to_options_env) — 18.40; belt at subscription-auth-guard.test.ts`.

## <a id="74"></a>74. A settings-FILE managed surface is made complete-by-construction by a PRESENCE-degrade (any present managed settings ⇒ degrade) — the settings-file analog of the env-scrub (L73); a managed-FIELD enumeration is the same unwinnable denylist

**Date:** 2026-07-24. **Source slice:** 18.39-B — the §ARM-18 CHECKPOINT-1 GATE-1 (b) leg (companion to L73).

The SDK `query()` also reads Claude-Code SETTINGS FILES (not just env), so a `settings.json`/managed-settings `apiKeyHelper`/`hooks`/`statusLine.command`/`headersHelper` (a cred-minting or auth-header-injecting field) is a 2nd shadow surface (L71). Enumerating the DANGEROUS managed FIELDS is the SAME unwinnable denylist as the env set (L72/L73): apiKeyHelper → +hooks/statusLine → +headersHelper, and every SDK version adds one — a silent rule-5 fail-open each bump (`headersHelper` mints dynamic auth headers on the MAIN request path, NOT env-gated, so the L73 env-scrub doesn't cover it). LESSON: close the settings-file surface COMPLETE-BY-CONSTRUCTION, the settings analog of the env allowlist inversion — TWO mechanisms: (a) **`settingSources:[]`** at the `query()` call DISABLES the user/project tiers entirely (their env/apiKeyHelper/hooks); the MANAGED/enterprise tier SURVIVES it. (b) **PRESENCE-degrade**: on the armed path, ANY present + non-empty MANAGED settings source (`managed-settings.json` + its `managed-settings.d/*.json` fragments + the device & per-user `/Library/Managed Preferences` plists + the `MANAGED_SETTINGS_PATH`/`REMOTE_SETTINGS_PATH` relocation vars) ⇒ fail-safe DEGRADE — stop enumerating which managed FIELDS are dangerous. This SUBSUMES headersHelper + every known + future managed field, is drift-immune, and RETIRES the per-version managed-field re-verify. The surface is BOUNDED (a managed source either exists or it doesn't — unlike the unbounded env namespace), so presence-degrade actually CLOSES it (not an unwinnable chase). Corollaries: (a) empty `{}` managed settings ⇒ clean (a personal dev Mac with no MDM has zero managed sources ⇒ byte-equiv; an enterprise-MDM Mac degrades — the SAFE direction for employer contexts; gate-4 op-prereq verifies "no managed settings on the armed host"); (b) `hooks`/`statusLine`/`headersHelper` are watched MANAGED-tier-ONLY — a user/project `hooks` is a common legit field disabled by `settingSources:[]`, so watching it tier-agnostically permanently false-degrades a legit config (the L65/NO_PROXY class); (c) the managed-tier-only soundness is a CROSS-FILE INVARIANT (the `query()` `settingSources:[]` at the call site ⟷ the guard's tier scope) — pin it with a source-anchored RED-on-weaken test (decoy-proof: a docstring copy can keep a naive presence-check green). Extends/companions L71/L73. `pin: subscription-settings-guard.test.ts (managed presence-degrade + managed/user hooks tier-aware + settingSources:[] coupling RED-on-weaken) — 18.39`.
