# .gitleaksignore — seeded by scaffold-generate (the project accretes real entries over time).
#
# TDD fixtures are false-positive-heavy by nature (fake keys in test data, recorded HTTP cassettes,
# example configs). When secrets-guard blocks a commit on a FIXTURE value:
#   1. Confirm it is genuinely fake/test-only (a real secret gets rotated, never ignored).
#   2. Take the `Fingerprint:` line from the gitleaks finding output.
#   3. Append it below, one fingerprint per line, with a trailing comment saying what it is.
#   4. Re-run the commit — the hook re-scans and passes.
# Never ignore by rule-id or path glob — fingerprints pin one specific finding, which is the point.
#
# Example (delete once real entries exist):
# 0123456789abcdef0123456789abcdef01234567:tests/fixtures/stripe-cassette.json:generic-api-key:12  # recorded test cassette, fake key

# ── Phase 4/5 TDD fixtures (fake keys that EXERCISE the scanners/redaction) ──
# fake sk- key testing the KnowledgeWriter blocking secret scan (task 4.3)
packages/knowledge/test/secret-scan.test.ts:generic-api-key:26
# fake provider key in the Claude ModelProviderPort adapter test (task 5.7)
packages/providers/test/claude-provider.test.ts:generic-api-key:180
# fake GitHub PAT testing provider-boundary log redaction (task 5.6)
packages/providers/test/provider-log-redaction.test.ts:github-pat:27

# ── Phase 6 §8 TDD fixtures (fake keys that EXERCISE the gateway redaction / no-leak assertions) ──
# fake sk-live key testing that the Connector Gateway never leaks a secret to the safe log (task 6.1)
packages/integrations/test/connector-gateway.test.ts:generic-api-key:249
# fake Google AIza key testing the broadened gateway-log redaction (Phase-6 finding-2 fix)
packages/integrations/test/gateway-redaction-credentials.test.ts:generic-api-key:13

# ── task 24.64 (knowledge leg) TDD fixture ──
# fake sk- planId testing the KnowledgeWriter commit AuditRecord sanitiser (writer.ts:603)
packages/knowledge/test/writer-audit-gate.test.ts:generic-api-key:101
# fake sk- deletionId testing the tombstone commit AuditRecord sanitiser (FIX-3, tombstone.ts:306)
packages/knowledge/test/tombstone.test.ts:generic-api-key:384

# ── DOD-worker-boot task 1 TDD fixtures (fake credential-shaped workspaceIds EXERCISING the new
#    auditFieldContainsSecret gate on createAuditPersistPort's workspaceId channel, boot.ts:797) ──
apps/worker/test/boot/auditPersistWorkspaceIdGate.test.ts:generic-api-key:85
apps/worker/test/boot/auditPersistWorkspaceIdGate.test.ts:generic-api-key:124

# ── task 24.130 deposit 2 TDD fixture (fake AKIA-shaped credential glued into an
#    otherwise id-legal string, proving looksLikeCredentialShape still refuses a
#    genuine credential shape even when it would pass the id charset) ──
packages/domain/test/redaction/identifier-benign-keyword.test.ts:generic-api-key:144

# ── task 24.123 measurement-leg synthetic corpus (fake credential shapes in the
#    POSITIVE_CONTROL_NOTES bucket, one per CREDENTIAL_PREFIX alternative family —
#    proving the false-positive measurement's benign bucket is 0/63 because the
#    notes are genuinely benign, not because the predicate is inert) ──
packages/domain/test/redaction/synthetic-note-corpus-false-positive-rate.test.ts:jwt:159
packages/domain/test/redaction/synthetic-note-corpus-false-positive-rate.test.ts:private-key:162
