{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "actionRef": {
      "type": "string",
      "minLength": 1
    },
    "planRef": {
      "type": "string",
      "minLength": 1
    },
    "subjectKind": {
      "type": "string",
      "enum": [
        "external_action",
        "semantic_mutation"
      ]
    },
    "workspaceId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "approved",
        "edited",
        "rejected",
        "deferred",
        "expired"
      ]
    },
    "actor": {
      "type": "string",
      "minLength": 1
    },
    "channel": {
      "type": "string",
      "enum": [
        "mac",
        "telegram"
      ]
    },
    "payloadHash": {
      "type": "string",
      "minLength": 1
    },
    "snoozeUntil": {
      "type": "string",
      "format": "date-time"
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "subjectKind",
    "workspaceId",
    "status",
    "actor",
    "channel",
    "payloadHash"
  ],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "sow:approval"
}
