// @sow/domain — pure rules, state machines, validators.
// Imports nothing app- or adapter-side (§2.5 import-direction rule).
// Populated from Phase 1 task 1.2 onward (schema gate, validators, state machines).
export {};
