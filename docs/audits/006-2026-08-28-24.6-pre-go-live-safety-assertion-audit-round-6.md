# Task 24.6 — pre-go-live safety-assertion audit, ROUND 6

**Date:** 2026-08-28 (autonomous run) · **Predecessors:** `001` (round 1, 4/6 partitions) ·
`002` (round 2, AC-3 + DOC-1 + AC-2b) · `003` (round 3, the `gcl/*` partition) · `004`
(round 4, retroactive) · `005` (round 5 — explicitly *not* a sweep)

**Status:** ✅ **The surface every prior round left unreached is now READ.**
**0 findings survived verification.** The deliverable of this round is the **coverage**, not
a finding count.

---

## Why this round existed

Round 2's own coverage ledger, and its partitions unprompted, named the gap:

> `packages/contracts/LESSONS.md` is DOC-2's thinnest area, self-reported: **~123 of 128
> one-liners never read even at index level.**

> AC-3 reported **full-read 19/273 (~7%)** alongside grep-concept-swept 273/273, and
> volunteered that this is not adequate as a first-pass sweep.

And round 1's governing defect: **"iterations to dry" measures SEARCH-KEY SATURATION, not
SURFACE COVERAGE** — partitions reported "dry" at ~16% of their surface.

⇒ The task's Done-when is explicit that *"a dry-iteration count over a fraction of the
surface"* does not satisfy it. This round reads the fraction that was missing.

---

## COVERAGE LEDGER — the deliverable

| partition | surface | read in full |
|---|---|---|
| contracts-A | `packages/contracts/LESSONS.md` entries 1–89 | **89 / 89** |
| contracts-B | entries ~89–137 | **48 / 48** |
| contracts-C | entries ~137–196 | **60 / 66** |
| contracts-D | entries ~191–256 | **65 / 65** |
| worker | `apps/worker/LESSONS.md` | **82 / 82** |
| desktop | `apps/desktop/LESSONS.md` | **23 / 23** |
| providers + knowledge + evals | all three ledgers | **13 / 13** |
| sections.ts | `packages/knowledge/src/markdown-vault/sections.ts` + its tests | **778 / 778 lines** |
| **TOTAL** | | ⭐ **1158 / 1164 — 99.5%** |

⭐ **Against ~7% and ~4% in round 2.** `sections.ts` — named in round 2's ledger as *"the
shared region-marker parser under three separate rule-1/rule-4 enforcement points, unread"* —
is now read in full along with its tests.

⚠ **The 6 unread units** are contracts-C's boundary estimate (the partition reported its
assignment's estimate of 66 rather than its after-the-fact anchor count of 60, and said so).
Stated rather than rounded away.

---

## Findings: 12 candidates → **0 confirmed**

Every candidate was put to an independent verifier instructed to default to refuted. All 12
were knocked down — most commonly because the entry **already named its own scope, governing
state, or expiry condition** ("re-derive when X", "measured YYYY-MM-DD"), which is
derived-with-a-stated-boundary and not the defect.

⛔ **This is NOT a clean bill of health for the codebase.** It is a clean bill for *these
ledgers*, on *this question*. The same session found four real code defects and nine stale
claims by hand, and rounds 2–4 found live issues in code these ledgers only describe.

⭐ **What a null result here does mean:** the ledgers are heavily self-qualifying. The
project's documentation culture — dating claims, naming re-derivation triggers, striking and
retaining errors — is doing real work. Round 4's headline stands: *the project reliably
catches this defect class; what it fails at is PROPAGATING the catch.*

---

## ⚠ Instrument anomaly observed mid-round — recorded, not diagnosed

The `contracts-B` partition reported, unprompted:

> `rg` was silently substituting matched multi-char identifiers with a bare `n` in some
> context lines (confirmed via `od` / plain `grep` that the real files were untouched) — every
> finding was re-verified with plain `grep -n` / `sed -n` / Read after that was caught.

⇒ consistent with `docs/findings/instrument-anomalies-rtk.md`: a proxy layer rewriting
command output above the shell. **The partition caught it, verified the files were fine, and
re-ran its evidence** — which is the correct handling and the reason its results are usable.

⛔ No mechanism is proposed here. Recorded as a datum for the toolchain family
(`### 24.122` / `### 24.131` / `### 24.87`).

---

## What this round does NOT close

- `denied`-class ACL behaviour, real-key provisioning, and anything needing the owner's actual
  secrets — see `§ARM-17` (whose classifier questions were separately discharged this session).
- The severity-grading caveats in `#### Residuals (24.6)` stand unchanged: gradings resting on
  reachability qualifiers, circular evidence, or instrument-derived counts still need
  re-derivation before they are relied on.
