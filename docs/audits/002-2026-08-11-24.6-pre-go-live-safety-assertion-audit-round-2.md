# Audit 002 — Task 24.6, pre-go-live safety-assertion audit, **ROUND 2**

**Date:** 2026-08-11 · **Dispatched by:** `main-orchestrator` (task #2) · **Predecessor:** `001-2026-07-31-24.6-pre-go-live-safety-assertion-audit.md`
**Status:** ⏳ **IN PROGRESS — written incrementally as partitions land, deliberately** (contracts **L51**: an intention held only in context dies with the session — the same discipline audit 001 used). **2 of 3 partitions returned.**

> ⛔ **READ 001 FIRST.** This round does not restate 001's mandate, its threat-model ruling, or its five binding constraints — it inherits them verbatim.

## Why round 2 exists, and what it is scoped to

Run 1 completed **4 of 6** partitions. **`AC-3` and `DOC-1` both died producing ZERO output** on an org monthly spend limit. ⛔ **Those were the two that mattered most for the task's own stated purpose:** `DOC-1` **is** binding constraint 3 (whose text says *a code-only audit would have CLEARED the archetype defect*), and `AC-3` was the archetype's own surface. ⇒ **run 1 was precisely the code-only audit 24.6 was written to prevent.**

**Round-2 partitions:**

| ID | Mechanism | Surface | Status |
|---|---|---|---|
| **AC-3** | asserted-constant | `apps/desktop` · `packages/contracts` · `packages/domain` | ✅ returned — **0 findings**, strong explicit negatives |
| **DOC-1** | both | docs + design authority | ✅ returned — **2 findings**, incl. the archetype's second locus |
| **AC-2b** | asserted-constant | ⭐ **the AC-2 UNREACHED set** — `knowledge/src/synthesis/*`, most of `integrations/src`, `providers/{model,broker}`, `policy/denials.ts` | ⏳ running |

⛔ **`AC-1` and `FO-1` were NOT re-run** — they succeeded in run 1 and produced tasks 24.7 / 24.8 / 24.9. The report's own status table is authoritative.

⭐ **`AC-2b` IS NOT A RE-RUN OF `AC-2` — it is AC-2's COMPLEMENT, and the orchestrator added it as a scope call (flagged to the lead, vetoable).** Round 2 as literally scoped (`AC-3` + `DOC-1`) touches **none** of the coverage ledger's unreached set, and that set — all of `packages/knowledge/src/synthesis/*` and almost all of `packages/integrations/src` — **is exactly what arming touches.** ⇒ **round 2 could have completed with the arming block still standing on the same evidence gap.** 24.6's own text says *"the report's coverage ledger IS round 2's scope."*

---

## ⭐ THE METHOD FIX WORKED — recorded first, because it governs how this report may be read

Round 1's governing defect was that **"iterations to dry" measures SEARCH-KEY SATURATION, not SURFACE COVERAGE** — two partitions honestly reported dry at **~16%** of their surface, and six such reports would have read as a swept repo. Round 2's prompts required **both** the iteration count **and** a coverage ledger (`files read / files in surface`), plus **out-of-scope-observed as a first-class output**.

**Result, and it is the load-bearing outcome of this round so far:**
- **AC-3** reported **full-read 19/273 (~7%)** alongside **grep-concept-swept 273/273**, and volunteered — unprompted — that this is adequate for *"a second-round re-verification audit of an already-heavily-audited surface"* and **"not adequate as a first-pass sweep."** It then listed all **256** unread files verbatim.
- **DOC-1** reported **21 of ~527** files reached, **stated its prioritisation as part of the ledger** (locked design authority 100% · all 27 phase-level acceptance bullets 100% · 483 historical briefs/sessions/handoffs 0%), and named its unreached set verbatim.

⛔ **Under round 1's metric, both passes would have reported "3 iterations to dry" and read as swept surfaces.** ⇒ **the instrument, not the auditors, was the round-1 defect** — both round-1 partitions had been honest; they were answering the question they were asked. ⭐ **Both round-2 partitions separated *concept-swept* from *read*, which is the distinction that makes a coverage number mean anything.**

**The second fix also paid.** Out-of-scope-observed was required as a first-class section, and **AC-3 used it to escalate a live rule-5 question across its own boundary** (below) — the same behaviour that produced run 1's F1.

---

## Findings

### F7 · The locked design authority still lists the deleted egress pill as a first-class always-visible governance signal
**Source:** DOC-1 · **Severity:** medium-high · **Safety rule 5** · ⭐ **→ task `### 24.10`**

`docs/design/ui-ux/material-direction.md:92`. The normative governance paragraph **opens** with *"⛔ AMENDED 2026-07-26 — an always-on governance SIGNAL must still be DERIVED and stated at the scope it applies to; the egress pill violated both… and was removed"* — **and then, in the same sentence, enumerates the signals that "stay first-class and visible" INCLUDING "the egress pill (toolbar)."**

⭐ **A PARTIAL EDIT THAT READS AS COMPLETE: the guardrail's preamble was rewritten and the list inside it was not.** Lines 58 and 84 of the same file already strike the pill (`~~…~~` + *"REMOVED 2026-07-26"*), so **the file contradicts itself three ways and the stale copy sits in the normative paragraph** — contracts **L94**, in the surface where it costs most.

⛔ **Why this is not a typo: `docs/design/**` is AUTHORITY** — consumed by screen-generation prompts and by implementers as the specification. ⇒ **a stale safety claim here is a defect FACTORY, not a stale sentence:** it re-emits the defect on every regeneration. ⭐ **This is exactly the locus constraint 3 was written for, and it earned its keep on its first real execution: run 1 was code-only and cleared it; DOC-1 found it.**

⚠ **Not reproduced in code today** — `AppShell.tsx:342-345` asserts the chrome makes no egress claim, regression-pinned by `chrome_makes_no_egress_claim`; `Copilot.tsx`'s docblock cites line 57 (already correctly struck), not 92. **Under the threat-model ruling that is the wrong reason to close it** — the risk is the next regeneration and the next implementer.

⛔ **AND THE PRIOR SWEEP RECORDED THIS FILE AS DONE.** `#### Residuals (9)` claims *"material-direction.md ×3"* corrected, counting *"its normative governance-legibility guardrail amended."* **It was amended and not corrected.** ⇒ **a completion record standing over an incomplete fix, inside the remediation ledger of the very defect it remediates.**
⇒ **ACTION: task `### 24.10`.** ⚠ **Owner/lead-gated — `docs/design/**` is locked authority**; the edit itself reconciles the doc with an owner-ratified removal rather than making a design decision. Overlaps `### 9.42`'s sibling obligation on the same file.

### F8 · Phase 3's acceptance bullet asserts the §5 denials "non-bypassable" and was never reconciled against the bypass found four weeks later
**Source:** DOC-1 · **Severity:** medium · **Safety rule 5** · ⭐ **→ task `### 24.11`**

`IMPLEMENTATION_PLAN.md:604-605` (Acceptance criteria (3), `bc18914b`, 2026-07-01, *"closes /phase-exit 3"*): *"The four §5 hard denials are enforced and non-bypassable."*

Task **`### 9.33`** (`ceb42987`, **2026-07-28** — nearly a month later) fixed a real fail-open in the identity layer **beneath that very veto**: `processorOfRoute` was **not total**, so a blank identity **THREW instead of denying**, handing the caller a crash whose error handling decided whether raw employer content leaked. The same slice closed a second bypass where an untyped triple-read could resolve a cloud provider as non-egress and **pass the veto with raw employer content** (`ARCHITECTURE.md:205`: *"a bypass of rule 5's central check"*).

⛔ **`#### Residuals (3)` tracks three other follow-ups and not this one; 9.33 does not point back.** ⇒ **the correction lives only at the point of the FIX, never at the point a reader CHECKS.** A reader auditing *"is the egress veto sound?"* who stops at the phase gate — the reasonable place to stop — gets an unqualified *"non-bypassable."*

⭐ **Severity honestly: the claim is probably TRUE today. That is exactly what makes it 24.6's subject matter rather than a bug** — *a claim true today that cannot move when the governing state moves is a decoration that reads as a guarantee.* **This one did not move when its own state moved.** Same class as **24.8** (Phase-6's OBS-2 line asserting a signal with no wiring).
⇒ **ACTION: task `### 24.11`.** ⛔ **Its Done-when requires sweeping for OTHER falsified-and-unreconciled acceptance bullets** — two instances is a pattern, and fixing only the bullet DOC-1 reached would be fix-where-noticed wearing a completion badge.

---

## Explicit negatives — checked and SOUND (evidence, not filler)

⭐ **Required of every partition deliberately: without it, a thin report and a clean surface are indistinguishable.**

**From AC-3 (`apps/desktop` · `packages/contracts` · `packages/domain`) — 0 findings, and it re-derived the archetype's removal independently rather than citing the banked lessons as proof:**
- **Hardcoded posture strings in the renderer — ABSENT.** AC-3 re-ran the project's own forbidden-pattern #7 regex itself: **zero hits.**
- **`renderer/lib/egress-status.ts` + `surfaces/workspace-settings/egress.tsx`** — the one place egress posture renders. Both fields read per-workspace from `systemHealth.egressStatus`, reconstructed from a **3-field allowlist** (rejects a widened payload — rule 7), **verify the returned `workspaceId` equals the requested one** (WS-8-adjacent), and fold any typed-err/throw/malformed-ok to `{ok:false}` → *"posture unavailable"* — **never the permissive value.** ⭐ The one hardcoded string there describes what the predicate **measures**, not its **value**; the value-driven pair renders alongside it.
- **Arming flags** (`main/worker-arming-env.ts`, `index.ts:114-137`, `worker-host/temporal-supervisor.ts:104`) — every flag is strict `=== "1" || === "true"` opt-in, **never truthy-coerce**; default-absent ⇒ `{}` ⇒ **byte-equivalent to unarmed.**
- **No cached posture field exists in renderer store state** that could hold a stale default between hydrations (measured, closing AC-3's own initial candidate).
- **Test suites carry no L69-class tested-false-assurance** — the desktop pins assert *derivation properties* (rejects a status for a different workspace; folds malformed-ok to `{ok:false}`), not static permissive values.
- **`packages/domain` redaction** is a positive allowlist classified by **frozen enum membership imported from contracts**, not regex-approximated; `state/approval.ts` carries a compile-time drift guard pinning it both ways against the frozen contract enum.

**From DOC-1 (docs + design authority):**
- **`design-system.md`** — §3's *"Safety-posture copy is DERIVED copy, never decoration"* **and** §5's paste-ready generation prompt both explicitly forbid the chrome-level egress badge **and state why** (a chrome element cannot know a per-workspace posture). ⭐ **This is the operative generation-prompt fix from the prior sweep, and it is intact** — which is what makes F7's survival in a *sibling* file the interesting result rather than a total miss.
- **`ui-ux-spec.md`** (8 pill mentions) and **`DESIGN-DISCUSSION-HANDOFF.md`** (3) — every one carries a `⛔ SCOPED`/`⛔ CORRECTED`/`⛔ NO egress pill` annotation, consistently.
- **All four runbooks read** use explicit per-step status tags (WORKS-NOW / WIRED-BUT-INERT / TEST-GATED / BUILT-BUT-INERT / UNBUILT / OWNER-GATED) and each closes with a **fail-closed summary naming what happens on every precondition omission** — ⭐ **this is the fail-open-by-omission mechanism done CORRECTLY: the omission case is stated, not left implicit.**
- **Phases 0/1/2/5/6/8 acceptance bullets** — read in full; each either carries explicit residual notes or is narrowly scoped to what was actually verified. **21 of the 27 phase-level bullets are self-reported OPEN/PARTIAL/OWNER-GATED with the unmet criteria named inline** — honest by construction.

---

## ⭐ OUT-OF-SCOPE OBSERVED — the cross-boundary escalations (first-class, per the round-2 prompt fix)

1. ⛔ **RULE 5, AND IT IS THE F1 SHAPE AGAIN — raised by AC-3, which correctly refused to either assert or drop it.** `renderer/surfaces/copilot/Copilot.tsx:156-167` renders the cloud-egress consent notice **only when** `reply.egressProcessor !== undefined` — **the fail-open-by-omission archetype.** AC-3 verified the **renderer half is sound** (the field is `.optional()` not `.catch()`-stripped; a failed ask renders explicit `ASK_FAILED`, never a bare answer; the branded `AdmittedCopilotAnswer` type makes a hand-built partial a compile error). ⛔ **But whether the ABSENCE is TRUTHFUL rests on four legs in `packages/providers` / `apps/worker` that contracts L66 names — recorded 2026-07-26 and never re-verified, and a great deal has landed since.** ⭐ **L66's own words: *"a consumer-side pin asserting 'absent ⇒ silent' encodes a conclusion whose premises it cannot see."*** ⇒ **routed into AC-2b's surface.**
2. **Onboarding does not disclose the employer-egress default-seed at the point of choice** — a user picks `employer_work` with no notice that the workspace is provisioned with cloud egress **pre-acknowledged** (owner-ratified default-seed, `bcde3d61`). **Not a coded false assertion — an absence of disclosure at a decision point.** AC-3 flagged it as a *different finding class* rather than forcing it into the audited mechanism. **Disposition owed.**
3. **`docs/HANDOFF.md`** is cited as *"current build state"* by an in-scope design doc but sits outside `docs/design/**`; **not read.** Its claims are unverified.
4. **`docs/audits/` (43 files) is itself an unswept surface.** DOC-1's reasoning is worth keeping: a prior audit finding later fixed but never cross-linked back into the audit file **reads as still-open to a future reader** — *the identical failure shape one level removed.* **This report is now one of those 43;** its own disposition section must be closed when round 2 completes.

---

## COVERAGE LEDGER — ⛔ the unreached set (round 3's scope if one is needed)

**AC-3 did not full-read 256 of 273 files** — verbatim list in its report; the highest-value named gaps are `renderer/lib/live.ts` (the stream→store wiring, *"exactly the kind of plumbing where a stale/default field could hide without matching any grep key"*), the `today`/`approvals`/`system-health`/`connectors`/`cross-workspace-links` surfaces, `packages/domain/src/validation/*` (REQ-F-017 no-inference enforcement), and **all 47 `packages/contracts/test/` + 19 `packages/domain/test/` files** — which constraint 5 puts explicitly in scope.

**DOC-1 did not reach ~506 of ~527 files.** Its own highest-priority named gaps, **both squarely in its declared surface**:
- ⭐ **All six `LESSONS.md` files** (contracts 2312 lines · worker 806 · desktop 325 · providers 144 · knowledge 72 · evals 35) — **entirely unread.** ⛔ **These are cited as authority throughout the tracker (*"contracts L56"*, *"worker L67"*), so a stale or wrongly-scoped lesson is a defect factory the same way `material-direction.md` is.**
- ⭐ **`ARCHITECTURE.md` §19 (Part II, lines 481-700) + Appendix A** — concept-grepped only. **§19 is the activation/go-live roadmap behind the seven owner-gated crossings** ⇒ under the threat-model ruling this is *the* place an assumes-the-gates-are-open re-read matters most. **DOC-1 recommends a future pass start here.**
- `IMPLEMENTATION_PLAN.md` Phases 10-26 task-level prose (~1573 lines) — concept-grepped only. ⚠ DOC-1 named its own method's limit honestly: a per-line grep in single-line-paragraph markdown **cannot see a claim hedged three sentences later.**
- `docs/runbooks/turn-on-and-smoke-test-runbook.md` (1697 lines, the largest runbook) — **not opened.** ⚠ This is task 24.5's own artifact, the one governing safe turn-on order.
- 483 historical files (`briefs`/`sessions`/`team-handoffs`/`planning`/`audits`/…) — deliberately deprioritised as historical record rather than authority.

## Disposition (to complete when AC-2b lands)
_Per 24.6's Done-when: every finding either fixed or recorded as a task; a one-pass string-shaped sweep does not satisfy this, **and neither does a dry-iteration count over a fraction of the surface.**_

- **F7 → `### 24.10`** (recorded `0e3dbc8f`) · **F8 → `### 24.11`** (recorded `0e3dbc8f`).
- ⛔ **The arming block is NOT lifted by this report.** AC-2b — the partition covering the surfaces arming actually touches — has not returned, and the unreached set above remains large in both returned partitions.
