# 005 — 24.6 audit-adjacent findings, 2026-08-12 (round 5, NOT a dispatched sweep)

**Date:** 2026-08-12 · **Author:** `main-orchestrator` (close-out) · **Task:** `### 24.6`
**Predecessors:** `001` (round 1) · `002` (round 2) · `003` (round 3, the `gcl/*` partition) · `004` (round 4, folded in retroactively)

---

## ⛔ WHAT THIS REPORT IS, AND WHAT IT IS NOT

⛔ **No audit partition was dispatched on 2026-08-12. This is NOT a round-5 sweep and must not be counted as coverage.**

It exists because the round produced **findings in the audit's own subject matter, discovered through ordinary slice work rather than through a partition** — and round 3's own coverage ledger names `packages/knowledge/src/gcl/**` as the surface it had audited. **That surface moved materially this round.** Leaving those findings only in commit messages and a Log entry would lose exactly the information a round-5 partition would be dispatched to collect.

⭐ **Recorded per contracts `L126`: a citation is evidence about the PAST; coverage is a claim about the PRESENT.** Nothing below upgrades any surface's audited status. It records **what changed in a surface that was audited**, so a future round starts from the real state rather than from `003`'s snapshot.

---

## 1 — `packages/knowledge/src/gcl/**` — four `L134` instances closed, and the file swept

**Round 3 audited this surface and returned a HIGH finding.** Since then, five slices have landed in it:

| Task | Commit | What |
|---|---|---|
| `24.18` | `91a68725` | GCL projection visibility DERIVED from projection type (round-3 finding `F2`) |
| `24.19` | `4cef394f` | Map/Set/non-enumerable/Symbol-key raw-content bypasses closed (`F3`) |
| `24.30` | `4730211b` | `gateReason` exhaustive switch, no `default:` absorb |
| `24.36` | `f5cac8a8` | `admitProjection`'s `DenialReason` narrowing made exhaustive |
| `24.38` | `5fc64421` | `guardCrossWorkspaceRawRead`'s narrowing made exhaustive |

⭐⭐ **THE FINDING THE CHAIN ITSELF CONSTITUTES — `24.23` → `24.30` → `24.36` → `24.38`: four instances of one shape, EACH surfaced by the PREVIOUS fix's own Step-8 review, NONE by a gate.** ⚠ **Three of the four wore an `if`/`if`/`return` costume rather than a literal `switch`/`default:`** — so a sweep keyed on `default:` finds one of four. ⇒ **`### 24.34`'s concept-level sweep must enumerate *un-guarded narrowing of a closed union*, not the `default:` token** (contracts `L64`: enumerate the CONCEPT, never the known string forms).

⭐ **The file is now swept, and the sweep is checkable rather than asserted.** `24.38`'s implementer enumerated **all 4 narrowing sites in `visibility-gate.ts` WITH classifications** — the two fixed, plus **two explicitly classified as NON-instances** (`registry === undefined`, a plain optional-param check; `issues.some(i => i.path === "sanitizedPayload")`, a boolean predicate). **No fifth instance in that file.** ⛔ **A bare "swept, none found" would have been worth far less** — the classifications are what make the negative load-bearing.

⚠ **Scope of this negative, stated so it can be refuted (`L100`): ONE FILE.** `visibility-gate.ts` only. **Sibling files in `gcl/**` were not re-swept for this shape**, and `24.34` remains the vehicle for that.

---

## 2 — A reachability-verification failure the audit's constraint set does not cover

⛔ **THREE WRONG REACHABILITY CALLS ON ONE MODULE IN ONE SESSION** — orchestrator's `apps/`-only grep (`L140`), orchestrator's "LIVE via the chain," lead's inner-hop-verified claim. **Ground truth: every path into `admitProjection` is dormant** — `resolveApprovedCrossWorkspaceSlice` and `reconcileGlobalMarkdown` both have zero production callers; the three commonly-cited sites are **comments**.

⛔ **THIS REACHED THE RELEASE CRITERIA.** Arming-block condition (d) was **extended to cover `24.33` on the strength of the false claim**, then reverted when `knowledge-implementer` falsified it from source at Step 0. **A slice was briefed and dispatched against a dead premise.**

⭐ **Why it matters to THIS audit specifically: `24.6`'s findings are repeatedly qualified by reachability** (*"dormant, therefore not a live breach"* appears across `001`–`004`, and `24.9`/`24.23`/`24.36`/`24.38` all carry that qualifier). ⇒ **the audit's severity grading rests on a class of claim this project has now demonstrably got wrong three times in one session, by three different methods, all by hand.**

⇒ **RECOMMENDATION for any round 5+: a reachability qualifier on an audit finding must cite `24.29`'s repo-wide mechanical census, not a hand grep.** Banked as contracts **`L141`**. ⚠ **The failure is BIASED toward "reachable"** — a found call site is pointable evidence; finding nothing feels like an incomplete search — **so hand-verification errs in the direction that under-states dormancy and over-states exposure**, which is the direction that adds release conditions.

---

## 3 — ⛔ A DEFECT CLASS THE AUDIT'S CONSTRAINTS DO NOT REACH: the test substrate bypasses the deployment mechanism

**`### 24.39`** — task `13.15`'s `task` table has **never had a migration generated, in either dialect**, despite being ticked `[x]` DONE at `54b052a7`. Verified: 0 hits for `CREATE TABLE "task"` across `packages/db/migrations/`; the only `task` string in `pg/0000_genesis.sql` is `"taskQueue"` at `:146`, a lease-table column. Schema exists at `src/schema/task.ts`, barrel-exported. **Sibling `14.6`'s `projectRegistry` DID get `0007`** — one missed step, not a general lag.

⭐⭐ **WHY THIS BELONGS IN A 24.6 REPORT RATHER THAN ONLY IN THE TRACKER: the repo-contract suite builds its test databases FROM THE DRIZZLE SCHEMA and never calls `applyMigrations()`.** ⇒ **every `TaskRepository` test passes against a table the deployment path cannot produce. The suite and the deployment disagree, and the suite is the one everybody reads.**

⛔ **`24.6`'s constraint set audits ASSERTIONS — claims derived from governing state. It has no constraint for "the artifact that verifies X is not the artifact that ships X."** This is `L89`/`L84`/`L85`'s family (a gate that runs but does not check; a suite that never reaches the code; a fake mirroring a real guard) **in a medium the audit never looked at: the boundary between the test substrate and the deployment artifact.**

⇒ **"Certified DONE" for a schema-touching task carries less than it appears to.** ⚠ **Open and consequential: whether any existing dev/prod database has been running without that table is UNKNOWN and must be established, not assumed** (`24.39` Done-when leg 4).

⭐ **`24.39`'s Done-when was widened by the lead from *fix the migration* to *install a detector*** — a test must exercise the `applyMigrations()` path so a schema table with no migration FAILS somewhere. **Without that, the concept-level sweep is a one-time snapshot and instance three arrives the way instance one did.**

---

## 4 — Coverage ledger delta (the only part that changes a future round's scope)

| Surface | `003`/`004` state | State after this round |
|---|---|---|
| `packages/knowledge/src/gcl/**` | audited round 3; `F2`/`F3` open | `F2` closed `91a68725` · `F3` closed `4cef394f` · **`visibility-gate.ts` additionally swept for the `L134` shape, 4 sites classified, no fifth** · ⚠ **sibling files NOT re-swept for that shape** |
| Interactive-Copilot denial path | condition (d)'s subject | `24.7` `0c8de4b2`, lead-verified against the tree |
| `packages/db` migrations vs schema | ⛔ **never in scope** | ⛔ **NEW GAP FOUND — `24.39`; scope of the check performed: `task` table only. Other tables NOT enumerated.** |
| Reachability qualifiers across `001`–`004` | taken as given | ⚠ **method now known unreliable by hand (`L141`) — qualifiers not re-derived; a future round should re-derive them via census** |

⛔ **Nothing here closes `### 24.6`.** It remains `[ ] OPEN`. **No round-5 scope has been assigned**, and the honest reading is that `24.34` (concept sweep) plus a census-based re-derivation of existing reachability qualifiers would cover more than a fresh partition would.

---

## Provenance

**Written at close-out from committed evidence and the round's own Step-9 reports — no new investigation was performed to produce it**, matching how `004` was folded in. **Every hash cited was read from `git log`, not from prose** (contracts `L133`: `git commit`'s receipt is unreliable, including its file count).
