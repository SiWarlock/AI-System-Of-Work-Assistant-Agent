# Session 048 — create-vs-patch split, then the §13.10a Copilot→KMP bridge (Slices A+B)

- **Date:** 2026-07-07 · **Track:** solo (workflows · contracts · worker) · **HEAD:** `e47cfa1` → `d38a319` (4 commits; NOT pushed yet at time of writing).
- **Gate:** repo-wide `pnpm -w turbo run typecheck test` **31/31** throughout. Dual-reviewer (security + code-quality) on every safety-touching slice.

## Arc 1 — the create-vs-patch split (the last deterministic dashboard slice) — `9288bcd`

Finished the real-dashboard deterministic build. The P3e projection was always-`NoteCreate`, which is a LIVE re-sync clobber bug: the KnowledgeWriter's `projectPlan` (writer.ts:358) BLINDLY OVERWRITES the whole file for a `NoteCreate` at an existing path, so every re-sync would wipe human scaffold outside the region (or jam `write_conflict`). Fix: a WS-8-scoped `NoteExistsReader` port feeds the pure projection a `noteExists` boolean; the build activity computes the path via the SINGLE `projectNotePath` authority, probes, and routes a discriminated `ProjectNoteMutation {create|patch}` into `plan.creates[]`/`patches[]`. First sync → full `NoteCreate`; re-sync → region `NotePatch` (`newBody` = region inner, byte-idempotent). Fail-closed → `build_failed` on a probe failure OR no safe anchor. Hardened `projectNotePath` with a workspace-segment guard (rejects `/`,`\`,`..`) — closing the tracked WS-8 LOW. Dual-reviewer: security CLEAR (5 properties HELD, 0 crit/high/med), code-quality 0 high/med.

## Finding + fork — the composition-wiring premise was false

Code-check for task #31 ("wire the concrete trio, replacing the fakes"): **6/9 projectSync ports have NO concrete impl** (ResolveRegistry, CommitStatus, SynthesizeNarrative, ProjectSyncProposeActions, ProjectSyncHealthSink, RawProgressReader) and `runProjectSync` is composed **nowhere** in the worker. Making the Projects dashboard LIVE is a large real-integration + Temporal effort (a project registry [unbuilt], agent-runtime synthesis [stub-only], connector-backed progress reader, Tool-Gateway propose), not a small wiring step. Surfaced to the owner as a fork. **Owner picked §13.10a** (the Copilot→KMP semantic-write bridge) — the highest-leverage, non-blocked direction — over the Temporal path.

## Arc 2 — §13.10a Copilot→KnowledgeMutationPlan bridge (Slices A+B; planned + approved)

**The gap (verified):** the Copilot can propose an EXTERNAL write (`copilotPropose.ts` → `ProposedAction`) but there is NO Copilot→KMP path; `Approval.actionRef` references only a `ProposedAction`; `applyPlan` is called only by workflow-driver commit activities, never an approval path. `copilotPropose.ts` is the exact template — §13.10a is its semantic-write sibling. First target = the typed Project note (reuses the create-vs-patch machinery).

- **Slice A `dd2915b` — frozen round `ProvenanceOrigin += "copilot_propose"`.** Enum (7→8) + regenerated `knowledge-mutation-plan.schema.json` & `project.schema.json` (UPDATE_SNAP; `.snap` field-set unchanged) + `shared.test.ts` membership case + a KMP positive test + ARCHITECTURE.md Appendix A + contracts CLAUDE.md invariants row. Mirrors the §13.5 project_capture/project_sync add.
- **Slice B `d38a319` — the pure `deriveCopilotProjectKnowledgePlan` (the security heart).** `apps/worker/src/api/procedures/copilotProposeKnowledge.ts`: untrusted model project intent (projectId/title/lifecycleState/optional summary — NO path/keys/percent) → a validated, human-gated KMP. Strict shape-guard (rejects a smuggled workspaceId/path key); single-line + region-marker-free guards on title/projectId (no frontmatter/region injection, KN-7); bounded region-marker-free summary. Path via the shared `projectNotePath` WS-8 authority (a Copilot-proposed note and its synced note resolve to the SAME file). create-vs-patch (caller-supplied `noteExists`); NO percent (REQ-F-011); `provenanceOrigin:copilot_propose`; `requiresApproval:true` (structural gate); sub-1 confidence; `sourceRefs≥1` (REQ-F-006); re-validated through `KnowledgeMutationPlanSchema`. DERIVES ONLY — no side effect. 26 tests.
  - **Design-Workflow note:** the pre-implementation survey→design→verify Workflow (`wf_2ec49a30-926`) STALLED (the single design agent ran 53 min / 300k tokens then hung on all retries — a known fragile failure mode). Fell back to the proven create-vs-patch playbook: inline design (the seed decisions) → TDD → **dual-reviewers on the real diff** (the robust adversarial verification). Security CLEAR (WS-8/injection, candidate-gate, REQ-F-011/006, one-writer, never-throws — all PASS, 0 crit/high/med).
  - **Review mediums folded:** hoisted the shared `PROJECT_STATUS_REGION` + `composeProjectStatusNote` into `noteSlug.ts` (projectSync + the propose bridge now target the same region by construction — no copy-paste drift); keyed the `planId` on the note PATH so slug-colliding projectIds derive ONE idempotent card. **Two go-live residuals documented in-code:** (1) the routing/executor slice must verify the existing note's frontmatter `projectId` on a patch (slug-collision — needs note content, not fixable in the pure derivation); (2) the KnowledgeWriter frontmatter serializer must YAML-escape values before the propose path commits to a real vault (this slice is the first untrusted→frontmatter exposure).

## STOP point — the Approval-shape architectural decision (owner)

Per the approved plan, Slices A+B are the first deliverable; the remaining §13.10a slices (a pending-KMP store, the sink→§9.8 Approvals, the on-approval→KnowledgeWriter executor, the tool+catalog+runner wiring, the desktop semantic-mutation card) hinge on **how a pending KMP rides §9.8 Approvals** — `Approval` currently references only an external `ProposedAction` (`actionRef: ActionId`). This is a frozen-contract + desktop-touching decision surfaced to the owner before proceeding. Two options: the HONEST model (Approval gains a semantic subject — additive `planRef?`/`subjectKind`) vs the LIGHTER opaque-ref reuse (`actionRef` points into a pending-KMP store, no frozen Approval change).

## Docs reconciled
`IMPLEMENTATION_PLAN.md` §13.10a · memory `sow-dashboard-real-producers` + `sow-copilot-skill-catalog` · the handoff RESUME block · this session doc. The dashboard plan's deferred tail (R5/P4/composition = task #31; R3 disposition-column; go-live follow-ups) is carried in the plan file's Carry-forward section + memory.
