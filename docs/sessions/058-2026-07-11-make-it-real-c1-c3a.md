# Session 058 — make-it-real arc: C1 → C3a (live sourceIngestion on local Temporal)

- **Date:** 2026-07-11
- **Phase:** Phase 11 (make-it-real, local-I/O-first) reaching into §9 (Temporal Workflows) + §16 (Observability)
- **Team:** `session-f2673cd5` — implementer `impl7`, orchestrator `orch7`, lead `team-lead` (single-track on `main`)
- **Predecessor:** [057-2026-07-11-osb-governance-arc-owner-ab-steer.md](057-2026-07-11-osb-governance-arc-owner-ab-steer.md)
- **Successor:** _(next implementer — C3b file-watcher)_

## Why this session existed

Round-7 pivot = **MAKE IT REAL** (local-only real-I/O first). The §9 `sourceIngestion` driver (task 7.7) was built + unit-tested but never called on a real worker; the arc activates it end-to-end over local Temporal, feeds it a REAL local file, corrects the §16 failure-class labeling it surfaced, expands the frozen taxonomy that correction needed, and adds the client-side dispatch path a future file-watcher will call.

## What was built (5 slices, all on `main`, each RED-first + mandatory adversarial review)

| Slice | Commit | Summary |
|---|---|---|
| **C1** | `25f14d0` | Live `sourceIngestion` on local Temporal — registered as a sandbox workflow + wired its 7 activity delegates through the proof-spine assembly (real `registerSource()` gate + deterministic composition-root leaves, guardrail-3). |
| **C2** | `8a48833` | Real ROOT-confined `node:fs` file-read transport behind `extractFileSource` — realpath-containment before open, bounded-read cap, NUL-sniff, fail-closed §16; a real file flows end-to-end to `applied`. |
| **C-fix** | `77c717e` | §16 failure-class made CAUSE-aware — drained the C1 Finding (register-malformed no longer mislabeled `worker_down`); `worker_down` reserved for infra. |
| **C-enum** | `0fea74c` | Frozen-contract expansion: `FailureClass` +`security_violation`/`policy_denial`/`egress_denied`/`isolation_breach`; retired the C-fix `arch_gap` interims; per-class severity default (security/isolation→critical, policy/egress→error). |
| **C3a** | `7a471d3` | Degraded-safe `dispatchSourceIngestion` — starts a run via a Temporal Client (idempotencyKey=workflowId + REJECT_DUPLICATE dedupe), fail-closed §16 never-throw; `createTemporalClientStartRun` adapter tested via `env.client`. |

### Files created
- `apps/worker/test/integration/sourceIngestion-live.test.ts` (C1; grew through C2/C3a) — the SOW_TEMPORAL-gated live harness (7 gated cases + 1 fast-unit registration).
- `packages/integrations/src/connectors/adapters/file-read-transport.ts` + `packages/integrations/test/file-read-transport.test.ts` (C2).
- `apps/worker/src/temporal/dispatchSourceIngestion.ts` + `apps/worker/test/integration/sourceIngestion-dispatch.test.ts` (C3a).

### Files modified
- `apps/worker/src/composition/buildActivities.ts` + `apps/worker/src/temporal/workflows.ts` (C1) — `SourceIngestionParams` + 7 source delegates + the `sourceIngestionWorkflow` sandbox wrapper.
- `packages/workflows/src/workflows/sourceIngestion.ts` (C-fix + C-enum) — cause-aware `agentFailureClass`/`commitFailureClass`; `surface()` `failureClass?` override; rewired to the new members.
- `packages/contracts/src/models/shared-enums.ts` + `schemas/health-item.schema.json` + `packages/workflows/src/activities/healthItem.ts` (C-enum) — the enum + regenerated schema + `defaultSeverityForFailureClass`.
- Test pins: `packages/contracts/test/{models/health-item,primitives/shared}.test.ts`; `packages/workflows/test/{source-ingestion,health-item}.test.ts`; `packages/evals/test/osb/anti-corruption.test.ts` (C2 count-pin 16→17, authorized).

## Decisions made
- **C1:** only `registerSource()` runs for real; every other leaf is a deterministic composition-root fake (guardrail-3). No boot-dispatch (deferred).
- **C2:** realpath-containment before any read (not string-prefix); default 10 MiB read cap; transport NOT barrel-exported → `node:fs` stays off the sandbox path by construction. The new connector-adapter tripped the Lesson-12 anti-corruption count-pin → escalated at Step 7.5; count-pin 16→17 authorized after an independent CERTIFY (separation of duties).
- **C-fix:** register/agent-gate causes → `schema_rejection`; commit/write → `write_through_failed`; machine unchanged (no `captured→rejected` edge). Security causes used least-wrong + `arch_gap` interim → escalated to owner, who directed C-enum.
- **C-enum (frozen-contract):** 4 additive members; consumer ripple SURVEYED empirically → `FailureClass` is a pass-through discriminant (zero tsc-breaking / zero mis-bucketing switches; only a membership-pin *test* needed bumping); severity via one exhaustive tsc-guarded `defaultSeverityForFailureClass` at the materializer.
- **C3a:** dispatch pure over an injected `StartWorkflowRun` port + a `createTemporalClientStartRun` adapter tested via `env.client`; `REJECT_DUPLICATE` (Context7-verified 1.19) folds `WorkflowExecutionAlreadyStartedError` → idempotent no-op; degraded health = `worker_down` (infra).

## Decisions explicitly NOT made (deferred)
- **Boot Temporal Client composition + the file-watcher trigger** → C3b (wired behind their real caller — the reachability discipline; no dormant untested live-Client code in C3a).
- **`budget_exceeded` → `budget_breach`** precision (non-terminal, possibly COST-1-reserved) — deferred.
- **The Lesson-12 guard `symlink`-prose false-positive** (pattern needs a call-context anchor) — routed to the eval-security track.
- **`DispatchOutcome` as a discriminated union** (make illegal states unrepresentable) — deferred to C3b.

## TDD compliance
**Clean.** Every slice was RED-first (failing test confirmed before implementation); each real-I/O / safety-adjacent surface got mandatory dual adversarial review (security + code-quality) — all CLEAR (no crit/high/med across the arc). C-enum was full frozen-contract discipline (schema regenerated via `UPDATE_SNAP=1`, `.snap` field-names correctly untouched).

## Cross-doc invariant audit
- **`FailureClass` (HealthItem row, ARCHITECTURE.md §16 / Appendix A L441)** — +4 members this session (C-enum). Paired doc edit **EXISTS + committed** by the orchestrator (`97759a7` "docs(contracts): mirror the §16 FailureClass expansion…"; contracts `CLAUDE.md` HealthItem row updated). No drift.
- No other frozen-model field changes (C1/C2/C-fix/C3a reuse existing contracts; the `defaultSeverityForFailureClass` is behavior, not a field).

## Reachability
- **C1** `sourceIngestionWorkflow` — reachable from the Temporal worker registration (`proofSpineWorkflowsPath` bundle + `buildRegisteredActivities`); executed live by the gated test.
- **C2** transport — the capture leaf: `createFileReadTransport → extractFileSource → RegisterSourceInput → SourceIngestionContext.source → live workflow`; exercised by gated case (d).
- **C-fix / C-enum** — inside the already-live driver / the `surface()`→`materializeHealthItem` path; exercised by driver + severity tests.
- **C3a** `dispatchSourceIngestion` — the dispatch entry; production caller (file-watcher + boot Client) = **C3b** (a Future TODO, not a silent gap); in C3a fully exercised by the gated test via the same production adapter (`createTemporalClientStartRun(env.client)`).

## Open follow-ups
- **C3b (next slice):** the file-watcher + the boot Temporal Client composition (calls `dispatchSourceIngestion` with trigger `connector_event` — the CLOSED `WorkflowTrigger`, per the C3a Finding).
- `budget_exceeded`→`budget_breach` precision (deferred).
- Eval-security: the Lesson-12 guard `symlink`-prose-FP call-context-anchor fix; stale `arch_gap` "no dedicated member" comments in `packages/policy/{audit-signal,egress}.ts` + `packages/providers/src/broker/*` now contradicted by the C-enum members (doc-drift cleanup).
- `DispatchOutcome` discriminated-union cleanup (C3a code-quality low).
