# Session 089 — 2026-07-16 — Crash recovery + 15.7/G7 + 15.8/G60 + /phase-exit 15

**Team:** `session-734f946b` — lead-carried orchestrator (orch25 role) + worker-impl4 + desktop-impl (re-spawned; the tmux-backed orch24/worker-impl3 died with the machine). Single-track `main`.
**Span:** origin/main `80c59c44` → `1461c815` (pushed). Two build rounds + a phase gate.

## 1. Crash recovery (the entry point)
The dev machine died mid-slice. State reconstructed from git + task tracker + handoff 006.
- **Finding:** G5 park-write had committed cleanly PRE-crash (`65e6b09a`) — MEMORY's "in flight" was stale. The only at-risk work was an **uncommitted, test-less 15.7 edit** to `buildActivities.ts` (it compiled but had ZERO tests — a lost test-first step).
- **Recovery (owner-approved clean re-run):** backed the edit up to scratchpad, reverted it, re-ran 15.7 PROPERLY test-first (6 RED → mirror fix → GREEN → reviews → commit).

## 2. 15.7 — source-ingestion propose via the real Tool Gateway (closes G7) — `010d53e5`
`sourcePropose` now delegates to the same real `propose` port (`createProposeActivity` over `dispatchExternalWrite`) that backs `meetingPropose`, replacing the in-memory `sourceReceiptByKey` Map (`ext-source-N` fake receipts). An approval-required employer_work write FAILS CLOSED to `approval_pending` + lands a pending §9 Approval (rules 3/5/7); DORMANT (no real transport bound — Phase-21). Security review CLEAR + code-quality (orphaned `ProposeResult`/`ProposeError` imports removed; t3 hardened with a distinct-key case).
- **Files:** `apps/worker/src/composition/buildActivities.ts`, `apps/worker/test/proof-spine-composition.test.ts` (+6 tests: 1 meetingPropose control oracle + 5 source pins).
- **Round docs (`80c41516`):** `IMPLEMENTATION_PLAN.md` (15.7 tick + crash-recovery Log entry), `docs/briefs/104-15.7-source-ingestion-external-write-propose.md`.

## 3. 15.8 — human routing-resolution loop (closes G60; Phase-15 spine complete)
Two legs in parallel (disjoint files, one shared pinned command contract), synchronized at the Step-2.5 review gate.

**Worker leg (`bf33b669`):** the ingestion-triage disposition command gains an optional registry-validated `target {workspaceId, projectId?}` for `reroute`. REQ-F-017 no-inference (no/blank target ⇒ `reroute_target_required`, validator never consulted, zero dispatch); target validated against the REAL 14.1/14.6 registries (`createRegistryValidatedRerouteTarget`, mirrors `createRegistryValidatedRescope`/L36 — unknown ws/project ⇒ typed reject, WS-8 never a raw bind, registry fault folds closed); `reroute_target_forbidden` on a non-reroute; empty-string projectId normalized to workspace-only. Worker-API-layer shape (NOT a contracts model). security CLEAR (6 invariants); code-quality 1 medium fixed + 2 low.
- **Files:** `apps/worker/src/api/procedures/triageCommands.ts`, `.../procedures/commands.ts`, `.../adapters/commands.ts`, `.../composition/dispositionDurable.ts`, `.../api/server.ts`, `.../boot.ts`; tests `test/api/commands.test.ts`, `test/composition/dispositionDurable.test.ts`, `test/api/adapters/commands.test.ts`, `test/api/uiSafe.test.ts`, `test/integration/api-live.test.ts`; + mechanical `rerouteTargets` stubs in `packages/evals/src/worker-api-auth/{auth-suite.ts, exactly-once-suite.ts}` (forced by the new required CommandDeps field, orchestrator-authorized).

**Desktop leg (`cd1b7cb4`):** Ingestion Inbox reroute/assign-project action; registry-sourced workspace/project picker (no free-text; submit blocked until a workspace is chosen); WS-8 project sub-picker scope-gating. Review catch (promoted from LOW): the reroute idempotencyKey encodes the FULL target (`${sourceId}:reroute:${workspaceId}[:${projectId}]`) so a failed-then-resubmit reroute to a DIFFERENT workspace is a distinct operation, never a silent re-drive to the stale target. security INVARIANT PASS; code-quality 0 high.
- **Files:** `apps/desktop/renderer/surfaces/ingestion-inbox/index.tsx`, `renderer/lib/triage-disposition.ts`, `renderer/lib/reroute-picker.ts` (NEW), `renderer/lib/live.ts`, `renderer/App.tsx`; tests `test/renderer/triage-disposition.test.ts`, `test/renderer/reroute-picker.test.ts` (NEW), `test-dom/ingestion-inbox-page.test.tsx`.

**Round docs (`1461c815`):** `ARCHITECTURE.md` (§19.2 `target` mirror + landed note), `IMPLEMENTATION_PLAN.md` (15.8 tick + round Log entry), `docs/briefs/105-15.8-triage-reroute-worker-leg.md`, `docs/briefs/106-15.8-inbox-reroute-desktop-leg.md`.

## 4. Gates
- Repo-wide typecheck 20/20; full suite 20/20 tasks green (worker 1516, desktop 336, evals 492, db 426, …). lint 11/11. spec-lint tests(15) PASS.
- `/phase-exit 15`: arch-drift + reachability(worker) + reachability(desktop) auditors dispatched → verdict recorded in the Log + `docs/audits/15-*.md`.

## 5. Carry-forward (Future TODOs — all flagged in-plan)
1. **Phase-16:** the 15.8 re-entry runner must re-scope the parked envelope BY the forwarded reroute target (validated + forwarded now; consumption NOT end-to-end-live).
2. **Phase-21:** the 15.7 pending-Approval `workspaceId` derives from `params.meetingJobInputs.workspaceId` (correct-by-coincidence under single-workspace-per-worker + dormant leg; source from the source action's routed ws + WS-8 re-gate when arming).
3. **LIVE `/design-review`** owed on the desktop surfaces (Phase-14 debt + the new reroute control + an `aria-controls` a11y item) at the next app-up window.

## 6. Next
`/phase-exit 15` verdict → then Phase 16 (§19.3 connector engine, pure-build) → Phase 17 (§19.4 Keychain = FIRST hard-line/owner-gate).
