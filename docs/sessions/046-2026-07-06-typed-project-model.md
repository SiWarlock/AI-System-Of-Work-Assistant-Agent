# Session 046 — the real dashboard, Arc P: typed Project model (P1–P3b)

- **Date:** 2026-07-06 · **Track:** solo (contract + domain + workflows + worker) · **HEAD:** `b0bbd14` → `7ba1892` (+ docs).
- **Owner direction:** "the real dashboard" → the canonical heavy path for Projects: **build the typed Project model** (§13.5) — a real 7th state machine + a frozen `Project` seam, so status/lifecycle become honest. This session builds the **deterministic core (P1–P3b)**; the wiring tail (BuildSyncOutputsPort + activation) is design-/Temporal-gated + deferred. Plan: `~/.claude/plans/snazzy-honking-stearns.md`; memory `sow-dashboard-real-producers`.
- **Gate:** repo-wide `pnpm -w turbo run typecheck test` **31/31** throughout.

## Sub-forks resolved at Arc-P start (all invariant-driven defaults, reversible)
- **The 7th machine = the ENTITY lifecycle** (`idea→planning→active`); the existing `projectSyncMachine` is the distinct *run* lifecycle. (§13.5 is explicit.)
- **Canonical Project state = Markdown frontmatter only** — the one-writer/Markdown-canonical invariant. NO new operational `projects` table; the dashboard stays in the REBUILDABLE read-model store.
- **`UiSafeProjectDashboard.status` stays a free-string display token** — the typed lifecycle lives in the `Project` seam + frontmatter; tightening the UI projection to the enum is a 2nd frozen change + desktop coordination, deferred.

## What shipped (4 slices, TDD, per-slice commit)

- **P1 `517659c` — the typed `Project` seam [frozen-contract round, additive].** `packages/contracts/src/models/project.ts` (+ schema + `.snap` + test + barrel + auto-registered) — `{id: ProjectId, workspaceId, slug, title, lifecycleState, timeline[], provenanceOrigin}`. A SEMANTIC entity (frontmatter, KnowledgeWriter-owned; no operational table). `timeline` is bi-temporal + APPEND-ONLY (`ProjectStatusEntry {state, eventTime, transactionTime}` — status never overwritten). Refine: `lifecycleState ≡ timeline[last].state`. Progress NOT stored (deterministic checkbox parse, REQ-F-011). New `ProjectLifecycleState` enum (idea/planning/active/paused/done/archived), `ProjectId` brand, `ProvenanceOrigin += project_capture, project_sync` (regen'd the KMP schema). Same-round mirrors: `ARCHITECTURE.md` Appendix A + the contracts `CLAUDE.md` invariants row + the enum-membership test + the `validProject` fixture. Contracts 646.
- **P2 `1ddbf10` — the 7th state machine.** `packages/domain/src/state/project.ts` — a pure, total `defineMachine` reducer (mirrors approval): idea→planning→active, paused⇄active, planning→idea fallback, archived-from-any-live, done-only-from-active; done/archived frozen terminals (NO idempotent-reentry). Compile-time drift-guard `ProjectState ≡ ProjectLifecycleState`. Stateless — the lifecycle string is persisted by the Markdown frontmatter timeline head. 10 tests; domain 287. (Also completed the P1 fixture wiring the domain 1.15 meta-test requires: `validProject` + the FIXTURES registry entry + the `ZOD_BY_ID` map.)
- **P3a `63a8d0b` — the pure dashboard-payload builder.** `buildProjectDashboardPayload` (`packages/workflows/src/activities/projectDashboard.ts`): facts + validated prose → a servable `UiSafeProjectDashboard`. **REQ-F-011 by construction** (`percentComplete` RE-DERIVED via `computePercent` from the counts, never trusted from input — satisfies `sanitizeProjectDashboards`' re-check). **No-inference** (REQ-F-017): pre-categorized `ExtractionField[]` → prose arrays with a `TBD` (unstated) value SKIPPED (never guessed), each single-lined + capped 50; the field-key→category extraction from `validated.fields` (the unpinned §9/Phase-7 synthesis schema) is the caller's job, so the builder stays decoupled. Default doc pack = 5 slots unlinked/unknown. Fail-closed→null. 8 tests; workflows 463.
- **P3b `7ba1892` — the concrete `ProjectSyncUpdateDashboardPort`.** `createProjectDashboardUpdatePort` (`apps/worker/src/api/projections/projectDashboardUpdate.ts`): upserts one project's `UiSafeProjectDashboard` into `read_models[project_dashboards]` preserving siblings (mirrors `provisionDev.upsertProjectRow` — the interim it replaces on the real path). Interprets the opaque port payload as a `{workspaceId, dashboard}` envelope — workspaceId is the SERVER-BOUND write key (BuildSyncOutputsPort stamps it from the registry; WS-2/WS-4), dashboard re-validated through `UiSafeProjectDashboardSchema` (fail-closed, defense-in-depth). Per-workspace scoped; never-throws→`dashboard_failed`. DORMANT (no production caller until P4). 7 tests; worker 775.

**So the read-and-write halves of the real Projects dashboard are both built** (P3a builder produces the row; P3b port persists it).

## Deferred — each design-gated or Temporal (NOT built, flagged with the specific blocker)
- **concrete `ValidateNarrativePort`** — the fake ALREADY runs the real `validateNoInference` (REQ-F-017 real-tested); the unbuilt half is the schema-gate composition, whose contract is design-ambiguous (the synthesis output schema is an unpinned arch_gap + possibly redundant with the broker's internal gate). Owner/design call — not built speculatively.
- **wire the builder → a concrete `BuildSyncOutputsPort`/`SyncOutputsProjection`** — needs the `validated.fields` field-key→category convention decided.
- **P4 activation** — register the projectSync activities + trigger `runProjectSync` to write live rows (Temporal-gated; app boots Temporal-degraded). Also the sibling **R5** (recent-changes always-on wiring).
- ⚠ Run the dual reviewers at the P3-wiring boundary (when a real caller connects builder→update-port) — REQ-F-011 + no-inference + the WS-8 server-bound-workspaceId are safety-touching.

## Docs reconciled
`IMPLEMENTATION_PLAN.md` §13.5 (P1–P3b ticked) · memory `sow-dashboard-real-producers` · this session doc. (Handoff `001` RESUME block covers the arc.)
