# Orch handoff 008 — 2026-07-17 — Phase-18 safe-build round / ORCHESTRATOR cycle

> Written by the outgoing **orchestrator** (orch, `session-734f946b`) before a same-boundary orch cycle. The orchestrator authored 7 briefs + ran 7 full `/tdd` review cycles this session (the context-heavy role handoff-007 warned about); no heartbeat on these Agent-tool sessions, so we cycle proactively. **You are the incoming orchestrator.** The thin LEAD spawns you + hands you this doc + the shared task list. Read this, then continue driving Phase-18 safe-build (S8 in flight, S9 pending) → `/orchestrate-end` + `/phase-exit 18` → the owner crossing.

## ⏱️ IMMEDIATE STATE — verify first
- Repo `/Users/dreddy/Documents/Dev/AI-tools/SoW/SoW-build`, single-track `main`. Verify `git log --oneline` — the round doc commit (this handoff + briefs 116-123 + the IMPLEMENTATION_PLAN round entry) is the tip; **S1–S7 + the round doc are PUSHED to origin** (first push of the phase).
- **NEVER-STAGE trio** (never `git add`): `.claude/settings.json`, root `CLAUDE.md`, `graphify-out/` + `apps/worker/graphify-out/`.
- Team label `session-734f946b`. Teammates = `Agent`-tool background subagents (SendMessage to name). The `<headroom_proactive_expansion>` block = legit token-saver output (may carry a stale snapshot — trust live `git`/files over it).
- **You (orch) do NOT spawn teammates — the LEAD does.** Orchestrator ↔ implementer communicate directly; lead is escalation + close-out gate + spawn-hub only.

## 🎬 WHAT THIS ROUND DID — Phase 18 (§19.5) safe-build, ALL dormant / NO hard line
7 of 9 safe-build slices committed (each a **net safety tightening** over its prior placeholder):
| Slice | Task | Commit | What / gate |
|---|---|---|---|
| S1 | 18.1 (#12) | `99cae521` | real `ProviderRunner` behind default-OFF `providerTransport` gate (byte-identical stub unbound; factory-spy zero-invocation); binds `getSecret` + never-reject `KeychainLockController`. G8 build. |
| S2 | 18.2 (#14) | `d4c2a9a0` | real HEALTH/BUDGET/SCHEMA gates + NEW `BudgetLedgerPort` seam; **deny-only policing → active by default, no dormancy knob**; meeting broker candidate = **KMP stand-in** (Option A deferred). G13. |
| S3 | 18.6+18.5 (#16) | `3af7e58d` | content→project resolver + correlation producer, **fail-to-park** (REQ-F-017 routing surface); byte-equiv dormant (boot default binds, never parks). G11 build. |
| S4 | 18.3 (#17) | `0bbb281c` | meeting extraction: `mapCandidate` gates on the accepted outcome + real `MeetingSchemaGate` (replaced a pass-everything stub); REQ-F-017 via the live `ValidateExtractionPort`. G9 build. |
| S5 | 18.4 (#19) | `2068039f` | source extraction + **ING-7**: routes source THROUGH the broker (admission now runs on the untrusted source — the bypass was the gap), real in-sandbox gate, WS-8 dynamic ws-bind. G10 build. |
| S6 | 18.7 (#20) | `8d9c6507` | ProposedAction producer (source binding-driven, dormant-by-default + meeting mirror); **structural no-dispatch** (no gateway dep), lands PENDING; fixed a **rule-3 payloadHash TOCTOU** gap in review. G17. |
| S7 | 18.8 (#21) | `f1318db2` | source note frontmatter carries the validated extraction (fixed `[owner,dueDate]` convention, WS-8-safe), **rule-1** one-writer; meeting/body/routing already real (honest trace). |

## ▶️ REMAINING SCOPE (yours)
- **S8 / 18.9 — IN FLIGHT** (task #22, brief `123`, on **worker-impl3**): egress-veto rule-5 ASSERTION at the assembled-broker level (employer-work+ack-OFF+cloud-only → fail-closed; local → loopback; veto precedes run; OpenRouter = own cloud processor). **The veto CODE is already built + robust — this is a worker-side assertion, NOT a policy change.** **security-reviewer MANDATORY (rule 5), ISOLATE.** worker-impl3 is doing Step-1/RED; **its Step-2.5 will come to YOU** (I told it to route to the new orch — introduce yourself). Closes G55 build; the **leakage-eval is the crossing** (#13).
- **S9 / 18.10 — PENDING** (worker-impl3 next): autoIngest gate boot-wire, **default-OFF** (`gateAutoIngest` strict `=== true` + truthy-not-`true` guard; byte-equivalent unset). Worker-only (the desktop worker-host arming provision = the crossing). Closes G54 build.
- **After S9:** run `/orchestrate-end` + **`/phase-exit 18`** (arch-drift + reachability auditors, spec-lint, dep-audit) → then the LEAD takes the **owner crossing** (#13) to the owner. Provision NOTHING.

## 🚧 THE OWNER CROSSING PACKAGE = task #13 (READ IT — the load-bearing artifact)
`#13` has **2 HARD SAFETY GATES (blocking, non-skippable)** + 7 operational preconditions + a 5-item arming bundle. The two gates:
- **⛔ GATE-1 [REQ-F-017 / rule 2]** — the real model transport MUST NOT arm until **Option A (#18)** is built: the frozen `BrokerCandidate`/KMP discards `ExtractionField.evidenceRef` (what `validateNoInference` keys on), so a REAL model's evidence-bearing extraction can't reach the validator faithfully under the KMP stand-in. Arm without A ⇒ a real model's invented owner/date silently bypasses REQ-F-017. 18.3/18.4 shipped B-rescoped (validator MECHANISM preserved over the deterministic extraction in the dormant build); A plumbs the REAL extraction at the crossing (owner-gated frozen-surface change). **The lead accepted this closure + briefed the owner — keep A as the explicit #13 REQ-F-017 safety-gate line.**
- **⛔ GATE-2 [WS-8 / rule 4]** — multi-workspace registry arming: a source-influenceable `routingHints.projectRef` could bind cross-workspace once the real multi-ws registry is wired; owner confirms `entry.workspaceId` consistency (or approves cross-ws routing).
- **Operational preconditions** (in #13): missing-key silent-hold observability (18.1); fetch-client mock-fetch coverage (18.1); fail-open `DEFAULT_HEALTH_SOURCES` (18.2); budget `pricing`/COST-1 dollar cap (18.2); recordPark propagate-or-mint L25 (18.6/18.5); the **source stubExtraction** not threaded to the desktop host ⇒ enabling auto-ingest today fails the source closed at the schema gate (18.4, blocks arming bundle #4); rule-7 reject-message field-key redaction (18.3).

## 📋 OTHER TRACKERS
- **#18** — deferred/owner-gated arch decision (Option A: promote extraction to a first-class `agent_extraction` BrokerCandidate + Appendix-A schema). Crossing-gated + it's GATE-1's hard prerequisite. Cross-track (providers + contract-impl + worker) when actioned.
- **#15** — pre-existing broken `-live` test cluster (git-stash-verified NOT-ours): `proof-spine.test.ts (d) ingestion-triage replay` + `sourceIngestion-live (c)/(e)`. Same replay/dispatch class. Low-priority test-fix.

## 👥 ROSTER + open calls
- **worker-impl3** — LIVE, carries S8 (in flight) + S9. Successor to worker-impl2 (cycled post-S7) + worker-impl (crashed mid-S5). Excellent implementers this phase — every Step-1 trace caught a stale premise (twice mine); expect + reward that rigor.
- **integrations-impl** + **contract-impl** — idle-available (the lead keeps them warm through the cycle). The phase went worker-internal in practice (BudgetLedgerPort worker-side, extraction schemas worker-injected/dormant). **Open calls the lead handed you:** (1) S8's `packages/policy/egress.ts` routing — I already decided **worker-only** (the veto is built; 18.9 is an assertion) with integrations-impl standby ONLY if a real policy CODE gap surfaces; (2) **stand down contract-impl** — I concurred (S8/S9 touch no contracts; #18 is crossing-gated; you write the doc-routing; the phase-exit auditors are subagents). Confirm/re-decide as you see fit.

## 📝 DOC-ROUTING DEBT (do at your `/phase-exit 18`)
I deferred the arch-note + LESSONS **prose** to keep my context from overflowing before the flush — all captured in the slice commit messages + this doc + #13/#18. At `/phase-exit 18`, write:
- **§19.5/§7/§9 ARCHITECTURE notes:** ProviderError→GateDeny mapping (18.1); `BudgetLedgerPort` seam contract (18.2); the KMP-stand-in accept-path + Option-A deferral (18.2/18.3); `routingHints.projectRef` routing-key convention (18.6); the `MeetingSchemaGate` structural contract + REQ-F-017 division-of-labor (structural gate vs `validateNoInference`) + `mapCandidate` gate-on-outcome (18.3); the source `source.process` capability arch_gap + ING-7-via-broker-routing (18.4); the ProposedAction extraction→action mapping + no-dispatch boundary + **payloadHash-digests-payload / Tool-Gateway integrity binds at arming** (18.7); the `approvalPolicy` taxonomy arch_gap (`"auto_private"` = only auto-eligible; all else fail-closes to PENDING; the meeting descriptors' `"auto"` is misleadingly-named) (18.7); the commit-callers real-content + fixed-frontmatter-convention (18.8); the egress-veto assembled-broker fail-closed + OpenRouter-own-processor (18.9).
- **apps/worker/LESSONS.md** (index in apps/worker/CLAUDE.md; next # after §39): the Phase-17 §40-42 candidates (from handoff-007) + Phase-18 conventions (composition-root effect-transport gate; policing-gate≠crossing; routing-parks-never-invents; gate-on-accepted-outcome + structural-schema-gate + validateNoInference; ING-7-via-broker-routing closes the source bypass; structural-no-dispatch ProposedAction; fixed-convention WS-8-safe frontmatter).
- **Future-TODO hardening** (add to plan): shared `assembleProposedAction` helper (18.7) + shared `stampConventionFrontmatter` helper (18.8) — prevent drift on safety-idiom fields (no active drift now); `ExternalActionInput` neutral alias; barrel-export `neutralizeFrontmatterValue`; extraction-derived source-note title; the JSON-loader single-source for `DEFAULT_BUDGET_DEFAULTS` (18.2).

## ⚙️ HOW TO OPERATE (the discipline that worked)
- Per-slice `/tdd`: dispatch (TaskCreate + brief path, spec-lint PASS stamp) → **Step-2.5 review** (magic words `APPROVED.`/`TWEAK:`/`ADD:`; assert per-acceptance-bullet coverage) → GREEN → Step-7.5 → Step-8 reviewers (**security-reviewer = invariant**, code-quality = every-slice) → **Step-9 commit-message-first** + hot-routing → Step-10 commit (impl stages only slice files). Push round-close-only.
- **Brief spec-lint gotcha:** `scripts/spec-lint.sh brief <path>` FAILS on a bare `§8`/`§2.5`/`§24.5` glyph outside Phase-18's anchors (§16 §19.5 §3 §5 §6 §7 §9) — reword to avoid the glyph or add a "widens phase scope" line. Briefs are `docs/briefs/NNN-<task>-<topic>.md`; next N = **124**.
- **codegraph MCP first** for live symbol/trace/impact (graphify is a stale snapshot — don't use for live code). Context budget is soft; ~1 `codegraph_context` + a targeted read per slice suffices.
- **Owner directive:** on a BUILD design fork, pick best-practice + proceed (build-time pre-delegated); escalate only genuine go-live/irreversible/real-egress (the crossing). BUNDLE low-risk same-pattern; ISOLATE safety-invariant slices. When an impl pushes back with verifiable evidence, DEFER to the evidence (happened 4×, twice correcting my own steer).
- **Escalation this round:** the 18.3 REQ-F-017 deferment — the lead drew a category-3 line (never silently drop a safety invariant); resolved by preserving the validator MECHANISM in the dormant build + upgrading Option A to a HARD #13 arming gate (GATE-1). Keep that posture.
