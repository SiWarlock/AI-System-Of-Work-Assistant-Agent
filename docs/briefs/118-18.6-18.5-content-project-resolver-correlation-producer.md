# /tdd brief — content_project_resolver_and_correlation_producer (18.6 + 18.5, co-dependent pair)

## Feature
Replace two placeholder routing legs with real, registry-backed, **fail-to-park** resolution — landed together per the plan's co-dependency mandate:
- **18.6** — `sourceRoute.classify` becomes a real content→workspace/project classifier over the 14.6 `ResolveRegistryPort` (not `confidence:1` for the single boot workspace; `projectId` no longer caller-supplied).
- **18.5** — the fixed `params.correlationSignals` (`confidence:0.95`) becomes a real confidence-scored correlation-signal producer for meeting binding.

**Both share the load-bearing REQ-F-017 routing rule: below-threshold / no-match / ambiguous ⇒ PARK to the Ingestion Inbox — NEVER a guessed `projectId` or invented binding.** The deterministic classify + threshold + park logic is TDD; any model-scored signal is eval-tested/deferred to the crossing. **SAFE-BUILD: no real model call — the scorer is injected/deterministic in tests; no endpoint/key/spend.**

## Use case + traceability
- **Task ID:** 18.6 + 18.5 (co-dependent — the plan mandates landing them together; 18.6 is the resolver 18.5 consumes)
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (primary), `§9` (workflows 1 & 4 routing), REQ-F-017 (no-inference: park/clarify, never invent)
- **Related context:**
  - **18.6 evidence:** `sourceRoute.classify` returns `confidence:1` for the single boot workspace; `deterministicProgress.ts` `projectId` is caller-supplied, never content-resolved.
  - **18.5 evidence:** `params.correlationSignals` is a fixed `confidence:0.95` binding; `boot.ts` hands it in hard-coded.
  - **The 14.6 registry (landed):** `ResolveRegistryPort.resolve(ctx: ProjectSyncContext) → Promise<Result<ProjectRegistryEntry, ResolveRegistryError>>` (`packages/workflows/src/ports/projectSync.ts:166`), boot-wired via `createProjectRegistryResolvePort` (`apps/worker/src/composition/projectRegistry.ts:65`); `ProjectIdentity {projectId,title,slug,lifecycleState}`.
  - **The park destination (existing):** `IngestionInboxProjectionPort.recordPark` (`apps/worker/src/api/projections/ingestionInboxProjection.ts`) — the UI-safe park upsert, dedup-by-sourceId. This is the "park to Ingestion Inbox" sink; the raw envelope is server-side-operational-only (L36, rule 7).
  - **REQ-F-017 has two surfaces:** this slice is the ROUTING surface (below-threshold → park, never invent a binding/projectId). The EXTRACTION surface (owner/dueDate → TBD, never invent) is the separate `NoInferenceView` binding at 18.3/18.4.
  - **Line refs in the plan may be stale** (the file evolved through 18.2's fixture aligns) — trace the real symbols (`sourceRoute.classify`, `params.correlationSignals`) at Step 1; the plan's names are approximate (memory: verify by tracing).

## Acceptance criteria (what "done" means)
**18.6 — content→project resolver:**
- [ ] `sourceRoute.classify` resolves workspace+project from CONTENT via `ResolveRegistryPort.resolve(ctx)` — a confident registry match yields the resolved `ProjectIdentity` + workspace binding (not `confidence:1` boot workspace).
- [ ] `deterministicProgress` `projectId` is CONTENT-resolved through the registry, not caller-supplied.
- [ ] **WS-8:** the resolved workspace comes from the registry resolution (registry-validated), never a smuggled caller field.
- [ ] **REQ-F-017:** no registry match / ambiguous / below-threshold ⇒ PARK to the Ingestion Inbox (`recordPark`), `projectId` NEVER guessed.

**18.5 — correlation-signal producer:**
- [ ] `params.correlationSignals` is replaced by a real producer yielding a confidence-scored meeting binding; the deterministic threshold → bind-or-park wiring is TDD. Any model-scored confidence comes through an INJECTED scorer seam (deterministic in tests; real model routing is eval-tested at the crossing).
- [ ] **REQ-F-017:** below-threshold confidence ⇒ PARK clarification (Ingestion Inbox), NEVER an invented binding.
- [ ] Above-threshold ⇒ the confidence-scored workspace+project binding, resolved through 18.6's classifier.

**Shared:**
- [ ] **SAFE-BUILD:** no real model call/endpoint/key/spend; the correlation scorer is injected/deterministic in tests; the eval-class pass is deferred to the crossing.
- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
The classifier + producer land in `apps/worker/src/composition/buildActivities.ts` (the source-ingestion + meeting-closeout activity factories) + `packages/workflows/src/activities/deterministicProgress.ts` (`projectId` resolution); reached in production via the source-ingestion + meeting-closeout workflows (`broker.runJob` / the deterministic progress path). The park path is the existing `IngestionInboxProjectionPort.recordPark`. Confirm both legs are reached on the real workflow path, not just tests.

## Files expected to touch
**New:**
- `apps/worker/test/composition/content-project-resolver.test.ts` (or split per leg) — the RED tests below.
- Possibly `apps/worker/src/composition/<correlation-scorer>.ts` — the injected `CorrelationScorerPort` seam (if not folded into buildActivities).

**Modified:**
- `apps/worker/src/composition/buildActivities.ts` — real `sourceRoute.classify` over `ResolveRegistryPort`; real correlation-signal producer replacing the fixed `params.correlationSignals`.
- `packages/workflows/src/activities/deterministicProgress.ts` — content-resolved `projectId` (registry), not caller-supplied.
- `apps/worker/src/boot.ts` — remove the hard-coded `confidence:0.95` correlation param + wire the resolver/scorer deps (inert/deterministic default).

> If implementation needs files beyond this (e.g. a registry-context builder), flag at Step 2.5.

## RED test outline (Step 2)
1. **`classify_resolves_project_from_registry_match`** — content with a confident registry match → resolved `ProjectIdentity` + workspace (not the boot-ws `confidence:1`). Why: 18.6 / §9.
2. **`classify_no_registry_match_parks_never_guesses`** — no match → `recordPark` called, `projectId` NOT guessed/emitted. Why: REQ-F-017.
3. **`classify_ambiguous_below_threshold_parks`** — ambiguous/below-threshold → park, no binding. Why: REQ-F-017.
4. **`deterministic_progress_projectId_is_content_resolved`** — `projectId` comes from the registry resolution, not the caller. Why: 18.6 evidence.
5. **`classify_workspace_from_registry_not_caller`** — WS-8: resolved ws from the registry; a smuggled caller ws is ignored. Why: WS-8 (L30/L32).
6. **`correlation_producer_above_threshold_binds`** — a confident (fake-scored) correlation → confidence-scored binding via the resolver. Why: 18.5.
7. **`correlation_below_threshold_parks_clarification`** — low (fake) confidence → park clarification, never an invented binding. Why: REQ-F-017.
8. **`correlation_scorer_is_injected_no_real_model_call`** — the scorer is injected (deterministic fake); no real model/network call fires. Why: SAFE-BUILD.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none anticipated — reuses `ProjectIdentity`, `ProjectRegistryEntry`, `Workspace`, `ProviderRoute`, `AgentJob`, `SourceEnvelope`. Confirm at Step 9.
- **Orchestrator doc rows to write hot:** none anticipated. A NEW `CorrelationScorerPort` seam is worker-internal (not an Appendix-A shared model) — confirm at Step 9.
- **Shared-contract (Appendix-A cross-track seam) model touched?** **No** anticipated — no Appendix-A field change; no schema-snapshot test required. If the classifier needs a new `ProjectSyncContext` field, flag it (that could be a cross-track seam → contract-impl).

## Things to flag at Step 2.5
1. **Threshold value + source** — a config-driven routing-confidence threshold vs a hardcoded constant; the below-threshold park boundary. My default vote: **config-driven threshold** (owner-tunable), fail-closed default = park when unset/ambiguous.
2. **Correlation scorer seam** — inject a `CorrelationScorerPort` (deterministic fake in tests; the real model-via-broker binds at the crossing, mirroring the 18.3/18.4 extraction-leg pattern) vs routing correlation directly through `broker.runJob` now. My default vote: **an injected `CorrelationScorerPort` seam** — keeps the deterministic threshold/park logic TDD-pinned and the model scoring cleanly deferred/eval-tested.
3. **Classifier matching logic** — how content maps to a registry project: registry lookup via `ProjectSyncContext` built from `routingHints`/explicit binding (deterministic), NOT model inference (that's the eval/crossing layer). My default vote: **deterministic registry lookup over `routingHints`/explicit binding**; content→ctx mapping carries no model call; anything requiring semantic inference parks (REQ-F-017).
4. **Co-dependency landing** — 18.6 (resolver) is the foundation 18.5 consumes; both in ONE bundled slice + ONE commit (the plan's land-together mandate, not the discretionary bundle rule). My default vote: **one bundled slice, 18.6 legs first then 18.5, single commit.**

## Dependencies + sequencing
- **Depends on:** 18.1 (`99cae521` — the broker the correlation scorer routes through when real; landed dormant), 14.6 (`ResolveRegistryPort` — landed), 15 (spine plumbing — landed).
- **Blocks:** 18.8 (commit callers carry real routing outputs), 18.7 (ProposedAction over resolved routing).

## Estimated commit count
**1** — one bundled co-dependent slice (18.6 + 18.5). **INVARIANT** (workspace/project routing — WS-8-adjacent — + REQ-F-017 no-inference at the routing layer) → **security-reviewer = invariant applies to the whole slice; its own commit, no further bundling.** The bundle is one logical unit by the plan's co-dependency mandate.

## Lessons-logged candidates anticipated
- **Convention candidate** — "content→workspace/project routing PARKS on no-match/ambiguity/below-threshold (REQ-F-017 routing surface), never invents a `projectId`/binding; the resolved workspace is registry-validated (WS-8), never a smuggled caller field."
- **Architecture-doc note candidate** — the `CorrelationScorerPort` seam + the routing-confidence threshold (deterministic wiring now; model scoring eval-tested at the crossing).
- **Future TODO — crossing:** the real model correlation scoring is eval-tested at the consolidated crossing (routes through the armed broker transport).

## How to invoke
1. **Read this brief end-to-end** — the 4 Step-2.5 questions need answers before GREEN.
2. **Run `/tdd content_project_resolver_and_correlation_producer`** in the `worker-impl` session.
3. **Step 0 (Restate)** — SAFE-BUILD, deterministic routing + park-not-guess; model scoring injected/deferred.
4. **Step 1 (Identify files)** — trace the REAL `sourceRoute.classify` / `params.correlationSignals` symbols (plan line-refs may be stale); flag any extra file (registry-context builder, scorer port).
5. **Step 2.5** — send the test write-up + votes on the 4 questions; wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 8 reviewers** — security-reviewer (invariant: WS-8 routing + REQ-F-017) + code-quality-reviewer (every-slice).
7. **Step 9** — categorized flags + ship-ask.
