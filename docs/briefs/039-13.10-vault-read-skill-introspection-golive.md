# /tdd brief — copilot_vault_read_skill_introspection_golive (C6 §13.10d — go-live flag-flip)

## Feature
Flip the two BUILT-but-flag-gated read-only Copilot skills LIVE on the desktop worker-host: `vault.read` (`copilotVaultRead`) and skill self-introspection `skills.list`/`skills.get` (`copilotSkillIntrospection`). The handlers, boot wiring, MCP servers, and scope/symlink machinery are already built + dual-reviewed (§13.10d, `75cf6a8`/`f7feb95`/`bf994e6`/`bdf4170`/`d693e32`); this slice is the **owner-authorized GO-LIVE** — flip the flags, verify the go-live safety properties hold on the now-live path, and pass a MANDATORY security + code-quality review. **SAFETY-RELEVANT** (exposes a filesystem read surface + the skill catalog to the LIVE cloud Sonnet-5 Copilot, within the approved personal-business envelope) — WS-8 (safety rule 4) + read-confinement.

## Use case + traceability
- **Task ID:** 13.10 (Copilot Skill Catalog C6 — the §13.10d vault-page-reads + skill-introspection go-live; owner-authorized 2026-07-12 "flip the 2 built read-only extras live"). Extends the already-live `copilotAgentMode` read agent (`35b8ad4`) with two more read-only tools behind their own flags.
- **Architecture sections it implements:** `ARCHITECTURE.md §7` (AgentRuntimePort / ING-7 admission — the Copilot agentic read-tool surface) + `§6` (GBrain/GCL / WS-8 read scope + candidate-data posture). (Both ∈ Phase 13 Spec anchors — no widen.)
- **Related context:** memory `sow-copilot-skill-catalog`; runbook `docs/runbooks/copilot-propose-go-live.md`. The read-only agent path (18 gbrain tools) is already LIVE + WS-8-scoped via the in-process scoped proxy.

**Confirmed current state (pre-orient, HEAD `2f8db61`):**
- Both are **built + flag-gated OFF**. Boot guards: vault wires only when `config.copilotVaultRead === true && config.vaultRoot !== undefined && wsScope !== undefined` (`boot.ts:605-613` → `createCopilotVaultMcpServer` + `createFsVaultReadFileExec` + `createFsRealpath` + `vaultRoot`); skills wires only when `config.copilotSkillIntrospection === true && wsScope !== undefined` (`boot.ts:618-621` → `createCopilotSkillsMcpServer`). Both register inside the runner's **scoped-proxy branch** (WS-8-scoped, per the boot comment "the handler path-guards + WS-8-scopes every read").
- `worker-host/index.ts` currently sets neither flag (→ OFF). `vaultRoot` is an optional config field (`:47`, passed through only if set, `:144`).
- Vault handler (`handleCopilotVaultReadCall`) is symlink-safe (3-layer: lexical WS-8+traversal → realpath containment + real-path re-attribution → size-capped read; a symlink-escape CRITICAL was found+fixed+re-verified in the build slice). Skills handler reads the STATIC catalog only (`COPILOT_READ_TOOLS`, not the combined `CATALOG` — NEVER reveals the propose tool), workspace-agnostic, zero-leak.

## Acceptance criteria (what "done" means)
- [ ] `apps/desktop/worker-host/index.ts` sets `copilotSkillIntrospection: true` (unconditional — static-catalog read, no vaultRoot needed) and `copilotVaultRead: true`.
- [ ] `vault.read` go-live safety — a deterministic boot/worker-tier test pins that when (and only when) `copilotVaultRead + vaultRoot + wsScope` are all present, the vault MCP server is wired into the runner **scoped to the served/asked workspace**; absent any precondition ⇒ NOT wired (fail-safe). The three properties hold on the live path: **(i)** WS-8-scoped — a vault read is confined to the asking workspace's content and CANNOT return another workspace's (personal-business only today; NOT employer-work); **(ii)** symlink-safe / realpath-confined to `vaultRoot` (no `../`/absolute-outside/symlink escape); **(iii)** read-only (no write/mutating vault op is registered or reachable).
- [ ] `skills.list`/`skills.get` go-live safety — a test pins flag-gated wiring + that introspection returns ONLY `COPILOT_READ_TOOLS` metadata and NEVER the propose/mutating tool; no secret / internal path / config leaks.
- [ ] `vaultRoot` runtime precondition documented — vault.read is functionally inert until the owner provides `vaultRoot` (the personal-business Obsidian vault path) via config/env; the guard fail-safes (no vaultRoot ⇒ vault server not wired), so the flag-flip is safe to land even before the path is set (like `MANAGE_GBRAIN_SERVE`).
- [ ] Repo-wide `pnpm -w turbo run typecheck test` green; `/preflight` clean. **MANDATORY Step-8 review = security-reviewer + code-quality-reviewer** (go-live safety surface).
- [ ] No frozen-contract change (catalog additions were already landed; this is a flag-flip). No propose/mutating flag touched — `copilotProposeMode`/`copilotProposeKnowledge` stay OFF (HARD LINE).

## Wiring / entry point (Step 7.5)
`none-new` — the flip activates the EXISTING boot wiring (`boot.ts:605-621`) that registers the vault + skills MCP servers into the live `createClaudeAgentCopilotRunner` scoped-proxy branch. The entry point is the already-live Copilot agent (`copilotAgentMode: true`); this widens its tool set by two read-only tools. Reachability: worker-host flags → `boot.ts` guards → runner scoped-proxy branch → the live Sonnet-5 agent.

## Files expected to touch
**Modified:**
- `apps/desktop/worker-host/index.ts` — add `copilotVaultRead: true` + `copilotSkillIntrospection: true` to the Copilot config block (alongside the existing `copilotAgentMode`/`copilotWorkspaceScoping`); a comment documenting the `vaultRoot` owner-runtime precondition + reversibility.
- `apps/worker/test/**` (or the existing copilot boot/wiring test) — the flag-gating + scope/symlink/read-only pins if not already asserted for the FLIPPED-ON path (the handlers' own unit tests exist — 17 vault + skills tests; this slice adds/【confirms】the BOOT flag-gating + scope-binding assertion).

If the safety verification needs to touch a handler (it should NOT — handlers are built+reviewed), **flag at Step 2.5** — that would mean the go-live surfaced a real gap.

## RED test outline (Step 2)
1. **`vault_wired_only_when_flag_and_root_and_scope`** — Asserts: boot wires `vaultRunnerDeps` iff `copilotVaultRead && vaultRoot && wsScope`; any missing ⇒ undefined (fail-safe). Tag `spec(§7)`.
2. **`vault_read_ws8_scoped_no_foreign_workspace`** — Asserts: a vault read via the live path is confined to the asking workspace (personal-business) and cannot surface another workspace's content. Why: WS-8 (§6). (Reuse/extend the existing vault scope test if present.)
3. **`vault_read_symlink_and_traversal_confined`** — Asserts: `../`/absolute-outside/symlink-escape read attempts fail closed (realpath-confined to `vaultRoot`). Why: read-confinement (cite the existing symlink-escape regression).
4. **`vault_read_no_mutating_op`** — Asserts: only a read op is registered on the vault MCP server (no write/delete). Why: read-only invariant.
5. **`skills_wired_only_when_flag_and_scope`** — Asserts: skills server wired iff `copilotSkillIntrospection && wsScope`. Tag `spec(§7)`.
6. **`skills_introspection_never_reveals_propose_tool`** — Asserts: `skills.list`/`get` project ONLY `COPILOT_READ_TOOLS`, never the propose/mutating tool; no secret/path leak. Why: zero-leak.

Most safety properties are already pinned by the handlers' unit tests (§13.10d build) — MAP each acceptance property to an existing test (cite it) OR add the boot-flag-gating pin; do NOT duplicate handler-internal tests.

## Cross-doc invariant impact
- **Model field changes:** none — a flag-flip; catalog additions already landed.
- **Orchestrator doc rows to write hot (Step 9):** tick the §13.10d vault/skill go-live items in `IMPLEMENTATION_PLAN.md` (orchestrator writes); update the go-live runbook if the reviewer names a residual.
- **Shared-contract seam model touched?** No (a flag-flip; no Appendix-A model).

## Things to flag at Step 2.5
1. **How `vault.read` enforces "personal-business ONLY" (the core safety design call).** My default: the vault read is WS-8-scoped by the per-ask `wsScope` binding + realpath-confinement to `vaultRoot`; on today's single personal-business envelope it's personal-business by construction. CONFIRM the exact mechanism against the handler — if `vaultRoot` could point at a multi-workspace vault, the scoping must partition by path (flag if the confinement is root-only, not workspace-partitioned — that's a go-live gap the security review must adjudicate before flip).
2. **`vaultRoot` provisioning.** My default: leave `vaultRoot` to the owner's runtime config (guard fail-safes if absent) — do NOT hardcode a path in worker-host. Confirm.
3. **Test scope.** My default: add ONLY the boot flag-gating + scope-binding pin; reuse the existing handler unit tests (17 vault + skills) for symlink/read-only/zero-leak — cite, don't duplicate. Confirm.
4. **Review depth.** My default: security-reviewer + code-quality-reviewer BOTH (go-live safety surface; WS-8 + fs read-confinement + cloud-model exposure). Confirm.

## Dependencies + sequencing
- **Depends on:** the built §13.10d handlers + boot wiring (present); the live `copilotAgentMode` scoped-proxy branch (`35b8ad4`, live).
- **Blocks:** nothing hard. Slice (b) Tier-2 synthesis is the next C6 slice (separate brief).

## Estimated commit count
**1.** A focused go-live flag-flip + safety verification. Safety-relevant → **security + code-quality review MANDATORY** (lead verifies at commit: WS-8 personal-business-only · symlink-safe/read-confined · read-only). One commit.

## Lessons-logged candidates anticipated
- **Convention candidate** — "a go-live flag-flip of a built+tested read surface to a LIVE cloud agent is a distinct reviewed slice: pin the flag-gated wiring + re-verify the safety properties on the now-live path, don't just flip." (If it recurs across the C6 tiers.)

## How to invoke
1. Read this brief (go-live flag-flip of built §13.10d vault.read + skill-introspection; safety-relevant; the core is the safety verification + review, not new logic). 2. `/tdd copilot_vault_read_skill_introspection_golive`. 3. Step 0 restate: flip `copilotVaultRead` + `copilotSkillIntrospection` live + pin the flag-gated WS-8-scoped/symlink-safe/read-only wiring. 4. Step 1: confirm the file list. 5. Step 2.5: the four decisions (personal-business-only mechanism, vaultRoot provisioning, test scope, review depth). 6. Step 8: **security + code-quality review (MANDATORY)**. 7. Step 9: flags + ship-ask (esp. any go-live safety residual).
