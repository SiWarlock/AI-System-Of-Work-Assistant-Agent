# /tdd brief — source_ingestion_external_write_propose (15.7)

## Feature
Route the **source-ingestion external-write propose** through the **real Tool Gateway** (`createProposeActivity` over the real gateway) instead of today's in-memory `sourceReceiptByKey` Map (`buildActivities.ts:703`) that mints `ext-source-N` receipts with NO `dispatchExternalWrite` — **mirroring the already-built `meetingPropose`** (`buildActivities.ts:436-443`). After this, a source-ingestion propose produces a real `ProposedAction` → `ExternalWriteEnvelope` (idempotencyKey + canonicalObjectKey, rule 3) → a **pending §9 Approval**, not an in-memory receipt. Worker-track. **Pure-build, NO hard line** — the propose lands a PENDING Approval (inert until the owner approves it in the §9.8 Approvals inbox); **no real external transport is bound (that is Phase 21)**, no real egress/spend, no propose-flip. (Confirmed: the mirror `meetingPropose` produces a pending Approval with zero real write — 15.9's t13 e2e exercised exactly this.) Closes **G7**.

## Use case + traceability
- **Task ID:** 15.7 (closes G7 — the source-ingestion external-write propose is real, not an in-memory receipt).
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (ingestion spine — source propose), `§8` (Connector/Tool Gateway — the external-write envelope: idempotencyKey + canonicalObjectKey + pre-write existence check + receipt).
- **Depends on:** P6 (§8 gateways), 15.1. **Pattern to mirror:** `meetingPropose` (`createProposeActivity` over the real gateway) — already dormant-safe (pending-Approval, no egress).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **The stub to replace — `apps/worker/src/composition/buildActivities.ts:703`:** the in-memory `sourceReceiptByKey` Map that mints `ext-source-N` with NO `dispatchExternalWrite` (a fake receipt, not a real ProposedAction/Approval).
  - **The pattern to mirror — `buildActivities.ts:436-443` `meetingPropose = createProposeActivity(...)` over the real gateway:** the source propose delegate adopts the SAME construction. Confirm the exact `createProposeActivity` deps (the gateway, the Approval sink, the envelope builder).
  - **The dormant-safety anchor:** `createProposeActivity` produces the `ProposedAction` + `ExternalWriteEnvelope` and lands a PENDING Approval; the real external WRITE (`dispatchExternalWrite` over a real transport) is a SEPARATE step and the real transport stays UNBOUND (Phase 21). Confirm at Step 1 that binding the source propose to the real gateway does NOT bind a real transport (byte-equivalent dormancy — no egress on the shipped default).

## Acceptance criteria (what "done" means)
- [ ] **Real propose, not a receipt:** the source-ingestion propose delegate is `createProposeActivity` over the real Tool Gateway (mirror `meetingPropose`), replacing the in-memory `sourceReceiptByKey` Map. A source-ingestion propose produces a real `ProposedAction`.
- [ ] **Pending Approval + envelope (rule 3):** the propose lands a **pending §9 Approval** carrying the `ExternalWriteEnvelope` (idempotencyKey + canonicalObjectKey — the external-write envelope, safety rule 3), NOT an `ext-source-N` in-memory receipt. Pin the idempotencyKey + canonicalObjectKey on the envelope.
- [ ] **Idempotency (rule 3):** a re-proposed same source-ingestion action (same canonical key) does NOT create a duplicate pending Approval (mirror the meeting propose idempotency). Pin the replay leg.
- [ ] **DORMANT — no real egress (NO hard line):** binding the source propose to the real gateway binds NO real external transport (Phase 21); the shipped default performs zero real external write (`dispatchExternalWrite`'s real transport stays UNBOUND). Pin that the propose path produces the pending Approval WITHOUT a real write (a factory-spy / unbound-transport assertion, mirroring the dormancy posture).
- [ ] **Candidate-gate / rule-7:** the proposed action's payload is redaction-safe (no raw content / secret rides the envelope or the Approval card). Confirm at Step 8.
- [ ] **Reachability:** the source propose delegate is wired into the ingestion path (mirror where `meetingPropose` binds); exercised by a test that drives a source-ingestion propose → a pending Approval. The real transport is Phase-21-waivered (L11).
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
The source-ingestion propose delegate (`buildActivities.ts:703`) becomes `createProposeActivity` over the real gateway; it's reached on the source-ingestion propose path (mirror `meetingPropose`'s binding). State: reachable + exercised by a source-propose → pending-Approval test; the real external transport is Phase-21-waivered (byte-equivalent dormancy). Name the exact binding site + confirm the no-real-transport dormancy at Step 9.

## Files expected to touch
**Modified:** `apps/worker/src/composition/buildActivities.ts` (`:703` — swap the in-memory Map for `createProposeActivity` over the real gateway, mirroring `:436-443`) + tests.
If the swap ripples a dep/type (the gateway/sink/envelope-builder deps `meetingPropose` already has), thread them the same way; flag at Step 2.5 if beyond a mirror.

## RED test outline (Step 2)
1. `source_propose_produces_a_real_proposed_action` — a source-ingestion propose ⇒ a real `ProposedAction` (not an `ext-source-N` receipt). (§19.2/§8 — the G7 fix)
2. `source_propose_lands_a_pending_approval_with_envelope` — the propose lands a pending §9 Approval carrying the `ExternalWriteEnvelope` (idempotencyKey + canonicalObjectKey). (rule 3 / §8)
3. `source_propose_is_idempotent` — a re-proposed same canonical action ⇒ no duplicate pending Approval. (rule 3)
4. `source_propose_binds_no_real_transport` — the shipped default performs ZERO real external write (real transport unbound, dormant — byte-equivalent). (NO hard line)
5. `source_propose_payload_is_redaction_safe` — no raw content/secret on the envelope/Approval card. (rule 7)

## Cross-doc invariant impact
- **Model:** none — consumes the existing `ProposedAction` + `ExternalWriteEnvelope` (task 15.7 "Cross-doc invariant: none"). **Shared-contract seam touched?** No.

## Things to flag at Step 2.5
1. **Dormancy confirmation (LOAD-BEARING).** Confirm that routing the source propose through the real gateway binds NO real external transport — the propose produces a pending Approval and the real `dispatchExternalWrite` transport stays UNBOUND (Phase 21). This is the "no hard line" guarantee — pin it (mirror how `meetingPropose` stays egress-free by default).
2. **Mirror fidelity.** Confirm the source propose delegate adopts `meetingPropose`'s exact `createProposeActivity` construction (gateway + Approval sink + envelope builder) — a divergence is a flag.
3. **canonicalObjectKey derivation.** Confirm how the source-ingestion action's `canonicalObjectKey` is derived (the external-write envelope's dedupe anchor) — it must be stable/idempotent (rule 3).

## Dependencies + sequencing
- **Depends on:** P6 (§8 gateways), 15.1, the `meetingPropose` pattern. **Blocks / closes:** **G7** (source-ingestion external-write propose is real, not an in-memory receipt).

## Estimated commit count
**1.** A focused mirror slice. **security-reviewer = invariant** (the external-write envelope rule-3 idempotency + rule-7 redaction + the dormancy/no-real-egress guarantee — a propose path is safety-adjacent even though it lands only a pending Approval). code-quality every-slice. **Pure-build — NO hard line** (pending Approval only; no real transport bound [Phase 21]; no real egress/spend/propose-flip).

## Lessons-logged candidates anticipated
- **Convention candidate (maybe)** — the source-ingestion external-write propose mirrors `meetingPropose` (`createProposeActivity` over the real gateway) to replace an in-memory receipt with a real `ProposedAction` → `ExternalWriteEnvelope` (idempotencyKey + canonicalObjectKey) → pending Approval, staying egress-free by default (the real transport is Phase-21-waivered) — a propose path is real machinery + a dormant transport, never a real write. Bank only if it adds beyond the existing propose lessons.

## How to invoke
1. Continue the worker-impl3 session.
2. Read this brief (Q1 dormancy confirmation is the load-bearing safety point — the propose must produce a pending Approval with NO real egress).
3. `/tdd source_ingestion_external_write_propose`.
4. Step 2.5 — ping the dormancy + mirror-fidelity answers. Don't GREEN until I sign off.
5. Step 9 — categorized flags; confirm G7 closed + the no-real-transport dormancy. After 15.7 → 15.8 (human routing-resolution loop; has a desktop leg for the next desktop-impl).
