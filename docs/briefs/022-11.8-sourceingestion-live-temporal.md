# /tdd brief — sourceIngestion_live_local_temporal (make-it-real Option-C, slice C1)

## Feature
Activate the previously-uncalled `sourceIngestion` §9 workflow driver on a REAL **local** Temporal worker: register it as a sandbox workflow, wire its activity set (the REAL `registerSource()` gate + the existing deterministic proof-spine/source fakes for every other leaf), and prove — via a `SOW_TEMPORAL`-gated `TestWorkflowEnvironment` integration test — that it schedules + executes its activities end-to-end and rests in the expected deterministic `sourceMachine` state. LOCAL-ONLY; no cloud, no external write, no real disk-content read (those are C2/C3).

## Use case + traceability
- **Task ID:** 11.8 (make-it-real Option-C arc, slice C1)
- **Architecture sections it implements:** `ARCHITECTURE.md §9` (Temporal Workflows & Automation — the source-ingestion flow / the integration spine), with `§16` (worker supervision lifecycle / never-throw-across-boundary) and `§7` (runtime DoD) as supporting anchors.
- **This brief widens phase scope because** it is a make-it-real activation that reaches into the `§9` source-ingestion workflow from the Phase-11 real-integration/runtime home (Phase 11's Spec anchors are §13/§4/§16/§7/§5/§12; §9 is legitimately cross-phase for a live-activation slice — it drains the deferred Phase-7 "10 drivers' 40 fake-only activity ports" carry-forward).
- **Implements:** REQ-F-010 (source ingestion — live activation)
- **Related context:** the existing proof-spine capstone `apps/worker/test/integration/proof-spine.test.ts` is the exact pattern to mirror (TestWorkflowEnvironment + `bundleWorkflowCode` + one shared activities object). The pure driver is `packages/workflows/src/workflows/sourceIngestion.ts` (task 7.7, done + unit-tested). Registration surface: `apps/worker/src/temporal/{workflows.ts (sandbox bundle), registerWorker.ts (createProofSpineWorker / buildRegisteredActivities / ProofSpineParams)}` + `apps/worker/src/composition/buildActivities.ts`. Deterministic leaf fakes already exist in `packages/workflows/test/support/source-fakes.ts`. **Guardrail (owner):** real Temporal orchestrating still-faked/local connectors is the EXPECTED shape here — only `registerSource()` (already real + pure) runs for real in C1.

## Acceptance criteria (what "done" means)
- [ ] The `sourceIngestion` driver is registered as a sandbox workflow reachable on the proof-spine task queue (added to `apps/worker/src/temporal/workflows.ts`), scheduling its activities via `proxyActivities`.
- [ ] The registered activities object exposes the `sourceIngestion` activity delegates (the real `registerSource` gate + the deterministic leaf ports), built through the SAME `buildProofSpineActivities`/`buildRegisteredActivities` assembly path (no divergent hand-rolled set).
- [ ] `SOW_TEMPORAL`-gated integration test: dispatching `sourceIngestion` live on a `TestWorkflowEnvironment` **HAPPY PATH** → the workflow runs to completion and rests in the deterministic terminal `sourceMachine` state the injected fakes produce; the REAL `registerSource()` gate was exercised (a valid fixture source → `registered`).
- [ ] `SOW_TEMPORAL`-gated: a **malformed source** fixture → the real `registerSource()` gate rejects it → the workflow rests in the `sourceMachine` failure/park state and emits a distinct §16 health item (nothing silent, no uncaught throw across the worker boundary).
- [ ] `SOW_TEMPORAL`-gated: **idempotency/replay** — the SAME `ResolveRunInput` run id driven twice reuses the run (`resolveRun`), no duplicated durable effect (mirrors proof-spine (d)).
- [ ] **Default suite unaffected:** without `SOW_TEMPORAL=1` the live test is `describe.skipIf`-skipped; `pnpm -w turbo run typecheck test` stays green with NO live Temporal server (repo-wide 31/31 preserved).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
The live **Temporal worker registration** is the entry point: `createProofSpineWorker` registers the sandbox workflows (`workflowsPath`/`workflowBundle` → the `workflows.ts` bundle now including `sourceIngestion`) + the activities object (now including the source-ingestion delegates). The `SOW_TEMPORAL`-gated integration test is the production-shaped driver of that path (it stands up the real worker + dispatches the workflow), exactly as `proof-spine.test.ts` drives the other three. **Production boot dispatch** (`bootWorker`/`bootstrapWorker` scheduling `sourceIngestion` on the dev-server path) is explicitly **out of scope — wiring lands in C3 (task 11.8).**

## Files expected to touch
**New:**
- `apps/worker/test/integration/sourceIngestion-live.test.ts` — the `SOW_TEMPORAL`-gated live integration test (mirrors `proof-spine.test.ts` harness setup).

**Modified:**
- `apps/worker/src/temporal/workflows.ts` — add the `sourceIngestion` sandbox workflow entry (proxyActivities → the driver).
- `apps/worker/src/composition/buildActivities.ts` — extend the built activity set with the source-ingestion delegates (real `registerSource` gate + deterministic leaf ports); extend the params input if needed.
- `apps/worker/src/temporal/registerWorker.ts` — extend `ProofSpineParams`/`buildProofSpineParams` (or add `SourceIngestionParams`) so the composition root binds the source-ingestion job identity; the `ProofSpineActivities` type gains the source-ingestion delegates.

If implementation needs files beyond this list (e.g. a shared deterministic source-fake promoted out of `test/support` into a reusable fixture), **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)
Tests to write in `apps/worker/test/integration/sourceIngestion-live.test.ts` (+ a fast unit assertion where noted):

1. **`buildRegisteredActivities_exposes_sourceIngestion_delegates`** (fast unit, no Temporal) — Asserts: the activities object built from real backends + params contains the source-ingestion activity delegates (registerSource + the leaf ports), shaped as async fns. Why: §9 — the workflow's scheduled activities must be registered, or the live run fails to find them. Tag `spec(§9)`.
2. **`sourceIngestion_live_happy_path_reaches_terminal_state`** (`SOW_TEMPORAL`-gated) — Asserts: a valid fixture source, driven through the live worker, rests in the deterministic terminal `sourceMachine` state; the real `registerSource()` gate returned `registered`. Why: §9 live spine — schedule→execute→rest. Tag `spec(§9)`.
3. **`sourceIngestion_live_malformed_source_parks_with_health_item`** (`SOW_TEMPORAL`-gated) — Asserts: a malformed fixture → real gate rejects → failure/park state + a distinct health item; NO uncaught throw across the worker boundary. Why: §16 never-throw + nothing-silent.
4. **`sourceIngestion_live_replay_reuses_run`** (`SOW_TEMPORAL`-gated) — Asserts: same run id twice → `resolveRun` reuses the run, one durable outcome. Why: §9/idempotency (mirrors proof-spine (d)).
5. **`live_suite_skipped_without_SOW_TEMPORAL`** — Asserts (implicit via `describe.skipIf(!SOW_TEMPORAL)`): default `pnpm test` needs no Temporal server. Why: the default-suite-never-needs-Temporal convention (`temporalGate.ts`).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — reuses the frozen `SourceEnvelope` / `RegisterSourceInput` / `KnowledgeMutationPlan` / `ProposedAction` contracts.
- **Orchestrator doc rows to write hot (Step 9 routing):** none anticipated (no contract/field change). If the activity-registration surface (`ProofSpineActivities`/`ProofSpineParams`) is treated as a documented seam, flag it — likely just a plan tick + Log entry.
- **§2.5-seam (shared-contract) model touched?** No — no Appendix-A field add/remove; no schema-snapshot test required.

## Things to flag at Step 2.5
1. **Sandbox bundle — extend the existing proof-spine bundle vs a new bundle?** My default vote: **extend the existing `workflows.ts` proof-spine bundle** — `sourceIngestion` is a peer driver; reuse the proven `PROOF_SPINE_IGNORE_MODULES` + `proofSpineWebpackConfigHook` wiring; a second bundle path is avoidable complexity.
2. **Params — extend `ProofSpineParams` vs a dedicated `SourceIngestionParams`?** My default vote: **a dedicated additive `SourceIngestionParams(Input)` composed alongside** (source ingestion has distinct job inputs — sourceRef/routing/extraction) so the meeting-params cohesion isn't diluted; wired through the same `buildProofSpineActivities` assembly.
3. **How much of the pipeline to exercise live, and which terminal state to assert?** My default vote: **the full happy path to whatever deterministic terminal state the existing `source-fakes.ts` leaves produce** (proves the whole spine executes), plus the rejected-source park path — assert the state the fakes already yield, don't invent new fake behavior.
4. **The commit leaf in C1 — deterministic fake (no vault write) or the proof-spine's real local KnowledgeWriter-over-tmpdir?** My default vote: **the deterministic fake — NO real vault write in C1.** C1 proves Temporal orchestration only; a real local KnowledgeWriter commit is a real (local) semantic write and a separate owner-visible decision — keep it out of the smallest first slice. (If the implementer finds the deterministic happy path can't reach a terminal state without a commit port that returns a committed revision, use a fake that RETURNS a deterministic `CommittedRevision` without touching the fs — flag if that's not already what `source-fakes.ts` does.)

## Dependencies + sequencing
- **Depends on:** task 7.7 (the pure `sourceIngestion` driver — done + unit-tested); the proof-spine live-worker harness (`registerWorker.ts` / `proof-spine.test.ts` — done).
- **Blocks:** C2 (real `node:fs` file connector leaf swaps into this live workflow); C3 (production boot dispatch).

## Estimated commit count
**1.** One focused slice — register the workflow + wire its activity set + the gated live test. It touches the safety-adjacent worker boundary (§16 never-throw, the candidate-data `registerSource` gate) → **mandatory adversarial review (security + code-quality) before ship**, but it is one logical unit and commits once at Step 10.

## Lessons-logged candidates anticipated
- **Convention candidate** — "a make-it-real live-Temporal activation reuses the proof-spine bundle + `buildRegisteredActivities` assembly; only the already-real gate runs for real, every other leaf stays the deterministic fake (guardrail-3 shape)."
- **Architecture-doc note candidate** — the `ProofSpineActivities`/`ProofSpineParams` surface now carries a second live workflow (source ingestion), not just meeting-closeout — a documented registration seam consumers extend per driver.
- **Future TODO — operational** — C3 production boot dispatch needs a running local Temporal dev-server; the default degraded-boot path must stay safe when none is running.

## How to invoke
1. **Read this brief end-to-end** — especially "Things to flag at Step 2.5" (the commit-leaf question #4 is the safety-scope one).
2. **Run `/tdd sourceIngestion_live_local_temporal`** in the implementer session.
3. **Step 0 (Restate)** — confirm the restatement matches the Feature line (live activation; leaves stay faked; only `registerSource` real).
4. **Step 2.5** — ping back with answers (or take defaults). Do not proceed to GREEN until orchestrator signs off.
5. **Step 8** — mandatory adversarial review (security + code-quality), this is a real-I/O + safety-adjacent slice.
6. **Step 9** — surface anything outside the anticipated lessons-logged candidates.
