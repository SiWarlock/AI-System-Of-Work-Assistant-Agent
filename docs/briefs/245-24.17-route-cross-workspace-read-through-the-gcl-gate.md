# /tdd brief — route_cross_workspace_read_through_the_gcl_gate

## Feature

**Task 24.17.** The cross-workspace read path positioned to be wired **bypasses the GCL Visibility Gate entirely** and omits the ceiling re-derivation. This slice closes that — or deliberately keeps two implementations and makes them agree, with the reason recorded.

⛔ **SAFETY RULE 4, and it is the HIGHEST-severity finding either round of 24.6 produced.** `security-reviewer` is **mandatory**; Step 9 routes to the **LEAD**.

## Use case + traceability
- **Task ID:** 24.17 (worker)
- **Architecture sections it implements:** `ARCHITECTURE.md §5` (WS-8) · `§6` · `§11`
- **Safety rule:** **4** (workspace isolation — the GCL Visibility Gate is its single sanctioned cross-workspace read path)
- **Related context:** `IMPLEMENTATION_PLAN.md` `### 24.17` · `docs/audits/003-…` finding **F13** · contracts **L123**
- ⚠ **This brief widens phase scope because** Phase 24's declared anchors (`§13 §19.11 §3 §4 §6`) do not include **`§5`**, where the invariant lives. ⛔ **NOT resolved by adding `§5` to the phase's anchor set** — standing ruling: *an anchor set that grew because a linter demanded it is worthless.*

## ⛛ PREMISES — VERIFIED vs UNVERIFIED (contracts L122)

**VERIFIED — read at HEAD by the orchestrator; cite these, do not re-derive:**
1. `apps/worker/src/composition/crossWorkspaceRead.ts` imports **only** `@sow/contracts` and `@sow/db`. **It does not import the gcl module at all.**
2. It reuses **only** the raw-content-shape leg (`GclProjectionSchema.safeParse`), **never `validateProjectionVisibility` / `isWithinDefault`** — the ceiling leg.
3. ⭐ **`:66` is `if (projection.visibilityLevel !== link.scopeVisibilityLevel) continue;` — equality against the link's FROZEN scope field, set at approval time.** ⇒ **tightening a source workspace's `defaultVisibility` does not move the link.** A claim that cannot move when its governing state moves.
4. ⭐ **Every non-test reference to `admitProjection` / `serveProjection` / `guardCrossWorkspaceRawRead` / `CrossWorkspaceLinkMap` outside `packages/knowledge/src/gcl/` is a COMMENT** — `buildGclProjection.ts`, `scopedRetrieval.ts`, `ports/dailyBrief.ts`, `ports/copilotQa.ts`. **Four files assert in prose that the gate is the path; none calls it.**
5. `packages/knowledge/src/gcl/projection.ts:50-62`'s `serveProjection` exists to be exactly this defence-in-depth re-gate **and is never called.**
6. ⚠ **The WRITE side is already live at boot (`boot.ts:1720`); the READ consumer is un-dispatched (Phase 25.2/25.4).** ⇒ **nothing crosses today.** ⛔ **Under the threat-model ruling that is the wrong reason to close it** — the read side is fully built and the natural candidate to wire is the bypass.

**⛛ UNVERIFIED — establish before Step 1; flag at Step 2.5 if false:**
- **(U1)** Can `apps/worker/src/composition/` import from `@sow/knowledge` without a layering violation? **I believe yes** — composition is where knowledge is wired — **but confirm against the §2.5 import-direction rule and the area's forbidden patterns before building on it.** If it cannot, option (i) is off the table and that is a Step-2.5 finding.
- **(U2)** Is the **current source workspace** (with its live `defaultVisibility`) reachable at `resolveApprovedCrossWorkspaceSlice`'s call site? **The whole ceiling re-derivation depends on it.** If only the frozen link row is in scope, say so — that changes the fix.
- **(U3)** Do `@sow/db`'s `CrossWorkspaceLinkRow` and `@sow/policy`'s `ApprovedLink` model the **same** concept with different shapes, or genuinely different ones? ⭐ **This decides whether option (i) is a merge or a rewrite.**
- **(U4)** WS-1 reported these call-graph facts **grep-grade** (its `codegraph` tools were unavailable — it said so rather than substituting silently). **I re-verified premises 1–5 myself. Re-verify anything you build on that I did not list.**

## The fork — named, not pre-ruled

**Option (i) — COLLAPSE TO ONE IMPLEMENTATION.** Route the per-row check through `serveProjection` / `validateProjectionVisibility` **against the CURRENT source workspace**. ⭐ **Closes the ceiling gap and makes the "single path" claim true.** Cost: a new cross-package dependency (U1) and possibly reconciling two link models (U3).
**Option (ii) — KEEP BOTH, MAKE THEM AGREE.** Re-derive the link's scope bounds against the **live** workspace default at every read, and **record why two independent approval representations exist** as a decision rather than leaving it as drift.

⭐ **My lean is (i)** — two implementations of one safety invariant is the condition that produced this finding, and *"never a second minting path"*'s read-side analogue. ⚠ **But (ii) is a legitimate answer if (U1) or (U3) makes (i) a rewrite rather than a merge** — a rushed merge of two safety predicates is worse than two that provably agree. **Choose at Step 2.5 with the code in front of you and say why.**

## ⛔ REQUIRED REGARDLESS OF THE OPTION — the prose

**The four comments in premise 4 must stop asserting a path that is not called.** Either they become true (option i wires it) or they are corrected to describe what actually happens. ⛔ **A comment describing an unimplemented architecture is this audit's own mechanism, and leaving four of them standing while fixing the code would be the fix that preserves the defect's cause.**
⚠ **`ports/dailyBrief.ts` and `ports/copilotQa.ts` are `packages/workflows` — yours. `scopedRetrieval.ts` and `buildGclProjection.ts` likewise.** If any comment sits outside your territory, **flag it at Step 9 rather than reaching.**

## Acceptance criteria (what "done" means)

- [ ] The cross-workspace read path's visibility check is **derived from the CURRENT source workspace**, not from a value frozen at approval time.
- [ ] ⛔ **Verified by MOVING THE GOVERNING STATE: a test that tightens a source workspace's `defaultVisibility` and asserts a previously-crossing projection now stops crossing.** ⭐ **This is the bullet that distinguishes the fix from the defect — the defect is precisely that the claim cannot move when the state moves, so a test that never moves the state cannot tell them apart.**
- [ ] Non-vacuity: a projection **within** the current ceiling still crosses. Both directions, or neither is a control.
- [ ] The existing guarantees are preserved, not merely unbroken — **the read-back workspace identity re-gate (`:63`) and the sanitizer fail-closed (`:69-72`) still hold**, pinned.
- [ ] Either the four prose comments are true, or they describe what actually happens.
- [ ] If option (ii): the reason two approval representations exist is **recorded in code**, not left implicit.
- [ ] Rule 7: nothing surfaced carries raw content, a path, or a workspace-identifying fragment beyond what already crosses.
- [ ] Repo-wide typecheck green across all 11 packages.

## Wiring / entry point (Step 7.5)

⚠ **`resolveApprovedCrossWorkspaceSlice` has NO production consumer yet** (Phase 25.2/25.4). ⇒ **declare `"none — consumer lands in 25.2/25.4"` explicitly rather than leaving Step 7.5 blank.** ⛔ **Do NOT wire a consumer in this slice** — that is a different task with its own gate, and building it here would arm a cross-workspace read path inside a hardening fix.

## Files expected to touch
`apps/worker/src/composition/crossWorkspaceRead.ts` (primary) · possibly `crossWorkspaceLink.ts` · `packages/workflows/src/{activities/scopedRetrieval.ts,activities/projections/buildGclProjection.ts,ports/dailyBrief.ts,ports/copilotQa.ts}` (comments) · tests in `apps/worker/test/**` + `packages/workflows/test/**`.
⛔ **NOT** `packages/knowledge/src/gcl/**` — ⭐ **that surface was audited and found SOUND; the defect is that it is bypassed, not that it is wrong.** Importing from it is fine (U1); editing it is a Step-2.5 finding. ⛔ **NOT** `packages/policy/**`.

## RED test outline (Step 2)
1. ⭐ **The governing-state test (the load-bearing one):** approve a link, confirm a projection crosses, **then tighten the source workspace's `defaultVisibility`** and assert it stops. **RED before the fix.**
2. Non-vacuity partner: within the current ceiling, it still crosses.
3. The read-back identity re-gate still refuses a row from another workspace.
4. A raw-content-shaped row still fails closed with the whole read withheld.
5. **Mutation-verify test 1** by reverting to the frozen-field comparison and confirming it goes green — **the discriminating power, not the presence.**

## Things to flag at Step 2.5
- (U1)–(U4) with the `file:line` that settles each.
- **(i) or (ii), and why**, with the layering answer.
- The coverage map: each acceptance bullet → its covering test or a `not-tested-because:`.
- ⭐ **Anything above the code contradicts.** Three times this round an implementer falsified a recorded premise from source — including two of mine. **Do it again if it applies.**

## Dependencies + sequencing
**Depends:** none blocking. **Blocks:** Phase 25.2/25.4's read consumer; and it is the **outcome** of arming-block condition (b).
⚠ **Concurrent:** knowledge in `packages/knowledge` on 24.12; providers-integrations in `packages/providers`/`policy`/`integrations`. **Your territory is `apps/worker` + `packages/workflows` + `packages/db`.**

## Estimated commit count
**1** — unless (U3) forces reconciling two link models, which is a second commit and needs my sign-off at Step 2.5 first.

## Lessons-logged candidates anticipated
The *"two implementations of one safety invariant, and the prose points at the one that is not wired"* shape is likely to generalise — flag it at Step 9.

## How to invoke
`/tdd docs/briefs/245-24.17-route-cross-workspace-read-through-the-gcl-gate.md`
