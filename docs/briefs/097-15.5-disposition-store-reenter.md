# /tdd brief — durable_disposition_store_and_reenter (15.5)

## Feature
Replace the **in-memory** ingestion disposition machinery with the durable operational store + real read seams (closes **G5**): (1) a durable `@sow/db` **SourceDisposition** store (dual-dialect, mirror 15.4's pattern) replacing `makeDispositionStore`'s in-memory CAS map; (2) a **real `isParked`** derived from that store (today hardwired `true`); (3) a **working parked-source read seam** (`parkedReader.read`, today always `err(source_unavailable)`); (4) a **re-enter runner** that reads a parked source back and re-drives the 7.7 ingestion pipeline **reusing the same idempotencyKey** (replay-safe — safety **rule 3** / inv-D). Worker/db-track; **ISOLATED (rule-3 replay-safety + WS-8, security-reviewer=invariant)**.

## Use case + traceability
- **Task ID:** 15.5
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (primary — parked sources re-enterable / ING-4 dead-end closed), `§9` (workflows — the ingestion-inbox re-classify + re-enter, replay-safe), `§4` (operational store). Safety **rule 3** (replay-safe idempotency — a re-entered run reuses the envelope, no duplicate side effect).
- **Depends on:** P2 (§4 storage), 15.1 (the C1 spine). Adjacent to 15.4 (same operational-store pattern).
- **Related context (confirm at Step 1 — you own this spine; you built C1 + 15.4):**
  - **The in-memory store (buildActivities.ts:808 `makeDispositionStore`):** CAS-inserts by the channel-free disposition key, mints an audit ref (nothing silent), exactly-once by key (a HIT reuses the prior audit ref — inv-A/inv-B); `isParked` is a stub `true`. Make it a durable dual-dialect `@sow/db` `SourceDispositionRepository` (mirror 15.4's SeenContentHashRepository: db-owned Row DTO, migration, ONE repo-contract suite both dialects, fail-closed both directions).
  - **The parked reader stub (buildActivities.ts:563 `parkedReader.read`):** today always `err(source_unavailable)` ("carry-forward: the real parked-source read seam"). Make it real — read the parked source back so `rescopeSource` (inv-C, owner override) + the re-enter runner can re-drive it. **The crux (Q1): where is the parked SourceEnvelope persisted so the reader reads it back?** — confirm at Step 1.
  - **The re-enter runner (buildActivities.ts:576 `reenterIngestion`):** re-drives the 7.7 pipeline REUSING the same idempotencyKey (inv-D). Make it work end-to-end over the durable store + real reader — a re-entered source that already committed REPLAYS (no duplicate note/write, rule 3).
  - **The disposition activities already exist:** `RecordDispositionPort` / `triageRecordDisposition` / `triageRescopeSource` (workflows.ts:348-352). This slice makes their STORE + READER durable/real, not the activity surface.
  - **WS-8 nuance:** a source parks in `queued_for_review` with its workspace STILL unbound (SourceIngestionContext.workspaceId is absent pre-classification). So the disposition store keys on the SOURCE identity, not a workspace; a rescope binds the workspace on re-enter. Confirm the key + how WS-8 applies to a pre-classification parked source (a rescope's owner-supplied workspace must be registry-validated — never a raw cross-workspace bind).

## Acceptance criteria (what "done" means)
- [ ] A durable `@sow/db` **SourceDisposition** table + dual-dialect migration + a `SourceDispositionRepository` passing the ONE repo-contract suite on BOTH dialects, replacing the in-memory `makeDispositionStore`. Exactly-once by the disposition key (a re-record HIT reuses the prior audit ref — inv-A/inv-B; first-write-wins).
- [ ] **Real `isParked`** derived from the store (a source is parked iff the store says so) — no longer the hardwired `true`.
- [ ] **Working parked-source read seam** (`parkedReader.read`) — reads a parked source back (from wherever the parked SourceEnvelope is persisted — Q1); a genuinely absent source is a typed not-found, distinct from a store fault.
- [ ] **Re-enter is replay-safe (rule 3 / inv-D):** the re-enter runner re-drives ingestion REUSING the same idempotencyKey, so a source that already committed does NOT produce a duplicate note / duplicate external write (the durable revision store / envelope replays). Pin the replay leg.
- [ ] **Rescope owner override (inv-C):** `rescopeSource` reads the parked source through the real reader and applies the owner's workspace/project override, preserving contentHash (inv-D). The owner-supplied workspace is registry-validated (WS-8 — never a raw/unregistered bind).
- [ ] **Fail-closed both directions (worker Lesson 3):** a disposition-store fault on record / isParked / read is a typed err — never masked as a false not-parked / false not-found / silent ok (a fault is distinguishable from a benign not-parked / not-found).
- [ ] `/preflight` clean (worker + repo-wide; repo-contract both dialects).

## Wiring / entry point (Step 7.5)
The durable store + real `isParked` + real `parkedReader` are consumed by `recordDisposition` / `rescopeSource` / `reenterIngestion` in the C1 source-ingestion binding (buildActivities.ts). Reachable through the C1 spine's triage/re-enter path (confirm at Step 7.5 which tests exercise it — `sourceIngestion-live` / the triage activity tests). **State: reachable via the C1 disposition/re-enter path** (name the exact site in Step-9); if a leg is only reachable via a dormant path, waiver per L11.

## Files expected to touch
**New:** `packages/db` disposition table (sqlite + pg) + migration; `SourceDispositionRepository` interface + both adapters; repo-contract registration; tests.
**Modified:** `apps/worker/src/composition/buildActivities.ts` (:808 durable store, :563 real parkedReader, real `isParked`; the re-enter runner wiring if needed).
If the parked-source persistence needs a field/store beyond this (Q1), **flag at Step 2.5**.

## RED test outline (Step 2)
1. `disposition_repo_contract_both_dialects` — record/read round-trip on both dialects; exactly-once by key (a re-record HIT reuses the prior audit ref, no duplicate). (§4)
2. `migration_up_down` — applies + reverts with the version guard. (forbidden-#4)
3. `is_parked_reflects_the_store` — `isParked` true iff a parked disposition exists; false otherwise (not the hardwired `true`). (§19.2)
4. `parked_reader_reads_back_a_parked_source` — the real reader returns a parked source; a genuinely absent source is a typed not-found (distinct from a store fault). (G5)
5. `reenter_reuses_idempotency_key_replay_safe` — re-entering an already-committed source re-drives with the SAME idempotencyKey ⇒ NO duplicate note/external write (replay). (rule 3 / inv-D)
6. `rescope_applies_owner_override_registry_validated` — rescope applies the owner workspace/project override, preserves contentHash; an unregistered workspace override is rejected (WS-8). (inv-C)
7. `disposition_faults_are_typed_errs` — a store fault on record / isParked / read ⇒ typed err, never masked. (Lesson 3)

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model:** NEW — `SourceDisposition` (db-owned operational record; NOT a frozen Appendix-A contract). Orchestrator writes the §4/§19.2 arch note + any worker cross-doc note at round close (mirror 15.4's SeenContentHashRow handling).
- **Shared-contract seam touched?** No → no schema-snapshot test (unless a shared port type changes — flag if so).

## Things to flag at Step 2.5
1. **Parked-source persistence + read source (LOAD-BEARING).** To re-enter, the runner needs the parked SourceEnvelope back. Where is it persisted — does the SourceDisposition store persist the SourceEnvelope (or a ref/payload) so `parkedReader.read` reads it back, or is the source read from another store? My default vote: persist enough of the parked source (the SourceEnvelope, or a durable ref that resolves it) in/alongside the disposition store so re-enter is self-contained; confirm the cleanest reachable source. Do NOT re-enter around the candidate gate (a re-entered source re-drives THROUGH the pipeline's gate, rule 2).
2. **Disposition key + WS-8 for a pre-classification parked source.** The channel-free disposition key keys on the source identity (workspace unbound at park time). Confirm the durable key + that a rescope's owner-supplied workspace is registry-validated (never a raw/unregistered bind), and the disposition is WS-8-safe once bound.
3. **Re-enter idempotency (rule 3).** Confirm the idempotencyKey reuse (inv-D) makes a re-entered already-committed source replay (no duplicate side effect) — the durable revision store (15.4-adjacent) / external-write envelope carries the exactly-once.
4. **Commit split.** May split into (a) the durable store + isParked + reader, (b) the re-enter runner wiring. Your call; keep both under the isolated security=invariant slice.

## Dependencies + sequencing
- **Depends on:** P2 (§4 storage), 15.1 (C1 spine). Same operational-store pattern as 15.4 (`19e626fe`).
- **Blocks:** the human routing-resolution loop (15.8 reroute-to-project) builds on a real disposition/re-enter; 15.9 (meeting dispatcher) is adjacent. Does NOT gate them.

## Estimated commit count
**1–2.** The durable store (table + migration + repo-contract) may split from the re-enter-runner wiring. **security-reviewer = invariant** (rule-3 replay-safety on re-enter + WS-8 registry-validated rescope + fail-closed store); code-quality every-slice. **Pure-build — NO hard line** (WS-8-scoped operational store; the re-enter reuses the existing idempotency substrate; no external egress/spend).

## Lessons-logged candidates anticipated
- **Convention candidate** — a durable ingestion-disposition store is a first-write-wins operational `@sow/db` repo (exactly-once by key, audit-ref-minted, fail-closed both directions distinguishing fault from not-parked/not-found — extends Lesson 3); a re-enter runner re-drives THROUGH the pipeline gate reusing the idempotencyKey so an already-committed source replays (no duplicate side effect, rule 3), and a rescope's owner workspace override is registry-validated (WS-8, never a raw bind).

## How to invoke
1. Continue the worker-impl2 session (oriented from 15.1/15.3/15.4 — no `/session-start` unless swapped).
2. Read this brief end-to-end (Q1 parked-source persistence + Q3 re-enter idempotency are load-bearing).
3. `/tdd durable_disposition_store_and_reenter`.
4. Step 1 — confirm the parked-source read source, the disposition key + WS-8 for a pre-classification park, and the idempotencyKey reuse path.
5. Step 2.5 — ping back the design answers (Q1 is load-bearing). Don't GREEN until I sign off.
6. Step 9 — categorized flags; surface the `SourceDisposition` cross-doc + any shared-port change.
