# /tdd brief — make_workspacepathcheck_required

## Feature

`### 24.26` **step 3 of 3 — the CONTRACT step.** Make `KnowledgeWriterDeps.workspacePathCheck` **required**, delete the `?? enforceWorkspacePathScope` fallback and the knowledge-side `LEGACY_UNPREFIXED_WORKSPACE_ID`, so the exempt workspace id has exactly **one** home and "two different exempt workspaces" becomes unrepresentable rather than merely fail-closed.

## Use case + traceability

- **Task ID:** `### 24.26` (step 3 of 3)
- **Architecture sections it implements:** `ARCHITECTURE.md §16` · Safety **rule 4** (defense-in-depth for the workspace-path guard)
- ⚠ **`spec-lint` scope note:** §16 sits outside Phase 24's declared anchor set (`§19.11 · §6 · §13 · §4 · §3`), so this brief will need the `widens phase scope because…` escape. **That is `### 24.54`, not a real scope widening** — the phase's declared set does not describe the phase. Do not read the escape as this slice reaching outside its phase.
- **Related context:** brief `271` (step 1) · brief `274` (step 2) · session doc `163` · `### 24.61` (blank-id tripwire — see Q3)

---

## ⛔⛔ READ THIS FIRST — THE STATE YOU INHERIT, AND THE ONE WAY THIS SLICE GOES WRONG

**Steps 1 (`e1293226`) and 2 (`e8ffd7a7`) have LANDED. You inherit a three-copies state; you do not create it.**

⛔⛔ **`LEGACY_UNPREFIXED_WORKSPACE_ID` NOW NAMES **TWO DIFFERENT CONSTANTS** IN TWO PACKAGES. THIS IS THE HIGHEST-RISK FACT IN THE SLICE.**

| Constant | Fate in step 3 |
|---|---|
| `packages/knowledge/src/knowledge-writer/workspace-path-guard.ts:78` | ⛔ **DELETE — this is your target** |
| `apps/worker/src/composition/legacy-workspace.ts:39` | ✅ **SURVIVES — it becomes the value's single home** |

⭐ **The worker const is the DESTINATION of this migration, not a casualty of it.** Its own file comment (`legacy-workspace.ts:23-28`) says so explicitly. **Deleting it would undo step 2 and break both production call sites.**

⛔ **After step 2 the value has three homes and only TWO are live.** The supplied check wins over `writer.ts:225`'s `??`, so **the knowledge-side constant already governs ZERO production write paths.** Typecheck is green, zero tests fail, nothing looks wrong — **which is exactly why this step is separately tracked: a round that stopped after step 2 looks finished and is not.**

---

## Premises — all verified at source by the orchestrator at HEAD `20df48c7`. Falsify freely; that is the job.

⚠ **Four line numbers in the tracker and in brief `271` are STALE — step 1 grew the guard file by ~97 lines. These are re-measured:**

| Site | Tracker said | **Actual** |
|---|---|---|
| knowledge constant | `:64` | **`workspace-path-guard.ts:78`** |
| `enforceWorkspacePathScope` const | `:86` | **`workspace-path-guard.ts:183`** |
| `makeEnforceWorkspacePathScope` factory | — | **`workspace-path-guard.ts:143`** |
| worker deps literal (proof spine) | `buildActivities.ts:575` | **`buildActivities.ts:577`** (field at `:594`) |
| worker deps literal (semantic approval) | `semanticApprovalDispatch.ts:61` | **`semanticApprovalDispatch.ts:63`** (field at `:76`) |
| optional field / the `??` | `writer.ts:157` / `:225` | ✅ **both exact** |

**Verified true:**
- `writer.ts:157` — `readonly workspacePathCheck?: WorkspacePathCheck;`
- `writer.ts:225` — `const workspaceScope = deps.workspacePathCheck ?? enforceWorkspacePathScope;`
- `workspace-path-guard.ts:183` — `export const enforceWorkspacePathScope: WorkspacePathCheck = makeEnforceWorkspacePathScope(LEGACY_UNPREFIXED_WORKSPACE_ID);` ⇒ **its initializer IS your deletion target. See Q1 — this is the slice's real design question.**
- **Barrel guarantee HOLDS.** `packages/knowledge/src/index.ts:41` named-exports **only** `makeEnforceWorkspacePathScope`; there is no `export *` from that module. Neither the constant nor `enforceWorkspacePathScope` is public surface ⇒ **deleting them is an internal change.**
- ⛔ **BUT THE FIELD IS NOT INTERNAL.** `packages/knowledge/src/index.ts:27` is `export * from "./knowledge-writer/writer"`, which **does** publish `KnowledgeWriterDeps`. **Flipping `workspacePathCheck` optional→required IS a public-surface change of `@sow/knowledge`** (in-repo consumers: `packages/workflows`, `apps/worker`). ⚠ **Do not carry step 1's "not a public-surface break" reassurance across to the field — it was true of the constant and is false of the type.**

## ⛔ TWO IN-FILE COMMENTS ARE FALSIFIED BY `e8ffd7a7` AND BOTH ARE YOURS TO FIX

**(1) `workspace-path-guard.ts:73-76`** — routed to you by worker at its Step 9. Verified verbatim; the falsification claim is **CORRECT but under-reports**:
- *"THIS CONSTANT IS STILL THE ONLY SOURCE IN PRODUCTION"* — **FALSE.** Both production deps literals now build from the *worker* const.
- *"NOTHING supplies an exempt id from a composition root"* — **FALSE.** Two do.
- *"`writer.ts:225` defaults to the instance built from it"* — literally true, **materially misleading**: zero production callers take that default. One *test* caller does.
- ⚠ Its own framing says *"UNTIL STEP 3 LANDS"* — but **step 2** is what falsified it.

**(2) ⛔ `workspace-path-guard.ts:178-182` — A SECOND FALSIFIED BLOCK, NOT RECORDED ANYWHERE UNTIL NOW.** It reads: *"Reached from `writer.ts:225` as the default when `deps.workspacePathCheck` is absent — **which is every production call site today**."* ⇒ **that is now ZERO production call sites — a more direct falsification than `:73-76`.** ⭐ **Found only because the enumeration was run by concept rather than by re-reading the one comment someone had already flagged.**

⚠ **Two more go stale when the fallback dies (fix in-slice):** `writer.ts:128` / `:156` (*"Default: the real predicate"*) and `packages/knowledge/src/index.ts:39-40` (*"its only consumer is `writer.ts`'s own default"* — a dangling reference once the default is gone).

---

## Acceptance criteria

- [ ] `KnowledgeWriterDeps.workspacePathCheck` is **required** (`writer.ts:157`) — the `?` is gone.
- [ ] `writer.ts:225`'s `?? enforceWorkspacePathScope` fallback is **deleted**; `deps.workspacePathCheck` is used directly.
- [ ] `packages/knowledge/src/knowledge-writer/workspace-path-guard.ts:78`'s `LEGACY_UNPREFIXED_WORKSPACE_ID` is **deleted**. ⛔ **`apps/worker/src/composition/legacy-workspace.ts:39` is UNTOUCHED.**
- [ ] **A `KnowledgeWriterDeps` literal omitting `workspacePathCheck` fails `tsc`** — pinned by a `@ts-expect-error` that goes **unused (`TS2578`) if the field is ever widened back to optional**. ⭐ **This is the slice's load-bearing pin** (`contracts L80`'s mutation-proved shape). A behavioural test cannot express it.
- [ ] **The inherited end-to-end tests now MEAN something** — an exempt-workspace unprefixed commit succeeds and a non-exempt one rejects, **and a wrong supplied id now changes behaviour** (at step 2 it could not; the fallback was the same factory over the same string).
- [ ] All four falsified/stale comments above are corrected **or deleted** — no comment left asserting a property the code no longer has (`L82`).
- [ ] `/preflight` clean, **per-package scoped**.
  ⛔ **THE TREE IS NOT GREEN AND THIS BRIEF DOES NOT CLAIM IT IS.** Known red, published by **name**: `apps/desktop/test/bundle/main-bundle-resolution.test.ts` — pre-existing, unowned, `### 24.25`. **Zero failing TESTS.** Any *other* failing test, or a different name, is **not** covered — investigate it.

## Wiring / entry point (Step 7.5)

**Already live — this slice removes an alternative, it does not add a path.** Entry point is `applyPlan(command, deps)` (`writer.ts:219-222`); the guard runs as **applyPlan step 4.5** (`writer.ts:261-271`), a per-changed-file loop short-circuiting to a `WorkspacePathViolation`. Production reach: `createCommitActivity` (`packages/workflows/src/activities/commitKnowledge.ts:110`) → `deps.applyPlan(...)` at **`commitKnowledge.ts:147`**, from three call sites — **`buildActivities.ts:596`**, **`buildActivities.ts:1087`** (both reusing the `:577` literal), **`semanticApprovalDispatch.ts:86`**. *(Brief `271`'s `:584`/`:1075`/`:77` have all moved.)*

⛔ **Confirm the guard is reached from a production entry point, not only from tests** — `buildActivities.ts:1087` is the real KnowledgeWriter `sourceCommit` path.

## Files expected to touch

**Modified:**
- `packages/knowledge/src/knowledge-writer/writer.ts` — field required (`:157`), fallback deleted (`:225`), stale comments (`:128`, `:156`)
- `packages/knowledge/src/knowledge-writer/workspace-path-guard.ts` — delete `:78`; fix `:73-76` and `:178-182`; resolve `:183` per **Q1**
- `packages/knowledge/src/index.ts` — the `:39-40` comment's dangling reference
- `packages/knowledge/test/workspace-path-guard.test.ts` — **the one construction site that reds** (see below)
- possibly `packages/knowledge/test/writer.test.ts` — the `@ts-expect-error` pin

⛔ **Do NOT touch:** `apps/worker/**` (worker territory; step 2's const is finished work) · `apps/desktop/**` · `packages/workflows/**` (see **Q2**).

⚠ **Per `L81` the EXHAUSTIVE blast radius is the typecheck OUTPUT, not this list.** Run `pnpm typecheck` inside your slice and reconcile against the census below. If it disagrees, **the typecheck is right** — flag the delta at Step 9.

### Census (orchestrator-measured, read-not-compiled — reconcile it, don't trust it)

**12 `KnowledgeWriterDeps` construction sites across 8 files. Exactly ONE should red:**
- **Production (2, both already supply → no red):** `buildActivities.ts:577` · `semanticApprovalDispatch.ts:63`
- **`packages/knowledge` tests (5, four already supply → no red):** `writer.test.ts:35` + `:118` · `secret-scan.test.ts:38` · `region-marker-neutralize.test.ts:145`
- ⛔ **THE ONE RED — `packages/knowledge/test/workspace-path-guard.test.ts:31`**, which omits the field **deliberately**, documented at `:39`: *"workspacePathCheck deliberately OMITTED — every test in this file exercises the REAL default."* ⭐ **The single typecheck failure is the file whose entire purpose is exercising the default you are deleting.** That is not an obstacle — **it is the slice's meaning.** After step 3 there is no default to exercise; those tests must be re-pointed at the factory.
- ⚠ **`packages/workflows` (5 sites, `commit-activity-base-revision.test.ts:32` + `meeting-activities.test.ts:738/756/784/802`): all `deps: {} as never`.** `never` is assignable to everything ⇒ **these give the change ZERO coverage. They will not red, will not need touching, and will not detect the flip.** See **Q2**.

## RED test outline (Step 2)

⛔ **You INHERIT the real tests — do not write them fresh.** The plan-driving pins that were **vacuous at step 2** (proven over a 3,588-case diff, zero divergences) become **genuine here**, because the fallback that made supplying and omitting indistinguishable is gone.

1. **`omitting_workspacepathcheck_fails_typecheck`** — a `@ts-expect-error` on a `KnowledgeWriterDeps` literal without the field.
   - Asserts: compiles today; the directive goes **unused (`TS2578`)** the moment the field is optional again.
   - Why: the Done-when's *"a call site omitting the check fails typecheck"* — **the only criterion no runtime test can express** (`contracts L80`).
2. **`exempt_workspace_commits_unprefixed`** — drive a plan for the supplied exempt workspace at an unprefixed path.
   - Asserts: `ok`.
3. **`non_exempt_workspace_rejects_unprefixed`** — the control (`L80`).
   - Asserts: `err(workspace_path_violation)`.
4. **`a_different_supplied_id_changes_behaviour`** — the same plan under a **deliberately different** exempt id.
   - Asserts: rejects. Why: ⭐ **this is the test that was IMPOSSIBLE at step 2** — with the fallback gone, the supplied value is the only source, so behaviour finally tracks it. **This is how you prove the migration actually completed** rather than asserting it.
5. **The re-pointed `workspace-path-guard.test.ts` cases** — its four `enforceWorkspacePathScope` calls (`:212`, `:218`, `:223`, `:236`) resolve per **Q1**.

⚠ **Mutation-verify the pins rather than asserting them** (step 1 did; `contracts L90` — an unproven pin is not a pin). At minimum: widen the field back to optional ⇒ test 1's directive goes unused; feed a wrong id ⇒ test 4 reds.

## Cross-doc invariant impact

- **Model field changes:** `KnowledgeWriterDeps.workspacePathCheck` optional → **required** (a public dep-surface change — see Premises).
- ⛔ **ORCHESTRATOR-RESOLVED, DO NOT CHASE AT STEP 9 — and the tracker's claim here was wrong.** The tracker says step 3 *"OWES a cross-doc row."* **Measured: there is no row to update.** `workspacePathCheck` / `KnowledgeWriterDeps` / `WorkspacePathCheck` are **ABSENT** from `ARCHITECTURE.md` Appendix A (36 rows, `:771-810`), **ABSENT** from `packages/contracts/CLAUDE.md`'s cross-doc table (`:132-162`), and `packages/knowledge/CLAUDE.md`'s table (`:121-131`) is **still the untouched `<model> | §X` template**. ⇒ ***the obligation is to CREATE a mirroring relationship this type has never had, not to update one*** — a scope decision, and mine. ⭐ **I am deliberately NOT manufacturing a row to satisfy an unverified claim** — that is the shape the lead already ruled against (widening a declared set because a check demanded it). **Flag the field change at Step 9 as you normally would; the row decision is not yours and is not a blocker.**
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No — `KnowledgeWriterDeps` is a DI seam, not an Appendix-A model. That is precisely what makes the row question a real one.

## Things to flag at Step 2.5

1. ⛔ **THE REAL DESIGN QUESTION — does `enforceWorkspacePathScope` (`:183`) survive?** Its initializer **is** your deletion target, so it cannot simply stay. Once `writer.ts:225` goes, its only remaining consumers are **four calls in `workspace-path-guard.test.ts`**. Options: **(a)** delete it too and re-point those four at `makeEnforceWorkspacePathScope("personal-business")`; **(b)** keep it, inlining the literal into its initializer.
   **My default vote: (a) DELETE.** ⭐ **(b) re-creates the exact defect `### 24.26` exists to delete** — a module-level instance built from a hardcoded string *is* a second home for the value, just spelled differently. **And the test file's own comment says its purpose is exercising "the REAL default"; after step 3 there is no default, so those tests need re-pointing under either option.** ⚠ **If you disagree, say so — this is the one call I would most like challenged**, because (a) trades a named export for four literals in a test and someone may reasonably read that as worse.
2. ⚠ **The `packages/workflows` `{} as never` hole — in scope or filed?** Five sites take zero coverage from this change. **My default vote: FILE IT, DO NOT FIX IT HERE** — `packages/workflows` is worker territory and this is a safety slice that should stay minimal and bisectable. ⛔ **But per `L106` a deferral must be a TRACKED TASK IN THIS ROUND, not a prose intention** — I will file it when you confirm the count at Step 2.5. **Report the number you actually measure**; mine is read-derived, not compiled.
3. **Does `### 24.61` bite here?** ⛔ **My reading: NO, and step 2 established why** — the supplied id is a compile-time worker `const`, which cannot produce a zero-width or whitespace value, so this leg is not one of `24.61`'s *"legs that will supply it."* **That tripwire arms when the id moves to CONFIG.** ⚠ **Step 3 does not move it to config** — confirm you agree; if your implementation introduces any runtime path to that argument, **say so at Step 2.5**, because that changes `24.61`'s status.
4. **Where does the `@ts-expect-error` pin live** — `writer.test.ts` or the guard test? **My default vote: `writer.test.ts`**, co-located with the type it pins rather than with the guard's behaviour. Weak preference; take yours if the guard test reads better.

## Dependencies + sequencing

- **Depends on:** `### 24.26` step 1 ✅ `e1293226` · step 2 ✅ `e8ffd7a7` (**both verified ancestors of HEAD `20df48c7`**)
- **Blocks:** — (this closes the three-step sequence; `### 24.26` becomes tickable)

## Estimated commit count

**1.** ⛔ **Do NOT bundle.** Safety-invariant slice (rule 4) and the final leg of a three-commit sequence — bisectability is the entire point of having split it.

## Lessons-logged candidates anticipated

- **Convention candidate** — *expand/migrate/contract's CONTRACT step is where the middle leg's vacuous tests become real; inherit them, don't rewrite them.* Step 2 could not verify itself **by construction**; step 3 is where that debt is paid.
- **Convention candidate** — *a comment that names a state ("until step 3", "every production call site today") is falsified by the step that changes the state, not by the step that names it* — two such blocks here, and only one had been noticed.
- **Architecture-doc note candidate** — whether a DI seam type like `KnowledgeWriterDeps` belongs in the cross-doc mirroring set at all.

## How to invoke

1. **Read this brief end-to-end**, especially the two-constants warning and Q1.
2. Run **`/tdd make_workspacepathcheck_required`**.
3. **Step 2.5** — send the write-up with answers to Q1–Q4 and your measured census. ⛔ **My line numbers and counts are read-derived, not compiled — falsify them; four of them were already stale when I started.**
