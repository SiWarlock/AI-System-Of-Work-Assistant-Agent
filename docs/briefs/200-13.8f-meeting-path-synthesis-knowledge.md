# /tdd brief — meeting_path_living_vault_synthesis (13.8f-A)

## Feature
Two knowledge-area commits: **(1)** reframe the `rewriteVaultForSource` dormancy pin so a flag-gated production binding is permitted (unblocks the worker's 13.8d binding without a RED window); **(2) 13.8f-A** — a pure `rewriteVaultForMeeting` planner in `packages/knowledge/src/synthesis/` that turns a meeting closeout's entity candidates into entity-grounded `LinkMutation`s + `FrontmatterPatch`es (+ region-bounded person/project page patches) under the KN-10 autonomy tier, over injected ports. Ships DORMANT — the worker threads it into the meeting producer in a later slice (13.8f-B).

## Use case + traceability
- **Task ID:** 13.8f (`IMPLEMENTATION_PLAN.md` L1514-1517) — the **knowledge half**. See "Scope split" below.
- **Architecture sections it implements:** `ARCHITECTURE.md §9` workflow-1 (L297 — KnowledgeWriter meeting/project/**person**/decision/daily targets), `§6` (KN-10 tiered autonomy, KN-11 entity convergence, KN-12 structural parity), `§5` (WS-8), REQ-F-017 (no-inference)
- **Scope note — this brief widens phase scope because** 13.8f implements a §9 workflow-1 behavior (the meeting path's person/project KW targets) while phase 13's header `Spec anchors:` line omits §9 — the plan task itself cites §9 W1, so the phase header is under-anchored; the orchestrator routes that as a plan/arch doc note rather than changing this slice's scope. The `§2.5` token below is the brief template's "shared-contract seam" section name, not a claimed anchor.
- **Related context:**
  - This is an **unimplemented-SPEC gap, not a new feature**: `ARCHITECTURE.md §9` W1 names project/person/decision/daily as KnowledgeWriter targets, but the meeting producer hardcodes `linkMutations: []` and `frontmatterUpdates: []` (`packages/workflows/src/activities/buildOutputs.ts`, in the `plan: KnowledgeMutationPlan` literal) — so a meeting closeout writes ONE meeting note and never updates a person or project page.
  - The source-path analog is DONE and is your model: `rewriteVaultForSource` — `packages/knowledge/src/synthesis/ingest-rewrite.ts:92`, with `IngestRewriteInput` (`:56`), `IngestRewriteDeps` (`:66` — `gbrain` / `reason` / `sections` / `structural` / `newPlanId` / `newRunId`), `IngestRewriteReceipt` (`:76`), `StructuralMutations` (`:41`).
  - Reusable primitives already landed: `synthesis/entity-resolver.ts` (13.8a — resolve / create-stub / withhold, WS-8-scoped, fail-closed to unresolved), `synthesis/link-healer.ts` (13.8b — faithful-match only; lossy/fuzzy/2+ withheld; NO backlinks authored), `synthesis/planner.ts` (13.8c — SENSE→REASON→EFFECT), `synthesis/match-keys.ts` (the ONE shared match-key authority — do not re-mirror it).
  - Existing dormancy pin to reframe: `packages/knowledge/test/ingest-rewrite.test.ts:152` `no_production_caller`.
  - Attendees today are rendered-only free text (`packages/workflows/src/activities/projections/meetingOutputs.ts:157`, `:169-170`) — resolving them to person ENTITIES is **13.8g**, the NEXT slice, not this one. Keep the seam ready but do not build it here.

## Scope split (⚠ standing rule — no cross-area single-implementer verticals)
`IMPLEMENTATION_PLAN.md` §13.8f lists `packages/workflows/src/activities/buildOutputs.ts` in its Files, which is **worker territory**. Per the producer-first rule this slice splits:
- **13.8f-A (this brief, knowledge):** the pure meeting-path synthesis planner in `packages/knowledge/src/synthesis/`, over injected ports, with faked deps in tests. **Ships dormant.**
- **13.8f-B (worker, next round):** thread it into `buildOutputs.ts`'s meeting `KnowledgeMutationPlan` (replace the hardcoded empties), byte-equivalent while unbound/flag-OFF.

Do **not** touch `packages/workflows/**` or `apps/**` in this slice. The orchestrator will re-point the plan's §13.8f Files line to reflect the split.

## Acceptance criteria (what "done" means)

**Commit 1 — dormancy-pin reframe**
- [ ] `no_production_caller` (`ingest-rewrite.test.ts:152`) is reframed from *"zero non-test importers"* to *"every non-test importer under `apps/` or `packages/workflows/` must be an arming-gated composition site"* — and **passes in BOTH states**: zero importers (today) AND the worker's flag-gated binding (tomorrow). No RED window either side of the worker's commit.
- [ ] The reframed pin is still non-vacuous: a test-only construction proves an UNGATED importer would fail it (don't weaken it to a tautology — that silently retires the dormancy guarantee).
- [ ] The same reframing is applied to (or written into) the new 13.8f-A module's own dormancy pin, so the next binding slice doesn't repeat this dance.

**Commit 2 — 13.8f-A meeting-path planner**
- [ ] A new exported entry (e.g. `rewriteVaultForMeeting`) takes a meeting-closeout input (workspace, meeting note path/identity, entity candidates for project + decision refs, source refs, provenance origin) + injected deps, and returns a receipt carrying `KnowledgeMutationPlan`(s) — the SAME shape discipline as `rewriteVaultForSource`.
- [ ] Every emitted target path is **entity-grounded via the 13.8a EntityResolver** — resolve-or-stub; a synthesis-named non-existent path NEVER produces a fabricated file reference (the governed form of ground-before-write).
- [ ] KN-10 tier split holds: additive/derived output (new links, a `@generated` region refresh on a person/project page) ⇒ `requiresApproval: false`; an edit to a human-relevant claim (status flip, contradiction reconcile, entity merge) ⇒ `requiresApproval: true`.
- [ ] Confinement: every patch is region-bounded into a `@generated`/`kw:region`; a `@user` region is **provably never** overwritten.
- [ ] No-inference (REQ-F-017): an owner/date/priority without evidence ⇒ `TBD`/unset — never invented.
- [ ] WS-8: a foreign-workspace candidate is DROPPED; the planner issues no cross-brain read and never resolves across workspaces.
- [ ] Links follow 13.8b discipline: faithful/unambiguous matches only; lossy/fuzzy/2+-candidate withheld to a proposed clarification; **no backlink section is ever authored**.
- [ ] PURE + never-throws + fail-safe: a throwing/faulting dep ⇒ an empty receipt (no partial plan), no exception escapes.
- [ ] Flood-bound: the candidate fan-out is capped (mirror `ingest-rewrite`'s cap behavior) so one meeting can't emit unbounded mutations.
- [ ] Dormant: no `apps/` or `packages/workflows/` importer (its own reframed pin), and the module reaches Markdown only by handing plans to KnowledgeWriter (safety rule 1 — never a second writer).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — production wiring lands in 13.8f-B (worker, `packages/workflows/src/activities/buildOutputs.ts`).** This slice's reachability claim is exactly: exported from `packages/knowledge/src/index.ts`, exercised by its own suite over faked ports, and pinned dormant. Say that verbatim at Step 7.5 — do not claim a production caller it doesn't have.

## Files expected to touch
**New:**
- `packages/knowledge/src/synthesis/meeting-rewrite.ts` — the pure meeting-path planner (name it to match the neighbours if you prefer; say which at Step 2.5).
- `packages/knowledge/test/meeting-rewrite.test.ts` — the suite below.

**Modified:**
- `packages/knowledge/test/ingest-rewrite.test.ts` — the reframed dormancy pin (commit 1).
- `packages/knowledge/src/index.ts` — export the new module (mirror `:54-58`).

**Do NOT touch:** `packages/workflows/**`, `apps/**`, `IMPLEMENTATION_PLAN.md`, `ARCHITECTURE.md`, `LESSONS.md`, `*/CLAUDE.md`, `docs/briefs/`. Flag at Step 9; the orchestrator writes them.

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)

**Commit 1 —** `packages/knowledge/test/ingest-rewrite.test.ts`
1. **`dormancy_pin_allows_gated_importer`** — Asserts: a simulated importer line that IS the arming-gated composition site passes; an ungated one FAILS. Why: the pin must survive the worker binding without retiring the guarantee.
2. **`dormancy_pin_still_passes_with_zero_importers`** — Asserts: today's state passes. Why: no RED window before the worker lands.

**Commit 2 —** `packages/knowledge/test/meeting-rewrite.test.ts`
3. **`grounds_every_target_via_resolver`** — Asserts: each emitted mutation's target path came from a resolver HIT, or the candidate was stubbed/withheld; no path appears that the resolver didn't return. Why: §6 KN-10 ground-before-write / 13.8a resolve-or-stub.
4. **`unresolved_candidate_stubs_never_fabricates`** — Asserts: an unresolvable entity ⇒ a create-stub decision or withhold, never a mutation against an invented path. Why: 13.8f Done-when ("a synthesis-named non-existent path never fabricates a file").
5. **`additive_auto_human_relevant_proposes`** — Asserts: a new link / `@generated` refresh ⇒ `requiresApproval:false`; a status-flip / claim edit ⇒ `requiresApproval:true`. Why: §6 KN-10 tiered autonomy.
6. **`user_region_never_overwritten`** — Asserts: given a note whose sections include a `@user` region, no emitted patch targets it. Why: 13.7b confinement primitive — the load-bearing safety property of autonomous writes.
7. **`no_inference_tbd`** — Asserts: an owner/date with no evidence ⇒ `TBD`/unset in the frontmatter patch, never a guess. Why: REQ-F-017.
8. **`ws8_foreign_candidate_dropped`** — Asserts: a candidate carrying/implying another workspace is dropped, and the resolver is never asked cross-workspace. Why: safety rule 4 / §5 WS-8.
9. **`link_faithful_only_no_backlinks`** — Asserts: a faithful Title→slug match emits a `LinkMutation`; a lossy/fuzzy/2-candidate match is withheld; no backlink section is ever authored. Why: §6 KN-11 / 13.8b.
10. **`pure_never_throws`** — Asserts: a throwing dep and a non-object input each yield an empty receipt, no throw. Why: total-function discipline at a synthesis boundary (mirrors `ingest-rewrite.test.ts:143`).
11. **`flood_bound`** — Asserts: candidates beyond the cap are sliced off. Why: bounded blast radius for an autonomous writer.
12. **`no_production_caller`** — Asserts: the reframed dormancy pin for THIS module. Why: dormant-by-default (L24).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none expected — reuses `KnowledgeMutationPlan` / `LinkMutation` / `FrontmatterPatch` / `NotePatch` as-is. **No frozen-contract round, no schema snapshot.** If you find you need a new field on an Appendix-A model, STOP and flag at Step 2.5 — that changes the slice's shape (frozen-contract rounds are contract-track work).
- **§2.5-seam model touched?** No (given the above). If that flips, the RED outline must gain the schema-snapshot test.
- **Orchestrator doc rows to write hot (Step 9):** an `ARCHITECTURE.md §9`/§6 note that the meeting path now has a synthesis planner (knowledge half) + the plan's §13.8f Files split; plus the dormancy-pin-phrasing lesson.

## Things to flag at Step 2.5
1. **Reuse `IngestRewriteDeps` or declare a meeting-specific deps type?** (a) reuse as-is, (b) a narrower `MeetingRewriteDeps` (drop `structural` — index/log parity is the source path's KN-12 concern, arguably not a meeting's), (c) share a base type. My default vote: **(b) narrower, sharing the `gbrain`/`reason`/`sections` port TYPES** — a meeting closeout shouldn't be able to rewrite index.md/log.md as a side effect, and a narrower dep set is a smaller blast radius. Flag if KN-12 parity genuinely should fire on meeting notes too.
2. **Does 13.8f-A emit ONE plan or a plan SET?** (a) one `KnowledgeMutationPlan` merged across entities, (b) a receipt with several. My default vote: **(b) a receipt with a plan set**, matching `IngestRewriteReceipt` — the worker's 13.8f-B then decides how to fold it into the meeting KMP, and per-entity plans keep the approval tier per-entity rather than collapsing an additive change into a propose-gated batch.
3. **How does 13.8f-B consume it if the meeting plan is a single KMP?** `buildOutputs.ts` builds ONE `plan` with `linkMutations`/`frontmatterUpdates` fields. If you return a plan set, the worker leg needs a documented merge rule (additive mutations fold into the meeting KMP; human-relevant ones become separate propose plans). My default vote: **return both** — the mutations destined for the meeting note's own KMP AND the separate per-entity plans — and document the contract in the module header so 13.8f-B has no design freedom left to get wrong.
4. **Attendee handling.** 13.8g owns attendee→person resolution. My default vote: **accept attendees as opaque candidates on the input and simply pass them through the same grounding path** (so 13.8g is a resolver/parsing slice, not a re-architecture), but build NO attendee parsing here. Flag if the input type can't be shaped now without pre-deciding 13.8g.
5. **`provenanceOrigin` value.** The plan notes a pending `living_vault_synthesis` provenanceOrigin decision (contract track). My default vote: **reuse the existing `meeting_close` origin for this slice** and flag the `living_vault_synthesis` value as a contract-track follow-up — adding an enum member here would make this a frozen-contract round it isn't scoped as.

## Dependencies + sequencing
- **Depends on:** 13.8a `c3d62436`, 13.8b `262df7b8`, 13.8c `461a0186` (+ eval `a34de8e1`), 13.8d `04e01eed`/`3d2d24f9` — all landed.
- **Blocks:** **worker brief 199 commit 2** (via the commit-1 pin reframe — land commit 1 FIRST and early), 13.8f-B (worker threading), 13.8g (attendee→person), 23.8 (the Granola meeting SPINE leg).
- ⏱ **Ordering ask:** commit 1 is small and gates another implementer. Ship it before starting commit 2's RED.

## Estimated commit count
**2.** Commit 1 (pin reframe) is tiny but independently valuable and unblocks the worker — it must not wait behind commit 2's review. Commit 2 carries safety pins (`@user` confinement, WS-8, no-inference) so it takes its own commit regardless.

## Lessons-logged candidates anticipated
- **Convention candidate** — "Phrase a dormancy pin as *every importer must be the arming-gated site*, never *zero importers* — the latter guarantees a RED window on the binding slice and tempts a cross-territory test edit."
- **Convention candidate** — "A second synthesis entry point reuses the resolver/link-healer/match-key authorities by import; a re-mirrored match key is a divergence bug waiting to happen."
- **Architecture-doc note candidate** — §9 W1: the meeting path's person/project KW targets are reached via the synthesis planner, with the additive/propose tier split.
- **Future TODO — next-brief working set** — 13.8f-B (worker threading) + 13.8g (attendee→person), with the merge contract from Step-2.5 Q3.

## How to invoke
1. Read this brief end-to-end, including the Scope split and all five Step-2.5 questions.
2. Run `/tdd meeting_path_living_vault_synthesis`.
3. Step 0 restate → Step 1 confirm the file list → **Step 2.5 send the test-design write-up + coverage map** (one `Asserts:` line per test; every acceptance bullet mapped to a test or an explicit `not-tested-because:`).
4. Step 8 reviewers: `code-quality-reviewer` (every-slice) + `security-reviewer` (**invariant** — `@user` confinement, WS-8, one-writer).
5. Step 9: categorized flags + ship-ask.
