# /tdd brief — commit_callers_carry_real_content

## Feature
Feed the **real** extraction (18.3/18.4) + routing (18.5/18.6) + body (15.3) outputs into the `createCommitActivity` caller sites so the committed notes carry **real body/frontmatter** instead of placeholder literals. The sole-writer path (`atomicCommit` / `applyPlan`) is already real; **only the callers feed stubs.** SAFETY RULE 1 (one-writer / no hidden brain — every semantic mutation flows through the validated `KnowledgeMutationPlan` → KnowledgeWriter, never around it). **SAFE-BUILD: deterministic (the "real" content is the deterministic extraction until the crossing); local fs commit, no egress/spend/hard line.**

## Use case + traceability
- **Task ID:** 18.8
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (primary), `§6` (KnowledgeWriter sole-writer / one-writer)
- **Related context:**
  - **Evidence:** `createCommitActivity` over the REAL `applyPlan` @ ~3 sites (`buildActivities.ts:388/391/689` — trace the real sites); `atomicCommit` (`packages/knowledge/src/markdown-vault/atomic-write.ts:59`) is the real staged-write→fsync→rename (transactional, rollback-safe). The commit path is already real + sole-writer — **the gap is the KMP CONTENT the callers build.**
  - **`createCommitActivity`** (`commitKnowledge.ts:96`): `commit(plan: KnowledgeMutationPlan)` → `applyPlan` — "plan is candidate data to the writer; it re-runs the composed gate." So the KMP is re-validated by the writer (rule 1/2).
  - **What's likely already real (verify at Step 1):** the note BODY (15.3 note-body projection writes it from the gate-validated `SourceEnvelope.body`, LESSON 35). **The likely gap:** the frontmatter (extraction owner/dueDate from 18.3/18.4) + the routing (workspace/project from 18.5/18.6) may still be placeholder literals in the committed KMP. **Trace the actual placeholder at Step 1** — 18.8 may be a small thread if prior slices already flow the content through.
  - **Prior slices (landed):** 18.3 `0bbb281c`, 18.4 `2068039f`, 18.6+18.5 `3af7e58d`, 18.7 `8d9c6507`, 15 (spine).

## Acceptance criteria (what "done" means)
- [ ] The committed note carries the **real extraction frontmatter** (owner/dueDate from the validated 18.3/18.4 extraction), not a placeholder literal.
- [ ] The committed note carries the **real routing** (workspace/project from the 18.5/18.6 resolution — registry-bound, WS-8), not a placeholder.
- [ ] The committed note carries the **real body** (15.3 `SourceEnvelope.body`) — confirm it already flows; thread it if not.
- [ ] **Rule 1 (one-writer):** the content reaches the note ONLY via the validated `KnowledgeMutationPlan` → `createCommitActivity` → KnowledgeWriter (which re-runs the gate); NO direct fs write around the writer.
- [ ] **Accept-path preserved:** the note is still committed (via the sole writer) — the content upgrades from placeholder → real, but the commit succeeds; existing tests asserting the OLD placeholder content are updated to assert the REAL content (never loosen a gate to keep a stale placeholder green).
- [ ] **SAFE-BUILD:** the "real" content is the deterministic extraction (no real model until the crossing); local fs commit; no egress/spend/hard line.
- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
The commit callers run in the meeting/source activities (`buildActivities.ts`) → build the `KnowledgeMutationPlan` from the extraction/routing/body → `createCommitActivity.commit(plan)` → `applyPlan` (KnowledgeWriter) → `atomicCommit`. Reached via the real meeting/source workflows. Confirm the KMP carries the real content on the production path (the `-live` run shows the committed note's real body/frontmatter).

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/buildActivities.ts` — the commit callers build the KMP from the real extraction/routing/body (remove placeholder literals).
- (possibly) `packages/workflows/src/activities/buildOutputs.ts` — if the KMP construction is here and needs the routing/extraction threaded.

> No contract change expected (reuse `KnowledgeMutationPlan`). Flag if a new field is needed.

## RED test outline (Step 2)
1. **`commit_carries_real_extraction_frontmatter`** — the committed note's frontmatter (owner/dueDate) == the validated extraction's values, not a placeholder. Why: 18.8 / §6.
2. **`commit_carries_real_routing`** — the committed note's workspace/project == the 18.5/18.6 resolution (registry-bound), not a placeholder. Why: 18.5/18.6 / WS-8.
3. **`commit_carries_real_body`** — the committed note body == the 15.3 `SourceEnvelope.body`, not a placeholder. Why: 15.3.
4. **`content_flows_through_validated_kmp_only`** — the content reaches the note ONLY via the validated KMP → KnowledgeWriter; a spy confirms no direct fs write around the writer. Why: safety rule 1.
5. **`accept_path_note_committed_with_real_content`** — the note commits successfully carrying the real content (proof-spine still produces its note). Why: accept-path.
6. **`safe_build_deterministic_content_no_model`** — the committed content is the deterministic extraction; no real model call. Why: SAFE-BUILD.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none anticipated — reuses `KnowledgeMutationPlan`. Confirm at Step 9.
- **Orchestrator doc rows to write hot:** none anticipated.
- **Shared-contract seam model touched?** No anticipated.

## Things to flag at Step 2.5
1. **The actual gap** — trace which committed content is STILL a placeholder (body likely real from 15.3; frontmatter/routing the likely gap). My default vote: **feed the real extraction frontmatter + routing; confirm the body already flows**. If prior slices already thread everything, 18.8 is a verify + pin slice — say so.
2. **The 3 `createCommitActivity` sites** — which feed a placeholder KMP? (proof-spine commit + meeting-reindex + approval-dispatch — trace). My default vote: **identify the site(s) building a placeholder KMP; the approval-dispatch commit is Phase-21/22-adjacent — scope to the proof-spine/extraction commit sites.**
3. **Byte-equivalence** — the committed content changes (placeholder → real deterministic extraction), so it's NOT byte-equivalent; existing tests asserting the placeholder need updating. My default vote: **not byte-equivalent (intended content upgrade); update the placeholder-asserting tests to assert the real content; NEVER loosen a gate to keep a stale placeholder green; accept-path (note committed) preserved.**
4. **Rule 1** — pin that the content flows through the validated KMP → KnowledgeWriter, never a direct write. My default vote: **spy-pin no direct fs write around the writer (rule 1).**

## Dependencies + sequencing
- **Depends on:** 18.3 (`0bbb281c`), 18.4 (`2068039f`), 18.6+18.5 (`3af7e58d`), 15 (spine). Landed.
- **Blocks:** Phase 19 (gbrain sync of the committed real content).

## Estimated commit count
**1** — one focused slice (worker-primary). **Invariant** (safety rule 1 — one-writer / semantic mutation via validated KMP) → **security-reviewer = invariant** (verify content flows through the validated KMP → sole writer, never around it) + code-quality = every-slice; own commit.

## Lessons-logged candidates anticipated
- **Convention candidate** — "the commit callers feed the KMP the REAL extraction/routing/body (never a placeholder literal); the content reaches the note ONLY through the validated KnowledgeMutationPlan → the sole KnowledgeWriter (rule 1); the deterministic extraction is the 'real' content until the crossing."
- **Architecture-doc note candidate** — the extraction/routing/body → KMP → committed-note content flow (§19.5/§6).
- **Future TODO — crossing:** the committed content becomes the REAL model extraction at the crossing (the deterministic extraction is the safe-build stand-in).

## How to invoke
1. **Read this brief end-to-end** — the 4 Step-2.5 questions need answers before GREEN.
2. **Run `/tdd commit_callers_carry_real_content`** in the `worker-impl2` session.
3. **Step 0 (Restate)** — SAFE-BUILD; feed real deterministic content into the commit via the validated KMP (rule 1).
4. **Step 1 (Identify files)** — TRACE the actual placeholder gap (body real from 15.3? frontmatter/routing the gap?) + the 3 commit sites; if prior slices already thread everything, say so (verify+pin slice).
5. **Step 2.5** — send the test write-up + votes on the 4 questions; wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 8 reviewers** — security-reviewer (invariant: rule 1 one-writer) + code-quality-reviewer (every-slice).
7. **Step 9** — categorized flags + ship-ask. **Then `/session-end` recap + ping me — this is your last slice before the cycle.**
