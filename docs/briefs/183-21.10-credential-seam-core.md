# /tdd brief — external_write_credential_seam_core

## Feature
The external-write-adapter **credential-seam CORE** (packages/integrations, provint territory): a `SecretsAccessor`/`getSecret` binding threaded into `adapter-core` + the Tool Gateway write path so each write adapter resolves its auth token at dispatch-time — **fail-closed** on an unavailable/throwing accessor (NO unauthenticated write, NO token in any log sink). Logic-in-package; the `backends.ts` `getSecret`-bind is a separate WORKER follow-up.

> **Territory split (logic-in-package / wire-at-boot):** the seam LOGIC lands here in `packages/integrations`. The composition-root bind (`apps/worker/src/composition/backends.ts` — bind `getSecret` into the write-adapter deps) is a separate WORKER task. This brief is the provint core only.

## Use case + traceability
- **Task ID:** 21.10 (core leg)
- **Architecture sections it implements:** `ARCHITECTURE.md §19.8` (External Write / Tool-Sync) · `§3` (SecretsPort — safety rule 7: write token resolved only via SecretsPort/Keychain, never logged).
- **Related context:**
  - Depends: 17.4 (the `keychain://<service>/<account>` secret-ref convention, landed) · 21.1 (the routing registry logic, `07145feb`).
  - Resolves each vendor's `connector-write:<vendor>` token + the `telegram-bot:*` token via the 17.4 ref convention. Actual per-vendor token PROVISIONING is the owner-confirmed crossing at 21.6/21.8 — NOT here.
  - **Blocks:** 21.5/21.6 (arming) · 21.8 (card renderer).

## Acceptance criteria (what "done" means)
- [ ] A `SecretsAccessor`/`getSecret` seam in `adapter-core.ts` threaded through `gateway.ts`'s write path — a write adapter resolves a `connector-write:<vendor>` / `telegram-bot:*` ref through an injected (mock) accessor at dispatch-time.
- [ ] **Fail-closed:** an unavailable / throwing accessor **fails the dispatch closed with a typed fault** — NO unauthenticated write (the write never proceeds without a resolved token).
- [ ] **No token in any log sink** — a test/scan pins the resolved token is never logged / never echoed in a fault (safety rule 7 redaction).
- [ ] **No real vendor token provisioned here** — the accessor is injected (mock in tests); real provisioning is 21.6/21.8 (owner-gated). Dormant.
- [ ] All unit tests in `packages/integrations/test/…` pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none in this slice — pure seam over an injected accessor; the WORKER binds `getSecret` at the composition root (`backends.ts`, separate task).** The Tool Gateway write path calls the seam at dispatch-time. State explicitly: no boot wiring here. (spec-lint requires this section.)

## Files expected to touch
**New/Modified:**
- `packages/integrations/src/tools/adapters/adapter-core.ts` — the `SecretsAccessor`/`getSecret` seam.
- `packages/integrations/src/tools/gateway.ts` — thread `getSecret` through the write path.
- test file(s).

⚠ provint territory (`packages/integrations`). Do NOT touch `apps/worker/*` (the bind is a worker task). `git add <path>` only.

## RED test outline (Step 2)
1. **`adapter_resolves_write_token_via_accessor`** — a write adapter resolves a `connector-write:<vendor>` ref through the injected accessor at dispatch. Why: §19.8 / §3.
2. **`unavailable_accessor_fails_dispatch_closed`** — an unavailable/throwing accessor ⇒ typed fault, NO write proceeds. Why: safety rule 7 (no unauthenticated write).
3. **`token_never_logged`** — the resolved token never appears in a log/fault (redaction). Why: safety rule 7.
4. **`telegram_bot_ref_resolves`** — the `telegram-bot:*` ref resolves via the same seam. Why: §19.8 (telegram write path).
5. **`no_real_token_provisioned`** — the accessor is injected; no real vendor token is provisioned/reached here (dormant). Why: 21.6/21.8 owner-gated.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** none anticipated.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Accessor shape.** Default vote: a `SecretsAccessor` port with `getSecret(ref): Promise<Result<token, fault>>` — fail-closed on unavailable/throw; mirror the 17.4 ref convention (`keychain://<service>/<account>`). Confirm.
2. **Fault redaction.** Default vote: the typed fault is code-only — never echoes the token or the raw ref-value (safety rule 7). Confirm.
3. **Dispatch-time vs construction-time resolution.** Default vote: resolve at DISPATCH-time (not construction) so a rotated/absent token fails the specific write closed. Confirm.

## Dependencies + sequencing
- **Depends on:** 17.4 (ref convention), 21.1 (registry logic, `07145feb`).
- **Blocks:** the worker `backends.ts` `getSecret`-bind · 21.5/21.6/21.8 (arming).

## Estimated commit count
**1** — one credential-seam core + unit tests; provint territory, dormant over a mock accessor. **security-reviewer = invariant** (safety rule 7 — no unauthenticated write, no token in any log sink, fail-closed). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the external-write credential seam resolves the auth token at DISPATCH-time through an injected SecretsAccessor (17.4 ref), fail-closed on unavailable/throw (no unauthenticated write), token never logged (rule 7); logic in-package, worker binds getSecret at boot; real provisioning owner-gated (21.6/21.8)."

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (accessor shape) + Q3 (dispatch-time resolution) need answers before GREEN.
2. Run `/tdd external_write_credential_seam_core`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
