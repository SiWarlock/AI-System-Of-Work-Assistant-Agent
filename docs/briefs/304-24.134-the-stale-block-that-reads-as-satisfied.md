# /tdd brief — the_stale_block_that_reads_as_satisfied

> ⛔⛔ **THE CONSTRAINT IS THE WHOLE SLICE (lead ruling): THE STALE CLAUSE MAY NOT SIMPLY BE DELETED. It currently blocks delegation for a DEAD reason — delete the dead reason and YOU DELETE THE BLOCK.** ⭐ **Strike the dead ground, name the LIVE one, in the present tense.**

## Feature

Correct `packages/policy/src/audit-signal.ts`'s delegation-blocking comment so the block survives on a live reason instead of a satisfied one.

## Use case + traceability

- **Task ID:** `### 24.134` · **Spec:** `ARCHITECTURE.md §16`
- ⛔ **widens phase scope because Phase 24's declared anchors (`§13 §19.11 §3 §4 §6`) do not contain `§16`** — `### 24.54`, declared not silenced; ⛔ do NOT widen the set.
- ⛔ **Safety: rule 7.** **Your own find, at `### 24.128` Step 9.**

**The finding, verbatim from your report:** the production comment carries the pre-`### 24.120` reason **in the present tense** and closes *"Tracked as task 24.120…; do NOT delegate this module until it is resolved."* ⛔ **`### 24.120` IS resolved** ⇒ ***the instruction reads as SATISFIED and licenses exactly the delegation `### 24.110`'s deliberately-unruled second axis forbids*** — 130 lines from the test that now says the opposite.

⭐ **`029`'s rotted-guard class with the rot in the RELEASE direction: an ordinary rotted guard invites a reader to DISMISS it; a satisfied precondition is an instruction to PROCEED.**

## Acceptance criteria

- [ ] **The comment states WHICH axis closed and WHEN** (`65524874` + `8b2b53ac` + `bf442753`, 2026-08-18).
- [ ] ⛔ **A LIVE blocking reason is stated IN THE PRESENT TENSE — `### 24.110`'s unruled second axis: delegating makes this module start stripping, so it stops refusing already-redacted content, a loosening on the sole-writer path that NOBODY HAS RULED ON.**
- [ ] ⛔ **The *do-not-delegate* instruction SURVIVES.** ⚠ **A reader arriving to delegate must still be stopped, and must be stopped for a reason they can check.**
- [ ] **The dead ground is STRUCK AND RETAINED, not deleted** (`L195` — retain the reasoning, past-tense the state; `L194` — the artifact naming the relationship is the only pointer anyone has).
- [ ] **No claim is made that the delegation is safe**, and no claim is made about `(C')` beyond what `### 24.110` records. ⚠ ***"Monotone" is a claim about LEAKS and is SILENT about AVAILABILITY*** — your own sentence; do not let the comment repeat the error you fixed in the test.
- [ ] **`packages/policy/src/audit-signal.ts` is the ONLY file touched.** Comment-only: ⛔ **no behaviour change, no pattern edited, no composition altered.** Verify by `--numstat` and by the suite being unchanged at **522/522**.
- [ ] **The citation names the entry that OWNS the block** (`L240` — your own lesson; `### 24.128` closes and would resolve to finished work).

## Wiring / entry point (Step 7.5)

⛔ **None — comment-only, and that is correct.** ⭐ **The "reachability" question here is HUMAN: the artifact is read by someone about to delegate, so the test is whether the live reason is present at the point of decision.** **State that in the Step-9 report rather than claiming a code path.**

## Files expected to touch

**Modified:** `packages/policy/src/audit-signal.ts` (the comment block at ~`:94-102` — ⛔ **cite by SYMBOL when you report, not by line; `### 24.93` moved every line number in a sibling file this round**)

## RED test outline (Step 2)

⚠ **THIS SLICE HAS NO RED AND MUST NOT MANUFACTURE ONE.** A comment carries no behaviour, and inventing a test that asserts on comment text would pin the wording rather than the property. ⛔ **Report this as a TDD deviation at Step 9 with the reason** — session `183` set that precedent and `### 24.113` followed it.

**What stands in for RED:** the suite must be **unchanged** — 522/522, same `it(` count, same `expect(` count. ⛔ **A comment-only slice that moves any count has done something else.**

## Cross-doc invariant impact

- **Model field changes:** none. **Cross-doc rows:** none anticipated. **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. **How much of the dead ground survives?** Default: **retain the mechanism reasoning (the space filler, the userinfo span) in PAST tense; strip every state claim.** ⭐ `L195`: striking a block does not tense-shift the sentences inside it.
2. ⛔ **Does the comment name `### 24.110` or `### 24.129` as the blocker?** Default: **`### 24.110`** — the second axis is what is unruled. ⚠ **`### 24.129` blocks a different promotion (`URL_CREDENTIAL_PARAM`); do not conflate two blocks.** **Flag if you measure otherwise.**
3. **Does anything else in the file carry a pre-`24.120` claim?** ⛔ **Your reviewers found the stale claims in your TEST file's header by convergence — check whether the same sweep applies here, and report the method.**

## Dependencies + sequencing

- **Depends on:** `### 24.128` (LANDED `6ea9748b`). **Blocks:** any future delegation decision on this module.

## Estimated commit count

**1.** ⛔ `git commit -F <msgfile> -- packages/policy/src/audit-signal.ts` — pathspec only; read the staged set as its own command first. **Three sessions are live.** Never `--amend`.

## Lessons-logged candidates anticipated

- **Convention** — *a precondition that has been SATISFIED is an instruction to proceed; a block must be re-grounded, never merely un-annotated.*

## How to invoke

1. Read end-to-end. 2. `/tdd the_stale_block_that_reads_as_satisfied`. 3. Step 2.5 answering Q2. 4. Step 9 with the TDD-deviation note.
