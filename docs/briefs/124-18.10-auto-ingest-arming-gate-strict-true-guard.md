# /tdd brief — auto_ingest_boot_gate_strict_arming_guard

## Feature
**Pin** the auto-ingest boot gate's strict-arming against a truthy-not-`true` false-arming vector (Lesson 28). `gateAutoIngest` already guards `opts.autoIngest !== true` (strict) and the desktop worker-host already wires it default-OFF byte-equivalent — this slice adds the ONE missing regression guard: a truthy-non-`true` value (`"true"`, `1`, `"false"`, `{}`) — the shape an env/IPC-sourced flag actually arrives as — must NOT arm the loop (returns `undefined`, proof-spine builder NOT invoked). **SAFE-BUILD / verify+pin: no CODE change to the gate (it's already strict + already wired); the genuinely-new coverage is the truthy-coerce guard. Arming the loop for real (owner sets `autoIngest=true` in the desktop host + threads the source `stubExtraction`) is the CROSSING (#13), not this slice.**

## Use case + traceability
- **Task ID:** 18.10
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (primary — the real ModelProvider/ingestion activation seam), `§16` (boot degradation: the shipped default stays the exact degraded boot — control-plane API only, `connectTemporal` degrades cleanly)
- **Related context (PREMISE CORRECTION — confirm at Step 1):** handoff 008 framed S9 as an *unstarted* worker-side "boot-wire." **A Step-1 trace shows the boot-wire is ALREADY built + committed:**
  - `gateAutoIngest` — the pure default-OFF gate helper, strict guard `opts.autoIngest !== true || vaultRoot === undefined` (`apps/worker/src/boot.ts:622`). Lesson 2/23 pattern.
  - `buildAutoIngestProofSpineParams` — the armed-path proof-spine builder with a real WS-8 sourceIngestion binding + inert meeting leaves (`apps/worker/src/boot.ts:1003`).
  - **Desktop worker-host wiring** — already calls `boot.gateAutoIngest({...}, config.vaultRoot, boot.buildAutoIngestProofSpineParams) ?? {}` and spreads the result into `bootWorker` (`apps/desktop/worker-host/index.ts:217`); default (opt-in OFF or no vaultRoot) ⇒ `undefined` ⇒ no vaultWatch / no proofSpineParams ⇒ today's exact degraded boot. Committed.
  - **Existing test** `apps/worker/test/boot-auto-ingest-gating.test.ts` (task 11.1) already pins: armed ⇒ WIRED; `false`/`undefined`/no-vaultRoot ⇒ `undefined` + `build` NOT called (the factory-spy zero-invocation OFF pin, L23/L11); ws/sensitivity/temporalAddress defaults+overrides; the WS-8 binding.
  - **The single missing pin:** the combos are `true`/`false`/`undefined` only — NO truthy-non-`true` case. Per **Lesson 28**, every arming `=== true` earns a truthy-not-`true` guard (incl. the string `"false"`). The desktop host passes `autoIngest: config.autoIngest` RAW (no host-side `=== true`) — the gate's strict check is the SOLE arming chokepoint, so pinning it at the gate covers the desktop path too.
  - **Safety framing:** the auto-ingest loop, once armed, drives the real proof-spine ingestion pipeline — a truthy-coerce false-arming (an env/IPC `"false"` reading as truthy) is a real hard-line-adjacent vector. This pins it closed.

## Acceptance criteria (what "done" means)
- [ ] **Truthy-non-`true` does NOT arm:** `gateAutoIngest({ autoIngest: <truthy-not-true> }, "/vault", build)` ⇒ `undefined` AND `build` NOT called, for each of `"true"` (string), `1`, `"false"` (string — the L28-named case), `{}` (truthy object). Values pass through the runtime seam via an explicit `as unknown as boolean` cast (L28: behavioral + co-located control where a runtime seam exists).
- [ ] **Positive control co-located:** the same block asserts the literal `true` DOES arm (so the guard proves *strictness*, not a blanket deny — non-vacuity, L28/L15).
- [ ] **No gate CODE change:** `gateAutoIngest`'s `!== true` guard is already correct — this slice adds test coverage only. If Step-1 finds the gate is NOT actually strict (a real CODE gap), STOP and flag (it becomes a fix, re-scope at Step 2.5).
- [ ] **Byte-equivalent / SAFE-BUILD:** no production wiring change; shipped default stays the exact degraded boot. No owner provisioning; the desktop `autoIngest=true` arming + source `stubExtraction` threading is the crossing (#13), untouched.
- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
`gateAutoIngest` (`apps/worker/src/boot.ts:622`) is already reachable in production — the desktop worker-host (`apps/desktop/worker-host/index.ts:217`) calls it and spreads the result into `bootWorker`. This slice adds no new production reachability; it pins the existing armed-vs-OFF arming boundary. `/wired gateAutoIngest` is already GREEN (confirm — the caller crosses the app boundary, so codegraph may not resolve the edge; the grep + the desktop call site prove it).

## Files expected to touch
**New/Modified:**
- `apps/worker/test/boot-auto-ingest-gating.test.ts` — extend with the truthy-not-`true` arming guard block (the L28 pin + its positive control).

> **Gate CODE (`gateAutoIngest`): NONE expected.** The strict `!== true` guard is already present + correct. This is a worker-side test-only verify+pin. **If** Step-1 finds the guard is not actually strict, or the desktop wiring is not byte-equivalent, STOP and flag — that becomes a fix, re-scoped at Step 2.5.

## RED test outline (Step 2)
1. **`auto_ingest_truthy_not_true_does_not_arm`** — `it.each` over `["true", 1, "false", {}]` (each `as unknown as boolean`): `gateAutoIngest({ autoIngest: v, ingestWorkspaceId: WS }, "/vault", build)` ⇒ `toBeUndefined()` AND `build` `not.toHaveBeenCalled()`. Why: L28 — a truthy-coerce must not arm the auto-ingest hard-line-adjacent gate; the env/IPC `"false"` string is the dangerous case.
2. **`auto_ingest_literal_true_arms` (positive control, co-located)** — `gateAutoIngest({ autoIngest: true, ingestWorkspaceId: WS }, "/vault", build)` ⇒ WIRED + `build` called with WS. Why: proves the guard narrows on strictness (non-vacuity), not a blanket deny. *(May reuse the existing combos row — if so, cite it rather than duplicate; the NEW assertions are the truthy cases.)*
3. *(Confirm at Step 1, do NOT add if redundant)* the OFF byte-equivalence (`false`/`undefined`/no-vaultRoot ⇒ undefined + no build) is ALREADY covered by the existing combos — cite + skip, don't re-pin.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — reuses `AutoIngestGateOpts` / `AutoIngestWiring`. Confirm at Step 9.
- **Orchestrator doc rows to write hot:** none anticipated.
- **Shared-contract seam model touched?** No.

## Things to flag at Step 2.5
1. **Premise correction** — confirm the Step-1 trace: is the boot-wire genuinely already-built + committed + reachable (gate + builder + desktop host + existing OFF/armed pins), leaving ONLY the truthy guard? My read: yes. If your trace disagrees (a real un-wired leg or a non-strict guard), say so — that re-scopes the slice from pin to build/fix, and I route accordingly.
2. **Guard site** — the strict check lives in `gateAutoIngest` (`!== true`); the desktop host passes `config.autoIngest` raw, delegating strictness to the gate. Pinning at the gate covers both. Confirm there's no SECOND arming `=== true` on the worker side that also needs a guard (grep `autoIngest` in `apps/worker/src`). My default vote: gate-only pin is sufficient.
3. **Cast vs source-assertion** — `autoIngest?: boolean` is typed, so a truthy-non-`true` value needs an `as unknown as boolean` cast through the runtime seam (L28 "behavioral + co-located control where a runtime seam exists"). My default vote: behavioral cast test (a runtime seam exists — `gateAutoIngest` is directly callable), NOT a source-assertion (that's only for inline-with-no-seam sites).
4. **Scope honesty** — this is a small verify+pin, not a build. If, at Step-1, you find the truthy guard is ALREADY present somewhere, this slice is a no-op ⇒ STOP and flag (a deferment → I take it to the lead). Don't manufacture coverage.

## Dependencies + sequencing
- **Depends on:** task 11.1 (`gateAutoIngest` + the existing gating test) + the desktop worker-host wiring (already committed). Nothing in-flight.
- **Blocks:** nothing on the safe-build path. The auto-ingest arming (owner flips `autoIngest=true` + threads source `stubExtraction`) is the crossing (#13).
- **Independent of S8** (18.9 egress-veto) — different files/concern; sequence after S8's commit (same impl, worker-impl3).

## Estimated commit count
**1** — one focused test-only verify+pin slice (worker). **INVARIANT-adjacent** (the auto-ingest arming gate governs activation of the real ingestion pipeline → a false-arming vector) → **security-reviewer = invariant (per the lead's Phase-18 policy)** + code-quality = every-slice; own commit.

## Lessons-logged candidates anticipated
- **Convention candidate (reinforces L28)** — the auto-ingest boot gate's strict `=== true` arming carries a truthy-not-`true` regression guard (incl. the env/IPC `"false"` string); the gate is the sole arming chokepoint (the desktop host delegates strictness), so one gate-side pin covers the whole activation path.
- **Future TODO — crossing:** arming the auto-ingest loop (owner `autoIngest=true` in the desktop host + the source `stubExtraction` threading precondition, #13) — a crossing item, owner-gated.

## How to invoke
1. **Read this brief end-to-end** — note the PREMISE CORRECTION: the boot-wire is already built; this is a truthy-guard pin.
2. **Run `/tdd auto_ingest_boot_gate_strict_arming_guard`** in the `worker-impl3` session (after S8's Step-10 commit).
3. **Step 0 (Restate)** — SAFE-BUILD verify+pin; add the L28 truthy-not-`true` guard to the auto-ingest arming gate; no CODE change; the arming is the crossing.
4. **Step 1 (Identify files)** — CONFIRM the premise (gate strict + wired + existing OFF/armed pins); locate the exact combos block in the existing test; grep `autoIngest` across `apps/worker/src` for any second arming check.
5. **Step 2.5** — send the write-up + answers on the 4 questions; wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 8 reviewers** — security-reviewer (invariant) + code-quality-reviewer (every-slice).
7. **Step 9** — categorized flags + ship-ask (confirm no cross-doc/contract touch; confirm the premise held).
