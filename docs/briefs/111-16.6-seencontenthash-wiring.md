# /tdd brief — seen_content_hash_wiring (16.6)

## Feature
Wire the **real persisted `SeenContentHashRepository` (15.4)** into `registerSource`'s dedupe probe — replacing the hardwired `() => Promise.resolve(false)` (fresh always-miss) at `apps/worker/src/composition/buildActivities.ts:621` (the C1 source-ingestion path). Today 15.4 built a durable, WS-8-scoped, dual-dialect dedupe store that has **ZERO live consumers** (the Phase-15 `/phase-exit` reachability auditor flagged it as dead code). This gives it its consumer: the already-LIVE fs-watcher ingestion path AND the 16.2 connector-poll path now dedupe re-imported identical content pre-dispatch, instead of relying solely on the Temporal `src:ws:hash` workflowId AlreadyStarted-backstop. Worker-track. **Pure-build — NO hard line.** (Closes the Phase-15 gate's seenContentHash dead-code note.)

## Use case + traceability
- **Task ID:** 16.6 (Phase-16 follow-up — the seenContentHash wiring the Phase-15 reachability auditor named).
- **Architecture:** `ARCHITECTURE.md §19.2` / `§4` (operational dedupe store), `REQ-F-010` (Flow-4 dedupe).
- **Depends on:** 15.4 (the `SeenContentHashRepository`), 15.1/16.2 (the drive paths that now benefit).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **`buildActivities.ts:621`** — the hardwired `seenContentHash: () => Promise.resolve(false)` in the C1 source-register deps. Replace with a probe over the real repo.
  - **The 15.4 `SeenContentHashRepository`** — confirm it's assembled in `backends` (`assembleBackends`); if NOT wired into the backends bundle yet, add it (mirror the other 15.x op-store repos: durable, dual-dialect, in the repo-contract suite). The repo is keyed WS-8-scoped `(workspaceId, contentHash)`, first-write-wins.
  - **Worker Lesson 34** — the store is a pre-dispatch OPTIMIZATION: a `has`-fault **PROCEEDs** to registerSource+dispatch (the Temporal `src:ws:hash` workflowId AlreadyStarted-dedupe is the real exactly-once backstop), NEVER a HOLD (a persistent store fault must not block the whole connector). The dedupe probe records the hash on a miss (first-write-wins).

## Acceptance criteria (what "done" means)
- [ ] **Real store bound:** `registerSource`'s `seenContentHash` probe reads the real `SeenContentHashRepository` (not the hardwired `false`). Reachable from the fs-watcher + 16.2 poll drive paths.
- [ ] **Dedupe holds across runs (REQ-F-010):** re-importing identical content (same `(workspaceId, contentHash)`) a second time is deduped (a no-op — not re-registered/re-dispatched); a FIRST import still ingests. Pin both.
- [ ] **WS-8-scoped (L34):** a content hash seen in workspace A does NOT dedupe the same hash in workspace B (the probe is per-`(workspaceId, contentHash)`). Pin.
- [ ] **`has`-fault PROCEEDs, never HOLDs (L34, safety):** a store `has`-probe fault does NOT block registration — it PROCEEDs to registerSource+dispatch (Temporal is the exactly-once backstop). Pin (a fault ≠ a dedupe-hit; a fault ≠ a HOLD).
- [ ] **First-write-wins record:** a miss records the hash (so the next identical import dedupes); the record is idempotent (first-write-wins keeps the original `seenAt`). Pin.
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
The `seenContentHash` probe is reached from `registerSource` on BOTH the live fs-watcher ingestion and the 16.2 connector-poll path. State: *"Reachable from registerSource (fs-watcher + connectorPoll drive paths)."* This is a dead-code→live-consumer wiring (closes the Phase-15 gate note).

## Files expected to touch
**Modified:** `apps/worker/src/composition/buildActivities.ts:621` (bind the real repo probe) + `apps/worker/src/composition/backends.ts` (if the `SeenContentHashRepository` isn't assembled yet) + tests. If a small adapter shim maps the repo's `has/record` to the `seenContentHash` probe shape, keep it minimal + fail-closed-to-PROCEED per L34.

## RED test outline (Step 2)
1. `second_identical_import_is_deduped_first_still_ingests` — re-import same `(ws, hash)` ⇒ 2nd is a no-op; 1st ingests. (REQ-F-010)
2. `dedupe_is_workspace_scoped` — same hash in ws B is NOT deduped by ws A. (WS-8 / L34)
3. `probe_fault_proceeds_never_holds` — a `has`-probe reject ⇒ registration PROCEEDs (dispatch happens; Temporal backstop), NOT a HOLD, NOT a false dedupe-hit. (L34 / safety)
4. `miss_records_the_hash_first_write_wins` — a miss records the hash idempotently (original seenAt kept on a re-record). (§4 / L34)

## Cross-doc invariant impact
- **Model:** none (the `SeenContentHash` op-store model already exists from 15.4). Confirm the repo is in the repo-contract suite (it should be, from 15.4).

## Things to flag at Step 2.5
1. **Fault-PROCEEDs (LOAD-BEARING, L34).** Confirm a store fault PROCEEDs to dispatch (never a HOLD, never a false dedupe-hit) — a persistent store fault must not silently block the connector or drop a source.
2. **Backends assembly.** Confirm whether the repo is already in `assembleBackends`; if you add it, mirror the other durable op-store repos (dual-dialect, contract suite).
3. **Probe-shape shim.** If mapping `repo.has/record` → the `seenContentHash` probe, keep it minimal + fail-closed-to-PROCEED.

## Dependencies + sequencing
- **Depends on:** 15.4, 16.2. **Closes:** the Phase-15 gate's seenContentHash dead-code note (de-deads 15.4). Last worker-track Phase-16 item.

## Estimated commit count
**1.** A focused wiring slice. **security-reviewer = invariant** (the dedupe gate is on the candidate-data ingestion path + the fault-PROCEEDs-not-HOLD is a fail-safe safety property). code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Continue the worker-impl4 session.
2. Read this brief (Q1 fault-PROCEEDs is the load-bearing safety point, L34).
3. `/tdd seen_content_hash_wiring`.
4. Step 2.5 — ping the fault-PROCEEDs + backends-assembly answers. Don't GREEN until sign-off.
5. Step 9 — confirm 15.4 now has a live consumer (the Phase-15 gate note closed). This completes the worker-track Phase-16 work.
