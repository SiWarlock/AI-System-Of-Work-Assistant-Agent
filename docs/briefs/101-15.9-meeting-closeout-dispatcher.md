# /tdd brief — production_meeting_closeout_dispatcher (15.9) — G1 FLAGSHIP (meeting-half)

## Feature
Build the **production meeting-closeout dispatcher** — the G1 flagship's MEETING-half (G1 = 15.1 source-half ✅ + 15.9; **G1 closes ONLY when both land**). Today `apps/worker/src/temporal/` has only `dispatchSourceIngestion.ts` (the sole production `client.workflow.start`, starting `sourceIngestionWorkflow`); `meetingCloseoutWorkflow` (workflows.ts:241) is started ONLY in test/fakes — so the owner's flagship *meeting transcript → closeout note → propose tasks* has NO production trigger (a Granola transcript arriving would run the generic source path, bypassing the meeting-specific `correlateMeeting`/propose machinery). This slice: (1) a NEW `dispatchMeetingCloseout.ts` analog (a real `client.workflow.start` of `meetingCloseoutWorkflow` with a real `MeetingCloseoutInput`, idempotent-by-construction), and (2) a completed-meeting bridge in `connectorPoll.onRecords` that routes a completed-meeting record (Granola/calendar) to the MEETING dispatcher instead of `registerSource`. Worker-track; **ISOLATED, security-reviewer=invariant** (candidate-gate routing + idempotency + WS-8 + the meeting-vs-source discrimination). Fires a FAKE completed-meeting (no real transport — Phase-16/23). Pure-build, NO hard line.

## Use case + traceability
- **Task ID:** 15.9 — **the G1 flagship gap** (⚠ do NOT let it slip; G1 closes only with 15.1 ✅ + this).
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (primary — the production meeting-closeout dispatch trigger + Granola→meeting correlation), `§9` (workflows — `meetingCloseoutWorkflow`, the closeout production trigger), `§8` (Connector/Tool Gateway completed-meeting→meeting-dispatch bridge).
- **Depends on:** 15.1 (`a6190122` — the connector→ingestion bridge; the source-half + the `onRecords` seam), P7 (workflows).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **The mirror pattern — `apps/worker/src/temporal/dispatchSourceIngestion.ts`:** the sole production `client.workflow.start` (`SOURCE_INGESTION_WORKFLOW_TYPE = "sourceIngestionWorkflow"`, `StartedRunHandle {workflowId,…}`). **IDEMPOTENT BY CONSTRUCTION — the run's deterministic `idempotencyKey` IS the Temporal `workflowId`**, so a re-dispatch of the same input dedupes at the Temporal layer (`workflowId`-reuse `REJECT_DUPLICATE`) AND the driver's `resolveRun` (two guards). Mirror this exactly for `dispatchMeetingCloseout`.
  - **The workflow — `apps/worker/src/temporal/workflows.ts:241` `meetingCloseoutWorkflow`:** the production start TARGET (today test/fakes-only).
  - **The bridge — `packages/workflows/src/activities/connectorPoll.ts` `onRecords`:** where the 15.1 source bridge routes records THROUGH `registerSource`. 15.9 DISCRIMINATES: a completed-meeting record → the meeting dispatcher (`correlateMeeting` → `dispatchMeetingCloseout`); a generic source record → the existing 15.1 `registerSource` path. `apps/worker/src/composition/buildActivities.ts` — the bridge wiring.
  - **The meeting machinery** (already built): `correlateMeeting`/the meeting-propose legs (18.3/18.5) + `MeetingCloseoutInput`. This slice TRIGGERS it in production, doesn't rebuild it.

## Acceptance criteria (what "done" means)
- [ ] A NEW `dispatchMeetingCloseout.ts` — a production `client.workflow.start(meetingCloseoutWorkflow, {workflowId, args:[MeetingCloseoutInput]})`, **idempotent-by-construction** (the deterministic meeting identity IS the `workflowId`, so a re-dispatched meeting dedupes via `REJECT_DUPLICATE` — mirror `dispatchSourceIngestion`'s two-guard dedupe).
- [ ] **Completed-meeting bridge:** `connectorPoll.onRecords` routes a completed-meeting record to `correlateMeeting → dispatchMeetingCloseout` (the meeting path), and a generic source record STILL routes to the 15.1 `registerSource` path — a correct, unambiguous discrimination (Q1). A meeting record NEVER runs the generic source path (that's the bypass this fixes); a source record NEVER runs the meeting path.
- [ ] **Candidate-gate + WS-8 (rule 2 / rule 4):** the meeting record's content is candidate data — the workspace/routing is stamped from the BOUND connector instance (never a content field, WS-8; mirror 15.1's `source_workspace_from_bound_instance_not_content`); the transcript flows THROUGH `meetingCloseoutWorkflow`'s gate (never around it).
- [ ] **Idempotency (rule 3):** a re-imported / re-polled completed-meeting re-dispatches with the SAME `workflowId` ⇒ no duplicate closeout (Temporal `REJECT_DUPLICATE`). Pin the replay leg.
- [ ] **End-to-end (Done-when):** a FAKE Granola-shaped completed-meeting record → the meeting bridge → `dispatchMeetingCloseout` → `meetingCloseoutWorkflow` runs end-to-end (under `TestWorkflowEnvironment`) producing a `ProposedAction` (the *transcript → closeout → propose-tasks* flagship path — via the meeting-specific machinery, NOT the generic source path).
- [ ] **Reachability:** the dispatcher + bridge are wired; the REAL Granola/calendar transport is Phase-16/23 (fires a fake completed-meeting today). Reachability posture mirrors 15.1 (the drive/poll binding is Phase-16) — waiver the real-transport leg per L11, but the dispatch→workflow path IS exercised end-to-end by the fake-record test.
- [ ] `/preflight` clean (worker + workflows + repo-wide).

## Wiring / entry point (Step 7.5)
`dispatchMeetingCloseout` is the production start for `meetingCloseoutWorkflow`; the bridge in `connectorPoll.onRecords` (wired via `buildActivities.ts`) routes completed-meeting records to it. **State: the dispatch→workflow path is exercised end-to-end by the fake-completed-meeting test (TestWorkflowEnvironment); the real Granola/calendar poll transport that FEEDS a real completed-meeting is Phase-16/23 (waivered, L11 — don't wire dormant-on-dormant).** Name the exact bridge site in Step-9. Confirm at Step 1 whether the bridge binds now (the onRecords seam is real, per 15.1) or the real-transport leg waivers.

## Files expected to touch
**New:** `apps/worker/src/temporal/dispatchMeetingCloseout.ts` (analog of dispatchSourceIngestion.ts) + its tests.
**Modified:** `packages/workflows/src/activities/connectorPoll.ts` (`onRecords` — the completed-meeting discrimination + meeting-dispatch route), `apps/worker/src/composition/buildActivities.ts` (the bridge wiring). `workflows.ts:241` `meetingCloseoutWorkflow` is the target (no change unless the production start needs a signature tweak — flag if so).
If beyond this, flag at Step 2.5.

## RED test outline (Step 2)
1. `meeting_record_dispatches_meeting_closeout_not_source` — a completed-meeting record ⇒ `dispatchMeetingCloseout` (meeting path); `registerSource` is NOT called (no generic-source bypass). (§19.2/§9 — the flagship fix)
2. `source_record_still_dispatches_source_ingestion` — a generic source record ⇒ the 15.1 `registerSource` path (unchanged); `dispatchMeetingCloseout` NOT called (no misroute). (no regression)
3. `meeting_dispatch_is_idempotent_by_workflowid` — a re-dispatched same completed-meeting reuses the deterministic `workflowId` ⇒ `REJECT_DUPLICATE`, no duplicate closeout. (rule 3 / mirror dispatchSourceIngestion)
4. `meeting_workspace_from_bound_instance_not_content` — the meeting's workspace/routing is stamped from the BOUND connector instance, never a record-content field (WS-8; mirror 15.1). (rule 4)
5. `fake_completed_meeting_runs_closeout_end_to_end` — a fake Granola-shaped completed-meeting → bridge → dispatch → `meetingCloseoutWorkflow` (TestWorkflowEnvironment) ⇒ a `ProposedAction` (transcript→closeout→propose, via the meeting machinery). (§19.2 Done-when / G1)
6. `meeting_transcript_flows_through_the_gate` — the transcript is candidate data routed THROUGH the workflow's gate, never around it (rule 2). (candidate-gate)

## Cross-doc invariant impact
- **Model:** none new — consumes the existing `MeetingCloseoutInput`, `ProposedAction`. (task 15.9 "Cross-doc invariant: none.")
- **Shared-contract seam touched?** No (unless a `meetingCloseoutWorkflow` start-signature tweak is needed — flag if so).

## Things to flag at Step 2.5
1. **Meeting-vs-source discrimination (LOAD-BEARING).** How does `onRecords` know a record is a completed-meeting (→ meeting dispatcher) vs a generic source (→ registerSource)? My default vote: the record/connector declares a type/kind (a completed-meeting kind, e.g. a Granola completed-meeting) that `onRecords` discriminates on; a completed-meeting → `correlateMeeting → dispatchMeetingCloseout`, else → the 15.1 source bridge. Confirm the exact predicate — it must be unambiguous (a meeting never runs source; a source never runs meeting) and derived from the BOUND instance / the record's structural kind, NOT an inferred content field (no-inference, WS-8).
2. **Deterministic meeting identity = workflowId (idempotency).** What is the meeting's canonical deterministic key (→ the `workflowId`)? Mirror dispatchSourceIngestion (its idempotencyKey = workflowId). My default: the completed-meeting's canonical identity (e.g. the calendar-event-id + transcript-id, or the Granola note id) → a deterministic `workflowId` so a re-poll dedupes. Confirm against `MeetingCloseoutInput`'s identity.
3. **`correlateMeeting` → `MeetingCloseoutInput`.** How the bridge builds the `MeetingCloseoutInput` from the completed-meeting record (transcript + calendar correlation). Confirm `correlateMeeting` (existing) is the builder; the bridge calls it, then dispatches.
4. **Reachability / waiver scope.** The onRecords seam is real (15.1). Does the meeting-bridge bind into it now (reachable via the fake-record e2e), with only the real Granola/calendar TRANSPORT waivered (Phase-16/23)? Confirm the exact reachable-vs-waivered split (mirror 15.1).

## Dependencies + sequencing
- **Depends on:** 15.1 (the onRecords bridge seam), P7 (§9 workflows), the existing meeting machinery (`correlateMeeting`, `meetingCloseoutWorkflow`, `MeetingCloseoutInput`).
- **Blocks / closes:** **G1** — the audit's flagship gap CLOSES when this lands (15.1 ✅ + 15.9). This is the *transcript → closeout note → propose tasks* production trigger.

## Estimated commit count
**1–2.** The `dispatchMeetingCloseout` dispatcher may split from the `onRecords` bridge wiring. **security-reviewer = invariant** (the meeting-vs-source routing discrimination + the WS-8 workspace-from-bound-instance + rule-2 candidate-gate-through-the-workflow + rule-3 idempotency). code-quality every-slice. **Pure-build — NO hard line** (fires a fake completed-meeting; the real Granola/calendar transport is Phase-16/23; the dispatch reuses the existing Temporal idempotency substrate; no external egress/spend).

## Lessons-logged candidates anticipated
- **Convention candidate** — the production meeting-closeout dispatcher mirrors `dispatchSourceIngestion` (deterministic identity = Temporal `workflowId` ⇒ two-guard idempotent dedupe) and the completed-meeting bridge DISCRIMINATES a meeting record (→ the meeting-specific `correlateMeeting`/propose machinery) from a generic source (→ registerSource), by the record's structural kind + the bound instance (never an inferred content field), routing the transcript THROUGH `meetingCloseoutWorkflow`'s gate — closing G1's meeting-half so the flagship *transcript → closeout → propose* has a real production trigger.

## How to invoke
1. Continue the worker-impl3 session.
2. Read this brief (Q1 discrimination + Q2 idempotency identity are load-bearing) — this is the **G1 flagship**, worth the care.
3. `/tdd production_meeting_closeout_dispatcher`.
4. Step 2.5 — ping the design answers. Don't GREEN until I sign off.
5. Step 9 — categorized flags; confirm G1 is now CLOSED (15.1 ✅ + 15.9). After 15.9 → the G5 park-write.
