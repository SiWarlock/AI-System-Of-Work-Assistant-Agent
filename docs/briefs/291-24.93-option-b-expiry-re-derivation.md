# /tdd brief — option_b_expiry_re_derivation

## Feature

Close `### 24.93`: a recorded rejection in `packages/policy/src/visibility.ts` **expires the moment `### 24.84`
lands**, and nothing announces it. **Re-derive Option (B) against the post-`24.84` schema** and either revive it
or record why it still fails — **on a ground that does not depend on the brand's looseness.**

⛔ **MEASUREMENT AND DECISION FIRST. This brief does NOT authorise implementing Option (B).**

## Use case + traceability

- **Task ID:** `### 24.93` · **Safety: rule 4** ⇒ Step-9 flags route to me **AND** `team-lead`.
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- **Depends:** `### 24.84` contracts leg (**HELD — see the coupling below**) · **Blocks:** —

> ⚠ **Widens phase scope because** `§5`/`§16` are outside Phase 24's declared set (`§13 §19.11 §3 §4 §6`).
> ⛔ Not resolved by adding them (`### 24.54` ruling).

## ⭐ WHY YOU EXIST THIS ROUND

`packages/policy` was unstaffed. Contract found this **in your territory while shipping their own slice** and
flagged rather than fixed; I filed it as ownerless rather than handing policy work to a staffed area, because
**a fact about staffing is not a fact about correctness** (`L120`). **The lead then spawned you.** ⇒ you are the
owner this finding was waiting for, and the narrow pointer-annotation workaround I was authorised to use instead
is **moot** — a real owner does the real fix.

## ⛔ THE FINDING — verified at source by me before dispatch (gate method)

`packages/policy/src/visibility.ts` ~`:463-467` records, verbatim:

> ⚠ A SHAPE remedy is NOT foreclosed and is not this task's: `### 24.84` exists to give `WorkspaceIdSchema` a
> defensible shape, which needs no counterpart and is symmetric across `from`/`to`. **Option (B) was rejected
> against the CURRENT schema (`.min(1)` + non-blank), so that rejection expires when `24.84` lands.**

**Confirmed live at HEAD; nothing has landed on this file since `7362d199`.** `denyDirectCrossWorkspaceRaw`
(`:388`) still interpolates unvalidated `from`/`to` into audit `refs` and still does not run the brand.

⛔⛔ **THE DEFECT IS NOT THE REJECTION — IT IS THAT A CONDITION WAS STORED WHERE NOTHING WATCHES IT.** The note is
**true** until `### 24.84` commits and **false** the instant it does, and its firing produces **no observable
event**. ⇒ ⭐ **`L176`'s fires-and-looks-silent direction, which is the harder one: the trigger was authored
correctly, names a real event, and has no mechanism to announce that the event occurred.** ⛔ **A condition whose
firing is invisible is not a condition — it is a comment with a date on it.**
⭐ **And it is `L187` from the other side: the author DID defend the decision at the site where it mattered. An
expiry condition needs a WATCHER, not just a location.**

## ⛔⛔ COUPLING YOU MUST KNOW BEFORE YOU START — `### 24.84` IS HELD ON AN OPEN CATEGORY-4 DECISION

Contract's brand is **written but NOT committed.** An escalation is live on whether the **read-path schema should
carry the write-path pattern** — because emitting it into `gcl-projection.schema.json` makes ajv reject a stored
row **before** the visibility stage, so `auditOf` returns `undefined` and the redaction gate is never reached
(knowledge's measurement); and because `packages/db`'s `workspace-read-gate.ts` threat model — *"the ONLY path to
a `WorkspaceSchema` violation is an out-of-band edit"* — **is falsified**, which can strand `egressRevoke`, the
**§5 emergency egress-OFF control**.

⭐ **WHAT THAT MEANS FOR YOU, AND WHY YOU ARE NOT BLOCKED: under EVERY option on the table the WRITE brand still
gains a defensible shape.** ⇒ **the expiry fires regardless, and Option (B)'s re-derivation is valid work now.**
⛔ **What is undecided is whether that shape is available at READ time** — so if your re-derivation concludes
Option (B) is viable, **say what it would validate and WHERE**, and flag any dependence on read-time
availability. ⚠ **Do not design a remedy that re-performs the same write/read collapse one layer down.**

## Acceptance criteria

- [ ] **Option (B) is re-derived against the POST-`24.84` shape**, with the ground stated explicitly — revived,
      or still-rejected **on a reason that does not depend on `.min(1)`+non-blank**.
- [ ] ⛔ **The note is rewritten to name the schema it was decided against — a commit or a version, never
      "CURRENT."** A relative word is what made this expire silently.
- [ ] ⛔ **Not closed by deleting the note** (`L153` half 1: a correction owes a replacement). It records a real
      design decision and an asymmetry — `from` needs an authenticated identity, `to` needs an entitlement check
      — **which survives regardless of the shape question.**
- [ ] **If Option (B) is revived, it is FILED as its own task, not implemented here** — and the filing is checked
      to exist (`L182`).
- [ ] **Any claim that the expiry "has not fired yet" states how that was established** — contract's brand is in
      the working tree, so *uncommitted* is not *not-in-effect* (a hold defers the COMMIT, not the EFFECT).
- [ ] `/preflight` scoped honestly — ⚠ **the tree carries three areas' WIP; name whose, and scope any green
      claim to your own files.**

## Wiring / entry point (Step 7.5)

**None — comment/decision correctness.** No production surface changes in this slice. If Option (B) is revived,
its wiring belongs to the task you file, not here.

## Files expected to touch

**Modified:** `packages/policy/src/visibility.ts` (the recorded-rejection note only).
⛔ **Not touched:** `packages/contracts/**` (contract's, held) · `packages/knowledge/**` (knowledge's, in flight)
· `packages/db/**` · `apps/worker/**`.

## RED test outline (Step 2)

⚠ **The subject is a recorded decision, so there is no behaviour to pin.** Substitute discipline:

1. **Establish the expiry's firing condition precisely** — what exactly about the schema the rejection depended
   on, and what the post-`24.84` shape provides instead. **Cite `file:line` for both.**
2. **A discriminating check on your own claim:** if you conclude Option (B) is *still* rejected, state a concrete
   input the new shape would NOT catch. ⛔ **A rejection with no counter-example is an assertion, not a finding.**
3. **If revived:** state what it validates, where, and — per the coupling above — **whether it needs the shape at
   read time.**

## Cross-doc invariant impact

**None expected.** ⚠ If the re-derivation changes what `§5` guarantees about cross-workspace refs, that is an
`ARCHITECTURE.md` note — **flag at Step 9; I write it, you do not.**

## Things to flag at Step 2.5

1. ⛔⛔ **Your re-derivation and its ground. This is the review surface.** *"Still rejected"* needs its
   counter-example; *"revived"* needs its validation site.
2. **Does the `from`/`to` asymmetry survive the shape remedy?** My default vote: **yes, and say so explicitly** —
   a shape validates *well-formedness*, never *authenticity* or *entitlement*, so the asymmetry the note records
   is orthogonal and must not be deleted along with the stale premise.
3. **Is `denyDirectCrossWorkspaceRaw` the only site with this coupling?** My default vote: **measure it, do not
   assume** — `:239` mentions "ONE residual, TWO sites (`24.65`)", so there is a sibling. Report method + control.
4. **Should the note carry a WATCHER rather than a date?** My default vote: **yes if cheap** — e.g. phrase it so
   `spec-lint`/`plan-lint` could one day assert it, or reference a task that owns the re-check. I would rather
   hear "not cheaply" than have it silently re-expire.

## Dependencies + sequencing

- **Depends on:** `### 24.84`'s SHAPE existing in the tree (it does, uncommitted). **Not blocked by the held
  commit** — the expiry fires under every option.
- **Blocks:** nothing.

## Estimated commit count

**1.** Comment/decision correctness, rule-4 subject ⇒ its own commit.

## Lessons-logged candidates anticipated

- **Convention candidate** — an expiry condition needs a watcher, not just a location; a relative word
  ("CURRENT") is what lets it expire silently.
- **Architecture-doc note candidate** — `§5` on what a shape remedy does and does not guarantee for cross-workspace refs.

## How to invoke

1. Read this brief end-to-end. ⛔ **Re-derivation before any edit; it comes to me at Step 2.5.**
2. Run `/tdd option_b_expiry_re_derivation`.
3. **Step 9** — rule 4 ⇒ routes to me AND `team-lead`.
