# /tdd brief — gcl_denial_gate_pin_green_under_both

## Feature

`24.45` (providers, **complete and HELD**) stops `validateProjectionVisibility` embedding an unvalidated `workspaceId` in `audit.refs`. That breaks `gcl-projection.test.ts:353`, whose fixture depends on exactly that value being unsafe. **Change the knowledge-side pin so it is GREEN UNDER BOTH the current and the post-`24.45` producer** — which is what makes the pair land with no red window at any commit.

## Use case + traceability

- **Task ID:** `24.45` — the **knowledge leg** of a lead-authorized cross-track pair (`### 24.45` is providers' side)
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16` · safety rules 4 and 7
- **Related context:** `24.44` (wrote the test **and predicted this break in-file**) · `24.33` (placed the gate) · `### 24.55` (the dead-code protection, folded in — see below) · `L84` (*the guard is not unasserted, it is UNREACHED*) · `L106` · `L82`

> ⛔ **Widens phase scope because** `### 24.45` declares `Spec: §5, §16`, neither in Phase 24's declared anchor set. Declared, not resolved by editing it — systemic version tracked at `### 24.54`.

## ⛔ THE ORDERING IS A LEAD RULING. Read it before anything else.

**Sequence: YOU land first (this slice) → providers lands `24.45` → done. No red window, both commits in-territory, no cross-territory write.**

⛔ **Why your change must be green under BOTH producers, and why the obvious move is wrong:** *"update the fixture to expect persistence"* reds **immediately**, because providers' producer change is not in the tree yet. **The only ordering with no red window is a knowledge-side change that passes against the producer as it is TODAY and as it will be AFTER `24.45`.**

⛔⛔ **IF YOUR DERIVATION FINDS NO GREEN-UNDER-BOTH CHANGE EXISTS, STOP AND COME BACK TO ME.** That becomes a genuine red-window decision and it is **the lead's**, explicitly: *"do not let anyone resolve it by picking the shorter red window on their own authority."*

## Premises

1. **The failing assertion:** `gcl-projection.test.ts:353` — `expect(auditPersist.calls).toHaveLength(0)` gets `1` once `24.45` lands, because the fixture's `workspaceId` (`https://u:hunter2@evil.example`) stops reaching `audit.refs`, so the signal becomes redaction-**safe** and the gate correctly persists it.
2. ⭐⭐ **YOUR PREDECESSOR PREDICTED THIS IN THE FILE, and their ruling stands.** `:350-352`: *"⚠ COUPLING (documented, not hidden): if 24.45 resolves via validate-or-omit, this exact unsafe value stops reaching audit.refs and this fixture breaks — that is a correct signal to **fix the fixture** (a different unsafe-signal construction), **not to delete the test**."* ⛔ **The lead declined to overturn that, explicitly: not adding a constraint, declining to overturn one a better-positioned party wrote down in advance.** ⇒ **THE TEST IS NOT DELETED OR WEAKENED IN ANY OUTCOME.**
3. **`:347-349` also records that the fixture value deliberately trips `URL_USERINFO_CREDENTIAL` and NOT `SENSITIVE_KEYWORD`** — *"swap it if that rule changes."* **The author had already thought about what makes the fixture's unsafety specific; use that.**
4. ⛔ **PROVIDERS' BOUNDED NEGATIVE — verified by them, and its BOUNDARY is your opening.** By enumeration over `validateProjectionVisibility` (two interpolations `:144`/`:147`; nine literal `afterSummary` values; `actor`/`event`/`payloadHash`/`beforeSummary` literal; `denialCode` closed): **no field IN THAT ENUMERATION can carry attacker-controlled text.** ⚠ **Stated as the bounded form, never *"the gate is unreachable"*.** ⭐ **They did NOT enumerate the SIBLING gates composed into `admitProjection` (raw-content / schema), which could have their own audit-bearing errors — and said so. That check is YOURS, and it is the one that decides whether option (i) exists.**
5. **`crossWorkspaceRead.ts:124` pre-filters** `projection.workspaceId !== link.toWorkspaceId`, so **from that caller the mismatch branch never fires.** ⚠ **Do not assume that branch is where a rebuilt fixture can enter.** ⛔ **This does NOT weaken providers' fix** — the policy predicate must hold independently of any caller's discipline, and the write path has no such filter.
6. **Providers' slice is COMPLETE and HELD, not in progress.** `@sow/policy` 493/493, `turbo typecheck` 20/20, one failing test repo-wide and it is this one.

## Your options — the choice is YOURS, not mine and not providers'

**(i) Source an unsafe signal from a DIFFERENT gate in `admitProjection`'s composition.** ⭐ **PREFERRED by the lead, because it preserves the real-chain pin, which is what the test is FOR.** **Availability depends on premise 4's unenumerated sibling gates — run that check first; it is the highest-value thing in this slice.**
**(ii) Re-pin the routing property without relying on an unsafe signal.** Second choice.
**(iii) Accept that the real-chain unsafe case is no longer constructible and say so in-file.** ⛔ **ONLY with the finding filed** — *"the real-chain unsafe case became unreachable and the test must change what it pins"* — **NOT a fixture tweak absorbed into a green diff.** That is `L84`'s shape and a sibling of `24.46`.

⚠ **Whichever you choose, it must satisfy the green-under-both constraint. (iii) most easily does; (i) is best if it exists. Do not trade the pin away for convenience, and do not keep a pin that no longer pins anything.**

## ⛔ FOLD IN: `### 24.55` — the dead-code protection

**`24.45` makes `persistDenialAudit`'s `isRedactionSafe` call unreachable *from `validateProjectionVisibility`*. It is NOT dead code — it is the sole redaction gate for the other ~40 `buildAuditSignal` producers.** ⇒ **add an in-code note at the call site** (`packages/knowledge/src/gcl/projection.ts`, ~`:61`) stating the structural fact and why the gate survives its own unreachability. **Per `L94` the in-code note is the load-bearing home — the person who would delete it reads the file, not the tracker.**
⛔ **SCOPE THE CLAIM: never the unqualified *"`isRedactionSafe` is unreachable"*** — that is false for the ~27 unenumerated producers and is exactly the over-broad form `24.45` exists to retract. ⚠ **`L145`-safe: state what is structurally true, never that a path is live.**

## Acceptance criteria

- [ ] **The changed pin is GREEN against the producer as it is today** — demonstrated by the suite passing on the current tree, providers' files still uncommitted.
- [ ] ⛔ **AND green against the post-`24.45` producer — demonstrated, not reasoned.** ⚠ **You cannot commit providers' change; establish this by a declared, reverted local experiment** (apply their `visibility.ts` delta in your working tree, run the test, **revert byte-identical**) — `L139`'s declared-window discipline. **State both runs in Step 9.**
- [ ] **The test still pins a real property.** ⛔ **If it ends up asserting something weaker, say so explicitly** — a pin that survives by being emptied is worse than a filed finding.
- [ ] **If (iii): the finding is FILED, with the mechanism**, not folded into the diff.
- [ ] **`### 24.55`'s in-code note lands at the `isRedactionSafe` call site**, scoped.
- [ ] **Nothing outside `packages/knowledge/`** — providers' four files stay untouched.
- [ ] `/preflight` clean, **per-package scoped** (repo-wide measures three writers' union).

## Wiring / entry point (Step 7.5)

**No new production wiring.** The surfaces are live-and-reached in the sense that `admitProjection`/`serveProjection` are real code paths, but ⚠ **providers established that `serveProjection`'s only production caller (`resolveApprovedCrossWorkspaceSlice`) has ZERO production callers** — **the read path is not live.** ⛔ **Do not describe this slice as fixing a live exposure; it is a test-integrity + precondition slice.**

## Files expected to touch

**Modified:** `packages/knowledge/test/gcl-projection.test.ts` (the pin) · `packages/knowledge/src/gcl/projection.ts` (`24.55`'s in-code note only — **no behaviour change**)

⛔ **DO NOT TOUCH — providers' four HELD files, which are UNCOMMITTED IN YOUR TREE:**
```
packages/policy/src/audit-signal.ts
packages/policy/src/visibility.ts
packages/policy/test/audit-signal.test.ts
packages/policy/test/visibility.test.ts
```
⛔ **Run `git diff --cached --name-only` BEFORE `git commit` and assert none appear.** Per-file staging only; `git add -A`/`git commit -a` banned. **The before-check is the gate; `git show --name-only` after is a receipt (`L109`).**

## RED test outline (Step 2)

⚠ **This slice CHANGES an existing pin rather than adding a behaviour, so the RED is unusual — say which of these you actually got.**

1. **The changed pin, run against the CURRENT producer** — must be green.
2. **The changed pin, run against the POST-`24.45` producer** (declared local experiment, reverted) — must be green. ⭐ **This is the acceptance criterion that cannot be satisfied by reasoning, and it is the whole point of the ordering.**
3. **If (i): the new unsafe construction genuinely trips the gate** — assert the refusal happens for the reason you intend, and **name which `isRedactionSafe` rule it trips** (premise 3's precedent: the old fixture recorded that it hit `URL_USERINFO_CREDENTIAL` and deliberately not `SENSITIVE_KEYWORD`). ⛔ **A fixture that passes for the wrong reason is this round's recorded failure — the orchestrator suggested exactly such a fixture on `24.44` and it was caught.**
4. **`24.55`'s note is not testable** — state it as `not-tested-because:` rather than dropping it silently.

## Cross-doc invariant impact

- **Model field changes:** none.
- **Orchestrator doc rows:** none anticipated. ⚠ **If (iii), flag it — "the real-chain unsafe case is unreachable" may deserve a §5/§16 line, and I write it.**
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⭐⭐ **Does an unsafe-signal construction exist in `admitProjection`'s SIBLING gates (raw-content / schema)?** This decides (i) vs (ii)/(iii) and **nobody has run it** — providers explicitly scoped it out as knowledge's. **My default vote: run this FIRST, before designing anything.** ⛔ **Report it as a bounded negative with its method if the answer is no** (`L100`) — *"no construction found by this enumeration,"* never *"none exists."*
2. **If (i) exists, is it green under BOTH producers?** A construction unsafe only *after* `24.45` fails the ordering. **My default vote: prefer one whose unsafety is independent of the `workspaceId` path entirely.**
3. **If (iii): what exactly does the test pin afterwards?** ⛔ **My default vote: it must still pin something real, and you must say plainly what was lost.** *"The refusal is pinned against a hand-built signal"* is the sibling convention (`:173-179`) — ⚠ **and that convention is itself under scrutiny as the third instance of the false coverage claim, so do not reach for it as if it were unexamined.**
4. **Should `24.55`'s note ride this commit or its own?** **My default vote: this one** — it is a comment, it is in a file you're touching, and splitting it risks it being lost. **Say no if you'd rather keep the pin change isolated.**

## Dependencies + sequencing

- **Depends on:** `24.26` step 1 ✅ `e1293226` (your previous slice) · providers' `24.45` **complete and HELD**
- **Blocks:** ⛔ **providers' `24.45` commit — they cannot land until this does.** ⚠ **They are holding an uncommitted safety slice in a shared tree; the window is a real cost and the lead has said to report it if it stretches.**

## Estimated commit count

**1.** ⛔ **Do not bundle beyond `24.55`'s note.**

## Lessons-logged candidates anticipated

- **Convention candidate** — *a fixture that depends on a producer's defect is coupled to that defect's fix; the coupling belongs in the fixture's own comment* (your predecessor did this, and it converted a surprise into a scheduled event — the strongest case for the habit this project has produced).
- **Convention candidate** — *green-under-both is the no-red-window ordering for a SEMANTIC coupling, where `24.44`'s type-level legality does not exist.*

## How to invoke

1. **Read this brief end-to-end**, especially the ordering ruling and premise 4's boundary.
2. **Run `/tdd gcl_denial_gate_pin_green_under_both`.**
3. **Step 2.5** — Q1 first; it decides the shape of everything else.
4. **Step 9** — state **both** runs (current producer + reverted local experiment). ⛔ **If no green-under-both change exists, STOP at Step 2.5 and return to me — that decision is the lead's.**
