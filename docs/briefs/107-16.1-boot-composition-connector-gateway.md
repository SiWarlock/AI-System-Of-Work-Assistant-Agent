# /tdd brief — connector_gateway_boot_composition (16.1)

## Feature
Compose `ConnectorPorts` + a `ConnectorGateway` at worker boot over the connector adapter factories (the 7 vendor read adapters + url/telegram), **against a FAKE HttpTransport with NO tokenRef bound** — today zero `create*Connector` / `create*HttpTransport` call exists in `apps/worker/src` or `packages/workflows/src` (non-test); the factories are exported only from the `packages/integrations` adapter barrel and never composed at boot. This stands up the connector-engine substrate INERT: the gateway exists + all adapters are wired, but no adapter can perform a live vendor call (no real transport, no tokenRef). Worker-track. **Pure-build (dormant substrate) — NO hard line** (binding a real network transport is Phase 23). Closes **G32**.

## Use case + traceability
- **Task ID:** 16.1. **Closes:** G32.
- **Architecture sections:** `ARCHITECTURE.md §19.3` (primary — connector engine composition), `§8` (Connector Gateway external-reads engine).
- **Depends on:** P6 (§8 gateways). **Enables:** 16.2 (the poll registration binds this composed gateway).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **The adapter barrel — `packages/integrations/src/connectors/adapters/index.ts`:** exports `makeConnector`, `createConnectorHttpTransport`, the `ConnectorSpec` type, + the 7 vendor read adapters + url/telegram. Confirm the exact factory set + their construction signatures.
  - **The FAKE transport:** the composition binds a fake/stub `HttpTransport` (no real `send`, no tokenRef) — mirror how the existing connector tests construct a fake transport. The shipped default performs ZERO live vendor calls.
  - **`ConnectorGateway` / `ConnectorPorts`:** confirm the gateway's shape + what "composed" means (the port set the gateway exposes). NEW file `apps/worker/src/composition/connectors.ts` is the composition home.

## Acceptance criteria (what "done" means)
- [ ] **Composed gateway at boot:** `apps/worker/src/composition/connectors.ts` composes a `ConnectorGateway` over `ConnectorPorts` built from all adapter factories (7 vendor + url/telegram) against a FAKE transport. Reachable from `bootWorker` (or the composition root it calls).
- [ ] **Zero live vendor call (dormant, byte-equivalent):** NO adapter performs a live vendor call — no tokenRef is bound, the transport's real `send` is unbound (the shipped default is inert). Pin with a factory-spy / no-real-transport assertion (mirror the dormancy posture, worker L11/L27).
- [ ] **All adapters present:** the composed gateway exposes every adapter (a missing adapter is a gap) — pin the adapter set.
- [ ] **No tokenRef / no secret read:** composition reads no secret (SecretsPort untouched — that is Phase 17). Confirm at Step 8.
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
`createConnectorGateway`-composition (new `connectors.ts`) is reached from `bootWorker` — name the exact binding site. State: *"Reachable from `bootWorker` → `composeConnectors()`."* The real transport binding is Phase-23-waivered (L11) — the composed gateway is inert-but-reachable.

## Files expected to touch
**NEW:** `apps/worker/src/composition/connectors.ts` (the composition). **Modified:** `apps/worker/src/boot.ts` (or the composition root — bind the composed gateway) + tests. Consumes (type-only + factory imports) `packages/integrations/src/connectors/adapters/index.ts` (barrel — do NOT modify it; it's providers-integrations territory). If a factory signature needs a change, FLAG it at Step 2.5 (cross-track — coordinate via the lead; integrations-impl owns that file).

## RED test outline (Step 2)
1. `boot_composes_a_connector_gateway_over_all_adapters` — the composed gateway exposes every adapter (7 vendor + url/telegram). (§19.3/§8)
2. `composed_gateway_binds_no_real_transport` — the shipped default binds a FAKE transport; no real `send` / no tokenRef (factory-spy zero-invocation). (dormancy / NO hard line — L11/L27)
3. `composition_reads_no_secret` — SecretsPort is never consulted (Phase-17 boundary). (rule 7 / dormancy)

## Cross-doc invariant impact
- **Model:** none (task 16.1 "Cross-doc invariant: none"). **Shared-contract seam touched?** No — composition only.

## Things to flag at Step 2.5
1. **Dormancy (LOAD-BEARING).** Confirm the composed gateway binds NO real transport + NO tokenRef — the shipped default is inert (pin factory-spy zero-invocation). This is the "no hard line" guarantee.
2. **Adapter-factory signature.** If composing an adapter needs a change to its factory (in `packages/integrations`), that's a CROSS-TRACK touch — flag it; the lead coordinates with integrations-impl (don't edit their file).
3. **Fake-transport construction.** Confirm the fake transport mirrors the existing connector-test fake (not a new bespoke stub).

## Dependencies + sequencing
- **Depends on:** P6 (§8 gateways). **Blocks:** 16.2 (poll registration binds this gateway). **Parallel-safe with:** the integrations leaf slices (16.4/16.5 — disjoint files). **Closes:** G32.

## Estimated commit count
**1.** A focused composition slice. **security-reviewer = invariant** (the dormancy/no-real-transport + no-secret-read guarantee — the connector engine is a trust boundary even inert). code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief (Q1 dormancy is the load-bearing safety point).
3. `/tdd connector_gateway_boot_composition`.
4. Step 2.5 — ping the dormancy + fake-transport + any cross-track factory concern to the lead. Don't GREEN until sign-off.
5. Step 9 — categorized flags; confirm G32 + the no-real-transport dormancy. After 16.1 → 16.2 (poll registration + connectorSyncHealth workflow + schedule).
