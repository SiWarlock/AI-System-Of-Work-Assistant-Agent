# /tdd brief — widen_reconcile_vault_reachability_census_repo_wide

## Feature
`24.13`'s two `execSync` census tests (pinning "zero production callers" and "no production override" for `reconcileVault`/`runWakeReconcile`/`createVaultWatcher`/`verifyKwSig`/`KwSigVerifier`/`defaultVerifyKwSig`) scan `apps/` only. The safety argument they back is repo-wide. Widen the scan to `apps/` AND `packages/`, correctly excluding non-call-site mentions.

## Use case + traceability
- **Task ID:** `24.29`
- **Architecture sections it implements:** §6 — Safety rule 1

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase — its tasks originate from the 24.6 safety-assertion audit, which sweeps every area, so a remediation task legitimately cites the anchors its originating finding lives under rather than only Phase 24's own declared set.**

- **Related context:** `IMPLEMENTATION_PLAN.md:3155-3159` (task 24.29, full text — read this first). Your own disclosed residual from `24.13`'s Step 9 — closing it yourself is the point (lead's framing).

## Acceptance criteria (what "done" means)
- [ ] Both census tests (or a third, if cleaner) scan `apps/` AND `packages/` — not `apps/` alone.
- [ ] `packages/knowledge/src/gcl/global-markdown-reconcile.ts`'s prose mention of `createVaultWatcher` (a comment, not a call site) is correctly excluded from the widened census — confirm this by reading the actual line first, don't assume the exclusion pattern from `24.13`'s original design still fits.
- [ ] Re-confirm the "zero production callers" claim actually holds at the wider scope BEFORE trusting it in the commit message — it may not. If the widened census finds a real caller somewhere in `packages/`, that changes `24.13`'s severity retroactively and must be reported as a finding, not silently absorbed.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
None — this widens an existing test's scan scope; no production code changes.

## Files expected to touch
**Modified:**
- `packages/knowledge/test/reconcile.test.ts` — widen the two `execSync` census patterns

## RED test outline (Step 2)
This is a test-only change with no production RED/GREEN in the usual sense — the "RED" here is: does the current `apps/`-only census pass while a `packages/`-scoped run would (hypothetically) catch something it doesn't? Confirm the widened scope actually changes what's scanned (i.e., the test isn't a no-op widening) by checking the census's file count/glob before and after.

1. **`census_scans_packages_and_apps`** — Asserts: the census's own file-enumeration step includes at least one file under `packages/` in its scanned set (a meta-test on the census mechanism itself, not on the codebase's callers). Why: proves the widening is real, not cosmetic.
2. **`census_excludes_the_known_comment_reference`** — Asserts: `global-markdown-reconcile.ts`'s prose mention of `createVaultWatcher` does not cause the census to report a false "caller found." Why: the specific residual named in `24.13`'s Step 9.
3. **`census_still_reports_zero_production_callers_or_flags_a_real_one`** — Asserts: run the widened census and assert its actual current result (zero, or a real finding) — write this test to assert whatever is TRUE after you run it, not what you expect. Why: this is the whole point of the task; don't pre-write an assertion that begs the question.

## Cross-doc invariant impact
- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** none.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **If the widened census DOES find a real production caller in `packages/`, what's the disposition — stop and escalate immediately, or finish widening the test (so the finding is pinned) and then flag at Step 9?** My default vote: **finish widening the test first** (so the fact is captured in a running, committed assertion rather than a verbal report that could be lost), then flag prominently at Step 9 — this is a safety-relevant finding if it happens, but the test existing IS the record, and a half-finished slice with an unpinned verbal claim is worse than a complete one reporting bad news.

## Dependencies + sequencing
- **Depends on:** `24.13` ✅ `7a4fe0ac` (the tests being widened already exist).
- **Blocks:** trusting `24.13`'s boot guard as a repo-wide guarantee.

## Estimated commit count
**1.** Small, focused test-scope widening.

## Lessons-logged candidates anticipated
- None anticipated beyond what `L118`/`L93` already cover (already banked, this is the fix for the gap those lessons named).

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md:3155-3159` (task 24.29).
2. Run `/tdd widen_reconcile_vault_reachability_census_repo_wide`.
3. Step 2.5's one question matters if the census turns something up — otherwise take the default and proceed.
