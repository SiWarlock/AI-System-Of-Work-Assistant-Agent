# /tdd brief — committed_l64_armed_autoingest_dryrun_harness

## Feature
A **committed, env-gated `-live` integration test** that boots the worker ARMED (subscriptionArm + autoIngest + vaultRoot + egress-allowlist + the `-live` Temporal seam) with a **FAKE completion seam ($0)**, drops ONE benign non-employer `.md` on the watched vault, and asserts a **real note is produced** end-to-end through broker→candidate-gate→KnowledgeWriter. This is the **L64 go/no-go** (proves the armed auto path produces a note, not spend-and-produce-nothing) — a clean repeatable `npx vitest run …` command, NOT a throwaway harness. **$0, no real subscription call, no real spend, flips nothing.**

## Use case + traceability
- **Task ID:** 18.33
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (real model transport / auto-ingest arming), `§9` (source ingestion proof-spine), `§7` (broker candidate-gate), `§6` (KnowledgeWriter commit)
- **Related context:** Worker Lesson 64 — arming a candidate KIND is not enough; the WORKER `CANDIDATE_MODEL_SCHEMAS` parser + the `outputSchemaId → sow:agent-extraction` flip must both be reachable, else an armed run emits candidate→`schema_rejected`→EMPTY→no note (real spend, zero deliverable). The maiden run's go/no-go was a **fake-completion dry-run** proving broker-accepts + note-produced BEFORE the first real spend — but those harnesses were **throwaway/uncommitted** (session 098). This slice makes that go/no-go a **committed** `-live` test so it's a repeatable command, not a fabricated string. Depends on the egress-allowlist seam (18.31) so the armed cloud route passes the veto, and mirrors the desktop-assembled armed config shape (18.32) so the two slices compose to prove the in-app path.

## Acceptance criteria (what "done" means)
- [ ] A committed env-gated `-live` test (extend `apps/worker/test/integration/vaultWatcher-live.test.ts` or a sibling) that: boots the worker with an ARMED config — `subscriptionArm:{enabled:true, makeCompletion: <FAKE returning a valid agent_extraction candidate>, checkReachable: <returns reachable>}` + `autoIngest:true` + `vaultRoot` + `egressAllowedProcessors:["claude-agent-sdk"]` (the 18.31 seam) + the existing `-live` Temporal seam — drops ONE benign non-employer `.md` on the watched path, and asserts a **note is committed** through the real broker→candidate-gate (`agent_extraction` schema)→`validateNoInference`→sole KnowledgeWriter `applyPlan`.
- [ ] **$0 / no real call:** the fake `makeCompletion` returns a deterministic candidate with `costUsd 0` (or equivalent) — assert no real subscription/network call occurs (the fake seam is the only completion source). `createClaudeSubscriptionCompletion` is NEVER invoked.
- [ ] **The note is REAL (L64):** assert the committed note exists with the expected `agent_extraction`-derived content/frontmatter — proving broker-ACCEPT + note-PRODUCED (not `schema_rejected`→EMPTY). An absent datum yields `TBD` (REQ-F-017), never invented.
- [ ] **Composition tie with 18.32 (anti-drift — lead ask).** The harness's armed plain-data shape (`autoIngest:true` + `subscriptionArm.enabled:true` + `egressAllowedProcessors:[<the canonical claude-agent-sdk processor id>]`) uses the **same worker-owned types** 18.32 targets (`AutoIngestGateOpts` + `BootConfig.subscriptionArm`) — so tsc breaks BOTH tests on any field-shape drift (the structural tie; no hand-mirror possible at the type level). Reference the **canonical worker processor-id constant** (derived from `CLOUD_EXTRACTION_ROUTE.runtime`, `apps/worker/src/composition/extraction-route-gate.ts` — NOT a magic string) for the allowlist value, so "the value that produces a note" has a single worker-side source of truth. The FAKE `makeCompletion`/`checkReachable` are the ONLY worker-side addition atop that desktop-producible plain-data shape (the documented IPC residual). Add a comment cross-referencing brief 146 / the 18.32 `arming-forward` test. **Note the layer fact:** the desktop is processor-AGNOSTIC (it forwards an opaque owner-set string); this slice owns the `claude-agent-sdk`→note semantic — so there is no hand-mirrored value copy to drift.
- [ ] **Env-gated (default-skip):** the test is gated behind the existing `-live` env (e.g. `SOW_TEMPORAL`/the `-live` gate) so the default `pnpm test` suite does NOT run it; the go/no-go is an explicit `npx vitest run <path> -t "<name>"` command. Name the test so the command is clean + self-documenting.
- [ ] The feedback-loop guard still holds: the produced note (under `sources/<ws>/`) does NOT re-fire the watcher (assert no second dispatch; L37).
- [ ] `/preflight` clean (the new test skips by default; runs green under the `-live` gate).

## Wiring / entry point (Step 7.5)
This is a **test/harness slice** — no new production wiring. It exercises the REAL production path: `startVaultWatcher` → capture → `dispatchSourceIngestion` → `bootWorker`-assembled armed broker (`assembleBackends` with `subscriptionArm` armed + `withSubscriptionExtractionArming` applied) → candidate schema-gate (`agent_extraction`) → `validateNoInference` → sole KnowledgeWriter `applyPlan`. The ONLY injected seam is the FAKE `makeCompletion` (+ `checkReachable`) — every gate (admission/egress-veto/schema/no-inference) runs REAL. No production code changes; if a tiny production test-seam is unavoidable (e.g. exposing an injection point), flag at Step 2.5 — prefer none.

## Files expected to touch
**Modified (or New sibling):**
- `apps/worker/test/integration/vaultWatcher-live.test.ts` — add the armed-path `-live` dry-run test (or a new `apps/worker/test/integration/autoIngest-armed-live.test.ts` sibling if cleaner).
- (test support) a fake-completion helper returning a deterministic valid `agent_extraction` candidate at `costUsd 0` — reuse any existing fake-completion test util if one exists (check the maiden-run-era test supports / `subscription-extraction-runner.test.ts` fakes) rather than re-authoring.

**Ideally NO production file changes.** (The egress-allowlist seam is 18.31; the desktop config is 18.32.)

## RED test outline (Step 2)
1. **`armed_autoingest_benign_source_produces_note_fake_completion`** — the full armed dry-run: fake completion → benign `.md` drop → assert a committed note via KnowledgeWriter.
   - Asserts: a note is committed; its content derives from the fake `agent_extraction` candidate; `costUsd 0`; `createClaudeSubscriptionCompletion` never called.
   - Why: §19.5/§7/§6 + L64 — the armed auto path produces a note (the go/no-go).
2. **`armed_autoingest_absent_datum_is_TBD_not_invented`** — the fake candidate omits one datum ⇒ frontmatter/field is `TBD`, never invented.
   - Asserts: `TBD` sentinel, no invention.
   - Why: REQ-F-017 no-inference, proven on the armed auto path.
3. **`armed_autoingest_note_does_not_refire_watcher`** — the committed note under `sources/<ws>/` does not re-dispatch.
   - Asserts: exactly one dispatch; the output-subtree note is excluded.
   - Why: §9 / L37 (G6) feedback-loop guard holds under arming.
4. **`armed_autoingest_denied_without_egress_allowlist`** (negative control) — same armed boot but `egressAllowedProcessors:[]` ⇒ the cloud route is DENIED `PROCESSOR_NOT_ALLOWED`, NO note.
   - Asserts: DENY + no commit — discriminates that the allowlist (18.31) is load-bearing (non-vacuous green; L7).
   - Why: §5 — the egress allowlist OFF-lock is real, not decorative.

## Cross-doc invariant impact
- **Model field changes:** none — test-only.
- **Orchestrator doc rows to write hot:** none anticipated (a worker LESSONS candidate — the committed L64 go/no-go harness convention — is likely; flag at Step 9).
- **Shared-contract (Appendix-A) seam touched?** No.

## Things to flag at Step 2.5
1. **Extend `vaultWatcher-live` vs a new `autoIngest-armed-live` sibling?** My default vote: **a new sibling `autoIngest-armed-live.test.ts`** — the armed dry-run is a distinct concern from the existing dormant `vaultWatcher-live` characterization, and a dedicated file gives a clean `-t`/path command. Flag if extending the existing file is cleaner given shared setup.
2. **Fake-completion seam source.** My default vote: **reuse an existing fake-completion test util** (the maiden-era CP2/dry-run harnesses were throwaway, but the unit tests `subscription-extraction-runner.test.ts` likely have a fake `ClaudeSubscriptionCompletion` — reuse it). If none is reusable, author a minimal deterministic fake returning a valid `agent_extraction` candidate at `costUsd 0`. Confirm no production test-seam is needed.
3. **Env gate name.** My default vote: **the existing `-live` gate** (whatever `vaultWatcher-live`/`sourceIngestion-live` already use, e.g. `SOW_TEMPORAL`) so it composes with the existing `-live` infra; do NOT invent a new gate. Confirm the gate + document the exact go/no-go command in the test header.
4. **Real vs fake Temporal.** My default vote: **the existing `-live` Temporal seam** (no real managed server needed for a $0 fake-completion dry-run) — the arming under test is the extraction/egress path, not Temporal durability. Flag if the armed path forces a real Temporal.

## Dependencies + sequencing
- **Depends on:** **18.31** (the `egressAllowedProcessors` seam — the armed cloud route needs `claude-agent-sdk` allowlisted to pass the veto and produce a note). Independent of 18.32 (this harness boots `bootWorker` directly; it does not go through the desktop IPC path — but its armed config MIRRORS 18.32's assembled shape so they compose).
- **Blocks:** the owner's in-app ENABLE go/no-go (this IS the go/no-go command the lead runs before the owner arms).

## Estimated commit count
**1 commit** — test/harness slice; security-reviewer MANDATORY (it exercises the armed egress/candidate-gate path — the reviewer confirms the fake seam can't mask a real safety-gate bypass, and the negative control is non-vacuous).

## Lessons-logged candidates anticipated
- **Convention candidate** — the L64 go/no-go is a COMMITTED env-gated `-live` fake-completion dry-run (real arm+broker+gate+KnowledgeWriter, $0), NOT a throwaway harness — a repeatable pre-spend note-produced proof + a non-vacuous egress-allowlist negative control.
- **Architecture-doc note candidate** — the armed auto-ingest path's note-produced go/no-go is now a standing test (§19.5).

## How to invoke
1. Read this brief end-to-end.
2. Confirm 18.31 has landed (the `egressAllowedProcessors` seam exists) before Step 4.
3. Run `/tdd committed_l64_armed_autoingest_dryrun_harness`.
4. Step 2.5 — ping back with answers to the 4 design questions (esp. new-file vs extend, fake-seam source) before GREEN.
5. Step 8 — **security-reviewer MANDATORY** (armed egress/candidate-gate path + non-vacuity of the negative control).
6. Step 9 — surface the L64-harness lesson candidate.
