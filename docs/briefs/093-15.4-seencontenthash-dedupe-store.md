# /tdd brief — seen_content_hash_dedupe_store (15.4)

## Feature
A **persisted `seenContentHash` dedupe store** (Flow-4 / REQ-F-010): a durable dual-dialect `@sow/db` store the ingestion checks/records so re-seen content is skipped exactly-once across restart — replacing the in-memory dedupe that loses state on restart. Worker/db-track; the same operational-store pattern as the Phase-14 registries (WorkspaceConfig / ProjectRegistry / ConnectorInstance / CrossWorkspaceLink). Pure-build — the 15.1 bridge already references a `seenContentHash` dedupe check (its `dedupe_hit` test); this makes it durable.

## Use case + traceability
- **Task ID:** 15.4
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (primary — Flow-4 dedupe), `§4` (operational store).
- **Related context (confirm at Step 1):**
  - **The consumer:** the 15.1 bridge (`a6190122`) checks a `seenContentHash` before dispatch (a hit ⇒ no dispatch); today that check is an in-memory/fake seam. This slice makes it a durable `@sow/db` repo (survives restart — exactly-once dedupe).
  - **Pattern (you've built it 4×):** a db-owned Row DTO + dual-dialect (SQLite + Postgres) table + migration + repo passing the ONE repo-contract suite on both dialects; keyed by the dedupe identity; **first-write-wins** (like the KnowledgeRevisionStore idempotency, worker Lesson 3); fail-closed both directions.
  - **The dedupe key:** the content-versioned identity (workspace-scoped `seenContentHash` — confirm the exact key shape at Step 1; likely `(workspaceId, contentHash)` so dedupe is WS-8-scoped, never cross-workspace).

## Acceptance criteria (what "done" means)
- [ ] A `@sow/db` seen-content-hash table + dual-dialect migration + a `SeenContentHashRepository` (`has(key)` / `record(key)` — or the exact shape from Step 1) passing the ONE repo-contract suite on BOTH dialects.
- [ ] **First-write-wins / exactly-once:** `record` of an already-seen key is idempotent (no duplicate, no error on the second record — UNIQUE); `has` returns true iff previously recorded. Survives a simulated restart (durable, not in-memory).
- [ ] **WS-8-scoped:** the dedupe key is workspace-scoped (a `contentHash` seen in workspace A does NOT dedupe the same hash in workspace B — never cross-workspace). A test pins the positive keying leg (A recorded under (A,hash) does not make (B,hash) a hit).
- [ ] **Fail-closed both directions (worker Lesson 3):** a store fault on `has` ⇒ typed err (the caller decides — do NOT silently return false/true and mask); a store fault on `record` ⇒ typed err (reject, never mask). Distinguish a genuine fault from the benign not-seen.
- [ ] `/preflight` clean (worker + repo-wide; repo-contract both dialects).

## Wiring / entry point (Step 7.5)
The store is the durable backing for the 15.1 bridge's `seenContentHash` check. Confirm at Step 1 whether this slice BINDS the real repo into the bridge's seam now (if the bridge's seam is reachable) or ships the repo + a binding deferred to the Phase-16 drive path (the bridge itself is reachability-waivered — don't wire dormant-on-dormant, Lesson 11). My expectation: ship the durable repo + bind it into the bridge's dedupe seam where that seam is exercised (the bridge is built; the repo is a real backing for its already-tested dedupe check) — confirm the cleanest reachable binding at Step 1.

## Files expected to touch
**New:** `packages/db` seen-content-hash schema (sqlite + pg) + migration; `SeenContentHashRepository` interface + both adapters; tests (repo-contract + the WS-8 keying + fail-closed pins).
**Modified (confirm at Step 1):** the repo-contract suite registration; possibly the 15.1 bridge's seam binding (if reachable) or a Future-TODO for the Phase-16 binding.

If beyond this list, **flag at Step 2.5**.

## RED test outline (Step 2)
1. `seen_content_hash_repo_contract_both_dialects` — has/record round-trip on both dialects; first-write-wins (double record ⇒ one row, no error). (§4)
2. `migration_up_down` — applies + reverts with the version guard. (forbidden-#4)
3. `dedupe_is_workspace_scoped` — (A,hash) recorded ⇒ has(A,hash)=true, has(B,hash)=false (positive keying leg). (WS-8)
4. `record_is_idempotent_exactly_once` — re-record ⇒ no duplicate, no error (UNIQUE first-write-wins). (REQ-F-010)
5. `has_and_record_fault_are_typed_errs` — a store fault ⇒ typed err both directions (never mask as false/true). (Lesson 3; the typed-Result error convention)
6. `survives_restart` — record, re-open the store, has=true (durable, not in-memory).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model:** NEW — the seen-content-hash Row (db-owned operational record; not a frozen Appendix-A contract). Orchestrator writes the cross-doc/arch note at round close.
- **Shared-contract seam touched?** No → no schema-snapshot test.

## Things to flag at Step 2.5
1. **Dedupe key shape** — `(workspaceId, contentHash)` (WS-8-scoped), or does the source identity add more (sourceId, connectorId)? My default vote: `(workspaceId, contentHash)` — WS-8-scoped, minimal; confirm against the 15.1 bridge's `seenContentHash` check shape.
2. **Binding now vs Phase-16 waiver** — bind the real repo into the bridge's dedupe seam now (if reachable) or ship the repo + defer the binding? My default vote: ship the durable repo + bind where the seam is exercised (the repo is a real backing for the bridge's already-tested dedupe check); if the only binding site is the dormant Phase-16 drive path, ship the repo + a Future-TODO (don't wire dormant-on-dormant).
3. **fault-vs-not-seen** — a store fault must be distinguishable from a benign not-seen (fail-closed, typed err, never mask). Confirm the caller (the bridge) handles a `has` err fail-closed (a fault ⇒ HOLD/skip per REQ-I-005, never a false not-seen that re-dispatches).

## Dependencies + sequencing
- **Depends on:** P2 (§4 storage), 15.1 (`a6190122` — the bridge's dedupe consumer).
- **Blocks:** the Phase-16 drive path (durable dedupe across restart); 15.5 (disposition) is adjacent.
- **NOTE (contract-track gap):** 15.2 (SourceEnvelope +body field) is a FROZEN-seam change = CONTRACT-track (not worker); the 15.2→15.3 body-threading chain needs a contract-impl — flagged to the lead separately.

## Estimated commit count
**1–2.** The db store (table + migration + repo-contract) may split from any bridge-seam binding. **security-reviewer=invariant** (WS-8-scoped dedupe key + fail-closed both directions); code-quality every-slice. Pure-build — NO hard line.

## Lessons-logged candidates anticipated
- **Convention candidate** — a persisted dedupe store is a first-write-wins WS-8-scoped `@sow/db` repo (survives restart, fail-closed both directions distinguishing fault from not-seen), replacing an in-memory dedupe that loses exactly-once across restart (extends worker Lesson 3).

## How to invoke
1. `/tdd seen_content_hash_dedupe_store` (session oriented from 15.1 — no /session-start unless swapped).
2. Step 1 — confirm the dedupe-key shape against the 15.1 bridge's `seenContentHash` check + the binding site (now vs Phase-16 waiver).
3. Step 2.5 — ping back the design answers (Q1 key + Q2 binding are load-bearing). Don't GREEN until I sign off.
4. Step 9 — categorized flags.
