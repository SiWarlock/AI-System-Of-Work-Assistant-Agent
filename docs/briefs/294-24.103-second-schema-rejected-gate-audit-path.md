# /tdd brief — second_schema_rejected_gate_audit_path

## Feature
Close the same silent-refusal gap `### 24.98` closed, on the **second** candidate-data gate: `generative-proposal-intake.ts` mints a `schema_rejected` shape on its **own local union** with no audit signal at all — and derive the population of such unions **by property**, so the census is not blind to its own subject.

## Use case + traceability
- **Task ID:** `### 24.103`
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KnowledgeWriter / candidate-data gate), `§16` (candidate-data gate, redaction)
- ⛔ **This brief widens phase scope because** `§16` is where redaction and the audit path live and is not in Phase 24's declared set (`§13 §19.11 §3 §4 §6`), while `### 24.103`'s own `Spec:` line cites it. Declared per `spec-lint`'s option (a). ⛔ **NOT resolved by widening Phase 24's anchor set** — lead ruling 2026-07-29; the systemic defect is tracked at `### 24.54`.
- ⛔ **Safety: rule 7** — same class as `### 24.98`, a refusal that produces no audit record. Step 9 auto-routes to the lead.
- **Related context:** `### 24.98` (`124e3f45` — the pattern this reuses) · `L192` · `L196` · brief `293`

---

## ⛔⛔ THE SITUATION — GATE-CHECKED AT SOURCE, AND THE OBVIOUS FIX IS THE DANGEROUS ONE

**Confirmed open, verified by the orchestrator before dispatch (not taken from the flag):**
- `:104-105` declares the local union — `code:"schema_rejected"`, `stage:"ajv"|"zod"`, `issues`.
- `:134` and `:140` construct it with **no `audit` field**.

⛔⛔ **AND THE PART THAT MAKES THIS A RULE-7 BRIEF RATHER THAN A CHORE — BOTH SITES ALREADY CAPTURE `message`:**
- `:134` → `{ path: e.path, message: e.message }` (ajv)
- `:140` → `{ path: i.path.join("."), message: i.message }` (Zod)

⭐ **`### 24.98` MEASURED that Zod's `invalid_enum_value` and `unrecognized_keys` EMBED ROW-AUTHORED CONTENT IN `message`.** ⇒ ***this union is already holding the echoing field; it simply has nowhere to send it yet.*** ⛔ **Whoever adds the missing signal will reach for `issues`, because that is what is there — and that is the leak.**

⭐ **`### 24.98`'s answer transfers verbatim and must NOT be re-derived from scratch:** structural material only — `code`, `stage`, **cut** paths, counts — with **`message` excluded categorically**, and paths cut at any row-keyed region. **It was measured once; the measurement lives in `### 24.98` and `L192`.**

⛔⛔ **WHY THIS IS A SEPARATE TASK AND NOT A COPY-PASTE: `### 24.98` made `audit` REQUIRED on the GCL union, so `tsc` enumerated every construction site and found a third nobody had listed. A SEPARATE LOCAL UNION INHERITS NONE OF THAT.** ⇒ ⭐ ***the compiler's enumeration stops at the union boundary, so a duplicated SHAPE is invisible to exactly the instrument that secured the original.*** **That is the finding; the fix is the easy half.**

---

## Acceptance criteria (what "done" means)
- [ ] A `schema_rejected` denial from this gate produces an `AuditSignal` that reaches a redaction-gated persist path, or — if no persist path exists at this layer — the signal is **produced and the absence of a consumer is stated as a scoped claim**, never as coverage.
- [ ] ⛔ The signal is **redaction-safe by construction**: no field derived from `GateIssue.message` or any other row-derived string. **The predicate that selected what may cross is stated in-file.**
- [ ] ⛔ **Row-keyed path regions are cut**, as `### 24.98` does — a row-authored key must be **unrepresentable** in `refs`, not merely absent today.
- [ ] The fail-closed refusal is **not weakened** — the candidate stays refused; it stops being refused *silently*. Pinned.
- [ ] ⛔ **The population of `schema_rejected`-shaped unions is derived BY PROPERTY** — *"refuses candidate data without producing an audit signal"* — **with method, control and boundary stated.** ⚠ **NOT by spelling:** `### 24.92` / `### 24.100` / `### 24.101` each show spelling- and position-based censuses blind to their own subjects. **Report the predicate that selected the members** (`L190`).
- [ ] ⛔ **The one-union question is ANSWERED EXPLICITLY, not left implied** — merging these unions would restore the compiler enumeration, which is a **safety property, not tidiness** (lead ruling). ⚠ **If the answer is "keep them separate," say what replaces the enumeration.**
- [ ] Every new/changed safety pin asserts **WHY** it failed (cause/code), never a bare falsity (`### 24.101`).
- [ ] Any fixture chosen for a **conjunction** of properties pins each conjunct separately (`L196`).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
`intakeGenerativeProposal` in `packages/knowledge/src/gbrain/remediation/generative-proposal-intake.ts` — **name its production callers at Step 2.5.** ⚠ **If the honest answer is that no persist port exists at this layer yet, that is legitimate and producer-first — but then write the SCOPED claim** (*"produced and gated; no adapter persists it, see `<task>`"*) **and name the consumer slice as a real tracked task in this round.** ⛔ Never the unqualified *"this gate is now audited."*

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gbrain/remediation/generative-proposal-intake.ts` — the union + both construction sites (`:134`, `:140`)
- `packages/knowledge/test/generative-proposal-intake.test.ts` — pins (10 tests exist today)

Flag at Step 2.5 if the census pulls in files beyond these.

## RED test outline (Step 2)
1. **`schema_rejected_from_this_gate_produces_an_audit_signal`** — Asserts: rejection code **and stage**, plus a defined signal identifying the gate **by code**.
2. **`signal_carries_no_row_derived_content`** — Asserts: a sentinel reaching `issue.message` appears in **no** field of the signal, **with a non-vacuity guard that the sentinel really did reach `message`** — so a message-derived implementation fails. ⛔ **And the fixture must TRIP the redaction heuristic** (`L192`) — a neutral sentinel makes this assertion decorative.
3. **`row_authored_key_cannot_reach_refs`** — Asserts: with a row-keyed region populated, no row-authored key appears in `refs`. Prove by **mutation**: disabling the path cut reds exactly this pin.
4. **`candidate_is_still_refused`** — Asserts: still an `err` with the **code asserted**, not a bare falsity.
5. **`census_pins_the_derived_population`** — Asserts: the by-property population is what the fix covers, with the predicate stated.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** the union is knowledge-local. ⛔ **If `AuditSignal` itself would change, STOP — that is a `@sow/policy` contract surface; flag it.**
- **§2.5-seam model touched?** <!-- spec-lint:mention --> Not expected; flag at Step 2.5 if the census says otherwise.

## Things to flag at Step 2.5
1. ⛔ **Q1 — ONE UNION OR TWO?** Merging restores the compiler enumeration that made `### 24.98` safe; keeping them separate preserves layer independence. **My default vote: report the trade with a recommendation and let it route** — the lead ruled this is a **design call, not cleanup**, so it is not yours to close silently and not mine to pre-empt. ⚠ **Whatever the answer, say what replaces the enumeration if they stay separate.**
2. **Q2 — is there a persist port at this layer at all?** If not, Step 7.5's scoped-claim path applies and the consumer slice must become a tracked task this round (`L106`/`L51`).
3. **Q3 — does the census find anything beyond these two unions?** Derive by property; report the predicate and the boundary. ⛔ **A count without its method is not a result.**
4. **Q4 — does the ajv side echo here?** `### 24.98` measured that `schema-gate.ts` drops `e.params`, but **`:134` uses `e.path`/`e.message` from a different error object.** ⚠ **Measure it for THIS call path rather than inheriting the conclusion** — that inheritance is exactly `L193`.

## Dependencies + sequencing
- **Depends on:** `### 24.98` (`124e3f45`) — **landed.** The pattern is code, not a decision.
- **Blocks:** —
- ⚠ **Contract is mid-landing on `### 24.84` in the shared tree.** Different package; no expected conflict. **Scope every green claim to your own files and name whose WIP was live.**

## Estimated commit count
**1**, unless the census pulls in a third union — in which case flag at Step 2.5 rather than growing the slice silently.

## Lessons-logged candidates anticipated
- *A duplicated shape on a separate union is invisible to the enumeration that secured the original* — likely the durable form of this task's finding.
- Whether "one union" is the general remedy for compiler-enumeration blind spots, or whether it trades one blind spot for a coupling.

## How to invoke
```
/tdd second_schema_rejected_gate_audit_path
```
⛔ **Report results as passed / failed / skipped** — your own Step-10 point, applied from the first run.
