# /tdd brief — narrow_the_handoff_pin

> ⛔⛔ **NARROW, NEVER DELETE. The pin has TWO assertions and exactly ONE died.** ⭐ **A pin that loses an assertion because a fix landed is being UPDATED; a pin that loses the FILE is being SILENCED.** ⛔ **The lead's leave-it-live hold is RELEASED for this slice and for no other reason — its two conditions (*"through the seal"*, *"in its author's absence"*) are both gone now that you are back in your own territory.**

## Feature

Narrow `packages/policy`'s cross-area handoff pin so it asserts the divergence that still exists, records which axis closed and when, and keeps a named owner for the surviving half.

## Use case + traceability

- **Task ID:** `### 24.128`
- **Architecture sections it implements:** `ARCHITECTURE.md §16`
- ⛔ **widens phase scope because Phase 24's DECLARED anchor set (`§13 §19.11 §3 §4 §6`) does not contain `§16`, while `§16` is what this task implements.** ⭐ **`### 24.54`, a filed and known defect with prior witnesses at `### 24.93`/`### 24.98`/`### 24.99`/`### 24.101`.** ⛔ **Do NOT resolve it by widening Phase 24's declared set — standing lead ruling 2026-07-29. Declared, not silenced.**
- ⛔ **Safety: rule 7** ⇒ **own commit; `security-reviewer` fires (`invariant`).**
- **Related context:** `### 24.120` (LANDED `65524874` + `8b2b53ac` + `bf442753` — **it is what made this pin red**) · `### 24.110` (the delegation, **still BLOCKED**) · `### 24.123` (**BLOCKED-ON-OWNER** — see Q3, it is probably the surviving divergence's owner)

### The state, measured — not inherited from this brief

**`packages/policy/test/audit-signal.test.ts`, the test `the_url_userinfo_axis_still_diverges_and_that_divergence_is_OWNED`, holds two assertions:**

| # | assertion | today |
|---|---|---|
| 1 | `packages/policy` REFUSES `//u:p[REDACTED:raw]q@h` — the span survives because policy does not strip | ✅ **STILL TRUE** |
| 2 | `@sow/domain` judges the same value **SAFE** — the space filler broke the userinfo span | ⛔ **NOW FALSE** — `### 24.120` replaced the filler with a span-preserving one |

⭐⭐ **THE PIN FIRED EXACTLY AS ITS OWN COMMENT PREDICTED IT WOULD, AND THE TRANSFERABLE PART IS SHARPER THAN THAT: it predicted the WRONG REMEDY — *"the minimal fix (substituting `""` instead of `" "`)"* — and the shipped fix was `^` plus a RETAINED space arm. It caught the change anyway, because its MECHANISM prediction held.** ⛔ **Preserve that in the narrowed comment. A cross-area pin that survives the remedy being different from the one predicted is the strongest form of the construct, and it is the reason this file gets narrowed rather than rewritten.**

⛔ **Verify the table above yourself before you touch anything.** `- [ ]` is not evidence, and a brief's premise is a hypothesis.

## Acceptance criteria (what "done" means)

- [ ] **Assertion 2 (the URL-userinfo axis) is REMOVED** — it asserts a falsehood.
- [ ] **The MARKER/KEYWORD axis is now PINNED in its place** — `[REDACTED:credential]` is refused by `packages/policy` and judged safe by `@sow/domain`, **policy STRICTER**. ⛔ **This is the axis the original comment deliberately declined to pin, and the reason it declined has EXPIRED — see Q1. Do not carry the old reasoning across.**
- [ ] **The test's NAME no longer names the closed axis.** The current name asserts the URL axis "still diverges"; it does not.
- [ ] **The comment records WHICH AXIS CLOSED AND WHEN**, and preserves the wrong-remedy-right-mechanism result above. ⛔ **`L194` — correct the claim, do not delete the artifact naming the relationship.**
- [ ] **The surviving divergence has a NAMED OWNER in the comment** (Q3) — *"policy stricter blocks nothing today"* is exactly the kind of fact that goes stale silently, which is why the Done-when demands an owner rather than a note.
- [ ] **`packages/policy` is GREEN** — the deliberate red is cleared **because it was answered**, not because it was quieted. Report the before count (`1 failed / 521 passed`) and the after.
- [ ] ⛔ **NO ASSERTION IS WEAKENED OR REMOVED ANYWHERE ELSE IN THE FILE.** `### 24.110`'s landing reported **zero deletions** across a +179 test diff; hold the same bar. Report the file's test count before and after.
- [ ] ⛔ **NOTHING IS DELEGATED.** `### 24.110`'s delegation half is still BLOCKED. This slice does not touch `packages/policy/src/audit-signal.ts`'s composition.
- [ ] `/preflight` clean **for your own files**; name whose red any other red is.

## Wiring / entry point (Step 7.5)

**This slice is a TEST-ONLY narrowing — there is no production entry point and that is correct.** The pin's "reachability" is the suite: it must RUN and it must be able to FAIL.

⛔ **THEREFORE THE STEP-7.5 OBLIGATION IS INVERTED AND IT IS THE ONE THAT MATTERS HERE: MUTATION-PROVE THE NARROWED PIN.** Break the surviving divergence deliberately (make policy stop refusing the marker) and show the new assertion REDS; restore byte-identical and show the sha matches. ⛔ **A mutation producing GREEN is ambiguous — either the pin is blind or the mutation never applied. Prove the mutation applied, by diff, for every green outcome.**

⚠ **And the fixture must be one the predicate actually discriminates.** Three `isRedactionSafe` assertions in this repo used a sentinel matching NONE of the heuristic's patterns — **they ran, passed, and proved nothing.** An assertion against a HEURISTIC needs a fixture the heuristic actually catches.

## Files expected to touch

**Modified:**
- `packages/policy/test/audit-signal.test.ts` — the one test; its name, its comment, its assertions

⛔ **That is the whole list.** If anything else looks necessary — including `packages/policy/src/audit-signal.ts` — **STOP and flag at Step 2.5.** A narrowing that reaches into source is not a narrowing.

## RED test outline (Step 2)

⚠ **This slice's RED is unusual and worth naming so Step 0 does not restate it wrongly: the RED ALREADY EXISTS — it is the live failing assertion.** Your Step 2 is the *replacement* assertion, which must be shown to fail against a mutated predicate before it is trusted.

1. **`the_marker_keyword_axis_still_diverges_and_that_divergence_is_OWNED`** (or your name — it must name the SURVIVING axis)
   - **Asserts:** `packages/policy` refuses `[REDACTED:credential]`; `@sow/domain` judges it safe. **Policy stricter.**
   - **Why:** `### 24.128` Done-when; §16. The one divergence that outlived `### 24.120`.
   - ⛔ **Mutation-proven both ways**, per Step 7.5 above.
2. **A control that could have disagreed** — ⛔ **`029`'s rule: concordance is evidence ONLY when the methods could have disagreed.** Before citing the two predicates as independent, state what each could have returned that the other could not. If the answer is nothing, you have one measurement written twice.

## Cross-doc invariant impact

- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** likely none — flag at Step 9 if the narrowed comment states a relationship the `packages/contracts/CLAUDE.md` cross-doc table should carry. **I write those rows.**
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⛔ **THE OLD COMMENT'S REASON FOR *NOT* PINNING THE KEYWORD AXIS HAS EXPIRED — CONFIRM THAT, DO NOT ASSUME IT.** It reads: *"there policy is STRICTER, which blocks nothing. Pinning that axis would have documented the deferral with a case that does not justify it."* ⭐ **That was correct WHEN THE URL AXIS EXISTED — the keyword axis was the weaker of two reasons.** ⇒ **It is now the ONLY divergence, so it is no longer being asked to justify a deferral; it is being asked to keep a real difference from going silent.** **Default vote: pin it, and say in the comment why the earlier decision not to was right at the time.** ⚠ **This is the round's own pattern — an assessment made about one question inherited by a different one.**
2. **DOES THE TEST GET RENAMED, OR SPLIT INTO TWO?** Options: (a) rename in place; (b) keep the name and add a second test. **Default vote: (a) rename in place** — the name is a load-bearing claim and a stale name is `### 24.71`'s rotted-pointer class. ⚠ **If you rename, the rename and the edits that justify it are ONE change and must stage together** — a rename staged alone lands as a pure `R100` and looks complete while the body still says the old thing (`029`, measured).
3. ⛔⛔ **WHO OWNS THE SURVIVING DIVERGENCE? MY ANSWER IS `### 24.123`, NOT `### 24.110`, AND IT IS REASONING NOT MEASUREMENT — CHECK IT.**
   I tried the `(C')` monotone trick on `### 24.110`'s delegation today (recorded on that entry, commit `40bbf578`). Under a `(C')`-shaped delegation — `domain.looksUnsafe(s) || policy_today(s)` — **`[REDACTED:credential]` is still refused, because policy's own arm is OR-ed in.** ⇒ ***the marker/keyword divergence SURVIVES the delegation entirely; the delegation does not own it.*** **What would retire it is a decision to stop refusing already-redacted content on the sole-writer path — and that is `### 24.123`'s span/granularity work.**
   **Default vote: name `### 24.123` as the owner, and say the delegation does not retire it.** ⛔ **If you can falsify that, say so — it is a claim about ANOTHER area's entry and I would rather be corrected here than have it land in a comment.**
4. **HOW MUCH OF THE OLD COMMENT SURVIVES?** It is ~15 lines of genuinely load-bearing reasoning about a mechanism that is now CLOSED. **Default vote: retain the reasoning in past tense, strip the state.** ⭐ **`L195`: striking a block does not tense-shift the sentences inside it — narrative survives forever, a STATE claim has no way to announce it stopped being true.** ⛔ **Retain the reasoning; past-tense or strip the state.**

## Dependencies + sequencing

- **Depends on:** `### 24.120` (LANDED — **verify against git before starting**, that is the dispatch gate).
- **Blocks:** nothing mechanically. ⭐ **But it clears the ONE deliberate red in the shared tree, which makes every other session's green claim cheaper to make** — and this round has three implementers in one checkout.
- **Explicitly NOT in this slice:** `### 24.110`'s delegation (BLOCKED, and now for a stated testable reason — read the `(C')` block on its entry before you form a view) · `### 24.124` (word boundary — not monotone) · `### 24.118` step 2 (**your leg, but it is BLOCKED behind contract's `### 24.118` step 1, which is in flight now**).

## Estimated commit count

**1.** ⛔ **Rule-7 ⇒ its own commit.** Do not fold `### 24.126` in.

⛔ **COMMIT FORM — `git commit -F <msgfile> -- <explicit paths>` ONLY.** The index is SHARED with two other live sessions. The add-then-commit form commits *the index* — everything **any** session has staged — and it swept two foreign files last round while the author's own check printed the staged set in the same invocation. ⭐ **A check only gates if a DECISION sits between reading it and acting; collapsing them makes the gate a RECEIPT.** Read the staged set as its own command. ⛔ **Never `--amend`.**

## Measurement discipline for this slice (the toolchain is wrapped — `### 24.122`)

- **Counts from `git rev-list` / `--numstat`.** Never from counting `+`/`-` in a diff body; never from a bare `git log` (silent 50-cap).
- **`turbo … --force`** and quote the `Cached:` line — a cached green replays in ~55ms having executed nothing. **Use `--continue` for suite scope, or a cascade reads as a second failure.**
- ⛔ **Positive-control every empty result.** An empty result is *calming* and closes the question — which is exactly why the under-reporting direction never gets a second look. `grep` fabricated a count twice in my first three commands today.
- ⚠ **A raw U+2028/U+2029 in a `//` comment TERMINATES the comment and breaks the parse.** Write the token as text. This bit four sessions in one evening, in the fixtures that test line-terminator hazards.
- ⚠ **`scripts/guards/git-guard.sh` cannot tell a command from a document about one (`### 24.125`) — it will block a commit message or session doc that QUOTES a banned form, and it blocks unrelated commands containing the word.** If it fires on prose, **say so on the record rather than silently paraphrasing** — silent paraphrase is the failure mode that entry names as the worst.

## Lessons-logged candidates anticipated

- **Convention candidate** — *a pin whose assertion dies because the fix landed is UPDATED; one that loses its file is SILENCED — and the difference is whether the surviving relationship still has a named owner.*
- **Convention candidate** — *a reason for not pinning something is scoped to the alternatives that existed when it was written.* (Q1.)
- **Architecture-doc note candidate** — the surviving policy↔domain marker divergence, if the narrowed comment ends up asserting something §16 should carry.

## How to invoke

1. **Read this brief end-to-end.**
2. **Verify `### 24.120`'s landed state and the two-assertion table against source** before touching anything.
3. **Run `/tdd narrow_the_handoff_pin`.**
4. **Step 2.5** — the write-up + coverage map, and **answer Q1 and Q3 explicitly.** ⛔ **Q3 is my claim about another area's entry; falsifying it is the useful outcome.**
5. **Step 9** — categorized flags + ship-ask.
