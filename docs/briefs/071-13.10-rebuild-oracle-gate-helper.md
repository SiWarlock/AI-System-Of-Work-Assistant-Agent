# /tdd brief — rebuild_oracle_gate_helper (Phase-4 producer arc, piece B)

## Feature
Build the **default-OFF `gateRebuildOracle` boot-gate HELPER** — the pure activation seam that turns piece A's `probeRebuildOracle` producer (committed `210e95e`) into a boot-resolvable `resolveOracleBuild: () => boolean` accessor for `createServingCoverageReader`, but ONLY when the owner has provisioned a real `IndexRebuildClient`. This is the **Lesson-23 split**: piece B adds the pure helper **byte-equivalent BY CONSTRUCTION** (an unreferenced exported helper — NO `bootWorker` edit, so it cannot change boot); piece C (next brief) adds the `bootWorker` call site + the async boot-await + cache + the `createServingCoverageReader` binding. Mirror `gateReconcile`/`ReconcileWiring` (`apps/worker/src/boot.ts:652`, arc piece F1, `e9c6d77`) exactly — same shape, same co-location, same OFF discipline.

**Still no hard line.** The gate's real `IndexRebuildClient` factory stays **UNBOUND** by default (owner-gated — the real gbrain scratch-import). Default (no factory) ⇒ gate returns `undefined` ⇒ `resolveOracleBuild` stays unbound ⇒ `oracleBuildOk` false ⇒ shipped default byte-equivalent. Binding a real client is the owner's arming crossing (piece C leaves it unbound; owner provisions at arming).

## Use case + traceability
- **Task ID:** 13.10 (Copilot propose/serving-oracle go-live arc — runbook Phase 4 producer, piece B of A→B→C).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (the serving-coverage `oracleBuildOk` leg + the rebuild-oracle producer arc — note `57fef4b`/piece-A note) + `§12` (parity GO conditions → serving degrades on any non-green leg).
- **Mirror precedent (follow it closely):** `gateReconcile(opts, deps): ReconcileWiring | undefined` (`apps/worker/src/boot.ts:652`; `ReconcileGateOpts:624` / `ReconcileWiring:630` / `ReconcileGateDeps:635`), test `apps/worker/test/boot/reconcileGate.test.ts`. F1 was byte-equivalent by construction (no `bootWorker` edit; strict `=== true`; factory-spy zero-invocation OFF pin). Worker **Lesson 23** (arming-seam) + **8** (independent OFF-locks) + **27** (AND-composed default-OFF selection, type-robust/fail-closed).
- **Consumes (piece A, committed `210e95e`):** `probeRebuildOracle(workspaceId, deps): Promise<RebuildOracleStatus>` + `RebuildOracleProbeDeps { readCommittedVault, rebuildClient, now, newHealthItemId, auditRef }` + `RebuildOracleStatus` (discriminant `outcome`: corroborated|absent|diverged|faulted; `oracleBuildOk` the consumed boolean) — all from `apps/worker/src/composition/rebuildOracleStatus.ts`.
- **Served-workspace source (piece C resolves; B takes as a dep):** `config.copilotGbrainWorkspaceId` (`boot.ts:1051` `servedWorkspaceIdStr`; the served-vault map at `:1139`).
- **Ultimate seam:** `createServingCoverageReader`'s OPTIONAL `resolveOracleBuild?: () => boolean` (`servingContextBootReaders.ts:94-102,139`).

## Acceptance criteria (what "done" means)
- [ ] A pure `gateRebuildOracle(opts, deps): RebuildOracleWiring | undefined` exists in `apps/worker/src/boot.ts`, co-located with `gateReconcile`/`gateAutoIngest`. **No `bootWorker` edit** — an added, unreferenced exported helper; boot byte-equivalent BY CONSTRUCTION (the `bootWorker` function body is outside every hunk).
- [ ] **OFF (default) ⇒ `undefined` + ZERO dep-thunk invocations:** when the owner-gated real client factory is absent/not-a-function (`typeof deps.makeRebuildClient !== "function"`) — or a required precondition is missing (no served workspaces) — the gate returns `undefined` and invokes NONE of its dep thunks (factory-spy zero-invocation pin — THE byte-equivalence proof). Type-robust/fail-closed (a malformed factory value degrades to OFF, never throws at gate time; Lesson 27).
- [ ] **ON (owner-provisioned real client) ⇒ assembled wiring:** returns a `RebuildOracleWiring` carrying a bound async compute step that, for each served workspace, runs `probeRebuildOracle` over `deps.makeRebuildClient()` + the bound `{ readCommittedVault, now, newHealthItemId, auditRef }`, then **folds fail-closed** to a single boot-global boolean and returns a SYNC accessor over it (+ the per-workspace statuses for piece C to route health).
- [ ] **Fold is fail-closed (never a false green):** the boot-global boolean is `true` IFF the served set is non-empty AND EVERY served workspace's status is `corroborated`; an empty served set OR any `absent`/`diverged`/`faulted` ⇒ `false`. The compute step never throws (piece A never throws; defend the fold anyway).
- [ ] **Dormant + reachability-waivered:** no `bootWorker` caller (piece C adds it), real client UNBOUND, no gbrain I/O in build/test (fake client + fake reader). `/preflight` clean; repo-wide green.

## Wiring / entry point (Step 7.5)
**none — reachability-waivered by construction** (mirror `gateReconcile` F1). `gateRebuildOracle` has no `bootWorker` caller; reachable only from its own test. Piece C adds the call site + closes reachability. State the waiver explicitly.

## Files expected to touch
**Modified:**
- `apps/worker/src/boot.ts` — ADD `RebuildOracleGateOpts` / `RebuildOracleWiring` / `RebuildOracleGateDeps` + `gateRebuildOracle`, co-located with `gateReconcile` (`:624-...`). **Do not touch `bootWorker`.**

**New:**
- `apps/worker/test/boot/rebuildOracleGate.test.ts` — mirror `reconcileGate.test.ts` structure.

## RED test outline (Step 2)
1. **`gate_off_absent_client_returns_undefined_zero_invocations`** — `makeRebuildClient` undefined ⇒ `undefined`; a factory/spy over every dep thunk (`readCommittedVault`, `makeRebuildClient`, `now`, `newHealthItemId`) asserts 0 invocations (byte-equivalence proof; §6/§12).
2. **`gate_off_malformed_client_degrades`** — `makeRebuildClient` a non-function value ⇒ `undefined`, no throw (type-robust, Lesson 27).
3. **`gate_off_no_served_workspaces_returns_undefined`** — empty `servedWorkspaceIds` ⇒ `undefined` (nothing to corroborate).
4. **`gate_on_all_corroborate_folds_true`** — real (fake) client makes every served ws corroborate ⇒ wiring's compute step resolves `resolveOracleBuild() === true`; statuses all `corroborated`. *(the green fold)*
5. **`gate_on_any_non_corroborated_folds_false`** — one served ws `diverged` (or `absent`/`faulted`) ⇒ `resolveOracleBuild() === false` (fail-closed AND; multi-ws parametrized).
6. **`gate_on_compute_never_throws`** — drive the assembled compute step over a fake reader/client that would degrade; it resolves a false accessor, never throws (total over dep faults; non-vacuous per Lesson 15).

## Cross-doc invariant impact (implementer flags at Step 9)
- **Model field changes:** NONE — `RebuildOracleGateOpts`/`RebuildOracleWiring`/`RebuildOracleGateDeps` are worker-internal boot-config seams (not contracts), mirroring `Reconcile*`. Reuses piece A's `RebuildOracleStatus` + frozen `IndexRebuildClient`.
- **Orchestrator doc rows (Step 9):** an `ARCHITECTURE.md §6` piece-B note (orchestrator writes). Possible worker-Lesson addendum to the arc lesson.
- **Seam (shared-contract) model touched?** No.

## Things to flag at Step 2.5
1. **Producer gate condition — client-presence vs also-arming-flag.** Option A (my rec): gate ON `typeof makeRebuildClient === "function"` (owner-provisioned real client) — piece C nests it inside the existing `copilotProvenanceStamping && provenanceBundle` construction branch, and `copilotServingOracleGoLive` continues to gate SELECTION separately (unchanged). This matches the runbook's step-6-before-step-7 isolation: the owner binds the client + verifies green coverage (`oracleBuildOk:true`) BEFORE arming selection. Option B: also require `copilotServingOracleGoLive === true` to bind — simpler dormancy but collapses "observe coverage" into "arm selection." **Default vote A** (provisioning-gate pattern, like `keychainSecrets`/`provenanceBundle`; not the effect-flag pattern).
2. **Fold rule.** `true` IFF served-set non-empty AND all `corroborated`; empty/any-non-corroborated ⇒ `false`. **Default vote: this fail-closed AND** (matches the coarse boot-global seam contract + the single-served-workspace shipped model; a healthy multi-ws fold is conservative-by-design). Flag if a per-workspace accessor is wanted instead (the seam takes no ws arg, so no — piece C fold).
3. **One-shot-at-boot-cached vs re-probe.** The accessor is a cached boot-global (mirrors `resolveRunning`, a cached startup probe). **Default vote: one-shot cached** — a stale `true` after a later divergence is an arming-era freshness concern that folds into the reconcile-trigger/schedule (a re-probe trigger), NOT this dormant seam. Name it as an arming follow-up.
4. **Health routing of `diverged` statuses.** B RETURNS the per-workspace statuses; piece C routes any `rebuild_divergence` HealthItem through the health surface (where the surface is in scope — mirrors reconcile F2). **Default vote: B stays pure** (returns statuses, routes nothing).

## Dependencies + sequencing
- **Depends on:** piece A (`210e95e`, committed). Blocks piece C (boot binding).
- **Blocks:** piece C — the `bootWorker` call site + async await/cache + `createServingCoverageReader` binding + reachability closure (still dormant; real client unbound).

## Estimated commit count
**1** — one focused helper slice (boot.ts additive + test). **Mandatory dual review (security + code-quality)** — the seam feeds the serve-time trust gate (safety-relevant). Not bundled.

## Lessons-logged candidates anticipated
- **Convention candidate:** likely an addendum to the arc lesson (piece A's Lesson 29 candidate) — "the producer's boot-gate is a default-OFF, client-presence-gated helper folding per-workspace corroboration to a fail-closed boot-global boolean, byte-equivalent by construction; provisioning the real client (not an arming flag) enables observation before selection is armed." Flag at Step 9 if durable (extends Lesson 23/27).
- **Architecture-doc note candidate:** the `§6` piece-B note (orchestrator writes).
