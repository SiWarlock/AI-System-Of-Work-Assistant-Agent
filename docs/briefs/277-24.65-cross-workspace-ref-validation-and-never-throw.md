# /tdd brief — cross_workspace_ref_validation_and_never_throw

> ⛔⛔ **ERRATUM — 2026-08-13, added at `#48`'s Step 9. THIS BRIEF'S DEFECT-2 PREMISE IS FALSE. Read this before anything below it.**
>
> **Every assertion in this brief that a `null` / `undefined` `sourceWorkspace` THROWS is WRONG** — including the ⛔-emphasized, unhedged one in "Premises" (*"`:195` — unguarded. THIS is the throw"*) and acceptance criterion 4. **Falsified by PROBE, not by reading:** null **and** undefined both return a typed `MALFORMED_POLICY_INPUT` denial and **do not throw**, because `24.45`'s own `?.` in `const srcId: unknown = sourceWorkspace?.id;` makes `srcId` `undefined`, so the `if (wsId !== srcId)` mismatch branch **returns before the `defaultVisibility` guard is ever reached.** ⇒ **§16 never-throw already holds for that input class; the correct work was a PIN, not a fix.**
>
> ⚠ **Q1 in "Things to flag at Step 2.5" was RIGHT about `:191` and WRONG about the scale** — I guessed the `:149-151` comment "overstates by one site." **Its whole claim is false**, and the implementer established that by reading `:126-145` (which I had flagged unread) *and* by probing. **The premise I most expected to break did break, in the direction that mattered.**
>
> ⚠ **`:156`/`:191`/`:195`/`:149-151` NO LONGER RESOLVE** — `#48` added ~12 comment lines and re-pointed every one. ⛔ **`:149-151` now lands on the retraction itself, so a reader following it sees this brief calling *the correction* false.** ⇒ **anchor on quoted symbols, not line numbers.**
>
> ⛔ **NOT REWRITTEN, DELIBERATELY.** This brief is the **fourth** artifact that carried the false claim — after the tracker, the in-file comment, and committed session doc `161:72`. **Silently correcting it would destroy the evidence that the claim propagated across four artifacts, which is the durable finding** (`contracts L148`, banked from this slice: *a residual-documenting comment outlives the residual, falsified by a fix aimed at something else*). **The wrong text stands, marked.**
>
> ⭐ **What the brief got right and still stands:** defect 1 (`from`/`to` interpolated after only a non-empty check) — **real**, dispositioned as residual under `contracts L147` per lead ruling, real fix filed as `#54`. The tracker's misattribution of defect 2 to `denyDirectCrossWorkspaceRaw` — **correctly caught here before dispatch.**

## Feature

`### 24.65` — two defects in `packages/policy/src/visibility.ts`: **(1)** `denyDirectCrossWorkspaceRaw` interpolates `from`/`to` into audit refs after only a non-empty-string check (`24.45`'s class, one function over), and **(2)** an unguarded `sourceWorkspace` dereference **throws** where §16 promises never-throw.

## Use case + traceability

- **Task ID:** `### 24.65`
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16` · Safety **rules 4 and 7**
- ⚠ **`spec-lint` scope note — this brief **widens phase scope because** Phase 24 declares `§19.11 · §6 · §13 · §4 · §3` and its tasks cite a far wider set; `§5` and `§16` are the two most-cited anchors in the phase (23 and 14 occurrences) and sit outside the declared five.** ⛔ **This is `### 24.54`, NOT a real scope widening** — the declared set does not describe the phase, so the escape is the default rather than an exception. **Recorded, not silently invoked**, and ⛔ **the fix is NOT to add these anchors to Phase 24's declared set** (lead ruling: an anchor set that grew because a linter demanded it is worthless).
- **Related context:** `### 24.45` (`48ec7c91`) — you landed its remedy; **this reuses that pattern** (`L39`). Both findings are **yours**, deliberately excluded from `24.45` to keep a safety commit minimal and its zero-delta argument clean.

---

## ⛔ THE TRACKER MISATTRIBUTES DEFECT 2 — CORRECTED HERE, VERIFIED AT SOURCE

`### 24.65`'s **Files** line puts both findings in `denyDirectCrossWorkspaceRaw`. **Defect 2 is not in that function.** `CrossWorkspaceRawRequest` (`:270-275`) has no `sourceWorkspace` field at all.

| Defect | Function | Lines |
|---|---|---|
| **1** — refs after non-empty check only | **`denyDirectCrossWorkspaceRaw`** | `packages/policy/src/visibility.ts:286` |
| **2** — unguarded deref / throw | **`validateProjectionVisibility`** | `packages/policy/src/visibility.ts:124` |

⭐ **Two different functions, ~160 lines apart, in one file.** The `24.49` false-negative shape (a citation that fails to resolve is a question, not a verdict — `contracts L93`), caught before dispatch rather than after.

## Premises — orchestrator-verified at source at HEAD `20df48c7`. Falsify freely.

**Defect 1 — CONFIRMED exactly as filed.** `:292-298` checks only `req != null`, `typeof === "string"`, `!== ""` for both ids. Then **`:314`**:
```ts
const refs: readonly string[] = [`ref:workspace:from:${from}`, `ref:workspace:to:${to}`];
```
⇒ a schema-valid but credential-shaped id renders raw into the audit refs. **Same class as `24.45`.**
⚠ **Different threat model, and this is why it was correctly left out of `24.45`:** the value is a **caller-supplied request**, not a stored candidate row. State that distinction in the commit — do not describe this as the same threat.

**Defect 2 — CONFIRMED, but NARROWER than both the tracker and the in-file comment say.**
- `:156` — `const srcId: unknown = sourceWorkspace?.id;` — **optional-chained, cannot throw.**
- `:191` — `if (wsId !== srcId)` — compares two already-bound locals. **I read this as NOT dereferencing `sourceWorkspace`, therefore NOT a throw site.**
- `:195` — `if (!isVisibilityLevel(sourceWorkspace.defaultVisibility))` — ⛔ **unguarded. THIS is the throw on a null/undefined `sourceWorkspace`.**

⛔ **THEREFORE A THIRD, UNFILED DEFECT — AND IT IS THE INTERESTING ONE.** The comment `24.45` added at **`:149-151`** reads: *"the equality test **and** the `defaultVisibility` guard below still dereference `sourceWorkspace` unguarded."* **If my reading of `:191` is right, that names two sites where only one qualifies** ⇒ ⭐ **a residual-documenting comment that overstates its own residual — inside the remedy for a finding about comments asserting what the code does not do.** *(`L82`/`L96`, third-order.)*

⚠ **I am NOT asserting this — see Q1. I did not read `:126-145`,** so an earlier unguarded dereference could exist and make the comment correct. **Check before you act on it.**

---

## Acceptance criteria

- [ ] `denyDirectCrossWorkspaceRaw`'s `from`/`to` are **validated-or-omitted** on the pattern `24.45` landed (`L39` — reuse it, do not invent a second form).
- [ ] A credential-shaped-but-non-empty `fromWorkspaceId`/`toWorkspaceId` **does not render raw** into audit refs; it renders the same sentinel `24.45` chose.
- [ ] ⛔ **A non-vacuity control:** a legitimate id **still renders normally**. A test that only proves redaction passes trivially if the function starts emitting a constant.
- [ ] A `null` / `undefined` `sourceWorkspace` reaching `validateProjectionVisibility` returns a **typed denial**, never throws (§16 never-throw).
- [ ] The `:149-151` comment states exactly which sites are unguarded — **corrected or deleted**, per Q1's finding (`L82`).
- [ ] Existing `packages/policy` tests pass **unmodified**, or every modification is justified at Step 9.
- [ ] `/preflight` clean, **per-package scoped**.
  ⛔ **THE TREE IS NOT GREEN AND THIS BRIEF DOES NOT CLAIM IT IS.** Known red **by name**: `apps/desktop/test/bundle/main-bundle-resolution.test.ts` — pre-existing, unowned, `### 24.25`. **Zero failing TESTS.** ⚠ **`knowledge-implementer` is concurrently in `packages/knowledge` on `### 24.26` step 3** (task #38), which flips a required field — **a hold defers the commit, not the effect**, so their uncommitted work is in your test runs. **Scope your preflight per-package.** Any other failing test is not covered — investigate rather than assume.

## Wiring / entry point (Step 7.5)

Both functions are **live policy predicates**, not dormant. `validateProjectionVisibility` is reached from `serveProjection` → `apps/worker/src/composition/crossWorkspaceRead.ts:139` on the production read path. ⛔ **Confirm `denyDirectCrossWorkspaceRaw`'s own production reachability yourself and report it** — `### 24.43` records that this repo's reachability claims were falsified three times in one day, and **your own `24.29` census is the instrument.** If it proves unreached, say so plainly; that changes disposition, **never** whether the fix is correct.

## Files expected to touch

**Modified:** `packages/policy/src/visibility.ts` · `packages/policy/test/visibility.test.ts`

⛔ **Do NOT touch:** `packages/knowledge/**` (knowledge is mid-slice there) · `apps/worker/**`.
⚠ **`packages/policy/test/visibility.test.ts:252+` is `### 24.63`'s subject** — the sole surviving candidate-side foreign-workspace coverage. **You may add beside it; do not restructure it.**

## RED test outline (Step 2)

1. **`cross_workspace_raw_denial_does_not_render_a_credential_shaped_id`** — a non-empty, schema-valid, credential-shaped `fromWorkspaceId`.
   - Asserts: refs carry the sentinel, not the raw value. Why: rule 7, `24.45`'s class.
2. **`cross_workspace_raw_denial_still_renders_a_legitimate_id`** — the control (`L80`/`L7`).
   - Asserts: a normal id renders normally. **Why: without this, test 1 passes on a function that emits a constant.**
3. **`null_source_workspace_returns_a_typed_denial`** — `validateProjectionVisibility` with `sourceWorkspace` null.
   - Asserts: a `MALFORMED_POLICY_INPUT` denial; **does not throw.** Why: §16 never-throw.
4. **`same_workspace_request_still_allows`** — regression pin on `:317-329`, untouched behaviour.
5. **(conditional on Q1)** — if the equality test *does* throw, a case pinning it; if it does not, **no test** and the comment gets corrected instead.

⚠ **Mutation-verify at least the rule-7 pin** (`contracts L90`): revert the validation ⇒ test 1 must red. A pin never observed to fail is unproven.

## Cross-doc invariant impact

- **Model field changes:** none expected — a predicate-internal fix. **If your remedy changes a `PolicyDecision` shape or adds a `DenialReason` member, that IS a cross-doc invariant change — flag it at Step 9; do not write the row.**
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⛔ **Does the equality test at `:191` actually throw?** I read it as comparing two bound locals, with `sourceWorkspace` already safely extracted at `:156` via `?.` — which would make the `:149-151` comment **overstate by one site**. **My default vote: the comment is wrong and gets corrected in-slice.** ⚠ **But I did not read `:126-145` and an earlier unguarded dereference would make it right.** **Read it and tell me** — this is the one premise I most expect to be wrong, and I would rather be corrected at 2.5 than have you follow it.
2. **What sentinel does the validated-or-omitted form use?** `24.45` chose one; **reuse it verbatim** (`L39`). ⛔ **A second spelling of the same concept is the defect `24.26` is spending three commits deleting.** Flag if `24.45`'s form does not fit and say why.
3. **Is `MALFORMED_POLICY_INPUT` the right denial for a null `sourceWorkspace`, or does it want its own member?** **My default vote: reuse `MALFORMED_POLICY_INPUT`** — a null source is malformed input, and `contracts L79` only calls for a new member when a caller could *recover differently*, which it cannot here. Push back if you disagree.
4. **Bundle or split?** Two defects, one file, both safety. **My default vote: ONE commit** — same file, same function family, one logical unit. ⛔ **But if your remedy for defect 1 turns out non-trivial, SPLIT** — a safety pin gets its own commit and bisectability outranks convenience.

## Dependencies + sequencing

- **Depends on:** `### 24.45` ✅ `48ec7c91` (the pattern this reuses)
- **Blocks:** — · **Related:** `### 24.63` (same test file, do not restructure) · `### 24.64` (gate-bypassing producers — **different task, do not absorb it**)

## Estimated commit count

**1** (possibly 2 — see Q4). Safety slice: **do not bundle with anything outside this task.**

## Lessons-logged candidates anticipated

- **Convention candidate** — *validated-or-omitted is a pattern with one spelling; the second function to need it reuses the first's form.*
- **Convention candidate** — *a comment documenting a residual is itself a claim about the code and goes stale the same way* (defect 3's shape, if Q1 confirms).
- **Architecture-doc note candidate** — §16's never-throw guarantee at policy predicate boundaries taking non-nullable params that callers can still pass null through.

## How to invoke

1. Read this brief end-to-end — especially the tracker's misattribution and Q1.
2. Run **`/tdd cross_workspace_ref_validation_and_never_throw`**.
3. **Step 2.5** — answers to Q1–Q4 plus your reachability finding for `denyDirectCrossWorkspaceRaw`. ⛔ **My line numbers are read-derived at HEAD `20df48c7`; falsify them.**
