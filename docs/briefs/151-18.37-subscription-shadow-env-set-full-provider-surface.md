# /tdd brief — subscription_shadow_env_set_full_provider_surface

## Feature
Extend the armed-subscription shadowing-env watched set (`SUBSCRIPTION_SHADOWING_ENV_KEYS`, 13 keys from 18.28) to the **grounded full subscription-shadow surface** surfaced by 18.36's live-Claude-Code-doc grounding — the vars that would silently make a "subscription" run authenticate/egress as a DIFFERENT account or a PAID cloud provider. Load-bearing additions: **`CLAUDE_CODE_OAUTH_TOKEN`** (shadows the local subscription login with a different account) + **`CLAUDE_CODE_USE_FOUNDRY`** (provider switch → cloud egress/spend); plus the grounded provider-credential + gateway-redirect surface (AWS/Vertex/mTLS creds · per-provider `ANTHROPIC_*_BASE_URL`). Because the constant is **single-sourced**, this hardens BOTH the `process.env` guard (18.28 `assertSubscriptionAuthEnv`) AND 18.36's settings-`env` leg in one edit. Over-inclusion is fail-safe (a spurious degrade only hits the owner-gated armed path; a miss is a silent fail-OPEN → include when uncertain, L65). No behavior change to the shipped default — the guard still runs ONLY on the armed boot path; extending the watched set is byte-equivalent when unarmed. Arms NOTHING; a §ARM-18 CHECKPOINT-1 pre-first-armed-run hardening.

## Use case + traceability
- **Task ID:** 18.37
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (subscription arm / no-surprise-spend — primary), `§7` (transport arming/degrade), `§5` (egress — a provider-switch/base-url redirect is a cloud-egress vector), `§16` (rule 7).
- **Related context:** origin — **18.36** Step-2.5 grounding Finding (the 18.28 set under-covers). Extends **18.28** `6ebe07a8` (`SUBSCRIPTION_SHADOWING_ENV_KEYS` 8→13, `apps/worker/src/composition/subscription-auth-guard.ts`) — same explicit-extensible-set discipline. Consumed by `assertSubscriptionAuthEnv` (18.28, armed boot) + 18.36's `subscription-settings-guard` settings-`env` leg (single-source). worker **Lessons 28/61/62/65** (explicit-extensible watched set · each-var-fails-closed enumeration · exclude bypass/routing values · over-inclusion fail-safe · ground-vs-live-docs-at-flip) + **L56** (never trust memory for a load-bearing external value — ground it).

## Acceptance criteria (what "done" means)
- [ ] `SUBSCRIPTION_SHADOWING_ENV_KEYS` is extended to include `CLAUDE_CODE_OAUTH_TOKEN` + `CLAUDE_CODE_USE_FOUNDRY` + the grounded provider-cred / gateway-redirect vars (exact set confirmed at Step-2.5 Q1).
- [ ] **Enumeration test extended (L62):** the existing `shadowing_env_full_set_*` enumeration test asserts EVERY new var, alone, DEGRADES the armed path fail-closed (each set on the armed boot ⇒ `assertSubscriptionAuthEnv` refuses; behavioral where a runtime seam exists, else the key-anchored source-assertion + RED-on-weaken mutation proof per L28).
- [ ] **Exclusions preserved (L65):** the `NO_PROXY`/`no_proxy` bypass-allowlist stays EXCLUDED; only redirect/injection/provider-switch vars are watched — a pure routing/safe value (e.g. a benign region pin) is NOT added. Pin the exclusions.
- [ ] **Both guards benefit (single-source):** a new var present in a settings `env` block ALSO degrades via 18.36's settings-`env` leg (which reuses this constant) — add/extend one cross-check so the single-source is provably load-bearing (not just the `process.env` path).
- [ ] **Shipped default byte-equivalent:** extending the watched constant changes ONLY the armed-path degrade breadth; the OFF/default boot (arm unset) is byte-identical (the guard is not consulted). Confirm no new var is read outside the armed path.
- [ ] **Fail-closed / degrade-not-crash (§16/L52):** every added var, when set on the armed path, DEGRADES the arm (strip transport → local/stub) — never a worker boot-throw.
- [ ] `/preflight` clean. No egress / spend / key read (presence-only, rule 7).

## Wiring / entry point (Step 7.5)
No new wiring — extends an already-consumed constant. `SUBSCRIPTION_SHADOWING_ENV_KEYS` is read by `assertSubscriptionAuthEnv` on `bootWorker`'s armed path (18.28/18.24) and by 18.36's `subscription-settings-guard` settings-`env` leg. Extending the array immediately widens both guards' watched set; the degrade path and the armed-path-only gating are unchanged.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/subscription-auth-guard.ts` — extend `SUBSCRIPTION_SHADOWING_ENV_KEYS` (+ the in-code "grounded vs live docs at flip" note).
- `apps/worker/test/composition/subscription-auth-guard.test.ts` — extend the enumeration test to the new vars + the exclusion pins.
- possibly `apps/worker/test/composition/subscription-settings-guard.test.ts` — the single-source cross-check (a new var in a settings `env` block degrades) if not already covered by 18.36's parametric test.

## RED test outline (Step 2)
Extend the existing `subscription-auth-guard.test.ts` enumeration + add:
1. **`shadowing_env_full_set_enumerated`** (extend) — each NEW var (`CLAUDE_CODE_OAUTH_TOKEN`, `CLAUDE_CODE_USE_FOUNDRY`, + each grounded provider-cred/base-url var), alone, on the armed path ⇒ `assertSubscriptionAuthEnv` degrades fail-closed.
   - Why: §19.5/§7 — each is a subscription shadow (different account / cloud provider / gateway redirect).
2. **`no_proxy_bypass_stays_excluded`** (confirm/extend) — `NO_PROXY`/`no_proxy` (and any grounded safe/routing value decided at Q1) present ⇒ NO degrade.
   - Why: L65 — a bypass-allowlist/routing value is not a redirect; watching it false-degrades a legit config.
3. **`settings_env_new_var_degrades_via_single_source`** — a new var in a settings `env` block ⇒ 18.36's settings-`env` leg faults (proves the single-source hardens both guards).
   - Why: single-source (L5/L37) — the extension must reach the settings leg too.
4. **`default_off_unaffected`** — arm unset ⇒ neither guard reads the new vars (byte-equivalent).
   - Why: L23/L44 — the safety gate runs only on the armed path.

## Cross-doc invariant impact
- **Model field changes:** none — a worker-internal security constant (not a frozen Appendix-A contract).
- **Orchestrator doc rows to write hot (Step 9):** possible `ARCHITECTURE.md §19.5`/§ARM-18 note that the shadow-guard set now covers the full grounded provider surface (CHECKPOINT-1 completion) — flag at Step 9; I route it.
- **Shared-contract (Appendix-A) seam touched?** No.

## Things to flag at Step 2.5
1. **⛔ Ground the EXACT full set vs live Claude-Code / Agent-SDK docs (gates correctness).** Confirm + finalize (via `claude-code-guide` / live docs — you already grounded much of this for 18.36's Q1): the load-bearing `CLAUDE_CODE_OAUTH_TOKEN` + `CLAUDE_CODE_USE_FOUNDRY`; the provider-credential surface (AWS: e.g. `AWS_BEARER_TOKEN_BEDROCK` / `AWS_ACCESS_KEY_ID` / `AWS_PROFILE`; Vertex: e.g. `GOOGLE_APPLICATION_CREDENTIALS` / `CLOUD_ML_REGION` / `ANTHROPIC_VERTEX_PROJECT_ID`; mTLS; the provider-switch flags `CLAUDE_CODE_USE_BEDROCK`/`CLAUDE_CODE_USE_VERTEX`); the gateway-redirect base-urls (`ANTHROPIC_BASE_URL` / `ANTHROPIC_BEDROCK_BASE_URL` / `ANTHROPIC_VERTEX_BASE_URL`). **Over-inclusion is fail-safe (include when uncertain); a miss is a silent fail-open.** My default: include all of the above; EXCLUDE pure routing/region pins that carry no credential/redirect (e.g. a region name alone) + the `NO_PROXY` bypass. Send me the finalized set.
2. **Enumeration-test form for the new vars.** My default vote: behavioral where the armed-boot seam exercises them (mirror 18.28's existing `shadowing_env_full_set_each_var_fails_closed`); a key-anchored source-assertion + RED-on-weaken mutation proof (L28) only where no lightweight runtime seam exists. Match whatever 18.28's existing enumeration test already uses.
3. **Any var that's ambiguous (credential-vs-routing)?** Flag it — default to INCLUDE (fail-safe) unless it's provably a safe/routing value like `NO_PROXY`.

## Dependencies + sequencing
- **Depends on:** 18.28 (the set it extends — landed), **18.36** (the settings-`env` leg that single-sources the set — sequenced immediately before; do not interrupt its cycle).
- **Blocks:** the FIRST employer-raw / real armed run (a §ARM-18 CHECKPOINT-1 prerequisite).

## Estimated commit count
**1 commit — do NOT bundle.** A security-critical watched-set edit → its OWN commit; **security-reviewer MANDATORY** (each-var-fails-closed + exclusions + over-inclusion-fail-safe + no-spend). Do not bundle with 18.36 or anything else.

## Lessons-logged candidates anticipated
- **Convention candidate** — a shadow/redirect env guard's watched set is single-sourced across every consuming leg (process.env + settings-file), extended by grounding vs live provider docs (over-inclusion fail-safe, exclude only bypass/routing values), byte-equivalent when unarmed.
- **Architecture-doc note candidate** — §ARM-18: the subscription-shadow guard now covers the full grounded provider surface (account-shadow + provider-switch + gateway-redirect); CHECKPOINT-1 shadow-guard MEDIUM fully closed.

## How to invoke
1. Read this brief end-to-end.
2. **Step 2.5 — send me the finalized grounded var set (Q1)** with your test-design write-up before GREEN. This is the load-bearing correctness gate.
3. Run `/tdd subscription_shadow_env_set_full_provider_surface`.
4. Step 8 — **security-reviewer MANDATORY**.
5. Step 9 — flag the §19.5/§ARM-18 arch note + any lesson candidate.
