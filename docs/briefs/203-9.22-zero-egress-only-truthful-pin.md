# /tdd brief — zero_egress_only_means_its_contract

> ⛔ **THIS BRIEF WAS REWRITTEN 2026-07-27. If you have read a previous version of this file, discard it.**
> The prior version's model of `allowedProcessors` was **INVERTED**, and a slice built faithfully against it
> would have created a NEW false assurance of exactly the class this task exists to remove. The implementer
> caught it at Step 2.5 and escalated instead of complying — that was correct, and it is why this file changed
> rather than the code. Every premise below now carries a `file:line` so you can falsify it cheaply. **Do that.**

## Feature

Make `zeroEgressOnly` mean what its own contract says: derive it from the state that governs it, on **both**
producers, through **one** shared derivation — under the owner-ruled **Option C** (two-axis, fail-closed on
both axes).

## Use case + traceability

- **Task ID:** 9.22
- **Architecture sections it implements:** `ARCHITECTURE.md §5` (EgressPolicy / egress veto), `§16`
- **Related context:** task **9.29** (a coupled durability precondition — read the interaction note below) ·
  task 9.23 / brief 204 (independent, in flight) · contracts **L56** (derive, never assert) · contracts **L69**
  (a tested false assurance is an active defense of the defect) · branch `wip/9.22-inverted-premise-DO-NOT-MERGE` (`cffbfc7f`)

## ⭐ The owner ruling this brief implements

**OPTION C**, ruled 2026-07-27. `zeroEgressOnly` is true **only** when the provider matrix resolves local-only
**AND** both allowlists are empty. Fail-closed on both axes.

**The reasoning, because you should understand the why and not just the shape:** this field's entire job is to
reassure, so it should be *hard to make it lie*. Option A ("both allowlists empty ⇒ true") cannot distinguish a
deliberately-locked-down workspace from one that was never provisioned. Option B (matrix-only) ignores a
leftover cloud destination sitting alongside a local matrix. **C requires two independent things to go wrong
before the app lies to the owner.** Six instances of wrongly-asserted safety in a single round is the evidence
base for taking the conservative shape.

## ⭐ SCOPE WIDENING INTO `providerMatrix` IS EXPLICITLY AUTHORIZED

Stated plainly because **the absence of this authorization is what made the previous brief wrong**: option C
reads `providerMatrix`, which no prior brief permitted, and an implementer who inferred the boundary rather
than being told it would have to guess. You are authorized to read `providerMatrix` in the derivation. You are
**not** authorized to *write* it, or to change provisioning behavior — that is task 9.29.

## The CORRECTED model — verified premises (falsify these first)

1. `packages/policy/src/egress.ts:138-144` — an **egressing** route (`processorOfRoute !== null`) with an
   **EMPTY** `allowedProcessors` ⇒ `PROCESSOR_NOT_ALLOWED` **deny**. So **empty allowlist = every egress route
   denied = the STRONGEST zero-egress state**, not the weakest. The prior brief had this exactly backwards.
2. `packages/policy/src/egress.ts:158` — a genuine loopback-local route (`processorOfRoute === null`) is
   egress-safe and allowed **without ever consulting the allowlist**.
3. `packages/policy/src/processors.ts:15` — `LOCAL_PROVIDERS = Set(["ollama","lm_studio"])`, and it is
   **module-private** (no `export`). `processorOfRoute` returns `null` for a genuinely loopback-local route
   (`processors.ts:437` is the provider-branch check).
4. **Therefore `["ollama"]` sitting in an EGRESS allowlist can only mean a REMOTE ollama endpoint** approved as
   a destination — the tunneled-local hole `processors.ts` exists to close. Claiming "local-only" for that
   config would assert safety over precisely the state that means raw content is leaving.

## Definition to implement

⚠ **AMENDED 2026-07-27 — THE OWNER ADDED A THIRD CONJUNCT. This supersedes the two-axis form below.**

`zeroEgressOnly === true` **iff ALL THREE hold** — conjunct 0 first:

- **Conjunct 0 — the model config is NON-EMPTY** (a real configured setup, not an absent one). Provided by
  `@sow/policy`; provint owns its precise definition.

**Why it exists — do not read it as belt-and-braces:** axes 1 and 2 are **not independent at the default
state**. `provisionWorkspace` seeds BOTH allowlists `[claude]`, so "both allowlists empty" *is* the
never-provisioned state, and that same absence leaves `providerMatrix` empty, satisfying axis 1 **vacuously**.
One missing event zeroed both, and an unconfigured workspace reported the strongest claim the system can make —
at exactly the state option A was rejected for. **An unconfigured workspace now reads `false`: the app does not
know yet, so it must not reassure.**

And the two original axes:

- **Axis 1 — routing pins local.** `providerMatrix.allowedProviders ⊆ LOCAL_PROVIDERS` **AND** every route in
  `providerMatrix.capabilityDefaults` is loopback-local (`processorOfRoute(route) === null`) **AND**
  `providerMatrix.rawCloudEgressEnabled === false`.
- **Axis 2 — no destination is approved.** `egressPolicy.allowedProcessors` is empty **AND**
  `egressPolicy.rawContentAllowedProcessors` is empty.

Anything else ⇒ `false`. Malformed / absent / unreadable input ⇒ `false` (never a positive claim from a state
you could not verify).

## Acceptance criteria (what "done" means)

- [ ] `zeroEgressOnly` is produced by **ONE shared derivation** consumed by **both** producers — `boot.ts` and
      `egressRevoke.ts`. Not two implementations that agree today.
- [ ] `boot.ts:571`'s `zeroEgressOnly: !acknowledged` is gone; the value derives from the workspace record.
- [ ] `egressRevoke.ts:84`'s hardcoded `zeroEgressOnly: true` is gone; it derives from the **post-revoke**
      stored state. A comment telling the next person to keep two sites in sync is not a mechanism — that is
      the arrangement that already failed here.
- [ ] **The fail-closed path does not assert an unverifiable positive.** `boot.ts:533-534` `failClosedEgress`
      currently returns `zeroEgressOnly: true` on a store fault — i.e. it makes its strongest safety claim
      exactly when it knows least. Under C that must be `false`.
- [ ] `[]` (both allowlists empty, matrix local) ⇒ `true`; `["ollama"]` in an egress allowlist ⇒ `false`.
- [ ] A **sparse array** cannot produce a spurious `true` (see below).
- [ ] `systemHealth.ts`'s `zeroEgressOnly` doc (`:42-43`, "workspace pinned local zero-egress provider") states
      the same two-axis contract the code implements.
- [ ] `egressRevoke.ts:38`'s header ("ack=false ⇒ zeroEgressOnly=true") is replaced — it states the inverted model.
- [ ] `/preflight` clean.

## ⚠ Model-independent defect that must be fixed whatever the shape

`isZeroEgressOnlyAllowlist(new Array(3))` returns `true` because **`Array.prototype.every` SKIPS HOLES** —
breaking the exported safety predicate's own documented "malformed ⇒ false" totality. **Note: this predicate
does NOT exist on `main`** (verified: zero hits repo-wide) — it lives only on `cffbfc7f`. So you are *creating*
it, not repairing it; the point is not to reintroduce the hole. A length-based check over the dense set, not a
bare `.every`. Pin it with a sparse-array fixture, not just an empty and a populated one.

## ♻ Salvage from `wip/9.22-inverted-premise-DO-NOT-MERGE` (`cffbfc7f`)

⭐ **VERIFIED AGAINST THE FIXTURE 2026-07-27 (not derived from the model — the lead flagged their own relay as
unverified and asked for a read).** `employerAcked` spreads `validWorkspace`
(`packages/contracts/src/fixtures/valid.ts`): `allowedProcessors: []`, `rawContentAllowedProcessors: []`,
`allowedProviders: ["claude"]`, `capabilityDefaults: {}`, `rawCloudEgressEnabled: false`. A revoke touches only
the ack fields. So under **C**: **axis 2 PASSES** (both allowlists empty) and **axis 1 FAILS** (`claude` ∉
`LOCAL_PROVIDERS{ollama, lm_studio}`) ⇒ **`zeroEgressOnly === false`.** The branch's literals are **correct**.

⚠ **READ THAT REASON CAREFULLY — it is the opposite of the intuitive one, and getting it wrong reproduces
option A.** This fixture is `false` because of **axis 1 (the matrix)**, NOT axis 2. Its allowlists *are* empty.
An implementer who builds only the allowlist half will compute `true` here, see the test expecting `false`, and
conclude the test is wrong — when it is their derivation that is missing an axis. **If your axis-2-only
implementation makes this fixture pass, you have implemented option A.** For the record: A ⇒ `true`, B ⇒
`false`, C ⇒ `false`. Note also that `main` currently asserts `true` at both sites (the old `!acknowledged`
model); the branch flipped them to `false`, which is the correct direction.

**Reuse wholesale — including the literal values:** the `egressCommands.test.ts` assertion sweep (`:83`/`:178`
flips, the independence assertion, the zero-survivor result). Under **C** those literal `false` values are
**correct** (B and C agree here; only A diverged). An earlier note filed this correction as "model-independent"
and then retracted it as model-*dependent* — both were half-right: it **is** model-dependent, and the ruling
settles it in favor of reuse. Do not redo the sweep.

**REPLACED, not rebuilt — and not by you:** the `processors.ts` predicate is **provint's (task #21, landed)**.
Nothing of the branch's policy half is reusable: it is inverted against C on **both** inputs. **Consume their
three exports.**

**Rebuild against the two-axis model** (the branch's versions are one-axis): the `boot.ts` +
`egressRevoke.ts` derivations · the worker-side test suite.

**Replace:** `systemHealth.ts`'s "iff non-empty and local-only" clause — that is the inverted wording itself.

⛔ **DO NOT reuse the branch's `packages/policy/test/processors-zero-egress.test.ts`.** provint's leg-1 verdict:
the branch predicate returns `true` for `["ollama"]` and `false` for `[]` — **inverted against C on both
inputs** — and its own test **asserts that inversion at `:31`**, which would lock it in. That test file is not
salvage, it is a trap: reusing it would make the correct predicate look like a regression (contracts **L69** —
a tested false assurance inverts the review gate from a check into a lock). The policy predicates are
**provint's** slice (task #21), not yours; consume them.

⛔ **Do not merge the branch.** Cherry-pick the worker sweep only; rebuild the rest.

## ⚠ EXPECTED BEHAVIOUR YOU MUST PIN: an unprovisioned workspace reads `false`

⚠ **An earlier version of this brief said the opposite** — that an unprovisioned workspace reads `true` and you
should pin that as deliberate. **The owner's third conjunct reversed it.** Rewritten here rather than left
standing with a correction bolted on: a brief that asserts both directions is one an implementer resolves by
picking whichever half they read first.

**Pin `false` for the unprovisioned workspace**, named explicitly as that case. The reasoning you are pinning:
`provisionWorkspace` seeds both allowlists `[claude]`, so empty allowlists mean *never provisioned*, and that
same absence empties `providerMatrix` — one missing event would otherwise satisfy both original axes at once.
Conjunct 0 is what stops an absence from reading as a guarantee.

**Still true, and still not yours to re-litigate:** if your implementation makes the unprovisioned case read
`true`, that is a defect in your derivation, not a discovery. Escalate at Step 9 rather than adjusting the
predicate.

## ⚠ SCOPE BOUND — these predicates cover MODEL-PROVIDER egress only

Connectors and Tool-Gateway writes **never consult `egressPolicy.allowedProcessors`**, so an
armed-connector workspace can satisfy every axis and still egress. The predicates are correct; the *name*
`zeroEgressOnly` is broader than what they prove. Do not widen the claim in comments, in
`systemHealth.ts`'s contract doc, or anywhere a reader could take it as global. Stating it unscoped is L56
in prose rather than in code.

## ⛔ BLOCKED BY task 9.29 — 9.29 lands BEFORE or WITH this slice

Under C the derivation reads `providerMatrix`. **`provisionWorkspace.ts:146` currently RESETS `providerMatrix`
on every same-type re-provision** (`defaultWorkspace()` rebuilds the whole aggregate;
`packages/contracts/src/models/workspace.ts:129-134`). So a re-provision wipes axis 1's input to
`{allowedProviders: [], capabilityDefaults: {}, rawCloudEgressEnabled: false}` — which satisfies axis 1
*vacuously*.

**Stated precisely, because overstating it would be the same defect class we are closing:** the resulting claim
is **not false** — an empty matrix genuinely denies, the same reasoning that makes an empty allowlist the
strongest state. But the owner's *configured* posture would have been destroyed and the surface would report
zero-egress for a reason the owner never chose. ⛔ **LEAD RULING 2026-07-27 — this is a HARD ordering constraint, not background colour.** 9.22 is what **renders**
the claim to the owner. Shipping it while the input can be silently wiped opens a live window where the surface
reports zero-egress vacuously — **on the exact screen the owner checks to confirm a revoke landed.** So **9.29
lands BEFORE or WITH this slice.** If you reach GREEN here and 9.29 has not landed, **stop and say so at Step 9**
rather than shipping.

**Still: do not fix 9.29 here, and do not add a workaround for it.** Separate slice, opposite safety direction,
its own review. Your job is to derive the claim correctly; 9.29's job is to make the input durable.

## Wiring / entry point (Step 7.5)

Both producers are already on the live path — no new wiring. `boot.ts` `createSystemHealthQueryPort`
(`:569-573`) serves `systemHealth.egressStatus`; `egressRevoke.ts` returns the post-revoke status to the 9.10-B
revoke command, which the 9.10-C desktop surface renders. Confirm the shared derivation is reached from **both**,
not just the one you edited second.

## Files expected to touch

**New:**
- `apps/worker/test/composition/zero-egress-only-truthful.test.ts` — derivation + both-producer agreement

**Modified:**
- ⛔ **NOT YOURS — `packages/policy/src/processors.ts` is provint's (task #21).** Do not edit it. If a predicate
  you need is missing or shaped wrong, say so at Step 2.5 and I'll route it to them.
- `apps/worker/src/boot.ts` — `failClosedEgress` + the `createSystemHealthQueryPort` derivation
- `apps/worker/src/composition/egressRevoke.ts` — derive rather than hardcode; fix the `:38` header
- `apps/worker/src/api/procedures/systemHealth.ts` — the contract doc at `:42-43`
- `apps/worker/test/api/procedures/egressCommands.test.ts` — the salvaged sweep

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)

1. **`empty_allowlists_with_a_CONFIGURED_local_matrix_is_zero_egress`** — Asserts: all three conjuncts
   satisfied ⇒ `true`. ⚠ **"local matrix" here means a CONFIGURED one** — non-empty `allowedProviders` AND
   non-empty `capabilityDefaults`. An EMPTY matrix now yields `false` (the owner's third conjunct), so the
   empty reading of this test name is exactly backwards. Why: §5 — empty allowlist is deny-all, the strongest
   state (`egress.ts:138-144`); the matrix must additionally be real.
2. **`remote_ollama_in_egress_allowlist_is_not_zero_egress`** — Asserts: `allowedProcessors: ["ollama"]` ⇒
   `false` even with a local-looking matrix. Why: the tunneled-local hole; this is the exact assertion the
   previous brief got backwards, so it is the one that must be unambiguous.
3. **`cloud_processor_in_raw_content_allowlist_is_not_zero_egress`** — Asserts: axis 2 covers
   `rawContentAllowedProcessors` independently. Why: raw-content path is a separate destination set.
4. **`non_local_provider_in_matrix_is_not_zero_egress`** — Asserts: `allowedProviders: ["claude"]` ⇒ `false`
   with both allowlists empty. Why: axis 1 independence — this is the case option A could not see.
5. **`raw_cloud_egress_enabled_defeats_zero_egress`** — Asserts: `rawCloudEgressEnabled: true` ⇒ `false`.
   Why: axis 1's third conjunct.
6. **`non_loopback_capability_route_is_not_zero_egress`** — Asserts: a `capabilityDefaults` route with a remote
   endpoint ⇒ `false`. Why: `processorOfRoute !== null` is the routing proof, not the `egressClass` claim.
7. **`sparse_allowlist_is_not_zero_egress`** — Asserts: `new Array(3)` ⇒ `false`.
   Why: `Array.prototype.every` skips holes; the predicate's own documented totality.
8. **`store_fault_does_not_claim_zero_egress`** — Asserts: `failClosedEgress` ⇒ `zeroEgressOnly: false`.
   Why: never assert the strongest safety claim from the state you know least about.
9. **`both_producers_agree_across_fixtures`** — Asserts: `boot.ts` and `egressRevoke.ts` derivations return the
   same value over ≥3 **divergent** fixtures. Why: the existing
   `revoke_return_agrees_with_the_visibility_reader` test is single-fixture and **vacuous** (both sides `false`).
   Parametrize it or it proves nothing.
10. **`zero_egress_is_independent_of_the_ack`** — Asserts: flipping `employerRawEgressAcknowledged` alone does
    not move `zeroEgressOnly`. Why: severs the original defect; salvaged from the branch.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)

- **Model field changes:** none. `UiSafeEgressStatus` keeps its three fields (`systemHealth.ts:47-51`) — this
  slice changes how one is *computed*, not the shape. If you find yourself needing a fourth field, **stop and
  flag it** — that is a frozen-seam change and a different conversation.
- **Orchestrator doc rows to write hot:** `ARCHITECTURE.md §5` note that `zeroEgressOnly` is a two-axis
  derivation; the contracts cross-doc `EgressPolicy` row if the derivation's inputs need naming. I write these
  — you flag them.

## Things to flag at Step 2.5

1. ⭐ **SETTLED — do not re-open. The predicates are NOT yours to write.** Lead ruled producer-first: **provint**
   owns them (task #21) and has already shipped the Step-2.5 design. `LOCAL_PROVIDERS` stays module-private in
   `packages/policy` (contracts forbidden-pattern #6 is why this is theirs). **You CONSUME three exports** from
   `packages/policy/src/processors.ts`:
   - `isLocalOnlyProviderMatrix(matrix)` — axis 1
   - `hasNoApprovedEgressDestination(policy)` — axis 2
   - `isZeroEgressOnlyWorkspace({ egressPolicy, providerMatrix })` — **the composed AND**

   ⚠ **This SUPERSEDES an earlier version of this brief that had you compose the AND yourself in worker
   composition.** That was my default vote and it was **wrong**: composing in worker means `boot.ts` and
   `egressRevoke.ts` each combine two predicates — **two sites that must independently agree, which is the exact
   arrangement this task exists to kill**, relocated one level up. provint argued it and I overrode myself.
   Call `isZeroEgressOnlyWorkspace` **once from each producer**; the "ONE shared derivation" criterion is then
   satisfied by construction rather than by discipline. Everything impure stays yours: the store read, the
   `Result` unwrap, and `failClosedEgress` returning **`false`**.

2. **One predicate or two?** Axis 1 (matrix) and axis 2 (allowlists) could be one `isZeroEgressOnly(workspace)`
   or two composable predicates. My default vote: **two small pure predicates + a composing derivation** —
   each axis gets its own falsifiable tests, and the composition site is where the AND is obvious.
3. ⭐ **SETTLED — see question 1.** The composed predicate lives in **policy**, not worker. Your job is one call
   site in each producer.
4. **Does any *other* producer of `UiSafeEgressStatus` exist?** Two are known. Before GREEN, sweep for a third
   **by concept** (who constructs this shape / who returns an egress status), not by grepping the two names you
   already have — contracts **L64**. Report the iteration count at Step 9.

## Dependencies + sequencing

- **Depends on:** the owner's Option-C ruling (landed, recorded in plan task 9.22) **AND task 9.29**. Independent of 9.23.
- **Blocks:** desktop **#8** (the zero-egress rendering — its acceptance bullet 1 is recorded UNMET with **no**
  approved deferral, so it gets met properly once this lands).
- ⛔ **BLOCKED BY:** task **9.29** (durability of axis 1's input) — lands BEFORE or WITH this slice (lead ruling).

## Estimated commit count

**1 — and it does NOT bundle.** This is a rule-5 safety slice; per the brief template's own rule a
safety-critical pin gets its own commit, always. Do not fold 9.29, the desktop rendering, or any drive-by
cleanup into it.

⚠ **Step-9 routes to the LEAD** (safety/rule-5), not to me alone. `security-reviewer=invariant` applies.

## Lessons-logged candidates anticipated

- **Convention candidate** — a safety posture composed of independent axes should fail closed on **each** axis
  separately; the conjunction is what makes a single wrong input insufficient to produce a false assurance.
- **Convention candidate** — when two producers must agree, the agreement test must be parametrized over
  divergent fixtures; a single-fixture agreement test where both sides return the same constant is vacuous.
- **Architecture-doc note candidate** — `§5`: `zeroEgressOnly`'s two-axis definition and why empty-allowlist is
  the strongest rather than the weakest state (the inversion that cost this task a full round).

## How to invoke

1. **Read this brief end-to-end**, including the ⛔ header — a previous version of this file said the opposite.
2. **Falsify the four premises** against `main` before writing tests. They are cited with `file:line` precisely
   so that is cheap. If one is wrong, that is a Step-2.5 escalation and a success, not friction.
3. **Run `/tdd zero_egress_only_means_its_contract`.**
4. **Step 2.5** — answer the four questions above (or take defaults) and send the coverage map.
5. **Step 9** — categorized flags + ship-ask, routed to the lead.
