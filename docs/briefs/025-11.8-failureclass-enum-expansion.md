# /tdd brief — failureclass_security_taxonomy_expansion (FROZEN-CONTRACT round; make-it-real arc, C-enum)

## Feature
Additively EXPAND the frozen §16 `FailureClass` enum with dedicated members for the terminal SECURITY / POLICY / EGRESS / ISOLATION causes the C-fix mapped to least-wrong interims, then rewire the C-fix `arch_gap` sites to the new members (retiring the markers). Owner-directed (2026-07-11): full frozen-contract discipline; `tsc` enforces that every exhaustive `FailureClass` consumer handles the new members.

## Use case + traceability
- **Task ID:** 11.8 (make-it-real arc — the C-enum bullet; discharges the category-4 owner decision the C-fix surfaced)
- **Architecture sections it implements:** `ARCHITECTURE.md §16` (Observability / System Health — the OBS-2 `FailureClass` taxonomy; inv-5 distinct-health-item-per-class).
- **FROZEN-CONTRACT ROUND (atomic doc-pairing, orchestrator territory).** `FailureClass` is an Appendix-A enumeration (ARCHITECTURE.md L441 HealthItem row) embedded in `health-item.schema.json` + `health-item.snap`. The implementer edits the Zod enum + regenerated JSON Schema + snapshot + tests + the consumer switches + the `sourceIngestion.ts` rewire; the **ORCHESTRATOR writes hot** the ARCHITECTURE.md §16 + Appendix A (L441) + `packages/contracts/CLAUDE.md` cross-doc rows. Implementer flags the contract change at Step 9; does NOT touch those two doc files.
- **Related context:** the interim being retired is the C-fix (`77c717e`) `arch_gap` mapping in `packages/workflows/src/workflows/sourceIngestion.ts`. Precedent for the frozen-contract mechanics: brief `021` (numbered-block provenance). `FailureClass` current members: `connector_unreachable · write_through_failed · budget_breach · missed_or_late_schedule · schema_rejection · worker_down · parity_defect · conflict_review · sync_lagging · rebuild_divergence`.
- **Implements:** REQ (via §16 OBS-2 / inv-5)

## Acceptance criteria (what "done" means)
- [ ] `FailureClass` (`packages/contracts/src/models/shared-enums.ts`) gains the new members ADDITIVELY (no rename/remove of any existing member): default taxonomy `security_violation`, `policy_denial`, `egress_denied`, `isolation_breach` (final set decided at Step 2.5).
- [ ] `health-item.schema.json` regenerated + `health-item.snap` updated to the new enum set; the schema-snapshot test (`spec(§16)`, model field/enum set == checked-in snapshot) is green.
- [ ] The C-fix `arch_gap` sites in `sourceIngestion.ts` are REWIRED to the new members and the `arch_gap` markers retired: `injection_detected`/`secret_found` → `security_violation`; `admission_rejected` → `policy_denial`; `egress_vetoed` → `egress_denied`; `ownership_violation` → `isolation_breach`. `unsupported_type` STAYS `schema_rejection`; `commit_failed` stays `write_through_failed`; `worker_down` stays reserved for genuine supervision/infra.
- [ ] Every EXHAUSTIVE `FailureClass` consumer handles the new members — `tsc` is green repo-wide (the new members break `assertNever`/exhaustive switches until each is handled). **AND** every DEFAULT-carrying switch is audited so a new member is NOT silently mis-bucketed (esp. any `default`→a wrong class/severity — a security_violation must not fall through to a benign default).
- [ ] Each new member gets a defensible SEVERITY at its creation/surfacing sites (a `security_violation`/`isolation_breach` likely warrants elevated severity — decided per consumer at Step 2.5).
- [ ] Repo-wide `pnpm -w turbo run typecheck test` green; `SOW_TEMPORAL=1` live green; `/preflight` clean.

## Wiring / entry point (Step 7.5)
None new — this is a contract expansion + a rewire of existing surfaced-failure sites already reachable via the live `sourceIngestion` driver + every existing `FailureClass` consumer. `none — no new entry point; the new members flow through the existing HealthItem/surface() paths.`

## Files expected to touch
**Modified (implementer):**
- `packages/contracts/src/models/shared-enums.ts` — add the new `FailureClass` members (additive).
- `packages/contracts/schemas/health-item.schema.json` — regenerate (the `failureClass` enum set).
- `packages/contracts/src/models/__snapshots__/health-item.snap` — update snapshot.
- `packages/contracts/test/models/health-item*.test.ts` (+ the schema-snapshot test) — assert the new enum set.
- `packages/workflows/src/workflows/sourceIngestion.ts` — rewire the 4 `arch_gap` sites to the new members; retire the markers.
- **The tsc-discovered exhaustive-switch consumers** (a subset of the ~45 `FailureClass` importers — e.g. `apps/worker/src/health/surface.ts`, `apps/worker/src/lifecycle/supervision-policy.ts`, `db/src/health/degraded-mode.ts`, `domain/src/redaction/redaction-rules.ts`, `domain/src/error-routing/route-failure.ts`, `contracts/src/api/ui-safe.ts`, `workflows/src/workflows/systemHealthSurfacing.ts`, …) — add cases for the new members.

**Orchestrator writes hot (Step 9 — do NOT touch):**
- `ARCHITECTURE.md` §16 (taxonomy prose) + Appendix A L441 (HealthItem `failureClass` enum set).
- `packages/contracts/CLAUDE.md` cross-doc invariants table (FailureClass row).

If the tsc ripple reaches a consumer whose correct handling is non-obvious, **flag at Step 2.5**.

## RED test outline (Step 2)
1. **`failureclass_has_new_members`** — Asserts: `FailureClass` includes `security_violation`/`policy_denial`/`egress_denied`/`isolation_breach`; existing members unchanged. Tag `spec(§16)`.
2. **`health_item_schema_snapshot_matches`** (schema-snapshot, `spec(§16)`) — Asserts: the regenerated `health-item` schema enum set == the checked-in snapshot (the cross-track-seam contract pin). Why: frozen-contract discipline.
3. **`sourceingestion_security_causes_map_to_new_members`** — Asserts: `injection_detected`/`secret_found`→`security_violation`; `admission_rejected`→`policy_denial`; `egress_vetoed`→`egress_denied`; `ownership_violation`→`isolation_breach`. Why: retire the arch_gap interim (drains the C-enum decision).
4. **`unchanged_mappings_preserved`** — Asserts: `unsupported_type`→`schema_rejection`, `commit_failed`→`write_through_failed`, register-malformed→`schema_rejection`; non-terminal mappings unchanged. Why: additive-only, no regression.
5. **(as needed)** a consumer-level test where a new member gets distinct severity/handling (e.g. a `security_violation` health item surfaces at the decided severity).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** YES — `FailureClass` enum gains members (additive). **Orchestrator writes hot:** ARCHITECTURE.md §16 taxonomy + Appendix A L441 enum set + `packages/contracts/CLAUDE.md` cross-doc row. Implementer flags at Step 9; does not edit those.
- **shared-contract seam model touched?** YES — `FailureClass`/`HealthItem` is Appendix-A + crosses subsystem seams (consumed cross-track). The **schema-snapshot test IS required** in this cycle (RED outline #2), authored + Step-2.5-reviewed like any test.
- Commits stagger: the implementer's Step-10 commit lands the enum + schema + snapshot + consumers + rewire; the orchestrator's doc rows land in the round routing.

## Things to flag at Step 2.5
1. **Taxonomy — member set + names.** Default vote: **four members `security_violation` / `policy_denial` / `egress_denied` / `isolation_breach`** (snake_case, matching the enum convention). Rationale: egress gets its OWN member (safety rule 5 is load-bearing; the `egress_status` named-constant precedent); security separates content-safety/secret from policy; isolation (WS-4) is distinct from a generic write failure. Alt (owner's 3): fold `egress_denied` into `policy_denial`. If you disagree with 4-vs-3 or a name, surface it — the owner delegated the taxonomy but flag a genuine divergence.
2. **Consumer-ripple handling + the DEFAULT-switch audit.** Default vote: **let tsc drive the exhaustive switches; for each, map the new member to the least-surprising existing BEHAVIOR (severity/redaction/display), elevating severity for `security_violation`/`isolation_breach` where the site assigns it.** CRITICAL: also audit DEFAULT-carrying switches — a `default` branch won't tsc-break but could silently mis-bucket a new member (e.g. `default`→`worker_down` would re-introduce the very mislabel we fixed). Report the consumer count + which needed distinct handling.
3. **Severity for the new members.** Default vote: `security_violation`/`isolation_breach` → the highest existing severity tier; `policy_denial`/`egress_denied` → the tier their interim (`schema_rejection`) used, unless the site has reason to elevate. Confirm at the severity-assigning sites.

## Dependencies + sequencing
- **Depends on:** C-fix (`77c717e`, the arch_gap interim this retires).
- **Blocks:** C3 (the file-watcher slice — so C3's live ingestion surfaces the CORRECT security/policy classes). Land this BEFORE C3.

## Estimated commit count
**1** (implementer code/schema/snapshot/consumers/rewire) **+ orchestrator doc rows** (staggered, round routing). A frozen-contract + safety-taxonomy slice — its OWN commit, never bundled; **mandatory adversarial review (contract correctness + safety-taxonomy: no member silently mis-bucketed, severities defensible, additive-only)**.

## Lessons-logged candidates anticipated
- **Convention candidate** — "expanding a frozen §16 taxonomy is additive-only + tsc-exhaustiveness-driven, but a DEFAULT-carrying switch must be hand-audited (tsc won't catch a silent mis-bucket); a security/isolation class warrants elevated severity."
- **Architecture-doc note candidate** — the OBS-2 `FailureClass` now distinguishes security/policy/egress/isolation terminal causes (retiring the least-wrong interim).

## How to invoke
1. **Read this brief end-to-end** — Step-2.5 #1 (taxonomy) + #2 (the default-switch audit) are the crux.
2. **Run `/tdd failureclass_security_taxonomy_expansion`.**
3. **Step 0 (Restate)** — confirm: additive frozen-contract enum expansion + rewire the arch_gap sites + full tsc-exhaustiveness + the schema-snapshot pin.
4. **Step 2.5** — surface the final taxonomy + the consumer-ripple handling (esp. any default-switch that needed a fix).
5. **Step 8** — mandatory adversarial review (contract + safety-taxonomy).
6. **Step 9** — flag the `Cross-doc invariant change` (the orchestrator writes ARCHITECTURE.md §16 + Appendix A + the cross-doc row hot).
