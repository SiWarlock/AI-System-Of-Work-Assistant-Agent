# /tdd brief — serve_projection_denial_audit (24.44 leg 2 of 2 — PRODUCER)

## Feature
Thread `24.33`'s existing `GclAuditPersistPort` through `serveProjection` so its **re-gate denial** persists an `AuditSignal` through the **same `persistDenialAudit` helper**, converting `serveProjection` to `async` — completing the coverage `24.33` deliberately scoped out.

## ⛔ SEQUENCING — the hard constraint, and it is the Done-when

The lead's ruling, verbatim in force:

> **Both commits land in the SAME round with NO RED WINDOW between them — that is the Done-when, not a preference.**

⭐ **Worker's leg 1 (brief `265`) lands FIRST and is already dispatched.** It adds a single `await` to `apps/worker/src/composition/crossWorkspaceRead.ts:139` **while your function is still synchronous** — which typechecks clean (verified with the repo's own `tsc` 5.9.3, `--strict`, exit 0; `await` on a non-thenable is a legal no-op, and the repo has **no ESLint config at all**, so no `await-thenable` rule objects).

⇒ By the time you flip `serveProjection` to `async`, the consumer **already awaits it**. Both commits are green. Neither intermediate state is red.

⚠⚠ **THIS IS A SINGLE SHARED WORKING TREE — not a worktree build.** Worker is editing `crossWorkspaceRead.ts` in the same tree you are working in.

⛔ **Do NOT make `serveProjection` async until worker's `await` is in the tree.** Verify it yourself before going GREEN — do not take my word or a message for it:

```sh
git log --oneline -- apps/worker/src/composition/crossWorkspaceRead.ts | head -3
grep -n "await serveProjection" apps/worker/src/composition/crossWorkspaceRead.ts
```

⚠ **`git ls-files` is NOT the check** — it lists the INDEX, so a merely *staged* file reads as tracked. **Committed means `git log -- <path>` returns a hash.** (Both traps cost real time in this project on 2026-08-12.)

⭐ **Steps 0–2.5 do not require the flip.** Explore, write the RED tests against the intended `async` signature (they will be RED — correctly, that is the point), and send the Step-2.5 write-up. Worker's one-word commit will land inside that window. If it somehow has not landed when you reach Step 3, **tell me — do not proceed and do not "just also edit worker's file."**

## Use case + traceability
- **Task ID:** `24.44` (leg 2 of 2)
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- ⚠ **This brief widens phase scope because** Phase 24 is a cross-cutting *remediation* phase whose declared anchors (`§19.11 §6 §13 §4 §3`) describe its own deliverables, while its findings land wherever the audited defect actually lives — here the GCL visibility/policy surface (`§5`) and the error convention (`§16`). ⛔ **The correct resolution is this declaration, NOT adding `§5`/`§16` to Phase 24's declared anchor set** — an anchor set that grew because a linter demanded it is worthless (the lead's 2026-07-29 ruling; Carry-forward 6(a0)(iii)). `### 24.44`'s own `Spec:` line already cites `§5, §16`, so the brief matches the tracker.
- **Safety:** rule 4 (workspace isolation / WS-8)
- **Related context:** `### 24.44` and `### 24.33` in `IMPLEMENTATION_PLAN.md` · your own `24.33` mechanism (`persistDenialAudit`, `GclAuditPersistPort`, `admitAndPersistProjection`) in `packages/knowledge/src/gcl/projection.ts`

## ⭐ The argument for doing this at all — it is YOURS, recorded so it is not rediscovered

From your own `24.33` Step 2.5: `serveProjection`'s re-gate denial is the **defense-in-depth catch of a post-write tamper** — **arguably the MORE safety-critical audit case of the two**, since `admitAndPersistProjection` catches a bad projection on the way **in**, while `serveProjection` catches one **already stored**. ⇒ **covering only the write path leaves the more important denial silent.**

⭐ **And why splitting it cost nothing real:** the whole path is **dormant** — neither function has a production caller — so "the more critical case stays silent" was true of a *future* state either way.

## ⭐ Provenance — you are inside a lead ruling, not an orchestrator's call
`24.44` is a **first-of-its-kind extension of `L121`** from test fixtures to **production** code. The previous orchestrator **correctly refused to authorize it** and routed it up. **The lead ruled it authorized.** Recorded here so the precedent is traceable.

## Acceptance criteria (what "done" means)
- [ ] `serveProjection` becomes `async`, returning `Promise<GclAdmitResult>`, and accepts the same optional `auditPersist?: GclAuditPersistPort` trailing parameter shape `admitAndPersistProjection` already uses
- [ ] Its deny path persists through **`24.33`'s existing `persistDenialAudit` helper** — ⛔ **NOT a second copy of the `isRedactionSafe` gate** (`L39`, single-sourcing)
- [ ] The same **four pins** `24.33` established, applied to this deny path:
  - [ ] **carries the signal** — the denial's `AuditSignal` reaches the port
  - [ ] **end-to-end durable record** — a real denial produces a durable record (⚠ **MOVE THE STATE**, per `24.17`'s precedent: a construction-side test cannot distinguish "the signal is constructed and dropped" from a fix, because that is exactly the defect)
  - [ ] **fail-closed on unsafe** — a signal failing `isRedactionSafe` is refused, never persisted
  - [ ] **allow persists nothing** — the admit path writes no audit
- [ ] `auditPersist` **unbound** ⇒ byte-equivalent behavior to today (no port, no persist, same `GclAdmitResult`)
- [ ] Repo-wide `tsc --noEmit` green **and** the full suite green — including `apps/worker`'s `crossWorkspaceRead` tests
- [ ] ⭐ **The stale doc comment is corrected — see below.** This is an acceptance bullet, not a nicety.

## ⭐⭐ The doc comment at `projection.ts:107-110` — fold this in, and mind exactly how

The current comment reads:

> *"Deliberately does NOT carry `auditPersist` (task 24.33's asymmetry): this function has a **real production caller** (`apps/worker/src/composition/crossWorkspaceRead.ts`) that calls it **SYNCHRONOUSLY** — widening this to `Promise<GclAdmitResult>` would break that file's compile. Filed as its own producer/consumer pair, `### 24.44`, rather than folded in here."*

**Every clause of it goes stale with this slice** — it will no longer lack `auditPersist`, the caller no longer calls it synchronously, and `24.44` will be done.

⛔⛔ **And the replacement must not repeat its sharpest flaw.** *"has a real production caller"* reads as a **liveness claim**, and this path is **dormant** — `resolveApprovedCrossWorkspaceSlice` has **zero production callers**; every call site is in its own test file, and `boot.ts:1765` / `buildGclProjection.ts:13` / `scopedRetrieval.ts:15` are **comments**. **You are the one who falsified that claim from source at `24.33` Step 0**, and the lead confirmed it independently — it is why `24.33` left the arming-block release path.

⭐ **This is `L145`'s exact shape** — *a comment asserting its own code is live/load-bearing is a reachability claim nothing re-measures, sitting where it is most believed* — and it is the **same class as `### 24.47`**, still open. The true statement is narrower: *a call site exists in a production source file*, which is **not** the same as *the path executes in production*.

**Done-when for the comment:** it states what is **structurally** true (this function re-gates a stored projection at serve time and now persists its denial audit) and, if it mentions the caller at all, says the path is **dormant/unwired** — ⚠ **keep the true half; do not reduce it to "this is dormant" and lose why the function exists.** Sweep the file for sibling occurrences, not just this one (`L94`).

## Wiring / entry point (Step 7.5)
**none — the path is dormant**, established from source (above) and **not** to be re-asserted as live. **Consumer slice:** Phase **25.2/25.4** port wiring, which `### 24.45` **blocks** (`isRedactionSafe` is a keyword heuristic whose doc overclaims its coverage — ⛔ *the defect arrives with the fix for it*). Both are real tracked tasks; `L106`'s obligation is satisfied.

⚠ **`24.45` is exactly why you must not "improve" `isRedactionSafe` while you are in here** — it is providers territory and its own task. Call the existing gate; do not touch it.

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gcl/projection.ts` — `serveProjection` → `async` + `auditPersist` threading + the doc-comment correction
- `packages/knowledge/test/…` — the new pins (and any existing `serveProjection` caller-tests that must now `await`; **converting your own direct-caller tests to `await` is in-scope, not scope creep** — worker `L13`'s precedent)

**New:** a test file if the existing one is the wrong home — your call, flag at Step 2.5.

⛔ **You do NOT touch `apps/worker/`.** Worker's leg 1 owns that edit. If repo-wide `tsc` shows a worker-side break after your change, that means leg 1 did not land — **stop and tell me**, do not fix it yourself.

## RED test outline (Step 2)
In `packages/knowledge/test/…` (mirror `24.33`'s own test home + naming):

1. **`serve_projection_denial_carries_the_signal`** — a re-gate denial reaches the injected port with the denial's `AuditSignal`. *Asserts:* port received the signal for the source workspace. *Why:* §5/§16, safety rule 4 — the `24.33` pin, applied to the serve path.
2. **`serve_projection_denial_is_durably_recorded_end_to_end`** — **move the state**: trigger a real denial (a stored row that fails the re-gate — tampered raw content or raised visibility) and assert the durable record exists. *Why:* `24.17`'s verification precedent; a construction-side assertion cannot distinguish the defect from the fix.
3. **`serve_projection_denial_fails_closed_on_unsafe_signal`** — a signal failing `isRedactionSafe` is refused, never persisted. *Why:* safety rule 7, `24.33`'s fail-closed house rule.
4. **`serve_projection_allow_persists_nothing`** — the admit path writes zero audits. *Why:* `24.33`'s fourth pin; keeps the audit a denial signal, not a traffic log.
5. **`serve_projection_without_audit_port_is_byte_equivalent`** — unbound port ⇒ same `GclAdmitResult`, zero port construction. *Why:* the dormant default must not change (`L11`/`L23` family).

⚠ **Make the deny-path tests non-vacuous** — an assertion that can pass because the denial never happened is the failure mode this whole task exists to fix. Include a positive control.

## Cross-doc invariant impact
- **Model field changes:** none expected — `GclAuditPersistPort` already exists from `24.33`
- **Orchestrator doc rows to write hot:** likely none; **flag at Step 9 if `serveProjection`'s exported signature counts as a contract surface** any other area's doc mirrors
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No new model; the signature widening is within `packages/knowledge`'s own export surface — **flag at Step 2.5 if you disagree**

## Things to flag at Step 2.5
1. **Parameter position for `auditPersist`.** Mirror `admitAndPersistProjection` (optional trailing param) or an options object? My default vote: **optional trailing param, mirroring the sibling exactly** — parity beats novelty in the same file, and it keeps every existing caller valid.
2. **Does `serveProjection` need its own `sourceWorkspace.id` for the persist call, or should the workspace come from the stored row?** My default vote: **`sourceWorkspace.id`, matching `admitAndPersistProjection:90`** — the served workspace, never a row-derived value (WS-8; a tampered row's own `workspaceId` is exactly what must not be trusted here). ⚠ **This is a safety-relevant call — if you think the row's value is right, say so before GREEN, not after.**
3. **Test home** — extend `24.33`'s existing test file or a new sibling? My default vote: **extend the existing one** if the fixtures are reusable; a new file if it would fight them.
4. **The doc-comment rewrite** — propose the exact replacement text in your Step-2.5 write-up. My default vote: state the re-gate purpose + the audit behavior + an explicit **dormant/unwired** note. I want to review the wording, given `L145` and `24.47` are live about precisely this.

## Dependencies + sequencing
- **Depends on:** `### 24.33` (the mechanism — landed) **and brief `265` / worker's `await` commit (must be in the tree before you go GREEN).**
- **Blocks:** nothing directly; completes `24.44`. Phase 25.2/25.4's wiring consumes both halves (gated behind `### 24.45`).

## Estimated commit count
**1.** Own commit. ⛔ **Do not bundle** — it is one half of a cross-area pair, it touches safety-rule-4 machinery, and it must be independently bisectable.

## Lessons-logged candidates anticipated
- **Convention candidate** — *a sync→async producer widening that must cross an area boundary lands CONSUMER-FIRST: `await` on a non-thenable is a legal no-op, so consumer-first is green at every commit while producer-first is red by construction.* ⭐ This is the reusable half of the whole pair and I expect it to be the round's most portable lesson.
- **Convention candidate** — *a doc comment explaining why a widening was deferred becomes false the moment the widening lands; the deferral note and the deferred work are one unit.*
- **Architecture-doc note candidate** — the serve-path denial-audit behavior, if consumers will depend on it.

## How to invoke
1. Read this brief end-to-end.
2. **Verify worker's `await` commit status yourself** (the two commands above) — note it in your Step-2.5 write-up either way.
3. Run `/tdd serve_projection_denial_audit`.
4. At **Step 2.5**, send the write-up including the proposed doc-comment text.
5. At **Step 9**, categorize as usual — safety rule 4 is in scope, so that routing reaches the lead as well as me.
