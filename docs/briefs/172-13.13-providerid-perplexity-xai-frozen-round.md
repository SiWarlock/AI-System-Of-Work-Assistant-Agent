# /tdd brief — providerid_perplexity_xai_frozen_round

## Feature
A **frozen-contract round** adding `perplexity` + `xai` to the `ProviderId` enum with **`ProviderMatrix` egress-cloud classing** (each its OWN processor id — NEVER an alias of `openai`/`openrouter`, safety rule 5) + the schema-snapshot, in ONE atomic round (§13.5/13.15 discipline). Dormant — the enum + matrix is the contract foundation for 13.13's research provider; NO transport/endpoint (provint binds it later via §ARM-RESEARCH).

## Use case + traceability
- **Task ID:** 13.13 (the frozen-contract ProviderId leg — the provider IMPL `research-provider.ts` is a separate provint slice that depends on this)
- **Architecture sections it implements:** `ARCHITECTURE.md §7` (Provider taxonomy / ModelProviderPort — RES-1) · `§5` (egress veto — the matrix egress-classing is what makes an employer-work ack-OFF call fail closed) + **REQ-F-022**.
- **Related context:**
  - Mirror the just-landed 13.15 frozen-contract discipline (`54b052a7`): the enum + matrix + schema-snapshot land in ONE round with repo-wide typecheck.
  - Current `ProviderId` (`packages/contracts/src/primitives/enums.ts:14`): `claude`/`openai`/`openrouter`/`ollama`/`lm_studio`. `EgressClass` is at `:17`. `ProviderMatrix` at `packages/contracts/src/models/provider-matrix.ts:38`.
  - Safety rule 5 (verbatim posture): **"OpenRouter is its own processor, not an OpenAI alias"** — the same applies to perplexity + xai: each its OWN processor id, never aliased. §ARM-RESEARCH: "each vendor its OWN processor id — perplexity/xai NEVER an alias".
  - **Blocks:** provint's 13.13 research-provider impl (`packages/providers/src/model-provider/research-provider.ts`) + 13.14 /research flows.

## Acceptance criteria (what "done" means)
- [ ] `ProviderId` gains `perplexity` + `xai` (additive); both validate through the enum.
- [ ] The `ProviderMatrix` classes each new provider as **egress / cloud** (NOT local-zero-egress) — a test pins each new provider's egress class is cloud, so an employer-work ack-OFF call through the broker egress veto **fails closed** (safety rule 5, no cloud fallback).
- [ ] Each new provider is its **OWN processor id** in the matrix — a test pins neither is aliased to `openai`/`openrouter` (rule 5, no-alias).
- [ ] The **schema-snapshot** is updated + frozen: the provider-enum snapshot test passes with the two new members (field-name/member set == checked-in snapshot, tagged `spec(§7)`).
- [ ] **ProviderId consumer ripple empirically surveyed** (13.15 lesson): any exhaustive switch over `ProviderId` handles the new members via one `assertNever`-guarded decision point, OR `ProviderId` is confirmed a pass-through discriminant pinned only by the membership test. Repo-wide `turbo typecheck` green.
- [ ] **No transport/endpoint reachable** — dormant; the enum + matrix is the contract, the real transport binds later (§ARM-RESEARCH). A test/scan confirms no real research endpoint is wired here.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — this is a frozen-contract foundation slice; consumers land downstream:** provint's `research-provider.ts` (13.13-impl) + the broker routing + the policy processor allowlist consume the new `ProviderId`s. This slice's reachability proof is the schema-snapshot + membership/egress-class/no-alias tests. State explicitly: **wiring lands in the provint 13.13 research-provider impl / §ARM-RESEARCH.** (spec-lint requires this section.)

## Files expected to touch
**Modified:**
- `packages/contracts/src/primitives/enums.ts` — `ProviderId += perplexity, xai` (confirm `EgressClass` has a cloud member to class them).
- `packages/contracts/src/models/provider-matrix.ts` — matrix entries classing perplexity/xai as egress-cloud, each its own processor.
- `packages/contracts/schemas/*.json` (the provider/ProviderId schema snapshot) + the field-snapshot test.
- any exhaustive `ProviderId` switch surfaced by the ripple survey (add the new members to its decision point).

If the round needs `config/providers.defaults.json` rows or a real transport, **flag at Step 2.5** — that is arming-adjacent / provint territory, deferred (see Q3).

## RED test outline (Step 2)
1. **`providerid_gains_perplexity_xai`** — both new members validate through `ProviderId`. Why: §7 provider taxonomy.
2. **`new_providers_classed_egress_cloud`** — the matrix classes each as cloud/egress (not local). Why: §5 / rule 5 (employer-work ack-OFF fails closed).
3. **`new_providers_own_processor_no_alias`** — neither perplexity nor xai aliases `openai`/`openrouter`; each is a distinct processor. Why: rule 5 verbatim ("its own processor, not an alias").
4. **`provider_enum_snapshot_frozen`** — the schema-snapshot member set == checked-in snapshot with the two additions. Why: §13.5 frozen-contract discipline.
5. **`providerid_exhaustiveness_holds`** — any exhaustive `ProviderId` consumer handles the new members (or the membership test is the sole pin). Why: 13.15 ripple-survey lesson.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **YES — `ProviderId` enum members + `ProviderMatrix` rows** (a frozen-contract change). Additive + fail-closed (classes the new providers as egress-vetoed) — a TIGHTENING, not a safety relaxation.
- **Orchestrator doc rows to write hot:** the orchestrator writes the `ARCHITECTURE.md` Appendix A / §7 provider-taxonomy row + the `packages/contracts/CLAUDE.md` cross-doc row for the two new `ProviderId`s atomic with the round commit. Implementer does NOT touch those docs — flag at Step 9.
- **Contracts-seam model touched?** Yes — `ProviderId`/`ProviderMatrix` is a contracts seam; the RED outline includes the **schema-snapshot test** (member set == checked-in snapshot, tagged `spec(§7)`).

## Things to flag at Step 2.5
1. **EgressClass for the new providers.** Default vote: **cloud / egress** — both Perplexity + xAI are cloud services; class both as the egress member so the veto fails closed for employer-work ack-OFF. Confirm the exact `EgressClass` member.
2. **Processor identity (rule 5).** Default vote: declare perplexity + xai as **distinct processors** in the matrix; confirm the matrix carries a processor-id/vendor field and pin that neither aliases an existing processor.
3. **Config defaults + transport = DEFERRED.** Default vote: this round is enum + matrix-classing + snapshot ONLY; `config/providers.defaults.json` rows + the real transport are provint's 13.13-impl / §ARM-RESEARCH (arming-adjacent). Confirm — do NOT add real config/transport here.
4. **Exhaustiveness survey.** Default vote: **empirically survey** `ProviderId` consumers (13.15 lesson — a pass-through discriminant can have zero tsc-breaking consumers); add the new members to any exhaustive switch, else confirm the membership test is the sole pin.

## Dependencies + sequencing
- **Depends on:** 13.1, P5 (ModelProviderPort), P3 (EgressPolicy) — all landed; the 13.15 frozen-contract pattern as the template.
- **Blocks:** provint's 13.13 research-provider impl (`research-provider.ts` + `free-source-aggregator.ts`) · 13.14 /research flows · §ARM-RESEARCH.

## Estimated commit count
**1** — the frozen-contract round is one atomic contract change (enum + matrix + snapshot + ripple), §13.5/13.15 discipline, own commit. **security-reviewer = invariant** (rule 5 egress-classing + no-alias pins). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "a new egress-classed provider lands as a frozen round: enum + ProviderMatrix (cloud class + OWN processor, never an alias — rule 5) + schema-snapshot + empirical ripple survey, all dormant (no transport until §ARM-RESEARCH)."
- **Cross-doc invariant** — the two new `ProviderId`s + their egress-cloud classing (orchestrator writes Appendix A / §7 + the cross-doc row).

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (egress class) + Q2 (processor identity) + Q3 (defer config/transport) need answers before GREEN.
2. Run `/tdd providerid_perplexity_xai_frozen_round`.
3. Step 2.5 → main-orchestrator (with the schema-snapshot test mapped). Step 9 → main-orchestrator (frozen-contract cross-doc rows route to me — additive/tightening, no safety escalation).
