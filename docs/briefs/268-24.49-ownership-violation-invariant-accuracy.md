# /tdd brief — ownership_violation_invariant_accuracy (24.49)

## Feature
Correct every comment that describes `ownership_violation` as a **workspace-isolation** concern when its canonical invariant is **KN-7/KN-8 section (region) ownership** — a right classification resting on a wrong recorded reason.

## ⛔ THE BRIEF'S SCOPE IS WRONG AS FILED — I ENUMERATED BEFORE WRITING AND THE TASK UNDERSTATES IT

`### 24.49`'s `Files:` line reads *"the comments introduced at `77c717e1` and the two `24.23` touched in `sourceIngestion.ts`"* — i.e. **~3 comments in one file.**

⭐ **Measured 2026-08-12 at `969284b7`** — `grep -rn "ownership_violation" --include="*.ts" packages apps`, **43 occurrences**, classified by hand (⚠ `L104`: most are *uses* — union members, test assertions — **not** the mischaracterization). **Candidate mischaracterizations, 6 across 4 files and THREE territories:**

| # | site | text | territory |
|---|---|---|---|
| 1 | `packages/contracts/src/models/shared-enums.ts:137` | *"isolation_breach — a workspace-isolation refusal (**WS-4 ownership_violation**)"* | ⛔ **contract — UNQUEUED** |
| 2 | `packages/workflows/src/workflows/sourceIngestion.ts:251` | *"ownership_violation (a **WS-4 workspace-isolation refusal**) → isolation_breach"* | worker |
| 3 | `packages/workflows/src/ports/meetingCloseout.ts:420` | *"cross-workspace ownership breach (**WS isolation**)"* | worker |
| 4 | `packages/workflows/src/workflows/deletionSaga.ts:274` | *"the plan targeted a region **outside the bound workspace**"* | worker |
| 5 | `packages/workflows/test/source-ingestion.test.ts:421` | test NAME: *"(a **WS-isolation breach** never retries blindly)"* | worker |
| 6 | `packages/workflows/test/source-ingestion.test.ts:434` | *"same isolation-class treatment as ownership_violation"* | worker |

⇒ ⛔ **This is `24.23`'s shape exactly — filed as "a few comments in one file," measured as 6 sites in 4 files** — and the lead has now named the cause: **a brief's scope is an artifact of DISCOVERY, not of the defect's extent.** ⭐ **Treat the table above as a CANDIDATE SET to classify, not a work list** — I have not verified each reading in context, and #4 may be a *third* distinct characterization rather than an instance.

⛔⛔ **SITE 1 IS `packages/contracts/` — NOT YOUR TERRITORY, AND CONTRACT IS AN UNQUEUED TRACK.** **Do not touch it.** ⚠ **And it is arguably the most load-bearing of the six**, because it sits on the shared `FailureClass` enum where every consumer reads the taxonomy's meaning. **I am routing it to the lead as an allocation decision** (`L121` does **not** cover this — that lesson is about a widening's own compile-break; this is editing another area's file for correctness, which is a crossing). **Flag it in Step 9 if your reading changes its severity.**

## ⚠ THE PREMISE YOU MUST CHECK FIRST — my semantic claim is a hypothesis

**My reading of the canonical meaning, from the evidence I have:** `ownership_violation` is raised by `packages/knowledge/src/knowledge-writer/ownership.ts` for **region/section ownership** — an attempted write into a human-owned or untargeted region:
- `ownership.ts:9` — *"Four rejection conditions, all surfaced as a typed `ownership_violation`"*
- `ownership.test.ts:127` — *"treats a `@generated` region as writer-owned — an untargeted `@generated` change ⇒ ownership_violation (**KN-8**, `== kw:region`)"*
- `ownership.test.ts:142` — *"unmarked prose is still **human-owned** + protected"*

⇒ **section ownership within a note, NOT workspace isolation.** ⛔ **But I have not read `ownership.ts`'s four conditions in full, and this brief is a hypothesis** (`L81`). **Establish the canonical invariant from source at Step 1 and tell me if I am wrong** — the whole slice turns on it, and getting it backwards would propagate a *new* wrong reason over an old one.

## Use case + traceability
- **Task ID:** `24.49`
- **Architecture sections it implements:** `ARCHITECTURE.md §16`
- ⚠ **This brief widens phase scope because** Phase 24 is a cross-cutting *remediation* phase whose declared anchors (`§19.11 §6 §13 §4 §3`) describe its own deliverables, while its findings land wherever the defect lives — here the error convention (`§16`). ⛔ **Not by adding `§16` to Phase 24's declared anchor set** (Carry-forward 6(a0)(iii)).
- **Related context:** `### 24.49`, `### 24.23` (which propagated it into two more comments), `L73` (a right answer on a wrong reason), `L145` (a comment claim nothing re-measures)

## ⭐ Why this is not cosmetic — the reason it was filed rather than dropped
**The CLASSIFICATION IS CORRECT and unaffected:** `workspace_path_violation → isolation_breach` stands on its own **WS-8** merits, and `ownership_violation`'s class predates `24.23`. ⛔ **What is wrong is the REASON recorded next to it.** ⭐ **`L73`'s meta, exactly: *a right answer on a wrong reason is indistinguishable from a right answer until someone builds on the reason.*** ⇒ **the next person reasoning about what belongs in `isolation_breach` reads these comments, infers section-ownership is a workspace-isolation matter, and classifies something new on a false premise.**

## Acceptance criteria
- [ ] The candidate set above is **classified** — each site either corrected, or recorded as **not** an instance with the reason (⭐ a per-member disposition is the artifact, not a count — `L107`)
- [ ] Corrected comments state each code's **actual** invariant: `ownership_violation` = **KN-7/KN-8 section ownership**; `workspace_path_violation` = **§5 WS-8 / safety rule 4**
- [ ] ⛔ **If both legitimately map to `isolation_breach`, the comments SAY WHY two different invariants share one failure class** — **do NOT silently delete the cross-reference; the shared class is real and a reader needs to know it is deliberate**
- [ ] Site 1 (`packages/contracts/`) **untouched**, and its disposition reported at Step 9
- [ ] Repo-wide `tsc` green; workflows suite green (⚠ **comment-only changes should move neither** — if a suite moves, a test NAME was load-bearing and that is a finding)
- [ ] The claim you write is **scoped**: *"corrected across `packages/workflows`; `packages/contracts/src/models/shared-enums.ts:137` carries the same defect and is out of territory"* — ⛔ never an unqualified *"the mischaracterization is fixed"*

## ⚠ TDD posture — disclosed exemption
**Comment-accuracy has no behavior to pin, and you should not manufacture a test for it.** Two sites (#5, #6) are **test names**, which you may rename — ⚠ **a rename is not a behavior change, but confirm the suite still passes and that no test's discrimination depended on its name.** Verification is: the classification table, `tsc` green, suites unmoved. **State the exemption at Step 9; do not let it read as a normal slice.**

## Wiring / entry point (Step 7.5)
**none — and NOT the deferred-consumer kind.** ⛔ **This is not *"wiring lands in slice X"***, which would carry `L106`'s obligation to name a real tracked consumer. **There is no wiring to land: the slice changes comments and two test names, adds no symbol, and alters no call path.** The code these comments describe (`mapWriteFailure`, `commitFailureClass`, `commitFailureState`, `commitFailureToVariant`) is **already reachable and already pinned** — `24.23` landed that at `68a83dd0`.
⭐ **The "entry point" here is a READER, not a call path** — which is exactly why this defect survives every gate: no test reds, no type complains, and the only failure mode is a person building on the wrong reason (`L73`). ⇒ **the correction must land where that reader looks**, which is why the enumeration spans four files rather than the one where it was noticed (`L94`).

## Files expected to touch
**Modified:** `packages/workflows/src/workflows/sourceIngestion.ts` · `packages/workflows/src/ports/meetingCloseout.ts` · `packages/workflows/src/workflows/deletionSaga.ts` · `packages/workflows/test/source-ingestion.test.ts`
⛔ **NOT** `packages/contracts/src/models/shared-enums.ts`.

## Things to flag at Step 2.5
1. **Is my canonical-invariant reading right?** Default vote: **KN-7/KN-8 section ownership** — but establish it from `ownership.ts`'s four conditions. ⚠ **If I am wrong, stop; do not correct comments toward a wrong target.**
2. **Is site #4 (`deletionSaga.ts:274`, *"a region outside the bound workspace"*) the same defect, or a THIRD characterization?** Default vote: **classify it explicitly rather than assuming** — it reads like a hybrid, and a hybrid may indicate the union member is genuinely used for two things somewhere.
3. **Do the test names (#5, #6) get renamed, or annotated?** Default vote: **rename** — a test name is a claim about scope (`L93`), and a wrong one misleads with no review. Disagree if a name is referenced elsewhere.
4. **Does any comment's correction change what a reader would conclude about the CLASS mapping?** If correcting the reason makes `isolation_breach` look wrong for `ownership_violation`, **that is a finding, not a comment fix** — surface it before Step 3.

## Cross-doc invariant impact
- **Model field changes:** none — comments only
- ⚠ **`FailureClass`'s documented semantics live in `packages/contracts` (site 1)** — if your classification concludes the enum's own doc is wrong, that's a **cross-doc invariant flag at Step 9**; I write it, you don't
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No — but site 1 sits on a shared enum, which is why it is lead-routed

## Dependencies + sequencing
- **Depends on:** `### 24.23` (landed `68a83dd0`) — it propagated the defect into two comments
- **Blocks:** — · ⚠ **Related, do NOT absorb:** `### 24.47` (same `L145` family, eval-security territory, unqueued)

## Estimated commit count
**1.** Comment-accuracy across one package. ⛔ Do not bundle site 1 in if it later gets authorized — that's a separate territory and a separate commit.

## Lessons-logged candidates anticipated
- **Convention candidate** — *a taxonomy comment that names the WRONG invariant survives every gate: the classification is right, the tests pass, and only a reader building on the reason ever discovers it.*
- **Future TODO** — site 1's disposition, if the lead authorizes it.

## How to invoke
1. Read end-to-end. 2. Establish the canonical invariant from source **first**. 3. `/tdd ownership_violation_invariant_accuracy`. 4. Step 2.5 with your classification table. 5. Step 9 with the scoped claim + site 1's disposition.
