# /tdd brief — calendar_http_transport (connector round 3, slice 2 — Google Calendar read transport; CLOSES round 3)

## Feature
Add **`createCalendarHttpTransport(deps): ConnectorTransport`** — the Google Calendar read-only HTTP transport — as a thin per-vendor spec over the round-2 `createConnectorHttpTransport` template. This is a near-mechanical MIRROR of the Drive slice (076); it CLOSES round 3. Supplies only the Calendar `ConnectorHttpSpec` (base URL, allowlisted host, resource path, buildQuery, candidate mapPage) + wires it to the existing `createCalendarConnector`.

**OAuth note (no code change — the template is token-agnostic).** Same as Drive: the template's `SecretsAccessor.getSecret(tokenRef) → bearer string` accepts a Google OAuth access token unchanged; refresh/expiry is ARMING-era behind the SecretsAccessor (401 → `auth_locked`).

**Dormant + no hard line.** Real `HttpTransport` + `SecretsAccessor` + OAuth token stay UNBOUND (byte-equivalent); binding a real transport + a real Google OAuth token is the owner's arming crossing.

## Use case + traceability
- **Task ID:** 13.12 (connector real-transport go-live — round 3, Google Calendar; runbook Phase 8).
- **Architecture sections it implements:** `ARCHITECTURE.md §8` (Connector & Tool Gateways — the connector-transport template) + `§5` (EgressPolicy / SecretsPort — SSRF admission + OAuth token resolution).
- **Consumes (committed):** the round-2 `createConnectorHttpTransport` template + `ConnectorHttpSpec` (`47c55c2`) + `isAllowedRemoteEndpoint` (`c2c6525`); mirror the Drive instance (`createDriveHttpTransport`/`driveMapPage`/`driveBuildQuery` in `drive.ts`, slice 076) EXACTLY.
- **Context7-grounded wire shape (per the process directive — candidate, confirmed at arming):** `/websites/developers_google_workspace_calendar_api` (`events.list`) — endpoint `GET /calendars/{calendarId}/events`; query `maxResults` (default 250, **max 2500**), `pageToken`, `singleEvents` (expand recurrences), `timeMin`/`timeMax` (RFC3339), `orderBy` (`startTime`|`updated`); response `{ kind (the calendar-events collection marker), updated, nextPageToken?, nextSyncToken?, items: [Event] }` — `nextPageToken` omitted on the LAST page (then `nextSyncToken` is present, and vice-versa); each Event carries `id` + `updated` + `status`. **Still a documented `arch_gap` candidate** (parsed fail-closed); the real shape is re-confirmed at the arming binding.
- **Wires into:** `createCalendarConnector(transport)` (`adapters/calendar.ts`, readScope `calendar.readonly`) via `makeConnector`.

## Acceptance criteria (what "done" means)
- [ ] `createCalendarHttpTransport(deps: ConnectorHttpTransportDeps): ConnectorTransport` = `createConnectorHttpTransport(CALENDAR_HTTP_SPEC, deps)`, added to the **EXISTING `adapters/calendar.ts`** (co-located with `createCalendarConnector`, mirror `drive.ts` — NOT a new file, so no OSB count-pin trip).
- [ ] `CALENDAR_HTTP_SPEC: ConnectorHttpSpec` = `{ baseUrl: "https://www.googleapis.com/calendar/v3", allowedHosts: ["www.googleapis.com"], resourcePath: "/calendars/primary/events", buildQuery: calendarBuildQuery, mapPage: calendarMapPage }`. (`primary` calendar is a documented candidate default — a configurable calendarId is an arming-era refinement.)
- [ ] **`calendarBuildQuery`** builds the events.list query from `TransportRequest.cursor` — candidate `?pageToken=<cursor>&maxResults=<n>&singleEvents=true` — **percent-encoding the cursor** (`encodeURIComponent`); no `pageToken` on the first (cursorless) page. arch_gap candidate.
- [ ] **`calendarMapPage` (arch_gap candidate):** maps the events.list envelope — candidate `{ items: Event[], nextPageToken?: string, nextSyncToken?: string }` → `items = events.map(e => ({ id: e.id, hash: payloadHash({ id: e.id, updated: e.updated }), raw: e }))`, `nextCursor = nextPageToken`, `done = nextPageToken === undefined` — parsed **FAIL-CLOSED** (missing/renamed `items`/non-array/event-missing-`id`/non-object envelope ⇒ `TransportFailure`, never a false page). `contentHash` via canonical `payloadHash`; `updated` is Calendar's change token (candidate — raw fallback if absent). **Name `nextSyncToken` in the arch_gap comment** — it's the incremental-sync token (arming-era; paging uses `nextPageToken`, NOT the sync token).
- [ ] **SSRF + redaction inherited + verified** (as Drive): admit `www.googleapis.com` on the final url; token in `Authorization` only, never in a failure; reject matrix is template-level (not re-tested).
- [ ] **Dormant + reachability-waivered:** no boot wiring; real transport/secrets/token UNBOUND; byte-equivalent. `/preflight` clean; repo-wide green.

## Wiring / entry point (Step 7.5)
**none (reachability-waivered)** — production caller is a later owner-arming boot binding (`createCalendarConnector(createCalendarHttpTransport(...))` with a real transport + an OAuth `calendar.readonly` token). State the waiver.

## Files expected to touch
**Modified:** `adapters/calendar.ts` (the spec + mapper + `createCalendarHttpTransport`) + `adapters/index.ts` (barrel export, mirror Drive).
**New:** `packages/integrations/test/connector-calendar-transport.test.ts` — the Calendar candidate-map + fail-closed + end-to-end-via-`createCalendarConnector` suite over fakes.

## RED test outline (Step 2)
1. **`calendar_2xx_maps_to_page`** — candidate `{items:[…], nextPageToken}` ⇒ `TransportPage` (event `id`→recordId, stable `payloadHash({id,updated})`, raw preserved, `nextCursor=nextPageToken`, `done=false`); no-`nextPageToken` ⇒ `done=true`.
2. **`calendar_buildQuery_encodes_cursor`** — crafted cursor `encodeURIComponent`'d into `pageToken`, cannot alter the host; no `pageToken` on the first page.
3. **`calendar_malformed_shapes_fail_closed`** — items-not-array / missing-items / event-missing-id / non-object ⇒ `TransportFailure`.
4. **`calendar_ssrf_and_redaction_inherited`** — admit `www.googleapis.com`; a non-2xx ⇒ safe status only; token never leaked.
5. **`calendar_end_to_end_via_createCalendarConnector`** — the whole `ConnectorPort.fetch` chain.

## Things to flag at Step 2.5
1. **File placement:** existing `calendar.ts` (mirror `drive.ts`) — no count-pin trip. Default vote: existing file.
2. **Calendar candidate wire shape + `nextSyncToken`.** Default vote: page via `nextPageToken`; NAME `nextSyncToken` in the arch_gap comment as the incremental-sync token (arming-era, not the paging cursor). `contentHash = payloadHash({id, updated})`. Confirm.
3. **`calendarId` default.** Default vote: `primary` as a documented candidate default in the resource path; a configurable calendarId is an arming-era refinement (note it). Confirm.

## Cross-doc invariant impact (implementer flags at Step 9)
- **Model field changes:** NONE — reuses the frozen template seams. `CALENDAR_HTTP_SPEC` is integrations-internal.
- **Orchestrator doc rows (Step 9):** the `§8` connector-template note now covers Asana + Drive + Calendar (orchestrator writes at round close). The OAuth arming residuals (token manager; read-only-scope invariant) are already in Carry-forward.
- **Seam (shared-contract) model touched?** No.

## Dependencies + sequencing
- **Depends on:** the round-2 template + the Drive slice (076, the mirror source). **CLOSES round 3.**
- **Blocks:** nothing in-round. Round 4+ = the remaining connectors (Todoist/Linear/GitHub) + Granola (its own local-source round) + Gmail from scratch. Real network binding is owner-gated arming.

## Estimated commit count
**1** — the Calendar spec + mapper + suite. **Mandatory dual review (security [external-I/O + SSRF + secrets = invariant] + code-quality).** Not bundled.

## Lessons-logged candidates anticipated
- **Convention candidate:** none expected (the 3rd application of providers Lesson 2's template); flag only if a Calendar-specific durable rule emerges (e.g. sync-token vs page-token).
- **Architecture-doc note candidate:** the `§8` note extension (Calendar instance).
