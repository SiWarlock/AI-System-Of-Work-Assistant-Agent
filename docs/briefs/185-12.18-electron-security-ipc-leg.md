# /tdd brief — electron_security_ipc_suite

## Feature
The **12.18 Electron security/IPC leg** (apps/desktop/test/security, desktop territory) — the DoD suite pinning the Electron MAIN-process security config (`contextIsolation:true` / `nodeIntegration:false` / `sandbox`) via a mocked electron ctor + the preload-surface (`contextBridge` exposes EXACTLY the inventoried channels, no extra, no Node escape). Completes 12.18 (the worker-auth leg landed `67c8f39a`).

## Use case + traceability
- **Task ID:** 12.18 (Electron security/IPC leg — the worker-API auth leg is done)
- **Architecture sections it implements:** `ARCHITECTURE.md §12` (Eval & Test Harness) · `§11` (Electron security — renderer isolation, preload bridge).
- **Related context:**
  - Mirror the 9.14 renderer-isolation harness pattern (`25029a76` — deterministic renderer-global altitude via a mocked electron ctor). 9.14 covered the RENDERER global; this leg covers the MAIN `webPreferences` + the preload-bridge==inventory.
  - The preload inventory (`preload/inventory.json`) + the snapshot test are the source of truth for the exposed channel set.

## Acceptance criteria (what "done" means)
- [ ] An **electron-isolation** suite pins the main-process `webPreferences` (via a mocked electron `BrowserWindow` ctor): `contextIsolation:true`, `nodeIntegration:false`, `sandbox` per the security posture — a config that weakens any of these FAILS.
- [ ] A **preload-surface** suite pins that `contextBridge` exposes EXACTLY the inventoried channels (bridge == inventory; no extra channel; no Node/`require`/`process` escape reaches the renderer).
- [ ] Deterministic — a **mocked electron ctor** (no live Electron runtime), mirroring 9.14.
- [ ] Distinct from 9.14 (main/preload, not the renderer global) — no duplicated assertions.
- [ ] The suite runs green; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — the security suite is the entry point** (desktop test suites under `apps/desktop/test/security/`); they assert the main `webPreferences` + preload-bridge==inventory via the mocked electron ctor + the preload inventory. State explicitly: no production change. (spec-lint requires this section.)

## Files expected to touch
**New:**
- `apps/desktop/test/security/electron-isolation.test.ts` — the main webPreferences suite.
- `apps/desktop/test/security/preload-surface.test.ts` — the bridge==inventory + no-Node-escape suite.

⚠ Desktop territory. `git add <path>` only.

## RED test outline (Step 2)
1. **`main_webprefs_context_isolation_on`** — the mocked BrowserWindow ctor is called with `contextIsolation:true`, `nodeIntegration:false`, `sandbox`; a weakened config fails. Why: §11 Electron isolation.
2. **`preload_bridge_equals_inventory`** — `contextBridge` exposes exactly the inventoried channel set (no extra). Why: §11 preload surface / inventory.
3. **`no_node_escape_to_renderer`** — no `require`/`process`/Node global reaches the renderer via the bridge. Why: §11 renderer isolation (main/preload boundary).
4. **`deterministic_mocked_ctor`** — the suite runs with a mocked electron ctor, no live runtime. Why: 9.14 pattern.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (test suite).
- **Orchestrator doc rows to write hot:** if the EVALUATION_CRITERIA matrix needs the 12.18-Electron row, flag at Step 9. Otherwise none.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Overlap with 9.14.** Default vote: this leg covers the **main `webPreferences` + preload-bridge==inventory** (9.14 covered the renderer global altitude) — no duplicated assertions; confirm the exact non-overlap boundary.
2. **Mocked ctor vs real runtime.** Default vote: a mocked electron ctor (deterministic, mirror 9.14) — NOT a live Electron runtime. Confirm.
3. **Inventory source.** Default vote: assert against `preload/inventory.json` (the existing snapshot source of truth). Confirm.

## Dependencies + sequencing
- **Depends on:** the desktop app + preload inventory (built) · 9.14 (`25029a76`, the harness pattern).
- **Blocks:** 12.18 full tick (`[x]` on this landing — both legs).

## Estimated commit count
**1** — one Electron security suite (electron-isolation + preload-surface), desktop territory, deterministic. **security-reviewer = invariant** (Electron isolation + preload-surface are security invariants — §11). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the Electron security DoD suite pins the MAIN webPreferences (contextIsolation/nodeIntegration:false/sandbox) via a mocked ctor + the preload bridge==inventory (no extra channel, no Node escape) — distinct from the renderer-global altitude (9.14); deterministic, no live runtime."

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (9.14 non-overlap) needs an answer before GREEN.
2. Run `/tdd electron_security_ipc_suite`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
