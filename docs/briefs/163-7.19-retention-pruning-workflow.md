# /tdd brief — retention_pruning_scheduled_workflow

## Feature
Rebuild the false-ticked **retention-pruning** durable workflow (RET-1/REQ-F-018): a pure `runRetentionPrune` driver + a `prunePolicy` activity that enforce the configurable retention policy on **assistant-held raw capture only**, routing any Markdown removal through **KnowledgeWriter (one-writer)** and never deleting human-owned sections or derived semantic notes (RET-3), idempotent + audit-logged, with LIFE-2 collapse-on-wake. Built **dormant** (unit-tested over injected fakes, mirroring `periodReview.ts`); the live Temporal schedule registration is owner-gated §19.12 and is NOT wired here.

## Use case + traceability
- **Task ID:** 7.19
- **Architecture sections it implements:** `ARCHITECTURE.md §9` (Temporal workflows — the driver+activity pattern; retention is a scheduled workflow) · `§5` (retention policy — REQ-F-018 default+configurable window) · `§16` (health items on failure states).

> **Widens phase scope because** it references `§10.1` (externally-authoritative sources kept as links, never pruned) as a prune-exclusion invariant and defers live scheduling to `§19.12` (owner-gated output-workflow arming) — both cited as boundaries, not implemented here.
- **Related context:**
  - **Confirmed false-tick:** `packages/workflows/src/workflows/retentionPrune.ts` and `activities/prunePolicy.ts` are absent repo-wide (find=0). This is a rebuild, not a re-tick.
  - **Template = `periodReview.ts`** (`packages/workflows/src/workflows/periodReview.ts`): a pure `runPeriodReview(input, deps)` over a local `defineMachine` + injected activity ports; imports NEITHER `@temporalio` NOR `node:crypto`, no `Date.now()` — fully Vitest-unit-testable. Mirror its shape exactly.
  - **LIFE-2 collapse:** reuse `collapsedNextRunFromClock(bookkeeping, clock, {intervalMs, catchUpWindowMs})` from `../runtime/catchUpWindow` (used at `periodReview.ts:599`) — a missed schedule collapses to one run on wake; `nextRun===null` parks with no durable write.
  - **Schedule bookkeeping:** injected `ScheduleStore` port (`packages/workflows/src/ports/operational.ts:142`); worker adapter `createScheduleStoreAdapter` (`apps/worker/src/lifecycle/last-run.ts:59`).
  - **RET-3 Markdown removal:** route through the KnowledgeWriter tombstone path — the reference is `deletionSaga.ts` + `activities/deletionPlan.ts`/`compensateDeletion.ts` (ordered, per-step-idempotent, "Markdown tombstone via KnowledgeWriter (commit point)", preserves human-owned content).
  - **What it prunes (thin substrate — see Q2):** the ONLY assistant-held raw payload store today is `SourceDispositionRow.sourceEnvelope.body` (`packages/db/src/repositories/interfaces.ts:407`, server-side-operational re-entry state). Audio/OCR/cached-payload producers are unbuilt. Externally-authoritative sources (Granola transcripts, Calendar events) are kept as **links** and NEVER re-stored or pruned (§10.1).

## ⚠ Safety (own commit; security-reviewer=invariant MANDATORY)
This slice touches **safety rule #1 (one writer / no hidden brain)** and **RET-3** (never delete human-owned/derived content). Every Markdown removal MUST route through KnowledgeWriter; the operational-store raw-payload deletion is a direct repo delete of assistant-held copies only. This gets its **own commit**; the implementer runs `security-reviewer` at Step 8 (invariant-gated).

## Acceptance criteria (what "done" means)
- [ ] `packages/workflows/src/workflows/retentionPrune.ts` exports a pure `runRetentionPrune(input, deps)` — no `@temporalio`/`node:crypto`/`Date.now()` import; deterministic over injected ports (mirrors `periodReview.ts`).
- [ ] `packages/workflows/src/activities/prunePolicy.ts` exports the prune-decision activity: given the configured window (default **30d**) + a clock, it selects which assistant-held records are due for prune; raw audio uses the distinct "delete after audited synthesis" predicate (Q1), NOT the window.
- [ ] **Assistant-held only:** the driver prunes only assistant-held copies via an injected `PrunablePayloadStore` port; externally-authoritative sources are never selected (a test pins a Granola/Calendar-linked record is skipped).
- [ ] **Re-entry guard:** an undispositioned parked source (still needed for triage re-entry) is NOT pruned (a test pins this — naive window-prune of parked sources would break re-entry).
- [ ] **RET-3 one-writer:** any Markdown removal routes through the injected KnowledgeWriter tombstone port; a test pins that the driver NEVER calls a raw fs/markdown delete and NEVER removes a human-owned/derived section.
- [ ] **Idempotent + audit-logged:** re-running the same prune is a no-op on already-pruned records; each removal is audit-logged.
- [ ] **LIFE-2:** a missed schedule collapses to one run on wake (`collapsed` true); nothing-due parks with no durable write.
- [ ] **Health:** failure states mint §16 HealthItems via a `failureClassFor`-style map reusing EXISTING members (`missed_or_late_schedule` for a missed/late prune; `write_through_failed` for a KW-removal/store-delete fault) — **no new FailureClass member** (decoupled from ARC 2).
- [ ] All unit tests in `packages/workflows/test/retention-prune.test.ts` pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — live wiring lands in the owner-gated §19.12 arming slice.** The driver + activity are built dormant and exercised by their unit tests over fakes now. Do NOT add `retentionPruneWorkflow` to the registered sandbox bundle (`apps/worker/src/temporal/workflows.ts`) or call `createSchedule` — that is the §19.12 output-workflow registration/scheduling crossing (matches the built-but-dormant posture of `periodReview`/`dailyBrief`/`deletionSaga`). State this explicitly at Step 7.5. The real activity binding in `buildActivities.ts` also defers to arming.

## Files expected to touch
**New:**
- `packages/workflows/src/workflows/retentionPrune.ts` — the pure driver + local state machine.
- `packages/workflows/src/activities/prunePolicy.ts` — the prune-decision activity (+ its ports, in-file like `periodReview` or in `ports/`).
- `packages/workflows/test/retention-prune.test.ts` — the driver decision table over fakes.

**Modified:**
- *(none in this slice — arming-time edits to `temporal/workflows.ts` + `composition/buildActivities.ts` are deferred to §19.12.)*

If the prune needs an operational-store port that doesn't exist yet (e.g. a `PrunablePayloadStore` interface), define it in `ports/` and inject a fake — **flag at Step 2.5** before GREEN.

## RED test outline (Step 2)
In `packages/workflows/test/retention-prune.test.ts`:
1. **`prune_selects_window_expired_assistant_payload`** — an assistant-held payload older than 30d is selected. Asserts: window predicate. Why: REQ-F-018 default window (§5).
2. **`prune_skips_externally_authoritative_link`** — a Granola/Calendar-linked record is never selected. Asserts: assistant-held-only. Why: §10.1 (kept as links, never pruned).
3. **`prune_skips_undispositioned_parked_source`** — a parked-but-undispositioned source is retained. Asserts: re-entry guard. Why: `SourceDispositionRow` re-entry constraint.
4. **`prune_markdown_removal_routes_through_kw_only`** — a removal calls the KnowledgeWriter tombstone port; no raw fs/markdown delete; no human-owned/derived section removed. Asserts: RET-3 + safety rule #1. Why: one-writer invariant.
5. **`prune_is_idempotent`** — re-running over already-pruned records is a no-op. Asserts: idempotence. Why: RET-1 done-when.
6. **`prune_missed_schedule_collapses_on_wake`** — >1 missed occurrence ⇒ one run, `collapsed` true; nothing-due parks with no write. Asserts: LIFE-2. Why: `collapsedNextRunFromClock` contract.
7. **`prune_failure_mints_existing_failureclass`** — a KW-removal fault ⇒ a §16 HealthItem with `write_through_failed`; a missed run ⇒ `missed_or_late_schedule`. Asserts: reuse existing members. Why: no-new-enum decoupling.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none. **No new FailureClass member.**
- **Orchestrator doc rows to write hot:** none new. Flag at Step 9 that 7.19 is now BUILT-dormant (I tick 7.19 `[x]` with a "(driver+activity dormant; live schedule = §19.12)" note and close Carry-forward #2 / re-open note).
- **§2.5-seam model touched?** No.

## Things to flag at Step 2.5
1. **Retention-window source + the raw-audio predicate.** My default vote: **injected config with a `DEFAULT_RETENTION_DAYS = 30` constant**; raw audio uses a distinct "delete after audited-synthesis" boolean predicate (a synthesis-complete marker), NOT the time window. Confirm the audited-synthesis signal source (an injected port field).
2. **Which stores does it prune (the thin-substrate reality)?** My default vote: an injected **`PrunablePayloadStore`** port abstracting the prunable set; the only real producer today is `SourceDispositionRow.sourceEnvelope.body`, so the concrete binding is thin/deferred — the driver stays substrate-agnostic. Flag that audio/OCR/cached stores are future producers.
3. **Dry-run vs delete + RET-3 routing.** My default vote: **real delete** of assistant-held operational-store copies (direct repo delete); **Markdown removal only via the KnowledgeWriter tombstone port** (reuse the `deletionSaga` pattern); idempotent + audit-logged; nothing-due parks with no write. No dry-run mode (keep it simple — the tests pin the selection logic).

## Dependencies + sequencing
- **Depends on:** 7.2/7.3/7.5 (workflow/activity patterns — landed), P4 KW (tombstone port — landed), P2 operational store (landed).
- **Blocks:** nothing in Wave 1. Live scheduling = owner-gated §19.12 (later).
- **Independent of ARC 2** (no FailureClass dependency).

## Estimated commit count
**1** — a single greenfield workflow (driver + activity + tests). Own commit (safety-sensitive RET-3/one-writer). security-reviewer=invariant at Step 8.

## Lessons-logged candidates anticipated
- **Convention candidate** — "a scheduled workflow = pure driver over injected ports (periodReview template) built dormant; live registration/scheduling is the §19.12 arming crossing, never bundled into the build slice."
- **Architecture-doc note candidate** — the re-entry guard: undispositioned parked sources are excluded from retention prune (a consumer-visible invariant).
- **Future TODO — operational** — the concrete `PrunablePayloadStore` bindings for audio/OCR/cached-payload stores land with those producers.

## How to invoke
1. Read end-to-end — Step-2.5 Q1/Q2/Q3 need answers before GREEN.
2. Run `/tdd retention_pruning_scheduled_workflow`.
3. Step 8 — run `security-reviewer` (invariant-gated: one-writer/RET-3).
4. Step 2.5 + Step 9 → `main-orchestrator-2` (coverage map at 2.5; the built-dormant status + the 7.19 tick at Step 9).
