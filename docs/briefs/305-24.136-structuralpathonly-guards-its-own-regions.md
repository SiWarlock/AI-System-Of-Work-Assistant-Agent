# /tdd brief — structuralpathonly_guards_its_own_regions

> ⭐ **`### 24.113` certified every DERIVED region name. This certifies the ones nobody derived.** ⛔ **Two rules in one gap, and the rule-1 half stops the sole writer from LOADING.**

## Feature

Give `structuralPathOnly` / `REGION_PATTERNS` a guard over its own region names, so a hand-added region fails loudly instead of silently returning the row-authored path — or throwing at module init.

## Use case + traceability

- **Task ID:** `### 24.136` · **Spec:** `ARCHITECTURE.md §6`, `ARCHITECTURE.md §16`
- ⛔ **widens phase scope because Phase 24's declared anchors contain `§6` but not `§16`** — `### 24.54`, declared not silenced; ⛔ do NOT widen the set.
- ⛔⛔ **Safety: rule 7 AND rule 1** ⇒ own commit; `security-reviewer` fires.
- **Related:** `### 24.113` (LANDED `106968f4` — creates this residual) · `### 24.119` (the measured fail-open this reaches by a different door) · `### 24.103`

**Your own measurements, executably reproduced this session:** regions `["@ext"]`, path `a.@ext.ROW_KEY` → ⛔ **returned VERBATIM, uncut** · `["ext$"]` → ⛔ **VERBATIM** · `["a|b"]` → mis-cut, alternation injected · ⛔⛔ `["pay(load"]` → **`SyntaxError` AT MODULE INIT**, and `REGION_PATTERNS` is a module-level `const` ⇒ **importing the module throws on `applyPlan`'s import path.**

⇒ **`\b` needs a word char on the region's inside edge, so a non-word edge makes the whole pattern fail and the `?? path` fallback returns the key verbatim.** ⚠ **Not reachable today — all four live regions are plain words. The trigger is an ordinary future property name like `@context` or `$meta`.**

## Acceptance criteria

- [ ] **A hand-added region with an unsafe name fails LOUDLY** — at construction, not by silently returning the path.
- [ ] ⛔ **THE MODULE-INIT THROW IS CONTAINED.** ⚠ **A sole-writer import path may not be able to throw on LOAD** — a bad region must produce an attributable, contained failure, not an import-time `SyntaxError`. **This is the rule-1 half and it is the reason this is not a `### 24.114` sibling.**
- [ ] **The guard cannot be unreachable in the case it exists for** — `### 24.113`'s own placement argument, applied here. ⛔ **State at the site WHY the ordering is what it is** (`L216`).
- [ ] **All four failure shapes are pinned:** non-word leading edge · non-word trailing edge · injected alternation · unbalanced group.
- [ ] **The four live regions still derive and cut exactly as today** — no behaviour change for valid input. ⭐ **`L239`: check the path the change exists to serve, not only the path it closes.**
- [ ] **`### 24.113`'s derivation guard and this one do not silently overlap** — say which covers which population, so neither is later deleted as redundant.
- [ ] `/preflight` for your own files; whose red is whose.

## Wiring / entry point (Step 7.5)

**`structuralPathOnly` → `buildRefusalSignal`, which you measured at 5 call sites in 5 files** (`writer.ts:660`, `router.ts:128`, `generative-proposal-intake.ts:129`+`:220`, `provenance-stamp.ts:248`, `visibility-gate.ts:81`) — ⛔ **four candidate-data channels PLUS the GCL gate, which is the correction to the brief that preceded it.** `applyPlan` is live at `buildActivities.ts:613`/`:1104`. ⛔ **Re-confirm by symbol, not from this brief.**

## Files expected to touch

**Modified:** `packages/knowledge/src/audit/validation-refusal.ts`
**Extended (test):** the four shape pins

## RED test outline (Step 2)

1. **`a_region_with_a_non_word_leading_edge_is_REJECTED_not_silently_uncut`**
2. **`a_region_with_a_non_word_trailing_edge_is_REJECTED`**
3. **`an_injected_alternation_is_REJECTED`**
4. **`an_unbalanced_group_fails_CONTAINED_not_at_module_init`** — ⛔ the rule-1 pin.
5. **`the_four_live_regions_are_unchanged`** — the served path.

⛔ **`L237`: the runner aborts at the first failing assertion, so a mutation over a multi-assertion block proves ONE assertion. Mutate per assertion, or split.** ⛔ **`L243`: branch on exit codes; never parse rendered `diff` output.** ⚠ **`L242` is aimed straight at you: a defect fixed in a structure recurs in the tooling built to audit it — you hit it once this round (`Object.hasOwn` in the walk). Re-read `### 24.103`'s and `### 24.119`'s fixes and ask whether each applies to THIS code.**

## Cross-doc invariant impact

- **Model field changes:** none. **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⛔ **WHERE DOES THE GUARD LIVE — at `REGION_PATTERNS` construction, or inside `structuralPathOnly`?** Default: **at construction**, so it cannot be bypassed by a caller. ⚠ **But construction is module-init, which is the rule-1 hazard — so the guard must not itself become the import-time throw.** **This tension is the design question; flag your resolution.**
2. **What is the safe-name predicate?** Default: **the same `/^[A-Za-z0-9_]+$/` you asserted in `### 24.113`**, so the two guards agree by construction rather than by coincidence. ⛔ **If they must differ, say why at both sites.**
3. **Does a rejected region degrade to `MAXIMAL_CUT` or refuse the whole operation?** Default: ⛔ **`MAXIMAL_CUT`** — the fail-safe direction, and it is what the module already does for an unknown schema id. **Flag if that conflicts with the rule-1 containment requirement.**

## Dependencies + sequencing

- **Depends on:** `### 24.113` (LANDED `106968f4` — ⛔ verify against git). **Blocks:** — . **Not in this slice:** `### 24.114` (per-ref length — your next).

## Estimated commit count

**1.** Rule 1+7 ⇒ its own commit. ⛔ `git commit -F <msgfile> -- <explicit paths>`; stage, read the staged set separately, commit. Never `--amend`.

## Lessons-logged candidates anticipated

- **Convention** — *a derivation guard certifies what it derives; the hand-written path needs its own.*
- **Convention** — *a module-level `const` that compiles user-influenced input makes every consumer's import a failure surface.*

## How to invoke

1. Read end-to-end. 2. `/tdd structuralpathonly_guards_its_own_regions`. 3. Step 2.5 answering Q1 and Q3. 4. Step 9 categorized.
