# /tdd brief — workspace_path_guard_factory

## Feature

Add `makeEnforceWorkspacePathScope(exemptWorkspaceId: string): WorkspacePathCheck` to the workspace-path guard — **its parameter required from birth** — and re-express the existing `enforceWorkspacePathScope` const as that factory applied to the module constant, so there is ONE implementation. **Nothing else changes.** This is **step 1 of 3** in a cross-track sequence; it deliberately removes no duplication yet.

## Use case + traceability

- **Task ID:** `24.26` (step 1 of 3)
- **Architecture sections it implements:** `ARCHITECTURE.md §16`; safety rule 4 (defense-in-depth for the workspace-path guard)
- **Related context:** `24.12` (`c86030f9`, the remedy that introduced this duplication) · contracts `L39` (reuse, don't re-implement) · `L87`/`L103` (one value one home; make the unsound state unrepresentable) · `L88` (accepted-duplication discipline) · worker `L81` (a widened interface's blast radius is the typecheck output, never a grep)

> ⛔ **This brief widens phase scope because** `### 24.26` declares `Spec: §16`, which is not in Phase 24's declared anchor set (`§19.11 · §6 · §13 · §4 · §3`). Declared, not resolved by editing that set (lead's 2026-07-29 ruling). ⚠ **Systemic — 23 Phase-24 tasks declare `§16` and 14 declare `§5`; the two most-cited anchors in the phase, neither declared. Routed to the lead as a Finding; do not resolve it here.**

## ⛔ THE SEQUENCE — read before anything else; this brief is ONE THIRD of a fix

**The lead authorized `24.26` as a cross-track pair and delegated the ordering. The ordering was derived against real code (not adapted from `24.44`, whose proof does not transfer — there is no async flip here). Result: THREE commits, forced.**

| step | track | what | state after |
|---|---|---|---|
| **1 — THIS BRIEF** | knowledge | add the factory (param required from birth); `enforceWorkspacePathScope` becomes `makeEnforceWorkspacePathScope(LEGACY_UNPREFIXED_WORKSPACE_ID)` | GREEN; **zero behavior change**; duplication still present |
| 2 | worker | both `KnowledgeWriterDeps` literals pass `workspacePathCheck: makeEnforceWorkspacePathScope(<value>)` | GREEN |
| 3 — **the make-it-required leg of `### 24.26`** | knowledge | `KnowledgeWriterDeps.workspacePathCheck` becomes REQUIRED; delete the `??` fallback and `LEGACY_UNPREFIXED_WORKSPACE_ID` | GREEN; duplication gone |

**Why not two commits** (each checked against real code): knowledge-first-and-required breaks both worker literals ⇒ RED, cross-territory. Worker-first passing the factory ⇒ `TS2305`, the export does not exist yet ⇒ RED. Worker-first passing the *existing* const ⇒ green today, but step 2 then deletes/retypes that symbol ⇒ RED.

⛔ **CITATION DEFECT CORRECTED 2026-08-13 — THIS BRIEF ORIGINALLY CITED STEP 3 AS "task `#38`", AND THAT WAS WRONG IN A DANGEROUS WAY.** `#NN` is a **harness task-list** id; `IMPLEMENTATION_PLAN.md` carries **its own independent `#NN` numbering**, and they COLLIDE — tracker `#38` is a **closed** worker `egressRevoke` residual (`:1372`, `64b682e5`). ⛔ **On the one comment whose job is to stop someone deleting `LEGACY_UNPREFIXED_WORKSPACE_ID` early, a traced-and-found-CLOSED id reads as *"step 3 already landed."*** **Caught by knowledge-implementer's reviewer after the id had been copied faithfully into in-code comments.** ⇒ ⭐ **CONVENTION, effective now: briefs and in-code comments cite tracker `### N.M` anchors and name legs by CONTENT — never a bare harness `#NN`.** *(`L97`'s shape — a bare identifier that resolves to something real and wrong is worse than one that resolves to nothing.)*
⛔ **STEP 3 — the make-it-required leg of `### 24.26` — IS TRACKED AND MUST NOT BE ABSORBED INTO THIS SLICE.** The lead's binding condition: a round that stops after step 2 **looks finished and is not** — the tree would carry a *third* copy of the value (guard constant + 2 worker call sites + desktop config), which is strictly worse than the start state for a task whose whole purpose is *one value, one home*. **Invisible to typecheck; no test reds.** ⭐ **The team died mid-round on a usage limit on 2026-08-12 — this is the scenario `#38` exists for, and it is evidence rather than hypothesis.**

## Premises — cited so they are cheap to falsify

**All verified by the orchestrator against the tree on 2026-08-13.** ⚠ **The tracker's own citations for this task were wrong in three ways before today; if anything below does not match what you read, that is a Step-2.5 finding.**

1. ⭐⭐ **THERE IS NO CONSTRUCTOR.** `enforceWorkspacePathScope` (`workspace-path-guard.ts:86`) is `export const enforceWorkspacePathScope: WorkspacePathCheck = (ctx: WorkspacePathContext) => {…}` — **a plain function VALUE**, reading the module constant at `:102`. ⛔ **The task text and the lead's own ruling both said "supply a required constructor dependency"; there is nothing to supply an argument to.** The factory must be created before anything can be threaded — which is exactly why this step exists.
2. **The constant is at `:64`**, not `:41` (`:41` is an `import` line). Tracker corrected 2026-08-13.
3. **It is deliberately a plain `string`, not the branded `WorkspaceId`** — `:47-48` states this: *"A plain string, not the branded `WorkspaceId`, matching `KnowledgeMutationPlan.workspaceId`'s own (structurally-required, non-branded) contract type."* **Keep it that way; the factory's parameter is `string`.**
4. **The seam already exists and is OPTIONAL.** `writer.ts:157` `readonly workspacePathCheck?: WorkspacePathCheck;` and `:225` `const workspaceScope = deps.workspacePathCheck ?? enforceWorkspacePathScope;`. ⛔ **This slice does NOT touch either line** — that is step 3's job.
5. **The consumers of the existing const, today:** `writer.ts:65` (import) + `:225` (the fallback), and `packages/knowledge/test/workspace-path-guard.test.ts` (`:13` import, `:207/213/218/231` direct calls). ⚠ **`workspace-path-guard.test.ts:34` deliberately OMITS `workspacePathCheck`** so its tests exercise the real default — that file is the one knowledge-side consumer step 3 will break.
6. **`SOURCE_NOTE_SUBTREE` (`:73`) is a SECOND duplicated constant in the same file** with the same drift-direction note. ⛔ **Out of scope — `24.26` names only the workspace id.** Do not opportunistically fold it in; if you think it should ride along, raise it at Step 2.5 (my vote: no — it duplicates `apps/worker`, a different owner, and would widen this into a second pair).

## ⛔ FORWARD OBLIGATION — `L61`'s enumeration rule, lead-mandated, recorded here so it cannot be lost between briefs

**THE CONSUMER SET FOR STEP 3 IS UNENUMERATED. This brief does not know how many call sites making `workspacePathCheck` REQUIRED will break, and does not claim to.**

- What IS established: 2 worker deps literals (`buildActivities.ts:575`, `semanticApprovalDispatch.ts:61`) feeding 3 `createCommitActivity` sites (`buildActivities.ts:584`, `:1075`, `semanticApprovalDispatch.ts:77` — `:1075` reuses the `:575` literal), plus `workspace-path-guard.test.ts:34` knowledge-side.
- ⛔ **What is NOT established: whether that is the closed set.** Worker `L81`: a widened interface's blast radius lives in the **typecheck output**, never in a grep of the type's name — *"a structurally-typed language lets a conforming value satisfy an interface with zero mention of it."*
- ⭐ **`L81` already landed on this derivation, live:** the `KnowledgeWriterDeps` **type-name** grep returned 2 sites; a **value-axis** search on `createCommitActivity` returned 3. The literal count was **accidentally** right, and that is only knowable because the second axis was run.
- **THE COMMAND THAT SETTLES IT:** make `writer.ts:157`'s `workspacePathCheck` non-optional, run the repo-wide typecheck, read the full error list, revert byte-identical (`L139`'s declared two-command window).
- **Scheduling — the lead's call, not negotiable here:** run it at a **quiet tree**, preferred window **after `24.48` and `24.45` land and before the worker leg dispatches**. If the tree is not quiet, the worker runs it **inside its own slice, first, and reports the size BEFORE implementing** — never after. ⛔ **The number is established by typecheck, never by grep.**
- **Why this paragraph exists:** `24.23` went 1→4 consumers, `24.37` 3→14 sites, `24.49` 3→9. **An unenumerated brief is not wrong; it is UNSCOPED — and the two need different handling.** Nobody should discover this slice's true size mid-implementation and silently absorb it into a scope that never claimed it.

## Acceptance criteria (what "done" means)

- [ ] `makeEnforceWorkspacePathScope(exemptWorkspaceId: string): WorkspacePathCheck` is exported, **its parameter required** — a call omitting it fails `tsc`.
- [ ] `enforceWorkspacePathScope` is re-expressed as `makeEnforceWorkspacePathScope(LEGACY_UNPREFIXED_WORKSPACE_ID)` — **ONE implementation, not two** (`L39`).
- [ ] ⛔ **ZERO BEHAVIOR CHANGE, demonstrated not asserted:** every existing test in `packages/knowledge/test/workspace-path-guard.test.ts` passes **unmodified**. If any test needs editing, that is a behavior change — **STOP and flag at Step 2.5.**
- [ ] A test drives the factory with an exempt id **different from** `"personal-business"` and proves the exemption follows the argument, not the constant.
- [ ] `writer.ts` is **untouched** (`:157` and `:225` unchanged) — this slice does not make the dep required.
- [ ] `LEGACY_UNPREFIXED_WORKSPACE_ID` still exists and is still exported — **its deletion is step 3's**, and deleting it here would break `writer.ts:225`.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)

**`none — the new factory's production consumer lands in step 2 (worker: `buildActivities.ts:575` + `semanticApprovalDispatch.ts:61`), and the dependency becomes required in step 3 (task `#38`).**

⭐ **Both named slices are real tracked obligations, which is what this answer requires** (`L106` — the template's own condition on a "none" answer): step 2 is the worker leg of `24.26`, step 3 is task `#38`. **The existing `enforceWorkspacePathScope` const remains reached from `writer.ts:225` throughout, so the guard's live behavior is continuously covered — this slice adds an unreached export, it does not leave a guarantee unenforced.**

⚠ **SCOPED CLAIM for the commit message — write this form, not the unqualified one:** *"the exempt workspace id is now suppliable as a required factory argument; NOTHING supplies it yet and the module constant is still the only source — see steps 2 and 3."* ⛔ **Never "the guard now takes its exempt workspace from the composition root."** That is false until step 3 and it is what gets quoted.

## Files expected to touch

**Modified:**
- `packages/knowledge/src/knowledge-writer/workspace-path-guard.ts` — add the factory; re-express the const in terms of it; update the `:50-63` doc comment, whose text currently says the composition-root fix *"is deliberately NOT this slice"* (it is now step 1 of exactly that fix)
- `packages/knowledge/test/workspace-path-guard.test.ts` — the new RED test

⛔ **Do NOT touch:** `writer.ts` (step 3) · `apps/worker/**` (step 2, worker territory) · `apps/desktop/worker-host/index.ts` (read-only reference at `:172`, comment-only).

## RED test outline (Step 2)

Tests in `packages/knowledge/test/workspace-path-guard.test.ts`:

1. **`factory_exempts_the_workspace_it_was_given_not_the_module_constant`** — build via `makeEnforceWorkspacePathScope("employer-work")`; drive an unprefixed path for plan workspace `"employer-work"`.
   - Asserts: `ok` — the supplied id is exempt
   - Why: the whole point of the factory; **this is the test that would fail if the factory ignored its argument and closed over the constant**

2. **`factory_does_not_exempt_the_module_constant_when_given_another_id`** — same factory instance; drive an unprefixed path for plan workspace `"personal-business"`.
   - Asserts: `err(workspace_path_violation)` — the *old* exempt workspace is NOT exempt on this instance
   - Why: ⭐ **the discriminating half.** Test 1 alone passes even if the factory exempts *both* the argument and the constant; only this one proves the constant stopped being consulted. **A closure bug that ORs the two is the realistic failure mode and test 1 cannot see it.**

3. **`the_shipped_const_behaves_byte_identically`** — the existing suite, unmodified, is this test. **No new test needed; the acceptance criterion is that nothing in the file changes.**
   - Why: `L139`-style — the proof of zero behavior change is that the existing pins still hold without edits

## Cross-doc invariant impact

- **Model field changes:** none. `WorkspacePathCheck` / `WorkspacePathContext` / `WorkspacePathViolation` are **unchanged** — the factory returns the existing `WorkspacePathCheck` type.
- **Orchestrator doc rows to write hot (Step 9 routing):** none anticipated for step 1. ⚠ **Step 3 will need one** (`KnowledgeWriterDeps.workspacePathCheck` optional→required is a dep-surface change); flag it then, not now.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No. `WorkspacePathCheck` is knowledge-local (`./writer`), not an Appendix-A model.

## Things to flag at Step 2.5

1. **Should `enforceWorkspacePathScope` be re-expressed via the factory, or kept as an independent implementation?** My default vote: **re-expressed — `export const enforceWorkspacePathScope = makeEnforceWorkspacePathScope(LEGACY_UNPREFIXED_WORKSPACE_ID);`**. Rationale: `L39`, one implementation; two copies of the predicate is the same defect `24.26` exists to fix, arriving through its own remedy (this round's most-repeated shape). ⛔ **If you disagree, say why before Step 3** — the alternative doubles the surface step 3 must delete.
2. **Does the factory need to guard its own argument?** Options: (a) accept any `string`; (b) reject empty/whitespace at construction; (c) reject anything failing the same segment-shape rules paths are checked against. **My default vote: (b), minimal** — an empty exempt id would make `workspaceId === ""` exempt, and `:101` already fails closed on an empty plan workspaceId, so an empty *exempt* id is the mirror hole. ⚠ **But this is a real design question and I hold it loosely** — (a) is defensible on "step 3 supplies it from the composition root, which is trusted," and (c) may over-constrain a legitimate id.
3. **The `:50-63` doc comment currently ends by saying the composition-root fix "is deliberately NOT this slice… it needs a worker-side wiring leg."** That sentence becomes stale the moment this lands. **My default vote: rewrite it to name the three-step sequence and point at task `#38`**, so the next reader of this file learns the fix is in flight rather than deferred (`L94` — the person who would undo this reads the file, not the tracker). ⚠ **`L145`-safe: state what is STRUCTURALLY true (a factory exists, nothing supplies it yet), never that the path is live.**
4. **`SOURCE_NOTE_SUBTREE` (premise 6) — fold in or leave?** My default vote: **leave.** It duplicates an `apps/worker` constant, so closing it is a different pair with a different owner. Raise it if you disagree; do not silently include it.

## Dependencies + sequencing

- **Depends on:** `24.12` ✅ `c86030f9`
- **Blocks:** the `### 24.26` worker leg (step 2) → the `### 24.26` make-it-required leg (step 3)
- ⛔ **Not gated by `24.50`/`24.52`** — those gate drain/port wiring; this slice wires nothing.

## Estimated commit count

**1.** One file plus its test. ⛔ **Do not bundle** — this is one leg of a three-commit safety sequence and its bisectability is the point.

## Lessons-logged candidates anticipated

- **Convention candidate** — *"supply it as a required constructor dependency" presumes a constructor; when the target is a module-level function VALUE, the first step is creating the factory, and that step is invisible in the task text.* Three parties (task author, lead, orchestrator) all wrote "constructor" about a `const` arrow function.
- **Convention candidate** — *a parallel-add of a required-from-birth API is strictly safer than expand/migrate/contract with an optional param*: the intermediate window never weakens the new surface, only delays adoption of it.
- **Architecture-doc note candidate** — none expected for step 1.

## How to invoke

1. **Read this brief end-to-end**, especially the SEQUENCE table and the FORWARD OBLIGATION — this slice is deliberately incomplete and the commit message must say so.
2. **Run `/tdd workspace_path_guard_factory`.**
3. **Step 2.5** — Q1 and Q2 both matter; Q2 is a genuine design call I do not want made silently.
4. **Step 9** — if you touched anything in `writer.ts` or needed to edit an existing test, say so prominently; either means the zero-behavior-change claim failed.
