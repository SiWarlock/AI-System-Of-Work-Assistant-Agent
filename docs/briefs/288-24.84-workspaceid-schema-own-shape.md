# /tdd brief — workspaceid_schema_own_shape

## Feature

`### 24.84`'s **contracts-first leg — the class fix.** Give `WorkspaceIdSchema` its **own** schema (a bounded
positive shape) instead of the shape-free generic `brandedIdSchema<WorkspaceId>()`, so that a credential-shaped
workspace id becomes **UNREPRESENTABLE rather than DETECTED**, at **one site**, for every model that validates
through the brand — **including models that do not exist yet** (`L103`).

## Use case + traceability

- **Task ID:** `### 24.84` (contracts-first leg). ⚠ **`#72`/`#73` are DEAD pointers; per `### 24.66` they do
  NOT map to `### 24.72`/`### 24.73`. Cite the `###` entry only.**
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- **Safety:** ⚠ **rule 7 — secrets / raw content in a durable sink.** Step-9 flags route to me AND the lead.
- **Depends:** `### 24.83` measurement (DISCHARGED) · **Blocks:** `### 24.84`'s worker leg

> ⚠ **This brief widens phase scope because** `§5` and `§16` are not in Phase 24's declared anchor set
> (`§13 §19.11 §3 §4 §6`) — **and `### 24.84` itself declares `Spec: §5, §16`, so task and phase disagree.**
> ⛔ **NOT resolved by adding them** — the resolution the lead ruled against on `### 24.54`.

## ⭐ WHY YOU EXIST, STATED PLAINLY

This task was **DEFERRED 2026-08-14 on a lead ruling**, and the reason was **staffing, not design**:
> *"This task's own analysis says the CHEAPER path is contracts-first… and the `contract` track is deliberately
> UNQUEUED. Dispatching to worker now would buy the EXPENSIVE path BY DEFAULT and then the tracker would record
> it as a considered choice."* — **`L120`: a fact about our STAFFING is not a fact about what is CORRECT.**
> The lead's words: ***"I am not letting staffing pick the design."***
⇒ **The owner staffed this area to unpark exactly this.** You are the cheaper path being taken deliberately.

## ⭐ PREMISES RE-DERIVED AT HEAD `3b74e497` — you inherit measurements, not claims

Verified by me at source before writing this (redirect-then-measure; **discriminating control: a bogus token
returned 0 rows**):

| fact | evidence |
|---|---|
| `brandedIdSchema` is a shape-free generic factory | `packages/contracts/src/primitives/zod-brands.ts:30` — `<B extends string>(): z.ZodType<B, z.ZodTypeDef, string>` |
| `WorkspaceIdSchema` is just that factory instantiated | `zod-brands.ts:38` — `brandedIdSchema<WorkspaceId>()` |
| **Tightening the FACTORY would silently re-shape every other brand** | `zod-brands.ts:39-46+` — `AgentJobIdSchema`, `ActionIdSchema`, `PlanIdSchema`, `SourceIdSchema`, `ApprovalIdSchema`, `WorkflowIdSchema`, `AuditIdSchema`, `ProcessorIdSchema` all share it |

⛔⛔ **THAT LAST ROW IS THE BINDING CONSTRAINT AND IT IS MEASURED, NOT INHERITED: give `WorkspaceIdSchema` its
OWN schema. DO NOT TIGHTEN `brandedIdSchema`.** Tightening the factory silently re-shapes at least eight other
id brands — a frozen-contract change to models this slice has no business touching.

## ⛔ PART 1 — TWO MEASUREMENTS BEFORE ANY IMPLEMENTATION. BOTH BINDING.

### (1a) THE AVAILABILITY GATE — this is the lead-adopted binding condition, in worker's own words

> ***"MEASURE WHETHER A SLUG ALLOWLIST WOULD REJECT ANY ID IN USE TODAY. Rejecting a live id is an AVAILABILITY
> BREAK, not a hardening."*** **Report it with its method.**

⛔ **A shape that rejects a workspace id someone is actually using is not a security improvement, it is an
outage.** Establish the live population — fixtures, seeds, migrations, dev/default workspaces, test corpora, and
any id literal shipped in the repo — and show your proposed shape accepts all of them. **State the boundary of
what you searched** (`L100`); a population you did not examine is recorded as unexamined, never as empty.

### (1b) THE CENSUS DELTA — I measured this and it disagrees with the tracker. Resolve it.

`### 24.84` states **17 frozen contract models carry `workspaceId`** and **15 of the 17 validate through the
brand**, leaving **2 that skip it, UNIDENTIFIED**. My own measurement at HEAD:

```sh
grep -rln "workspaceId" --include="*.ts" packages/contracts/src/models              # 17 model files
grep -rln "workspaceId: WorkspaceIdSchema" --include="*.ts" packages/contracts/src/models   # 14 files
```
⇒ **17 total, 14 via the brand by SPELLING ⇒ 3 skip, not 2.**

⛔⛔ **DO NOT TAKE MY 14. IT IS A SPELLING COUNT AND I AM HANDING YOU ITS DEFECT ALONG WITH IT.** A model that
writes `workspaceId:` and `WorkspaceIdSchema` on separate lines, or wraps it (`.optional()`, `.nullable()`, a
composed object), may validate through the brand and **not match my pattern**. That is **measurement mechanism 5
— grouping by SPELLING, not by TYPE** — the exact error `### 24.85` was re-scoped to undo.
⇒ ⭐ **Derive the set by TYPE, and per `L179` the DELTA between the tracker's 15 and my 14 is itself the finding:
report which number is right, which models actually skip the brand, and what the discrepancy was.**
⚠ **Identifying the skippers is a deliverable, not a footnote** — they are the models the class fix does NOT
reach, and an unidentified gap in a fix that reads as complete-by-construction is `L82`'s completion badge.

## PART 2 — THE SHAPE

⛔⛔ **"CREDENTIAL-SHAPED" IS THE WRONG QUESTION AND IS WITHDRAWN. DO NOT TRY TO ANSWER IT.** The orchestrator
who filed it was corrected, and the correction is the design:
> ***Do not ask "is this a credential?" — ask "is this a well-formed workspace id?"***
- The first is `worker L73`'s **structurally unwinnable denylist** — `isRedactionSafe`'s own doc concedes an
  employer codename, a person's name, or an internal identifier all pass. ⛔ **Importing that heuristic would
  inherit exactly the over-claim `### 24.62` was corrected for** (`L151`: that gate's NAME is an ambition and
  its TEST is a heuristic).
- The second is a **bounded positive charset** that makes every credential shape **unrepresentable** rather than
  detected. `L73`'s inversion, on a new surface.
⛔ **The redaction-at-sink alternative is also withdrawn**, on worker's reason: *"redaction-at-sink would be 17
sinks' worth of work to avoid defining one string's shape."*

⭐ **IF THE HONEST ANSWER IS THAT NO DEFENSIBLE BOUNDED SHAPE EXISTS, THAT IS A FINDING AND YOU SHOULD SAY SO** —
it re-derives the remedy rather than shipping a check that reads as coverage it cannot provide (`L82`). I would
much rather hear that at Step 2.5 than review a charset nobody can defend.

## Acceptance criteria

- [ ] **(1a) The availability measurement is reported BEFORE implementation** — the live-id population, the
      method, the search boundary, and proof the proposed shape rejects none of them.
- [ ] **(1b) The census delta is resolved** — the by-TYPE count, which models skip the brand, and an account of
      why the tracker said 15 and a spelling count said 14.
- [ ] `WorkspaceIdSchema` has its **own** schema with a **stated, defensible** shape — or its impossibility is
      stated and the remedy re-derived.
- [ ] ⛔ **`brandedIdSchema` is UNCHANGED**, and the slice says so explicitly. No other id brand's accepted set
      moves. Pin this — a test that a sibling brand still accepts a value the new workspace shape rejects.
- [ ] An id failing the shape is **rejected by the schema** with a typed failure; a well-formed id passes.
- [ ] **Schema-snapshot test** for the changed model surface (see cross-doc below) — field-name set vs the
      checked-in snapshot, tagged with the model's spec section.
- [ ] ⛔ **No coverage is claimed beyond what this leg provides.** This leg defines the shape. It does **NOT**
      stop the create path bypassing it — that is the worker leg. **The commit message must say so** (the
      unqualified *"workspace ids are now validated"* is false the moment it leaves this package).
- [ ] `/preflight` clean; **`turbo typecheck lint --force`** with `0 cached` reported. ⚠ A cached typecheck is
      not a typecheck.

## Wiring / entry point (Step 7.5)

`WorkspaceIdSchema` is already consumed by the models listed in Part 1b — this leg **tightens an existing,
already-reachable validator**. No new export, no new surface.
⛔ **The create path (`parseCreateWorkspace`) does NOT route through this schema today and this slice does not
change that** — wiring lands in **`### 24.84`'s worker leg**, which is a real tracked task (the entry exists;
its worker half is what remains after this lands). ⇒ **the scoped claim is *"the shape is DEFINED at the brand;
the create path still bypasses it, see `### 24.84` worker leg"*** — never the unqualified form.

## Files expected to touch

**Modified:** `packages/contracts/src/primitives/zod-brands.ts` (`WorkspaceIdSchema` only — **not**
`brandedIdSchema`) · its test file · the schema snapshot if the snapshot surface moves.
⛔ **Not touched:** `apps/worker/**` (worker leg) · any other brand's schema.

## RED test outline (Step 2)

1. **`workspace_id_schema_rejects_out_of_shape_value`** — Asserts: a value outside the defined shape fails.
   Why: the class fix's whole point; §5/§16.
2. **`workspace_id_schema_accepts_every_live_id`** — Asserts: each id from Part 1a's measured live population
   parses. ⛔ **Why: this is the availability pin, and it is the one that stops a hardening becoming an outage.**
3. **`sibling_brands_are_unchanged`** — Asserts: `AgentJobIdSchema` (and at least one more) still accepts a
   value the new workspace shape rejects. Why: proves the factory was not tightened — **the constraint measured
   at `zod-brands.ts:39-46`.**
4. **Schema-snapshot test** — field-name set vs checked-in snapshot for the touched seam model.
5. **A discriminating control** — replace the new shape with an accept-everything constant and confirm test 1
   goes GREEN (i.e. the suite can actually tell the difference). ⛔ Without it, a shape that matches nothing and
   a shape that works are indistinguishable (`L80`).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)

- **Model field changes:** none — the field's *shape* narrows, its name does not.
- ⛔ **This IS a frozen-contract change.** **Orchestrator doc rows I will write hot at Step 9:** the
  `packages/contracts/CLAUDE.md` cross-doc row + the `ARCHITECTURE.md` Appendix-A row for the narrowed
  `WorkspaceId` shape. **Do not edit those files yourself** — flag at Step 9 and I write them.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> **YES** — a brand consumed across
  subsystem-dependency-DAG edges ⇒ **the schema-snapshot test is mandatory in this same `/tdd` cycle.**

## Things to flag at Step 2.5

1. ⛔⛔ **Part 1a (availability) and Part 1b (census delta). This is the review surface and I will not approve
   without both.** A shape without its live-population proof is an outage waiting for a deploy.
2. **What exactly is the shape?** My default vote: **a bounded positive charset** (lowercase alphanumeric plus a
   separator, with a length bound) — but **you have measured the live ids and I have not**, so the population
   decides it, not my preference. If the live set contains something my default would reject, the default is
   wrong and I want to hear that.
3. **Does narrowing the brand break any EXISTING test fixture or seed?** My default vote: **treat a broken
   fixture as signal, not friction** — it is the availability gate firing inside the suite. Report it; do not
   quietly widen the shape to make a fixture pass.
4. **Are the models that skip the brand skipping it deliberately?** My default vote: **find out before assuming
   it is an oversight** — one of them may have a reason, and that reason bounds the class fix's completeness claim.

## Dependencies + sequencing

- **Depends on:** `### 24.83` measurement (discharged). No live blockers.
- **Blocks:** `### 24.84`'s worker leg (routing the create path through the brand). ⛔ **PRODUCER-FIRST — the
  worker leg is NOT dispatched and will not be until this lands.** Do not reach into `apps/worker`.

## Estimated commit count

**1.** Frozen-contract change with a safety (rule-7) subject ⇒ **its own commit, not bundled.**

## Lessons-logged candidates anticipated

- **Convention candidate** — a shape-free generic brand factory is a *type* guarantee that reads as a *value*
  guarantee; per-brand shapes are where value constraints belong.
- **Convention candidate** — a hardening at a write boundary owes an availability measurement against the live
  population before it ships.
- **Architecture-doc note candidate** — Appendix-A row for the narrowed `WorkspaceId` shape.

## How to invoke

1. Read this brief end-to-end. ⛔ **Part 1a and 1b before any implementation, and both come to me at Step 2.5.**
2. Run `/tdd workspaceid_schema_own_shape`.
3. **Step 9** — rule-7 subject ⇒ routes to me AND `team-lead`. Flag the cross-doc rows; I write them.
