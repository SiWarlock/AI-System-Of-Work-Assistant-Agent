# /tdd brief — typed_task_model_and_failureclass_operational_members

## Feature
The ARC-2 **frozen-contract batch, done in ONE schema-snapshot + repo-wide-typecheck round**: (a) a new typed `Task` Appendix-A model carrying an optional `priority` (never inferred — absent ⇒ unset, REQ-F-017) + an optional `projectId` binding, with a dual-dialect `TaskRepository`; (b) four new **operational** `FailureClass` members — `db_unavailable`, `provider_routing_unavailable`, `outbox_blocked`, `write_through_blocked` — each wired into `defaultSeverityForFailureClass` + the HealthItem generated schema. (13.5 typed Project is already frozen — verify-only, no change.)

> **Widens phase scope because** the FailureClass/HealthItem members implement §16 (cross-phase) and are folded into this Phase-13 contract round per the owner-approved ARC-2 batching (`docs/planning/remaining-build-order.md` — one snapshot round, not two frozen rounds).

## Use case + traceability
- **Task ID:** 13.15
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (Task/priority gap named explicitly at the §6 "no typed Task model … no priority field" passage) · `§2.5` (contracts/domain seam) · `§16` (OBS-2 HealthItem `failureClass`) · `§3` (shared contracts) · `§9` (workflow KW targets). Appendix A model inventory.
- **Related context:**
  - **13.5 is already frozen (verify-only):** typed `Project` model + 7th state machine + Appendix-A row + `project.snap` all landed (`517659c`/`1ddbf10`). `packages/contracts/src/models/project.ts` is the **exact template** for `task.ts` (interface + `z.ZodType<Task,ZodTypeDef,TaskInput>` annotation to dodge TS4023 on branded ids — contract Lesson 1). Do NOT touch Project.
  - **§DEC-CAT4 is MOOT / stale:** the FailureClass *security* members (`security_violation`, `policy_denial`, `egress_denied`, `isolation_breach`) ALREADY EXIST (`shared-enums.ts:124-127`, the 2026-07-11 C-enum block). This batch adds only the four **operational** members. (Orchestrator reconciles the stale §DEC-CAT4 "PENDING" plan text at round close — not your edit.)
  - **The single tsc-breaker:** `packages/workflows/src/activities/healthItem.ts` `defaultSeverityForFailureClass` is an exhaustive `switch` with a `const _exhaustive: never` default — adding members breaks compile until each gets a `case` arm. Every other FailureClass site (`route-failure.ts` switches a *different* taxonomy `FailureVariantKind`; `redaction-rules.ts` derives a Set; the `failureClassFor(...)` mappers merely *return* the enum) is a non-breaking pass-through (contract Lesson 18 — survey the ripple empirically; add ONE assertNever-guarded decision point).
  - **13.16** (downstream, NOT this slice) consumes typed Task + priority via a `task_rollup` read-model producer.

## Acceptance criteria (what "done" means)
- [ ] `packages/contracts/src/models/task.ts` exports a frozen `TaskSchema` + `interface Task` with `TASK_SCHEMA_ID = "sow:task"`, `.strict()`, fields per Step-2.5 Q1 (default: `id: TaskId, workspaceId: WorkspaceId, projectId?: ProjectId, title, status (TaskLifecycle), priority?: Priority, dueDate?: ISO-datetime, provenanceOrigin`).
- [ ] `priority` is **optional** and **never inferred**: an input with no priority validates with `priority` absent (the unset/TBD state) — a test pins that absent stays absent, never defaulted/guessed (REQ-F-017).
- [ ] `TaskId` brand added to `packages/contracts/src/primitives/zod-brands.ts` (mirror `ProjectId`); `Priority` closed enum added to `shared-enums.ts` (default vocabulary `p0|p1|p2|p3` — Q2).
- [ ] `packages/db/src/repositories/interfaces.ts` declares `TaskRepository` (dual-dialect, mirror `ProjectRegistryRepository`) and it passes the **repository contract suite on BOTH SQLite and Postgres**.
- [ ] `FailureClass` gains `db_unavailable`, `provider_routing_unavailable`, `outbox_blocked`, `write_through_blocked` (`shared-enums.ts`), each **semantically distinct** (Step-2.5 Q5 — `write_through_blocked` = a precondition/gate holds the write, vs existing `write_through_failed` = the write attempt errored; `outbox_blocked` = the external-write outbox is gated).
- [ ] `defaultSeverityForFailureClass` (`healthItem.ts`) has a `case` arm for each of the four new members with an assigned severity (Q4); the `never` exhaustive default still compiles.
- [ ] `health-item.schema.json` regenerated (the ONLY generated schema embedding the enum values); `health-item.test.ts`'s `for (const fc of FailureClass)` auto-covers the new members (no test edit).
- [ ] **Schema-snapshot tests (frozen-seam):** `task.snap` (top-level field-name set) + `task.schema.json` generated + a `test/models/task.test.ts` freezing fieldSet + generated schema; `registry-all.test.ts` stays green (auto-discovers `TASK_SCHEMA_ID` via the barrel).
- [ ] Repo-wide `turbo typecheck` green (a frozen-enum + new-model change ⇒ repo-wide typecheck — contract LESSON).
- [ ] `/preflight` clean.
- [ ] Cross-doc invariant rows flagged at Step 9 (orchestrator writes Appendix-A Task row + HealthItem member add + `packages/contracts/CLAUDE.md` cross-doc rows + the model-count note hot).

## Wiring / entry point (Step 7.5)
Two live entry points land in THIS slice; the richest consumer is a later slice:
- **FailureClass members are reachable immediately:** `defaultSeverityForFailureClass` (a real production function on the §16 health path) resolves a severity for each new member, and `health-item.schema.json` accepts them — no dormant enum values. Optionally (Q6) retype `broker.ts`'s `NO_ELIGIBLE_PROVIDER_HEALTH_CLASS` (already emits the literal `"provider_routing_unavailable"`) to the enum type — zero behavior change, closes an arch_gap.
- **`TaskRepository` is exercised by the dual-dialect repository contract suite** (its production driver wiring — create-vs-patch + the task-rollup producer — lands in **13.16**, not here). `Task` model + `priority` are consumed by 13.16's `task_rollup` read-model. State this explicitly: *production driver wiring lands in 13.16; this slice freezes the contract + repo surface.*

## Files expected to touch
**New:**
- `packages/contracts/src/models/task.ts` — `TaskSchema` + `interface Task` + `TASK_SCHEMA_ID`.
- `packages/contracts/schemas/task.schema.json` — generated (via `UPDATE_SNAP=1`), never hand-written.
- `packages/contracts/src/models/__snapshots__/task.snap` — frozen top-level field-name set.
- `packages/contracts/test/models/task.test.ts` — freezes fieldSet + generated schema + behavior (priority-absent-stays-unset).

**Modified:**
- `packages/contracts/src/models/shared-enums.ts` — `FailureClass` +4 members (append to the operational cluster with a dated comment block, mirroring the 2026-07-11 block); NEW `Priority` enum.
- `packages/contracts/src/primitives/zod-brands.ts` — `TaskId` brand (mirror `ProjectId`).
- `packages/contracts/src/index.ts` — barrel export `TASK_SCHEMA_ID` + Task types (so `registry-all.test.ts` auto-registers).
- `packages/contracts/schemas/health-item.schema.json` — regenerate (embeds the enum values).
- `packages/db/src/repositories/interfaces.ts` — `TaskRepository` (dual-dialect).
- `packages/domain/` — Task validators / `TaskLifecycle` state helper if a state machine is warranted (Q1 — default: a simple status enum, NOT a full 8th state machine unless a transition invariant emerges).
- `packages/workflows/src/activities/healthItem.ts` — 4 new `case` arms in `defaultSeverityForFailureClass` (**the tsc-breaker**).
- *(optional, Q6)* `packages/providers/src/broker/broker.ts` — retype the already-emitted literal.

If implementation needs files beyond this list (e.g. a db migration for a real Task table), **flag at Step 2.5** before GREEN.

## RED test outline (Step 2)
In `packages/contracts/test/models/task.test.ts`:
1. **`task_schema_freezes_field_set`** — `fieldSet(emitJsonSchema(TaskSchema, TASK_SCHEMA_ID)) === loadFieldSnapshot("task")`. Asserts the frozen seam. Why: §2.5 shared-contract snapshot discipline (Task is a NEW Appendix-A seam model).
2. **`task_generated_schema_frozen`** — `freezeGenerated(task.schema.json URL, emitJsonSchema(...))` round-trips. Why: Appendix-A four-file-set discipline.
3. **`task_priority_absent_stays_unset`** — parse an input with no `priority` ⇒ result has `priority` undefined (never defaulted/guessed). Why: REQ-F-017 no-inference.
4. **`task_priority_rejects_out_of_vocabulary`** — a non-enum priority string ⇒ Zod error. Why: closed-vocabulary validation (candidate-data gate).
5. **`task_projectid_optional`** — a task with no `projectId` validates; with a valid branded `ProjectId` validates. Why: additive/optional cross-entity ref (contract Lesson 15 — no required cross-entity gate).

In `packages/contracts/test/models/health-item.test.ts` (existing — verify auto-coverage) + a `defaultSeverityForFailureClass` test (in `packages/workflows/test/`):
6. **`default_severity_covers_new_members`** — each of the four new members resolves to its assigned severity; the function is total (no throw / no `never` fallthrough). Why: §16 "distinct severity per failure class" + the assertNever decision point (Lesson 18).

In the db repository contract suite:
7. **`task_repository_roundtrip_both_dialects`** — put→get a Task on SQLite AND Postgres; equality preserved. Why: 13.15 done-when (dual-dialect repo-contract).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** NEW `Task` Appendix-A model; `FailureClass` +4 members; NEW `Priority` enum; `TaskId` brand.
- **§2.5-seam (shared-contract) model touched?** YES — `Task` is a new Appendix-A model on the contracts seam ⇒ the schema-snapshot test (tests 1–2) is IN this cycle (above).
- **Orchestrator doc rows to write hot (Step 9 routing):**
  - `ARCHITECTURE.md` Appendix A — ADD a `Task` row (`§3, §6, §9, §13.15` + the frozen field list); ADD `db_unavailable`/`provider_routing_unavailable`/`outbox_blocked`/`write_through_blocked` to the HealthItem row's inline `failureClass ∈ {…}` set (+ their default severities).
  - `packages/contracts/CLAUDE.md` cross-doc table — ADD a `Task` row; add the 4 members to the HealthItem row; bump the frozen-model count note.
  - Reconcile the stale §DEC-CAT4 "PENDING" text (security members already at `shared-enums.ts:124-127`).
- **Territory:** you write ONLY code/test/snapshot files under `packages/contracts`, `packages/domain`, `packages/db`, and the `healthItem.ts` tsc-fix (+ optional `broker.ts`). `git add <path>` per file, never `-A`. Appendix-A / CLAUDE.md / plan edits are the orchestrator's.

## Things to flag at Step 2.5
1. **Task field set.** Mirror Project's frozen shape. My default vote: **`id, workspaceId, projectId?, title, status (TaskLifecycle), priority?, dueDate?, provenanceOrigin`** — SEMANTIC/Markdown-canonical like Project, no operational-only fields. Confirm whether `status` warrants a full domain state machine (default: **no** — a closed status enum suffices unless a transition invariant is named; a state machine is a bigger frozen commitment).
2. **`priority` type + unset sentinel.** My default vote: **closed enum `p0|p1|p2|p3` in `shared-enums.ts`, `priority?` optional, absent IS unset/TBD** (mirrors `AgentExtractionCandidate` "TBD"). Avoid a numeric scale (invites inference, harder to validate). Owner may prefer `high|medium|low` — confirm the label vocabulary.
3. **Task→Project ref.** My default vote: **optional `projectId?: ProjectId`** (additive; a task without a project is valid). Add `TaskId` brand mirroring `ProjectId`.
4. **Severities for the 4 members in `defaultSeverityForFailureClass`.** My default vote: `db_unavailable` → **warn** (matches its current least-wrong `worker_down`→`degraded-mode.ts` mapping); `provider_routing_unavailable` → **error** (fail-closed no-eligible-provider); `outbox_blocked` → **error**; `write_through_blocked` → **warn** (a precondition-gate hold, recoverable). Confirm tiers vs owner intent.
5. **⚠ `outbox_blocked` + `write_through_blocked` distinctness (the lead's explicit gate).** Each new member must have a DISTINCT semantic + a real-or-near-planned emit site, else it pollutes the frozen enum. My default vote: **keep all four** — `db_unavailable`/`provider_routing_unavailable` are grounded now (`degraded-mode.ts` L32 / `broker.ts` L59 interim arch_gaps); `write_through_blocked` (precondition/gate holds the write) is distinct from `write_through_failed` (attempt errored) and lands a Phase-19 producer; `outbox_blocked` lands a Phase-21 external-write-outbox producer — batching now avoids two future frozen rounds. **DROP any member you and I agree has neither a distinct semantic nor a near-term producer** (report at Step 9 if you drop one).
6. **Rewire the two named-constant arch_gaps this round?** My default vote: **retype `broker.ts` (zero behavior change — already emits the literal), DEFER flipping `degraded-mode.ts` `worker_down`→`db_unavailable`** (a visible health-item-class behavior change; the member-add alone discharges the frozen-contract debt). State the scope line explicitly.
7. **Snapshot bump procedure.** My default vote: author `task.snap` + `task.schema.json` by running `task.test.ts` once with `UPDATE_SNAP=1`; regenerate `health-item.schema.json` with `UPDATE_SNAP=1`; then full `@sow/contracts` suite + `registry-all.test.ts` + repo-wide `turbo typecheck`.

## Dependencies + sequencing
- **Depends on:** P1 (contract-freeze pattern) · P2 (db repo pattern) · 13.5 (Project template — landed).
- **Blocks:** 13.16 (priority/due-ranked `task_rollup` read-model) · desktop 9.8 `UiSafeApproval` enrichment reuse of the same discipline. **This is the ARC-2 bottleneck — Wave-2 knowledge/prov-int/worker slices that touch Task/Project wait on this seal.**

## Estimated commit count
**2** (both landing before the single repo-wide `turbo typecheck` + snapshot regen, so it stays ONE frozen round):
1. **`feat(contracts): typed Task model + priority + TaskRepository (frozen-contract)`** — task.ts + brand + Priority enum + repo + snapshot + tests.
2. **`feat(contracts): FailureClass operational members + severities`** — the 4 members + `healthItem.ts` case arms + health-item schema regen.

They have independent consumer bases (Task vs health path) ⇒ bisectable separately; neither is a *safety* invariant (severity mapping is operational, no writer/egress/isolation rule touched) ⇒ splitting is allowed but not required. If you prefer one bundled commit, that's a Step-2.5 call. **Report the split you take at Step 9.**

## Lessons-logged candidates anticipated
- **Convention candidate** — "a new Appendix-A model = the four-file set (schema+type / generated json / snap / test) in one round; barrel-export the SCHEMA_ID so `registry-all` auto-registers."
- **Architecture-doc note candidate** — the `write_through_blocked` vs `write_through_failed` semantic split (precondition-gate vs attempt-errored) — consumers will depend on the distinction.
- **Future TODO — operational** — flipping `degraded-mode.ts` to `db_unavailable` (deferred behavior change) once the member exists.

## How to invoke
1. Read this brief end-to-end — the Step-2.5 questions (esp. Q5 member-distinctness) need answers before GREEN.
2. Run `/tdd typed_task_model_and_failureclass_operational_members`.
3. Step 0 — confirm the restatement matches the Feature line.
4. Step 2.5 — reply to `main-orchestrator-2` with the design answers + the coverage map (each acceptance bullet → its test or a not-tested-because).
5. Step 9 — surface the commit split taken + any dropped member + the cross-doc rows for me to write hot.
