# /tdd brief — isredactionsafe_pattern_divergence

## Feature
`### 24.110` — `packages/policy` does **not** delegate to `@sow/domain`'s redaction classifier. It defines its **own local** `looksUnsafe` over its **own local** copies of the three patterns, and **`CREDENTIAL_PREFIX` is missing the `/i` flag.** Close the divergence **by construction** — by consuming domain's exported patterns — not by re-synchronising two lists.

## Use case + traceability
- **Task ID:** `### 24.110`
- **Architecture sections it implements:** `ARCHITECTURE.md §16` (cross-cutting: redaction)
- ⛔ **This brief widens phase scope because** `§16` is outside Phase 24's declared set (`§13 §19.11 §3 §4 §6`) while `### 24.110`'s own `Spec:` line cites it. Declared per `spec-lint` option (a). ⛔ **NOT resolved by widening the set** — standing lead ruling 2026-07-29; durable home `### 24.54`.
- ⛔⛔ **SAFETY: rule 7 — and rule 1 by REACH (see below). Step 9 auto-routes to the lead on both.**
- **Related context:** `### 24.46` (the two-independently-maintained-lists class) · `### 24.102` (where it was found) · `L39` (reuse the gate, do not reimplement) · `L151` · `L194`

---

## ⛔⛔ THE FINDING, MEASURED — AND ITS REACH IS WORSE THAN ITS TITLE

**`packages/policy/src/audit-signal.ts:75-76` and `packages/domain/src/redaction/redaction-rules.ts:60-61` hold `CREDENTIAL_PREFIX` alternations that are CHARACTER-IDENTICAL and differ ONLY in the trailing `/i`, which policy lacks.** ⇒ ⭐ **that is a COPY THAT DRIFTED, not an independent design** — `### 24.46`'s class on a rule-7 surface. ⚠ **`SENSITIVE_KEYWORD` carries `/i` in BOTH**, which is why a casual side-by-side read clears it and it survived however long it has been wrong.

**Reproduced executably (orchestrator):**

| value | `@sow/domain` (`/i`) | `packages/policy` (no `/i`) |
|---|---|---|
| `SK-ANT-API03-ABCDEF` | unsafe | ⛔ **SAFE** |
| `XOXB-123456789012` | unsafe | ⛔ **SAFE** |
| `GHP_ABCDEFGHIJKLMNOPQRST` | unsafe | ⛔ **SAFE** |
| `sk-ant-api03-abcdef` | unsafe | unsafe |

⛔⛔ **THE REACHING CONSUMER IS NOT AN AUDIT PATH — IT IS THE KNOWLEDGEWRITER'S BLOCKING PRE-COMMIT SECRET SCAN, ON THE SOLE-WRITER PATH.** `packages/knowledge/src/knowledge-writer/secret-scan.ts`'s `contentContainsSecret(value)` is `!isRedactionSafe(probe)` with **`refs: [value]` where `value` is THE WHOLE RENDERED FILE CONTENT**, and `scanForSecrets` is `writer.ts` pipeline **step 6 — immediately before the atomic commit.** **Production-wired secure-by-default at three composition sites** (`buildActivities.ts:582-583`, `:1094`, `semanticApprovalDispatch.ts:68` — each leaving `secretScan` UNSET so the real scanner binds). ⇒ ***the weaker of the two heuristics is the one guarding the canonical Markdown.***

**GAP MEASURED against both patterns AND the keyword net — 10 realistic cases, 6 true gaps.** ⭐ **Lowercase `-----begin rsa private key-----` is NOT a gap: `SENSITIVE_KEYWORD` catches `private key`. Defence-in-depth working, and it is recorded because a round that logs only failures teaches the wrong lesson about the system.** True gaps: `-----Begin Certificate-----` · lowercased `akiaiosfodnn7example` · case-transformed `SK-ANT-…` / `Sk_Live_…` / `XOXB-…` / `GHP_…`.

⚠ **HONEST BOUND, and it must not be inflated OR used to soften: several gap cases are NOT valid credential formats AS ISSUED** (a real AWS key is uppercase; real Anthropic/Slack/GitHub prefixes are lowercase) ⇒ **realistic exposure is CASE-TRANSFORMED content — a title-cased PEM header, an editor-mangled paste — not a freshly-issued key.** ⛔ **That narrows the LIKELIHOOD and not the DEFECT: `domain` put `/i` there DELIBERATELY, so two files disagree about a safety property and the sole-writer path uses the weaker one.**

⛔ **THE FRAMING TO REFUSE, and it is the sentence someone will try to argue around (lead-hardened):** ***a known-partial control silently losing case-insensitivity is a REGRESSION AGAINST ITS OWN STATED BOUND, NOT AN INSTANCE OF IT.*** `audit-signal.ts` conceding it misses an employer codename is not licence to accept a second, different miss.

---

## ⛔⛔ THE SECOND HALF, AND IT IS A LEAD RULING: THE FALSE-ASSURANCE COMMENT IS PART OF THE REMEDY

`secret-scan.ts`'s docblock says: ***"Detection REUSES the @sow/policy redaction predicate … Single source of truth ⇒ no pattern drift: a string is unsafe to commit iff it would be unsafe as an audit field."***

⛔ **TRUE PREMISE, FALSE CONCLUSION** — knowledge *does* reuse policy's predicate, **but POLICY IS ITSELF THE DRIFTED COPY OF DOMAIN.** ⇒ ***it reasons about drift between knowledge and policy while the drift is between policy and domain, ONE LAYER UP*** — and it is the strongest available reason not to check the thing that is broken.

⛔ **CORRECT IT. DO NOT DELETE IT (lead ruling).** ⭐ *Deleting repairs the DOCUMENT and leaves the DEFECT, and destroys the only artifact naming the reuse relationship at all* (`L194`). **Retarget it to name the layer it actually reasons about — knowledge↔policy — and state where the drift lives.**

⚠ **`secret-scan.ts` is `packages/knowledge` — NOT your territory.** ⛔ **Flag it at Step 9 with the replacement wording; do NOT edit it.** *(If the remedy makes the sentence true again, say so — that is the better outcome and it changes what the correction should say.)*

---

## Acceptance criteria (what "done" means)

- [ ] ⛔ **The divergence is closed BY CONSTRUCTION: `packages/policy` consumes `@sow/domain`'s EXPORTED patterns** (`L39` — reuse the gate, do not reimplement it), **or the brief-level reason it must not is stated in-file with what prevents re-divergence.**
- [ ] ⛔⛔ **NOT by re-synchronising two lists.** ⭐ `### 24.46`'s whole point is that a second hand-maintained copy re-diverges; **two lists that agree today are a defect with a delay on it.**
- [ ] ⛔ **A pin exists that REDS on an uppercase credential shape** — nothing today would have caught this. ⭐ **Mutation-prove it fires**; a guard that cannot be made to red is guarding nothing.
- [ ] **The `import` direction is legal** — `packages/policy` may depend on `packages/domain` (check the §2.5 DAG before assuming; if it may not, that is a finding and a STOP, not a workaround).
- [ ] ⛔ **No behaviour regression on the LOWERCASE cases** — everything policy catches today it still catches. Pinned, including at least one case the KEYWORD net catches rather than the prefix, so the two mechanisms stay distinguishable.
- [ ] **Reachability is recorded** as measured (the sole-writer secret scan, three composition sites) rather than left as the entry's open question — ✅ **already measured; restate, do not re-derive.**
- [ ] **Every new/changed safety pin asserts WHY it failed**, never a bare falsity (`### 24.101`).
- [ ] ⛔ **No file outside `packages/policy` is edited.** `secret-scan.ts` (knowledge) and `redaction-rules.ts` (domain) are **Step-9 flags**.
- [ ] `/preflight` clean, on two surfaces that could have disagreed.

## ⛔ TERRITORY — READ BEFORE YOU START

⛔⛔ **`packages/policy/src/visibility.ts` IS EXPLICITLY NOT YOURS THIS ROUND.** worker-implementer is landing an orchestrator-authored note edit there under brief `297`. ⇒ **if your work appears to need that file, STOP and ask through me.** ⚠ **Two sessions in one package is a situation being managed by DISCLOSURE rather than by distance — that only works if the carve-out is honoured.**

## ⛔ Instrument rules binding this slice

- **`turbo` replays a cached green in ~55ms.** Always `--force`; **quote the `Cached:` line.** Totals include CANCELLED tasks.
- **`npx` is INTERMITTENTLY intercepted**, returning fabricated success. ⛔ **One clean probe proves nothing.** Confirm via `node_modules/.bin/*` or `pnpm`, **state both**.
- ⚠ **"lint" in this repo IS `tsc --noEmit`** — reporting it as lint coverage would be a false-assurance claim inside a false-assurance slice.
- **`grep` fabricates counts; `awk` from `xargs` reports cumulative `NR`** (use `FNR`, or single-file `sed`). ⛔ **REDIRECT TO A FILE AND MEASURE THE FILE — a pipe truncates silently and UNDER-reports** (`L155`; it produced a false negative for me this round inside a safety scoping).
- ⛔⛔ **`git` IS WRAPPED HERE.** `--porcelain` returns literal `ok`; a `git diff` body came back EMPTY for a genuinely modified file this round while `--numstat` was correct. ⛔ **Verify with `--numstat`, never by counting a diff body.**
- ⛔ **Never `--amend`.** ⛔ **`git commit -F <msgfile> -- <explicit paths>` ONLY** — the index is shared and **two other implementers are live in this tree right now.**
- ⭐ **A SUCCESS EXIT IS AMBIGUOUS** between *it worked* and *it never applied* (`L205`) — verify every mutation and every destructive command with a **different** instrument than the one that reported success.

## Wiring / entry point (Step 7.5)
`isRedactionSafe` (`packages/policy/src/audit-signal.ts`) ← `secret-scan.ts`'s `contentContainsSecret` ← `scanForSecrets` ← `writer.ts` step 6 ← three production composition sites. **Reachability is ESTABLISHED — state it as reachability, not as "it has callers."**

## Files expected to touch
**Modified:** `packages/policy/src/audit-signal.ts` (`CREDENTIAL_PREFIX`, `SENSITIVE_KEYWORD`, `URL_USERINFO_CREDENTIAL`, `looksUnsafe`) · `packages/policy/test/audit-signal.test.ts`
⚠ **Flag at Step 2.5 if the import pulls in anything else.**

## RED test outline (Step 2)
1. **`an_uppercase_credential_shape_is_refused`** — Asserts: `isRedactionSafe` is **false** for `SK-ANT-API03-…` / `XOXB-…` / `GHP_…`. ⛔ **Confirm RED for the right reason** — *"the pattern is case-sensitive"*, not a fixture typo. **Mutation-prove: restore the case-sensitive pattern and exactly these red.**
2. **`lowercase_credential_shapes_still_refused`** — the no-regression pin. Green-on-arrival; say so.
3. **`the_keyword_net_still_catches_what_only_it_catches`** — Asserts: a case the PREFIX misses and the KEYWORD catches (e.g. lowercase PEM) still refuses ⇒ **the two mechanisms stay distinguishable and one cannot silently absorb the other.**
4. **`a_benign_value_is_still_safe`** — the non-vacuity control. ⛔ **Without it, a pattern matching everything passes 1–3.**
5. **`the_patterns_are_the_domain_ones`** — Asserts referential identity / single-source, so a future re-fork is a test failure rather than a silent drift (`L39`'s three-way census shape).

## Cross-doc invariant impact
⚠ **Likely none** — no model field changes. ⛔ **Flag at Step 9 if the fix moves an export across a package boundary** in a way `ARCHITECTURE.md §2.5`'s import DAG describes.

## Dependencies + sequencing
- **Depends:** nothing. **Blocks:** nothing queued.
- **Not in this slice:** `secret-scan.ts`'s comment (knowledge — flag with wording) · the historical-exposure scan (**owner-gated, scoped on the entry, NOT to be run**) · `### 24.112`.

## Estimated commit count
**1.** ⚠ If the import direction turns out to be illegal, **stop** — that is a design question, not a bigger commit.

## Things to flag at Step 2.5
1. **How you consume domain's patterns and what prevents a future re-fork** — the load-bearing decision.
2. **The §2.5 import-direction check**, stated as checked rather than assumed.
3. **Whether the remedy makes `secret-scan.ts`'s sentence TRUE again** — it changes what the correction should say.
4. **The coverage map** — each acceptance bullet → its covering test or an explicit `not-tested-because:`.

## Lessons-logged candidates anticipated
- ⭐ **A copy that drifted by ONE FLAG**, where the sibling pattern in the same file kept its flag — **so a side-by-side read confirms rather than catches.** The diff between two near-identical lists is where the defect hides, not in either list.

## How to invoke
Run `/tdd` with this brief. ⛔ **Do NOT run `/session-start`** — you already ran it at boot. ⛔ **Session-doc numbers are assigned by main-orchestrator from committed history — ASK ME at `/session-end`; do not compute `max+1`.**
