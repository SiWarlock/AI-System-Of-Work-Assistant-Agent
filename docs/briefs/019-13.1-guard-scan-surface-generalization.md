# /tdd brief — osb_guard_scan_surface_generalization (task 13.1 gate-a hardening — generalize the anti-corruption write-path guard's scan surface from the `*-source.ts` extractors to the FULL `connectors/adapters/` read edge, closing the naming-convention coverage bound; dormant, governance-boundary)

## Feature
Close the documented coverage bound on the task-13.1 anti-corruption guard. Today `scanForWriteSurfaces` (built in `3fdb0c6`) is applied by its live test ONLY to the 6 `*-source.ts` extractors (count-pinned to 6) — so a read/emit-only connector adapter with a non-conforming name (`obsidian-vault-mcp.ts`, and the connector adapters `asana.ts`/`calendar.ts`/…) ESCAPES the structural one-writer guard (the Finding logged on both 13.1 and 13.4). Generalize the guard's LIVE scan surface to the **entire Connector Gateway read edge** — every adapter in `packages/integrations/src/connectors/adapters/` (excluding the `index.ts` barrel) — which is write-free by architecture: the Connector Gateway owns external READS only; the external-WRITE (Tool Gateway) adapters live in the SEPARATE `packages/integrations/src/tools/adapters/` tree (correctly out of scope). Result: every read/emit-only adapter (extractors + `obsidian-vault-mcp` + the connectors) is now proven to reach NO `@sow/knowledge` sole-writer / fs-vault / Tool-Gateway write surface (safety rule 1, KN-4/KN-9), self-maintaining (a new connector adapter auto-joins the scan + a count-pin makes the addition deliberate). This is a scan-SURFACE change only — the 22-token denylist + the pure `scanForWriteSurfaces` core are unchanged.

## Use case + traceability
- **Task ID:** 13.1 (gate-a hardening). Closes the Finding recorded in the 13.1 + 13.4 plan annotations ("this module sits outside the `*-source.ts` scan surface; the named registry-derivation follow-up would extend the canonical guard to cover it").
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KnowledgeWriter sole writer — KN-4/KN-9, the surface the guard proves no adapter reaches) + `§8` (Connector Gateway — the external-READ edge whose adapters are all write-free by architecture; the read edge is exactly the scan surface). Safety rules pinned: **1 (one writer)**, **6 (ING-7 — the read edge ingests untrusted content, no mutating path)**. Confirm §6/§8 at Step 0.
- **Related context:** builds directly on slice 13.1-a (`3fdb0c6`, brief `017`, `packages/evals/src/osb/anti-corruption-guard.ts`) + LESSON 12 (the guard's non-vacuity/count-pin discipline). The architecture split that makes this sound: `packages/integrations/src/index.ts` documents "Connector Gateway — external READS" (`connectors/…`) as DISTINCT from "Tool Gateway — the ONLY external-WRITE path" (`tools/…`). So every `connectors/adapters/*.ts` is a read/emit-only adapter; none may reference a write surface.
- **The adapter set (from recon — verify the exact live list at Step 0):** `connectors/adapters/` today = `asana`, `base`, `calendar`, `capture-source`, `drive`, `file-source`, `github`, `granola`, `index`, `linear`, `podcast-source`, `telegram-capture`, `todoist`, `url-source`, `web-source`, `youtube-source`. The scan set = all of these EXCEPT `index.ts` (the pure re-export barrel). `base.ts` (`makeConnector`) is read-edge infra and IS in scope (it must be write-free too).

## Scope boundary (IN vs deferred)
- **IN (this slice):** MODIFY `packages/evals/src/osb/anti-corruption-guard.ts` (the live-scan helper / glob) + `packages/evals/test/osb/anti-corruption.test.ts`:
  - Change the LIVE scan surface from `adapters/*-source.ts` to `adapters/*.ts` MINUS `index.ts` (the full read edge). Keep the pure `scanForWriteSurfaces(files)` core + the 22-token denylist UNCHANGED (surface change only).
  - Update the count-pin from `EXPECTED_EXTRACTOR_COUNT = 6` to the full adapter-file count (`EXPECTED_CONNECTOR_ADAPTER_COUNT = N`) so a moved/deleted adapter OR a new adapter added without a bump fails (never vacuously green; the bump stays deliberate).
  - Add a POSITIVE coverage assertion: `obsidian-vault-mcp.ts` (and each named connector adapter) IS in the scanned set (the exact file the Finding named must be covered).
- **DEFERRED (named follow-ups — record, do NOT build):** (1) extending the scan to the connector-CORE files (`connectors/{port,gateway,transport,source-register,backoff,health}.ts`) — the Gateway core writes operational cursors via a repository port (NOT canonical Markdown), so it's out of the write-surface threat model; a separate audit could add it if desired. (2) a true code-derived registry (parse `adapters/index.ts` exports rather than a directory glob) — the glob is sufficient + simpler; note it. (3) removing `obsidian-vault-mcp.ts`'s own 13.4 inline write-surface self-check — KEEP it (belt-and-suspenders local tripwire; the canonical guard now ALSO covers it).

## Acceptance criteria (what "done" means)
- [ ] **Full read-edge coverage:** the live conformance test scans EVERY `connectors/adapters/*.ts` except `index.ts` (extractors + `obsidian-vault-mcp` + the connector adapters + `base.ts`) ⇒ ZERO write-surface violations across the whole set.
- [ ] **The Finding's file is covered:** a positive assertion that `obsidian-vault-mcp.ts` is a member of the scanned file set (the exact coverage gap the Finding named is closed).
- [ ] **Count-pinned (non-vacuous, deliberate bump):** `scannedCount === EXPECTED_CONNECTOR_ADAPTER_COUNT (N) && > 0`; a moved/renamed/deleted adapter that escapes the glob, OR a new adapter added without bumping the constant, fails the count guard.
- [ ] **Pure core + denylist unchanged:** `scanForWriteSurfaces(files)` and `WRITE_SURFACE_TOKENS` (22 tokens) are byte-for-byte unchanged (this is a scan-surface change only); the data-driven every-token-self-detects test still passes.
- [ ] **0-false-positive verified across the FULL set:** every connector adapter (`asana`/`calendar`/`drive`/`github`/`linear`/`todoist`/`granola`/`telegram-capture`/`capture-source`/`url-source` + the extractors + `obsidian-vault-mcp` + `base`) has ZERO write-surface token (empirically verified at Step 0/GREEN, as 13.1-a verified the 6). If ANY connector adapter DOES reference a write-surface token, that is either a real one-writer Finding (escalate) or a needed denylist/scope refinement — flag at Step 2.5, do not silently exclude it.
- [ ] `packages/evals` `turbo typecheck test` green; repo-wide `pnpm -w turbo run typecheck test` green (this touches only `packages/evals`; no cross-package change — run repo-wide belt-and-suspenders).

## RED test outline (`packages/evals/test/osb/anti-corruption.test.ts` — extend/replace the live-scan cases)
1. `live_full_read_edge_is_clean` — read every `connectors/adapters/*.ts` except `index.ts`, scan ⇒ 0 violations AND `scannedCount === N`. (Replaces the old `scannedCount===6` case.) Why: §6/§8 full-read-edge one-writer.
2. `finding_file_now_covered` — the scanned file set includes `obsidian-vault-mcp.ts` (positive membership assertion). Why: closes the named coverage bound.
3. `count_pin_deliberate` — `scannedCount > 0` AND `=== EXPECTED_CONNECTOR_ADAPTER_COUNT`; a synthetic "adapters set of size N-1 or N+1" ⇒ fails (never vacuously green; a new adapter forces a bump). Why: LESSON 12 non-vacuity.
4. (unchanged, re-run) the pure-core unit tests + the data-driven every-token-self-detects test + the pin tests — all still green (denylist/core unchanged).

## Cross-doc invariant impact
- **NONE.** Modifies only `packages/evals` (the guard's live-scan helper + its test). No contract model, schema, snapshot, Appendix-A row, or cross-doc table. NOT a frozen-contract round.

## Things to flag at Step 2.5 (design questions — default votes)
1. **Scan set boundary.** Default vote: **all `connectors/adapters/*.ts` MINUS `index.ts`** (include `base.ts` — it's read-edge infra and must be write-free). Rationale: the whole `connectors/` tree is the external-READ edge (per the integrations barrel); the Tool-Gateway write adapters are in `tools/adapters/`, correctly excluded. Confirm whether `base.ts` is in (default: yes) and whether to also fold the connector-CORE files (`port`/`gateway`/`transport`/…) — default NO (the Gateway core writes operational cursors via a repo port, not Markdown; out of the write-surface threat model; named as a deferred follow-up).
2. **A connector adapter that references a write-surface token.** Default vote: **treat it as a real signal** — if the empirical 0-FP check finds a token in a connector adapter, STOP and flag (it's either a genuine one-writer Finding to escalate, or a denylist refinement — e.g. a legitimate read-only `readFile`-adjacent token the word-boundary regex should exempt). Do NOT silently narrow the scan to dodge it. Confirm.
3. **Count-pin constant name + value.** Default vote: rename `EXPECTED_EXTRACTOR_COUNT` → `EXPECTED_CONNECTOR_ADAPTER_COUNT`, value = the verified live count (the implementer counts the real `adapters/*.ts` minus `index.ts` at Step 0). Confirm.
4. **Keep the pure core + denylist frozen.** Default vote: **yes — surface change ONLY**; do not touch `scanForWriteSurfaces` or `WRITE_SURFACE_TOKENS` (keeps the diff a pure widening, bisectable). Confirm.

## Wiring / entry point (Step 7.5)
- **Entry point:** the guard's own conformance test (the governance boundary runs in `turbo test`) — reachability is satisfied by the live test scanning the REAL full read edge (self-verifying). No production-code wiring (this is a test/conformance-surface change). State this at Step 7.5.
- **Depends on:** 13.1-a (`3fdb0c6`, the guard) + 13.4 (`60ebd9d`, `obsidian-vault-mcp.ts` — the file being newly covered). **Blocks:** nothing (a standalone hardening).

## Estimated commit count
**1.** The scan-surface generalization + count-pin + positive-coverage assertion, all in `packages/evals`. Governance-boundary change (safety rule 1) ⇒ **Step-8 adversarial review MANDATORY** (focused): the widened scan actually covers the full read edge (no adapter silently excluded), the count-pin can't be gamed vacuous, the 0-FP holds across the full set, and the pure core/denylist are genuinely unchanged.

## Lessons-logged candidates (implementer flags Step 9)
- Likely NONE new (this applies LESSON 12 — the count-pin/non-vacuity discipline — to a widened surface). Possible refinement note if a connector adapter forces a denylist word-boundary exemption. Implementer's call.
- **Architecture-doc note candidate:** the anti-corruption guard now covers the ENTIRE Connector Gateway read edge (not just OSB extractors) — a standing structural one-writer boundary consumers can rely on.

## How to invoke (implementer)
1. `/tdd` against this brief. Step 0 — `ls packages/integrations/src/connectors/adapters/` (count the live `*.ts` minus `index.ts` = the count-pin) + read `anti-corruption-guard.ts` + the current live test (the `*-source.ts`/count-6 cases to replace) + spot-check 2–3 connector adapters (`asana.ts`/`granola.ts`/`calendar.ts`) to confirm they're write-free `makeConnector` one-liners. Confirm §6/§8.
2. Step 2.5 — ping Q1 (scan-set boundary) + Q2 (what to do if a connector adapter trips a token) BEFORE editing; Q3/Q4 defaults.
3. RED first — the `scannedCount===N` + `obsidian-vault-mcp covered` + count-pin cases fail against the current 6-count guard, pass after the widening.
4. **Step 8 — MANDATORY adversarial review** (general-purpose Agent): the widened scan genuinely covers every read-edge adapter (no silent exclusion), the count-pin is non-vacuous, 0-FP across the full set, the pure core + 22-token denylist are byte-unchanged.
5. Step 9 — categorized flags (the deferred connector-core scan + the code-derived-registry follow-up) + ship-ask.
