# /tdd brief — hermes_commit_failure_discrimination

## Feature

`hermesAutomation`'s knowledge-commit failure path collapses every `KnowledgeCommitFailureCode` onto the single FSM state `write_conflict`, so no `failureClass` discriminates among the six codes. Either derive the state + class per code (reusing the in-repo taxonomy the other two commit paths already share), **or** document the collapse as deliberate with a test pinning that the code survives in the reason string.

## Use case + traceability

- **Task ID:** `24.48`
- **Architecture sections it implements:** `ARCHITECTURE.md §16` (error convention / FailureClass taxonomy)

> ⛔ **This brief widens phase scope because** `### 24.48`'s own declared anchor is `§16` (error convention), and **§16 is not in Phase 24's declared Spec-anchor set** (`§19.11 · §6 · §13 · §4 · §3`). **The widening is declared here rather than resolved by editing that set** — per the lead's 2026-07-29 ruling and Carry-forward 6(a0)(iii), *an anchor set that grew because a linter demanded it is worthless.* ⚠ **This is NOT a one-brief quirk — and the first number I put here was WRONG, corrected 2026-08-13 before you acted on it.** I wrote *"13 tasks"* from an intercepted `grep`; the real count by `awk` is **23 Phase-24 tasks declaring `Spec: §16`**, several already shipped. ⛔ **Worse: `§16` (23 tasks) and `§5` (14 tasks) are the TWO MOST-CITED anchors in Phase 24 and NEITHER is in its declared set** (`§19.11 · §6 · §13 · §4 · §3`, of which `§3` is cited exactly once). ⇒ **the declared set does not describe the phase**, so `spec-lint` fails nearly every Phase-24 brief and forces every author into this same escape — which is what makes the escape meaningless. Routed to the lead as a Finding. **Do not resolve it inside this slice; do not read this line as the systemic answer.**
- **Related context:** `24.23` (`mapWriteFailure` — the `L134` origin instance, which widened `KnowledgeCommitFailureCode` to six members) · contracts `L134` (a `default:` over a closed union silently absorbs new members) · contracts `L39` (reuse, don't re-implement) · contracts `L119` (two copies of a mapping that must agree, with nothing tying them together) · contracts `L82` (make the claim true or withdraw it)

> ⚠ **PATH CORRECTION — the tracker was wrong until today.** `### 24.48`'s `Files:` line read `apps/worker/…/hermesAutomation.ts`. **No such file exists under `apps/worker/`.** The real path is `packages/workflows/src/workflows/hermesAutomation.ts`. `Track: worker` is correct and unchanged (`packages/workflows/` is worker territory). Corrected in the tracker before this brief was authored; found independently by the orchestrator and by worker-implementer.

## Premises — every one is `file:line` and MEANT TO BE FALSIFIED

Per this template's own pitfall ("the brief's PREMISE is a hypothesis"), each claim below is cited so it is cheap to check. **I verified 1–5 directly. If any is wrong, that is a Step-2.5 finding, not friction.**

1. **The collapse is real.** `hermesAutomation.ts:572-576` — `deps.commit.commit(plan)` fails ⇒ `state = advance(state, ["knowledge_committed", "write_conflict"])` unconditionally, then `surface(state, \`knowledge commit failed: ${committed.error.code}\`)`. **The code is never read for control flow.**
2. **The code DOES survive in free text** at `:575` (interpolated into the message). This is what makes Done-when branch B testable at all.
3. **The union has six members** — `packages/workflows/src/ports/meetingCloseout.ts:433-439`: `schema_rejected | write_conflict | ownership_violation | secret_found | workspace_path_violation | commit_failed`.
4. ⭐⭐ **THE IN-REPO TEMPLATE ALREADY EXISTS, AND HERMES IS THE ODD ONE OUT.** `sourceIngestion.ts:270` `export function commitFailureClass(code): FailureClass` and `sourceIngestion.ts:350` `function commitFailureState(code): SourceState` — **both exhaustive over all six members, both `assertNever`-guarded, both citing `24.23`/`L134` in their `default:` block.** `meetingCloseout.ts:81` **imports `commitFailureClass` from `./sourceIngestion`** (13.8f-C, explicitly to avoid "a second, independently-drifting copy" — `L119`). ⇒ **three workflows commit knowledge; two share the taxonomy; `hermesAutomation.ts:99` imports the `KnowledgeCommitFailureCode` type and then discards the value.**
5. ⛔⛔ **THE FIX HAZARD — READ THIS BEFORE CHOOSING A BRANCH; IT IS WHY THIS BRIEF IS LONGER THAN THE TASK.** `advance` (`:406-417`) **returns the cursor unchanged on an illegal transition** (`:413` `if (!isOk(step)) return cursor;` — it does not throw and does not signal). And the adjacency table (`:166`) gives `knowledge_committed` exactly three successors: `external_actions_pending | completed | write_conflict`. ⇒ **if a per-code mapping returns any state that is not one of those three, `advance` silently returns `knowledge_committed` — a SUCCESS state — on a commit failure.** ⚠ **Mapping `secret_found` or `workspace_path_violation` to a terminal failure state without also widening the adjacency table would make a safety-relevant failure report as success, with no test, type, or lint objecting.** ⭐ **This is the round's recurring shape — *the defect arrives with the fix for it* (`24.45`/`24.50`/`24.52`) — and here it is inside the remedy itself.**
6. **NOT verified by me, flagged as open:** whether `failureClassFor` (`:420-435`) — which switches over `HermesAutomationState` and **has a bare `default:` returning `write_through_failed`** — should be brought under the same exhaustiveness discipline. It is `L134`'s literal shape (a new failure/park state added to `HERMES_AUTOMATION_STATES` absorbs silently), but it is arguably out of this task's scope. **See Step-2.5 question 3.**

## Acceptance criteria (what "done" means)

- [ ] **The branch choice is made explicitly at Step 2.5 and recorded with its reason** — not settled by whichever was easier to implement.
- [ ] **If branch A (discriminate):** every one of the six `KnowledgeCommitFailureCode` members maps to a deliberate `HermesAutomationState`, the mapping is exhaustive and `assertNever`-guarded (mirroring `sourceIngestion.ts:350`'s landed pattern), and **exhaustiveness is verified empirically — remove a case, confirm `tsc` fails — not asserted.**
- [ ] **If branch A:** ⛔ **every state the mapping can return is a legal successor of `knowledge_committed` in `hermesAutomationTransitions`, pinned by a test that drives a real commit failure per code and asserts the resulting state is NOT `knowledge_committed`.** This is the premise-5 hazard; a construction-side assertion cannot catch it — **move the state.**
- [ ] **If branch B (document the collapse):** the collapse is stated in-file **with the reason it is acceptable here**, and a test pins that the surfaced `message` still carries `committed.error.code`, so the information is recoverable. ⛔ **Branch B must not leave the file reading like an oversight** (`L82`).
- [ ] **Either branch:** `FailureClass` reuse is settled — if a class is derived per code, it comes from `commitFailureClass` (imported), **not a re-implementation** (`L39`/`L119`).
- [ ] All tests in `packages/workflows/test/hermes-automation.test.ts` pass
- [ ] `/preflight` clean

## Wiring / entry point (Step 7.5)

⛔ **`none — 7.18 is unregistered; this slice is a wiring PRECONDITION (`L106`).**

⛔⛔ **THIS SECTION WAS WRONG AND IS CORRECTED 2026-08-13. It read: *"Already wired — this is a behavior change on a live path, not new capability."* `runHermesAutomation` HAS ZERO PRODUCTION CALLERS.** Falsified by worker-implementer at Step 2.5, by method: `git grep -n runHermesAutomation -- packages apps` returns the definition (`:474`), its docstring (`:344`), and **two test files only**; `grep -n hermes apps/worker/src/temporal/workflows.ts` exits 1 (**not registered as a Temporal workflow**); `git grep -n hermes -- apps/worker/src` returns **one hit, a comment** in `dispatchSourceIngestion.ts:18`. **It is exported from the barrel (`packages/workflows/src/index.ts:124`) — importable, which is what I mistook for wired.**

⇒ ⛔ **CONSEQUENCE, BINDING ON STEP 9: the severity under-report this slice fixes is LATENT, NOT LIVE. Do not report a live safety breach.** The fix is still owed — dormancy is not a guarantee (`L106`), and this is a precondition for whenever 7.18 arms — but the Step-9 routing and any tracker text must say *latent*. ⭐ **This is `L141`'s exact shape — a reachability qualifier asserted rather than measured, biased toward over-claiming — committed by the orchestrator in the same brief that quotes `L141` at the implementer.**

## Files expected to touch

**Modified:**
- `packages/workflows/src/workflows/hermesAutomation.ts` — the commit-failure branch (`:572-576`); possibly the adjacency table (`:166`) **if branch A**; possibly `failureClassFor` (`:420`) **if Step-2.5 Q3 says yes**
- `packages/workflows/test/hermes-automation.test.ts` — the RED tests

**Possibly modified (branch A only, flag at Step 2.5 before touching):**
- `packages/workflows/src/workflows/sourceIngestion.ts` — **only** if `commitFailureState`'s sibling needs exporting. ⚠ **`commitFailureState` returns `SourceState`, a DIFFERENT alphabet from `HermesAutomationState` — it is NOT directly reusable.** `commitFailureClass` (returns the shared `FailureClass`) **is**.

If implementation needs files beyond this list, **flag at Step 2.5 before going GREEN.**

## RED test outline (Step 2)

Tests in `packages/workflows/test/hermes-automation.test.ts`:

1. **`hermes_commit_failure_state_discriminates_by_code`** *(branch A)* — drive the driver with a commit failing each of the six codes; assert the resting state differs where the design says it should.
   - Asserts: the returned `outcome.state` per code matches the deliberate mapping
   - Why: `24.48` Done-when branch A; §16 error convention

2. ⛔ **`hermes_commit_failure_never_rests_on_a_success_state`** *(branch A — THE LOAD-BEARING ONE)* — for **every** one of the six codes, assert the resting state is **not** `knowledge_committed` and **not** `completed`.
   - Asserts: `advance` actually moved; the mapped state is a legal successor
   - Why: premise 5 — `advance` (`:413`) fails silently on an illegal transition, so an unreachable mapped state degrades to a success state. **This test is the only thing standing between the fix and a silent safety regression.**

3. **`hermes_commit_failure_class_matches_the_shared_taxonomy`** *(branch A)* — assert the surfaced `failureClass` for a `secret_found` / `ownership_violation` / `workspace_path_violation` commit failure matches `commitFailureClass`'s verdict.
   - Asserts: `security_violation` / `isolation_breach` / `isolation_breach` respectively (per `sourceIngestion.ts:270-292`)
   - Why: `L119` — the two mappings must not drift

4. **`hermes_commit_failure_reason_carries_the_code`** *(branch B — and worth keeping under branch A too)* — assert the surfaced `message` contains `committed.error.code` verbatim.
   - Asserts: the code is recoverable from the health item
   - Why: `24.48` Done-when branch B's explicit requirement

5. **`hermes_commit_failure_mapping_is_exhaustive`** *(branch A)* — the empirical exhaustiveness check: comment out one case, confirm `tsc` errors, restore byte-identical.
   - Why: `24.39`/`24.38` precedent — exhaustiveness **verified**, not asserted (`L139`'s two-command declared window)

## Cross-doc invariant impact

- **Model field changes:** none — `KnowledgeCommitFailureCode` is **not** widened by this slice; it is consumed.
- **Orchestrator doc rows to write hot (Step 9 routing):** none anticipated. ⚠ **If branch A widens `hermesAutomationTransitions`, flag it** — the FSM alphabet/adjacency is behavior a consumer could depend on, and that is an Architecture-doc-note candidate I will route, not you.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No. `HermesAutomationState` is workflows-local (`:114` states `@sow/domain` ships none for 7.18). `FailureClass` is shared but **not modified** here — only consumed.

## Things to flag at Step 2.5

1. ⭐⭐ **WHICH BRANCH — and this is the whole slice, so do not take a default.** Branch A (discriminate per code, reusing the landed pattern) vs branch B (document the collapse + pin the reason string). **My vote: A, but held loosely and explicitly conditional on your read.** The argument for A is premise 4 — the taxonomy exists, is exhaustive, is `assertNever`-guarded, and is already shared by the other two commit paths, so hermes is the sole hold-out and branch B entrenches the divergence `L119` warns about. **The argument for B, which I cannot dismiss from where I sit:** hermes is a 7.18 automation spine whose failure states may deliberately be coarser than the source path's, and `24.48`'s own text says *"the question is whether this path should discriminate at all."* ⛔ **You are better placed than I am to answer that from the driver's actual semantics — I am supplying the evidence, not the verdict.**
2. ⛔ **If A: how far does the adjacency table widen, and does widening it change the machine's totality guarantee?** Options: (i) add the needed failure states as successors of `knowledge_committed`; (ii) reuse the existing `schema_rejected` for `schema_rejected` and keep everything else on `write_conflict` (a partial discrimination); (iii) introduce a new state. **My default vote: (i), narrowly — add only the states the six codes actually require, and only after test 2 exists to prove they're reachable.** Rationale: the machine's stated invariant (`:116-117`, `:172-173`) is that every failure/park state carries a back-edge to `triggered` so the machine stays TOTAL — a new state must carry one too or it breaks that.
3. **Does `failureClassFor`'s bare `default:` (`:434`) come into scope?** It is `L134`'s literal shape over `HermesAutomationState`. **My default vote: YES if branch A (you're already in that switch's neighborhood and a widened state set makes the absorb newly reachable), NO if branch B (it becomes unrelated cleanup in a slice that is otherwise documentation).** ⚠ **Either way, say which you did and why** — a silent skip is what `24.39`'s security-reviewer skip got credit for flagging explicitly.
4. **Is the commit-failure branch the ONLY collapse site in this file?** I checked `write_conflict`'s occurrences (`:123`, `:140`, `:166`, `:177`, `:428`, `:463`, `:571`, `:574`) and believe `:574` is the only *mapping* site — but **I searched by the symbol, and per `L64`/`L104` the search key was the symbol while a second collapse could live in prose or a differently-named state.** ⛔ **Enumerate by concept before you conclude I was right, and report the method with the count** (`L61`'s enumeration rule: a count without its method is `L118` wearing a number).

## Dependencies + sequencing

- **Depends on:** `24.23` ✅ (widened `KnowledgeCommitFailureCode` to six members and set the `assertNever` precedent this brief points at)
- **Blocks:** — (no queued slice consumes this)
- ⛔ **Not gated by `24.50`/`24.52`** — those gate drain/port wiring; this slice wires nothing.

## Estimated commit count

**1.** Single file + its test, one logical unit either way. ⚠ **Do NOT bundle with anything else this round** — branch A touches a state machine's adjacency table, which is a bisectability-relevant change, and `24.48` is the only task in the queue that touches this file.

## Lessons-logged candidates anticipated

- **Convention candidate** — *a state machine whose `advance` degrades silently on an illegal transition converts a mapping bug into a false success*; the mapping and the adjacency table are one unit and must be changed together, with a test that moves the state.
- **Convention candidate** — the third instance of *"reuse the landed taxonomy rather than a second independently-drifting copy"* (`L119`): `sourceIngestion` → `meetingCloseout` (13.8f-C) → hermes. **A helper exported for one sibling is evidence of a convention, not of a special case.**
- **Architecture-doc note candidate** — if the adjacency table widens, §16's Hermes-automation state alphabet gains members; I route that row, not you.

## How to invoke

1. **Read this brief end-to-end**, including the premises — they are hypotheses, and premise 5 is the one most worth attacking.
2. **Run `/tdd hermes_commit_failure_discrimination`.**
3. **Step 2.5** — answer question 1 first; questions 2–4 only matter once the branch is chosen. Do not proceed to Step 4 without my `APPROVED.`
4. **Step 9** — surface anything outside the anticipated candidates, especially if premise 5's hazard turned out to be wrong or larger than described.
