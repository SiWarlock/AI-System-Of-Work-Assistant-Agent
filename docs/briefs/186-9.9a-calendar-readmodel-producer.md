# /tdd brief — calendar_readmodel_producer

## Feature
The **9.9a worker calendar read-model producer** (apps/worker, worker territory) — projects GCL-sanitized busy/free availability into the frozen `UiSafeSchedule` type: generic conflict explanations ONLY (never raw cross-workspace event detail; Flow 3 / REQ-F-009), no workspace/timing attribution (WS-8). Ships DORMANT / empty-until-wired (the 9.7 ingestion precedent). This is commit-1 of the 9.9 split (per the lead's territory ruling); the renderer surface is 9.9b (desktop, #25, consumes this).

## Use case + traceability
- **Task ID:** 9.9 (the worker read-model producer leg — 9.9a)
- **Architecture sections it implements:** `ARCHITECTURE.md §11` (Calendar surface, Flow 3) — **REQ-F-009** (busy/free + generic conflict explanations; never raw cross-workspace event detail).
  > **Widens phase scope** into `§10` (read-model serving) + `§5` (GCL Visibility Gate / WS-8) — the worker producer behind the §11 surface.
- **Related context (from the 9.9 research digest):**
  - The `UiSafeSchedule`/`UiSafeScheduleEntry` type landed (#21, `28dd42ba`): `{ start, end, busy, conflictExplanation? }` (single-line-bounded), workspaceId-FREE by design; the type header DEFERS two cross-field invariants to this producer: `start<=end`; `busy===false ⇒ conflictExplanation` absent.
  - Availability source: `packages/workflows/src/ports/crossCalendarScheduling.ts` — `BusyWindow{sourceId,start,end,genericReason?}` ("GENERIC sanitized reason — NEVER raw event detail"), read through the GCL Visibility Gate; unreachable → `calendar_unreachable` (REQ-F-009 no-silent-free).
  - Mirror the producer pattern: `apps/worker/src/api/projections/ingestionInboxProjection.ts` (pure factory, never-throws, drop-rules at write, worker L76/L77). `READ_MODEL_KEYS` (`adapters/readModel.ts`) has NO schedule key today. Query router: `procedures/queries.ts` `buildQueryRouter` (a new procedure needs no `server.ts` edit).

## Acceptance criteria (what "done" means)
- [ ] A `READ_MODEL_KEYS.schedule` key + a `readScheduleEntries` guard (structural field-copy → `[]` on malformed) in `readModel.ts`.
- [ ] `calendarProjection.ts` — a pure factory (over injected {availability-read, read-model-put, now}, never-throws) that maps GCL-sanitized busy windows → `UiSafeScheduleEntry[]`, applying BOTH deferred invariants at write (`start<=end`; `busy===false ⇒ conflictExplanation` absent).
- [ ] `conflictExplanation` is **generic-only** (from `BusyWindow.genericReason`) — a raw title/attendee/body input NEVER reaches it.
- [ ] **No workspaceId / timing attribution leak** — windows from ≥2 workspaces emit entries with zero source attribution (WS-8).
- [ ] REQ-F-009 **no-silent-free** — an unreachable/partial source surfaces empty/degraded, NEVER "free".
- [ ] A `query.calendar` procedure + `sanitizeCalendar` re-validating each row via `UiSafeScheduleEntrySchema` (defense-in-depth); `entries` cap 200; malformed row → empty `ok`.
- [ ] Producer ships **UNBOUND (empty-until-wired)** — the `buildProofSpineActivities` composition binding is a DEFERRED follow-up; `query.calendar` returns an empty schedule until bound. (Avoids over-coupling; the 9.7 precedent.)
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
The `query.calendar` procedure mounted via `buildQueryRouter` (no `server.ts` edit); it reads `READ_MODEL_KEYS.schedule`. The producer's DORMANT composition binding at `buildProofSpineActivities` (`backends.ts`) is DEFERRED (empty-until-wired). State explicitly. **Blocks 9.9b (desktop renderer, #25)** — that consumes `query.calendar`.

## Files expected to touch
**New:** `apps/worker/src/api/projections/calendarProjection.ts` + tests.
**Modified:** `apps/worker/src/api/adapters/readModel.ts` (schedule key + guard + async reader) · `apps/worker/src/api/projections/uiSafe.ts` (`toUiSafeScheduleEntry` drop seam) · `apps/worker/src/api/procedures/queries.ts` (`ReadModelQueryPort.calendar` + `sanitizeCalendar` + the `calendar` procedure).

⚠ Worker territory. Coordinate: if worker's concurrent slices touch `queries.ts`/`readModel.ts`, sequence to avoid a same-file collision. `git add <path>` only.

## RED test outline (Step 2)
1. **`producer_maps_busy_windows_to_uisafe_entries`** — GCL busy windows → valid `UiSafeScheduleEntry[]`. Why: §11/§10.
2. **`conflict_explanation_generic_only_no_raw`** — a raw title/attendee/body never reaches `conflictExplanation`. Why: REQ-F-009.
3. **`no_workspace_or_timing_attribution_leak`** — ≥2-workspace windows emit zero source attribution. Why: WS-8.
4. **`busy_false_no_explanation_and_start_le_end`** — the two deferred cross-field invariants hold at write. Why: ui-safe.ts deferred-invariant contract.
5. **`unreachable_source_never_shows_free`** — a `calendar_unreachable` source surfaces empty/degraded, not "free". Why: REQ-F-009 no-silent-free.
6. **`query_calendar_revalidates_and_caps`** — `sanitizeCalendar` re-validates via `UiSafeScheduleEntrySchema`, caps 200, malformed → empty `ok`. Why: defense-in-depth (mirror `sanitizeIngestionInbox`).
7. **`producer_unbound_returns_empty`** — with no binding, `query.calendar` returns empty (honest-empty). Why: dormant/empty-until-wired.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (consumes the frozen `UiSafeSchedule`; the `READ_MODEL_KEYS.schedule` key is an internal registry entry).
- **Orchestrator doc rows to write hot:** if `READ_MODEL_KEYS` is mirrored in a cross-doc table, add the `schedule` row (flag at Step 9).
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Read-model scope.** Default vote: a dedicated `READ_MODEL_KEYS.schedule` key fed by the dormant producer (mirror ingestion), NOT a read-time derive off `global_surface`. Confirm.
2. **Row scope.** Default vote: a GLOBAL `workspaceId=null` merged view (like `global_surface`) — `UiSafeSchedule` is workspaceId-free by design; `query.calendar` takes no workspaceId input. Confirm.
3. **Deferred invariants location.** Default vote: BOTH — producer write-time gate AND `sanitizeCalendar` read-time re-gate. Confirm.
4. **Composition binding deferral.** Default vote: DEFER the `buildProofSpineActivities` binding (empty-until-wired); surface at Step 7.5 if you believe the slice needs it to be meaningful. Confirm.

## Dependencies + sequencing
- **Depends on:** 9.3 (landed) · the `UiSafeSchedule` type (#21) · the `crossCalendarScheduling` availability backend (built).
- **Blocks:** 9.9b (desktop renderer, #25) → `/phase-exit 9`. Sequence AFTER worker's #39 (the flip) in the worker queue.

## Estimated commit count
**1** — the worker read-model producer + `query.calendar` (worker area). **security-reviewer = invariant** (REQ-F-009 generic-conflict-only + WS-8 no-raw-cross-workspace-detail/no-attribution). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "a busy/free calendar read-model composes generic reasons (never a raw-event passthrough), carries no workspace/timing attribution (WS-8), enforces the ui-safe.ts-deferred cross-field invariants at BOTH producer-write and sanitize-read, and ships dormant/empty-until-wired (honest-empty)."

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (read-model source) + Q2 (row scope) + Q4 (binding deferral) need answers before GREEN.
2. Run `/tdd calendar_readmodel_producer`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
