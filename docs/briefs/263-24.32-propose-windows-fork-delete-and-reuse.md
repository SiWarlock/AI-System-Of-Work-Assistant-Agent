# /tdd brief — propose_windows_fork_delete_and_reuse

## Feature
`packages/workflows/src/activities/proposeWindows.ts:90-102` maintains an **independent, unfixed fork** of the raw-content-shape traversal that `24.19` hardened in `packages/contracts`. **Delete the fork and reuse the canonical predicate** — do not re-harden the copy. Second finding in the same file: its comment calls itself *"the LOAD-BEARING Flow-3 leakage check"* while nothing calls it.

## Use case + traceability
- **Task ID:** `24.32`
- **Architecture sections it implements:** §16 (candidate-data gate) · Safety rules 3/6 (external-write envelope; candidate-data-in)

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 audit and its own Step-9 findings.

- **Related context:** `IMPLEMENTATION_PLAN.md` task `24.32` (**read both findings**) · **`24.19`** (`4cef394f`) — the hardening this fork missed (Map/Set/non-enumerable/Symbol-key bypasses) · **`24.34`** (`ecf0c9ce`) — the concept sweep that confirmed these are the **only two** instances repo-wide · contracts **`L39`** (a safety predicate lives ONCE — re-export, never re-define) · **`L138`** (a propagation gap expressed as duplicated CODE is worse than one expressed as duplicated prose).

## Acceptance criteria (what "done" means)
- [ ] `proposeWindows.ts` **imports and calls the canonical predicate from `packages/contracts`** (`isRawContentShaped` / `carriesRawContent` — **confirm the exact exported name and module at Step 1**, don't trust this brief's spelling). ⛔ **The local traversal is DELETED, not repaired.**
- [ ] **The fork's own behavior is not silently narrowed OR widened.** ⚠ **The canonical predicate is STRICTER than the fork** (`24.19` added Map/Set/non-enumerable/Symbol-key handling the fork never had) ⇒ **more inputs will now be rejected.** **That is the point** — but **verify no legitimate input the activity depends on is newly rejected**, and say so at Step 9.
- [ ] ⭐ **THE `lives_once` CENSUS PIN, INHERITED FROM `24.34` AND OWED BY THIS SLICE:** a DEFINITION census asserting **exactly one** implementation repo-wide. ⛔ **`24.34` deliberately did NOT write it, because TWO definitions legitimately existed until this slice deletes one — it would have been RED against reality.** **This slice is where it becomes writable, and `24.34`'s Step-9 record says so explicitly.** ⚠ **Per `L39`: census BOTH `function` AND `const` forms** (`export const` re-fork is the shape that actually happens), **both-anchor the regex** (a prefix-anchored census accepts a superset — `L70`'s confirming instance), **and mutation-verify it** by reintroducing a fork and confirming RED.
- [ ] **The false-liveness comment is corrected.** It claims *"the LOAD-BEARING Flow-3 leakage check"* gating the Tool Gateway; **`createProposeWindowsActivity` has zero production callers** (established `24.32`'s own filing, grep-and-classify, every hit read — ⛔ **do NOT re-derive this by hand; contracts `L141`**). ⭐ **Keep what is TRUE** (this is where the check belongs when the activity is wired) **and strike the claim that it is load-bearing today.**
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
`none — the activity is dormant and stays dormant.` **This slice changes which predicate it calls, not whether it runs.** ⚠ **State reachability precisely at Step 9 — "dormant, unchanged" — and do not describe the activity as live.**

## Files expected to touch
**Modified:**
- `packages/workflows/src/activities/proposeWindows.ts` — delete the local traversal, import the canonical one, fix the comment

**New:**
- a census pin (place it where this repo's existing single-source censuses live — **check `packages/workflows/test/` for the `neutralizer-single-source.test.ts` precedent `L39` cites, and mirror it rather than inventing a shape**)

## RED test outline (Step 2)
1. **`propose_windows_rejects_what_the_canonical_predicate_rejects`** — Asserts: an input the **hardened** predicate rejects but the **old fork** accepted (a Map/Set/non-enumerable/Symbol-keyed raw-content payload — `24.19`'s own cases) is now rejected here. **RED today.** Why: proves the reuse actually took, rather than the import being added while the fork still runs.
2. **`propose_windows_still_accepts_a_clean_payload`** — Asserts: a benign payload still passes. Why: guards against the stricter predicate over-rejecting — the risk direction a delete-and-reuse introduces.
3. **`raw_content_shape_predicate_lives_once`** — the census pin above. **Mutation-verify it** (reintroduce a fork → RED → remove).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — this consumes an existing predicate.
- **Orchestrator doc rows to write hot:** none anticipated. ⚠ **If the canonical predicate needs an export it doesn't currently have, that's a `packages/contracts` change — flag at Step 2.5, it's another track's territory.**
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **Is the canonical predicate importable from `packages/workflows` without inverting the dependency direction?** My default vote: **yes** — `packages/contracts` is the pure root and everything may depend on it. ⛔ **But confirm rather than assume; if it turns out the predicate lives in `packages/knowledge` rather than `contracts`, STOP** — that would be a layering problem, not an import, and it changes the slice.
2. **Does the fork have behavior the canonical predicate lacks?** My default vote: **almost certainly not** — `24.19` hardened the canonical one and the fork is the unfixed original. ⚠ **But read both before deleting: "stale" and "redundant" are different claims, and only the second licenses deletion.** ⭐ **You made exactly this distinction on `24.43`'s `DOMAIN_TABLES`; same discipline here.**

## Dependencies + sequencing
- **Depends on:** `24.19` ✅ `4cef394f` · `24.34` ✅ `ecf0c9ce` (which bounded the instance count at two and deferred the census pin here).
- **Blocks:** — (closes the last known fork).

## Estimated commit count
**1.** One file's predicate swap plus its census pin — one logical unit.

## Lessons-logged candidates anticipated
- **Convention candidate** — possibly: **this is the round's third false-liveness comment** (`24.33`'s task text, the `gcl-projection.test.ts` comment knowledge found, and this one). ⭐ **If it lands as a third instance, the lesson is about comments asserting reachability where nothing re-measures them** — flag at Step 9 and I'll decide whether it earns its own entry or amends `L138`.

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md` task `24.32` (both findings) + `24.19`'s diff.
2. Run `/tdd propose_windows_fork_delete_and_reuse`.
3. Step 2.5 — question 2 especially; you already have the muscle for it from `24.43`.
