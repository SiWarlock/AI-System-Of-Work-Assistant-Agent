# /tdd brief — keychain_boot_gate_getsecret (17.3 — BUILD PORTION ONLY)

## ⚠⚠ HARD-LINE BOUNDARY — read first
Task 17.3 completes the credential wiring the real provider (Phase 18) + provenance stamp (Phase 19) consume — a HARD LINE. **This brief covers ONLY the BUILD portion**: wire `buildKeychainSecrets` at boot + thread the `getSecret` facade into the ModelProvider/Stamper deps **INERT / DEGRADED by default**. It **crosses NO line**:
- The gate default is ABSENT ⇒ the facade is degraded (no real creds flow); shipped default byte-equivalent to today.
- **DO NOT** provision credentials, spawn the real `security` process against real creds, or ARM the facade to real provisioned creds.
The **ARMING (binding `getSecret` to real provider/stamper deps over provisioned credentials) is the OWNER-GATED CROSSING** — the lead brings the owner the confirm. Build the wiring degraded-by-default and STOP. If a test wants real creds/arming, STOP and flag it.

## Feature
Wire `buildKeychainSecrets` at boot and thread the `getSecret` facade into the ModelProvider deps (§7) + Stamper deps (§6) — today `keychainSecrets?.secrets` feeds only the oracle bundle and `getSecret` is NEVER wired — with a **keychain-locked/missing DEGRADED handler** that mints a **keychain-locked `HealthItem`** and **fails closed (never a silent plaintext fallback)**. This includes the deferred `KeychainLockController.onKeychainLocked` routing of a `locked` result (the Slice-4 `it.todo`). Worker-track. **Pure-build (the wiring, degraded-by-default); the arming to real creds is the owner crossing.** Closes the **BUILD half of G49**; G49 fully closes at the owner arming crossing.

## Use case + traceability
- **Task ID:** 17.3 (BUILD portion). **Closes:** G49 build half (full close at the owner crossing).
- **Architecture:** `ARCHITECTURE.md §19.4`, `§7` (provider-key resolution), `§6` (HMAC signing key for SignedProvenanceStamp), `§16` (keychain-locked degradation + HealthItem, LIFE-6 wake re-attempt, redaction). **Implements safety rule 7** (secrets only via SecretsPort/Keychain).
- **Depends on:** 17.2, P5 (§7 broker).
- **Related context (confirm at Step 1 — like 17.1/17.2, much may already be built):**
  - **`apps/worker/src/secrets/keychain-boot.ts:88-95`** — returns `{ secrets, getSecret }`; the `getSecret` facade (fail-closed missing/locked/denied — built in 17.2's slice).
  - **`apps/worker/src/boot.ts:1333,1380,391`** — thread `getSecret` into ModelProvider deps + StamperDeps; add the degraded handler.
  - **The deferred `it.todo`** — `KeychainLockController.onKeychainLocked` routing a `locked` result to a keychain-locked HealthItem (this slice lands it).
  - **Worker Lessons 2 + 8 + 11** — activate a built-but-dormant boot capability via a pure fail-safe gate helper (default-OFF, thunk'd deps, owner-config not hardcoded); the shipped default stays byte-equivalent; a go-live seam is AND-composed of independent OFF-locks; real I/O behind an owner-provisioning gate (default-absent ⇒ inert/byte-equivalent, factory-spy zero-invocation OFF pin).

## Acceptance criteria (BUILD half — what "done" means)
- [ ] **`getSecret` threaded into ModelProvider + Stamper deps** (the wiring exists) — but INERT/DEGRADED by default (gate absent ⇒ no real creds; shipped default byte-equivalent). Pin the degraded default.
- [ ] **Keychain-locked/missing degraded handler:** a locked/missing Keychain ⇒ a keychain-locked `HealthItem` + fail-closed (degraded providers/stamper), **NEVER a silent plaintext fallback**. Pin (the load-bearing safety property).
- [ ] **`onKeychainLocked` routing:** the `KeychainLockController` routes a `locked` result to the keychain-locked HealthItem (the deferred `it.todo` — remove it, land the behavior). Pin.
- [ ] **Redaction (rule 7):** no secret / no key value reaches the HealthItem, logs, Markdown, or the renderer. Pin.
- [ ] **Dormancy (L11, no crossing):** the gate default is ABSENT ⇒ the real facade/creds are NOT bound; factory-spy zero-invocation on the unprovisioned/default path; shipped default byte-equivalent to pre-slice. Pin — this is the "no hard line" guarantee for the build portion.
- [ ] `/preflight` clean (worker + repo-wide). **NO provisioning / NO real Keychain / NO arming in any test** (all via mock/absent-gate).

## Wiring / entry point (Step 7.5)
`getSecret` is threaded into ModelProvider/Stamper deps at `boot.ts`; the degraded handler is on the locked/missing path. State: *"getSecret wired into the deps, DEGRADED by default; the real-cred arming is the owner-gated crossing (not this slice)."* The armed path is owner-crossing-waivered (L11).

## Files expected to touch
**Modified:** `apps/worker/src/secrets/keychain-boot.ts` (getSecret facade + onKeychainLocked), `apps/worker/src/boot.ts` (thread getSecret into ModelProvider deps + StamperDeps + the degraded handler) + tests (mock/absent-gate). NO provisioning, NO real Keychain, NO arming.

## RED test outline (Step 2)
1. `getSecret_threaded_into_provider_and_stamper_deps_degraded_by_default` — the deps carry getSecret, but the absent-gate default yields DEGRADED (no real creds). (§7/§6/L11)
2. `locked_or_missing_keychain_mints_healthitem_fails_closed_no_plaintext` — a locked/missing Keychain ⇒ keychain-locked HealthItem + degraded, NEVER a plaintext fallback. (§16 — load-bearing)
3. `onKeychainLocked_routes_a_locked_result_to_the_healthitem` — the deferred it.todo behavior. (§16)
4. `healthitem_carries_no_secret` — redaction (rule 7).
5. `default_absent_gate_is_byte_equivalent_no_real_creds_bound` — factory-spy zero-invocation on the default path (L11 dormancy — no crossing).

## Cross-doc invariant impact
- **Model:** none — SecretsPort, SignedProvenanceStamp. Confirm.

## Things to flag at Step 2.5
1. **Degraded-by-default / NO-arming (LOAD-BEARING).** Confirm the shipped default is DEGRADED (no real creds bound) + byte-equivalent; the arming is the owner crossing, NOT this slice.
2. **Fail-closed, no plaintext fallback (LOAD-BEARING, §16/rule 7).** Confirm a locked/missing Keychain degrades (HealthItem + fail-closed), never a plaintext/env fallback.
3. **Already-built-vs-complete** (like 17.1/17.2). Confirm what exists vs. what 17.3's build half finalizes.

## Dependencies + sequencing
- **Depends on:** 17.2, P5. **Blocks:** 17.4 (ref convention extends this facade). **Closes:** G49 build half. **The owner arming crossing is separate** — the lead gates it (consolidated with 17.2's provisioning).

## Estimated commit count
**1** (the build half). **security-reviewer = invariant** (credential wiring + the fail-closed-no-plaintext degrade + rule-7 redaction + L11 dormancy — the top safety surface). code-quality every-slice. **Pure-build — the arming is the owner-gated hard line, NOT in this slice.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief — the HARD-LINE BOUNDARY + the fail-closed-no-plaintext degrade are the load-bearing safety points.
3. `/tdd keychain_boot_gate_getsecret`.
4. Step 2.5 — ping the degraded-by-default + no-plaintext-fallback + already-built-vs-complete answers. Don't GREEN until sign-off.
5. Step 9 — confirm the G49 build half + degraded-by-default dormancy + NO arming/provisioning. After 17.3 → 17.4 (ref convention), then the lead brings the owner the consolidated provisioning+arming crossing.
