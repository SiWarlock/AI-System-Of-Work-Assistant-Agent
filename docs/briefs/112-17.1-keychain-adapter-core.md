# /tdd brief — keychain_secrets_adapter_core (17.1)

## Feature
Finalize the fail-closed **`KeychainSecretsAdapter`** (a `SecretsPort` over a **mockable backend**) so it resolves secret refs through an injected backend, **rejects a malformed ref with 0 backend calls**, **rejects a zero-length key**, and **never throws** — fully testable end-to-end against a MOCK backend with **NO real macOS Keychain touched**. Worker-track. **Pure-build — crosses NO hard line by itself** (the real `security`-CLI backend + provisioning is 17.2, the HARD LINE). Closes **G47**.

## ⚠ Phase-17 posture — this slice is the SAFE half
Phase 17 is the Keychain HARD-LINE phase, but **17.1 crosses no line**: it's the adapter core over an INJECTED mockable backend. Do NOT construct or spawn the real `security` process; do NOT read a real Keychain; do NOT provision anything. All tests run against a mock backend. (The real OS-credential I/O is 17.2 — owner-confirmed arming.)

## Use case + traceability
- **Task ID:** 17.1. **Closes:** G47.
- **Architecture:** `ARCHITECTURE.md §19.4` (primary), `§3` (SecretsPort — references, NEVER raw secrets), `§16` (never-throws / redaction).
- **Depends on:** P3 (§3 SecretsPort interface).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **`apps/worker/src/secrets/keychain-adapter.ts:91-104`** — the fail-closed ref parse (verify/complete). The adapter likely already exists (Lessons 9/10 built the SecretsPort adapter + CLI reader); this slice FINALIZES/verifies its fail-closed posture against a mock. Confirm what's already there vs. what needs completing.
  - **Worker Lessons 9 + 10** (the secrets adapter): map every fault to a typed ref-only error (fixed class token, never the value/raw detail); never throw (fail-closed on rogue kind / malformed ref / backend throw); hold key bytes transiently (never stringify/log; reject a zero-length key); fail-closed-parse the opaque ref (0 backend calls on malformed); real I/O behind a mockable backend seam. DE-ALIAS returned bytes (`new Uint8Array(subarray)`, never `.slice` — a Node Buffer's slice shares the backing pool). This slice must uphold ALL of these.

## Acceptance criteria (what "done" means)
- [ ] **Resolves refs via the mock backend:** a well-formed ref resolves to its key bytes through the injected backend (no real Keychain).
- [ ] **Malformed ref ⇒ 0 backend calls + typed err:** a malformed/rogue-kind ref fails closed with a typed ref-only error and does NOT call the backend (pin the zero-invocation).
- [ ] **Zero-length key rejected:** a backend returning a zero-length key ⇒ typed err (never a usable empty secret).
- [ ] **Never throws (§16):** a backend that THROWS (or rejects) ⇒ the adapter returns a typed err, never propagates the throw (fail-closed on the injected boundary — L9/L20).
- [ ] **Ref-only errors (rule 7):** every error carries a fixed class token + the ref, NEVER the key value / raw backend detail. Pin.
- [ ] **Byte hygiene:** returned bytes are de-aliased (`new Uint8Array(subarray)`, not `.slice`); the adapter never stringifies/logs the key. Confirm at Step 8.
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
The `KeychainSecretsAdapter` is the `SecretsPort` implementation; its production wiring (the real backend + boot gate) is 17.2/17.3. State: *"Adapter core reachable via the SecretsPort seam; real backend binding is 17.2-waivered."* This slice proves the adapter over a mock — reachable-by-test + the boot wiring is 17.3.

## Files expected to touch
**Modified/verified:** `apps/worker/src/secrets/keychain-adapter.ts` (the fail-closed ref parse + the SecretsPort contract) + tests (mock backend). NO real `security`-CLI, NO Keychain, NO provisioning.

## RED test outline (Step 2)
1. `well_formed_ref_resolves_via_mock_backend` — a valid ref ⇒ key bytes (mock). (§3)
2. `malformed_ref_zero_backend_calls` — malformed/rogue-kind ref ⇒ typed err, backend NOT called (spy zero-invocation). (§3/§16)
3. `zero_length_key_rejected` — mock returns empty ⇒ typed err. (§16)
4. `backend_throw_yields_typed_err_never_propagates` — mock throws/rejects ⇒ typed err, no throw escapes. (§16/L9)
5. `error_is_ref_only_no_secret_value` — the err carries the class token + ref, never the key/raw detail. (rule 7)
6. `returned_bytes_are_de_aliased` — mutating the backend's source buffer does NOT change the returned bytes (new Uint8Array, not .slice). (L10)

## Cross-doc invariant impact
- **Model:** none — SecretsPort (task 17.1 "Cross-doc invariant: none"). Confirm.

## Things to flag at Step 2.5
1. **Already-built vs. to-complete.** Confirm what `keychain-adapter.ts` already has (L9/L10 built much of this) vs. what 17.1 finalizes — don't rebuild working code; pin the fail-closed posture where gaps exist.
2. **No-real-Keychain (LOAD-BEARING).** Confirm every test uses the mock backend; the real `security` process is NEVER constructed here (that's 17.2's hard line).
3. **Byte hygiene.** Confirm the de-alias + no-stringify/log posture (L10).

## Dependencies + sequencing
- **Depends on:** P3. **Blocks:** 17.2 (real backend), 17.3 (boot gate + getSecret). **Closes:** G47. This is the SAFE build half of Phase 17.

## Estimated commit count
**1.** **security-reviewer = invariant** (SecretsPort fail-closed + rule-7 ref-only errors + byte hygiene — a secrets adapter is a top safety surface even over a mock). code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief (Q2 no-real-Keychain is the load-bearing safety point — this slice touches NO real Keychain).
3. `/tdd keychain_secrets_adapter_core`.
4. Step 2.5 — ping the already-built-vs-complete + no-real-Keychain + byte-hygiene answers. Don't GREEN until sign-off.
5. Step 9 — confirm G47 + that NO real Keychain/`security` process was touched. **17.2 (real provisioning) is the HARD LINE — the lead brings the owner the crossing confirm; do NOT provision anything.**
