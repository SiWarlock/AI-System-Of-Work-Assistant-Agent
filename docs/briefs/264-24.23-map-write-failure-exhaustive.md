# /tdd brief — map_write_failure_exhaustive_no_default_absorb

## Feature
`mapWriteFailure`'s `default:` silently absorbs `workspace_path_violation` — the code `24.12` added — into `commit_failed`, so a workspace-scope rejection is reported as a generic commit failure at the workflows boundary. **This is the ORIGIN instance of the `L134` chain and the last one open.** Make the mapper exhaustive.

## Use case + traceability
- **Task ID:** `24.23`
- **Architecture sections it implements:** §16 (error convention)

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 audit and its own Step-9 findings.

- **Related context:** `IMPLEMENTATION_PLAN.md` task `24.23` (**read the SEVERITY paragraph — it is deliberately precise**) · contracts **`L134`** (the lesson this instance originated) · ⭐ **`24.30` `4730211b` · `24.36` `f5cac8a8` · `24.38` `5fc64421` — the three siblings ALREADY FIXED. Mirror their landed pattern; do not invent a fourth idiom.**

## Acceptance criteria (what "done" means)
- [ ] `mapWriteFailure` (`packages/workflows/src/activities/commitKnowledge.ts`) is **exhaustive over `KnowledgeCommitFailureCode`** — every member has its own explicit case, terminated by an `assertNever`-style guard. ⛔ **No `default:` returning a value for an unhandled member.**
- [ ] **`workspace_path_violation` surfaces as itself**, not as `commit_failed`.
- [ ] **Every other member's mapping is byte-identical to before** — regression pin. ⚠ **This is a fix to exhaustiveness, not a re-design of existing correct mappings.**
- [ ] **A future 6th member becomes a COMPILE ERROR here**, not a silent absorption. **Verify empirically** (remove a case, confirm `tsc` fails at the guard line, restore) — the three siblings all did this and it is the only check that proves the mechanism rather than today's cases.
- [ ] **Confirm the current member list from the type definition** (`packages/workflows/src/ports/meetingCloseout.ts:424-429`), not from this brief or the tracker. ⚠ **`24.36` found the tracker's own count wrong (3 vs the real 15); re-derive it.**
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
None new — `mapWriteFailure` is already called on the existing commit path. This changes its internal mapping, not its callers or signature. ⚠ **State reachability precisely at Step 9.**

## Files expected to touch
**Modified:**
- `packages/workflows/src/activities/commitKnowledge.ts` — `mapWriteFailure`

**New / possibly modified:**
- test coverage under `packages/workflows/test/` (confirm the existing convention)
- ⚠ **`apps/worker/test/.../proof-spine-composition.test.ts` is named in the task as possibly asserting the flattened value.** **If it does, that is `apps/worker` — YOUR OWN territory, so no crossing** — but **check it before assuming; if the assertion encodes the OLD flattening, it is a `L69` tested-false-assurance and updating it is part of this fix, stated explicitly in the commit.**

## RED test outline (Step 2)
1. **`workspace_path_violation_surfaces_as_itself`** — Asserts: a `workspace_path_violation` write failure maps to its own distinct value, not `commit_failed`. **RED today.** Why: the literal defect.
2. **`existing_failure_codes_map_unchanged`** — Asserts: every pre-existing member maps exactly as before. Why: regression pin — the risk direction of an exhaustiveness refactor is over-correcting a mapping that was already right.
3. **`map_write_failure_is_exhaustive_at_compile_time`** — the empirical removal test, documented the way `24.36`/`24.38` documented theirs. ⚠ **Report WHICH case you removed.**

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — `KnowledgeCommitFailureCode` already has the member; this fixes a consumer's handling of it.
- **Orchestrator doc rows to write hot:** none anticipated.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No. ⚠ **If you conclude the union itself needs a new member, STOP** — a design change to a closed union in a port is a crossing, and the task records that it was correctly routed rather than reached once already.

## Things to flag at Step 2.5
1. **Does `workspace_path_violation` need its own distinct outward value, or does it map to an existing one that is honestly correct?** My default vote: **its own** — the whole finding is that flattening loses the reason. ⚠ **But check what the outward type actually offers before inventing a value; if the outward taxonomy has no honest home for it, that is a finding, not a thing to force.**
2. **Does `proof-spine-composition.test.ts` assert the flattened value?** My default vote: **check first, and if it does, fix it in this slice and SAY SO** — a test encoding the old wrong behavior is `L69`'s tested-false-assurance, and leaving it makes the correct fix look like a regression to the next person.

## Dependencies + sequencing
- **Depends on:** `24.12` ✅ `c86030f9` (which introduced the code).
- **Blocks:** honest reporting of a `24.12` rejection at the workflows boundary.

## Estimated commit count
**1.** One mapper, one concern.

## Lessons-logged candidates anticipated
- **None new** — `L134` already banks the class, and this is its **origin instance closing last**. ⭐ **Worth one line in Step 9 for the record: `24.23` → `24.30` → `24.36` → `24.38`, four instances, each surfaced by the previous fix's own review and NONE by a gate — and the first-found is the last fixed.**

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md` task `24.23` (especially the SEVERITY paragraph) + `24.38`'s diff as the pattern.
2. Run `/tdd map_write_failure_exhaustive_no_default_absorb`.
3. Step 2.5 — question 2 first; it changes the file list.

> ⛔ **SEVERITY, stated because the compressed version is FALSE and gets quoted: THE WRITE IS STILL REJECTED. Fail-closed holds; only the REASON is lost.** *Guarantee holds, detection degraded.* **Do NOT write "the gate does not work across the boundary" in the commit or anywhere else.**
