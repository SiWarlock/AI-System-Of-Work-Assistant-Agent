# /tdd brief — redaction_safe_producer_enumeration

## Feature

`isRedactionSafe` is a keyword/prefix heuristic, and the coverage claim that justifies trusting it ("no representable violation… today") rests on a producer enumeration that omits most of the repo's `AuditSignal` producers — including `validateProjectionVisibility`'s `MALFORMED_POLICY_INPUT` path, which embeds the candidate's own **unvalidated** `workspaceId` into `audit.refs`. Close the gap by the route Step 2.5 selects, and **re-derive** the enumeration rather than inheriting it.

## Use case + traceability

- **Task ID:** `24.45`
- **Architecture sections it implements:** `ARCHITECTURE.md §5` (policy / egress), `§16` (observability + redaction)
- **Related context:** `24.33` (placed the `isRedactionSafe` call in knowledge's `persistDenialAudit`, so the gate is REACHED — this task is about whether it is RIGHT) · `24.44` leg 2 (amplified the scope — see premise 5) · `24.53` (a refusal is silent — the sibling defect; **do not fix it here**) · contracts `L96` (a coverage claim in a doc comment, unverified against a producer the author didn't enumerate) · `L123` (a heuristic standing where a gate is assumed) · `L82` (make the claim true or withdraw it) · `L100` (a negative inherits its search's boundaries)

> ⛔ **This brief widens phase scope because** `### 24.45` declares `Spec: §5, §16` and **neither is in Phase 24's declared Spec-anchor set** (`§19.11 · §6 · §13 · §4 · §3`). Declared here rather than resolved by editing that set (lead's 2026-07-29 ruling; Carry-forward 6(a0)(iii)). ⚠ **Systemic, and my first count of it was WRONG (corrected 2026-08-13): not 13 but **23** Phase-24 tasks declare `Spec: §16`, and **14** declare `§5` — the two most-cited anchors in the phase, neither in its declared set.** ⇒ **the declared set does not describe Phase 24**, so this escape is what nearly every Phase-24 brief needs, which is what empties it of meaning. Routed to the lead as a Finding. **Do not resolve it in this slice.**

## Premises — cited so they are cheap to falsify

**I verified 1–4 and 6 directly. Premise 5 I inherited from the task and did NOT re-verify — treat it accordingly.**

1. **`isRedactionSafe` is a heuristic, not a shape allowlist.** `packages/policy/src/audit-signal.ts:103-116` scans six string fields against three regexes (`CREDENTIAL_PREFIX` `:75`, `SENSITIVE_KEYWORD` `:81`, `URL_USERINFO_CREDENTIAL` `:88`). **Anything not matching those patterns passes** — there is no allowlist of permitted shapes.
2. ⭐ **The coverage claim lives on `assertRedactionSafe`, not on `isRedactionSafe`** — `audit-signal.ts:123-135`, ending *"Redaction-safety holds structurally at every construction site, not by luck — there is no representable violation for this assert to catch today."* **Its enumeration names seven producer families:** egress-veto · schema-gate · budget-enforcer · provider-health · model-availability · provider-runner · broker. ⚠ **Note the claim is scoped to `assertRedactionSafe` but is the only justification anywhere for trusting `isRedactionSafe`'s heuristic — that mismatch is itself worth a Step-2.5 opinion.**
3. ⛔⛔ **THE ENUMERATION IS NOT MERELY MISSING ONE PRODUCER — IT OMITS THE MAJORITY, INCLUDING ITS OWN PACKAGE.** Method (re-run it; do not trust my count): `find packages apps -name "*.ts" -not -path "*/node_modules/*" -not -path "*/dist/*" -not -path "*/test/*" -print0 | xargs -0 awk 'FNR==1{f=FILENAME} /buildAuditSignal\(/ {print f":"FNR}'`. **Result: 43 matches — 1 is the definition in `audit-signal.ts:55`, 1 is an eval test ⇒ 41 production call sites across 16 files** (⚠ **I first wrote "12 files"; corrected 2026-08-13 — providers re-derived it and the file count was mine, wrong. The 43/41 figures reproduced exactly.** Unit: matching **lines**, per `L100`.) **14 enumerated** (broker/* 13 + provider-runner 1); **27 unenumerated** = policy 24 · secret-scan 2 · subscription-extraction-runner 1. The seven named families cover `packages/providers/src/broker/*` (13 sites) + `provider-runner.ts` (1). **Unenumerated:** `packages/policy/src/visibility.ts` (**8**) · `session-auth.ts` (5) · `admission.ts` (4) · `approval-policy.ts` (3) · `egress.ts` (2) · `provider-matrix.ts` (2) · `packages/knowledge/src/knowledge-writer/secret-scan.ts` (**2**) · `apps/worker/src/composition/subscription-extraction-runner.ts` (1). ⭐ **`secret-scan.ts` is unenumerated and its entire subject matter is secrets.** ⇒ **the claim's blast radius is far wider than `24.45` as filed describes.**
4. ⛔ **THE SPECIFIC GAP, and the branch that embeds it is the one that exists to catch a mismatch.** `packages/policy/src/visibility.ts:131-134` builds `refs` **before any validation**, interpolating `wsId` verbatim whenever `typeof wsId === "string"`. `denyMalformed` (`:136-149`) passes that `refs` into `buildAuditSignal`. **The branch at `:162-164` — `wsId !== sourceWorkspace.id`, "projection workspaceId does not match source workspace" — fires precisely when `wsId` is a FOREIGN value, and by then the foreign value is already in `refs`.** ⇒ a sensitive-but-not-credential-shaped `workspaceId` (an employer project codename, a person's name, an internal identifier) passes all three regexes and lands in the audit. **Rule-4 flavour, exactly as the task says.**
5. ✅ **CONFIRMED at Step 2.5 by providers-integrations-implementer, and QUALIFIED in the direction that lowers its own urgency.** Trace: `serveProjection` (`projection.ts:133`) → `admitProjection` → `visibility-gate.ts:159` → `validateProjectionVisibility`; denial → `auditOf` → `persistDenialAudit` → `isRedactionSafe` (`projection.ts:61`). ⭐ **STRUCTURAL CONSEQUENCE THAT SIMPLIFIES THE SLICE: both paths funnel through the SAME `admitProjection` → `validateProjectionVisibility`, so a fix in `visibility.ts` covers both BY CONSTRUCTION** — acceptance criterion 3 is satisfied structurally, not by two changes. ⛔ **REACHABILITY QUALIFIER (handoff 026's open caveat, `L141`): `projection.ts:114-120` records that `serveProjection`'s only production call site, `resolveApprovedCrossWorkspaceSlice`, has ZERO production callers — re-checked, it stands. THE READ PATH IS NOT LIVE.** `24.44`'s *"hostile input is the designed-for case"* is a claim about the **threat model**, not live reachability. **Both paths dormant ⇒ this remains a PRECONDITION, not an incident** — do not let the amplification language silently upgrade urgency.
6. **Nothing binds a real port today**, so nothing durably persists — which is why this is a *precondition*, not an incident. ⛔ **That is exactly why it must be closed BEFORE Phase 25.2/25.4 binds one, not after.**

## Acceptance criteria (what "done" means)

- [ ] **The route is chosen at Step 2.5 and recorded with its reason** (allowlist / validate-or-omit / retract-and-enumerate — see Q1).
- [ ] ⛔ **The producer enumeration is RE-DERIVED by a stated command, not inherited** — and the brief's own count (premise 3) is re-run rather than trusted. **Report the count WITH its method** (`L61`: a count without its method is `L118` wearing a number).
- [ ] **Whichever route: BOTH reach paths are covered** — the write path (`admitProjection`) and the read path (`serveProjection`). A remedy closing only one is incomplete (premise 5).
- [ ] **If the doc comment survives in any form, its claim is TRUE as written** — either narrowed to the producers actually audited, or replaced by the re-derived set. ⛔ **Do not re-point the existing claim at handier evidence** (`L82`).
- [ ] **A test pins the gap itself** — a projection whose `workspaceId` is sensitive-but-not-credential-shaped must not reach `audit.refs` unredacted (or must be refused, depending on route). ⚠ **MOVE THE STATE** — a construction-side assertion cannot distinguish "constructed and dropped" from a fix.
- [ ] **Any acceptance bullet asserting an absence names the search that establishes it** — which pattern, which paths, and whether the identifier can be assembled at runtime (`L100`).
- [ ] All tests in `packages/policy/test/` for the touched modules pass
- [ ] `/preflight` clean

## Wiring / entry point (Step 7.5)

**`validateProjectionVisibility` is live and reached** — `packages/policy/src/visibility.ts:124`, consumed by the GCL gate in `packages/knowledge/src/gcl/visibility-gate.ts` (`admitProjection`) and, per premise 5, by `serveProjection` on the read path. `isRedactionSafe` is reached from knowledge's `persistDenialAudit` (`24.33` placed it there deliberately). **No new wiring lands in this slice** — the port that would make the audit durable is Phase 25.2/25.4's and is explicitly NOT this slice's to bind.

⛔ **`24.50` and `24.52` gate that port binding. This slice must not bind it, and must not be read as unblocking it.**

## Files expected to touch

**Modified (route-dependent — confirm at Step 2.5 before going GREEN):**
- `packages/policy/src/audit-signal.ts` — `isRedactionSafe` (`:103`) and/or the `assertRedactionSafe` doc claim (`:123-135`)
- `packages/policy/src/visibility.ts` — the `refs` construction at `:131-134` and/or `denyMalformed` (`:136`), **if** the validate-or-omit route is chosen
- `packages/policy/test/…` — the RED tests

⛔ **OUT OF SCOPE, do not touch:** `packages/knowledge/**` (knowledge territory — `serveProjection`/`persistDenialAudit` live there; if the chosen route needs a change there, **STOP and flag it at Step 2.5** — it becomes a cross-track pair and the pair call is the lead's, not mine and not yours). Also out of scope: `24.53`'s silent-refusal observability gap, and any port binding.

## RED test outline (Step 2)

Tests in `packages/policy/test/` (mirror the existing visibility/audit-signal test file layout):

1. **`visibility_malformed_denial_does_not_leak_unvalidated_workspace_id`** — build a `GclProjection` whose `workspaceId` is a sensitive-but-not-credential-shaped string that mismatches the source workspace; drive `validateProjectionVisibility`.
   - Asserts: the returned decision's `audit.refs` does not carry that value verbatim (route-dependent: absent, redacted, or the signal refused)
   - Why: premise 4 — the mismatch branch is the one that embeds the foreign value

2. **`redaction_heuristic_admits_a_sensitive_non_credential_string`** — a characterization test for the *current* behavior.
   - Asserts: `isRedactionSafe` returns `true` for a signal whose `refs` carry such a value
   - Why: pins the gap so the fix has something to flip; ⚠ **if the chosen route is "retract the claim," this test documents what the retraction is about and should stay green with a renamed intent**

3. **`redaction_safe_still_admits_the_legitimate_ref_shapes`** — the allow-side control (`L80`).
   - Asserts: `ref:workspace:<uuid>`, `ref:job:<id>`, `sha256:<hex>`, and the marker constants still pass
   - Why: a shape allowlist that rejects real production refs would fail closed across every producer — **this is the test that catches an over-tight fix**

4. **`visibility_denial_audit_shape_is_stable_across_both_paths`** — drive the same malformed projection through the write-path gate and (per premise 5) the read-path gate.
   - Asserts: both produce the same redaction outcome
   - Why: the acceptance criterion that the remedy covers both paths; **if premise 5 turns out wrong, replace this with a `not-tested-because:` note rather than deleting it silently**

## Cross-doc invariant impact

- **Model field changes:** none expected. ⚠ **If the route changes `AuditSignal`'s admissible field shapes, that IS a contract-surface change** — flag at Step 9; **I write the `packages/contracts/CLAUDE.md` cross-doc row and the `ARCHITECTURE.md` Appendix-A row, not you.**
- **Orchestrator doc rows to write hot (Step 9 routing):** likely one — §5/§16's redaction posture gains "the enumerated producer set" as a named artifact if the retract-and-enumerate route wins.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> `AuditSignal` is a shared contract model. **If and only if its field shapes change**, the RED outline must add the schema-snapshot test. **Confirm at Step 2.5 which route you're on before deciding whether this applies.**

## Things to flag at Step 2.5

1. ⭐⭐ **WHICH ROUTE — and premise 3 should move your prior.** The task offers three: (a) `isRedactionSafe` becomes a shape allowlist; (b) the `MALFORMED_POLICY_INPUT` producer validates-or-omits the `workspaceId`; (c) the doc claim is retracted and replaced with the enumerated producer set it actually rests on. **My default vote: (b) + (c) together, and NOT (a) alone.** Rationale: (b) closes the specific live gap narrowly and provably; (c) is what stops the *next* unenumerated producer being trusted by inheritance. **(a) is the most complete-sounding and the most dangerous to do quickly** — a shape allowlist across ~41 producers is a fail-closed change to every audit path in the repo, and getting it wrong silently refuses real audits (which, per `24.53`, nobody would see). ⛔ **If you want (a), say so and argue it — but I'd want it scoped as its own slice, not folded in here.**
   ✅ **RESOLVED AT STEP 2.5 — (b)+(c) chosen, and (a) is OFF THE TABLE ENTIRELY rather than merely deferred.** providers-integrations found the reason; the orchestrator verified it independently at `packages/knowledge/src/knowledge-writer/secret-scan.ts:54-64`: **`contentContainsSecret(value)` is implemented as `!isRedactionSafe(probe)` with `probe.refs = [value]`** ⇒ **`isRedactionSafe` is doing DOUBLE DUTY as KnowledgeWriter's blocking pre-commit secret detector.** Its own doc comment states the stakes: *"Blocking pre-commit secret scan. Rejects the whole commit on a match… Wired as `KnowledgeWriterDeps.secretScan` (writer.ts step 6)."* ⭐⭐ **Under a shape allowlist, arbitrary prose fails the allowlist ⇒ `isRedactionSafe` returns false ⇒ `contentContainsSecret` is true for essentially every commit ⇒ the sole-writer path rejects everything.** ⇒ **(a) is a repo-breaking change on safety rule 1's own path, not a slice-local option.** ⛔ **The route that read as "most complete" was the one that would have broken the sole writer, and NOTHING in the task text, the original finding, or this brief's first draft surfaced that coupling — it was found only because the implementer went looking for the remedy's blast radius before choosing it.**
2. ⛔ **Does the enumeration re-derivation belong in this slice or its own?** ~41 sites across 12 files is an audit, and `L107` says the enumeration IS the deliverable, not the count. **My default vote: enumerate WITHIN this slice but scope the FIX to the visibility path** — i.e. produce the full producer list as the artifact, fix the one proven gap, and file anything else the list surfaces as new tasks rather than fixing it here. **Report the list even if it surfaces nothing further** (a bare "swept, none found" is worth far less — `24.38`'s precedent).
3. **Is `secret-scan.ts` (2 unenumerated sites, `packages/knowledge`) a live gap or structurally safe?** I did not check it and it is **outside your territory to fix**. **My default vote: CHECK it as part of the enumeration and FILE what you find — do not fix it.** ⚠ It is the highest-prior candidate on the list by subject matter alone.
4. **Does the `assertRedactionSafe`-vs-`isRedactionSafe` claim mismatch (premise 2) need its own disposition?** The coverage claim justifying the heuristic sits on a function with **deliberately zero production callers**, while the function that IS called carries no such claim. **My default vote: fold it into route (c)** — whatever enumeration lands should be attached to the function people actually call.

## Dependencies + sequencing

- **Depends on:** `24.33` ✅ (placed the gate) · `24.44` ✅ (created the second reach path)
- **Blocks:** ⛔ **Phase 25.2/25.4's `GclAuditPersistPort` binding** — alongside `24.50` and `24.52`. **This is a wiring PRECONDITION, not a follow-up:** as a follow-up it would be guarded only by dormancy, which is not a guarantee (`L106`).

## Estimated commit count

**1**, assuming route (b)+(c). ⛔ **This is a safety slice (rules 4 and 7) — it gets its OWN commit and is NOT bundled with anything.** If Step 2.5 lands on route (a), expect 2 and say so before starting.

## Lessons-logged candidates anticipated

- **Convention candidate** — *a coverage claim's enumeration decays silently as producers are added*: the claim named 7 families and the repo grew to ~41 sites in 12 files, with no mechanism tying the two together. **The general fix is not a better enumeration — it is a claim that cannot go stale** (derive it, pin it with a test that counts producers, or scope the claim to what it actually checked).
- **Convention candidate** — *a value interpolated into an audit ref BEFORE the validation that would reject it is, by construction, the untrusted value* — the ordering at `visibility.ts:131` vs `:162` is the whole defect.
- **Architecture-doc note candidate** — §5/§16 gain an explicit statement of which producers the redaction posture has actually been audited against.

## How to invoke

1. **Read this brief end-to-end.** Premise 5 is inherited and unverified — check it early, because acceptance criterion 3 depends on it.
2. **Run `/tdd redaction_safe_producer_enumeration`.**
3. **Step 2.5** — answer Q1 first. Do not proceed to Step 4 without my `APPROVED.`
4. **Step 9** — ⛔ **this slice touches safety rules 4 and 7, so `security-reviewer` is REQUIRED (the `invariant` tier fires), and Step-9 flags on rules 1/4/5/6 route to the LEAD as well as to me.**
