# /tdd brief — secret_ref_convention (17.4 — SAFE BUILD, no crossing)

## Feature
Extend the `getSecret` facade + secret-ref convention to cover **every secret kind** the activation needs — the KW HMAC signing key, model/provider keys, per-vendor connector **read** tokens (Phase 23), per-vendor connector **write** tokens (Phase 21), and the Telegram bot token (21.8 outbound + 23.6 inbound) — so every downstream seam resolves ONE facade with a stable ref namespace. This is the **ref-convention + facade coverage over the MOCK backend**; the actual per-vendor/per-token PROVISIONING happens at each vendor's own owner-confirmed crossing (21.6/21.8/23.2). Worker-track. **Pure-build — NO hard line** (mock backend; no real token provisioned here). Closes the completeness finding (write/Telegram secret-ref coverage); unblocks 21.10/21.8/23.2/23.6.

## ⚠ Boundary
This is a SAFE build slice: the ref NAMESPACE + facade coverage, resolved through the MOCK backend. Provision NOTHING; touch no real Keychain. Each real per-vendor token is provisioned at its vendor's later owner crossing — NOT here.

## Use case + traceability
- **Task ID:** 17.4. **Closes:** the completeness finding (write/Telegram secret-ref coverage).
- **Architecture:** `ARCHITECTURE.md §19.4`, `§7` (provider-key resolution), `§3` (SecretsPort ref convention), `§16`.
- **Depends on:** 17.3 (the getSecret facade / accessor).
- **Related context (confirm at Step 1 — like 17.1-17.3, the facade + ref format already exist):**
  - **`apps/worker/src/secrets/keychain-boot.ts:88-95`** — the `getSecret` facade; extend the ref namespace: `model:*` (or the existing provider-key ref), `hmac:*`, `connector-read:<vendor>:*`, `connector-write:<vendor>:*`, `telegram-bot:*`.
  - **NEW `apps/worker/src/secrets/secretRefConvention.ts`** — the ref-convention (parse/build) module. Ref format is `keychain://<service>/<account>` (confirm the exact form 17.1/17.2 use). Provider keys = `keychain://providers/<provider>` for the EXACT set **anthropic / openai / openrouter / voyage** (finalize this set here — it feeds the lead's owner-crossing confirm).
  - **Worker Lesson 9/10** (the SecretsPort ref parse: fail-closed, 0 backend calls on a malformed ref).

## Acceptance criteria (what "done" means)
- [ ] **Ref convention covers every secret kind:** `getSecret` resolves a ref of each kind — `model`/provider (`keychain://providers/<provider>`, the anthropic/openai/openrouter/voyage set), `hmac` (`keychain://sow/kw-signing`), `connector-read:<vendor>`, `connector-write:<vendor>`, `telegram-bot` — through the MOCK backend with the stable convention. Pin each kind resolves.
- [ ] **Malformed/unknown ref fails closed (0 backend calls, L9):** an unknown-kind or malformed ref ⇒ typed err, backend NOT called. Pin (spy zero-invocation).
- [ ] **No per-vendor real token provisioned here:** every test uses the mock; no real Keychain / provisioning. Pin (the build boundary).
- [ ] **Stable, parseable convention:** the ref format round-trips (build → parse → same components); a ref with an injected separator / traversal in a component fails closed (no ambiguity). Pin (like the note-path traversal-safety posture, L5 — a vendor/account component can't smuggle a different service/kind).
- [ ] **Redaction (rule 7):** a ref-parse error carries the (non-secret) ref only, never a value. Pin.
- [ ] `/preflight` clean (worker + repo-wide).

## Cross-doc invariant impact
- **Model:** extended — the SecretsPort **ref convention** (the ref namespace covering all secret kinds). **Flag at Step 9 as a `Cross-doc invariant change`** — I mirror the ref-convention into `ARCHITECTURE.md §19.4/§3` at round-close. **Shared-contract seam touched?** Confirm whether the ref convention is a `packages/contracts` type or a worker-secrets-internal shape (17.1-17.3 kept SecretsPort worker-internal — likely internal; flag if it needs contracts).

## RED test outline (Step 2)
1. `each_secret_kind_resolves_via_the_mock_backend` (it.each: model/provider, hmac, connector-read, connector-write, telegram-bot) — the convention resolves each. (§3/§7)
2. `unknown_kind_or_malformed_ref_fails_closed_zero_backend_calls` — typed err, backend spy 0 calls. (L9)
3. `ref_round_trips_build_then_parse` — build→parse→same components. (§3)
4. `a_component_with_an_injected_separator_or_traversal_fails_closed` — a vendor/account can't smuggle a different service/kind. (L5-style safety)
5. `ref_parse_error_is_ref_only_no_secret` — redaction (rule 7).

## Things to flag at Step 2.5
1. **Provider set = anthropic/openai/openrouter/voyage** — confirm this is the exact set (it feeds the owner crossing). If a provider is uncertain, flag it.
2. **No-provisioning / no-real-Keychain** — confirm the whole slice is mock/convention-only.
3. **Contracts vs worker-internal** — whether the ref convention belongs on a contracts model (flag for the contract track) or stays worker-secrets-internal.

## Dependencies + sequencing
- **Depends on:** 17.3. **Unblocks:** 21.10/21.8/23.2/23.6. **This is the LAST safe build slice of Phase 17** — after it, the lead brings the owner the consolidated provisioning+arming crossing (17.2 provision + 17.3 arm + the finalized provider set).

## Estimated commit count
**1.** **security-reviewer = invariant** (the secret-ref parse/convention — fail-closed + traversal-safe + rule-7). code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief (no-provisioning boundary + the traversal-safe ref parse are the load-bearing points).
3. `/tdd secret_ref_convention`.
4. Step 2.5 — ping the provider-set + no-provisioning + contracts-vs-internal answers. Don't GREEN until sign-off.
5. Step 9 — confirm the completeness finding closed + the Cross-doc invariant change (ref convention) + NO provisioning. **This is the last Phase-17 build slice — the lead then does the build round-close (push) + brings the owner the crossing.**
