# /tdd brief — source_ingestion_extraction_leg_ing7

## Feature
Replace the fixed `sourceAgent.run` (returns `sourceBinding.extraction`, **bypasses the broker entirely**) + the hand-built `{owner,dueDate:TBD}` with a real source-extraction leg that **routes the source through the broker** (so ING-7 admission + the candidate-data gate apply), GATES on the accepted outcome, and enforces REQ-F-017 — the SOURCE analog of 18.3's meeting leg (same B-rescoped pattern) PLUS the **ING-7 read-only admission** pin. **SAFE-BUILD: the run leg is 18.1's dormant stub — no real model; the deterministic routing + admission + gates are TDD; the real prompt + eval is deferred to the crossing.**

## Use case + traceability
- **Task ID:** 18.4
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (primary), `§9` (workflow 4 — source ingestion), REQ-S-006 candidate-data gate (§3/§7), **ING-7** (untrusted-content tool-stripping)
- **Related context:**
  - **Evidence:** `sourceAgent.run` returns a fixed `sourceBinding.extraction` (comment "the real broker/model path is C2/C3") and **bypasses the broker** — so the source agent job never hits admission/gates today; `sourceExtraction` is a hand-built `{owner,dueDate:TBD}` at boot. (Plan cites `buildActivities.ts:611-620` + `boot.ts:1013` — trace the real symbols at Step 1.)
  - **ING-7 = `admitJob`** (`packages/policy/src/admission.ts:43`, the broker's step-1 admit): `!isTrusted(job) && admitsMutating(job.toolPolicy) ⇒ deny UNTRUSTED_CONTENT_MUTATING_TOOL`. Source-AGNOSTIC + fail-closed (worker **LESSON 6**). `admitsMutating` (`tool-policy.ts:26`): `read_only` mode ⇒ never mutating. **So routing the source through the broker is what SUBJECTS the source agent to ING-7** — today the bypass means admission never runs for the source path. That's the safety-material tightening (like 18.3's gate-on-outcome).
  - **Reuses the 18.3 B-rescoped pattern (commits `0bbb281c`/`d4c2a9a0`):** gate-on-outcome (`mapCandidate` reads the accepted outcome, rejection → empty → downstream reject, never a blind echo) + a real worker-injected extraction schema gate (analog of `createMeetingExtractionSchemaGate`) + `validateNoInference` (REQ-F-017) at the live `ValidateExtractionPort`. The faithful evidence-bearing extraction-from-broker-outcome reconstruction is DEFERRED to #18 (the KMP stand-in discards `evidenceRef` — same constraint as 18.3; GATE-1).
  - **Prior slices (landed):** 18.1 `99cae521` (dormant run leg + admission runs in the broker), 18.2 `d4c2a9a0` (schema gate + KMP stand-in), 15 (SourceEnvelope carries `body`).

## Acceptance criteria (what "done" means)
- [ ] **Route through the broker:** `sourceAgent.run` becomes a `broker.runJob` over a source `AgentJob` (built from the dropped source body), replacing the fixed `() => ok(sourceBinding.extraction)` bypass — the source job now goes through admission (ING-7) + gates + the (dormant 18.1 stub) run leg + schema.
- [ ] **ING-7 (safety rule 6):** the source agent job carries a `read_only` toolPolicy; a source job (untrusted content) declaring a mutating tool policy is REJECTED at admission (`admitJob` → `UNTRUSTED_CONTENT_MUTATING_TOOL`). Pin the rejection AND the read-only admit. Closes the gap where the source bypasses admission today.
- [ ] **Gate-on-outcome:** the source `mapCandidate` reads the accepted `BrokerOutcome` — a rejection propagates (no blind echo of the fixed extraction).
- [ ] **Real source-extraction schema gate** (worker-injected, analog of `MeetingSchemaGate`): structural validation over the source extraction fields; a malformed field → `schema_rejected`; never coerces/throws.
- [ ] **REQ-F-017:** the source extraction's owner/dueDate → `TBD` via `validateNoInference` (source `ValidateExtraction` path); remove the hand-built `{owner,dueDate:TBD}`.
- [ ] **Accept-path preserved:** the source proof-spine still produces its note (the source fixture passes admission read-only + the real schema gate + no-inference); full worker suite green.
- [ ] **SAFE-BUILD:** no real model — the source extraction comes from the dormant stub run leg; the real prompt + eval-class is deferred to the crossing; the faithful evidence-bearing reconstruction is deferred to #18 (GATE-1).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
The source path: `dispatchSourceIngestion` → `sourceIngestionWorkflow` → (18.4: route through `broker.runJob`) → source `mapCandidate` → `ValidateExtraction` → commit. Reached in production via the real source dispatch (15 threaded the body). ING-7 runs at the broker's step-1 `admitJob`. Confirm the source agent is reached on the real workflow (the source `-live` run is the end-to-end check).

## Files expected to touch
**New:**
- `apps/worker/src/composition/source-extraction.ts` (or fold into an existing composition module) + test — the real source `mapCandidate`/schema gate (analog of `meeting-extraction.ts`).

**Modified:**
- `apps/worker/src/composition/buildActivities.ts` — `sourceAgent.run` routes through `broker.runJob`; source `mapCandidate` gates on the outcome; inject the real source schema gate.
- `apps/worker/src/boot.ts` — remove the hand-built `{owner,dueDate:TBD}`; wire the source agent's read-only toolPolicy / job shape.

> **Providers leg — defer.** The real source-extraction PROMPT belongs in `packages/providers/src/model` but only executes at the crossing (dormant run leg). Defer it. The source schema gate is worker-injectable (like `MeetingSchemaGate`) — flag at Step 1 only if a providers/contract schema is genuinely required.

## RED test outline (Step 2)
1. **`source_routed_through_broker_not_bypass`** — the source extraction job goes through `broker.runJob` → `admitJob` (not the fixed bypass). Why: route-through-broker (S2 Q4).
2. **`ing7_source_mutating_tool_rejected_at_admission`** — a source job (untrusted) with a mutating toolPolicy → `admitJob` denies `UNTRUSTED_CONTENT_MUTATING_TOOL`. Why: ING-7 (rule 6).
3. **`ing7_source_read_only_admitted`** — a `read_only` source job → admitted. Why: ING-7 accept.
4. **`source_mapCandidate_gates_on_outcome`** — source `mapCandidate` reads the accepted outcome; non-accepted → empty/reject (not blind echo). Why: gate-on-outcome.
5. **`source_schema_gate_rejects_structurally_invalid`** — malformed source extraction → `schema_rejected`. Why: rule 2.
6. **`source_noinference_inferred_owner_rejected`** — concrete owner + no evidenceRef → `no_inference_violation`. Why: REQ-F-017.
7. **`source_noinference_tbd_passes`** — TBD owner/date → ok. Why: REQ-F-017.
8. **`accept_path_source_passes_real_gate`** — the source proof-spine extraction passes admission read-only + the real schema gate + no-inference. Why: accept-path.
9. **`safe_build_no_real_model`** — the source extraction comes from the stub run leg; no real model. Why: SAFE-BUILD.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none anticipated — reuses `AgentJob`, `SourceEnvelope`, `ToolPolicy`, `AgentExtraction`/`ExtractionField`, `BrokerOutcome`. Confirm at Step 9.
- **Orchestrator doc rows to write hot:** none anticipated (the source schema gate is worker-internal).
- **Shared-contract seam model touched?** No anticipated (no new frozen model). Flag if the source job's shape needs a new contract field.

## Things to flag at Step 2.5
1. **Route-through-broker mechanics** — how `sourceAgent.run` becomes a `broker.runJob`: the source `AgentJob` shape (capability, `toolPolicy: read_only`, `trustLevel: untrusted`, `outputSchemaId` = the KMP stand-in like 18.2), built from the SourceEnvelope body. My default vote: **mirror the meeting job's broker routing; source job `toolPolicy = read_only`** (ING-7). Trace the exact source dispatch at Step 1.
2. **Source schema gate location** — worker-injected real gate (analog of `MeetingSchemaGate`, reuse `ExtractionField`), replacing any source stub. My default vote: **worker-injected**; flag if a providers/contract schema is required.
3. **ING-7 pin** — `admitJob` enforces it source-agnostically (LESSON 6) once the source is routed through the broker. My default vote: **pin at the broker-routed source job** — a mutating source toolPolicy → `admitJob` denies; a read_only one admits. Verify the source is genuinely subjected to admission (the whole point — today it bypasses).
4. **Same B-rescoped pattern as 18.3** — gate-on-outcome + prompt deferred + faithful reconstruction deferred to #18 (same evidenceRef constraint). My default vote: **confirm the same pattern** (REQ-F-017 preserved via the live `ValidateExtractionPort` over the deterministic extraction; the real-model faithful reconstruction is GATE-1/#18).

## Dependencies + sequencing
- **Depends on:** 18.1 (`99cae521`), 18.2 (`d4c2a9a0`), 15 (SourceEnvelope body). All landed.
- **Blocks:** 18.7 (ProposedAction from source extraction), 18.8 (commit callers carry real source extraction).

## Estimated commit count
**1** — one focused slice (worker-primary). **TRIPLE-INVARIANT** (candidate-data gate rule 2 + REQ-F-017 + **ING-7 rule 6**) → **security-reviewer = invariant; own commit, no bundling.**

## Lessons-logged candidates anticipated
- **Convention candidate** — "routing the source extraction through the broker SUBJECTS it to ING-7 admission (a mutating untrusted-source agent is rejected at `admitJob`) — the bypass was the gap; same gate-on-outcome + real-schema-gate + validateNoInference pattern as the meeting leg (18.3)."
- **Architecture-doc note candidate** — the source-extraction leg's broker routing + ING-7 admission + the source schema contract (§19.5/§9).
- **Future TODO — crossing:** the real source-extraction prompt + eval-class (model-driven half) + the faithful reconstruction (#18/GATE-1).

## How to invoke
1. **Read this brief end-to-end** — the 4 Step-2.5 questions need answers before GREEN.
2. **Run `/tdd source_ingestion_extraction_leg_ing7`** in the `worker-impl` session.
3. **Step 0 (Restate)** — SAFE-BUILD; route-through-broker + ING-7 + gate-on-outcome + real source schema gate; prompt deferred.
4. **Step 1 (Identify files)** — trace the real `sourceAgent.run` bypass + how to route it through the broker; confirm the source is subjected to `admitJob`; flag any providers schema need.
5. **Step 2.5** — send the test write-up + votes on the 4 questions; wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 8 reviewers** — security-reviewer (invariant: candidate-data gate + REQ-F-017 + ING-7) + code-quality-reviewer (every-slice).
7. **Step 9** — categorized flags + ship-ask; report the fixture set + the route-through-broker mechanics.
