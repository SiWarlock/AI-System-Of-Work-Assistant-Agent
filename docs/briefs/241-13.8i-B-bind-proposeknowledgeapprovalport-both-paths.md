# /tdd brief — bind_propose_knowledge_approval_port_both_paths

## Feature
Bind `ProposeKnowledgeApprovalPort` at the worker composition root so a withheld
PROPOSE-tier living-vault plan can mint a PENDING §9.8 Approval — on **BOTH** the
source-ingestion path (13.8i's) and the meeting-closeout path (13.8f-C's). 13.8i and
13.8f-C each shipped the routing mechanism; **neither shipped the binding.**

## ⛔ HARD-LINE GUARD — READ BEFORE STEP 1, THIS OUTRANKS THE REST OF THE BRIEF
⚠ **AMENDED AFTER DISPATCH — the premise below CHANGED, and the STOP behaviour did
NOT.** As first written this section said the propose-bridge flip *"remains DEFERRED to
the owner."* **That is now false:** the owner reaffirmed authorization and the lead ruled
**all four hard lines crossable THIS RUN** (`docs/team-handoffs/020-…`, decision log).
⛔ **But a NEW sequencing ruling replaces the old gate: task 24.6 (the pre-go-live
safety audit) RUNS FIRST, before ANY arming slice** — arming before the audit means its
findings land on a surface already writing to the world. ⇒ **the STOP below is unchanged
in behaviour and only changes in reason: it is no longer "unauthorized," it is
"sequenced after 24.6."** Corrected here rather than only in a message, because the
brief is the durable channel (contracts L94).

## ⛔ BINDING AND ARMING MUST REMAIN SEPARATELY OBSERVABLE
⭐ **Reworded at the lead's instruction, and the reason is the whole point: the old
header read *"authorized only because binding ≠ arming,"* which makes this guard look
like it EXPIRES with the permission. It does not.** Binding ≠ arming is a distinction
**the code either honours or it doesn't**, and if binding alone could mint a live
Approval card on a default boot, then *"the binding works"* becomes indistinguishable
from *"the bridge is live"* — and we would have lost the ability to tell them apart at
exactly the moment we start flipping things on. ⇒ **A permission being granted is not a
reason to stop verifying the thing the permission was about.** The living-vault leg
stays dormant by its own arming flag and by the absence of `MeetingRewriteDeps`, so a
bound port must be reached by nothing — **verify that; it is the deliverable, not a
formality.**

⛔ **YOU MUST ESTABLISH THAT, NOT ASSUME IT.** If at any point you find that binding
alone could cause a real Approval card to be minted in a default boot — or that
completing this slice requires flipping an arming flag, setting a gate to `true`, or
enabling a leg — **STOP, do not proceed, and route it to the orchestrator as a
Finding.** That would be the deferred propose-bridge crossing wearing a wiring
costume, and it is the owner's call, not ours. **"It was the obvious next step" is
exactly how an irreversible crossing gets taken.** Take it to armed-but-off and stop.

## Use case + traceability
- **Task ID:** 13.8i-B · **Architecture:** `ARCHITECTURE.md §6` (KN-10 tier) · `§9`
  (§9.8 Approvals) · REQ-F-021
- **Related:** `#### 13.8i-B` in `IMPLEMENTATION_PLAN.md` (**widened 2026-07-31 to
  cover both construction sites — read the widening note; it explains why this is one
  task and not two**). Upstream: 13.8i (`a7d4ae9d`), 13.8f-C (`8199a61f`).

## ⭐ PREMISE, ESTABLISHED FROM SOURCE — and it is NARROWER than the task's own wording
The tracker says *"`ProposeKnowledgeApprovalPort` has ZERO construction sites,"* which
is true of the **port** and misleading about the **work**. ⛔ **Do not build a factory
or a sink — both already exist and are tested.** Verified:
- **The factory exists:** `createProposeKnowledgeApprovalPort(sink)` at
  `apps/worker/src/composition/living-vault.ts:288`, with its adapter contract already
  pinned in `apps/worker/test/living-vault-propose-approval.test.ts` (13.8i built it).
  It is called from **tests only — zero production call sites.**
- **The sink instance already exists at boot:** `createApprovalsKnowledgeProposeSink({
  approvals, pendingKmp, workspaceConfig, now, … })` is constructed at
  `apps/worker/src/boot.ts:1756` as `knowledgeProposeSink`.
- **Two consumers declare the port and both leave it unbound:**
  `packages/workflows/src/workflows/sourceIngestion.ts:131` and
  `packages/workflows/src/workflows/meetingCloseout.ts:135`, each
  `readonly proposeKnowledgeApproval?: ProposeKnowledgeApprovalPort;` and each
  guarded by `=== undefined` at its call site.
- **The threading precedent is in the same file you'll edit:** `params.livingVault` →
  `buildActivities.ts:1176`, `params.meetingVault` → `:542`.

⇒ **The whole slice is: wrap the existing sink with the existing factory once, thread
it to both deps objects, and pin that both paths receive it.** If your Step-1 file
list is materially bigger than that, say so at Step 2.5 before going GREEN.

## Acceptance criteria
- [ ] `proposeKnowledgeApproval` is bound on the **source-ingestion** deps path,
      constructed from the **existing** `knowledgeProposeSink` via the **existing**
      `createProposeKnowledgeApprovalPort` — ⛔ **never a second sink and never a
      second factory** (`living-vault.ts:280` states this in its own voice).
- [ ] Bound on the **meeting-closeout** deps path, from the **same** port instance —
      one construction, two consumers.
- [ ] ⛔ **Each path pinned SEPARATELY.** One binding passing says nothing about the
      other; a single test covering "the port is bound" satisfies the letter and
      leaves half the task undone (this is why 13.8i-B was widened rather than split).
- [ ] **The unbound path remains the fail-closed default when unwired** — pinned, both
      paths. Removing the binding must still yield withhold-and-surface, never
      auto-commit.
- [ ] ⛔ **Dormancy UNCHANGED and verified by RUNNING, not reasoning:** the
      living-vault arming flag stays OFF, `gateMeetingVaultRewrite` still requires
      strict `=== true`, `MeetingRewriteDeps` stays unconstructed, and
      `meeting-rewrite.test.ts`'s `no_production_caller` stays green **unmodified**.
- [ ] **A default boot mints ZERO Approval cards** — pinned as a behavior, not
      asserted in prose. This is the acceptance bullet that discharges the hard-line
      guard above; if you cannot pin it, that is a Finding, not a note.
- [ ] `/preflight` clean — report as **"typecheck + tests clean; no lint coverage
      exists."**

## Wiring / entry point (Step 7.5)
`apps/worker/src/boot.ts` (composition root) → `buildActivities` params → both
workflow deps objects. **Already-reachable production path** (`runSourceIngestion`
and `runMeetingCloseout` both have real call sites); this slice adds no new entry
point and changes no gate. ⛔ **Scoped claim for the commit:** *"the propose port is
bound at the composition root; both legs remain dormant and a default boot mints
nothing"* — **never** *"PROPOSE plans now reach Approvals."* That unqualified form is
precisely what 13.8i's tracker entry exists to forbid.

## Files expected to touch
⚠ **CANDIDATE list — re-derive at Step 1** (worker L81; full-graph typecheck).
- `apps/worker/src/boot.ts` — wrap the existing sink; pass the port into `buildActivities`
- `apps/worker/src/composition/buildActivities.ts` — accept + thread to both deps
  (mirror `params.livingVault` / `params.meetingVault`)
- tests for both paths + the zero-cards-on-default-boot pin

## RED test outline (Step 2)
1. **`source_path_receives_the_bound_propose_port`** — the source deps object gets the
   port; a withheld PROPOSE plan reaches the sink exactly once.
2. **`meeting_path_receives_the_bound_propose_port`** — same for meeting. ⛔ Separate
   test, not a shared parameterized one — see the acceptance bullet.
3. **`both_paths_share_ONE_port_instance`** — pins the no-second-sink rule
   structurally rather than by comment.
4. **`an_unbound_source_path_still_withholds_fail_closed`** — the default remains
   correct; mutation-verify by removing the binding.
5. **`an_unbound_meeting_path_still_withholds_fail_closed`** — same, other path.
6. **`a_default_boot_mints_zero_approval_cards`** — ⭐ the hard-line discharge.
   Non-vacuity: assert the sink is invoked **zero** times, and prove the assertion can
   fire by driving one armed-fake case that DOES invoke it (else it passes because
   nothing ran — L90).
7. **`dormancy_pins_still_hold`** — run the knowledge suite; `no_production_caller`
   unmodified.

## Cross-doc invariant impact
- **Model field changes:** none expected — composition wiring only.
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No.
- Flag at Step 9 if the threading forces a shared port/deps interface change (that
  would break other areas' fakes — contracts L50).

## Things to flag at Step 2.5
1. ⭐ **Is `boot.ts:1756`'s `knowledgeProposeSink` construction inside a CONDITIONAL
   scope?** Its indentation suggests a nested block. **If it is, the port binding
   inherits that condition** — which may be correct (it rides existing dormancy) or may
   mean the port is bound only in some boots, which is worse than unbound because it is
   inconsistent. **Establish which, and say so explicitly.** My default vote: bind at
   the same scope the sink is already constructed in, and pin the resulting behavior
   rather than assuming the scope is right.
2. **One port instance shared by both paths, or one per path?** Default vote: **one
   instance, two consumers** — the sink is idempotent-by-planId and a second instance
   buys nothing while inviting a second sink later.
3. **Does the arming documentation need updating in this slice?** 13.8i-B's Done-when
   says the arming docs must state **both** gates (armed AND bound). Default vote:
   **yes, in this slice** — it is the slice that makes "bound" a real state, and
   deferring it recreates the ambiguity the task exists to close. Flag if it lands
   outside your territory (then it is an orchestrator hot-write).

## Dependencies + sequencing
- **Depends on:** 13.8i (`a7d4ae9d`) · 13.8f-C (`8199a61f`) — both landed.
- **Blocks:** the living-vault arming crossing (§ARM-RESEARCH) — ⛔ **which is
  owner-gated and NOT this slice.**

## Estimated commit count
**1.** Composition wiring for one port. ⚠ **Safety-adjacent ⇒ its own commit
regardless** — do not bundle anything else into it.

## Step-9 routing
⚠ **SAFETY slice — route Step 9 to the LEAD via the orchestrator**, per the standing
rule for safety/rule-adjacent work. `security-reviewer` is **MANDATORY** (invariant:
candidate-data gate + the §9.8 approval boundary). State the dormancy evidence and the
zero-cards pin explicitly in the Step-9 report.

## Lessons-logged candidates anticipated
- **Convention candidate** — "binding and arming are separate gates, and a task that
  closes one must state the other is still shut; 'we wired it' reads as 'it works'."
- **Architecture-doc note** — the arming path now has two independent gates (armed AND
  bound) that must both be stated wherever either is.

## How to invoke
1. **Read the hard-line guard first.** If the slice turns out to require arming, stop.
2. Verify the premise yourself — factory and sink both already exist; do not rebuild.
3. `/tdd bind_propose_knowledge_approval_port_both_paths`; Step 1 re-derives files.
