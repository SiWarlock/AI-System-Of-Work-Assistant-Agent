# /tdd brief — the audit drill-down summary shape (9.41 leg A, contracts)

## Feature

Freeze one narrow UI-safe summary shape for an audit drill-down — `event` + `occurredAt` **only** — so the worker resolver (leg B) has a contract to return and `auditRef` has no way to reach the renderer.

## Use case + traceability

- **Task ID:** 9.41
- **Architecture sections it implements:** `ARCHITECTURE.md §10` (UI-safe projection) · `§16` (System Health, REQ-S-002)
- **Related context:** the 9.10-D sub-arc's design rationale, which stays in `### 9.10`'s entry · the lead's 2026-07-29 arc ruling · `packages/contracts/CLAUDE.md` ADR-008 / its lessons on the 4-file model recipe

## Why this leg, and why it is leg A

The lead's binding arc shape: **contracts (one new narrow `UI_SAFE_ALLOWLIST` entry) → worker (the drill-down resolver) → desktop (the affordance).** ⛔ **No `packages/db` change.** You are leg A, and nothing blocks you.

⛔ **THE INVARIANT THE WHOLE ARC EXISTS TO PRESERVE: `auditRef` NEVER LEAVES THE WORKER.** The summary carries **`event` + `occurredAt` only** — no `beforeSummary`/`afterSummary` (the lead dropped those explicitly as *"unbounded raw"*), no `actor`, no `refs`, no `payloadHash`, no `workspaceId`.

⭐ **The argument a future reader must DEFEAT rather than merely re-raise:** *an audit link's value is **provenance, not contents** — the renderer already shows the change.* If you think a field is missing, that is the claim you have to beat, and beating it is a Step-2.5 question, not an implementation choice.

## Verified starting facts (orchestrator-established; contradict them if the code disagrees)

Positions are data as of `03a5cc91`, not the citation.

- `UiSafeRecentChange` (`packages/contracts/src/api/ui-safe.ts:313-318`) carries **`changeId`, `kind`, `summary`, `occurredAt` only.**
- `UI_SAFE_ALLOWLIST.recentChange` (`:662`) = `["changeId","kind","occurredAt","summary"]` — **no `auditRef`.** `.healthItem` (`:658`) likewise has none.
- Those types **deliberately drop** the audit fields, and say so: `:181` (`UiSafeHealthItem` drops `auditRef`/`parityReportRef`/`factIdentity`), `:205-206` (`UiSafeWorkflowRunRef` drops `auditRefs`), `:308-310` (`UiSafeRecentChange` drops `payloadHash`/`actor`/`refs`/`beforeSummary`/`afterSummary`).
- ⇒ **No drill-down summary shape exists anywhere. This leg is net-new** — you are adding, not modifying.
- ⛔ **`AuditRecord` has NO `id` field** (`packages/contracts/src/models/audit-record.ts`) and `AuditRepository` exposes only `append` + `query(filter, limit)` (`packages/db/src/repositories/interfaces.ts:561-565`) — **no `get(id)`.** That is *why* the arc resolves a **summary** rather than fetching by ref. **Do not design around a row id; there isn't one.**
- ⭐ **The Carry-forward hold note that named `packages/db/src/repositories/interfaces.ts` as "the file to watch" does NOT apply to you** — it flagged collision risk from a sibling worker slice. **Leg A does not touch that file.** Your surface is contracts only.

## Acceptance criteria (what "done" means)

- [ ] A new UI-safe drill-down summary interface exists carrying **exactly** `event` + `occurredAt`, with its Zod schema, generated JSON Schema, and **frozen field-set snapshot** — the package's 4-file recipe, not a bare interface.
- [ ] Its `UI_SAFE_ALLOWLIST` entry lists **exactly** those two field names.
- [ ] A test **fails** if the field set grows — the snapshot is the gate, and it must be non-vacuous.
- [ ] A test asserts the shape **cannot** carry `auditRef`, `actor`, `refs`, `payloadHash`, `beforeSummary`, `afterSummary`, or `workspaceId` — ⛔ **assert the exclusion explicitly; do not rely on the snapshot alone to imply it.** The snapshot pins what *is* there; this pins what must *never* be.
- [ ] `registry-all` / the schema-registry suite stays green with the new schema registered.
- [ ] `packages/contracts` + `packages/domain` suites green + `tsc` clean. ⚠ Write **"typecheck + tests clean; no lint coverage exists in this repo (`lint` is `tsc --noEmit`)"** — never "lint clean."

## Wiring / entry point (Step 7.5)

**`none — wiring lands in 9.41 leg B`** (the worker drill-down resolver).

⚠ **That answer carries the L106 obligation, and it is already discharged:** leg B is a **real tracked leg on `### 9.41`'s checkbox line**, not an intention in prose — which is exactly why I promoted this sub-arc out of 9.10's narrative before briefing it. **Write the SCOPED claim in your commit:** *"the shape is frozen; nothing returns it yet — leg B."* ⛔ **Never** the unqualified *"audit drill-down is now available."*

## Files expected to touch

**Modified / new:**
- `packages/contracts/src/api/ui-safe.ts` — the new interface + its Zod schema + the `UI_SAFE_ALLOWLIST` entry.
- `packages/contracts/src/api/__snapshots__/` (or wherever the ui-safe snapshots live — **confirm the existing convention rather than inventing a path**) — the frozen field-set snapshot.
- `packages/contracts/schemas/` — the generated JSON Schema, if the recipe emits one for ui-safe types.
- `packages/contracts/test/` — the pins.

⚠ **Follow the existing ui-safe convention exactly.** If `UI_SAFE_ALLOWLIST` entries are snapshot-tested somewhere I haven't named, use that; **if the 4-file recipe doesn't apply cleanly to ui-safe types (as opposed to Appendix-A models), flag at Step 2.5** — I'd rather you tell me the recipe differs than have you force it.

## RED test outline (Step 2)

1. **`drill_summary_field_set_is_frozen`** — the field-name set equals the checked-in snapshot.
   - Asserts: `fieldSet(schema) === snapshot`, exactly `["event","occurredAt"]` (order per the existing helper's convention).
   - Why: §10 — a UI-safe projection's field set is the boundary; snapshot-or-it-drifts.
2. **`drill_summary_rejects_the_audit_fields`** — each forbidden field is rejected or absent.
   - Asserts: the schema does not admit `auditRef`/`actor`/`refs`/`payloadHash`/`beforeSummary`/`afterSummary`/`workspaceId` (strict-mode rejection, per the package's `.strict()` posture).
   - Why: ⛔ the arc invariant. **This is the test that makes the leak unrepresentable rather than merely absent.**
3. **`drill_summary_is_registered`** — the schema is in the registry and round-trips.
   - Asserts: registry lookup by id returns it; `registry-all` stays green.
   - Why: an unregistered schema is invisible to the gate that validates candidate data.
4. **`non_vacuity`** — a valid summary is accepted.
   - Asserts: `{event, occurredAt}` parses successfully.
   - Why: a rejection-only suite passes trivially against a schema that rejects everything.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)

- **Model field changes:** a **new** UI-safe type — additive, nothing existing changes shape.
- **Orchestrator doc rows to write hot (Step 9 routing):** the `packages/contracts/CLAUDE.md` cross-doc row + the `ARCHITECTURE.md` Appendix A row for the new type, plus 9.41's leg-A tick. ⛔ **I write those. Do not touch `IMPLEMENTATION_PLAN.md`, `ARCHITECTURE.md`, or `packages/contracts/CLAUDE.md`** — and note that the last one is *shared with me*, so if you need a lesson banked, flag it at Step 9 rather than editing.
- **Shared-contract seam model touched?** <!-- spec-lint:mention --> A UI-safe projection is a cross-boundary surface (worker → renderer), so **the snapshot test IS the seam pin and it is required in this same cycle** — that's acceptance bullet 3.

## Things to flag at Step 2.5

1. **Does the Appendix-A 4-file recipe apply to a `ui-safe` type, or do ui-safe types have their own lighter convention?** **My default vote: follow whatever the neighbouring ui-safe types actually do**, not the Appendix-A recipe by analogy. I deliberately did not pin a snapshot path in Files — **tell me the real convention.** If ui-safe types have no JSON Schema emission, say so and drop that acceptance bullet with a `not-tested-because:` note rather than inventing one.
2. **Naming.** Something like `UiSafeAuditDrillSummary`? **My default vote: mirror the closest existing ui-safe name's morphology** rather than my invention. Cite the neighbour you matched.
3. **Should `event` be a typed union or a string?** `AuditRecord.event` presumably has a type — if so, reusing it couples the UI-safe type to an internal enum, which may be right (fidelity) or wrong (a UI-safe type shouldn't leak an internal vocabulary). **My default vote: reuse the existing type if it is already a closed union of non-sensitive labels; use a narrowed string if it can carry anything free-form.** ⛔ **Establish which it is — do not assume.** This is the one design question in the slice that could quietly widen the surface.

## Dependencies + sequencing

- **Depends on:** nothing. Leg A is unblocked today.
- **Blocks:** leg B (worker resolver) → leg C (desktop affordance) → 9.5's audit-link affordance.
- **Concurrency:** contracts-only. No overlap with worker's live 9.38 slice (`apps/worker/`, `packages/db/`) — verified, that is the file the hold note was about and this leg does not touch it.

## Estimated commit count

**One.** Single area, additive, one focused shape. Not bundled: it is a **frozen-contract surface**, and per the area's conventions a seam-model addition carries its schema + snapshot in the same commit and nothing else.

## Lessons-logged candidates anticipated

- **A "summary, not a reference" projection as a leak-prevention pattern.** Returning a resolved narrow summary instead of an opaque ref means the sensitive handle has no representation on the wire at all — the L103 shape applied to a projection boundary rather than a function signature. **Check L103 and the ui-safe lessons before banking; this may be a worked instance, not a new lesson. Do not bank a duplicate.**
- If Step-2.5 Q3 reveals `AuditRecord.event` is free-form, the fact that a UI-safe type nearly inherited an unbounded string is worth a line.
