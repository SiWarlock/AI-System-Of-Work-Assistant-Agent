# /tdd brief — fold 13.8f-A's partitioned receipt into `buildOutputs.ts`'s meeting KMP (13.8f-B)

## Feature

Give `rewriteVaultForMeeting` its first worker call site and fold the partition it returns into the meeting note's `KnowledgeMutationPlan` — replacing a hardcoded `linkMutations: []`, **arming-gated and dormant.**

## Use case + traceability

- **Task ID:** 13.8f-B
- **Architecture sections it implements:** `ARCHITECTURE.md §9` (workflow-1 / the meeting-closeout producers) · `§6` (KnowledgeWriter, the candidate gate)
- **Related context:** 13.8f-A (`d723a4fc`) — the pure planner and **the merge contract's author** · 13.8d + `apps/worker/src/composition/living-vault.ts` — the SOURCE-path precedent to mirror · 13.8g-B — the attendee leg, **not this slice**

## ⭐ Why this slice matters beyond its own scope

**This is the keystone.** Nothing calls `rewriteVaultForMeeting` in production today, so **knowledge's 13.8m meeting consumer is blocked on you specifically** — it's step 3 of 3 and you're step 1. Desktop is queued behind other worker legs. **The unblocking is the point; the behaviour delivered is deliberately narrow.**

## ⛔ Scope honesty — read before Step 0, because it's easy to over-claim this

**This is dormant plumbing with a narrow real-behaviour sliver.** Established, not assumed:

- **Attendees never reach the correlation step.** `CorrelationSignals` (`packages/workflows/src/activities/correlateMeeting.ts:33-39`) carries only `confidence`, `workspaceId?`, `projectId?`, `reason?` — **no attendee field**, and `packages/workflows/src/ports/meetingCloseout.ts` has none either (grep: 0 hits). ⇒ **the person/attendee leg — the visible "who was in this meeting" win — stays empty until 13.8g-B.**
- **A narrow non-empty path does exist:** `CorrelationSignals.projectId?` could seed a project `EntityRef`, so runs with a resolved project could produce real project-page mutations. Runs without one receipt-empty.
- **13.8f's own Done-when accepts proving this "over faked resolver/planner"** (`IMPLEMENTATION_PLAN.md:1835`) — **dormant/test-only is an acceptable outcome, not a shortfall.**

⇒ **Do NOT write a commit message claiming the meeting path now produces entity-grounded output.** The honest claim is: *the call site exists, gated; the merge contract is honoured; the person leg awaits 13.8g-B.*

## ⭐ The merge contract — quoted verbatim, because it is the spec

`packages/knowledge/src/synthesis/meeting-rewrite.ts:23-35`:

> The receipt is **PARTITIONED so the worker can never double-write**:
> - `meetingNoteLinkMutations` — additive links whose `srcPath` **IS** the meeting note. The worker folds these into the meeting note's OWN `KnowledgeMutationPlan` in `buildOutputs.ts` (the currently hardcoded `linkMutations: []`). **They are REMOVED from `plans`.**
> - `plans` — the entity-page plans (person/project), **already tiered AUTO/PROPOSE**, committed as their own KMPs alongside the meeting KMP.
>
> There is **no `meetingNoteFrontmatter` counterpart BY CONSTRUCTION**: frontmatter is a human-relevant claim edit, so KN-10 tiers it PROPOSE — it can never fold into the meeting note's additive plan. **The worker leaves `frontmatterUpdates: []` there**; the propose plan carries them.

⛔ **So `frontmatterUpdates: []` at `buildOutputs.ts:243` STAYS HARDCODED — permanently, by design.** It is **not** an oversight and **not** part of this slice. Touching it would double-write the thing the partition exists to prevent. ⚠ **If you find yourself wanting to wire it, re-read the paragraph above — that instinct is the defect the contract was written against.**

## ⭐ The dormancy pin does NOT forbid a caller — only an ungated one

This is the fact that makes the slice possible, and it's easy to get backwards.

`packages/knowledge/test/meeting-rewrite.test.ts:421-427`'s `no_production_caller` asserts `ungatedImporters(...)` is empty. Its classifier's own header (`packages/knowledge/test/support/dormancy-pin.ts:1-14`) says it outright: *"Zero importers still passes (today's state); a flag-gated binding passes (tomorrow's); an **UNGATED** binding fails."*

`classifyImporterSource` (`dormancy-pin.ts:85-91`) accepts either a strict `=== true` arming check **in the importing file**, or — the load-bearing branch — a marker:

```
// dormancy-waiver(<task-id>): <where the strict `=== true` gate actually lives>
```

`EXPLICIT_WAIVER = /dormancy-waiver\(\s*[^)\s][^)]*\)/` (`dormancy-pin.ts:63`) — **a non-empty task id is required.**

**The SOURCE-path precedent to mirror, verified as real:** `apps/worker/src/composition/living-vault.ts:6` carries `dormancy-waiver(13.8d): arming runs through boot.ts gateLivingVaultRewrite (strict === true + a non-empty vaultRoot)`, and **`gateLivingVaultRewrite` genuinely exists** — `apps/worker/src/boot.ts:706`, exported, with its own test at `apps/worker/test/boot-living-vault-gating.test.ts`. The shape is **composition-root gate returning `wiring | undefined` on a strict `=== true`; the binding file one hop away carries the waiver marker instead of its own check** — *"logic-in-package, wire-at-boot."*

⚠ **And mirror its honesty too:** `living-vault.ts:11-15` states plainly that `gateLivingVaultRewrite` **has no `bootWorker` call site yet**. If your gate likewise isn't called from boot, **say so in-code in the same voice.** A gate nobody calls is dormant by absence *as well as* by flag, and claiming otherwise is the exact over-claim this project keeps finding.

## Verified starting facts

- `buildOutputs.ts:232-250` builds the meeting KMP as an **inline object literal** (no builder function), with `linkMutations: []` at **`:242`** and `frontmatterUpdates: []` at **`:243`**.
- Types: `linkMutations: LinkMutation[]` / `frontmatterUpdates: FrontmatterPatch[]` on `KnowledgeMutationPlan` (`packages/contracts/src/models/knowledge-mutation-plan.ts:60-61`); `LinkMutation` = `{ op: "add"|"remove"; srcPath: string; dstSlug: string; field?: string }` (`packages/contracts/src/models/shared-shapes.ts:64-72`).
- Receipt fields: `plans` (`meeting-rewrite.ts:98`), `meetingNoteLinkMutations` (`:104`).
- ⚠ **`MeetingBuiltOutputs = { plan, actions }`** — a **single** plan (`buildOutputs.ts:252`, type in `packages/workflows/src/ports/meetingCloseout.ts`). **`receipt.plans` is plural and has nowhere to go today.** See Q1 — the module header does **not** settle this.

## Acceptance criteria (what "done" means)

- [ ] `rewriteVaultForMeeting` has a **real worker call site**, and the importing file carries a valid `dormancy-waiver(13.8f-B): <where the strict === true lives>` marker.
- [ ] **`no_production_caller` still passes** — verified by running it, not by reading the classifier.
- [ ] `receipt.meetingNoteLinkMutations` folds into the meeting KMP's `linkMutations` at `:242`.
- [ ] ⛔ **`frontmatterUpdates: []` at `:243` is UNCHANGED**, and a test or comment records that this is by construction.
- [ ] `receipt.plans` is carried per Q1's resolution — **not silently dropped.** If it can't land this slice, it is a **named tracked follow-up**, not a TODO comment.
- [ ] The binding is **inert without the arming gate** — pinned by a test that the un-armed path produces the pre-slice behaviour (`linkMutations: []`).
- [ ] ⛔ **Nothing logs the receipt.** See the rule-7 note below.
- [ ] `apps/worker` + `packages/workflows` + `packages/knowledge` suites green + `tsc` clean. ⚠ **"typecheck + tests clean; no lint coverage exists in this repo (`lint` is `tsc --noEmit`)"** — never "lint clean."

## Wiring / entry point (Step 7.5)

The meeting-closeout activity chain (`apps/worker/src/composition/buildActivities.ts` registers `meetingCorrelate → … → meetingBuildOutputs → …`) is already live; this slice **widens `meetingBuildOutputs`' behaviour behind an arming gate.** ⚠ **Name explicitly whether your gate is called from `bootWorker` or not** — if not, the capability is dormant by absence as well as by flag, and that must be stated rather than implied.

## Files expected to touch

**Modified:**
- `packages/workflows/src/activities/buildOutputs.ts` — the fold at `:242`.
- `apps/worker/src/composition/**` — the binding + waiver marker (a meeting-path analogue of `living-vault.ts`; **none exists today**).
- `apps/worker/src/boot.ts` — the composition-root gate, if you mirror `gateLivingVaultRewrite`.
- `packages/workflows/src/ports/meetingCloseout.ts` — **only if** Q1 resolves toward widening `MeetingBuiltOutputs`.
- Tests in the touched packages.

⛔ **Not to touch:** `packages/knowledge/src/**` (13.8f-A is done and is the contract's author, not a participant) · `packages/contracts/**` unless Q1 forces a port-type change, which is a **cross-doc invariant → flag at Step 9, don't decide.**

## RED test outline (Step 2)

1. **`armed_binding_folds_meeting_note_link_mutations`** — over a faked planner returning a known `meetingNoteLinkMutations`, the built KMP's `linkMutations` contains them.
   - Why: the merge contract's first clause. **Fake the planner** — 13.8f's Done-when explicitly permits it.
2. **`unarmed_binding_is_byte_identical_to_pre_slice`** — without the arming gate, `linkMutations` is `[]`.
   - Why: dormancy is the safety property. ⭐ **This is the pin that makes "dormant" a fact rather than a claim.**
3. **`frontmatter_updates_stay_empty_even_when_the_receipt_has_plans`** — a receipt carrying entity plans does **not** populate `frontmatterUpdates`.
   - Why: the partition's second clause, and the one an implementer is most likely to "helpfully" break.
4. **`no_production_caller_still_passes_with_the_waiver`** — run the existing pin.
   - Why: it's the gate that permits this slice at all. ⚠ **Confirm the marker's task id is non-empty** — the regex requires it, and an empty one silently fails the branch.
5. **`receipt_plans_are_not_dropped`** — per Q1's resolution.
   - Why: silently discarding the entity-page plans would produce a green slice that delivers half the contract.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)

- **Model field changes:** **none expected.** ⚠ If Q1 resolves toward widening `MeetingBuiltOutputs`, that **is** a port-type change → **flag at Step 9; I write the doc rows.**
- **Orchestrator doc rows:** the 13.8f state line, and 13.8m's blocked-on note **discharged** (this unblocks it).
- **Shared-contract seam model touched?** <!-- spec-lint:mention --> Not unless Q1 widens the port type. `KnowledgeMutationPlan` and `LinkMutation` are **consumed as-is**, not changed ⇒ no snapshot test owed on the expected path.

## Things to flag at Step 2.5

1. ⭐ **THE REAL DESIGN DECISION: where does `receipt.plans` go?** `MeetingBuiltOutputs` models **one** plan; `plans` is plural. Options: **(a)** widen `MeetingBuiltOutputs` to carry sibling plans; **(b)** commit them via a separate downstream call, leaving the port shape alone; **(c)** defer them to a named follow-up and land only the `linkMutations` fold. **My default vote: (b) if a natural downstream commit point exists, else (c).** Reason: **(a)** changes a port type — a cross-doc invariant on a seam consumed by other activities — for plumbing that is dormant anyway, which is a lot of blast radius for a gated path. **(c) is honest and acceptable** *provided* the follow-up is a **tracked task before the round closes**, not a comment. ⛔ **What is NOT acceptable is silently dropping `plans`** — that ships half a contract under a green suite. **The module header does not settle this; tell me what the code makes natural.**
2. **Does your gate get a `bootWorker` call site, or not?** **My default vote: mirror `living-vault.ts` exactly — including its honesty.** If the call site isn't wired, say so in-code in the same voice. **Dormant-by-absence and dormant-by-flag are different claims and the difference has bitten this project repeatedly.**
3. **Is `projectId`-seeded `entityRefs` in scope, or strictly faked ports?** **My default vote: strictly faked ports this slice.** Deriving a real project `EntityRef` from `CorrelationSignals.projectId` is a genuine behaviour change that deserves its own slice and its own reasoning about what a wrong project association would write. **Flag if the fake feels so hollow the fold isn't really exercised** — that would be worth knowing.

## Dependencies + sequencing

- **Depends on:** 13.8f-A (`d723a4fc`) — landed.
- **Blocks:** **13.8m's meeting consumer** (knowledge) · 13.8g-B's attendee threading builds on this call site.
- **Concurrency:** ⚠ **knowledge is live in `packages/knowledge` (13.21) and contract in `packages/{contracts,domain}/test/**` (13.22).** You touch `packages/workflows`, `apps/worker`, and read `packages/knowledge` — **the read is fine; do not edit there.** If you see an unexpected red outside your files, **ask before assuming it's yours.**

## Estimated commit count

**One or two.** One if `plans` resolves via (b)/(c). **A second, separate commit if Q1 forces the port widening** — that's a seam change and belongs on its own for bisectability. ⛔ Not bundled with anything else.

## Lessons-logged candidates anticipated

- ⭐ **A partitioned return value as a double-write defence.** 13.8f-A shaped the receipt so the worker *structurally cannot* write the same mutation twice, rather than documenting "don't double-write." That's L103 applied to a **data shape across a package boundary**, and the `frontmatterUpdates` clause is the interesting half — a field deliberately left un-foldable. **Check L103's family before banking.**
- **Dormant-by-flag vs dormant-by-absence** — if your gate has no boot call site, the honest framing is both, and conflating them is a live over-claim pattern here. Possibly already covered; flag rather than assume.
