# /tdd brief — agent_extraction_end_to_end (#13 Finding C)

## Feature
Make an ARMED subscription extraction actually produce a note end-to-end — the flagged **#13 Finding C**. worker-impl's pre-spend dry-run proved a real run today would SPEND but produce NO note: a source.process job hardcodes `outputSchemaId: KMP`, the worker broker schema-gate registry has no `agent_extraction` parser, so a real `agent_extraction` candidate is `schema_rejected` → `mapAcceptedMeetingExtraction` returns EMPTY → no commit → no note. The GATE-1 machinery already EXISTS (schema 18.11, providers normalizer CP-2, the faithful reconstruction 18.12b — all built + unit-proven, reachability-WAIVERED L11); this slice makes it REACHABLE on the armed path + verifies it end-to-end.

## Use case + traceability
- **Task ID:** 18.27 (#13 Finding C — "route extraction through subscription synthesis")
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (real extraction → note), `§7` (broker schema-gate / candidate-data gate), `§9` (extraction leg), `§16` (REQ-F-017 no-inference — the GATE-1 hard gate)
- **Related context:** `packages/contracts/src/models/agent-extraction.ts` (`AGENT_EXTRACTION_SCHEMA_ID` + `AgentExtractionCandidateSchema` — 18.11/GATE-1) · `packages/providers/src/broker/output-normalizer.ts` `bySchemaIdNormalizer` (already maps `agent_extraction`, CP-2) · `apps/worker/src/composition/meeting-extraction.ts` `mapAcceptedMeetingExtraction` (already faithfully reconstructs value+evidenceRef, CP-2/18.12b — its comment cites "#13 Finding C" as the reachability gate) · `apps/worker/src/composition/backends.ts:589` `CANDIDATE_MODEL_SCHEMAS` (the gap — no agent_extraction) · `buildActivities.ts:795/824-830` (source.process outputSchemaId=KMP + mapCandidate=mapAcceptedMeetingExtraction) · the arm's `withSubscriptionExtractionArming` (the co-gate seam) · doc 097 handoff.

## Acceptance criteria (what "done" means)
- [ ] **Register the agent_extraction schema (b):** add `[AGENT_EXTRACTION_SCHEMA_ID]: AgentExtractionCandidateSchema` to the worker `CANDIDATE_MODEL_SCHEMAS` (`backends.ts:589`) so the broker structural schema-gate ACCEPTS + validates an `agent_extraction` candidate (it currently `schema_rejected`s — "no model parser"). Ships ON but behaviorally DORMANT — the registry entry is inert until a job carries `outputSchemaId = sow:agent-extraction` (armed-path only); pin byte-equivalence for existing KMP/ProposedAction jobs.
- [ ] **Flip outputSchemaId on the ARMED path (a):** on the armed subscription path, the source.process (and meeting.close, for parity) `AgentJob.outputSchemaId` becomes `sow:agent-extraction` — co-gated with `withSubscriptionExtractionArming` / the same arm signal (dormant default stays `KMP`, so the shipped default is byte-equivalent). Pin: unarmed ⇒ outputSchemaId=KMP (byte-equivalent); armed ⇒ agent-extraction.
- [ ] **Confirm the faithful reconstruction end-to-end (c — the SAFETY leg):** with (a)+(b) live, an accepted `agent_extraction` candidate flows: worker schema-gate (validates AgentExtractionCandidateSchema) → `bySchemaIdNormalizer` (kind:agent_extraction) → `mapAcceptedMeetingExtraction` (faithful value+evidenceRef reconstruction) → `validateNoInference` (REQ-F-017 runs on the REAL evidenceRef — a concrete value with no evidenceRef ⇒ rejected; `TBD` ⇒ ok) → note via the sole KnowledgeWriter `applyPlan`. The reconstruction LOGIC is already built + unit-proven (18.12b) — this makes it REACHABLE + adds the end-to-end assertion. Pin: a real-shaped agent_extraction candidate ⇒ a committed note; an inferred (concrete value, no evidenceRef) candidate ⇒ REJECTED, no note (REQ-F-017 fail-closed preserved).
- [ ] **Re-run the spend-free dry-run (d):** the FOCUSED harness (fake completion at the SDK seam, real everything-else) over the armed path ⇒ broker ACCEPTS the agent_extraction candidate + a real note is produced (no schema_rejected, no EMPTY). This is the go/no-go gate for the real run.
- [ ] `/preflight` clean. **security-reviewer MANDATORY** (GATE-1 / REQ-F-017 / candidate-data-gate path). Shipped default byte-equivalent (unarmed ⇒ KMP, no agent-extraction jobs ⇒ the registry entry inert).

## Wiring / entry point (Step 7.5)
The armed subscription path: `withSubscriptionExtractionArming` (or the arm co-gate) sets `outputSchemaId = agent-extraction` on the source.process/meeting.close job when `effectiveArmed`; the worker `CANDIDATE_MODEL_SCHEMAS` (backends.ts) gains the agent_extraction schema (always registered, selected only for agent-extraction jobs). The dormant default (unarmed) keeps KMP ⇒ byte-equivalent. Reachability: the flip + registration are WIRED (they make the already-built 18.11/CP-2/18.12b machinery reachable on the armed path).

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/backends.ts` — register `AGENT_EXTRACTION_SCHEMA_ID → AgentExtractionCandidateSchema` in `CANDIDATE_MODEL_SCHEMAS` (+ the import).
- `apps/worker/src/composition/buildActivities.ts` (or the arm co-gate `withSubscriptionExtractionArming`) — flip source.process (+ meeting.close) `outputSchemaId → agent-extraction` on the armed path (co-gated; dormant default KMP).
- test files: the registry-accepts-agent-extraction test · the armed-vs-unarmed outputSchemaId test · the end-to-end reconstruction+no-inference assertion · the dry-run harness (or fold into an existing arm test).

*(Trace the exact co-gate site — `withSubscriptionExtractionArming` vs the buildProofSpineActivities job assembly — and confirm at Step 2.5.)*

## RED test outline (Step 2)
1. **`registry_accepts_agent_extraction_candidate`** — CANDIDATE_MODEL_SCHEMAS validates a well-formed AgentExtractionCandidate (no more schema_rejected); a malformed one ⇒ schema_rejected (§7/rule 2).
2. **`existing_candidates_byte_equivalent`** — KMP + ProposedAction jobs unaffected by the new registry entry (L23 byte-equivalence).
3. **`armed_flips_outputschemaid_agent_extraction`** / **`unarmed_stays_kmp`** — the co-gate; dormant default KMP (§19.5; L28/L57 armed-vs-default).
4. **`agent_extraction_reconstructs_and_commits`** — an accepted agent_extraction candidate ⇒ faithful value+evidenceRef reconstruction → validateNoInference passes → a committed note (the GATE-1 payoff, end-to-end, REQ-F-017).
5. **`inferred_candidate_rejected_no_note`** — a concrete value with NO evidenceRef ⇒ validateNoInference REJECTS ⇒ no note (REQ-F-017 fail-closed preserved — the load-bearing safety pin).
6. **`dry_run_focused_harness_produces_note`** — the spend-free FOCUSED harness (fake completion) over the armed path ⇒ broker accepts + note produced (the go/no-go for the real run).

## Cross-doc invariant impact
- **Model field changes:** none — `AgentExtractionCandidate` (18.11) + KMP schemas unchanged; this REGISTERS an existing schema + flips a job field value (not a schema change). **Orchestrator doc rows (Step 9):** an `ARCHITECTURE.md §19.5` note (#13 Finding C landed — agent_extraction reachable on the armed path; the maiden-run premise now true) + worker `LESSONS.md` candidate. **§2.5-seam model touched?** No (registering/selecting an existing frozen schema).

## Things to flag at Step 2.5
1. **The co-gate site for the outputSchemaId flip.** Options: (a) inside `withSubscriptionExtractionArming` (mirrors the route/ContextRef co-gate — one arm signal drives all); (b) at the buildProofSpineActivities job assembly gated on the arm signal. My default vote: **(a)** — keep the whole armed-shape change (route + ContextRef + outputSchemaId) behind the ONE arm signal, no split-brain (L52/L61). Confirm the exact seam.
2. **meeting.close parity.** Flip meeting.close's outputSchemaId too, or source.process only (the maiden run)? My default vote: **both** (the arm should route BOTH legs through agent_extraction for consistency; the meeting reconstruction is already built + proven). If meeting.close has a different assembly path that complicates it, source.process-only is acceptable for this slice with meeting.close as a fast-follow — flag it.
3. **Slice split.** (a)+(b) wiring vs (c)+(d) verification. My default vote: **one cohesive slice** — it's the single "make agent_extraction reachable + verify" unit; the REQ-F-017 reconstruction is already built (not new safety code), so a split adds little. security-reviewer covers the whole. Decide with me.

## Dependencies + sequencing
- **Depends on:** 18.11 (agent_extraction schema), CP-2 (providers normalizer), 18.12b (faithful reconstruction), the arm (18.24/18.25/18.26 — the co-gate seam). All landed.
- **Blocks:** the maiden real run (the run-go premise "a real extraction produces a note" becomes true only after this).

## Estimated commit count
**1** (cohesive: register + flip + end-to-end verify). security-reviewer MANDATORY (GATE-1/REQ-F-017). Split to 2 only if Step-2.5 surfaces a reason.

## Lessons-logged candidates anticipated
- **Convention candidate** — the arm build must include the outputSchemaId flip + the worker schema-registration, or the armed extraction spends-and-produces-nothing (schema_rejected → EMPTY → no note); the pre-spend dry-run (fake completion, real everything-else) is the go/no-go that catches a downstream candidate→note gap the spend-free CP2 arm-verification cannot.
- **Architecture-doc note candidate** — §19.5: #13 Finding C landed; agent_extraction is reachable on the armed path; the GATE-1 reconstruction (18.12b) now runs end-to-end.

## How to invoke
1. Read end-to-end; trace the co-gate site (Step-2.5 #1) + confirm the meeting.close parity call (#2) before tests.
2. `/tdd agent_extraction_end_to_end`.
3. Step 2.5 — the co-gate site + parity + split decisions. Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
4. **Step 8 — security-reviewer MANDATORY** (REQ-F-017: the inferred-candidate-rejected pin is load-bearing — a concrete value with no evidenceRef MUST be rejected, never committed).
5. Step 9 — surface the §19.5 note + worker LESSONS candidate + the dry-run result (broker accepts + note produced). Then the real run is unblocked (routine, per the owner recalibration).
