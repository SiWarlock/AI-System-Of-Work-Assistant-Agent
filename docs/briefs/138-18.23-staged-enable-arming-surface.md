# /tdd brief — staged_enable_arming_surface

## Feature
Build the DORMANT staged-ENABLE arming surface + its rule-5 safety proof: the AND-locked `selectExtractionRoute` route knob (default local, cloud `{runtime}` only when armed), the rule-5 fail-closed verification for the cloud `{runtime}` route, the `ANTHROPIC_API_KEY`-unset boot guard, the extraction cost cap, and the `HealthGateSources` wrap over the 18.22 probe. Every piece stays default-OFF / fail-safe — the shipped default is byte-equivalent; **nothing arms** (the gate flip is the owner's step 6, HARD STOP — not this round).

## Use case + traceability
- **Task ID:** 18.23
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (real transport / arming surface), `§7` (broker HEALTH/BUDGET; the two ports), `§5` (egress veto — the rule-5 proof)
- **Related context (staged ENABLE — steps 0,2,3b,4,5):** owner GO on ENABLE steps 0–5 ONLY. **⛔ HARD STOP before step 6 (arm the gate) + step 7 (real run).** Do NOT set `config.providerTransport`, do NOT flip the route knob armed, do NOT run real extraction, provision NOTHING (no API key). This slice builds the pieces so the owner's step-6 flip is a single arming action; every piece is default-OFF and the shipped default byte-equivalent.
- **Depends on 18.22** (`docs/briefs/137-…`): the barrel-exported `probeClaudeSubscriptionHealth` + its verdict type (for the step-3b wrap).

## Acceptance criteria (what "done" means)
**Step 4 — `selectExtractionRoute(armed: boolean): ProviderRoute` route knob:**
- [ ] A pure default-OFF helper (mirror `selectProviderRunner`/L23): `armed === false` (default) ⇒ the shipped `{provider:"ollama", model:"local-default", endpoint:"http://127.0.0.1:11434", egressClass:"local"}` route (byte-identical to `boot.ts:1094`); `armed === true` ⇒ the cloud `{runtime:"claude-agent-sdk", model:<subscription model>, endpoint:"https://api.anthropic.com", egressClass:"cloud"}` route. STRICT `=== true` (a truthy-not-`true` value ⇒ local, never arms). AND-locked to the SAME arming as `providerTransport` (one flip; no split-brain — Step-2.5 #1).
- [ ] The shipped `capabilityDefaults` (`boot.ts:1094`) is unchanged in the default (still `source.process → local`); the armed set adds `meeting.close`. Prove byte-equivalence: default boot's matrix is unchanged (a factory/default-spy or a `.toStrictEqual` on the default route).

**Step 5 — rule-5 fail-closed VERIFICATION (SAFETY — the crux):**
- [ ] Add a cloud `{runtime}` route case to `apps/worker/test/composition/egress-veto-assembled.test.ts` (mirror the 18.9 pattern): drive the REAL `assembleBackends({})` broker with an employer-raw job + ack-OFF egress + a `runtimeCloudRoute = {runtime:"claude-agent-sdk", model:"claude-…", endpoint:"https://api.anthropic.com", egressClass:"cloud"}` ⇒ assert `isErr`, `error.stage === "egress_veto"`, `error.reason === "EMPLOYER_RAW_EGRESS_UNACKNOWLEDGED"`, **no local fallback** (the DENY never reaches the run leg ⇒ works dormant, no subscription runner needed). The predicate is already route-shape-agnostic (`processorOfRoute` keys off `egressClass`/processor, not provider-vs-runtime) — this pins the assembled-broker coverage gap.
- [ ] **⛔ If this does NOT fail closed → STOP + escalate as a rule-5 Finding** (do not proceed / do not paper over).

**Step 0 — `ANTHROPIC_API_KEY`-unset guard:**
- [ ] On the armed subscription path (co-gated with `providerTransport` arming — reached only when armed), assert/refuse `process.env.ANTHROPIC_API_KEY === undefined` (a stale key shadows the subscription by resolution precedence). A set key on the armed path ⇒ a fail-closed refusal (typed, boot-visible) — NOT a silent raw-API fallback. Dormant: on the default (unarmed) path the guard is never reached (byte-equivalent). Pin the guard fires only when armed + only when the key is set.

**Step 2 — cost cap:**
- [ ] Add `perCapability["source.process"]` (and `"meeting.close"`) `= { maxCostUsd: 1.5, maxRuntimeSeconds: <keep the existing/appropriate> }` to `DEFAULT_BUDGET_DEFAULTS` (`apps/worker/src/composition/budget-ledger.ts:65`). Deny-only/fail-safe — ships without arming (it only lowers the cap; the SDK `maxBudgetUsd` in-flight cap for the runtime route, Finding-F). Pin: `resolveEnforcedBudget` for a `source.process` job now yields `maxCostUsd:1.5`. **The value is a re-confirm-at-final-spend placeholder — comment it as such (owner sets the exact value at step 7).**

**Step 3b — health wrap:**
- [ ] `createSubscriptionHealthSources(probe): HealthGateSources` wrapping the 18.22 `probeClaudeSubscriptionHealth` into `{ health, availability }`: a healthy verdict ⇒ `{state:"healthy"}` / `{modelPresent:true, conformanceStatus:"passing"}`; an unhealthy verdict ⇒ a non-healthy `health.state` (⇒ the HEALTH gate denies) + fail-closed `availability`. Rides `gate.healthSource` ONLY (never `config.healthSources` — L52). Dormant (only selected on the armed path).

**General:**
- [ ] `/preflight` clean. Shipped default byte-equivalent (no arming; factory/default-spy where a seam exists). No cross-doc invariant change (route knob / health wrap / cost cap / auth guard are internal worker seams; `ProviderRoute`/`ProviderMatrix` unchanged).

## Wiring / entry point (Step 7.5)
All pieces are consumed at the **owner ENABLE flip (step 6, HARD STOP — NOT this round)**: `selectExtractionRoute(true)` feeds the armed `capabilityDefaults`; `createSubscriptionHealthSources` feeds `buildRealProviderTransportGate.healthSource`; the auth guard fires on the armed boot path; the cost cap is already live (deny-only). The rule-5 verification is a TEST (its own entry point — the assembled broker). **none arm this slice; the flip is the #13 ENABLE.** Reachability-WAIVERED (L11) for the route knob / health wrap / auth guard.

## Files expected to touch
**New:**
- `apps/worker/src/composition/extraction-route-gate.ts` — `selectExtractionRoute` (+ the cloud/local route constants).
- `apps/worker/src/composition/subscription-health-sources.ts` — `createSubscriptionHealthSources`.
- test files for each + the auth-guard test.

**Modified:**
- `apps/worker/src/composition/budget-ledger.ts` — the `DEFAULT_BUDGET_DEFAULTS` cost-cap entries.
- `apps/worker/src/boot.ts` — the `ANTHROPIC_API_KEY`-unset armed-path guard (+ the armed-route wiring stays behind the OFF gate).
- `apps/worker/test/composition/egress-veto-assembled.test.ts` — the cloud `{runtime}` rule-5 case.

## RED test outline (Step 2)
1. **`route_knob_default_is_shipped_local`** / **`route_knob_armed_is_cloud_runtime`** / **`route_knob_truthy_not_true_stays_local`** — the route knob (§5/§19.5; L28 truthy guard).
2. **`assembled_broker_denies_employer_raw_cloud_runtime`** — the rule-5 SAFETY proof (§5): employer-raw + ack OFF + cloud `{runtime}` ⇒ DENY `EMPLOYER_RAW_EGRESS_UNACKNOWLEDGED`, no local fallback. **The load-bearing test.**
3. **`anthropic_key_set_on_armed_path_refuses`** / **`anthropic_key_unset_on_armed_path_ok`** / **`default_path_never_checks_key`** — the auth guard (§19.5; dormant-on-default).
4. **`source_process_cost_cap_is_1_5`** — `resolveEnforcedBudget` for a `source.process` job ⇒ `maxCostUsd:1.5` (§7 COST-1).
5. **`health_wrap_healthy_and_unhealthy`** — the verdict→`HealthGateSources` mapping; unhealthy ⇒ non-healthy `health.state` (§7).
6. **`shipped_default_byte_equivalent`** — default boot constructs no cloud route / no armed health source / no key check (byte-equivalent; L23/L27).

## Cross-doc invariant impact
- **Model field changes:** none. **Orchestrator doc rows to write hot (Step 9):** an `ARCHITECTURE.md §19.5` note (the staged arming surface + the rule-5 cloud-runtime proof) + worker `LESSONS.md` candidate(s). **Shared-contract seam model touched?** No.

## Things to flag at Step 2.5
1. **Route-knob arming coupling (the load-bearing design Q).** The cloud `{runtime}` route must arm together with `providerTransport` (one flip, no split-brain — L52). Options: (a) a separate `selectExtractionRoute(armed)` knob AND-locked to the same arming signal the owner sets at step 6; (b) fold the route into `ProviderTransportGate`. My default vote: **(a) a distinct route knob AND-locked to the same arming flag** (per the recon: the route + transport gate are independent today; do NOT overload `providerTransport` to double as the route switch — keep them distinct but flipped together). Confirm how the owner's step-6 flip drives BOTH (the arming signal that both read).
2. **Auth-guard placement + failure mode.** Where in `bootWorker` + what does "refuse" mean — a typed boot error, a degraded HealthItem, or a hard throw? Default vote: **a fail-closed refusal that surfaces (a boot-visible typed error / degraded), NOT a silent fallback** (§16 — prefer degrade+surface over crash where possible; but a mis-provisioned armed path SHOULD be loud). Co-gate with the arming so the default path never reads env.
3. **Slice may SPLIT.** This bundles 5 dormant pieces. If it's too big for one clean commit/review, split at Step 2.5 into **(A) route knob + rule-5 proof + auth guard** (the egress/auth SAFETY commit, security-reviewer) and **(B) cost cap + health wrap** (the mechanical dormant knobs). My default vote: **split A/B** — A is the safety-critical commit, B is mechanical. Decide with the orchestrator.

## Dependencies + sequencing
- **Depends on:** 18.22 (the health probe, for the 3b wrap), 18.1/18.18a (the gate + `buildBackendsConfig`).
- **Blocks:** the owner ENABLE (#13 steps 6–7).

## Estimated commit count
**1–2.** Prefer **2** (split per Step-2.5 #3): (A) route knob + rule-5 proof + auth guard = the SAFETY commit (security-reviewer MANDATORY); (B) cost cap + health wrap = the mechanical dormant knobs. Never bundle the safety pieces with unrelated work.

## Lessons-logged candidates anticipated
- **Convention candidate** — the staged-ENABLE arming surface: an AND-locked route knob (one flip arms transport + route, no split-brain), the rule-5 proof for a cloud `{runtime}` route (the veto is route-shape-agnostic), the armed-path `ANTHROPIC_API_KEY`-unset guard, all default-OFF/byte-equivalent.
- **Architecture-doc note candidate** — §19.5: the staged arming surface built dormant; step-6 flip drives the AND-locked route+transport arming.

## How to invoke
1. Read this brief end-to-end; trace the arming-coupling (Step-2.5 #1) + decide the split (Step-2.5 #3) before tests.
2. `/tdd staged_enable_arming_surface`.
3. Step 2.5 — send the test design + the 3 decisions (arming coupling, auth-guard failure mode, split). Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
4. **Step 8 — security-reviewer MANDATORY** (route/egress + auth + gate-wiring). If the rule-5 test does NOT fail closed → STOP + escalate as a Finding.
5. Step 9 — surface the §19.5 arch note + worker LESSONS candidate(s) + the #13 step-6/7 preconditions.
