# /tdd brief — rebuild_replaced_strict_equality (knowledge-track false-green hardening)

## Feature
Harden the wholesale-replace gate in `rebuildIndexFromMarkdown` against a truthy-non-`true` `replaced` value: change `packages/knowledge/src/gbrain/rebuild.ts:160` from the truthy check `if (!receipt.replaced)` to strict `if (receipt.replaced !== true)`, and pin it with a **guard test** (mirror worker Lesson 28). Surfaced by impl22's security review of the rebuild-oracle producer: once the REAL `IndexRebuildClient` binds at arming, a client returning a truthy-non-`true` `replaced` (e.g. `1`, `"false"`, `{}` across the I/O boundary — TS types aren't runtime-enforced) would SLIP the non-replacing-rebuild guard, letting a merge-not-replace rebuild reach a success `Result` ⇒ a **false-green into the serve-time trust gate** (the rebuild-oracle `oracleBuildOk` leg corroborates `true` over an unsound rebuild). This is the exact trust-gate false-green class the propose guard `392e7db` proactively hardened.

**Not live today** (the real client is UNBOUND — the rebuild-oracle producer arc keeps it unbound). This is an **arming-gate blocker**: it must land before any real `IndexRebuildClient` is bound. Byte-equivalent for every well-typed `replaced: boolean` — it only tightens the truthy-non-`true` edge.

## Use case + traceability
- **Task ID:** 13.10 (Copilot serving-oracle go-live arc — trust-gate false-green hardening; arming-prep).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (GBrain rebuild-from-Markdown — the WHOLESALE-REPLACE disposable-brain invariant, safety rule 1: a non-replacing rebuild could leave a quarantined DB-only fact in retrieval) + `§12` (fail-closed parity / GO conditions).
- **The exact site (`packages/knowledge/src/gbrain/rebuild.ts:159-169`):** step 4 — `if (!receipt.replaced) { return err({ code: "non_replacing_rebuild", healthItem: … }) }`. The receipt comes from the INJECTED `IndexRebuildClient.rebuildFromMarkdown` (`rebuild.ts:61-65`), i.e. an external-I/O boundary at arming — so the runtime value can violate the `IndexRebuildReceipt.replaced: boolean` type.
- **Precedent (mirror it):** worker **Lesson 28** — every owner-gated arming `=== true` check earns a truthy-not-`true` regression guard (incl. the string `"false"`); a behavioral test with a co-located positive control. The propose/serving-oracle strict-equality guard `392e7db` is the sibling.

## Acceptance criteria (what "done" means)
- [ ] `rebuild.ts:160` reads `if (receipt.replaced !== true)` (strict), not `if (!receipt.replaced)`.
- [ ] A well-typed `replaced: true` still passes the gate (positive control — the wholesale-replace path is unchanged; a genuine replace still reaches the recovery-count check + `ok`).
- [ ] A well-typed `replaced: false` still yields `non_replacing_rebuild` (unchanged).
- [ ] **NEW guard:** a truthy-non-`true` `replaced` (parametrized: `1`, `"true"`, `"false"`, `{}`, `[]` — cast through the fake client) yields `non_replacing_rebuild` (NOT a success `Result`, NOT the recovery check) — the false-green vector is closed. `"false"` is the sharpest (truthy string).
- [ ] In-code comment names WHY strict (a truthy-non-`true` `replaced` from the external client boundary must never be read as a wholesale replace — safety rule 1 / Lesson 28).
- [ ] `/preflight` clean; repo-wide green. Byte-equivalent for all boolean inputs.

## Wiring / entry point (Step 7.5)
**none** — a strict-equality tightening of an already-wired, already-reachable check inside `rebuildIndexFromMarkdown` (reachable from the rebuild-oracle producer + the reconcile oracle path). No new production symbol. State the no-new-wiring note.

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gbrain/rebuild.ts` — the one-line strict `=== true` + the in-code WHY comment.

**New / Modified (tests):**
- `packages/knowledge/test/rebuild*.test.ts` (the existing rebuild test file, or a new one) — the truthy-not-`true` guard + the `true`/`false` positive/negative controls.

## RED test outline (Step 2)
1. **`non_replacing_strict_rejects_truthy_replaced`** — parametrized truthy-non-`true` `replaced` (`1`, `"true"`, `"false"`, `{}`, `[]`) via a fake `IndexRebuildClient` returning `{ ..., replaced: <val> as unknown as boolean }` ⇒ `err.code === "non_replacing_rebuild"`. FAILS today (`!receipt.replaced` is falsy for a truthy value ⇒ slips to the recovery check / ok).
2. **`replace_true_still_recovers`** (positive control) — `replaced: true` + `nodeCount === facts.length` ⇒ `ok` (the wholesale-replace path unchanged; guard is non-vacuous).
3. **`replace_false_still_rejects`** (negative control) — `replaced: false` ⇒ `non_replacing_rebuild` (unchanged).

## Cross-doc invariant impact (implementer flags at Step 9)
- **Model field changes:** NONE. `IndexRebuildReceipt` unchanged; this is a runtime strictness fix.
- **Orchestrator doc rows (Step 9):** possibly a knowledge-track LESSONS addendum (the Lesson-28 pattern applied in the knowledge layer) — orchestrator writes if durable. Likely just references worker Lesson 28.
- **Seam (shared-contract) model touched?** No.

## Things to flag at Step 2.5
1. **Truthy value set.** Default vote: `1`, `"true"`, `"false"`, `{}`, `[]` — include the string `"false"` explicitly (truthy ⇒ the sharpest silent false-green vector; the most convincing strictness proof). Mirror `392e7db`.
2. **Test file placement.** Default vote: extend the existing `rebuild` test (co-locate the guard with the wholesale-replace coverage) rather than a new file; flag if the existing file's fake-client harness doesn't already exist.
3. **Any sibling truthy checks in rebuild.ts?** Default vote: scope to `:160` only (the audit-named site). Note (don't fold in) if `receipt.nodeCount`/other fields have a similar truthy-vs-strict exposure — a separate flag, not this slice.

## Dependencies + sequencing
- **Depends on:** nothing — `rebuild.ts` exists at HEAD. **Independent of piece C** (worker) — can run fully in parallel (different package/track).
- **Blocks:** the owner ARMING GATE — this MUST land before any real `IndexRebuildClient` is bound (arming-gate blocker).

## Estimated commit count
**1** — one-line fix + guard test. **Mandatory dual review (security-reviewer [invariant-touching — trust-gate false-green] + code-quality-reviewer [every-slice]).** Not bundled.

## Lessons-logged candidates anticipated
- **Convention candidate:** likely just a knowledge-track reference to worker Lesson 28 (the strict-`=== true` arming-guard pattern, now applied to the rebuild wholesale-replace gate at an external-I/O boundary). Flag at Step 9 if it reads durable as its own knowledge lesson.
- **Architecture-doc note candidate:** none expected.
