# /tdd brief — asana_context7_verify (connector correctness pass — round 3 slice 3; owner directive)

## Feature
**Verify the built Asana connector against the authoritative Context7 wire shape, and correct any drift.** The round-2 Asana adapter (`createAsanaHttpTransport`/`asanaMapPage`/`asanaBuildQuery`/`asanaContentHash`, sealed `47c55c2`) was authored from training memory BEFORE the Context7-grounding directive. This slice diffs it field-by-field against the authoritative Asana docs on Context7, corrects any drift as a proper TDD change, and produces a per-field CONFORMANT/CORRECTED verdict. Owner directive: candidate-accuracy hygiene so arming is smooth + the built code is right. Still DORMANT — candidates only, real transport UNBOUND, no hard line.

## Use case + traceability
- **Task ID:** 13.12 (connector real-transport go-live — the owner correctness pass; runbook Phase 8).
- **Architecture sections it implements:** `ARCHITECTURE.md §8` (Connector & Tool Gateways — the Asana connector-transport instance) + `§5` (EgressPolicy / SSRF — the Asana host allowlist entry).
- **Verifies (built, `47c55c2`):** `createAsanaHttpTransport` + `ASANA_HTTP_SPEC` + `asanaMapPage` + `asanaBuildQuery` + `asanaContentHash` in `packages/integrations/src/connectors/adapters/asana.ts`.
- **Authoritative source (Context7-first, per the process directive):** `resolve-library-id "Asana API"` → `query-docs` (the tasks/list + task-get endpoints); **WebFetch `developers.asana.com`** ONLY as a fallback if Asana is not indexed. Never training memory.

## Step 1 — the field-by-field diff (the investigation; do FIRST)
Resolve Asana on Context7 and diff the built candidate against the authoritative shape on each axis. Report matched vs drifted CONCRETELY (not a hand-wave):
- **base host** (the SSRF `allowedHosts` entry — is it the real Asana API host, e.g. `app.asana.com`?)
- **auth** (`Authorization: Bearer <PAT>` — the least-privilege header)
- **list + get endpoint paths** (e.g. `GET /api/1.0/tasks`, `GET /api/1.0/tasks/{gid}`)
- **query params** (the list params — `opt_fields`, `limit`, project/assignee/workspace scoping)
- **pagination** field names (offset-based `offset` + the `next_page` envelope: `{ offset, path, uri }`)
- **response → candidate map** (`{ data: [...], next_page }` → items; `data[]` element shape)
- **resource id / gid shape** (the task `gid`)
- **status → typed-fault map** (401/403/429 → the `TransportFailure` codes)

## Step 2 — the outcome
- **CONFORMANT on an axis** ⇒ no code change on that axis; record it in the verdict (the field checked + that it matches). Update the in-code `arch_gap` comment to cite the Context7 grounding (`resolve-library-id`'d source) so the candidate is no longer "memory-only."
- **DRIFT on an axis** ⇒ a **proper TDD correction**: a RED test pinning the CORRECTED candidate (the mapper/query/host/fault-map field), then the fix, GREEN, dual-reviewed. Each corrected field gets a test; the correction stays fail-closed (a candidate, never hardcoded-confirmed — confirmed at arming).

## Acceptance criteria (what "done" means)
- [ ] The built Asana candidate is diffed against the Context7-resolved Asana shape on ALL eight axes above; each axis is recorded CONFORMANT (field cited) or CORRECTED (delta cited).
- [ ] **If CONFORMANT overall:** no behavior change; the in-code `arch_gap` comment is updated to cite the Context7 source (the candidate is now Context7-grounded, not memory-only). A light test/assertion may pin the confirmed candidate shape if useful; else the diff + verdict suffices (state `not-tested-because: no behavior change`).
- [ ] **If DRIFT:** each corrected field is pinned by a RED-first test (the mapper/query/host/fault delta), fixed, GREEN; the corrected candidate stays fail-closed + `arch_gap`-documented. Existing Asana tests stay green (or are updated to the corrected candidate with a note).
- [ ] A per-field **VERDICT** for the Step-9 report: `CONFORMANT (fields: …)` or `CORRECTED (deltas: …)`.
- [ ] Still DORMANT — real transport UNBOUND, byte-equivalent (a candidate-map/query change is a dormant-candidate refinement, not a boot change). `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none** — a verify (+ possible candidate correction) of an already-built, reachability-waivered dormant adapter. No new production symbol; no new wiring. State it.

## Files expected to touch
- `packages/integrations/src/connectors/adapters/asana.ts` — the `arch_gap` comment (Context7 citation) + any corrected candidate field (spec/mapPage/buildQuery/host/fault-map) IF drift.
- `packages/integrations/test/connector-http-transport.test.ts` (or the Asana test file) — a corrected/added test per drifted field IF drift.

## RED test outline (Step 2 — conditional on drift)
- **On drift:** one RED test per corrected axis — e.g. `asana_list_maps_corrected_shape` (the real `data[]`/`next_page` fields), `asana_pagination_offset` (the real offset param), `asana_status_fault_map` (the real 401/403/429 → codes). Each fails against the drifted candidate, passes after the correction.
- **On conformant:** no new RED (the diff is the deliverable); optionally an assertion pinning the confirmed candidate shape so a future drift is caught. State `not-tested-because` for any acceptance bullet with no behavior change.

## Things to flag at Step 2.5
1. **Asana Context7 availability.** If `resolve-library-id "Asana API"` finds no High-reputation match, WebFetch `developers.asana.com` (the tasks endpoints) as the authoritative fallback + note the source used. Confirm the source at Step 2.5.
2. **Scope of correction (if drift).** Correct ONLY candidate-accuracy drift (host/path/params/pagination/map/fault) — do NOT change the template, the SSRF machinery, or add behavior beyond candidate alignment. Confirm.
3. **Conformant-path deliverable.** If fully conformant, the deliverable is the verdict + the Context7-citation comment update (a doc-only change) — no gratuitous test. Confirm that's acceptable vs adding a shape-pinning assertion.

## Cross-doc invariant impact (implementer flags at Step 9)
- **Model field changes:** NONE (candidate refinements are integrations-internal; the frozen seams are untouched).
- **Orchestrator doc rows (Step 9):** the per-connector VERDICT (Asana CONFORMANT/CORRECTED) → I fold into the round-close report + the `§8` note. A providers LESSONS candidate only if a durable "ground candidates on Context7, verify built ones" rule reads worth banking.
- **Seam (shared-contract) model touched?** No.

## Dependencies + sequencing
- **Depends on:** the built Asana adapter (`47c55c2`) + Context7 access. Round-3 slice 3 (after Drive + Calendar). CLOSES the round-3 correctness pass.
- **Blocks:** nothing. The per-connector verdicts feed the round-3 close report.

## Estimated commit count
**1** — the verdict + (conformant) the Context7-citation comment OR (drift) the corrected candidate + tests. **Mandatory dual review** if any code/candidate change (external-I/O candidate accuracy = security-relevant); a pure-verdict-no-change path still gets a code-quality skim. Not bundled.

## Lessons-logged candidates anticipated
- **Convention candidate:** possibly "ground every connector candidate on Context7 at authoring, and back-verify any built pre-Context7" — flag at Step 9 if it reads durable (providers LESSONS).
- **Architecture-doc note candidate:** none beyond the `§8` verdict fold.
