# /tdd brief — ws8_unswept_surface_residual

## Feature

Close `### 24.79`'s **recorded residual**: the WS-8 "by construction" overstatement sweep was bounded to
`apps/worker/src`, and **five named WS-8-subject claims inside worker rest on DIFFERENT, untraced mechanisms**.
Establish their status in both directions and correct what is overstated.

⛔⛔ **READ THIS FIRST — `### 24.79` ITSELF IS DONE. YOU ARE NOT REDOING IT.** Its checkbox is `[ ]` but its
slice **shipped at `5f18f894`** ("four by-construction WS-8 claims corrected"). I verified in the tree, not from
the note: `copilotGbrainSubprocess.ts:10` now says *"BY CONSTRUCTION: exactly ONE workspace"*, `:15` says
*"⛔ NOT by construction: ANY REGISTERED workspace reads the one combined brain"*, `:217` says *"by SCOPE
FILTERING, not by construction"*, and `copilotClaudeSynthesis.ts:449-451` is branch-qualified. **The four
dispatched sites are fixed. This slice is the part that was explicitly left unexamined.**

## Use case + traceability

- **Task ID:** `### 24.79` (residual leg; residual origin `#68` Step 9 — ⚠ that `#NN` is DEAD, cite the `###`)
- **Architecture sections it implements:** `ARCHITECTURE.md §5`
- **Safety:** ⚠ **rule 4 — a false assurance ABOUT rule 4.** Step-9 flags route to me AND the lead (all seven auto-route).
- **Depends:** `### 24.79` main leg (SHIPPED `5f18f894`) · **Blocks:** —

> ⚠ **This brief widens phase scope because** `§5` is not in Phase 24's declared anchor set
> (`§13 §19.11 §3 §4 §6`) — **and `### 24.79` itself declares `Spec: §5`, so the task and its phase disagree.**
> ⛔ **NOT resolved by adding `§5` to Phase 24's set** — the resolution the lead ruled against on `### 24.54`.
> ⭐ **This is at least the FIFTH consecutive brief this round to declare a widening** (280, 281, 282, 283, this)
> ⇒ **the recurring declaration is itself a finding about Phase 24's anchor set, not about these briefs.**
> Routed to the orchestrator as a Step-9 observation; **do not act on it in this slice.**

## ⛔ THE RESIDUAL, QUOTED FROM THE TRACKER SO YOU INHERIT ITS BOUNDARY AND NOT MY PARAPHRASE

> **Sweep boundary was `apps/worker/src` ONLY; NOT swept: `packages/**`, `apps/desktop/**`, tests, docs.**
> **Five WS-8-subject claims elsewhere in worker rest on DIFFERENT mechanisms that were not traced —**
> `copilotBriefing.ts:40` and `:109`, `copilotGbrainRetrieval.ts:10`, `readModel.ts:515`, `boot.ts:2184`.
> ⛔ **Their status is UNKNOWN in both directions.**

⛔⛔ **"UNKNOWN IN BOTH DIRECTIONS" IS THE LOAD-BEARING PHRASE AND IT BINDS YOUR REPORT.** These are **not**
known-bad sites awaiting a fix. Some may be correct. **A sweep that returns "all five were fine" is a real and
valuable result — but only if it says how it established that** (`L100`: an absence claim must specify the search
that establishes it). ⛔ **Do not report a site as correct because it reads plausibly; trace its mechanism.**

## ⭐ THE METHOD IS ALREADY DECIDED, AND GETTING IT WRONG IS THE FAILURE MODE

⛔⛔ **CLASSIFY BY SUBJECT, NEVER BY PHRASE. The tracker measured this and it is the whole trap:**
> **~60 `by construction` occurrences in `apps/worker/src` were overwhelmingly a DIFFERENT SUBJECT** —
> redaction-safe, idempotent, traversal-safe, UI-safe, byte-equivalent.
⇒ ⭐ **A phrase census is a CANDIDATE LIST; the subject classification is the work.** A grep for
`by construction` will hand you ~60 rows of which the large majority are irrelevant, and **the count is not the
finding.** Report the candidate count and the classified count as **two different numbers with their units**.

⛔ **The converse also binds: a site can overstate WS-8 without containing the phrase `by construction`.** The
subject is *"does this claim that workspace isolation holds more strongly than its mechanism delivers?"* — any
wording. **Selecting on the phrase alone reproduces measurement mechanism 5 (grouping by SPELLING, not by KIND).**

⛔ **Redirect-then-measure, and report the non-vacuity control.** Never `head` a list you will total; a pipe
truncates silently and under-reports. ⚠ **`grep` in this environment has fabricated row counts and emitted
`path:0:` rows it cannot produce (`### 24.87`) — I hit it twice this session.** Redirect to a file and measure
the file. Do **not** use `codegraph_callers` as a census (`L157`: a hit is evidence, an empty is a question).

## ⭐ THE POSITIONAL PATTERN — USE IT TO PRIORITISE, NOT TO CONCLUDE

`### 24.79` banked this as an `L161` amendment:
> **THREE OF THE FOUR DEFECTIVE SITES WERE THE ENTRY DOC FOR THEIR SCOPE** (field doc · file header · config
> doc); **the one that stayed correct was the INLINE COMMENT NEXT TO THE CODE.**
> ⇒ *a new branch lands beside the code, so the author updates the comment they are already editing —
> **THE ENTRY DOC IS NOBODY'S DIFF.*** The doc most likely to be stale is the one most likely to be read first.
⇒ **Check file headers, exported-symbol docs, and operator-facing config docs FIRST.** ⚠ **But this is a
prior, not a verdict — an inline comment can still be wrong, and finding the pattern confirmed is not evidence
you have finished.**

## Acceptance criteria

- [ ] **All five named sites are dispositioned** — `copilotBriefing.ts:40`, `copilotBriefing.ts:109`,
      `copilotGbrainRetrieval.ts:10`, `readModel.ts:515`, `boot.ts:2184` — each either **traced to its mechanism
      and confirmed accurate**, or **corrected to be branch-qualified**. ⛔ Neither disposition may rest on how
      the comment reads; each cites the mechanism it traced.
- [ ] **The sweep's method, non-vacuity control, and boundary are reported**, with candidate count and classified
      count as **separate numbers with their units**.
- [ ] ⛔ **Any site left unexamined is recorded as UNEXAMINED, explicitly NOT as correct** (`L100`) — this slice
      must not repeat the defect it exists to close by shipping a silent new boundary.
- [ ] **`L153` half-2 check on every site touched:** is the text DESCRIPTIVE or does it INSTRUCT? An imperative
      can invert when corrected — flag any instructing variant rather than editing it in place.
- [ ] ⛔ **A correction that removes a true statement owes it a replacement** (`L153` half 1). `### 24.79` hit
      exactly this: a header carried **the only in-repo statement of the real long-term limitation** and it was
      preserved verbatim while the false claim around it was fixed. **Striking the block would have discarded it.**
- [ ] **Zero logic delta proven, not asserted** — strip every `+`/`-` line, filter comment-only lines, show the
      non-comment residue is EMPTY. Report added/removed counts.
- [ ] `/preflight` clean; **`turbo typecheck lint --force`** with `0 cached` reported. ⚠ **A cached typecheck is
      not a typecheck** — I replayed a 55ms fully-cached green suite this session and it measured nothing.

## Wiring / entry point (Step 7.5)

**None — comment/doc correctness only. No production surface changes, no new export, nothing becomes reachable.**
This is not a `none — wiring lands in <slice>` deferral; there is no wiring to defer.

## Files expected to touch

**Modified (expected):** `apps/worker/src/api/procedures/copilotBriefing.ts` ·
`apps/worker/src/api/procedures/copilotGbrainRetrieval.ts` · `apps/worker/src/**/readModel.ts` ·
`apps/worker/src/boot.ts` — **only where a claim is traced and found overstated.**
⚠ **Paths above are the tracker's citations; resolve each by REPO-RELATIVE PATH before editing.** This repo has
duplicate basenames across sibling directories and a path-less citation already produced one confident false
negative here. If a cited line does not resolve, that is **a question, not a verdict** (`L93`).
⛔ **NOT in scope:** `packages/**`, `apps/desktop/**`, tests, docs. If you believe the boundary should widen,
that is a Step-2.5 flag, not an absorption.

## RED test outline (Step 2)

⚠ **This slice's subject is comment correctness, so there is no behaviour to pin and the usual red-first shape
does not apply.** The discipline transfers as follows and this section is the substitute, not a waiver:

1. **The trace IS the measurement.** For each of the five sites, establish the mechanism the claim rests on
   (which branch, which filter, which construction) and record it. **This is what goes to me at Step 2.5.**
2. **A discriminating control on the sweep** — a token you know is absent must return 0 rows, and a token you
   know is present must return a non-zero count you can verify by hand. **Report both.** Without them, a sweep
   that silently matched nothing is indistinguishable from one that found nothing.
3. **Zero-logic-delta proof** as an explicit mechanical step on the final diff, not an eyeball.

## Cross-doc invariant impact

**None expected** — no model field, no Appendix-A seam, no schema. ⚠ If a trace shows a WS-8 claim in
`ARCHITECTURE.md` itself is overstated, **that is orchestrator territory: flag at Step 9, do not edit it.**

## Things to flag at Step 2.5

1. ⛔⛔ **The five traces — this is the review surface and I will not approve without them.** For each site: the
   mechanism, the verdict, and the evidence. **"Reads correctly" is not a verdict.**
2. **Did the subject-classification sweep find sites BEYOND the five?** My default vote: **report them and do
   NOT absorb them.** `### 24.79`'s main leg found twice its filed scope and the two it added were
   worse-positioned than the two dispatched — so a wider find is likely and is a scope conversation with me.
3. **Is any of the five actually a LOGIC defect rather than a comment defect?** My default vote: **if the
   mechanism does not deliver what the comment claims, that is a rule-4 Finding and it escalates to the lead —
   it does not get fixed by rewording the comment.** ⛔ This is the one outcome that must not be quietly
   downgraded into a doc edit.
4. **Do any of the five sit in an operator-facing config doc?** My default vote: **treat those as highest
   severity** — `boot.ts:2184` is a candidate, and `### 24.79` found the operator-facing config doc was one of
   its worst sites because it is the surface read when deciding whether to turn the flag ON.

## Dependencies + sequencing

- **Depends on:** `### 24.79` main leg (SHIPPED `5f18f894`). No live blockers.
- **Blocks:** nothing. ⚠ **Interacts with `### 24.62` boundary (b)** — a reader answering that task lands on
  exactly these claims, which is why the residual is worth closing rather than carrying.

## Estimated commit count

**1.** Single area, single concern, comment-only expected. ⛔ **If a trace turns up a logic defect, that is a
separate slice and a separate commit — do not bundle a rule-4 behaviour fix into a comment-correctness commit.**

## Lessons-logged candidates anticipated

- **Convention candidate** — the entry-doc staleness gradient (`L161` amendment): the doc read first is the doc
  updated last, and the two properties compound.
- **Convention candidate** — a phrase census as candidate-list vs subject classification as the actual work.
- **Architecture-doc note candidate** — if WS-8's guarantee differs by branch in a way `§5` does not state.

## How to invoke

1. Read this brief end-to-end. ⛔ **The five traces come before any edit, and they come to me at Step 2.5.**
2. Run `/tdd ws8_unswept_surface_residual`.
3. **Step 9** — rule-4 subject ⇒ routes to me AND `team-lead`.
