# /tdd brief — app_managed_local_temporal_supervision (14.4)

## Feature
The worker-host **spawns + supervises a LOOPBACK-only local Temporal dev-server** (127.0.0.1:7233), mirroring the existing **`MANAGE_GBRAIN_SERVE`** pattern, so product workflows run in-product instead of only under `TestWorkflowEnvironment`; the app leaves **"Temporal-DEGRADED mode"** once the managed server is healthy, and cleanly stops the server on quit. **Build-substrate — G62 hard-line→substrate downgrade, OWNER RATIFIED 2026-07-15.** Loopback-only, cleanly supervised → touches NONE of the 4 hard lines (no external egress / spend / credential / real-transport). Desktop-track (worker-host). Closes **G62**.

## Use case + traceability
- **Task ID:** 14.4 (the LAST Phase-14 desktop leg; after this → `/phase-exit 14`)
- **Architecture sections it implements:** `ARCHITECTURE.md §19.1` (primary — "make the app supervise a local Temporal dev-server, mirroring the already-managed `gbrain serve`"), `§13` (V1-local topology: app-managed local Temporal supervision + install doctor).
- **⚠ GOVERNANCE — G62 owner ratification (RECORDED):** the 66-gap inventory marked G62 `hard-line`; the plan downgraded it to **`build (substrate)`** because loopback Temporal touches none of the 4 hard lines. The owner **RATIFIED** this downgrade on **2026-07-15** (escalated by orch23 → lead → owner). It is recorded in IMPLEMENTATION_PLAN task 14.4 and must be re-recorded in the 24.5 arming runbook when authored. **The commit body MUST note:** `G62 hard-line→substrate downgrade — OWNER RATIFIED 2026-07-15.`
- **Related context (confirm at Step 1 — you own the worker-host):**
  - **`apps/desktop/worker-host/index.ts`:** `:28` `MANAGE_GBRAIN_SERVE` (the supervision pattern to MIRROR — spawn/health/stop of the managed `gbrain serve`); `:53-54` `temporalAddress` (the loopback address the worker connects to — 127.0.0.1:7233); `:4` the `Temporal-DEGRADED` default (the state the app sits in until a managed Temporal is healthy). Confirm these exact seams at Step 1.
  - **NEW `apps/desktop/worker-host/temporal-supervisor.ts`** — the supervisor (spawn on boot / health-probe / stop on quit), mirroring the gbrain-serve supervisor's shape.
  - **The CONTROLLED/MOCK-process discipline (LOAD-BEARING — the owner condition):** build + TEST against a CONTROLLED/MOCK process — **NO real Temporal server is spawned in the test suite.** A real loopback Temporal server spawns ONLY when the app actually runs. So the supervisor must take an INJECTED process-spawner/factory seam (a port) that tests fill with a mock/controlled process; the real Temporal-CLI spawn binds only in the app's boot path (exactly like the gbrain-serve pattern). Confirm the spawn seam at Step 1.

## Acceptance criteria (what "done" means)
- [ ] A `temporal-supervisor.ts` that, on the worker-host boot path, spawns a LOOPBACK-only (127.0.0.1) local Temporal dev-server and supervises it (health-probe; leave `Temporal-DEGRADED` once healthy; stop cleanly on app quit), mirroring `MANAGE_GBRAIN_SERVE`.
- [ ] **Loopback-only binding:** the spawned server binds 127.0.0.1 only — never 0.0.0.0 / a routable interface (pin it). No external network is opened.
- [ ] **Spawn safety:** the process is spawned with an args-array + absolute/resolved binary (no shell string interpolation — no injection surface; mirror worker Lesson 10's args-array + absolute-bin discipline for a spawned process).
- [ ] **Clean lifecycle:** spawn on boot (when the supervision env-flag is on — mirror `MANAGE_GBRAIN_SERVE`'s gating), stop on quit (no orphaned process), and the app transitions OUT of `Temporal-DEGRADED` when the managed server is healthy.
- [ ] **MOCK-process tests only (owner condition):** every test drives a CONTROLLED/MOCK process via the injected spawner seam — the suite NEVER spawns a real Temporal server (assert the real spawn path isn't invoked in tests, e.g. a factory-spy zero-real-invocation pin). Cover: spawn-on-boot, health→leave-DEGRADED, stop-on-quit-no-orphan, loopback-only-bind, crash→supervised behavior (Q4).
- [ ] **Byte-equivalent when OFF:** with the supervision env-flag OFF (default, mirroring the gbrain pattern's gating), the worker-host boot is unchanged / stays in the existing `Temporal-DEGRADED` posture (no spawn) — the shipped default doesn't spawn anything.
- [ ] `/preflight` clean (desktop + repo-wide).

## Wiring / entry point (Step 7.5)
The supervisor is wired into the worker-host boot (`apps/desktop/worker-host/index.ts`) — spawn on app launch (env-gated), stop on quit — a REAL production entry (the app manages Temporal like it manages gbrain serve). It is TESTED against a mock process (never a real spawn in-suite). **State: reachable via the worker-host boot path (production entry); tested via the injected mock spawner.** Confirm the boot-wire site + the env-gating at Step 7.5.

## Files expected to touch
**New:** `apps/desktop/worker-host/temporal-supervisor.ts` + its tests (mock-process).
**Modified (confirm at Step 1):** `apps/desktop/worker-host/index.ts` (boot-wire the supervisor; the `temporalAddress` :53-54 + the `Temporal-DEGRADED` default :4 + the `MANAGE_GBRAIN_SERVE`-mirroring env-gate :28).
If beyond this, **flag at Step 2.5**.

## RED test outline (Step 2) — all against a CONTROLLED/MOCK process
1. `spawns_loopback_temporal_on_boot_when_enabled` — with the supervision flag on, boot spawns the (mock) process bound to 127.0.0.1; the real spawn is never invoked in-test (factory-spy). Why: §19.1 supervision.
2. `binds_loopback_only_never_routable` — the spawn args/config bind 127.0.0.1 only, never 0.0.0.0. Why: loopback-only (the G62 substrate condition).
3. `leaves_degraded_when_healthy` — once the (mock) server reports healthy, the app transitions out of `Temporal-DEGRADED`. Why: §13 health.
4. `stops_cleanly_on_quit_no_orphan` — on quit, the supervised (mock) process is stopped; no orphan. Why: clean lifecycle.
5. `off_by_default_is_byte_equivalent` — with the flag OFF, no spawn; the worker-host boot is unchanged. Why: shipped-default safety.
6. `spawn_uses_args_array_absolute_bin` — the spawn is args-array + absolute/resolved bin (no shell) — no injection surface. Why: spawn safety (Lesson 10 analog).
7. (Q4) `crash_is_supervised` — a (mock) process crash is handled per the chosen policy (bounded restart or back-to-DEGRADED). Why: supervision robustness.

## Cross-doc invariant impact
- **Model:** none (task 14.4 "Cross-doc invariant: none"). No frozen-seam change.
- **Shared-contract seam touched?** No → no schema-snapshot test.
- **Governance record:** the G62 ratification is recorded in task 14.4 (done by orchestrator) + must be in the commit body (see above) + the 24.5 arming runbook later.

## Things to flag at Step 2.5
1. **Spawn seam for mock-testing (LOAD-BEARING — the owner condition).** How is the process spawner injected so tests use a mock + the real Temporal-CLI spawn binds only at app boot? My default vote: an injected `ProcessSpawner`/factory port (mirror the gbrain-serve supervisor); the real spawn factory is bound only in the boot path, tests inject a mock — pin a factory-spy that the real spawn is never called in-suite. Confirm the gbrain-serve pattern's exact shape.
2. **Crash/restart policy (Q4).** Bounded restart-on-crash (mirror the worker-supervision pattern) or crash→back-to-DEGRADED? My default vote: mirror whatever `MANAGE_GBRAIN_SERVE` does (consistency); if gbrain-serve does bounded-restart, match it; else crash→DEGRADED + a health signal. Your call — keep it simple + consistent.
3. **Env-gating.** Mirror `MANAGE_GBRAIN_SERVE`'s env-flag gating so the shipped default is OFF/byte-equivalent (a real loopback Temporal spawns only when the operator opts in / the app runs in the managed topology). Confirm the flag name/pattern.
4. **Health-probe shape.** How does the supervisor probe the loopback Temporal's health (to leave DEGRADED)? Confirm the probe (a connection/readiness check to 127.0.0.1:7233) + that the mock can simulate healthy/unhealthy.

## Dependencies + sequencing
- **Depends on:** P7 (Phase-7 workflows — the workflows that will run in-product), P11 (§13 deploy — the V1-local topology). The gbrain-serve supervision pattern (existing) is the template.
- **Blocks:** `/phase-exit 14` (this is the last Phase-14 desktop leg). No hard dependency downstream.

## Estimated commit count
**1.** The supervisor + boot-wire + mock-process tests. **security-reviewer = invariant** — not a WS-8/candidate-gate slice, BUT it's the owner-ratified hard-line-downgrade item AND it spawns a process (loopback-only-bind enforcement, args-array/absolute-bin no-injection, clean-shutdown-no-orphan are real security legs). code-quality every-slice. **Pure-build / build-substrate — NO hard line** (G62 OWNER RATIFIED; loopback-only, no egress/spend/credential/real-transport; mock-tested).

## Lessons-logged candidates anticipated
- **Convention candidate** — app-managed local-process supervision (loopback-only bind, args-array/absolute-bin spawn, env-gated so the shipped default spawns nothing, injected spawner seam so the suite never spawns a real server, clean stop-on-quit) mirrors the gbrain-serve pattern; a hard-line→substrate downgrade is owner-ratified + recorded, never self-adjudicated.

## How to invoke
1. Continue the desktop-impl session (no `/session-start` unless swapped).
2. Read this brief end-to-end (Q1 spawn seam + Q3 env-gating are load-bearing; the MOCK-process discipline is the owner condition).
3. `/tdd app_managed_local_temporal_supervision`.
4. Step 1 — confirm the `MANAGE_GBRAIN_SERVE` pattern + `temporalAddress`/`Temporal-DEGRADED` seams + the injected spawner shape.
5. Step 2.5 — ping back the design answers. Don't GREEN until I sign off.
6. Step 9 — categorized flags. **Commit body MUST include:** `G62 hard-line→substrate downgrade — OWNER RATIFIED 2026-07-15.` Then → `/phase-exit 14`.
