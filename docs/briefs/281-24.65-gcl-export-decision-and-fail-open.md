# /tdd brief — gcl_export_decision_and_visibility_fail_open

## Feature

Decide the **export question** on the GCL gate — measured, not argued — and then close `### 24.65`'s
fail-open: a **zero-own-property workspace returns ALLOW** from `validateProjectionVisibility`.

⛔ **THE EXPORT QUESTION IS FIRST AND IT IS A DECISION, NOT A TASK.** It has been **undecided by design for two
rounds, specifically because nobody measured it.** ⭐⭐ **THE PERSON WHO MEASURES IT DECIDES IT** — lead ruling,
and it binds me too. **Neither the lead nor I have run the check, and neither of us gets to pre-load the answer.**

## Use case + traceability

- **Task ID:** `24.65` (shared task `#58`)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (GCL Visibility Gate), `§5` (workspace isolation / WS-8)
- **Safety:** ⚠ **rules 4 and 7.** Step-9 flags route to the lead (ALL SEVEN auto-route).
- **Origin:** `#48` / `24.65` Step 9 (providers-integrations + `security-reviewer`). **PRE-EXISTING.** `#48` landed
  `ba0deb1b` comment-only, mechanically-verified zero logic delta.

> ⚠ **This brief widens phase scope because** `§5` is not in Phase 24's declared anchor set
> (`§13 §19.11 §3 §4 §6`), and the workspace-isolation half of this finding is specified there.
> ⛔ **NOT resolved by adding `§5` to the set** — forbidden, and the resolution the lead ruled against on
> `### 24.54`. Recorded here as another instance of `24.54`, same as brief 280.

## ⛔ PART 1 — THE EXPORT DECISION (do this FIRST; nothing else starts until it is recorded)

**The question, in the barrel's own words:** do the two `export *`d GCL hops meet
`packages/knowledge/src/index.ts`'s **own stated bar** — *"widen this line only for something a composition root
must actually construct"*?

**Measured at source and re-derivable (orchestrator, 2026-08-13 — verify, do not inherit):**
- `packages/knowledge/src/index.ts:97` — `export * from "./gcl/visibility-gate"`
- `packages/knowledge/src/index.ts:99` — `export * from "./gcl/cross-workspace-links"`
- These are **`export *`, not named exports** — 9 and 7 exported symbols, **16 public**, of which
  `guardCrossWorkspaceRawRead` and `CrossWorkspaceLinkMap.authorizeCrossWorkspaceRawRead` reach
  `denyDirectCrossWorkspaceRaw`.
- ⇒ **a SINGLE `import`, by anyone, in any package, with NO PORT BOUND AT ALL**, makes a rule-4 gate live with
  fully caller-supplied `from`/`to`, and with `CrossWorkspaceLinkMap` never constructed.

⭐⭐ **THE ASYMMETRY THAT MAKES THIS SHARP: THE SAME FILE ALREADY STATES THE RULE IT IS NOT APPLYING HERE.**
`index.ts:36-41` narrow-exports `makeEnforceWorkspacePathScope` **by name**, under a comment **re-grounded this
round** — *"⛔ STILL NOT `export *` … the rule survives its rationale … widen this line only for something a
composition root must actually construct."* **That discipline is live, articulated, and 56 lines above two
`export *`s that were never measured against it.** (`contracts L72`'s operational half — *when you find a defect
shape, check the CONTAINER* — and note this is **the same shape as `24.65`'s own fail-open**: a discipline
applied to one member and not its sibling, in one file.)

⛔⛔ **HOW TO DECIDE, AND THE TRAP I AM DELIBERATELY NOT SPRINGING FOR YOU.**
The evidence available **reads one-sided** — the barrel's stated bar, nothing constructing `CrossWorkspaceLinkMap`
in production, `contracts L103` favouring make-it-unrepresentable over document-it. ⛔ **THAT IS NOT A LICENCE,
AND A ONE-SIDED READING IS EXACTLY WHAT AN UNMEASURED QUESTION LOOKS LIKE FROM THE OUTSIDE.** Three people have
now looked at this summary and formed the same impression without running the check; **that is not corroboration,
it is one impression held three times** (`L155`'s independence discriminator — *could these have failed
independently?* No: same summary, same reasoning).

**What the measurement must actually establish, per symbol, before any decision:**
1. **Who imports each of the 16 symbols today** — production and test, stated separately, by repo-relative path.
   ⛔ **Do NOT use `codegraph_callers` as the census** (`contracts L157`: it returned a confident *"No callers
   found"* for a symbol with two production call sites, both in callback position). **A hit is evidence; an empty
   is not.** Redirect-then-measure with a non-vacuity control, and report the control.
2. **Whether anything constructs `CrossWorkspaceLinkMap` in production** — and if not, whether that is because it
   is unbuilt or because it is bound elsewhere.
3. **What un-exporting would break** — compile-wise, at HEAD, measured by doing it in a scratch worktree.

**Then decide, and record the decision with its evidence:** **un-export** (hazard becomes unrepresentable —
`L103`) **or keep-with-a-stated-reason against the file's own bar.** ⛔ **Either answer is acceptable; an
UNRECORDED one is not, and neither is a decision that cites this brief's framing instead of your measurement.**

⚠ **INVERSE RISK, AND IT CUTS THE OTHER WAY — do not let the un-export reading run away with it:**
`denyDirectCrossWorkspaceRaw` **HAS two production callers**; it is the *chain* that terminates at nothing.
**"Unreachable" is not "uncalled,"** and a reader who conflates them may delete a gate that describes itself as
the WS-8 enforcement point.

## PART 2 — THE FAIL-OPEN (only after Part 1 is recorded)

⛔ **HEADLINE IS THE FAIL-OPEN, NOT THE THROWS.** A workspace built via `Object.create({id, defaultVisibility})`
— **zero own properties** — returns **ALLOW** from `validateProjectionVisibility`. ⭐ **A throw is loud; a
fail-open on a visibility gate is SILENT AND GRANTS ACCESS.**
⭐ `isVisibilityConsistentWithProjectionType` **hardens against exactly this class with `Object.hasOwn`, in the
same file.** The workspace reads do not. (`contracts L65` / `L128` — prototype-chain resolution defeating a
guard; `L72` — a guard applied to one field and not its siblings.)

**Second — §16 never-throw is not total. COUNT IS CORRECTED AND THE CORRECTION IS LOAD-BEARING:**
**TWO shapes MEASURED** (a throwing `id` getter on `sourceWorkspace`; a throwing `workspaceId` getter on
`projection`). ⛔ **THREE FURTHER SHAPES WERE NEITHER PINNED NOR EXCLUDED — treat as UNKNOWN, not as findings.**
*(An earlier "six measured" was an orchestrator relay of a number nobody re-derived — `L155`'s family.)*
⚠ `24.45`'s `?.` guards a **nullish left side, not an ACCESSOR**, so it closes **neither** measured shape.

⭐ **PRECEDENT ALREADY ADJUDICATED — do not re-litigate whether accessor-bearing inputs are in scope:** §5 /
task 9.33, `ARCHITECTURE.md:204-205`, **even when JSON-unreachable.**

## Acceptance criteria

- [ ] **The export question is DECIDED and RECORDED** — un-export, or keep with a stated reason measured against
      the file's own bar. The decision cites **your** measurement, not this brief.
- [ ] The importer census states its **method and its non-vacuity control**, production and test separately.
- [ ] A **zero-own-property workspace no longer returns ALLOW** from `validateProjectionVisibility`.
- [ ] The **two MEASURED throw shapes return typed denials**, each **named individually** in its own test.
- [ ] The **three unmeasured shapes** are either measured or **explicitly scoped out with the reason** (`L100`).
- [ ] The file's never-throw doc claim is **made true or qualified** (`L82` — never re-pointed at handy evidence).
- [ ] ⛔ **No new importer of either symbol** is introduced by this slice.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)

`validateProjectionVisibility` is already reached on the GCL projection path; this slice hardens an existing
reachable predicate rather than adding a surface. **If Part 1 decides UN-EXPORT, the barrel edit is the wiring
change and it must be stated as such** (a public-surface reduction, with the compile-check evidence).

## Files expected to touch

**Modified (expected):** `packages/policy/src/visibility.ts` (the fail-open + throw shapes) ·
`packages/knowledge/src/index.ts` (**only if Part 1 decides un-export**) · tests in the owning package.
⚠ **`packages/knowledge/src/index.ts` is knowledge territory.** If Part 1 decides un-export, **flag at Step 7.5
before touching it** — the barrel edit may need to split into a knowledge leg (`contracts L121`: a compile-break
caused by your own change is part of the change; a deliberate public-surface reduction is not).

## RED test outline (Step 2)

1. **`zero_own_property_workspace_is_denied`** — Asserts: a workspace via `Object.create({...})` returns a typed
   DENY, not ALLOW. Why: the headline fail-open; rule 4.
2. **`throwing_id_getter_on_source_workspace_returns_typed_denial`** — Asserts: typed denial, no throw. Why: §16.
3. **`throwing_workspace_id_getter_on_projection_returns_typed_denial`** — Asserts: typed denial, no throw. Why: §16.
4. **An ALLOW-side control** differing from the deny fixture in **exactly the field the gate decides on**, plus a
   **reason-CODE** assertion. ⛔ **Why this is mandatory, not optional (`contracts L80`): without it, the suite
   cannot distinguish a working gate from a constant DENY** — and `validateProjectionVisibility` is named in `L80`
   as one of the two guards proven blind by exactly that substitution.

## Things to flag at Step 2.5

1. ⛔⛔ **Your Part-1 decision and the measurement behind it — this is the review surface.** State what you
   measured, what the census method and control were, and which way you decided. **If your measurement
   contradicts anything in this brief, that is the process working** (`contracts L81`).
2. **Does the fail-open fix belong in `validateProjectionVisibility` or in a shared workspace-read helper?**
   My default vote: **whichever makes the sibling `isVisibilityConsistentWithProjectionType` and the workspace
   reads share ONE hardened accessor** — the defect is that two readers in one file disagree, so a fix that
   hardens only the reported one re-creates `L72` in the fix.
3. **Do the three unmeasured throw shapes get measured in this slice or scoped out?** My default vote:
   **measure them if cheap, scope them out with the reason if not** — ⛔ **but do not report them as absent.**

## Dependencies + sequencing

- **Depends on:** — · **Blocks:** any new importer of the two GCL symbols; `Phase 25.2/25.4` port binding.

## Estimated commit count

**2 — deliberately not bundled.** (1) the Part-1 export decision (+ barrel change if un-exporting), (2) the
fail-open + throw-shape fix. **Both touch a safety invariant, so each gets its own commit.**

## Lessons-logged candidates anticipated

- **Convention candidate** — a barrel's own narrow-export discipline stated in a comment and not applied to
  sibling lines in the same file (`L72`'s container half, second instance).
- **Architecture-doc note candidate** — whether the GCL gate's public surface is intended to be importable at all.

## How to invoke

1. Read this brief end-to-end. ⛔ **Part 1 before Part 2, and Part 1's output is a recorded decision.**
2. Run `/tdd gcl_export_decision_and_visibility_fail_open`.
3. **Step 2.5** — lead with your Part-1 measurement and decision.
4. **Step 9** — safety (rules 4 and 7) ⇒ routes to the lead.
