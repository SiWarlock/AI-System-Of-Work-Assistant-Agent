# /tdd brief — copilot_concept_synthesis

## Feature
An ON-REQUEST, READ-ONLY Copilot **concept-synthesis** skill: given a concept term, retrieve the asking workspace's knowledge for it and synthesize a governed explanation through the SAME single-sourced governed core as `answerCopilotQuestion`/`answerCopilotBriefing` (`runGovernedCopilotSynthesis`: WS-8 re-guard → authoritative posture → egress veto → synthesis on the veto-cleared route → candidate/UI-safe gate), returning a `UiSafeCopilotAnswer`. NO write, NO commit, propose bridge STAYS OFF. This is C6 slice (b) sub-slice 2 of 2 (the fast-follow to the 040 briefing skill).

## Use case + traceability
- **Task ID:** 13.10 (Copilot Skill Catalog C6 — slice (b) Tier-2 LOCAL synthesis skills; owner-authorized 2026-07-12: on-request briefing + concept-synthesis). The concept half of (b); the briefing half landed as `b1048c3` (brief 040).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (GBrain/GCL — WS-8 workspace-scoped knowledge retrieval + candidate-data posture) + `§7` (AgentRuntimePort / synthesis — the governed read-only synthesis path). Both ∈ phase-13 Spec anchors. (No Phase-9/Today binding — concept-synthesis reads workspace KNOWLEDGE, not the Today read-model the 040 briefing bound to; no scope widen.)
- **Related context:** the landed governed core it reuses —
  - `apps/worker/src/api/procedures/copilot.ts` — `runGovernedCopilotSynthesis(deps, workspaceId, question, scopedContext)` + `GovernedCopilotSynthesisDeps` + `CopilotRetrievalPort` + `CopilotDeps` (= `GovernedCopilotSynthesisDeps` + `retrieval: CopilotRetrievalPort`) + `enforceRetrievalScope` + `unknownWorkspace` (all exported, landed `b1048c3`).
  - `apps/worker/src/api/procedures/copilotBriefing.ts` — the SIBLING pattern to mirror: `assemble → enforceRetrievalScope → runGovernedCopilotSynthesis(deps, ws, DIRECTIVE, scoped)`. Concept mirrors it with the KNOWLEDGE retrieval (`CopilotRetrievalPort.retrieve(ws, concept)`) instead of the Today assembly.
  - `apps/worker/src/api/procedures/queries.ts` — `copilotAsk` mount (`:526`) reuses `copilot: CopilotDeps`; concept reuses the SAME injected `copilot` bundle (no new deps).

## Acceptance criteria (what "done" means)
- [ ] `answerCopilotConcept(deps, input)` returns `Result<UiSafeCopilotAnswer, FailureVariant>` for a KNOWN workspace: retrieves knowledge for the concept term, synthesizes, gates the candidate → UI-safe.
- [ ] **WS-8 fail-closed** — unknown workspace fails closed (`WORKSPACE_NOT_FOUND`); the retrieved context is RE-GUARDED (`enforceRetrievalScope`) so a foreign-workspace context can NEVER be synthesized.
- [ ] **Governed core reused, not re-implemented** — `answerCopilotConcept` routes through `runGovernedCopilotSynthesis` (the same veto + gate as Q&A/briefing); no duplicated egress/gate logic.
- [ ] **Egress veto governs the concept answer** — the fail-closed Employer-Work egress veto runs BEFORE synthesis; the `egressProcessor` notice threads onto the answer for employer-work cloud egress. NO external fetch.
- [ ] **Client concept term is bounded** — `parseConceptInput` bounds the concept term (length/single-line, mirroring `parseAskInput`); the term is the RETRIEVAL query AND is folded into the server-fixed concept directive passed to synthesis. (Same injection posture as Q&A — NOT the smaller briefing surface.)
- [ ] **Read-only / propose OFF** — no side effect, no ProposedAction/write path; propose flags untouched (HARD LINE).
- [ ] **Candidate-data gate** — the synthesized concept answer passes `toUiSafeCopilotAnswer`; a leak-shaped/over-cap/empty candidate is REJECTED.
- [ ] `query.copilotConcept` is mounted in `buildQueryRouter` behind `authedResolver` (auth + WS-8) and reachable through `composeAppRouter`.
- [ ] All new unit tests pass; repo-wide `pnpm -w turbo run typecheck test` green; `/preflight` clean. **MANDATORY Step-8 review = security-reviewer + code-quality-reviewer** (WS-8 / candidate-gate / egress surface; lighter than 040 since the governed core is unchanged/already-reviewed).
- [ ] No frozen-contract change (reuses `UiSafeCopilotAnswer`).

## Wiring / entry point (Step 7.5)
`query.copilotConcept` — a new `.query()` in `buildQueryRouter` (`queries.ts`), mirroring `copilotAsk`: `publicProcedure.input(parseConceptInput).query(authedResolver<CopilotConceptInput, UiSafeCopilotAnswer>((_ctx, input) => answerCopilotConcept(copilot, input)))`. Reuses the EXISTING injected `copilot: CopilotDeps` bundle (no new `QueryRouterDeps`/`ApiServerDeps`/boot dep). Reachable through `composeAppRouter` on the mounted API server. Renderer affordance ("explain a concept" in the Copilot sidebar) is a SEPARATE desktop-track follow-on — `none for this slice; renderer wiring lands in a later desktop slice`.

## Files expected to touch
**New:**
- `apps/worker/src/api/procedures/copilotConcept.ts` — `CopilotConceptInput` (`{ workspaceId, concept }`), `CONCEPT_DIRECTIVE` + the concept-framed directive builder, `answerCopilotConcept(deps: CopilotDeps, input)`. Reuses `CopilotRetrievalPort` + `runGovernedCopilotSynthesis` from `copilot.ts` (no new deps type — `CopilotDeps` already = governed core + retrieval).
- `apps/worker/test/api/procedures/copilotConcept.test.ts` — the RED tests below.

**Modified:**
- `apps/worker/src/api/procedures/queries.ts` — mount `query.copilotConcept` + `parseConceptInput` (reuse the existing `copilot` dep; NO new `QueryRouterDeps` field).
- `apps/worker/test/api/procedures/queries.test.ts` — reachability test for the new mount (mirror the `copilotBriefing` reachability test).

**Explicitly NOT touched:** `server.ts`/`boot.ts` (no new deps — reuses the `copilot` bundle), `copilot.ts` (the governed core is already extracted — concept only imports it). If implementation needs to modify `copilot.ts` or add a write/propose path, **STOP and flag at Step 2.5**.

## RED test outline (Step 2) — `apps/worker/test/api/procedures/copilotConcept.test.ts`
1. **`concept_synthesizes_for_known_workspace`** — Asserts: `answerCopilotConcept` for a known workspace + a concept term returns `ok(UiSafeCopilotAnswer)`, cited. Why: §6/§7 happy path.
2. **`concept_unknown_workspace_fails_closed`** — Asserts: unknown workspace ⇒ `err WORKSPACE_NOT_FOUND`; no synthesis call. Why: WS-8 fail-closed (§6).
3. **`concept_reguards_foreign_workspace_context`** — Asserts: a retrieval returning a context keyed to a DIFFERENT workspace fails closed via `enforceRetrievalScope` (`RETRIEVAL_SCOPE_MISMATCH`) — no synthesis. Why: WS-8 defense-in-depth (§6).
4. **`concept_term_is_retrieval_query_and_directive`** — Asserts: the concept term is passed as the retrieval query AND folded into the concept-framed directive handed to `runGovernedCopilotSynthesis` (a spy retrieval + spy synthesis see it). Why: the concept-framing contract.
5. **`concept_egress_vetoed_employer_work_cloud_ack_off`** — Asserts: employer-work + cloud route + ack OFF ⇒ veto DENY ⇒ fails closed BEFORE synthesis. Why: safety rule 5 (egress veto).
6. **`concept_employer_work_cloud_egress_notice`** — Asserts: employer-work + an egressing allowed route threads `egressProcessor` onto the answer. Why: the Employer-Work notice (P1.2b).
7. **`concept_reuses_governed_core`** — Asserts (structural): `answerCopilotConcept` routes through `runGovernedCopilotSynthesis` (spy synth sees the veto-cleared route). Why: single-sourced safety machinery (no drift).
8. **`concept_input_bounds_the_term`** — Asserts: `parseConceptInput` rejects/bounds an over-cap or non-single-line concept term (mirror `parseAskInput`). Why: input bounding (injection surface = Q&A-equivalent).
9. **`query_copilotConcept_reachable`** (in `queries.test.ts`) — Asserts: `buildQueryRouter` exposes `copilotConcept` behind `authedResolver`, callable with the fake `copilot` bundle; unknown ws fails closed. Why: Step 7.5 reachability.

## Cross-doc invariant impact
- **Model field changes:** none — reuses `UiSafeCopilotAnswer`.
- **Orchestrator doc rows to write hot (Step 9):** none frozen-contract. The C6 slice-(b) concept item ticks (orchestrator writes at round-seal). The convention lesson banked with 040 already covers this pattern (concept is its second instance — a good confirmation, no new lesson needed unless a fresh gotcha surfaces).
- **Shared-contract (Appendix-A / subsystem-boundary) model touched?** No.

## Things to flag at Step 2.5
1. **Deps — reuse `CopilotDeps` directly vs a new `CopilotConceptDeps`.** My default: **reuse `CopilotDeps`** (it already = `GovernedCopilotSynthesisDeps` + `retrieval: CopilotRetrievalPort`, exactly what concept needs) — no new deps type, no boot/server change. A dedicated alias adds nothing.
2. **How the concept term reaches synthesis.** My default: the term is BOTH the retrieval query (`retrieve(ws, concept)`) AND folded into a server-fixed `CONCEPT_DIRECTIVE` framing (e.g. `${CONCEPT_DIRECTIVE} "${concept}"`) passed as the `question` to `runGovernedCopilotSynthesis`. Unlike the briefing's fully-server-fixed directive, concept carries CLIENT input — so the injection posture equals Q&A's (bounded at `parseConceptInput`, governed by the egress veto + candidate gate). Confirm the reviewer treats this as Q&A-equivalent surface, not briefing-equivalent.
3. **Output contract — reuse `UiSafeCopilotAnswer`.** My default: **reuse** (a concept explanation is answer-shaped). No new frozen contract.
4. **Distinctness — separate procedure vs a Q&A mode.** My default: **separate procedure** (`answerCopilotConcept` + `query.copilotConcept`), consistent with the briefing + the banked convention ("each on-request skill = own procedure reusing the governed core"). Keeps the skill surfaces clean + independently mountable.
5. **Prose = eval.** My default: the deterministic wiring (retrieval query binding, directive framing, governed-core reuse, WS-8, egress, gate, mount) is TDD'd here; the concept-explanation PROSE quality = eval-security follow-on (a case joins `copilot-governance.test.ts` — flagged, NOT built; do NOT touch `../SoW-build-evalsec`).

## Dependencies + sequencing
- **Depends on:** brief 040 (`b1048c3`) — the extracted `runGovernedCopilotSynthesis` + exported `CopilotRetrievalPort`/`enforceRetrievalScope`/`unknownWorkspace`/`CopilotDeps`. Landed.
- **Blocks:** nothing hard. After this lands, the queued #41 round close-out runs (Bucket A runbook + Bucket B resume menu + docs round-seal).

## Estimated commit count
**1.** A thin, SAFETY-RELEVANT slice (WS-8 / candidate-gate / egress — same surface family as Q&A/briefing) → own commit + **MANDATORY security + code-quality review**. Thinner than 040 (no new deps, no `copilot.ts` change, no read-model retrieval — reuses the workspace-knowledge retrieval + the landed governed core). One commit.

## Lessons-logged candidates anticipated
- **Convention confirmation** — concept is the SECOND instance of the "on-request skill = own procedure + own retrieval + reuse the single-sourced governed core" convention (banked with 040). A clean second instance confirms the lesson; no new lesson unless a fresh gotcha surfaces.
- **Future TODO — next-brief working set** — the concept-explanation governance-eval case (eval-security track); the renderer "explain a concept" affordance (desktop track).
- **Architecture-doc note candidate** — none new (concept reuses the §6 knowledge retrieval + the §7 governed synthesis already documented).

## How to invoke
1. Read this brief end-to-end (esp. Step-2.5 #2 — the concept-term-as-client-input note, which sets the review posture to Q&A-equivalent). 2. `/tdd copilot_concept_synthesis`. 3. Step 0 restate: an on-request read-only concept-synthesis skill over workspace knowledge, reusing the governed core; no write, propose OFF. 4. Step 1: confirm the file list (4 files; NO server.ts/boot.ts/copilot.ts change). 5. Step 2.5: the five decisions. 6. Step 8: **security + code-quality review (MANDATORY)**. 7. Step 9: categorized flags + ship-ask.
