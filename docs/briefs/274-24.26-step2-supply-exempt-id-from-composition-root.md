# /tdd brief — supply_exempt_workspace_id_from_composition_root

## Feature

Step 1 made the exempt workspace id **suppliable** as a required factory argument. **Step 2 supplies it** — both worker `KnowledgeWriterDeps` literals pass `workspacePathCheck: makeEnforceWorkspacePathScope(<id>)`. **Nothing becomes required yet; that is step 3.**

## Use case + traceability

- **Task ID:** `### 24.26` — **step 2 of 3** (worker leg of a lead-authorized cross-track sequence)
- **Architecture sections it implements:** `ARCHITECTURE.md §16` · safety rule 4 (defense-in-depth for the workspace-path guard)
- **Related context:** `### 24.26` step 1 ✅ `e1293226` (knowledge) · `### 24.61` (the blank-id tripwire's real limits) · brief `271` (step 1) · contracts `L39`/`L87`/`L103`

> ⛔ **Widens phase scope because** `### 24.26` declares `Spec: §16`, not in Phase 24's declared anchor set. Declared, not resolved by editing it — systemic version at `### 24.54`.

## ⛔ THE SEQUENCE — you are the middle leg, and the middle leg is the one that looks finished and isn't

| step | track | state after |
|---|---|---|
| 1 ✅ `e1293226` | knowledge | factory exists, param required from birth; **nothing supplies it** |
| **2 — THIS BRIEF** | **worker** | **both literals supply it; `workspacePathCheck` still OPTIONAL; the constant still exists** |
| 3 — the make-it-required leg of `### 24.26` | knowledge | `workspacePathCheck` REQUIRED; `??` fallback and `LEGACY_UNPREFIXED_WORKSPACE_ID` deleted |

⛔⛔ **AFTER YOUR COMMIT THE TREE CARRIES A *THIRD* COPY OF THE VALUE** — the knowledge constant, your two call sites, and the desktop config — **which is strictly worse than the start state for a task whose whole purpose is *one value, one home*.** ⚠ **Typecheck stays green; no test reds.** ⇒ **say so in your commit message.** **That exposure is exactly why step 3 is separately tracked, and why a round ending here would look finished and not be.**

## Premises — all verified by the orchestrator; falsify freely

1. **The factory exists and is barrel-reachable:** `makeEnforceWorkspacePathScope(exemptWorkspaceId: string)` exported from `@sow/knowledge` (a **named** export added in step 1 — deliberately not `export *`, because `export *` would publish `LEGACY_UNPREFIXED_WORKSPACE_ID`, which step 3 deletes).
2. **The two literals are** `apps/worker/src/composition/buildActivities.ts:575` and `apps/worker/src/composition/semanticApprovalDispatch.ts:61`. ⛔ **NOT `boot.ts`** — it constructs no `KnowledgeWriterDeps` (the tracker said otherwise until 2026-08-13).
3. **Three `createCommitActivity` call sites are fed by those two literals** — `buildActivities.ts:584`, `:1075` (reuses the `:575` literal), `semanticApprovalDispatch.ts:77`. **You change the literals, not the call sites.**
4. **`writer.ts:157`'s `workspacePathCheck?` is OPTIONAL and `:225` falls back to `enforceWorkspacePathScope`.** ⛔ **Do not touch `writer.ts` — that is step 3.**
5. **The value is a plain `string`, not a branded `WorkspaceId`** — `workspace-path-guard.ts:47-48` states this deliberately, matching `KnowledgeMutationPlan.workspaceId`'s own non-branded contract type.

## ⛔ WHERE DOES THE VALUE COME FROM? This is the slice's real question — see Step-2.5 Q1

**Step 1's whole point was that the id stops being a hardcoded constant.** ⛔ **Passing `makeEnforceWorkspacePathScope("personal-business")` from the composition root would move the literal, not remove it — a THIRD hardcoded copy, and it would make step 3 pointless.** **Q1 decides this and I do not have a confident default.**

## Acceptance criteria

- [ ] Both `KnowledgeWriterDeps` literals pass `workspacePathCheck` built from `makeEnforceWorkspacePathScope`.
- [ ] **The supplied value is sourced from config, not a new literal** — or, if Step 2.5 concludes no config source exists yet, **the literal is introduced with an explicit in-code note naming it as step 3's deletion target** (`L82`).
- [ ] ⛔ **The factory is invoked ONCE at composition, not per job.** Step 1's own note: constructing it on a job path turns a config fault into an uncaught throw where `applyPlan` promises a typed `WriteFailure`.
- [ ] **`writer.ts` untouched;** `packages/knowledge/**` untouched.
- [ ] **A test pins that the guard actually reached the writer with the supplied id** — ⚠ **MOVE THE STATE**: a construction-side assertion cannot distinguish "wired" from "constructed and dropped." Drive a plan whose workspace is the exempt one and one whose isn't.
- [ ] `/preflight` clean, **per-package scoped** (repo-wide measures other writers' union).

## Wiring / entry point (Step 7.5)

**This slice IS the wiring.** Both literals feed `createCommitActivity` → `applyPlan` → the writer's step 4.5 guard. ⛔ **Confirm the guard is reached from a production entry point and not only from tests** — and note `buildActivities.ts:1075`'s `sourceCommit` is the **real** KnowledgeWriter commit path, so this is a live-path change, unlike `24.48`.

## Files expected to touch

**Modified:** `apps/worker/src/composition/buildActivities.ts` (`:575`) · `apps/worker/src/composition/semanticApprovalDispatch.ts` (`:61`) · a worker test
⛔ **Do NOT touch:** `packages/knowledge/**` (steps 1 and 3) · `apps/desktop/worker-host/index.ts` (read-only reference at `:185`) · `writer.ts`.

## RED test outline (Step 2)

1. **`exempt_workspace_commits_unprefixed_through_the_real_composition`** — drive a plan for the exempt workspace with an unprefixed path through the composed deps.
   - Asserts: `ok` — the supplied exemption is in effect
2. **`non_exempt_workspace_still_rejects_unprefixed`** — the control (`L80`).
   - Asserts: `err(workspace_path_violation)` — **this is the test that catches a factory wired with the wrong id, or wired and dropped**
3. **`the_guard_is_built_once_at_composition`** — assert the factory is invoked once, not per commit.
   - Why: acceptance criterion 3; a per-job construction is the fault-shape step 1 warned about

## Cross-doc invariant impact

- **Model field changes:** none. ⚠ **Step 3 will need a row** (`workspacePathCheck` optional→required is a dep-surface change) — **flag it then, not now.**
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⭐⭐ **WHERE DOES THE EXEMPT ID COME FROM? This is the slice.** Options: (a) an existing workspace/config surface already reaching the composition root; (b) a new config field; (c) a literal at the composition root with a deletion note. ⛔ **I do NOT have a confident default and am not supplying one** — I have not traced what config actually reaches `buildActivities`. ⭐ **If (c), say plainly that the slice moves the literal rather than removing it, and that step 3 is what removes it** — that is honest and fine; **pretending otherwise is not.**
2. **Do the two literals want the SAME id?** They are different composition paths (proof-spine activities vs semantic-approval dispatch). **My default vote: yes, one source** — two sources would be `24.26`'s own defect re-created at a new layer. **But confirm they can see the same config.**
3. **Does `### 24.61` bite here?** The blank-id tripwire covers only ECMAScript whitespace; zero-width ids construct fine and clear `WorkspaceIdSchema`. ⛔ **Not yours to fix** — but **if your source can produce an unvalidated string, say so at Step 2.5**, because that is `24.61`'s "legs that will supply it."

## Dependencies + sequencing

- **Depends on:** `### 24.26` step 1 ✅ `e1293226`
- **Blocks:** ⛔ **step 3 (make-it-required), which is separately tracked and MUST NOT be absorbed here.**

## Estimated commit count

**1.** ⛔ **Do not bundle** — one leg of a three-commit safety sequence; bisectability is the point.

## Lessons-logged candidates anticipated

- **Convention candidate** — *the middle leg of an expand/migrate/contract is the one that looks finished and isn't; its commit message must name the transient worse-than-start state.*

## How to invoke

1. **Read the SEQUENCE table first** — your commit deliberately leaves the tree worse than it started.
2. **Run `/tdd supply_exempt_workspace_id_from_composition_root`.**
3. **Step 2.5** — Q1 is the slice; do not take a default I did not give.
4. **Step 9** — if Q1 landed on (c), flag the literal explicitly so step 3 inherits a named target.
