# /tdd brief — retrieval_reranker_logic

## Feature
A deterministic / local **retrieval re-ranker LOGIC module** (packages/knowledge, knowledge territory) that re-scores retrieved passages by query-passage relevance and reorders them BEFORE the cap — replacing raw gbrain embedding-order with true-relevance order. Zero-egress / no-spend; reuses 13.3a's RRF/fusion primitive. The worker wiring into `copilotGbrainRetrieval.ts` is a SEPARATE worker binding (logic-in-package / wire-at-boot pattern).

> **Territory split (standing ARC-3/5 pattern):** the re-ranker LOGIC lands here in `packages/knowledge`. The worker BINDING — insert the re-ranker into `apps/worker/src/api/procedures/copilotGbrainRetrieval.ts` (reorder before the `DEFAULT_GBRAIN_RETRIEVAL_LIMIT=8` cap) — is a separate worker task. This brief is the knowledge logic leg only.

## Use case + traceability
- **Task ID:** 13.17 (logic leg)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (retrieval / re-ranking).
- **Related context:**
  - Today the Copilot retrieval path takes the first 8 passages in raw gbrain EMBEDDING order with NO re-scoring (`copilotGbrainRetrieval.ts` `parseGbrainSearchResult`). 13.17 adds a re-ranker pass over the retrieved passages BEFORE the cap.
  - Depends: **13.3a** (landed — the RRF hybrid-fusion primitive K=60/0-indexed/id-asc; reuse its scoring). Default a deterministic/local re-scorer (query-passage relevance) consistent with the zero-egress posture; a MODEL-based re-ranker leg is OWNER-GATED (egress) — NOT in this slice.
  - **⚠ SEC-MED (carried from 13.3a — belongs to the WORKER binding slice, noted here so it's not lost):** when this re-ranker + 13.3a's retrieval get wired into `copilotGbrainRetrieval.ts` and raw Employer-Work can flow, the retrieval embed's `egressGate` MUST bind the REAL `egressVeto` (loopback-proof), NOT the self-declared-egressClass DI seam. The re-ranker itself is post-retrieval (local, no embed/egress), so this pin rides the worker binding, not this logic slice.

## Acceptance criteria (what "done" means)
- [ ] A deterministic re-ranker fn re-scores retrieved passages by query-passage relevance and **reorders them** (a fixed input set with a known relevance signal produces a PINNED re-ranked order — beating raw embedding order for that fixture).
- [ ] Reuses 13.3a's RRF/scoring primitive (consistent local zero-egress posture) — **no new egress / no spend / no model call** in the re-ranker.
- [ ] The **model-based re-ranker leg is OUT** (owner-gated egress) — a test/scan confirms no cloud endpoint is reachable from this module.
- [ ] Candidate-data posture unchanged — re-ranking **reorders, never fabricates or mutates** passage content.
- [ ] TOTAL never-throws over a malformed/empty passage set (fail-safe: returns the input order or an empty result, never throws).
- [ ] All unit tests in `packages/knowledge/test/…` pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none in this slice — the re-ranker is a pure logic module; worker binds it into the retrieval procedure (separate task).** The re-ranker fn is consumed by `apps/worker/src/api/procedures/copilotGbrainRetrieval.ts` (reorder-before-cap) — that BINDING (+ the SEC-MED real-egressVeto pin above) is a worker task, sequenced later. State explicitly: no production wiring here. (spec-lint requires this section.)

## Files expected to touch
**New:**
- `packages/knowledge/src/gbrain/rerank.ts` — the deterministic re-ranker (reuses the 13.3a fusion primitive).
- `packages/knowledge/test/gbrain/rerank.test.ts` (or the package's flat `gbrain-rerank.test.ts` convention).

**Modified:**
- `packages/knowledge/src/index.ts` — barrel-export the re-ranker for the worker binding to import.

⚠ Do NOT touch `apps/worker/*` — `copilotGbrainRetrieval.ts` is the worker binding. `git add <path>` only.

## RED test outline (Step 2)
1. **`reranker_reorders_by_relevance`** — a fixed passage set with a known relevance signal re-ranks to a PINNED order that beats raw embedding order. Why: §6 re-ranking.
2. **`reranker_reuses_local_fusion_no_egress`** — the re-scorer uses the 13.3a local primitive; no model/endpoint call. Why: zero-egress posture.
3. **`model_leg_absent`** — no cloud re-ranker path is reachable from the module. Why: owner-gated egress (model leg deferred).
4. **`reranking_preserves_passage_content`** — output passages are the same content, only reordered (no fabrication/mutation). Why: candidate-data posture.
5. **`malformed_or_empty_never_throws`** — an empty/malformed passage set yields a fail-safe Result (input order or empty), never a throw. Why: LESSON 11 TOTAL.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** none anticipated (the 13.3a RRF-ordering arch note I'm already banking covers the shared fusion contract).
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Re-ranker algorithm.** Default vote: **reuse 13.3a's deterministic RRF/lexical-relevance scoring** (fuse a query-passage lexical-relevance rank with the retrieval order) — consistent zero-egress, no new dependency. Confirm vs a distinct local re-scorer.
2. **Model-based leg is OUT.** Default vote: confirm the model-based re-ranker (egress) is owner-gated and NOT built here; the default is deterministic/local only. Confirm.
3. **Eval-bar validation split.** Default vote: this slice pins the re-ordering LOGIC deterministically (fixture-based); the "beats raw embedding order on the 13.3 recall@10 bar" validation is a **13.3b follow-on** (the recall eval harness isn't built yet — evalsec). Confirm the split.

## Dependencies + sequencing
- **Depends on:** 13.3a (landed — RRF fusion primitive), P4.
- **Blocks:** the worker binding into `copilotGbrainRetrieval.ts` (+ the SEC-MED real-egressVeto pin) · the 13.3b eval-bar validation confirms the ranking improvement.

## Estimated commit count
**1** — one re-ranker logic module + unit tests; knowledge territory, no egress. **security-reviewer = invariant** (retrieval-path posture — confirm no egress in the re-scorer, model leg is out, and re-ranking never fabricates/mutates content). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the retrieval re-ranker is a pure local re-scorer (reuses the RRF fusion primitive) that reorders BEFORE the cap — no egress, no model call, content-preserving; the model-based leg is owner-gated; logic in-package, worker wires it into the retrieval procedure."
- **Future TODO — worker** — the `copilotGbrainRetrieval.ts` binding + the SEC-MED real-egressVeto pin on the retrieval embed.

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (algorithm) + Q3 (eval-bar split) need answers before GREEN.
2. Run `/tdd retrieval_reranker_logic`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
