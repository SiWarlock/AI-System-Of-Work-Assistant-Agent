# /tdd brief — audit_persist_second_channel_and_two_false_comments

## Feature

`### 24.62` — `createAuditPersistPort` takes **two** data channels and gates **one**: `isRedactionSafe` scans the `AuditSignal`, `workspaceId` rides beside it unscanned. Make the port's second-channel contract explicit and **make both `console.error` lines true**, each of which currently asserts a redaction-safety property the line beneath it does not have.

## Use case + traceability

- **Task ID:** `### 24.62`
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16` · Safety **rule 7**
- ⚠ **`spec-lint` scope note — this brief **widens phase scope because** Phase 24 declares `§19.11 · §6 · §13 · §4 · §3` while `§5`/`§16` are its two most-cited anchors.** ⛔ **That is `### 24.54`, not a real widening**, and ⛔ **the fix is NOT to add these to Phase 24's declared set** (lead ruling).
- **Related:** `### 24.45` (`48ec7c91`) — the validate-or-omit pattern · `### 24.55` (trusted provenance ≠ validated shape) · `### 24.53` (`e85953d3`) · `### 24.64` (**separate task — do NOT absorb**)

---

## ⛔⛔ SEVERITY IS RESOLVED. READ THIS BEFORE THE DONE-WHEN — IT CHANGED WHAT THIS SLICE IS.

**The sealed record says "SEVERITY UNDETERMINED — PENDING EXACTLY ONE CHECK."** That framing was wrong in two ways, both now corrected in the tracker. **This task has TWO defects; the "one check" covered only one of them. Both have now been run.**

**Defect 2 — the refusal log at `boot.ts:588-590` interpolating `signal.event`: UNREACHABLE AS A LEAK.** The complete set of `event` values reaching that line is **three hardcoded literals** — `"egress.denied"` (`packages/policy/src/egress.ts:204`) · `"job.admission.rejected"` (`packages/policy/src/admission.ts:51`) · `"egress.veto.route_substituted"` (`packages/providers/src/broker/egress-veto.ts:105`). ⚠ **The tracker's "three data-derived `buildAuditSignal` producers" alarm was FALSE — `event: input.event` is inside `buildAuditSignal` itself, `event: signal.event` is inside `toAuditRecordInput` (the mapper `:593` calls), and `event: match.event` builds a `UiSafeAuditDrillSummary` that is not an `AuditSignal` at all.** A passthrough and a mapper counted as producers.

**Defect 1 — `workspaceId` at `:593`/`:599`: REGISTRY-VALIDATED, NOT A CALLER-INJECTION CHANNEL.** Two gates precede `persistDenial` **on the denial path**: `apps/worker/src/api/procedures/copilot.ts:580-581` (`workspacePosture.resolve` as the first statement, fail-closed `WORKSPACE_NOT_FOUND`) and ⭐ a **read-back identity re-gate** at `apps/worker/src/api/adapters/storeBackedWorkspacePosture.ts:39` — `String(ws.id) !== workspaceId`, strict equality.

⛔ **BUT THE RESIDUAL IS REAL AND IS THE REASON THIS SLICE EXISTS:** registry-validated means *the id exists in the registry*, **NOT** *the id is well-shaped*. ⇒ ***a credential-shaped id sitting in the workspace CONFIG still reaches the audit raw*** — which `24.45`'s own comment already states at `packages/policy/src/visibility.ts:152-155`. **`### 24.55`'s rule, one layer down.**

⚠ **NO SEVERITY ESCALATION IS AUTHORIZED BY THIS BRIEF. Do not use the words "live", "exploit", or "breach"** — two people have already retracted "LIVE" on this task, and a third would be worse than the defect.

---

## Premises — orchestrator-verified at source at HEAD `20df48c7`

`apps/worker/src/boot.ts:579-604`, `createAuditPersistPort`:
- **`:585`** `if (!isRedactionSafe(signal))` — gates the signal **only**; `workspaceId` is a separate parameter, never scanned.
- **`:586-587`** comment: *"deliberate, redaction-safe (event name only, never a field value)"* — ⛔ **`event` IS one of the six fields `isRedactionSafe` scans** (`packages/policy/src/audit-signal.ts:170-177`), so on the refusal path at least one of the six is unsafe by definition and the handler cannot know which. **The comment's reasoning is wrong even though its conclusion happens to hold** (defect 2's literals).
- **`:593`** `const record = { ...toAuditRecordInput(signal, deps.now()), workspaceId };` — raw id written **durably**.
- **`:596-597`** comment: *"deliberate, redaction-safe (event/code only)"* — ⛔ **`:599` prints a THIRD thing: `workspaceId`.** The comment omits the one value it does not cover.
- **`:599`** interpolates `signal.event`, **`workspaceId`**, and `appended.error.code`.

⭐ **The symmetry is the reusable part: `:588-590` interpolates a SCANNED field on the one path where a scanned field is known bad; `:596-599` interpolates an UNSCANNED field. Both unsafe, for OPPOSITE reasons — which is exactly why one of them still looked like the good example, and why brief `275` cited it as the precedent to mirror.**

## Acceptance criteria

- [ ] **Both comments are made true or withdrawn** (`L82`). `:586-587` must stop claiming a general "event name only ⇒ safe" property; `:596-597` must stop asserting *"event/code only"* over a line that also prints `workspaceId`.
- [ ] **The port's second-channel contract is explicit** — either `workspaceId` is validated-or-omitted on `24.45`'s pattern before it is persisted or logged, **or** the port's doc states the second channel is caller-trusted **and cites the gate that makes it so** (see Q1; the evidence now exists).
- [ ] ⛔ **The refusal notice at `:588-590` carries NO signal-derived value.** Per the tracker, the safe form carries none at all — **including `denialCode`, which is unscanned because it is closed BY TYPE, not validated at runtime** (`### 24.55`: trusted provenance ≠ validated shape).
- [ ] **A test pins that a refused signal's field content does not reach the log sink**, with a **non-vacuity control** — a *persisted* (non-refused) signal still produces its normal record. ⛔ **Without the control, a test passes trivially against a function that logs nothing.**
- [ ] **Behaviour on the success path is unchanged** — the durable record still carries `workspaceId` unless Q1 rules otherwise; this is an audit record whose whole purpose is workspace attribution.
- [ ] `/preflight` clean, **per-package scoped**.
  ⛔ **THE TREE IS NOT GREEN.** Known red **by name**: `apps/desktop/test/bundle/main-bundle-resolution.test.ts` (pre-existing, unowned, `### 24.25`). Zero failing tests. ⚠ **Two implementers are concurrently mid-slice** — `knowledge` in `packages/knowledge` (`#38`, flipping a required field) and `providers-integrations` in `packages/policy` (`#48`). **A hold defers the commit, not the effect** — their uncommitted work is in your test runs. Any other failing test is **not** covered.

## Wiring / entry point (Step 7.5)

**Live, not dormant.** `createAuditPersistPort` is constructed once at **`apps/worker/src/boot.ts:2045-2048`** and reaches exactly two `persistDenial` call sites: **`apps/worker/src/api/procedures/copilot.ts:592`** (egress-veto DENY) and **`apps/worker/src/api/procedures/copilotAgentSynthesis.ts:588`** (ING-7 admission DENY) — both on the shipped `24.7` Copilot surface.

⛔ **The GCL leg is UNBOUND and this brief does not touch it:** `apps/worker/src/composition/crossWorkspaceRead.ts:139` calls `serveProjection(projection, sourceWorkspace.value)` with **two** arguments, so the optional 5th `auditPersist` is `undefined` and `persistDenialAudit` early-returns. **`packages/knowledge/src/gcl/projection.ts:129` is knowledge territory — do not edit it.**

## Files expected to touch

**Modified:** `apps/worker/src/boot.ts` (`:579-604` only) · a worker test

⛔ **Do NOT touch:** `packages/knowledge/**` · `packages/policy/**` (both areas mid-slice) · `apps/desktop/**`.

## RED test outline (Step 2)

1. **`refused_signal_logs_no_signal_derived_value`** — drive `persistDenial` with a signal that fails `isRedactionSafe`.
   - Asserts: the refusal notice contains **no** value derived from the signal — not `event`, not `denialCode`. Why: rule 7; the refusal exists *because* content was unsafe.
2. **`persisted_signal_still_produces_its_record`** — the non-vacuity control (`L7`/`L80`).
   - Asserts: a safe signal still appends a record carrying `workspaceId`. **Why: test 1 passes trivially if the function stops logging or stops persisting.**
3. **`append_failure_notice_matches_its_own_comment`** — force `deps.audit.append` to err.
   - Asserts: whatever the notice emits is exactly what the adjacent comment claims. ⭐ **The pin is the comment/code agreement, which is the defect** (`L82`).
4. **(conditional on Q1)** — if validate-or-omit is chosen: a credential-shaped `workspaceId` renders the sentinel, **plus** a legitimate id rendering normally (control).

⚠ **Mutation-verify test 1** (`contracts L90`): restore the `signal.event` interpolation ⇒ it must red. A pin never observed to fail is unproven.

## Cross-doc invariant impact

- **Model field changes:** none expected. **If you change `AuditPersistPort`'s signature, that IS a cross-doc invariant change — flag at Step 9, do not write the row.**
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5

1. ⛔ **Validate-or-omit, or document-as-caller-trusted?** The Done-when allows either, and **the evidence for the second now exists** — both call sites are registry-validated with a read-back re-gate. **My default vote: DOCUMENT the durable write as caller-trusted (citing the two gates by `file:line`), and VALIDATE-OR-OMIT on the LOG line only.** Rationale: the audit record's purpose *is* workspace attribution, so scrubbing it there destroys the artifact; a log sink has no such need, and the residual (a credential-shaped id in workspace **config**) reaches the log by the same door. ⚠ **Push back if you think that splits one concept across two policies** — it is the weakest part of my reasoning.
2. **Does `denialCode` come out of the refusal notice?** ⛔ **My default vote: YES, remove it.** It is unscanned because it is closed **by type**, not validated at runtime — `### 24.55`'s exact rule. ⚠ **But then the notice may carry nothing but a fixed string, which is `### 24.53`'s "a notice that carries nothing" problem.** **If you conclude the notice becomes useless, say so** — that is a real design tension and I would rather hear it than have you resolve it silently.
3. **Do the two open boundaries belong here?** (a) **registry population** — who may insert a `workspace_config` row, and is that path authz-gated; (b) **authz does not scope `workspaceId`** (`apps/worker/src/api/trpc.ts:97-118` gates *whether* a caller may call, not *which* workspace they may name). **My default vote: NEITHER — file both, fix neither.** Both are real, both are outside this port, and this stays a minimal rule-7 slice. **Confirm and I'll file them.**
4. **Bundle?** ⛔ **My default vote: NO.** Safety-invariant slice; own commit.

## Dependencies + sequencing

- **Depends on:** `### 24.7` (the shipped surface) · `### 24.45` `48ec7c91` (the pattern)
- **Blocks:** ⛔ **Phase 25.2/25.4's GCL port binding** — `24.62` is a **wiring precondition** (lead ruling): the port must not bind while `boot.ts` writes and logs a raw `workspaceId`. ⭐ **`24.50`/`24.52`'s shape: safe BY CONSTRUCTION, not safe by dormancy (`L106`).**

## Estimated commit count

**1.** Safety slice — do not bundle.

## Lessons-logged candidates anticipated

- **Convention candidate** — *a two-channel port must state which channels its gate covers; a gate named for the function it sits in reads as covering the function.*
- **Convention candidate** — *"trusted provenance ≠ validated shape" applies to closed-by-type values too (`denialCode`), not only to strings.*
- **Architecture-doc note candidate** — the `AuditPersistPort` second-channel contract, whichever way Q1 lands.

## How to invoke

1. Read this brief end-to-end — **especially the severity block; it changed what this slice is.**
2. Run **`/tdd audit_persist_second_channel_and_two_false_comments`**.
3. **Step 2.5** — answers to Q1–Q4 plus your bullet→test coverage map. ⛔ **My line numbers are read-derived at HEAD `20df48c7`; falsify them.**
