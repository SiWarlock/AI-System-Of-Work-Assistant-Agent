# /tdd brief — keychain_cli_backend (17.2 — BUILD PORTION ONLY)

## ⚠⚠ HARD-LINE BOUNDARY — read first
Task 17.2 is the **first real Keychain / OS-credential I/O** — a HARD LINE. **This brief covers ONLY the BUILD portion**: verify/finalize the no-shell `security`-CLI backend **wrapper** against a **MOCKED `execFile`** (an injected fake). It **crosses NO line**:
- **DO NOT** spawn the real `security` process against real credentials.
- **DO NOT** run `security add-generic-password` / provision anything.
- **DO NOT** read a real Keychain entry.
The **real-binary provisioning + read-back is the OWNER-GATED CROSSING** — the lead brings the owner the specific confirm; you build/verify the wrapper against a mock and STOP. If a test seems to need the real `security` binary or a provisioned entry, STOP and flag it.

## Feature
Verify/finalize the bounded **no-shell `execFile` `security`-CLI backend wrapper** (`apps/worker/src/secrets/keychain-backend.ts`) — args-array (no shell, no PATH), absolute bin, fail-closed fault classification, bounded+scrubbed debug detail, de-aliased returned bytes — proven end-to-end against an **injected fake `execFile`**, with the real `security` process constructed ONLY on the (owner-gated, not-in-this-slice) provisioned path. Worker-track. **Pure-build (the wrapper + fail-closed behavior); the real-binary provisioning is the owner crossing.** Closes the **BUILD half of G48**; G48 fully closes at the owner provisioning crossing.

## Use case + traceability
- **Task ID:** 17.2 (BUILD portion). **Closes:** G48 build half (full close at the owner crossing).
- **Architecture:** `ARCHITECTURE.md §19.4`, `§13` (Keychain creds in V1-local topology).
- **Depends on:** 17.1 (the adapter core).
- **Related context (confirm at Step 1 — you own the worker track; like 17.1, much may already be built by Lessons 9/10/11):**
  - **`apps/worker/src/secrets/keychain-backend.ts:22`** — the no-shell `execFile` wrapper (args-array — verify).
  - **`apps/worker/src/secrets/keychain-boot.ts:93`** — the real `security` process on the provisioned path (the construction guarded to the provisioned/owner path).
  - **Worker Lesson 10** (the CLI-backed secrets reader): args-array + **absolute bin** (no shell/PATH); the secret only in the ok Result (a fault path never reads stdout); classify faults by **stderr pattern + a live-verify note** (a fault NEVER maps to exit code 0); bound + scrub debug detail; **de-alias** returned bytes (`new Uint8Array(subarray)`, never `.slice` — a pooled Buffer's slice shares memory).
  - **Worker Lesson 11** (real-I/O adapter behind an owner-provisioning gate): the real process is constructed ONLY on the provisioned path; default-absent ⇒ inert/byte-equivalent; pin with a factory-spy zero-invocation on the unprovisioned path.

## Acceptance criteria (BUILD half — what "done" means)
- [ ] **Wrapper verified against a fake `execFile`:** the backend reads a key via an INJECTED fake exec (args-array, absolute bin) — NO real `security` spawned. Pin the args-array shape (no shell metacharacters, no PATH reliance).
- [ ] **Fail-closed fault classification (L10):** a non-zero exit / stderr-pattern fault ⇒ a typed err; a fault NEVER maps to a success (pin: a stderr fault with a misleading exit does not read stdout as a key). Pin the fail-closed-on-unprovisioned/locked behavior via the fake.
- [ ] **Secret only in the ok Result:** the fault path never surfaces stdout; debug detail is bounded + scrubbed (no key/raw stderr). Pin (rule 7).
- [ ] **De-aliased bytes (L10):** the backend copies stdout's pooled Buffer via `new Uint8Array(subarray)` (not `.slice`) — mutating the source pool doesn't change the returned bytes. Pin.
- [ ] **Real process constructed ONLY on the provisioned path (L11 dormancy):** with no owner provisioning, no real `security` process is constructed (factory-spy zero-invocation). Pin — the shipped default is inert.
- [ ] `/preflight` clean (worker + repo-wide). **NO real Keychain touched in any test.**

## Wiring / entry point (Step 7.5)
The `security`-CLI backend is the `KeychainBackend` the 17.1 adapter consumes; its real construction is guarded to the owner-provisioned path (17.3 boot gate). State: *"Wrapper reachable via the KeychainBackend seam over a fake exec; the real `security` process + provisioning is the owner-gated crossing (not this slice)."*

## Files expected to touch
**Modified/verified:** `apps/worker/src/secrets/keychain-backend.ts` (the wrapper) + tests (fake `execFile`). Confirm `keychain-boot.ts:93`'s real-process construction is guarded to the provisioned path (do NOT invoke it). NO provisioning, NO real `security`.

## RED test outline (Step 2)
1. `reads_a_key_via_the_fake_execfile_args_array` — a fake exec returns key bytes; the wrapper builds an args-array (no shell), absolute bin. (§13/L10)
2. `nonzero_exit_or_stderr_fault_fails_closed` — a fake exec with a fault ⇒ typed err, stdout NOT read as a key. (L10)
3. `fault_detail_is_bounded_and_scrubbed` — the err carries no key / no raw stderr (rule 7).
4. `returned_bytes_are_de_aliased` — mutating the fake's source pool doesn't change the returned bytes. (L10)
5. `no_real_security_process_on_the_unprovisioned_path` — factory-spy: 0 real `execFile`/`security` constructions in the shipped/unprovisioned default. (L11 dormancy)

## Cross-doc invariant impact
- **Model:** none. Confirm.

## Things to flag at Step 2.5
1. **NO-real-Keychain / NO-provisioning (LOAD-BEARING).** Confirm every test uses a fake `execFile`; the real `security` binary is NEVER spawned against real credentials; nothing is provisioned. This slice builds the wrapper only.
2. **Already-built-vs-complete** (like 17.1 — L9/L10/L11 likely built much). Confirm what exists vs. what 17.2's build half finalizes; don't rebuild working code.
3. **The owner crossing.** Name exactly what the owner crossing will be (provision the HMAC key + provider keys via `security add-generic-password`; then a real read-back) so the lead can bring the owner a precise confirm.

## Dependencies + sequencing
- **Depends on:** 17.1. **Blocks:** 17.3 (boot gate + getSecret). **Closes:** G48 build half. **The owner provisioning crossing is separate** — the lead gates it.

## Estimated commit count
**1** (the build half). **security-reviewer = invariant** (the OS-credential I/O wrapper + fail-closed + rule-7 scrub + L11 dormancy — a top safety surface). code-quality every-slice. **Pure-build — the real provisioning is the owner-gated hard line, NOT in this slice.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief — the HARD-LINE BOUNDARY at the top is the load-bearing safety point: **build/verify against a fake exec, provision NOTHING**.
3. `/tdd keychain_cli_backend`.
4. Step 2.5 — ping the no-real-Keychain + already-built-vs-complete + owner-crossing-naming answers. Don't GREEN until sign-off.
5. Step 9 — confirm the G48 build half + that NO real `security`/provisioning happened + the exact owner-crossing definition. **The lead then brings the owner the provisioning confirm before 17.2 fully closes.**
