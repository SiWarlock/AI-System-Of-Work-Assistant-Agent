# /tdd brief — multitask_source_note_frontmatter_projection

## Feature
Align the SOURCE-note frontmatter projection to the real model's MULTI-TASK extraction shape: the maiden run emitted task-prefixed `agent_extraction` fields (`task1_owner`, `task1_dueDate`, `task2_owner`, …) but the L49 fixed `["owner","dueDate"]` key loop reads only the BARE keys → both frontmatter fields degraded to the `TBD` fail-safe (safety held, no invention, but the note under-populated). Project the per-task fields into frontmatter while PRESERVING L49's structural injection-resistance. Pure/dormant note-shape cleanup — no new wiring, no upstream change (the candidate already carries every task field intact).

## Use case + traceability
- **Task ID:** 18.29
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (source extraction leg → note), §9 (workflow 1/4 source projection), §6 (KnowledgeWriter commit carries real content)
- **Related context:** worker LESSONS **L49** (the FIXED `[owner,dueDate]` frontmatter allow-list — structurally can't inject arbitrary frontmatter; degrades absent → `TBD`; neutralizes region-markers), **L35** (the `SourceBuildOutputsPort` fork; body written verbatim), **L51** (the open `agent_extraction` `fields` record); the Carry-forward "Note-projection ↔ extraction-schema field-name alignment" (origin 2026-07-18 maiden run CP3). Scoping confirmed the per-task data survives 1:1: candidate `agent-extraction.ts:82` → `mapAcceptedMeetingExtraction` (`meeting-extraction.ts:60`, iterates ALL fields) → `validateCloseout.ts:77` (`validated.fields = extraction.fields` verbatim) → the projection's fixed-key read is the ONLY lossy hop.

## Acceptance criteria (what "done" means)
- [ ] A source whose validated extraction carries task-prefixed fields (`task1_owner`, `task1_dueDate`, `task2_owner`, …) projects those values into the note frontmatter (no longer a blanket `TBD/TBD`) — via `SourceBuildOutputsPort.build` (`apps/worker/src/composition/buildActivities.ts:856`).
- [ ] **L49 injection-resistance PRESERVED:** the projected key set is a STRICT structural allow-list — bare `owner`/`dueDate` (backward-compat) PLUS keys matching exactly `^task\d+_(owner|dueDate)$`; an arbitrary/smuggled field (`workspaceId`, a path field, `../x`, a non-matching key) NEVER lands in frontmatter (pin with a hostile-fields test — the L49 load-bearing property).
- [ ] **Absent → `TBD` fail-safe retained (REQ-F-017):** a task field present in the pattern-space but absent from `fields` degrades to `TBD`, never invented; a genuinely single-task source (bare keys only) still projects `owner`/`dueDate` as today (byte-equivalent for the existing shape).
- [ ] **Marker-neutralization retained:** every projected value goes through `neutralizeFrontmatterValue(frontmatterValue(...))` (parity with the meeting projection; the L49 source-mutation pin extends to the task-prefixed values).
- [ ] **Determinism:** the projected task keys are emitted in a canonical order (numeric task-index ascending) with a defensive bound (see Step-2.5 Q2) — a re-projection of the same extraction yields byte-identical frontmatter.
- [ ] **Path unaffected (WS-8/traversal):** the note path is unchanged — `deriveSourceNotePath` keys ONLY on `SourceNoteIdentity {sourceId, contentHash}` (`sourceNotePath.ts:83`), independent of `fields`; pin that a multi-task frontmatter derives the IDENTICAL path.
- [ ] Reaches the note ONLY via the validated KMP → sole KnowledgeWriter `applyPlan` (rule 1 — no direct fs write); worker `/preflight` clean (typecheck + `pnpm --filter @sow/worker test`; repo lint/format remain env-waived).

## Wiring / entry point (Step 7.5)
**Already wired — no new wiring.** The projection runs inside `SourceBuildOutputsPort.build` (`buildActivities.ts:856-948`), bound on the source-ingestion run leg (`mapCandidate` at `:842`); the note commits via `sourceCommit`/`createCommitActivity` → `applyPlan` (`buildActivities.ts:963`). This slice changes the projection SHAPE at `buildActivities.ts:898/904-911` only. `/wired SourceBuildOutputsPort` to confirm the call site is unchanged.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/buildActivities.ts` — replace the fixed `SOURCE_FRONTMATTER_FIELDS = ["owner","dueDate"]` 2-key loop (`:898`, `:904-911`) with the multi-task-aware allow-list projection (bare + `^task\d+_(owner|dueDate)$`, canonical order, bound). Reuse `frontmatterValue` (`buildOutputs.ts:137`) + `neutralizeFrontmatterValue` (already imported `:177`).
- `apps/worker/test/composition/…` — the RED tests (new test file or extend the existing buildActivities/source-projection test).

If the chosen shape is (D) nested `tasks:` (see Step-2.5 Q1), it ALSO touches `packages/workflows/src/activities/projections/noteSlug.ts` (extend `neutralizeFrontmatterValue` to recurse object values) — **that crosses into `packages/workflows`; flag at Step 2.5 before GREEN** (I'd re-scope area then).

## RED test outline (Step 2)
1. **`multitask_fields_project_into_frontmatter`** — a validated extraction with `task1_owner/task1_dueDate/task2_owner/task2_dueDate` ⇒ those values appear in frontmatter (not TBD). Why: the L49-gap fix; §19.5/§9.
2. **`hostile_or_arbitrary_keys_never_land`** — `fields` carrying `workspaceId`, a path-ish key, a non-`task\d+_` key ⇒ NONE reach frontmatter (only bare + `^task\d+_(owner|dueDate)$`). Why: **L49 injection-resistance** (the load-bearing safety pin) — RED-on-weaken.
3. **`absent_task_field_degrades_to_TBD`** — a `taskN_owner` in the projected space but absent from `fields` ⇒ `TBD`, never invented. Why: REQ-F-017 fail-safe.
4. **`single_task_bare_keys_byte_equivalent`** — a bare `owner`/`dueDate`-only extraction projects exactly as today. Why: backward-compat / no regression.
5. **`multitask_frontmatter_marker_neutralized`** — a task value carrying a region-marker is neutralized. Why: L49 marker-neutralization parity (source-mutation pin).
6. **`multitask_frontmatter_path_unchanged`** — the note path/planId for a multi-task extraction == the single-task/identity-derived path. Why: WS-8/traversal (path is `SourceNoteIdentity`-only).
7. **`projection_is_deterministic`** — canonical task-index ordering; re-projection byte-identical. Why: determinism.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** NONE — reads the existing open `agent_extraction`/`ValidatedExtraction.fields` record; no contract/frozen surface changes. No schema-snapshot owed.
- **Orchestrator doc rows to write hot (Step 9):** none (no Appendix-A row). Likely a worker LESSONS update EXTENDING L49 (the fixed-key convention generalizes to a strict PATTERN allow-list that still can't inject arbitrary frontmatter) — flag at Step 9; the orchestrator writes it. Delete the Carry-forward "Note-projection" bullet on landing.

## Things to flag at Step 2.5
1. **Projection SHAPE.** (A) per-task frontmatter passthrough — strict `^task\d+_(owner|dueDate)$` allow-list, values emitted as flat `task1_owner: …` frontmatter keys; (B) aggregated single owner/dueDate (lossy — collapses N→1); (C) tasks list in the note BODY (frontmatter stays identity/bare, a `## Tasks` block in body); (D) nested `tasks:` array (crosses into `packages/workflows` — neutralizer must recurse objects). **My default vote: (A)** — least-lossy, keeps the data queryable as frontmatter, worker-only, mirrors the meeting projection loop (`meetingOutputs.ts:211`), and the strict pattern preserves L49's injection-resistance by construction. Consider (C) additionally if human-readability of many tasks matters — but (A) is the frontmatter-fidelity answer the Carry-forward asked for. Avoid (D) unless we accept the cross-package change.
2. **Bound + ordering.** The `agent_extraction` candidate is NOT yet key-count-bounded (maxProperties is a deferred Future-TODO, L51). Project all matching keys, or cap? **My default vote: canonical numeric-task-index ascending order + a defensive cap (e.g. first N by index, with a documented truncation note)** — determinism + a sane frontmatter size; the cap is belt-and-suspenders until the candidate schema bounds key count.
3. **Bare + task-prefixed union.** Keep projecting bare `owner`/`dueDate` (single-task, existing) alongside the task-prefixed pattern? **My default vote: YES (union)** — a single-task source keeps its current frontmatter; multi-task adds the task-prefixed keys; no regression.

## Dependencies + sequencing
- **Depends on:** the Phase-18 crossing (18.11–18.27, landed); the maiden-run observation. No blocker.
- **Blocks:** nothing hard — a quality refinement. (It improves the note fidelity for any future source extraction; independent of the auto-ingest ARM.)

## Estimated commit count
**1.** A focused note-projection shape change in one function. It touches the L49 injection-resistance invariant, so it gets its own commit + security-reviewer (do not bundle).

## Lessons-logged candidates anticipated
- **Convention candidate** — generalize L49: a FIXED-key frontmatter allow-list becomes a STRICT PATTERN allow-list (`^task\d+_(owner|dueDate)$` ∪ bare) that still structurally refuses arbitrary/smuggled keys — the injection-resistance is in the allow-list being an explicit pattern, not an open passthrough; absent → TBD (REQ-F-017) + marker-neutralization + canonical ordering retained.
- **Architecture-doc note candidate** — none expected (§19.5/§9 already describe the source→note projection; this is a shape refinement).

## How to invoke
1. **Read this brief end-to-end** — the shape (Q1) + bound/ordering (Q2) need answers before GREEN.
2. **Run `/tdd multitask_source_note_frontmatter_projection`.**
3. **Step 2.5** — ping back the asserted-invariant lines + coverage map (esp. the hostile-keys / injection-resistance pin) + your shape vote. security-reviewer MANDATORY at Step 8 (touches L49 injection-resistance).
4. **Step 9** — surface the L49-generalization LESSONS candidate + flag the Carry-forward "Note-projection" bullet for deletion.
