# /tdd brief — source_note_body_projection (15.3)

## Feature
Kill the `body: "source ingestion (C1)"` **placeholder** (`apps/worker/src/composition/buildActivities.ts:660`) — make the source-ingestion note projection build the note's **real body (and frontmatter)** from the gate-validated candidate extraction (the `SourceEnvelope.body` field added in 15.2), degrading to a safe minimal note when body is absent (Lesson 15). The note still commits through the REAL KnowledgeWriter `applyPlan` (safety rule 1 — sole Markdown writer, unchanged); the body is candidate data that already passed the `SourceEnvelope` schema gate (rule 2 — never routed around it). Worker-track; **ISOLATED (candidate-data → Markdown, security-reviewer=invariant)**.

## Use case + traceability
- **Task ID:** 15.3
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (primary — "body/frontmatter validated extraction" note projection), `§6` (KnowledgeWriter = sole autonomous Markdown writer; Markdown canonical), `§8` (source ingestion / SourceEnvelope). Safety **rule 1** (one writer) + **rule 2** (candidate-data gate).
- **Depends on:** **15.2** (`a6495293` — `SourceEnvelope.body?`) — LANDED. 15.1 (`a6190122` — the C1 bridge context).
- **Related context (confirm at Step 1 — you own this spine; you built C1 + 15.1):**
  - **The placeholder site:** `buildActivities.ts` `sourceBuildOutputs.build(validated: ValidatedExtraction, ws, source: SourceNoteIdentity)` builds a `KnowledgeMutationPlan` whose single `creates[0]` today hardcodes `body: "source ingestion (C1)"`, `frontmatter: {}`, `title: "Ingested: <sourceId>"`. This slice makes `body` (and `frontmatter`) real.
  - **The real content source:** `SourceIngestionContext.source: SourceEnvelope` now carries `body?` (15.2 — the candidate extracted text). The driver holds the full context; `build()` is deliberately handed only `SourceNoteIdentity` (`{sourceId, contentHash}`) so **attacker-influenceable fields are structurally excluded from PATH derivation** (`deriveSourceNotePath`). The note **BODY** may use the validated content — but the note **PATH** must STILL derive only from `SourceNoteIdentity` (never from `body`). Keep that split intact.
  - **Candidate-gate discipline (rule 2):** `body` is candidate data. It already cleared the `SourceEnvelope` Zod + JSON-Schema gate (15.2 — string-if-present, rejected otherwise). The note body must be the GATE-VALIDATED value (via `validated`/derived-from `source.body`), NOT raw bytes threaded around the gate. No new bypass; the projection stays deterministic.
  - **One-writer (rule 1):** unchanged — the plan still commits via `createCommitActivity`/`applyPlan` (the sole Markdown writer). This slice only fills the plan's `creates[0].body`/`frontmatter`; it does not add a writer.
  - **Lesson 15 (additive-field, contracts) in action:** `body` is OPTIONAL; the consumer (this slice) degrades gracefully when it's absent — a real minimal note, NOT the old placeholder and NOT a failure.

## Acceptance criteria (what "done" means)
- [ ] The source-ingestion note projection populates `creates[0].body` from the **gate-validated candidate extraction** (the `SourceEnvelope.body` value threaded to the build), replacing the `"source ingestion (C1)"` placeholder. A note built from a source **with** `body` contains that real content.
- [ ] **Absent-body degrade (Lesson 15):** a source **without** `body` produces a safe **minimal real note** (e.g. a title/frontmatter-only note or an explicit "no extracted content" marker — your call at Step 2.5), NEVER the old placeholder string and NEVER a build failure. Pin both legs (with-body → real content; without-body → the degrade).
- [ ] **Frontmatter (scope — confirm at Step 2.5):** if in scope, `creates[0].frontmatter` is populated from the source/validated extraction instead of `{}`; else explicitly deferred with a one-line note. ARCHITECTURE says "body/frontmatter validated extraction" — default to body+frontmatter unless frontmatter needs its own slice.
- [ ] **Path unchanged / structurally body-independent (WS-8 / traversal):** the note PATH still derives ONLY from `SourceNoteIdentity` (`deriveSourceNotePath(ws, source)`) — `body` NEVER influences the path (a pin: two sources with the same identity but different `body` derive the SAME path; a hostile `body` can't traverse). `SourceNoteIdentity` is NOT widened to carry `body`.
- [ ] **Candidate-gate intact (rule 2):** the body written is the schema-gate-validated value; a non-string / gate-rejected body never reaches the plan (it was already rejected at the `SourceEnvelope` gate — pin that the projection consumes only validated content, adds no bypass).
- [ ] **One-writer intact (rule 1):** the note commits via the existing real KnowledgeWriter `applyPlan` path — no new writer, no direct Markdown write.
- [ ] `/preflight` clean (worker + repo-wide); the C1 source-ingestion integration (`sourceIngestion-live.test.ts`) + the projection unit tests green.

## Wiring / entry point (Step 7.5)
`sourceBuildOutputs.build` in `buildActivities.ts` — the note-projection leaf of the C1 source-ingestion binding. Reachable through the C1 spine's build→commit path (exercised by `sourceIngestion-live.test.ts`); the binding itself is the OPTIONAL make-it-real C1 seam (`sourceBinding === undefined` leaves boot byte-equivalent). Confirm at Step 7.5 whether the body-threading is exercised by the live integration (expected) or needs a waiver note if the only path is dormant (L11). Name the exact threading site in your Step-9. **State: reachable via the C1 build path (integration-exercised).**

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/buildActivities.ts` — the `sourceBuildOutputs.build` projection (real `body`/`frontmatter`; the body-threading).
- possibly `packages/workflows/src/ports/sourceIngestion.ts` — IF the cleanest threading adds the validated body to `ValidatedExtraction` or a new **explicit** build param (NOT onto `SourceNoteIdentity`). Confirm at Step 1.
- possibly the C1 driver that calls `build()` (to thread `context.source.body` / the validated content through).
- tests: the projection unit (with-body + absent-body-degrade + path-body-independence pins) + any `sourceIngestion-live` assertion for the real note body.

If the threading needs files beyond this, **flag at Step 2.5**.

## RED test outline (Step 2)
1. `note_body_is_real_content_when_source_has_body` — a source with `body` ⇒ `creates[0].body` is that gate-validated content (not the placeholder). (§19.2)
2. `note_body_degrades_safely_when_body_absent` — a source without `body` ⇒ a safe minimal real note (title/frontmatter-only or explicit marker), NOT `"source ingestion (C1)"`, NOT a failure. (Lesson 15)
3. `note_path_is_body_independent` — two sources same `SourceNoteIdentity`, different `body` ⇒ SAME `deriveSourceNotePath` result; `body` never reaches the path. (WS-8 / traversal-safety)
4. `body_written_is_gate_validated_not_raw_bypass` — the projection consumes only the validated body; there is no path by which an ungated/non-string body reaches the plan `creates[0].body`. (rule 2)
5. `note_commits_via_real_knowledgewriter_unchanged` — the plan still flows to the real `applyPlan` commit (no new writer, rule 1) — assert the commit path is unchanged / the projection only fills body+frontmatter. (rule 1)
6. (if frontmatter in scope) `note_frontmatter_populated_from_source` — `creates[0].frontmatter` reflects the source/validated extraction, not `{}`. (§19.2)

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model:** IF the threading adds a field to `ValidatedExtraction` or the source-ingestion port types (a shared `@sow/workflows`/`@sow/contracts` seam), flag it as a `Cross-doc invariant change` — I write the arch/cross-doc row. If the change is internal to `buildActivities.ts` (no shared-seam field), no cross-doc row. Confirm which at Step 2.5.
- **Shared-contract seam touched?** Only if `ValidatedExtraction`/port types gain a field → then a schema-snapshot test if it's an Appendix-A model. Confirm.

## Things to flag at Step 2.5
1. **Body-threading path (LOAD-BEARING).** How does the gate-validated body reach `build()`? My default vote: thread it as an EXPLICIT input distinct from `SourceNoteIdentity` — either (a) the validated body lives on `ValidatedExtraction` (semantically "the validated extraction that becomes the note") and `build()` reads `validated.<body>`, or (b) a new explicit body/content param on `build()`. Do NOT fold `body` onto `SourceNoteIdentity` (that would leak an attacker-influenceable field into path derivation). Pick the cleanest reachable one against your C1 driver; keep the path structurally body-independent.
2. **Absent-body degrade shape.** What is the minimal real note when `body` is absent? My default vote: a title + (frontmatter if in scope) note with an explicit "no extracted content yet" body marker (honest, not the C1 placeholder, not empty-that-reads-as-real). Your call on the exact degrade content.
3. **Frontmatter scope.** body-only this slice, or body+frontmatter? My default vote: include frontmatter if it threads trivially from the same validated extraction; if it needs its own extraction/derivation, defer frontmatter to a follow-up and note it. ARCHITECTURE names both.
4. **Cross-doc surface.** Does your threading touch a shared port/contract type (→ cross-doc flag + possibly schema-snapshot) or stay internal to `buildActivities.ts`? Tell me at Step 2.5 so I can pre-stage any Appendix-A/cross-doc row.

## Dependencies + sequencing
- **Depends on:** 15.2 (`a6495293`, LANDED), 15.1 (C1 bridge context).
- **Blocks:** the real make-it-real source-ingestion note quality; adjacent to 15.5 (disposition) / 15.9 (meeting dispatcher). Does NOT gate them.

## Estimated commit count
**1.** The projection change + its threading + tests. **security-reviewer = invariant** (candidate-data → Markdown body; the rule-2 gate-intact + rule-1 one-writer + the body-must-not-influence-path traversal legs); code-quality every-slice. **Pure-build — NO hard line** (the C1 binding stays optional/dormant; the sole real writer is unchanged; no external egress).

## Lessons-logged candidates anticipated
- **Convention candidate** — a make-it-real note projection consumes the GATE-VALIDATED candidate body (never raw-around-the-gate, rule 2), degrades to a safe minimal real note when the optional body is absent (Lesson 15), and keeps the note PATH structurally body-independent (only `SourceNoteIdentity` keys the path — a hostile body can't traverse), committing through the unchanged sole KnowledgeWriter (rule 1).

## How to invoke
1. Continue the worker-impl2 session (oriented from 15.1/15.4 — no `/session-start` unless swapped).
2. Read this brief end-to-end (Q1 body-threading + Q4 cross-doc surface are load-bearing).
3. `/tdd source_note_body_projection`.
4. Step 1 — confirm the threading path (`ValidatedExtraction` vs an explicit build param), the C1 driver call site, and whether a shared port/contract type changes.
5. Step 2.5 — ping back the design answers (Q1 threading is load-bearing). Don't GREEN until I sign off.
6. Step 9 — categorized flags; surface any shared-seam/cross-doc change so I write the row hot.
