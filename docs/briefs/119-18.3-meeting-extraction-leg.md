# /tdd brief — meeting_transcript_extraction_leg

## Feature
Make `mapCandidate` read the **accepted** `BrokerOutcome` (today it ignores the outcome and echoes the injected `params.meetingExtraction`), gated by a real meeting-extraction **output schema** (candidate-data gate) + a **`NoInferenceView`** enforcing REQ-F-017 (ambiguous/absent owner or date ⇒ `TBD`, NEVER an invented value). **SAFE-BUILD: the run leg is still 18.1's dormant stub, so no real model/prompt executes — the deterministic mapCandidate + schema + no-inference wiring is TDD; the real prompt + model extraction is eval-tested/deferred to the crossing.**

## Use case + traceability
- **Task ID:** 18.3
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (primary), `§9` (workflow 1 — meeting extraction), candidate-data gate REQ-S-006 (§3/§7)
- **Related context:**
  - **Evidence:** `mapCandidate:(_outcome)=>params.meetingExtraction` ignores the accepted `BrokerOutcome` and echoes the injected extraction; 18.3 makes it TRACE the accepted outcome. (`mapCandidate`/`MeetingBroker`/`RunAgentJobActivityDeps`/`defaultMapRejection` live in `packages/workflows/src/activities/runAgentJob.ts`; injected via `buildActivities.ts` — plan line-refs may be stale, trace the real symbols at Step 1.)
  - **`NoInferenceView`** (`packages/providers/src/broker/schema-gate.ts:55`): `(job, validatedOutput) => Result<unknown, {field,code}[]>` — the REQ-F-017 seam, injected via `SchemaGateConfig`. This slice authors the concrete meeting view (owner/date) + injects it. **This is the tracked REQ-F-017 EXTRACTION-surface binding (deferred from 18.2 — the routing surface landed at 18.6/18.5).**
  - **Builds on 18.2's KMP stand-in:** 18.2 made the meeting broker candidate a KMP under `sow:knowledge-mutation-plan`, injected `modelSchemas={KMP,PA}` worker-side, and left `mapCandidate` echoing `params.meetingExtraction`. 18.3 threads the real (schema-gate-validated) extraction through to `mapCandidate` — trace at Step 1 how `candidateOutput → BrokerCandidate → mapCandidate` carries the extraction (does the meeting `outputSchemaId` move off the KMP stand-in to a real extraction schema + normalizer, or does mapCandidate read the KMP? — Q4).
  - **Prior slices (landed):** 18.1 `99cae521` (dormant run leg), 18.2 `d4c2a9a0` (schema gate + KMP stand-in), 15.9 (meeting dispatcher — the production trigger via `dispatchMeetingCloseout`).

## Acceptance criteria (what "done" means)
- [ ] `mapCandidate` reads the **accepted** `BrokerOutcome`'s candidate (schema-gate-validated), NOT `params.meetingExtraction` — the accepted extraction traces through to the closeout note.
- [ ] **Candidate-data gate (rule 2 / REQ-S-006):** a schema-invalid meeting extraction is rejected at the SCHEMA gate (`schema_rejected`), no downstream side effect.
- [ ] **REQ-F-017 (extraction surface):** an ambiguous/absent owner or dueDate yields **`TBD`**, never an invented value — enforced by the injected meeting `NoInferenceView`; the gate NEVER coerces a field to satisfy the schema.
- [ ] **Accept-path preserved:** the meeting.close proof-spine still produces its closeout note for a valid extraction (byte-equivalent via the accepted outcome); full worker suite green.
- [ ] **SAFE-BUILD:** no real model/prompt executes — the extraction comes from the (dormant 18.1 stub) run leg; the real prompt + eval-class pass is deferred to the crossing.
- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
Reached in production via `dispatchMeetingCloseout` (15.9) → the meeting-closeout workflow → `runAgentJob` → `broker.runJob` → `mapCandidate`. This slice changes `mapCandidate` to trace the accepted outcome + injects the meeting schema/`NoInferenceView` into the gate (18.2's `SchemaGateConfig`). Confirm the accepted-outcome path is reached on the real workflow (not just tests) — the -live meeting-closeout run is the end-to-end check.

## Files expected to touch
**New:**
- `apps/worker/test/composition/meeting-extraction.test.ts` (or the meeting-activities test) — the RED tests below.
- Possibly a meeting-extraction schema + `NoInferenceView` module (worker composition, or `packages/providers/src/model` — see Q1).

**Modified:**
- `packages/workflows/src/activities/runAgentJob.ts` and/or `apps/worker/src/composition/buildActivities.ts` — `mapCandidate` reads the accepted `BrokerOutcome`; inject the meeting extraction schema + `NoInferenceView` into the gate config.

> **Providers leg — flag at Step 1.** The real PROMPT belongs in `packages/providers/src/model` but only executes at the crossing (dormant run leg now) — **defer the prompt to the crossing/eval unless you find it's needed for the deterministic path.** The extraction SCHEMA + `NoInferenceView` may be worker-injectable (like 18.2's `modelSchemas`) — if instead they must be providers-authored, flag at Step 1 for a thin `integrations-impl` co-task (S4a providers schema → S4b worker wiring).

## RED test outline (Step 2)
1. **`mapCandidate_reads_accepted_outcome_not_params`** — with an accepted `BrokerOutcome` carrying a distinct extraction, `mapCandidate` returns THAT (traced), not `params.meetingExtraction`. Why: 18.3 evidence / §9.
2. **`extraction_schema_rejects_invalid`** — a schema-invalid extraction → `schema_rejected`, no side effect. Why: rule 2 / REQ-S-006.
3. **`extraction_schema_accepts_valid`** — schema-valid extraction fields accepted. Why: §7 accept.
4. **`noinference_ambiguous_owner_yields_tbd`** — ambiguous/absent owner → `TBD`, never invented. Why: REQ-F-017.
5. **`noinference_ambiguous_date_yields_tbd`** — ambiguous/absent dueDate → `TBD`. Why: REQ-F-017.
6. **`noinference_never_coerces_to_satisfy_schema`** — the gate rejects/TBD-marks rather than coercing an unstated field to pass. Why: REQ-F-017 (no silent invention).
7. **`accept_path_meeting_close_stays_green`** — the meeting.close proof-spine produces its note via the accepted outcome (byte-equivalent for a valid extraction). Why: accept-path.
8. **`safe_build_no_real_model_call`** — the extraction comes from the (stub) run leg; no real model/prompt executes. Why: SAFE-BUILD.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** confirm at Step 9 — if the meeting-extraction schema reuses/extends an EXISTING contract type (e.g. `ValidatedNarrative`/`ExtractionField`), no field change; if it introduces a NEW frozen extraction model, that's a cross-doc invariant + possible contract-impl (Appendix-A + snapshot). **Prefer reusing the existing extraction shape.**
- **Orchestrator doc rows to write hot:** none anticipated unless a new frozen model lands.
- **Shared-contract (Appendix-A cross-track seam) model touched?** Flag if the extraction schema is a new frozen model crossing a cross-track (Appendix-A subsystem-boundary) seam → the RED outline must add the schema-snapshot test + I route the Appendix-A row.

## Things to flag at Step 2.5
1. **Extraction schema location** — worker-injected into `SchemaGateConfig` (like 18.2's `modelSchemas`), reusing/extending the existing `ValidatedNarrative`/`ExtractionField` shape, vs a NEW providers/contracts schema. My default vote: **worker-injected, reuse the existing extraction shape** — no new frozen model, no contract-impl. Flag at Step 1 if a providers/contract schema is genuinely required.
2. **Prompt deferral** — the real prompt executes only when the transport is armed (crossing); defer it. My default vote: **defer the prompt to the crossing/eval**; safe-build authors schema + `NoInferenceView` + mapCandidate only.
3. **`NoInferenceView` scope + "invented" detection** — which fields (owner, dueDate) get the TBD check, and how "invented" is detected. My default vote: **owner + dueDate (the REQ-F-017 named fields); the DETERMINISTIC check is "ambiguous/absent ⇒ TBD, no coercion"**; a semantic "value-not-grounded-in-transcript" check is eval-territory (the model's job) — the deterministic gate pins the TBD/no-coercion contract, not transcript-grounding. Flag the boundary.
4. **18.2 KMP-stand-in threading** — does the meeting `outputSchemaId` move off the KMP stand-in to a real extraction schema (+ a normalizer extraction→KMP), or does `mapCandidate` read the KMP candidate? My default vote: **trace at Step 1; the goal is `mapCandidate` reads the accepted schema-validated extraction** — pick the threading that keeps the accept-path green + the gate strict, and report it at Step 9.

## Dependencies + sequencing
- **Depends on:** 18.1 (`99cae521`), 18.2 (`d4c2a9a0` — schema gate + KMP stand-in), 15.9 (meeting dispatcher). All landed.
- **Blocks:** 18.7 (ProposedAction from real extraction), 18.8 (commit callers carry real extraction).

## Estimated commit count
**1** — one focused slice (worker-primary). **INVARIANT** (candidate-data gate rule 2 + REQ-F-017 extraction surface) → **security-reviewer = invariant; own commit, no bundling.** If a providers schema co-task is needed (Q1), it's a separate commit (S4a) that S4b builds on.

## Lessons-logged candidates anticipated
- **Convention candidate** — "the extraction leg traces the ACCEPTED (schema-gate-validated) `BrokerOutcome`, never the injected params; REQ-F-017's extraction surface (ambiguous owner/date ⇒ TBD, no coercion) is a `NoInferenceView` injected into the gate — distinct from the routing surface (park-not-guess, 18.6/18.5)."
- **Architecture-doc note candidate** — the meeting-extraction schema shape + the `NoInferenceView` owner/date contract (§19.5/§9).
- **Future TODO — crossing:** the real meeting-extraction prompt + eval-class pass (the model-driven half) = the consolidated crossing.

## How to invoke
1. **Read this brief end-to-end** — the 4 Step-2.5 questions need answers before GREEN.
2. **Run `/tdd meeting_transcript_extraction_leg`** in the `worker-impl` session.
3. **Step 0 (Restate)** — SAFE-BUILD; deterministic mapCandidate + schema + no-inference; prompt deferred.
4. **Step 1 (Identify files)** — trace the real `mapCandidate`/schema threading (18.2 KMP stand-in); flag if a providers schema/prompt co-task is needed (Q1).
5. **Step 2.5** — send the test write-up + votes on the 4 questions; wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 8 reviewers** — security-reviewer (invariant: candidate-data gate + REQ-F-017) + code-quality-reviewer (every-slice).
7. **Step 9** — categorized flags + ship-ask; report the KMP-stand-in threading resolution (Q4).
