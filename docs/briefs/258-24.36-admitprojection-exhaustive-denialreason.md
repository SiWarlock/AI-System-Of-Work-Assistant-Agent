# /tdd brief — admit_projection_exhaustive_denial_reason_no_ifelse_absorb

## Feature
`admitProjection`'s `DenialReason` narrowing (`packages/knowledge/src/gcl/visibility-gate.ts:128-146`) is an un-guarded `if`/`if`/trailing-return — the third instance this round of `L134`'s shape (a closed union narrowed without exhaustiveness, silently absorbing any future member into the wrong branch), wearing an `if`/`if`/`return` costume instead of a literal `switch`/`default:`. Its own comment directly above claims the opposite of what the code does. Make it exhaustive so a future 4th `DenialReason` member is a compile error here, not a silent `malformed_policy_input` mislabel.

## Use case + traceability
- **Task ID:** `24.36`
- **Architecture sections it implements:** §6 — Safety rule 4

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 safety-assertion audit and its own Step-9 findings, which sweep every area, so a remediation task legitimately cites the anchors its originating finding lives under rather than only Phase 24's own declared set. Same widened-scope shape as briefs 254/255/257.

- **Related context:** `IMPLEMENTATION_PLAN.md:3212-3218` (task 24.36, full text). `packages/contracts/LESSONS.md#134` — *a `default:` branch in a mapper over a closed union is a silent absorber; terminate with `assertNever` instead*. Direct sibling: `docs/briefs/254-24.30-gatereason-exhaustive-switch.md` (`24.30`, same file's neighboring function, same fix shape, already landed `4730211b`) — mirror its pattern rather than inventing a new one.

## Acceptance criteria (what "done" means)
- [ ] `admitProjection`'s narrowing over `decision.reason` (a `DenialReason = HardDenial | SupportDenial`, per the type this brief's premise rests on — **confirm the current full member list against the real type before Step 1**, don't trust this brief's count as authoritative) is exhaustive — every member has its own explicit branch.
- [ ] The narrowing terminates with an `assertNever`-style exhaustiveness guard (mirror `24.30`'s landed pattern in this same package), never an un-guarded trailing `return` that absorbs any unhandled case into `malformed_policy_input`.
- [ ] The comment directly above the narrowing (currently claiming "never a `default:`/trailing-else absorb... avoided here on purpose") is corrected to match what the code now actually does — it was wrong before this fix and must not go stale again after it.
- [ ] Today's three real reasons (`VISIBILITY_EXCEEDS_SOURCE`, `VISIBILITY_TYPE_MISMATCH`, and whatever the third member turns out to be — confirm it's genuinely `MALFORMED_POLICY_INPUT` as an explicit union member, not just "whatever's left") each produce byte-identical output to before this fix — regression pin, not a rewrite of correct behavior.
- [ ] A future `DenialReason` member addition becomes a compile error here, not a silent absorption — verify structurally (the RED outline's type-level test), not by checking today's members individually.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
None new — `admitProjection` is already called from its two existing (currently-dormant per `24.33`'s own in-flight finding, verified separately — not this brief's concern) call sites; this slice changes its internal narrowing logic, not its callers or signature.

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gcl/visibility-gate.ts` — `admitProjection`'s `DenialReason` branch, lines ~124-147, plus its now-inaccurate comment

**New:**
- test file(s) under `packages/knowledge/test/gcl/` (confirm exact existing path/convention before Step 1 — likely alongside `24.30`'s new tests if that suite already exists in this package, or `admitProjection`'s own existing test file if one covers this function today)

## RED test outline (Step 2)
1. **`admit_projection_visibility_exceeds_source_unchanged`** — Asserts: a `VISIBILITY_EXCEEDS_SOURCE` decision still produces `{code: "visibility_exceeds_source", ...}` byte-identical to before. Why: regression pin on the first pre-existing correctly-handled case.
2. **`admit_projection_visibility_type_mismatch_unchanged`** — Asserts: same, for `VISIBILITY_TYPE_MISMATCH` → `{code: "visibility_type_mismatch", ...}`. Why: regression pin on the second pre-existing correctly-handled case.
3. **`admit_projection_malformed_policy_input_still_explicit`** — Asserts: the third/remaining reason still produces `{code: "malformed_policy_input", ...}`, but now via its OWN explicit branch, not a trailing fallthrough. Why: confirms the fix didn't just rename the fallthrough — it must be a real branch keyed on the actual reason value.
4. **`admit_projection_denial_reason_narrowing_is_exhaustive_at_compile_time`** — type-level pin, not a runtime assertion: confirm (mirror whatever mechanism `24.30`'s own equivalent test used — check its test file for the pattern first) that adding a hypothetical new `DenialReason` member fails to compile until this narrowing handles it. Why: this is the actual point of the fix — the three runtime tests above only pin today's members, not the mechanism that stops a future one from being silently absorbed.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — `DenialReason`/`HardDenial`/`SupportDenial` are unchanged; this fixes a consumer's handling of them.
- **Orchestrator doc rows to write hot:** none anticipated.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **Confirm `DenialReason`'s exact current member set before writing any test** — this brief's premise (three total reasons) comes from the tracker's own finding text, not a fresh read. My default vote: **re-derive it from the type definition directly** (likely in `packages/policy`, wherever `validateProjectionVisibility` and its `HardDenial`/`SupportDenial` union live) rather than trusting either this brief or the tracker's finding text as authoritative — same premise-checking discipline that caught `24.33`'s "live, not dormant" error earlier today.
2. **Mirror `24.30`'s exhaustiveness-guard mechanism exactly, or does this function's different return shape (typed `Result` error objects, not a plain string) need a different pattern?** My default vote: **mirror `24.30`'s mechanism** unless the return-shape difference genuinely forces a variant — state which, and why, at Step 2.5 rather than silently diverging.

## Dependencies + sequencing
- **Depends on:** `24.30` ✅ `4730211b` (the sibling fix in the same file this one mirrors).
- **Blocks:** none tracked.

## Estimated commit count
**1.** Focused exhaustiveness fix, one function, same shape as `24.30`'s single-commit precedent.

## Lessons-logged candidates anticipated
- None new anticipated — this is `L134` applied a third time in the same codebase (after `24.23` and `24.30`), not a new pattern. If this instance reveals something `24.30`'s framing missed (e.g. the `if`/`if`/return costume vs. a literal `switch`/`default:` — already noted in the tracker as worth asking whether `24.34`'s concept sweep should cover both shapes), flag it at Step 9 rather than assuming it's identical.

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md:3212-3218` (task 24.36) + `packages/contracts/LESSONS.md#134` + `docs/briefs/254-...md` (the sibling fix to mirror).
2. Run `/tdd admit_projection_exhaustive_denial_reason_no_ifelse_absorb`.
3. Step 2.5 — both questions are quick confirmations against a sibling precedent, not open design forks; take the defaults unless the real type/return-shape contradicts them.
