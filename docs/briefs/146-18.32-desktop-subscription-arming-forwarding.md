# /tdd brief — desktop_subscription_arming_forwarding

## Feature
Thread the **subscription-extraction arming opt-in** (plain-data `subscriptionArm: {enabled, model?, betas?}`) + the **egress-processor allowlist** (`egressAllowedProcessors: string[]`) through the desktop `WorkerHostConfig` → `bootWorker`, sourced from env in Electron main. Makes the app **capable** of running subscription-armed auto-ingest — **dormant, default-OFF, byte-equivalent** when the env is unset. This is the Path-β desktop leg; it does NOT flip anything on.

## Use case + traceability
- **Task ID:** 18.32
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (real model transport / subscription arming — the desktop config surface for it), `§5` (egress allowlist)
- **Related context:** Today `apps/desktop/main/index.ts:73-90` builds `WorkerHostConfig` with no subscription-arming or egress-allowlist field, so the packaged app can only run the **unarmed KMP/stub** extraction (a benign source → NO real note — the silent-no-note trap the maiden run taught us). The worker `BootConfig.subscriptionArm` (`apps/worker/src/boot.ts:280-285`, `{enabled?, model?, makeCompletion?, checkReachable?}`) is the Option-B path; the egress-allowlist worker seam lands in **18.31** (must be merged first). **IPC CONSTRAINT (load-bearing):** `WorkerHostConfig` crosses a `child_process.fork` IPC channel (structured-clone), so **function-valued deps cannot cross** — the desktop passes only the plain-data `{enabled, model, betas}` slice; `bootWorker` supplies the real `makeCompletion` default, and the real `checkReachable` probe + the actual go-live flip stay **worker-host-side / owner-gated future steps** (this slice makes the app CAPABLE, not armed).

## Acceptance criteria (what "done" means)
- [ ] BOTH `WorkerHostConfig` interfaces (`apps/desktop/main/worker-supervisor.ts:12` and `apps/desktop/worker-host/index.ts:53`) gain: `subscriptionArm?: { enabled?: boolean; model?: string; betas?: readonly string[] }` (plain-data only — NO thunks) and `egressAllowedProcessors?: readonly string[]`. Kept in sync (the interfaces mirror by contract).
- [ ] `apps/desktop/worker-host/index.ts` `start()` forwards both into the `bootWorker({...})` object via **conditional-spread** (`...(config.subscriptionArm !== undefined ? { subscriptionArm: config.subscriptionArm } : {})`), and passes `egressAllowedProcessors` into the `gateAutoIngest(...)` call (the 18.31 param).
- [ ] `apps/desktop/main/index.ts` reads env (default-OFF) and populates the new fields via the existing conditional-spread idiom: `SOW_SUBSCRIPTION_ARM` (`"1"|"true"` strict-parse, mirror `SOW_INGEST_WATCH`) → `subscriptionArm.enabled`; `SOW_SUBSCRIPTION_MODEL` → `.model`; `SOW_EGRESS_ALLOWED_PROCESSORS` (comma-split, trimmed, empties dropped) → `egressAllowedProcessors`.
- [ ] **Byte-equivalent default (safety/dormancy):** with none of the new env vars set, the object passed to `bootWorker` is **key-for-key identical** to today's (the new keys are OMITTED, never `: undefined`). A new worker-host config-builder test pins: unset ⇒ no `subscriptionArm`/`egressAllowedProcessors` keys; set ⇒ present with the parsed values.
- [ ] Arming via env ALONE does not produce spend: with `SOW_SUBSCRIPTION_ARM=1` but no worker-host-side real `checkReachable` injected, the arming degrades HEALTH-denied (`FAIL_CLOSED_REACHABILITY`) — assert/observe this is the intended dormant posture (another OFF-lock), not a regression.
- [ ] **Composition tie with 18.33 (anti-drift — lead ask).** The `arming-forward` helpers return the **shared worker-owned types** (`AutoIngestGateOpts` + `BootConfig.subscriptionArm`), so tsc breaks BOTH this test and 18.33 on any field-shape drift (the structural tie — no hand-mirror possible). This slice stays **processor-AGNOSTIC**: it forwards `egressAllowedProcessors` as opaque owner-set strings (never hard-codes the `claude-agent-sdk` semantic — that value's "→note" meaning is owned by 18.33's worker constant + the runbook). Add a comment in the `arming-forward` test cross-referencing brief 147 / the 18.33 harness (they compose: this pins desktop→armed-shape, 18.33 pins armed-shape→note).
- [ ] `/preflight` clean (desktop `tsc --noEmit` + vitest; note: `pnpm lint`/`format:check` are env-blocked repo-wide — flag, not a blocker).

## Wiring / entry point (Step 7.5)
Production chain (all real, no waiver): `app.whenReady()` → `startWorker()` (`main/index.ts:61`) → builds `WorkerHostConfig` from env (`:73-90`) → `createWorkerSupervisor({fork, config})` → `fork(../worker/desktop-host.mjs)` → IPC `send({type:"config", config})` (`worker-supervisor.ts:115`) → worker-host `start(config)` (`worker-host/index.ts:89`) → `boot.bootWorker({...})`. The new env reads + forwards are reached on **every app launch**; dormant (default-OFF) when env unset. No later-slice wiring — this closes the desktop capability.

## Files expected to touch
**Modified:**
- `apps/desktop/main/worker-supervisor.ts` — `WorkerHostConfig` interface (+2 optional fields).
- `apps/desktop/worker-host/index.ts` — `WorkerHostConfig` interface (mirror) + the `bootWorker({...})` construction (conditional-spread forwards + pass `egressAllowedProcessors` into `gateAutoIngest`).
- `apps/desktop/main/index.ts` — env reads (`SOW_SUBSCRIPTION_ARM`/`SOW_SUBSCRIPTION_MODEL`/`SOW_EGRESS_ALLOWED_PROCESSORS`) + populate the config via conditional-spread.

**New:**
- `apps/desktop/test/main/worker-host-config.test.ts` (or nearest existing home) — the byte-equivalence + forward pins (no worker-host→bootWorker test exists today; introduce a small config-builder unit or extract the mapping so it's testable without spawning a real worker).

## RED test outline (Step 2)
1. **`config_omits_new_keys_when_env_unset`** — no new env ⇒ the `bootWorker` config object (or the extracted builder's output) has NO `subscriptionArm`/`egressAllowedProcessors` keys.
   - Asserts: `"subscriptionArm" in cfg === false` + `"egressAllowedProcessors" in cfg === false`; `.toStrictEqual` the pre-change shape.
   - Why: byte-equivalent dormancy (Lessons 23/50; the conditional-spread idiom at `worker-host/index.ts:210-211`).
2. **`subscription_arm_env_populates_plain_data_only`** — `SOW_SUBSCRIPTION_ARM=1` + `SOW_SUBSCRIPTION_MODEL=claude-sonnet-5` ⇒ `subscriptionArm === {enabled:true, model:"claude-sonnet-5"}`, no function fields.
   - Asserts: enabled true; model set; NO `makeCompletion`/`checkReachable` keys (IPC-safe).
   - Why: §19.5 IPC data-only constraint.
3. **`subscription_arm_strict_parse`** — `SOW_SUBSCRIPTION_ARM` values `"true"`/`"1"` ⇒ enabled; `"false"`/`"TRUE"`/`"0"`/absent ⇒ omitted/false.
   - Asserts: only `"1"`/`"true"` arm (mirror `SOW_INGEST_WATCH` truthy-not-`true` guard, L28).
   - Why: arming opt-in must not truthy-coerce.
4. **`egress_allowlist_env_comma_split`** — `SOW_EGRESS_ALLOWED_PROCESSORS="claude-agent-sdk, foo"` ⇒ `egressAllowedProcessors === ["claude-agent-sdk","foo"]` (trimmed, empties dropped); unset ⇒ key omitted.
   - Asserts: split/trim; empty-string env ⇒ omitted (not `[""]`).
   - Why: §5 — the allowlist forward into 18.31's `gateAutoIngest`.
5. **`both_interfaces_stay_in_sync`** (lightweight) — a type-level or structural assertion that the two `WorkerHostConfig` field sets match (or a comment-anchored note if a type test is impractical).
   - Why: the mirrored interfaces must not drift (existing contract, `worker-supervisor.ts:11`).

## Cross-doc invariant impact
- **Model field changes:** none — `WorkerHostConfig` is a desktop-internal IPC config type, not an Appendix-A contract. `subscriptionArm` (`BootConfig`) + the egress-allowlist opt (18.31) already exist worker-side.
- **Orchestrator doc rows to write hot:** possible ARCHITECTURE §19.5 note (the desktop can now pass the subscription-arming opt-in + egress allowlist, dormant) — flag at Step 9; orchestrator writes it.
- **Shared-contract (Appendix-A) seam touched?** No.

## Things to flag at Step 2.5
1. **Env var names.** `SOW_SUBSCRIPTION_ARM` / `SOW_SUBSCRIPTION_MODEL` / `SOW_EGRESS_ALLOWED_PROCESSORS`. My default vote: **these names** (mirror the `SOW_*` convention). Naming is a genuine design choice — if the owner/lead prefers different names, say so at Step 2.5 (cheap to change now).
2. **Pass `subscriptionArm` given IPC can't carry `makeCompletion`/`checkReachable`?** My default vote: **yes — pass the plain-data `{enabled, model, betas}`**; `bootWorker` supplies the default real `makeCompletion`, and the real `checkReachable` probe injection + the actual flip stay worker-host-side owner-gated FUTURE steps. This slice makes the app capable + dormant; document the residual clearly (the in-app L64 dry-run — next step — still needs a worker-host-side fake-completion seam, since IPC can't carry the fake thunk).
3. **Keep the two `WorkerHostConfig` interfaces mirrored, or unify?** My default vote: **keep mirrored** + extend both (matches the existing pattern; unifying is an out-of-scope refactor). Add the sync-pin (test 5).
4. **Where does the config-builder test live** (no worker-host test dir exists)? My default vote: **extract the `WorkerHostConfig → bootWorker`-arg mapping into a small pure builder** and unit-test THAT (keeps the byte-equivalence pin without spawning a worker), rather than a full integration test. Flag if the reviewer prefers otherwise.

## Dependencies + sequencing
- **Depends on:** **18.31** (the `gateAutoIngest`/`buildAutoIngestProofSpineParams` `egressAllowedProcessors` param — the desktop forward won't typecheck until it exists). The `subscriptionArm` forward alone has no worker dependency, but bundle them (same interface edit) — so gate the whole slice on 18.31.
- **Blocks:** the in-app L64 fake-completion dry-run (the go/no-go before the owner's ENABLE) + the eventual owner in-app flip.

## Estimated commit count
**1 commit** — do NOT bundle with unrelated desktop work. Touches the arming/egress safety surface ⇒ security-reviewer MANDATORY.

## Lessons-logged candidates anticipated
- **Convention candidate** — an Electron-main → worker-host IPC config forwards ONLY plain-data arming opts (function deps stay worker-side); arming via env sets the opt-in but a fail-closed reachability probe keeps it dormant until a worker-host-side injection (a clean OFF-lock composition across the IPC boundary).
- **Architecture-doc note candidate** — the app is now capable of subscription-armed auto-ingest via config; the real probe + flip remain owner-gated (§19.5).
- **Future TODO** — the in-app L64 fake-completion dry-run harness (worker-host-side fake-completion seam, since IPC can't carry the fake thunk) — the go/no-go before the owner ENABLE.

## How to invoke
1. Read this brief end-to-end.
2. Confirm 18.31 has landed (the `gateAutoIngest` egress param exists) before Step 4.
3. Run `/tdd desktop_subscription_arming_forwarding`.
4. Step 2.5 — ping back with answers to the 4 design questions (esp. env naming) before GREEN.
5. Step 8 — **security-reviewer MANDATORY** (arming + egress surface).
6. Step 9 — surface cross-doc / lesson candidates.
