# /tdd brief — connector_adapter_coverage_and_binary (16.4 + 16.5 bundle)

## Bundle note
Two low-risk, same-track (providers-integrations), disjoint-file leaf-adapter slices bundled per the owner's same-pattern directive. Run `/tdd` RED→2.5→GREEN for **16.4** then **16.5**, then Step 6+ over the whole slice, **one commit**. Neither touches a WS-8 / safety-invariant surface. Both are pure-build.

---

## Feature A — 16.4: Drive `incompleteSearch` coverage-degrade (closes G29)
Honor Google Drive's `incompleteSearch?: boolean` response field as a **partial-corpora coverage-degrade signal** — mint a coverage-degrade `HealthItem` instead of silently dropping it (today it is IGNORED, an arch_gap). When Drive reports `incompleteSearch=true`, the search did not cover all corpora, so the ingested set is incomplete and must be surfaced (fail-VISIBLE, never a false-complete).

- **Architecture:** `ARCHITECTURE.md §19.3` / `§8` (Connector Gateway health + coverage). **Depends on:** 16.1 (logical), 14.3 (health surface).
- **File:** `packages/integrations/src/connectors/adapters/drive.ts:33-35` (consume `incompleteSearch`).
- **Acceptance:**
  - [ ] A fake Drive response with `incompleteSearch=true` surfaces a coverage-degrade signal (a `HealthItem` or the adapter's coverage-degrade channel — confirm the existing coverage-degrade convention at Step 1, mirror it; e.g. the Drive/connector health surface from 14.3), NOT silently dropped.
  - [ ] `incompleteSearch=false` / absent ⇒ NO degrade (byte-equivalent to today). Pin both.
  - [ ] The degrade signal is redaction-safe (no raw content/secret; rule 7).
- **RED tests:** `drive_incomplete_search_true_degrades_coverage`, `drive_complete_search_no_degrade`.

## Feature B — 16.5: File/PDF extractor binary parsing (closes G31)
Extend the real root-confined fs transport (text-only today — a NUL byte ⇒ binary ⇒ reject) with **PDF/doc binary parsing** so a binary source file extracts real text instead of rejecting.

- **Architecture:** `ARCHITECTURE.md §19.3` / `§8`. **Depends on:** 16.1 (logical).
- **File:** `packages/integrations/src/connectors/adapters/file-read-transport.ts:1-5,111-114` (add a binary/PDF parse path to the root-confined `node:fs` read). Also wired into the real vault watcher at `apps/worker/src/watch/vaultWatcher.ts:34-40` (do NOT modify that worker file — it's worker territory; the transport is consumed there unchanged).
- **Acceptance:**
  - [ ] A PDF/doc source file extracts REAL text (not a binary reject) through the root-confined transport.
  - [ ] A NUL-only / genuinely-unparseable binary file STILL rejects (typed, not a crash) — the binary parse is additive, not a blanket "accept all binary."
  - [ ] The read stays **root-confined** (the path guard is unchanged — a `../` escape still rejects; confirm the root-confinement guard is untouched, safety-adjacent).
  - [ ] Redaction-safe typed faults (§16, rule 7).
- **RED tests:** `pdf_file_extracts_real_text`, `nul_only_file_still_rejects`, `path_escape_still_rejects_root_confined`.
- **Note (use Context7):** if a PDF-parse library is needed, ground its API on Context7 (resolve→query-docs) before use; adding a `packages/integrations` dep is expected — declare it in the manifest + flag at Step 9. Prefer a small, well-maintained pure-parse lib; NO network, local file read only.

---

## Cross-doc invariant impact
- **Model:** none — 16.4 consumes the existing `HealthItem`; 16.5 no model change. **Shared-contract seam touched?** No.

## Things to flag at Step 2.5
1. **16.4 coverage-degrade convention:** confirm you're mirroring the EXISTING connector coverage-degrade/health channel (14.3), not inventing a new one.
2. **16.5 root-confinement preserved:** confirm the binary parse path does NOT weaken the root-confinement / path guard (a binary file outside the root still rejects) — this is the safety-adjacent point.
3. **16.5 dep add:** name the PDF-parse library + confirm it's pure/local (no network).

## Estimated commit count
**1** (bundle). **security-reviewer = invariant** for 16.5 (the root-confinement path guard is safety-adjacent — a parse path that widens the read surface is a traversal risk); 16.4 is code-quality only. code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Run `/session-start`, confirm scope with the lead.
2. Read this brief.
3. `/tdd connector_adapter_coverage_and_binary` — RED→2.5→GREEN for 16.4, then 16.5, then Step 6+ over the bundle.
4. Step 2.5 — ping the coverage-degrade-convention + root-confinement + dep-add answers to the lead. Don't GREEN until sign-off.
5. Step 9 — categorized flags; confirm G29 + G31 + the new dep. After this → 16.3 (the reusable read-only HTTP transport wrapper — the substantive integrations slice, SSRF/egress/ING-7 surface).
