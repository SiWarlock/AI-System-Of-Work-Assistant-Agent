# /tdd brief — numbered_block_provenance (task 13.7a — FROZEN-CONTRACT round: add an optional numbered-block back-reference `block?` to `CanonicalSourceRef` + a pure segregated-inference `block-provenance` domain validator; ADDITIVE + DORMANT, owner-authorized)

## Feature
Add osb's numbered `(src: Bn)` block-provenance to the provenance model, as a **FROZEN-CONTRACT round** (owner-queued slice (b)). Two coupled parts, one coherent feature:
1. **Contract (frozen, additive):** add an OPTIONAL `block?: string` (the enumerated source-block id, e.g. `"B3"`) to `CanonicalSourceRef` (`shared-shapes.ts`) — a numbered-block back-reference, orthogonal to the existing `span?` (line/char range). Regenerate the embedding model's JSON Schema (`gbrain-proposed-fact.schema.json`). Backward-compatible (all existing refs still parse; `block` is optional).
2. **Domain validator (pure, standalone):** `packages/domain/src/validation/block-provenance.ts` — segregated-inference: `hasBlockProvenance(ref)` (true iff the ref carries a non-empty numbered `block`) + `distillBlockProvenance(refs)` partitioning block-traceable refs from block-less ones (the "keep the numbered-block-traceable claim, flag/segregate the inferred one" primitive). Mirrors `no-inference.ts`; pure, total, typed `Result`, never throws.

**DORMANT + ADDITIVE — no regression:** NO producer emits `block` today (grep-confirmed across extractors / canonical-fact-deriver / generative path), so the field ships optional-and-empty and the validator is **standalone + fixture-tested — NOT wired as a required gate** into the live generative path. (Wiring a block-REQUIRED gate now would drop EVERY existing ref — none carry `block` — breaking `intakeGenerativeProposal`. So composition-into-intake is a NAMED FOLLOW-UP that lands WITH producer emission.) The drop-a-claim-with-no-traceable-source behavior ALREADY ships (`validateNoInference` / `intakeGenerativeProposal`); this slice adds the NUMBERED-BLOCK granularity + its validator, not a from-scratch drop gate.

> **FROZEN-CONTRACT ROUND — atomic doc-pairing (orchestrator territory).** `CanonicalSourceRef` is a shared sub-shape of the frozen Appendix-A model `GBrainProposedFact`. The implementer edits the Zod + the regenerated JSON Schema + tests + the validator; the ORCHESTRATOR writes the `ARCHITECTURE.md` Appendix A (L450 GBrainProposedFact row) + `packages/contracts/CLAUDE.md` cross-doc row hot (accumulating for the round commit). Implementer does NOT touch those two doc files.

## Use case + traceability
- **Task ID:** 13.7a (the block-provenance half; the sentinel-marker half 13.7b landed `1180136`). `IMPLEMENTATION_PLAN.md` 13.7: "Block-provenance distillation `(src: Bn)` + segregated-inference strengthen the candidate gate / no-inference validator." Owner-authorized as a frozen-contract round + named producer coordination.
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KnowledgeWriter candidate gate / no-inference — REQ-F-017; the `GBrainProposedFact.evidenceRefs` provenance the `block` extends) + `§7`. Safety posture: candidate-data-in, no-inference (a claim's source must be traceable). Confirm §6/§7 at Step 0. (The test file already tags `spec(§6/§7)`.)
- **The exact surfaces (from recon — read at Step 0):**
  - `packages/contracts/src/models/shared-shapes.ts` **L85-91** — `CanonicalSourceRefSchema = z.object({ kind: z.enum(["markdown","source_envelope"]), ref: z.string().min(1), span: z.string().min(1).optional() }).strict()`; type at L92. Add `block: z.string().min(1).optional()` here.
  - `packages/contracts/schemas/gbrain-proposed-fact.schema.json` **L26-53** — the inlined `evidenceRefs.items` (the `CanonicalSourceRef` JSON Schema); MUST regenerate to gain `block` (else `freezeGenerated` fails; `.strict()` ⇒ `additionalProperties:false`).
  - `packages/contracts/test/models/gbrain-proposed-fact.test.ts` — the freeze tests: `fieldSet` snapshot (TOP-LEVEL only — a nested `block` does NOT change the `.snap`, so the `.snap` file is UNCHANGED) + `freezeGenerated(url, emitJsonSchema(...))` (**this leg catches the nested `block` add** — regenerate the checked-in `.schema.json`).
  - `packages/contracts/test/models/shared-shapes.test.ts` **L96-110** — the `CanonicalSourceRef` parse tests (incl. L108 "rejects extra key" — stays green because `block` becomes a KNOWN key).
  - `packages/domain/src/validation/no-inference.ts` — the validator MIRROR (`checkExtractionField`/`validateNoInference`, `Result`, codes, TBD sentinel; pure/total).
  - `packages/knowledge/src/gbrain/remediation/generative-proposal-intake.ts` **L148-151** — the EXISTING drop gate (`isEvidenceAdmissible` injected dep); the new validator composes ALONGSIDE it as a FOLLOW-UP (do NOT wire now).

## Scope boundary (IN vs deferred)
- **IN (this slice):**
  - `shared-shapes.ts`: add `block: z.string().min(1).optional()` to `CanonicalSourceRefSchema` (+ update the L83-84 doc-comment to name the numbered-block back-ref).
  - `gbrain-proposed-fact.schema.json`: regenerate (evidenceRefs.items gains optional `block`).
  - NEW `packages/domain/src/validation/block-provenance.ts`: `hasBlockProvenance(ref): boolean` + `distillBlockProvenance(refs): { blockTraceable: CanonicalSourceRef[]; withoutBlock: CanonicalSourceRef[] }` (segregated-inference partition) + optionally `validateBlockProvenance(refs): Result<CanonicalSourceRef[], BlockProvenanceRejection[]>` (a REQUIRE-block aggregate for the future gate) with a code like `missing_block_provenance`. Pure, total, never throws.
  - Tests: `shared-shapes.test.ts` (parse a ref WITH `block`; a ref without `block` still valid), `gbrain-proposed-fact.test.ts` (accepts an evidence ref carrying `block`; freezeGenerated regenerates), NEW `packages/domain/test/validation/block-provenance.test.ts`.
- **ORCHESTRATOR writes hot (my round commit — do NOT touch):** `ARCHITECTURE.md` Appendix A L450 (GBrainProposedFact row: note the optional `block` numbered back-ref) + `packages/contracts/CLAUDE.md` cross-doc GBrainProposedFact row.
- **DEFERRED (named follow-ups — do NOT build):** (1) **producer coordination** — wire numbered-block emission into the 13.2 extractors + the generative-proposal generator so `evidenceRefs` carry `(src: Bn)` (the field is dormant until then). (2) **compose the validator as a REQUIRED gate** into `intakeGenerativeProposal` (alongside `isEvidenceAdmissible`) — lands WITH (1), never before (a required-block gate today drops every ref). (3) propagating `block` into the intake `sourceRefs` map (L174).

## Acceptance criteria (what "done" means — additive + dormant, zero regression)
- [ ] **Additive optional field:** `CanonicalSourceRefSchema` accepts a ref WITH `block` (`{kind,ref,span?,block?}`) AND a ref WITHOUT `block` (backward-compatible); `block` (when present) is a non-empty string; `.strict()` still rejects an unknown key.
- [ ] **Frozen-contract regen:** `gbrain-proposed-fact.schema.json` regenerated so `freezeGenerated` passes; the top-level `fieldSet` snapshot (`gbrain-proposed-fact.snap`) is UNCHANGED (evidenceRefs stays a top-level key); every existing contract test passes byte-for-byte.
- [ ] **`hasBlockProvenance(ref)`:** true iff the ref carries a non-empty `block` (a valid `kind`/`ref`); false otherwise. Pure.
- [ ] **`distillBlockProvenance(refs)`:** partitions into `blockTraceable` (have `block`) vs `withoutBlock` (segregated-inference) — order-preserving, no mutation.
- [ ] **`validateBlockProvenance(refs)` (require-block aggregate):** returns typed rejections (`missing_block_provenance`, one per block-less ref) — the future required-gate primitive; pure, never throws.
- [ ] **DORMANT / no regression (load-bearing):** the validator is STANDALONE + fixture-tested; it is NOT wired into `intakeGenerativeProposal` or any live path as a required gate (a block-required gate today would drop all refs). `generative-proposal-intake` behavior is UNCHANGED (regression pin: its tests still green).
- [ ] `packages/contracts` + `packages/domain` `turbo typecheck test` green; repo-wide `pnpm -w turbo run typecheck test` green (cross-package: `CanonicalSourceRef` is consumed by contracts + knowledge — repo-wide is mandatory).

## RED test outline
1. `canonical_source_ref_accepts_block` (`shared-shapes.test.ts`) — `{kind,ref,span,block:"B3"}` parses; `block` present. Why: additive field.
2. `canonical_source_ref_without_block_still_valid` — `{kind,ref}` parses (backward-compat); a `block:""` (empty) rejected (`.min(1)`). Why: optional + non-empty.
3. `gbrain_fact_accepts_evidence_with_block` + `freezeGenerated` (`gbrain-proposed-fact.test.ts`) — an evidence ref with `block` parses; the regenerated JSON schema freezes. Why: frozen-contract regen.
4. `has_block_provenance` (`block-provenance.test.ts`) — true for a ref with `block`, false for one without / empty. Why: the predicate.
5. `distill_partitions_by_block` — mixed refs ⇒ `blockTraceable` / `withoutBlock` partition, order-preserved, inputs unmutated. Why: segregated-inference.
6. `validate_requires_block_typed_rejections` — block-less refs ⇒ `missing_block_provenance` rejections (one each); all-block refs ⇒ ok. Never throws. Why: the future required-gate primitive.
7. `intake_unchanged_REGRESSION` (assert, not re-implement) — the validator is not imported by `generative-proposal-intake.ts`; its existing tests stay green (dormant / no required-gate wiring). Why: NO regression.

## Cross-doc invariant impact
- **YES — a frozen-contract additive field.** Model field change: `CanonicalSourceRef` gains optional `block?` (shared sub-shape of `GBrainProposedFact`). **Orchestrator doc rows to write hot (Step-9 routing):** `ARCHITECTURE.md` Appendix A L450 (GBrainProposedFact evidenceRefs row — note the optional `block` numbered back-ref) + `packages/contracts/CLAUDE.md` cross-doc GBrainProposedFact row. **Shared-contract seam:** `GBrainProposedFact` is a frozen Appendix-A model → the RED outline INCLUDES the schema-freeze test (test 3, `freezeGenerated`) — the implementer authors/regens it this cycle. The `.snap` field-set snapshot is UNCHANGED (nested add), so no `.snap` edit. The implementer does NOT touch ARCHITECTURE.md / contracts-CLAUDE.md (orchestrator territory).

## Things to flag at Step 2.5 (design questions — default votes)
1. **Design (A) additive `block?` on `CanonicalSourceRef` — DECIDED (owner pre-delegated the design), confirm no blocker.** Rationale: minimal, backward-compatible, mirrors the existing optional `span?` string idiom (span = line/char range; `block` = enumerated back-ref), 3-consumer ripple (shared-shapes defn / gbrain-proposed-fact embed / intake). Alternative (B) a new `BlockProvenance` model = a full new frozen Appendix-A model (heavier; fragments one ref's provenance across two shapes) — only if `block` needs required sub-structure. Confirm (A) holds against the code; flag if you see a blocker.
2. **Validator shape + semantics.** Default: `hasBlockProvenance` (predicate) + `distillBlockProvenance` (partition, segregated-inference) + `validateBlockProvenance` (require-block aggregate, typed rejections). STAND-ALONE (pure over `CanonicalSourceRef`), does NOT fold into `validateNoInference`. Confirm the trio + the `missing_block_provenance` code.
3. **DORMANT — do NOT wire as a required gate (SAFETY).** Default: ship the validator standalone + fixture-tested; do NOT compose it into `intakeGenerativeProposal` (a block-required gate today drops every ref — no producer emits `block`). Confirm you'll leave the live path unchanged (regression pin, test 7).
4. **Schema regeneration mechanics.** Default: regenerate `gbrain-proposed-fact.schema.json` via the same emit path `freezeGenerated` uses (so the checked-in schema == `emitJsonSchema` output). Confirm the `.snap` field-set is genuinely unchanged (top-level only) — no `.snap` edit needed.
5. **`block` id format.** Default: an opaque non-empty string (`z.string().min(1)`) — do NOT over-constrain to a `^B\d+$` regex yet (the producer defines the exact id scheme; over-constraining now risks a re-freeze). Confirm opaque-string.

## Wiring / entry point (Step 7.5)
- **none — wiring lands with producer coordination.** The `block` field + the validator are the DORMANT contract + primitive; they are reachable/self-verified by the contract tests + the validator's own suite. The production wiring (producers emitting `block`; the validator composed as a required gate in `intakeGenerativeProposal`) is the NAMED follow-up — state this at Step 7.5. Reachability = judgment-satisfied by the test suites exercising the real Zod + validator.
- **Depends on:** `CanonicalSourceRef`/`GBrainProposedFact` (present). **Blocks:** the producer-coordination follow-up + the required-gate composition.

## Estimated commit count
**1** (implementer): the `block?` field + regenerated schema + the `block-provenance.ts` validator + tests — one coherent frozen-contract feature. **+ the orchestrator's Appendix-A/cross-doc rows ride the round commit** (staggered, orchestrator territory). This touches a FROZEN contract + the candidate-gate/no-inference posture ⇒ **Step-8 adversarial review MANDATORY** (security + code-quality): additive-only (backward-compat; existing contract + intake tests byte-for-byte green), the field is genuinely dormant (NOT wired as a required gate — no regression), the validator is pure/total/never-throws, the schema regen is faithful (freezeGenerated passes; `.snap` unchanged).

## Lessons-logged candidates (implementer flags Step 9)
- Possible (implementer's call): "a frozen-contract additive field on a SHARED SUB-SHAPE (`CanonicalSourceRef`) ripples only through its embedder's generated JSON Schema (`freezeGenerated` catches the nested add; the top-level `fieldSet` `.snap` is unchanged); a dormant provenance field + its validator ship STANDALONE (never wired as a required gate until producers emit the field, else the gate drops all existing data)."
- **Architecture-doc note candidate:** `CanonicalSourceRef` now carries an optional numbered-block back-ref (`block`) alongside `span` — the block-provenance granularity for future segregated-inference.

## How to invoke (implementer)
1. `/tdd` against this brief. Step 0 — read `shared-shapes.ts` (L83-92), `gbrain-proposed-fact.schema.json` (L26-53), `gbrain-proposed-fact.test.ts` (the `fieldSet` + `freezeGenerated` legs) + `_helpers/freeze`, `no-inference.ts` (the validator mirror), `generative-proposal-intake.ts` (L148-151 — the gate you must NOT wire into). Confirm §6/§7 + the `emitJsonSchema` regen path.
2. Step 2.5 — Q1 (confirm design A holds) + Q3 (DORMANT — no required-gate wiring; the safety point) are load-bearing; ping me your write-up + the exact validator signatures BEFORE cases. If (A) has a real blocker, flag it (I own the design call).
3. RED first — additive-field parse + backward-compat + freezeGenerated + the validator trio + the intake-unchanged regression are load-bearing.
4. **Step 8 — MANDATORY adversarial review** (general-purpose Agent): additive-only (backward-compat; existing contract + intake tests byte-for-byte green); field genuinely dormant (NOT a required gate — zero regression); validator pure/total/never-throws; schema regen faithful (freezeGenerated green, `.snap` unchanged); `.strict()` still rejects unknown keys.
5. Step 9 — categorized flags (the frozen-contract doc rows for me to write hot: Appendix A L450 + cross-doc; the producer-coordination + required-gate follow-ups) + ship-ask (commit-message-first).
