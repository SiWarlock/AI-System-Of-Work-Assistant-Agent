# /tdd brief — guard_cross_workspace_raw_read_exhaustive_denial_reason

## Feature
`guardCrossWorkspaceRawRead` (`packages/knowledge/src/gcl/visibility-gate.ts`, ~line 208) narrows `DenialReason` via ONE explicit `===` check against `DIRECT_CROSS_WORKSPACE_RAW_RETRIEVAL` followed by an un-guarded trailing return to `malformed_policy_input` — the fourth instance of `L134`'s shape, one function below the one `24.36` just fixed in this same file. Make it exhaustive so a future `DenialReason` member is a compile error here, not a silent mislabel.

## Use case + traceability
- **Task ID:** `24.38`
- **Architecture sections it implements:** §6 — Safety rule 4

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 safety-assertion audit and its own Step-9 findings, which sweep every area, so a remediation task legitimately cites the anchors its originating finding lives under rather than only Phase 24's own declared set. Same widened-scope shape as briefs 254/255/257/258.

- **Related context:** `IMPLEMENTATION_PLAN.md` task `24.38` (full text, incl. the producer trace that establishes why this is not urgent — read it, don't re-derive it). `packages/contracts/LESSONS.md#134`. ⭐ **`docs/briefs/258-24.36-admitprojection-exhaustive-denialreason.md` + the landed `24.36` commit are the direct precedent — you wrote it; mirror your own `denialToGateError` pattern rather than inventing a second idiom in the same file.**

## Acceptance criteria (what "done" means)
- [ ] `guardCrossWorkspaceRawRead`'s narrowing over `decision.reason` is exhaustive across all 15 `DenialReason` members (4 `HardDenial` + 11 `SupportDenial` — the count `24.36` established; re-confirm against `packages/policy/src/denials.ts` rather than trusting this brief).
- [ ] The narrowing terminates with an `assertNever`-style exhaustiveness guard, mirroring `24.36`'s landed pattern in this same file — **not** an un-guarded trailing return.
- [ ] The two reasons `denyDirectCrossWorkspaceRaw` can genuinely emit today each produce byte-identical output to before this fix — regression pin. `DIRECT_CROSS_WORKSPACE_RAW_RETRIEVAL` is the live rule-4/WS-8 hard-denial enforcement branch; it must not change behavior.
- [ ] The 13 members that cannot be emitted here resolve to `malformed_policy_input` (fail-closed, matching `FAIL_CLOSED_DENIAL`'s designation) via an explicit grouped case-list, not a fallthrough.
- [ ] Exhaustiveness verified **empirically, not asserted** — remove one case, confirm `tsc` fails at the guard line with the `never` assignability error, restore, reconfirm clean. (`24.36`'s methodology; state the removed case in the Step-9 report.)
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
None new — `guardCrossWorkspaceRawRead` is an existing export; this slice changes its internal narrowing, not its signature or callers. ⚠ **State its reachability precisely in the Step-9 report** (per your own 24.33 finding — do not restate anything in this file as "live" without a caller trace).

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gcl/visibility-gate.ts` — `guardCrossWorkspaceRawRead`'s narrowing
- `packages/knowledge/test/gcl-visibility-gate.test.ts` — the suite `24.36` just extended

## RED test outline (Step 2)
1. **`guard_cross_workspace_raw_read_direct_retrieval_unchanged`** — Asserts: a `DIRECT_CROSS_WORKSPACE_RAW_RETRIEVAL` denial still produces `{code: "direct_cross_workspace_raw_denied", ...}`, byte-identical. Why: regression pin on the live rule-4 enforcement branch — the one branch in this function that carries a safety invariant.
2. **`guard_cross_workspace_raw_read_malformed_input_unchanged`** — Asserts: the other genuinely-emittable reason still produces `{code: "malformed_policy_input", ...}`. Why: regression pin on the second real case.
3. **`guard_cross_workspace_raw_read_non_emittable_reasons_fail_closed`** — Asserts: the remaining `DenialReason` members each resolve to `malformed_policy_input`. Why: pins the fail-closed grouping. ⚠ **Same honesty note you made on `24.36` applies — a grouped case-list and a trailing fallthrough are output-identical by construction, so this is a genuine regression pin, not proof of the structural property. Say so; don't let it read as more than it is.**
4. **`guard_cross_workspace_raw_read_narrowing_is_exhaustive`** — the empirical removal-test per acceptance bullet 5, documented the way `24.36`'s was.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — `DenialReason`/`HardDenial`/`SupportDenial` unchanged; this fixes a consumer's handling of them (same as `24.30` and `24.36`).
- **Orchestrator doc rows to write hot:** none anticipated.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **Extract a named function (mirroring `denialToGateError`), or fix in place?** My default vote: **whichever `24.36`'s own extraction made natural.** The extraction there was justified by needing to exercise members `validateProjectionVisibility` can't emit; the same argument applies here if the 13 non-emittable members are otherwise untestable end-to-end. But the return type differs (`CrossWorkspaceRawDenial`, not `GclGateError`) so this cannot reuse `denialToGateError` itself — if a second extracted function would just be ceremony around a 2-case switch, fix in place and say why. **You have the file loaded; your call, state the reasoning.**
2. **Is this the LAST instance in this file, or is there a fifth?** ⭐ **Before Step 9, sweep `visibility-gate.ts` for any remaining un-guarded closed-union narrowing** — the 24.23→24.30→24.36→24.38 chain has surfaced one more instance at every single review, so the base rate of "one more exists" is high. My default vote: **sweep it explicitly and report the result either way** — a stated "swept, none remain" is worth a great deal more than silence here, and if a fifth exists I'd rather file it now than have a fifth review find it.

## Dependencies + sequencing
- **Depends on:** `24.36` (the pattern this mirrors; land that first so this diff is against the fixed file).
- **Blocks:** none tracked. Feeds `24.34`'s concept-level sweep with a fourth data point.

## Estimated commit count
**1.** Focused exhaustiveness fix, one function, same single-commit shape as `24.30` and `24.36`.

## Lessons-logged candidates anticipated
- **Convention candidate** — possibly, and it would be about the CHAIN rather than the shape: four instances, each found by the previous fix's own review sweep, none by a gate. If `24.36`'s reviewers hadn't swept a sibling, this would still be open. Worth asking at Step 9 whether "when you fix a closed-union narrowing, sweep every sibling in the same file before Step 9" deserves banking as a rule — the evidence for it is now four-for-four.

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md` task `24.38` + your own `24.36` diff (the pattern to mirror).
2. Run `/tdd guard_cross_workspace_raw_read_exhaustive_denial_reason`.
3. Step 2.5 — question 1 is genuinely yours to call; question 2's sweep is not optional, report it either way.
