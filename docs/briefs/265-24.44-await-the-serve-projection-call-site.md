# /tdd brief — await_the_serve_projection_call_site (24.44 leg 1 of 2 — CONSUMER FIRST)

## Feature
Add one `await` to the `serveProjection(...)` call at `apps/worker/src/composition/crossWorkspaceRead.ts:139`, **while `serveProjection` is still synchronous** — a preparatory commit with **no observable behavior delta** that lets knowledge's leg 2 flip the producer to `async` with **no red window at any commit**.

> ⭐ **CORRECTED 2026-08-12 by worker-implementer, before starting — the original text of this brief claimed "zero behavior delta," and that was overstated.** `await` on a non-thenable **still queues a microtask**, so this is **one microtask tick per row** inside the per-link loop. ⚠ **It is unobservable *here*, and the reason is what makes the claim safe rather than merely convenient:** every binding the loop mutates (`out`, `visibilityExceededCount`) is **function-local**, single-threaded, with no concurrent writer — and `resolveApprovedCrossWorkspaceSlice` is **already `async` with 3 existing awaits**, so it already yields; this adds yield points, not a new interleaving class. ⇒ **Say "no observable delta; one microtask tick per row, all mutated state function-local" — never "zero."** (`L100`: state the scope with the claim. An unqualified "zero" is the form that gets quoted, and it is false.)

## ⛔ READ THIS FIRST — why this tiny slice exists, and why it goes FIRST

`24.44` is a genuine producer/consumer PAIR across two areas. The lead's ruling is explicit and is the Done-when, not a preference:

> **Both commits land in the SAME round with NO RED WINDOW between them.**

The obvious order — producer first — **cannot satisfy that.** The moment `serveProjection` becomes `async`, this call site's `isErr(admitted)` / `admitted.value` stop compiling, so the intermediate commit is RED.

⭐ **The reverse order is green at every commit, and this was verified empirically rather than assumed** — with the repo's own compiler (`node_modules/.bin/tsc`, TypeScript **5.9.3**), `--strict`, on a snippet replicating this exact shape (sync producer returning a `Result` union + `isErr` narrowing + `.value` read, with `await` applied):

```
TSC_EXIT=0
```

`await` on a non-thenable is legal TypeScript and evaluates to the value itself (`Awaited<T>` = `T` for non-promise `T`). ⚠ **Also checked, because it is the one thing that could have broken this:** the repo has **no ESLint configuration at all** — `.eslintrc*` and `eslint.config.*` both return no matches — so no `@typescript-eslint/await-thenable` rule exists to flag an await on a sync call. (Consistent with the tracker's own standing finding that `pnpm lint` IS typecheck, and that ESLint appears in zero manifests.)

⇒ **This commit lands first and is green. Knowledge's leg 2 lands second and is green. Neither intermediate state is red.**

⚠ **Single shared working tree — this is not a worktree build.** Knowledge is developing leg 2 concurrently in the same tree. **Your commit needs to land promptly**, because knowledge cannot flip `serveProjection` to `async` until it does.

## ⚠ TDD posture — stated plainly rather than faked

**This slice has no observable behavior delta, so there is no failing test to write first, and you should not manufacture one.** `await` on a synchronous value changes nothing observable **at this call site**: same `Result`, same narrowing, same per-row loop, same returns — modulo the per-row microtask tick noted above, which no assertion here can or should pin. A test asserting "the call is awaited" today could only be a source-assertion.

⛔ **Do NOT add a source-assertion pin for the `await`.** It would be redundant **by construction** the moment leg 2 lands: once `serveProjection` returns a `Promise`, omitting the `await` is a compile error, and the type is a far better pin than a grep. Adding one now creates a test that exists only to be deleted, and (per contracts `L145`'s family) a pin nobody re-measures.

**Verification for this slice is therefore:**
1. `tsc --noEmit` green across the repo (the change is type-level).
2. The **existing** `crossWorkspaceRead` suite stays green, unchanged — no test edits.
3. You state in the Step-9 report **and the commit message** that there is **no observable delta — one microtask tick per row, all mutated state function-local** — and why. ⛔ **Not "zero."**

This is a deliberate, disclosed TDD exemption for an ordering device — **not** a slice where the tests were skipped. Say so explicitly at Step 9; do not let it read as a normal slice.

## Use case + traceability
- **Task ID:** `24.44` (leg 1 of 2)
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- ⚠ **This brief widens phase scope because** Phase 24 is a cross-cutting *remediation* phase whose declared anchors (`§19.11 §6 §13 §4 §3`) describe its own deliverables, while its findings land wherever the audited defect actually lives — here the GCL visibility/policy surface (`§5`) and the error convention (`§16`). ⛔ **The correct resolution is this declaration, NOT adding `§5`/`§16` to Phase 24's declared anchor set** — an anchor set that grew because a linter demanded it is worthless (the lead's 2026-07-29 ruling; Carry-forward 6(a0)(iii)). `### 24.44`'s own `Spec:` line already cites `§5, §16`, so the brief matches the tracker.
- **Safety:** rule 4 (workspace isolation / WS-8) — the surrounding gate is rule-4 machinery; **this slice changes none of it**
- **Related context:** `### 24.44` in `IMPLEMENTATION_PLAN.md` · `### 24.33` (`5fc64421`-era mechanism knowledge built: `GclAuditPersistPort` + `persistDenialAudit`) · the lead's `L121` ruling recorded below

## ⭐ Provenance you should know you are inside of
`24.44` is a **first-of-its-kind extension of `L121`** — that lesson covers a widening's own compile-break in another area's **test fixture**; this is **production** code. The previous orchestrator **correctly refused to authorize it** and routed it to the lead. **The lead has now ruled it authorized.** You are executing a lead ruling with that provenance, not an orchestrator's unilateral call.

## Acceptance criteria (what "done" means)
- [ ] `crossWorkspaceRead.ts:139` reads `const admitted = await serveProjection(projection, sourceWorkspace.value);`
- [ ] **Nothing else in the file changes** — no refactor of the loop, the comment block above the call, the `isErr` branch, or the error mapping (surgical-changes rule)
- [ ] `resolveApprovedCrossWorkspaceSlice` is **already `async`** (`crossWorkspaceRead.ts:81`, already carries 3 `await`s) — confirm this by reading it; **no signature change, no cascade to any caller**
- [ ] Repo-wide `tsc --noEmit` green
- [ ] The existing `crossWorkspaceRead` test suite passes **with zero test-file edits**
- [ ] Step-9 report + commit message state **no observable delta (one microtask tick per row, all mutated state function-local)** — ⛔ never the unqualified "zero" — and name leg 2 as the commit that makes the `await` structurally required

## Wiring / entry point (Step 7.5)
**none — the path is dormant, and that is measured, not assumed.** `resolveApprovedCrossWorkspaceSlice` has **zero production callers**: every call site is in its own test file, and `boot.ts:1765` / `buildGclProjection.ts:13` / `scopedRetrieval.ts:15` are **comments**, not calls. This was established by knowledge-implementer from source and independently confirmed by the lead when it falsified the earlier "LIVE" claim that briefly attached `24.33` to the arming-block release path.

⛔ **Do not re-assert liveness here, and do not weaken the claim to "probably dormant."** If your own reading disagrees with the above, that is a **Finding** — surface it at Step 2.5, because this exact reachability claim has been falsified before (contracts `L141`).

**Consumer slice that makes this reachable:** Phase **25.2/25.4** port wiring (a real `GclAuditPersistPort` binding), plus `### 24.45` which **blocks** that wiring. Both are real tracked tasks — `L106`'s obligation is satisfied.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/crossWorkspaceRead.ts` — **one word**, line 139

**New:** none.

If you find yourself touching a second file, **stop and flag at Step 2.5** — that means the "no cascade" premise is wrong and the pair needs resequencing.

## Things to flag at Step 2.5
1. **Does the `await` genuinely typecheck against the still-sync producer in THIS repo's config?** I verified it with the repo's own `tsc` on an isolated replica (exit 0) — but I did not run it against the real file. My default vote: **proceed, and let repo-wide `tsc --noEmit` be the real proof.** If it fails, that is a genuine premise falsification — report it rather than working around it, because the whole no-red-window ordering rests on it.
2. **Is a source-assertion pin warranted after all?** My default vote: **no**, for the redundant-by-construction reason above. Disagree if you think leg 2 could land in a form that does not make the `await` mandatory.
3. **Should this instead be folded into leg 2 as one cross-area commit?** My default vote: **no.** The standing rule is no cross-track single-impl verticals, and two green commits satisfy the lead's constraint without anyone writing outside their territory.

## Cross-doc invariant impact
- **Model field changes:** none
- **Orchestrator doc rows to write hot:** none
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Dependencies + sequencing
- **Depends on:** nothing. ⭐ **This is the unblocker** — it must land **before** knowledge's leg 2.
- **Blocks:** `24.44` leg 2 (knowledge: `serveProjection` async + audit-port threading). Knowledge is working it concurrently in the same tree and is waiting on your commit.

## Estimated commit count
**1.** One word. Its own commit deliberately — it is one half of a cross-area pair and must be independently bisectable.

## Lessons-logged candidates anticipated
- **Convention candidate** — *when a sync→async producer widening must cross an area boundary with no red window, land the CONSUMER's `await` first: `await` on a non-thenable is a legal no-op, so consumer-first is green at every commit while producer-first cannot be.* This is the generalizable half and is worth banking regardless of how the rest of the pair goes.
- **Architecture-doc note candidate** — none expected.

## How to invoke
1. Read this brief end-to-end.
2. Run `/tdd await_the_serve_projection_call_site`.
3. At **Step 2.5**, send the write-up — it will be short, and it should state the TDD exemption explicitly rather than listing tests you did not write.
4. At **Step 9**, categorize as usual; **flag the zero-behavior-delta framing** so it is recorded honestly.
