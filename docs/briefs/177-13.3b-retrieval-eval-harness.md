# /tdd brief — retrieval_eval_harness_recall_bar

## Feature
The retrieval eval harness (packages/evals, evalsec territory) — the §13.1c recall@10 bar that validates the landed local-embed retrieval (13.3a) + re-ranker (13.17) against a corpus, gating at the recorded hybrid recall@10 ≥ ~0.91 with a no-ranking-regression check. The paired leg of 13.3 deferred off the knowledge slice (packages/evals is evalsec territory).

> **Scope note (13.3 split):** 13.3a (local-embed primitive, `1129ad98`) + 13.17 (re-ranker) landed in knowledge; this is **13.3b — the eval harness** in evalsec territory. On this landing, 13.3 → `[x]` (both legs done).

## Use case + traceability
- **Task ID:** 13.3 (leg b — the retrieval eval harness / §13.1c pin re-validation)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (retrieval — the recall bar validating the local-zero-egress retrieval path).
  > Widens into the §12 eval-harness surface — it's the eval leg that measures the §6 retrieval; declare it if spec-lint flags the anchor.
- **Related context:**
  - Imports knowledge's exported retrieval surface: the 13.3a local-embed retrieval (RRF fusion, `1129ad98`) + the 13.17 re-ranker (`rerank`, landing) + the extracted `lexicalRelevanceRank`.
  - Ports osb's `retrieval_eval.py` as the §13.1c pin re-validation (recorded bar: hybrid recall@10 ≥ ~0.91).

## Acceptance criteria (what "done" means)
- [ ] The harness loads a retrieval corpus (queries + relevance labels) and computes **recall@10** over it using 13.3a's local-embed retrieval (RRF fusion) + 13.17's re-ranker.
- [ ] Gates at the recorded bar: **hybrid recall@10 ≥ ~0.91** (§13.1c) — a run below the bar FAILS the suite.
- [ ] **No ranking regression:** the re-ranked order (13.17) is ≥ raw-embedding-order on the bar (the re-ranker helps or at least does not hurt) — the harness measures raw-embedding vs fused (13.3a) vs re-ranked (13.17).
- [ ] **Zero-egress:** the eval uses a LOCAL backend only (no cloud embed) — a check/assert confirms no cloud endpoint is invoked and no employer-content leaks through the corpus.
- [ ] Deterministic / CI-able (fixture mode); an optional live-backend mode gated behind an env flag (see Step-2.5 Q1).
- [ ] The suite runs green; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — the eval harness is the entry point** (a vitest suite under `packages/evals/`, run by the evals runner). It imports knowledge's exported retrieval + re-ranker; it edits no knowledge/worker file. State explicitly: no production wiring. (spec-lint requires this section.)

## Files expected to touch
**New:**
- `packages/evals/src/retrieval/` — the harness (corpus loader + recall@10 scorer + the raw/fused/re-ranked comparison).
- `packages/evals/suites/retrieval/…test.ts` (or `test/…`) — the suite gating at the bar.
- a retrieval corpus fixture (queries + relevance labels; recorded embeddings if fixture mode) — confirm the corpus source at Step 2.5.

⚠ Evalsec territory — create in `packages/evals`; IMPORT knowledge's exported retrieval/re-ranker (never edit `packages/knowledge`). `git add <path>` only.

## RED test outline (Step 2)
1. **`recall_at_10_meets_bar`** — the harness computes recall@10 over the corpus ≥ ~0.91. Why: §13.1c / §6 recorded bar.
2. **`reranker_no_regression`** — re-ranked recall@10 ≥ raw-embedding-order recall@10 (13.17 helps or ≥). Why: §6 re-ranking value.
3. **`raw_vs_fused_vs_reranked_measured`** — the harness reports all three orders' recall (the comparison surface). Why: §6 (measured, not asserted-by-fiat).
4. **`eval_is_zero_egress_local_only`** — no cloud endpoint invoked; local backend only. Why: §5 rule-5 posture (the retrieval path stays zero-egress under eval).
5. **`below_bar_fails_suite`** — a degraded ranking (fixture) drops recall below the bar ⇒ the suite FAILS (the gate bites, non-vacuous). Why: the bar is a real gate.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (eval harness; imports existing surface).
- **Orchestrator doc rows to write hot:** the EVALUATION_CRITERIA acceptance-matrix row for the recall@10 bar (flag at Step 9 if the §12 matrix needs the entry) + the recorded measured value. Otherwise none.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Embedding source.** Default vote: a **recorded-embedding fixture corpus** (deterministic, CI-able — the recall bar validated against recorded embeddings), with an OPTIONAL live-Ollama mode behind an env flag (like the SOW_* live-integration gates). Confirm — a live-only harness needs Ollama infra + isn't CI-deterministic.
2. **Bar as a hard gate.** Default vote: gate at **recall@10 ≥ ~0.91** (the §13.1c recorded bar) as a hard fail + the re-ranker no-regression. Confirm the exact bar value against the recorded osb `retrieval_eval` figure.
3. **Scope.** Default vote: validate **both** 13.3a fusion AND 13.17 re-ranking (measure raw vs fused vs re-ranked) — the harness is the quality gate for the whole retrieval path. Confirm.
4. **Corpus source.** Default vote: port osb's `retrieval_eval` corpus (or reuse an existing eval corpus if one carries retrieval labels). Confirm the corpus location.

## Dependencies + sequencing
- **Depends on:** 13.3a (local-embed, `1129ad98`) + 13.17 (re-ranker, landing) — both knowledge exports.
- **Blocks:** 13.3 full tick (`[x]` on this landing) · the retrieval quality gate for 13.8c synthesis + the Copilot path.

## Estimated commit count
**1** — one eval harness + suite + corpus fixture, `packages/evals` territory, zero-egress. **security-reviewer = invariant** (confirm the eval is local/zero-egress — no cloud embed — and no employer-content leaks through the corpus). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the retrieval recall@10 bar runs against a recorded-embedding fixture corpus (deterministic/CI-able), gates hard at the §13.1c bar + a re-ranker no-regression, measures raw vs fused vs re-ranked, and stays zero-egress (local backend only)."
- **Architecture-doc note** — the measured recall@10 value into the EVALUATION_CRITERIA matrix.

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (embedding source) + Q4 (corpus) need answers before GREEN.
2. Run `/tdd retrieval_eval_harness_recall_bar`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
