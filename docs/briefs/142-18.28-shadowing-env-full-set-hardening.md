# /tdd brief — shadowing_env_full_set_hardening

## Feature
Close the `/phase-exit 18` security-MEDIUM: extend `SUBSCRIPTION_SHADOWING_ENV_KEYS` (the armed-boot `assertSubscriptionAuthEnv` guard) from the 8-var set to the **full grounded set** by adding the omitted egress-redirect vars — lowercase `http_proxy`/`https_proxy`, `ALL_PROXY`, and `ANTHROPIC_CUSTOM_HEADERS` — so a lowercase-only proxy / all-proxy / custom-header override can no longer silently arm the subscription path (a Class-B **fail-OPEN**). The guard's `some()` logic is unchanged (L61 — extensible without a logic change); a set var still DEGRADES the arm fail-closed (L52/L62), never crashes boot.

## Use case + traceability
- **Task ID:** 18.28
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (Real Model Transport; the subscription/Option-B armed path), §7 (broker auth/route), §5 (egress veto — the redirect vars are the egress-redirect class), §16 (redaction / fail-closed observability)
- **Related context:** the crossing-gate security audit `docs/audits/18-crossing-security.md` (the MEDIUM finding, `subscription-auth-guard.ts:43`); the runbook `docs/runbooks/phase-18-subscription-enable-decision.md` CHECKPOINT-1 (grounded `http_proxy`/`https_proxy` lowercase + `ANTHROPIC_CUSTOM_HEADERS` as flip-verify candidates; Context7 `/nothflare/claude-agent-sdk-docs`); worker LESSONS **L61** (explicit-extensible watched set — a missed var fails OPEN), **L62** (the full-set enumeration is a flip precondition, DEGRADE-not-crash), **L52** (deny-only source AND-locked; degrade+surface over boot-throw). This is the pre-employer-raw-arming completion of L62's "full enumeration deferred-to-flip."

## Acceptance criteria (what "done" means)
- [ ] `SUBSCRIPTION_SHADOWING_ENV_KEYS` enumerates the full grounded set: the existing 8 **plus** `http_proxy`, `https_proxy`, `ALL_PROXY`, `ANTHROPIC_CUSTOM_HEADERS` (and `all_proxy` iff the Step-2.5 vote lands there — see Q1).
- [ ] For EACH added var: `assertSubscriptionAuthEnv(true, {[var]:"x"})` ⇒ typed refusal `{code:"anthropic_key_set_on_armed_path"}`; `assertSubscriptionAuthEnv(true, {})` ⇒ ok; `assertSubscriptionAuthEnv(false, {[var]:"x"})` ⇒ ok (the shipped/unarmed default NEVER reads env — byte-equivalent, L23/L28).
- [ ] Rule-7 preserved: the fault never echoes the env VALUE (the `not.toContain` pin extends to the added vars).
- [ ] The armed BOOT path DEGRADES the arm (fail-closed, boot-visible) — NOT a crash — when an added var is set (L52/L62). Confirm via the existing arm-wiring path (`subscription-arming-boot-wiring.test.ts` / the arm degrade), extending it if the added-var degrade isn't already covered.
- [ ] The in-file doc comment + the "⚠ FLIP-VERIFY CANDIDATE" notes are updated to reflect the now-enumerated set (the `apiKeyHelper` out-of-band caveat STAYS — it is not an env var this guard can see).
- [ ] `/preflight` clean (worker typecheck + `pnpm --filter @sow/worker test`; repo-wide `pnpm lint`/`format:check` remain the known env-blocked gates — waive, don't fight).

## Wiring / entry point (Step 7.5)
**Already wired — no boot edit.** `assertSubscriptionAuthEnv` is consumed on the ARMED boot path (`withSubscriptionExtractionArming` / the arm wiring in `boot.ts`, reachable e2e — confirmed by `docs/audits/18-crossing-reachability-worker.md`). This slice extends the CONSTANT that the already-called guard reads, so the new vars are enforced through the existing call site with zero new wiring. Entry point to name at Step 7.5: `SUBSCRIPTION_SHADOWING_ENV_KEYS` → `assertSubscriptionAuthEnv(armed)` → the arm-boot degrade path. `/wired assertSubscriptionAuthEnv` to confirm the call site is unchanged.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/subscription-auth-guard.ts` — extend `SUBSCRIPTION_SHADOWING_ENV_KEYS` + update the doc comment (the `some()` guard body is UNCHANGED).
- `apps/worker/test/composition/subscription-auth-guard.test.ts` — extend `FULL_SHADOWING_SET` (the exact-set `toStrictEqual` pin) + the per-var fail-closed loop already iterates it.

**Possibly modified (only if the added-var degrade isn't already covered):**
- `apps/worker/test/composition/subscription-arming-boot-wiring.test.ts` — add/extend a case proving an added var set ⇒ arm DEGRADES (not crash).

If implementation needs files beyond this list, **flag at Step 2.5** before GREEN.

## RED test outline (Step 2)
Tests in `apps/worker/test/composition/subscription-auth-guard.test.ts` (+ the boot-wiring test):

1. **`shadowing_env_full_set_enumerated`** (extend the existing) — Asserts: `[...SUBSCRIPTION_SHADOWING_ENV_KEYS].sort()` == the full grounded set including `http_proxy`, `https_proxy`, `ALL_PROXY`, `ANTHROPIC_CUSTOM_HEADERS`. Why: a var dropped from the constant is a silent fail-OPEN (L61); §19.5 armed-path auth integrity. **RED first** (currently 8, asserting 12 fails).
2. **`shadowing_env_full_set_each_var_fails_closed`** (already loops `FULL_SHADOWING_SET`) — Asserts: for each added var, armed+set ⇒ refuse; unarmed+set ⇒ ok; rule-7 value never echoed. Why: §5 egress-redirect class + §16 redaction; L61.
3. **`armed_boot_degrades_on_lowercase_proxy`** (boot-wiring, if not already covered) — Asserts: the armed boot with `http_proxy` set DEGRADES the arm (arm not effective / boot-visible fail-closed), NOT a throw/crash. Why: L52/L62 degrade-not-crash; §16.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** NONE — `SUBSCRIPTION_SHADOWING_ENV_KEYS` is a worker-internal composition constant, not an Appendix-A / frozen contract surface. No schema-snapshot test owed; no shared-contract seam model (no Appendix-A model on a cross-track dependency edge) is touched.
- **Orchestrator doc rows to write hot (Step 9 routing):** none (no Appendix-A / cross-doc table row). Likely a worker LESSONS update to L62 (or a new lesson) — the full-set enumeration completing the "deferred-to-flip" piece pre-employer-raw-arming — flag it at Step 9; the orchestrator writes it.
- The runbook CHECKPOINT-1 flip-verify note is orchestrator/runbook territory — flag at Step 9 that it's now RESOLVED (the orchestrator updates the runbook), don't edit `docs/runbooks/` from the implementer.

## Things to flag at Step 2.5
1. **Include lowercase `all_proxy` too (both cases for all three proxy vars)?** Options: (a) add `ALL_PROXY` only; (b) add BOTH `ALL_PROXY` + `all_proxy`, and confirm the existing `HTTP_PROXY`/`HTTPS_PROXY` get their lowercase siblings. My default vote: **(b) enumerate both cases for all three proxy vars** — Node/undici honor lowercase proxy env, and a missed case is a fail-OPEN; the marginal cost of extra constant entries is nil, and fail-closed-over-permitted-egress is the safe direction. Context7-confirm which cases the Agent-SDK/undici path honors.
2. **Enumerate exact keys vs a case-insensitive scan of `process.env`?** My default vote: **enumerate the exact keys** (keep the `some()` exact-key match, L61) — a case-insensitive scan over ALL of `process.env` risks false-positives on unrelated vars and changes the guard's shape/contract; explicit enumeration stays auditable + byte-equivalent-logic.
3. **Context7-ground the added vars before landing?** My default vote: **YES** — `query-docs` `/nothflare/claude-agent-sdk-docs` (or claude-api) to confirm the Agent SDK `query()` (runs on Claude Code) honors lowercase `http_proxy`/`https_proxy`, `ALL_PROXY`, and `ANTHROPIC_CUSTOM_HEADERS`; this closes the runbook "re-verify at flip" precondition. Cite the grounding in the commit/Step-9.

## Dependencies + sequencing
- **Depends on:** 18.24/18.23 (the guard + its 8-var set, landed); the crossing-gate security audit (the finding).
- **Blocks:** the FIRST employer-raw armed run (this guard must enumerate the full set before employer-raw content can route the armed cloud path — the dormant-mitigation the audit relied on is the egress veto; this removes the residual code-backstop fail-open). Does NOT block the phase seal (it's a pre-employer-raw-arming hardening).

## Estimated commit count
**1.** A focused SAFETY hardening (auth-shadowing / rule-5 egress-redirect guard) — gets its OWN commit; no bundling (safety-critical pin, per the bundle criteria).

## Lessons-logged candidates anticipated
- **Convention candidate** — completing an "explicit extensible watched set" to its full grounded value before the gated go-live: a shadowing/redirect env guard must enumerate BOTH cases of every proxy var (Node honors lowercase) + the custom-headers override; a missed case/var is a silent fail-OPEN for *permitted* egress (the egress veto covers employer-raw, not permitted-egress redirect). Extends/annotates L61/L62.
- **Architecture-doc note candidate** — none expected (worker-internal constant; §19.5 already documents the armed-path auth guard).
- **Future TODO — operational** — the `apiKeyHelper` settings-injection stays an out-of-band OWNER deployment-checklist item (a #13 precondition), NOT closeable in this guard.

## How to invoke
1. **Read this brief end-to-end** — especially the Step-2.5 questions (the proxy-case + Context7 grounding need answers before GREEN).
2. **Run `/tdd shadowing_env_full_set_hardening`.**
3. **Step 0 (Restate)** — confirm against the Feature line.
4. **Step 1 (Identify files)** — confirm vs Files expected to touch.
5. **Step 2.5** — ping back with the proxy-case + Context7 answers (or take defaults). security-reviewer is MANDATORY at Step 8 (safety-invariant slice).
6. **Step 9** — surface the LESSONS candidate (L61/L62 continuation) + flag the runbook CHECKPOINT-1 "now RESOLVED" for the orchestrator.
