# /tdd brief — connector_poll_registration_and_schedule (16.2)

## Feature
Register the `connectorPoll` activity with a **real `resolve()`** (bind an adapter from the 16.1 `ComposedConnectors` + the cursor repo + `onRecords` = the 15.1 connector→ingestion bridge + backoff) and add the `connectorSyncHealth` workflow to the worker's registered bundle **with a periodic schedule** — today only the proofSpine workflows are registered and `connectorSyncHealth` is absent. After this, a scheduled poll resolves the activity, fetches records, and bridges to `registerSource` → `dispatchSourceIngestion` → note — end-to-end, but **INERT in the shipped default** (16.1's transport default is inert / no connector instance is armed). Worker-track. **Pure-build (dormant substrate) — NO hard line** (a real transport / tokenRef is Phase-23). Closes **G33**.

## Use case + traceability
- **Task ID:** 16.2. **Closes:** G33.
- **Architecture:** `ARCHITECTURE.md §19.3` (primary), `§9` (workflow 10 connector sync & health, LIFE-4/LIFE-6 drain-on-wake), `§8` (poll engine: cursors/retry/health).
- **Depends on:** **16.1** (binds `resolve()` off `ComposedConnectors.ports` / `BootedWorker.connectors`), **15.1** (`connectorIngestionBridge` = the `onRecords` seam), P7 (§9 workflows).
- **Related context (confirm at Step 1 — you own the worker track):**
  - **`packages/workflows/src/activities/connectorPoll.ts:47-51`** — the `resolve()` seam (today unbound). Bind: adapter (by connectorId from `ComposedConnectors.ports`) + cursor repo + `onRecords` (the 15.1 bridge) + backoff.
  - **`apps/worker/src/temporal/registerWorker.ts:261`** — add `connectorSyncHealth` to the registered bundle past `proofSpineWorkflowsPath()`.
  - **15.1 `connectorIngestionBridge`** — the `onRecords → RegisterSourceInput → dispatchSourceIngestion` seam (reachability-waivered until now — 16.2 is where it gets its real caller). Wire it as `onRecords`.

## ⚠ LOAD-BEARING dormancy design (address at Step 2.5)
The shipped default must NOT spam health. A periodic `connectorSyncHealth` run in the shipped default drives the **inert** transport (16.1) over **zero armed connector instances** (no tokenRef until Phase-23). Confirm the design so a shipped-default tick is a **no-op**, NOT a per-tick `unreachable` health item:
- Prefer: the poll iterates only **enabled/armed connector instances** (from the 14.2 connector-instance registry) — in the shipped default that set is EMPTY ⇒ the scheduled run does nothing (no fetch, no health). The e2e test CONFIGURES a fake connector instance + injects a fake-records transport to prove the path.
- The `connectorSyncHealth` workflow is REGISTERED (in the bundle) either way; the question is whether the periodic run fetches. Pin that the shipped default fetches nothing / mints no error-health on a tick.

## Acceptance criteria (what "done" means)
- [ ] **`connectorSyncHealth` registered:** the workflow appears in the worker's registered bundle (past `proofSpineWorkflowsPath()`); a schedule is defined.
- [ ] **Real `resolve()`:** the `connectorPoll` activity resolves an adapter (from `ComposedConnectors.ports`) + cursor repo + `onRecords` (15.1 bridge) + backoff.
- [ ] **End-to-end over a FAKE transport (test):** a scheduled/driven poll with an injected **fake-records** transport + a configured fake connector-instance fetches records → bridges to `registerSource` → `dispatchSourceIngestion` → note. Pin the full path with fakes (zero network).
- [ ] **Shipped-default INERT (dormant):** with the inert transport default + zero armed connector instances, a scheduled run fetches nothing and mints NO error-health (no `unreachable` spam). Pin it.
- [ ] **LIFE-4 / LIFE-6 drain-on-wake:** a missed schedule collapses to ONE run on wake (LIFE-2/LIFE-4), and an in-flight resume reuses the idempotency envelope (LIFE-3/LIFE-6) — mirror the existing schedule discipline (worker forbidden-pattern #5). Pin the catch-up-collapse.
- [ ] **No tokenRef / no secret** read in the shipped default (Phase-17/23 boundary). Confirm at Step 8.
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
`connectorSyncHealth` is reachable from the worker's Temporal registration (`registerWorker.ts`) + its schedule; the `connectorPoll` activity's `resolve()` is reached on a poll run. State: *"Reachable from the registered `connectorSyncHealth` workflow → `connectorPoll` (resolve) → 15.1 bridge → registerSource."* The real transport/tokenRef arming is Phase-23-waivered (L11).

## Files expected to touch
**Modified:** `packages/workflows/src/activities/connectorPoll.ts` (real `resolve()`), `apps/worker/src/temporal/registerWorker.ts` (register `connectorSyncHealth` + schedule), the worker composition (bind the poll deps: `ComposedConnectors` + cursor repo + the 15.1 bridge + the connector-instance registry) + tests. If the schedule needs a new op-store cursor/bookkeeping seam, mirror the existing schedule bookkeeping (LIFE-5).

## RED test outline (Step 2)
1. `connector_sync_health_is_registered_in_the_bundle` — the workflow is in the registered set. (§9)
2. `poll_resolve_binds_adapter_cursor_bridge_backoff` — `resolve()` returns the bound quad. (§8)
3. `scheduled_poll_over_fake_records_bridges_to_a_note` — fake transport + fake instance ⇒ records → registerSource → dispatchSourceIngestion → note (full path, fakes). (§19.3/§9)
4. `shipped_default_tick_is_inert_no_health_spam` — inert transport + zero armed instances ⇒ no fetch, no error-health. (dormancy / NO hard line)
5. `missed_schedule_collapses_to_one_run_on_wake` — catch-up burst-collapse (LIFE-2/LIFE-4). (§9)

## Cross-doc invariant impact
- **Model:** none (task 16.2 "Cross-doc invariant: none"). Confirm.

## Things to flag at Step 2.5
1. **Dormancy — no health spam (LOAD-BEARING).** Confirm the shipped-default tick fetches nothing + mints no error-health (poll only armed instances; empty set ⇒ no-op). This is the "inert substrate" guarantee.
2. **15.1 bridge as `onRecords`.** Confirm you're wiring the existing `connectorIngestionBridge` (not a new seam) — this is its first real caller.
3. **Schedule discipline.** Confirm LIFE-4 catch-up-collapse + LIFE-6 resume mirror the existing schedule bookkeeping (forbidden-pattern #5), not a new mechanism.

## Dependencies + sequencing
- **Depends on:** 16.1, 15.1, P7. **Closes:** G33. This is the slice that gives the 15.1 bridge + 15.4 seenContentHash store their first real drive path (the reachability-auditor's Phase-16 note).

## Estimated commit count
**1.** **security-reviewer = invariant** (the poll drives the ingestion candidate-gate path + the dormancy/no-tokenRef guarantee — a trust boundary even inert). code-quality every-slice. **Pure-build — NO hard line.**

## How to invoke
1. Continue the worker-impl4 session (`/session-start` if cycled).
2. Read this brief (Q1 dormancy no-health-spam is the load-bearing safety point).
3. `/tdd connector_poll_registration_and_schedule`.
4. Step 2.5 — ping the dormancy-no-spam + 15.1-bridge-wiring + schedule-discipline answers. Don't GREEN until sign-off.
5. Step 9 — categorized flags; confirm G33 + that the shipped default is inert. After 16.2, the worker-track Phase-16 slices are done (16.3/16.4/16.5 are integrations).
