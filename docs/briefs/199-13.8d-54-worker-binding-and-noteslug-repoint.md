# /tdd brief — noteslug_repoint_and_living_vault_ingest_binding

## Feature
Two worker-area commits: **(1) #54** — collapse the duplicated `neutralizeRegionMarkers` region-marker-neutralization predicate in `packages/workflows` to a re-export of the canonical `@sow/knowledge` definition (a safety predicate lives ONCE); **(2) 13.8d worker-binding** — bind the landed `rewriteVaultForSource` living-vault rewrite into `runSourceIngestion`'s derive step behind a default-OFF arming flag, with realpath containment on every vault path it touches (ships DORMANT, byte-equivalent default).

## Use case + traceability
- **Task ID:** #54 (carry-forward, origin #51) + **13.8d** worker-binding leg (`IMPLEMENTATION_PLAN.md` §13.8d, L1504-1507 — the knowledge side is DONE `04e01eed`+`3d2d24f9`; this is its production binding)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KN-10/KN-12 living-vault synthesis + structural parity), `§9` (sourceIngestion derive step), REQ-F-021
- **Scope note — this brief widens phase scope because** the binding lands in the §9 source-ingestion workflow while phase 13's header `Spec anchors:` line omits §9 (task 13.8d itself cites §9's producer, so the phase header is under-anchored — the orchestrator routes that as a plan/arch doc note, not a scope change here). The `§2.5` token below is the brief template's "shared-contract seam" section name, not a claimed anchor.
- **Related context:**
  - Canonical predicate: `packages/knowledge/src/markdown-vault/sections.ts:108` (`export function neutralizeRegionMarkers`) — landed with #51 `04e01eed`.
  - The duplicate to retire: `packages/workflows/src/activities/projections/noteSlug.ts:43` (second `export function` of the same name).
  - In-repo consumers of the workflows copy: `noteSlug.ts:66` + `:124`, `projections/meetingOutputs.ts:58/165`, `projections/projectSyncOutputs.ts:34/159/160/204`.
  - Existing behavior pin (must stay green, byte-for-byte): `packages/workflows/test/region-marker-neutralization.test.ts` (~20 assertions incl. nesting, idempotence, pathological input).
  - `packages/workflows/package.json` ALREADY declares `@sow/knowledge: workspace:*`, and `packages/knowledge` does NOT depend on `@sow/workflows` → **no dependency cycle**; the re-export is a pure import-direction change.
  - Living-vault entry: `rewriteVaultForSource` — `packages/knowledge/src/synthesis/ingest-rewrite.ts:92`, exported from the package index (`packages/knowledge/src/index.ts:57`). Deps interface `IngestRewriteDeps` (`ingest-rewrite.ts:66`): `gbrain` / `reason` / `sections` / `structural` / `newPlanId` / `newRunId`. Returns `IngestRewriteReceipt` (`:76`) — `runId`, `plans: KnowledgeMutationPlan[]`, `planIds`, `autoCount`, `proposeCount`.
  - Binding site: `runSourceIngestion` step 6 "DERIVE outputs … (`SourceBuildOutputsPort`)" — `packages/workflows/src/workflows/sourceIngestion.ts` (step-6 contract documented at ~L296-297; port type imported at `:61`, dep declared at `:99`; port defined in `packages/workflows/src/ports/sourceIngestion.ts`).
  - Realpath containment reference implementation: `apps/worker/src/api/procedures/copilotVaultRead.ts:104` — realpath BOTH the target AND the root, then re-check containment on the REAL paths (symlink escape closed). Desktop `guardVaultPath` (`apps/desktop/main/open-in-vault.ts`, desktop L16) is the pure analog. ⚠ handoff 016 labels this "Lesson 17"; **worker L17 is a coverage-leg-seam lesson, not containment** — cite the primitive by path, and flag the correct lesson id at Step 9 (see Step-2.5 Q4).

## ⛔ HARD PRECONDITION for commit 2 (do commit 1 first while it clears)
`packages/knowledge/test/ingest-rewrite.test.ts:152` (`no_production_caller`) shell-greps the repo and asserts **zero** non-test importers of `rewriteVaultForSource` matching `/^(apps|packages\/workflows)\//`. Binding into `packages/workflows` **turns that pin RED**. The pin lives in **knowledge territory** — do NOT edit it.

The knowledge implementer is dispatched (brief 200, commit 1) to reframe it to *"any non-test importer must be the flag-gated composition site; zero importers still passes"* — green before AND after, so there is no RED window. **Sequence:** land commit 1 (#54, no dependency) first; before going GREEN on commit 2, confirm the reframed pin has landed (`git log --oneline -- packages/knowledge/test/ingest-rewrite.test.ts`). If it has not landed by the time commit 1 ships, say so at Step 7.5 rather than editing the knowledge test or shipping a knowingly-RED repo.

## Acceptance criteria (what "done" means)

**Commit 1 — #54 re-point**
- [ ] `packages/workflows/src/activities/projections/noteSlug.ts` no longer DEFINES `neutralizeRegionMarkers`; it re-exports the `@sow/knowledge` definition (call sites unchanged).
- [ ] Exactly ONE `export function neutralizeRegionMarkers` exists in the repo (grep pin, `packages/knowledge/src/markdown-vault/sections.ts`).
- [ ] `packages/workflows/test/region-marker-neutralization.test.ts` passes UNCHANGED (byte-equivalent behavior — the whole point; do not relax an assertion to make the re-export fit).
- [ ] The thin DISPATCHER at `noteSlug.ts:59-66` still delegates to the single authority (it is not a second neutralizer).

**Commit 2 — 13.8d worker-binding (dormant)**
- [ ] `runSourceIngestion`'s derive step can route through `rewriteVaultForSource`, and does so ONLY when the arming flag is strictly `=== true`; unarmed/unbound ⇒ the existing single-note `SourceBuildOutputsPort.build` result is returned **byte-equivalent** (a test asserts the shipped default is unchanged).
- [ ] Every vault path the binding hands to a write/read seam is realpath-contained against the configured vault root (realpath target AND root, re-check on REAL paths); an escaping path is rejected fail-closed with NO write and NO path disclosure in the surfaced fault.
- [ ] The binding never throws: a throwing/faulting rewrite ⇒ the fail-safe path (existing single-note derive OR a typed rejection routed through the health sink per inv-5), never a partial commit.
- [ ] Every emitted mutation still reaches Markdown ONLY via KnowledgeWriter (safety rule 1 — the binding adds no second writer; it passes `KnowledgeMutationPlan`s to the EXISTING step-7 commit path).
- [ ] Default boot registers/arms nothing: a boot-level test proves the flag defaults OFF.
- [ ] `/preflight` clean; repo-wide `turbo typecheck` green (a port-interface change ripples).

## Wiring / entry point (Step 7.5)
- **Commit 1:** no new entry point — `neutralizeRegionMarkers` stays reachable from its existing production callers (`meetingOutputs.ts:165`, `projectSyncOutputs.ts:159/160/204`, `noteSlug.ts:66/124`). Verify with `/wired neutralizeRegionMarkers` that the re-export resolves to the knowledge definition on the real path.
- **Commit 2:** production entry point = `runSourceIngestion` (`packages/workflows/src/workflows/sourceIngestion.ts`), reached from the worker's source-ingestion composition (`apps/worker/src/composition/`). The living-vault leg is reachable-but-inert: the composition root supplies the `rewriteVaultForSource` dep + flag; the shipped config leaves the flag OFF. State this explicitly at Step 7.5 — "reachable, arming-gated, dormant" — the same shape as the other dormant bindings (worker L16: bind the real thing as a NESTED arg INSIDE the existing dormancy gate so lazy eval keeps the default byte-equivalent).

## Files expected to touch
**Modified:**
- `packages/workflows/src/activities/projections/noteSlug.ts` — drop the local definition, re-export from `@sow/knowledge`.
- `packages/workflows/src/workflows/sourceIngestion.ts` — the flag-gated living-vault derive leg.
- `packages/workflows/src/ports/sourceIngestion.ts` — the optional rewrite dep/seam on `SourceIngestionDeps` (optional + fail-closed, mirroring worker L17's `deps.resolve?.() ?? false` shape: unbound ⇒ byte-equivalent, throw ⇒ degrade, NEVER defaults on).
- `apps/worker/src/composition/` + `apps/worker/src/boot.ts` — supply the dep + the default-OFF flag (composition-root = worker, logic-in-package/wire-at-boot).

**New (tests):**
- `packages/workflows/test/` — the binding's unit/integration pins (byte-equivalent default, armed routing, containment reject, never-throws).
- A grep pin for the single-definition invariant (co-locate with the existing region-marker test).

**Do NOT touch:** `packages/knowledge/**` (knowledge territory — including the `no_production_caller` pin), `IMPLEMENTATION_PLAN.md`, `ARCHITECTURE.md`, `LESSONS.md`, `*/CLAUDE.md`, `docs/briefs/`. Flag those at Step 9; the orchestrator writes them.

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)

**Commit 1 —** `packages/workflows/test/` (extend/adjacent to `region-marker-neutralization.test.ts`)
1. **`single_definition_repo_wide`** — Asserts: exactly one `export function neutralizeRegionMarkers` across `packages/*/src` (grep, test-files excluded). Why: a safety-relevant sanitizer must live once (knowledge L1/L2 "predicate lives once" discipline; #51 made knowledge canonical).
2. **`workflows_export_is_the_knowledge_authority`** — Asserts: the symbol imported from the workflows path is referentially IDENTICAL to the one imported from `@sow/knowledge`. Why: proves a re-export, not a re-implementation that happens to agree today.
3. The existing `region-marker-neutralization.test.ts` suite is the behavior-preservation pin — **run it unchanged**.

**Commit 2 —** `packages/workflows/test/` (+ a worker composition pin)
4. **`default_off_is_byte_equivalent`** — Asserts: with the flag absent/false/`"true"`-string, the derive step's output deep-equals the pre-change single-note result and the rewrite dep is **never invoked** (spy call count 0). Why: dormancy (worker L24 / knowledge L2 — strict `=== true`, a truthy-non-`true` is a false-green vector).
5. **`armed_routes_through_rewrite`** — Asserts: flag strictly `true` + dep bound ⇒ the multi-entity plan set from a faked `rewriteVaultForSource` reaches the step-7 KnowledgeWriter commit path (plan ids preserved). Why: §6 KN-10 — the vault rewrites itself on ingest.
6. **`containment_rejects_escaping_path`** — Asserts: a symlink/traversal path escaping the vault root ⇒ typed fail-closed reject, zero write-seam calls, and the surfaced fault/health message contains no absolute path. Why: containment on REAL paths (`copilotVaultRead.ts:104` pattern) + rule 7 (no raw content/paths in a log sink).
7. **`rewrite_fault_never_partial_commits`** — Asserts: a throwing rewrite ⇒ the fail-safe branch, machine state routes through the health sink (inv-5), and NO KnowledgeMutationPlan is half-committed. Why: §9 step-6 "derivation failure → rejected, NO partial commit".
8. **`no_second_writer`** — Asserts: the binding calls the injected KnowledgeWriter commit port only; a grep/inventory pin shows no direct fs/Markdown write introduced. Why: safety rule 1 (one writer).
9. **`boot_default_leaves_flag_off`** — Asserts: the shipped worker config yields the OFF path. Why: dormant-by-default arming discipline.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — reuses `KnowledgeMutationPlan` unchanged (no frozen-contract round, no schema snapshot).
- **§2.5-seam model touched?** No. `SourceIngestionDeps` is a workflows-layer type, not an Appendix-A model.
- **Orchestrator doc rows to write hot (Step 9):** likely an `ARCHITECTURE.md §6` note that the living-vault rewrite has a real (arming-gated) production binding on the source path; a `packages/contracts/CLAUDE.md` cross-doc row is expected to be **unnecessary** — confirm at Step 9. Plus the #54 dedup as a convention/lesson row.

## Things to flag at Step 2.5
1. **`neutralizeRegionMarkers` re-export mechanics.** (a) `export { neutralizeRegionMarkers } from "@sow/knowledge";` from `noteSlug.ts`, (b) import-then-re-export so the local dispatcher keeps using it, (c) drop the workflows export entirely and re-point all 4 call sites at `@sow/knowledge`. My default vote: **(b)** — one-line diff at each existing consumer's import stays untouched, the dispatcher at `:59-66` keeps its documented "thin dispatcher over the single authority" role, and no call site churns. Flag if `@sow/knowledge`'s package entry doesn't re-export `sections.ts` cleanly.
2. **Where the rewrite leg attaches.** (a) inside `SourceBuildOutputsPort.build`'s composition (worker composition swaps in a rewriting implementation), (b) a NEW optional dep on `SourceIngestionDeps` consulted at step 6, (c) a post-step-6 plan-set augmentation. My default vote: **(a)** — it keeps the workflow's step sequence and inv-5 failure routing untouched, makes the arming a pure composition-root concern (logic-in-package, wire-at-boot), and gives the smallest byte-equivalent default. Push back if the port's single-plan return type makes (a) a wider change than (b).
3. **Flag naming + home.** Follow the existing optional-boolean convention (`copilotProposeMode` at `boot.ts:405`, `goLiveArmed` at `servingContextLoader.ts:225`). My default vote: **`livingVaultRewrite?: boolean`** on the same worker config surface, strict `=== true`. Name it whatever matches the neighbours — say which you chose.
4. **The "L17" citation.** Handoff 016 says "realpath containment L17" but worker L17 is about coverage-leg seams. My default vote: **cite the primitive by path** (`copilotVaultRead.ts:104`, desktop L16 `guardVaultPath`) and flag at Step 9 that the handoff's lesson id was wrong so the orchestrator corrects the record — do not invent a lesson reference.
5. **Realpath seam in a workflow context.** `runSourceIngestion` is Temporal-workflow code (determinism constraints — real `fs` calls belong in an activity/adapter, not the workflow body). My default vote: **the containment check lives in the injected adapter/activity seam (worker side), and the workflow only sees typed already-contained paths** — if your read of the code says otherwise, flag it; this is the one place this slice could accidentally put I/O in a workflow.

## Dependencies + sequencing
- **Depends on:** 13.8d knowledge legs (`04e01eed`, `3d2d24f9` — landed); #51 canonical `neutralizeRegionMarkers` (landed); **for commit 2 only:** the knowledge `no_production_caller` pin reframe (brief 200 commit 1).
- **Blocks:** 13.8e scheduled synthesis (needs a real ingest binding), 13.8f-B meeting-path threading (next worker leg), the SPINE arc.

## Estimated commit count
**2 — do NOT bundle into one.** Commit 2 carries safety pins (containment, one-writer, dormancy) so it gets its own commit per the safety rule; commit 1 is an independent de-duplication with its own bisect value. Same brief, two Step-10 commits, in this order (#54 first — it has no cross-area precondition).

## Lessons-logged candidates anticipated
- **Convention candidate** — "A shared sanitizer/predicate that migrated to a lower package leaves a RE-EXPORT, never a second definition; pin it with a referential-identity test, not just a behavior test."
- **Convention candidate** — "A dormancy pin phrased as *zero importers* becomes a merge hazard the moment the binding slice lands; phrase it as *every importer must be the flag-gated site* from the start."
- **Architecture-doc note candidate** — §6: the living-vault rewrite's production binding point on the source-ingestion path + its arming flag.
- **Future TODO — operational** — plan-set size/flood bounds when a real multi-entity rewrite runs against a large vault.

## How to invoke
1. Read this brief end-to-end, including the ⛔ HARD PRECONDITION and all five Step-2.5 questions.
2. Run `/tdd noteslug_repoint_and_living_vault_ingest_binding`.
3. Step 0 restate → Step 1 confirm the file list → **Step 2.5 send the test-design write-up with the coverage map** (one `Asserts:` line per test + each acceptance bullet → its covering test).
4. Step 8 reviewers: `code-quality-reviewer` (every-slice) + `security-reviewer` (**invariant** — commit 2 touches containment + one-writer).
5. Step 9: categorized flags + ship-ask. Commit 2 is safety-touching → expect the orchestrator to route its sign-off via the lead.
