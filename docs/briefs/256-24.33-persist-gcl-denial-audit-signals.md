# /tdd brief — persist_gcl_denial_audit_signals

> ⛔⛔ **RE-AUTHORED 2026-08-12 AGAINST A CORRECTED PREMISE. THE ORIGINAL VERSION OF THIS BRIEF WAS BUILT ON A CLAIM THAT WAS FALSE.**
>
> It asserted that `admitProjection`'s deny branches were **LIVE**, and on that basis arming-block condition (d) was extended to cover this task. **That was wrong.** `resolveApprovedCrossWorkspaceSlice` and `reconcileGlobalMarkdown` — the only two callers — **both have zero production callers**; every call site is in their own test files, and `boot.ts:1765` / `buildGclProjection.ts:13` / `scopedRetrieval.ts:15` are **comments**. **knowledge-implementer caught this from source at Step 0, before writing a line; the lead confirmed it independently; the owner reverted condition (d) to `24.7` only.**
>
> ⇒ **`24.33` is OFF the release path. It is a WIRING PRECONDITION (class of `24.9`): required before the cross-workspace read path is wired (Phase 25.2/25.4), not before general arming.** ⭐ **The DEFECT is real and unchanged in severity — only its reachability claim was wrong.** Contracts **`L141`**.
>
> ⛔ **DO NOT hand-verify this task's reachability again.** Three hand-verified calls on this one module were wrong in a single session, by three different methods, all by competent people who had just watched the previous one fail. **If you need reachability, run `24.29`'s census.**

## Feature
`admitProjection`'s deny branches build a mandatory `AuditSignal` and **discard it** — the same shape `24.7` fixed on the interactive-Copilot path. Thread audit persistence through the GCL admission path so a policy denial there produces a durable, operator-visible record. ⚠ **Build the MECHANISM; do NOT wire it into production** — its callers are dormant and wiring them is Phase 25.2/25.4's job.

## Use case + traceability
- **Task ID:** `24.33`
- **Architecture sections it implements:** §5 (policy/denial taxonomy), §16 (audit + error convention) · Safety rule 4 (workspace isolation)

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 safety-assertion audit, which sweeps every area.

- **Related context:** `IMPLEMENTATION_PLAN.md` task `24.33` (**read its corrected reachability block first**) · **`24.7`** (`0c8de4b2`) — **the mechanism to mirror**: `toAuditRecordInput` → `AuditRepository.append`, behind a port gated on `isRedactionSafe` · `packages/policy/src/decision.ts:39-41` (`audit: AuditSignal` is non-optional on every deny) · `docs/sessions/155-…md` (the correction, in the implementer's own words).

## Acceptance criteria (what "done" means)
- [ ] `admitProjection`'s deny branches **carry** their `AuditSignal` outward instead of dropping it — the signal must reach the layer that can persist it, rather than dying inside the gate.
- [ ] A denial on the GCL admission path, driven end-to-end through the real `admitProjection` → `serveProjection` / `admitAndPersistProjection` chain (fake **only** at the storage-repository seam, matching how `crossWorkspaceRead.ts`'s existing suite is built), **lands a durable, queryable audit record**.
- [ ] ⭐ **The persist capability is INJECTED and UNBOUND in production** — same dormant-but-complete shape `24.33`'s own sibling half (`guardCrossWorkspaceRawRead`) already carries, and the shape `L24` prescribes: logic in the package, wiring at the composition root, in a **separate** slice.
- [ ] ⛔ **`isRedactionSafe`-equivalent gating before any persist**, mirroring `24.7`'s `createAuditPersistPort` — **fail-closed refuse-to-persist**, never a best-effort write of a possibly-unsafe signal.
- [ ] **A benign (ALLOW) run persists NOTHING** — zero calls to the sink. ⚠ **This is the pin that keeps the channel meaningful**: firing on every path destroys the distinction it exists to create (`L86`).
- [ ] ⛔ **The claim in the commit message and in any acceptance note is SCOPED**: *"durable and queryable at the admission boundary; **no production caller reaches this path yet** — see Phase 25.2/25.4."* **Never the unqualified "GCL denials are now audited"** (`L106`: a capability is not a guarantee, and the unqualified form is what gets quoted).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
⛔ **`none — wiring lands in Phase 25.2/25.4`**, which are real tracked tasks (`### 25.2`, `### 25.4`). **This is deliberate and is the whole point of the task's reclassification: `24.33` exists so the mechanism is correct and complete the moment either caller goes live.** ⚠ **State reachability precisely in Step 9 — "tested and correct but unwired" — and do NOT describe anything in this file as live.**

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gcl/visibility-gate.ts` — `admitProjection`'s deny branches carry the signal
- `packages/knowledge/src/gcl/` — wherever `serveProjection` / `admitAndPersistProjection` compose (confirm exact file at Step 1)

**New:**
- test coverage in `packages/knowledge/test/` (confirm the existing convention — `gcl-visibility-gate.test.ts` is where `24.36`/`24.38` landed)

⚠ **If this genuinely requires touching `apps/worker` (`crossWorkspaceRead.ts` / `boot.ts`), STOP and flag at Step 2.5** — that is cross-track and is the follow-up leg, not this slice.

## RED test outline (Step 2)
1. **`admit_projection_deny_carries_its_audit_signal`** — Asserts: a denial's result carries the `AuditSignal` rather than dropping it. Why: the literal defect.
2. **`gcl_denial_lands_a_durable_audit_record_end_to_end`** — Asserts: driving a real denial through the real chain, faking only the repository, produces a persisted record. Why: `24.7`'s precedent — **MOVE THE STATE.** ⛔ **A construction-side test cannot distinguish "the signal is built and dropped" from a fix, because that is exactly the defect.**
3. **`redaction_unsafe_signal_is_refused_not_persisted`** — Asserts: fail-closed. Why: mirrors `24.7`'s `isRedactionSafe` gate; a persist path that writes unsafe content is a rule-7 breach.
4. **`allow_path_persists_nothing`** — Asserts: zero sink calls on a benign run. Why: `L86` — a channel that fires always carries no information.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none expected — `AuditSignal` / `AuditRecord` already exist and `24.7` established the mapping. ⚠ **If you find yourself needing a new field, flag at Step 9; do not add one.**
- **Orchestrator doc rows to write hot:** none anticipated.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No — this consumes existing models.

## Things to flag at Step 2.5
1. **Reuse `24.7`'s `createAuditPersistPort` directly, or a GCL-local equivalent?** My default vote: **reuse if it is importable without inverting the package dependency direction; a local equivalent that DUPLICATES the `isRedactionSafe` gate is the outcome to avoid** (`L39`: a safety predicate lives once — re-export, never re-define). **State which, and why.**
2. **Where does the injected capability sit — a parameter on `admitProjection`, or on the composing layer (`serveProjection`/`admitAndPersistProjection`)?** My default vote: **the composing layer.** `admitProjection` is documented pure + fail-closed and returns a typed `Result`; threading a side-effecting port into a pure predicate is the change most likely to cost you that property. **If you disagree, say so — you know this file better than the brief does.**

## Dependencies + sequencing
- **Depends on:** `24.7` ✅ `0c8de4b2` (the mechanism this mirrors).
- **Blocks:** the wiring of `crossWorkspaceRead` / `reconcileGlobalMarkdown` — **Phase 25.2 / 25.4**, not general arming.

## Estimated commit count
**1.** One focused mechanism. ⚠ **Safety-adjacent (rule 4 / rule 7 surface) ⇒ its own commit, not bundled.**

## Lessons-logged candidates anticipated
- **Convention candidate** — possibly about the reclassification rather than the code: **a defect whose severity is real but whose reachability claim was wrong does not become less real; it changes WHICH GATE it blocks.** `24.33` moved from a release condition to a wiring precondition without its severity changing at all, and that distinction is worth a line if it recurs.

## How to invoke
1. Read this brief **including the correction block at the top** + `IMPLEMENTATION_PLAN.md` task `24.33` + `24.7`'s diff (`0c8de4b2`).
2. Run `/tdd persist_gcl_denial_audit_signals`.
3. Step 2.5 — question 2 is genuinely yours to call.
