# /tdd brief — persist_gcl_admitprojection_denial_audit_signals

## Feature
`admitProjection`'s deny branches (the GCL Visibility Gate — the single cross-workspace read path, safety rule 4) discard a mandatory `AuditSignal` on every denial, identical to `24.7`'s interactive-Copilot shape. This leg is **arming-block condition (d)'s second half** — the block does not lift until both `24.7` (shipped) and this land. Reuse `24.7`'s exact mechanism (`toAuditRecordInput` → `AuditRepository.append`), do not re-derive it.

## Use case + traceability
- **Task ID:** `24.33`
- **Architecture sections it implements:** §5, §16 — **Safety rule 4** — **arming-block condition (d), second leg**

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase — its tasks originate from the 24.6 safety-assertion audit, which sweeps every area, so a remediation task legitimately cites the anchors its originating finding lives under rather than only Phase 24's own declared set.**

- **Related context:** `IMPLEMENTATION_PLAN.md:3188-3196` (task 24.33, full text — READ THIS FIRST, it documents the orchestrator's own two filing errors and their corrections, checkable line by line). `docs/briefs/252-24.7-…md` — the sibling brief this one mirrors; read its "Wiring/entry point" and RED outline for the pattern to reuse, not reinvent. `apps/worker/src/boot.ts`'s `createAuditPersistPort` (24.7, `0c8de4b2`) — the EXISTING port implementation; this slice needs the SAME mechanism reachable from `packages/knowledge`, which already depends on `@sow/db` directly (confirm this before assuming a new port is needed — `packages/knowledge` may not need `apps/worker`'s narrow-port layering at all, since it isn't the `apps/worker/api/procedures` layer that pattern was scoped to).

## Acceptance criteria (what "done" means) — outcome-phrased, per the owner's ruling on condition (d)
- [ ] A real `admitProjection` deny (any of `VISIBILITY_EXCEEDS_SOURCE`/`VISIBILITY_TYPE_MISMATCH`/`MALFORMED_POLICY_INPUT`) triggered through the REAL live chain (`admitProjection` ← `serveProjection` ← `apps/worker/src/composition/crossWorkspaceRead.ts:139`) lands a durable, queryable `AuditRecord` — verified by an END-TO-END test that triggers the real denial and queries the repository afterward, not a construction-side-only assertion. **MOVE THE STATE**, per `24.7`'s and `24.17`'s precedent — a test asserting `decision.audit` is non-null does not discharge this.
- [ ] The allow path mints zero new audit records through this call (same both-directions requirement `24.7` pinned).
- [ ] Persisted record is code-only, redaction-safe (confirm `buildAuditSignal`'s guarantee survives unchanged — don't re-derive, cite the existing pin).
- [ ] A persistence fault never changes `admitProjection`'s return value — the deny guarantee never depends on the audit write succeeding (same principle `24.7` established).
- [ ] `guardCrossWorkspaceRawRead` (the confirmed-DORMANT half — see task text, its only path traces to two unregistered Phase-25/26 workflow files) gets the same fix OPPORTUNISTICALLY if cheap in the same file/pattern, but is NOT required for condition (d) — only `admitProjection`'s live half is on the release path. State explicitly in the commit which half you did.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
Already live and already established this session — `admitProjection` ← `serveProjection`/`admitAndPersistProjection` (`packages/knowledge/src/gcl/projection.ts`) ← `crossWorkspaceRead.ts:139`. This slice does not change whether/when `admitProjection` runs, only whether its deny branches persist their audit signal.

## Files expected to touch
**Modified:**
- `packages/knowledge/src/gcl/visibility-gate.ts` — `admitProjection`'s deny branches
- `packages/knowledge/src/gcl/projection.ts` (`serveProjection`/`admitAndPersistProjection`) — likely needs to thread a persist capability through, mirroring how `24.7` threaded `auditPersist` through `runGovernedCopilotSynthesis`
- wherever `packages/knowledge`'s composition root binds its dependencies (confirm exact file — likely something `apps/worker/src/boot.ts` calls, or `packages/knowledge` exposes a factory `apps/worker` invokes) — bind to the same `backends.repos.audit`/`backends.now` `24.7` already uses

**New:**
- test file(s) under `packages/knowledge/test/gcl/` matching existing convention

## RED test outline (Step 2)
Mirror `24.7`'s `copilotDenialAudit.test.ts` RED outline exactly, adapted to this entry point:
1. **`admitprojection_deny_persists_durable_audit_record`** — trigger a real deny through the live chain, assert a queryable record lands.
2. **`admitprojection_allow_mints_zero_records`** — allow path, assert no new record.
3. **`persisted_record_is_redaction_safe`** — confirm the existing guarantee holds through this path too.
4. **`persistence_fault_never_changes_admitprojection_return`** — a failing persist port still returns the correct deny/allow `Result`.
5. **`sustained_probing_queryable_distinguishable`** — N denials for one actor/workspace query as N; a quiet workspace queries zero.

## Cross-doc invariant impact
- **Model field changes:** none — reuses `AuditRecord`/`AuditRepository`, already frozen and tested.
- **Orchestrator doc rows to write hot:** none anticipated.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **Does `packages/knowledge` need its own narrow `AuditPersistPort`-equivalent, or can it depend on `@sow/db`'s `AuditRepository` directly?** My default vote: **check first** — `24.7`'s narrow-port design was specifically to keep `apps/worker/api/procedures`'s import surface narrow; `packages/knowledge` may have a different existing convention (it may already import `@sow/db` types elsewhere). Don't copy `24.7`'s port shape reflexively if it doesn't fit this package's actual pattern — cite what you find.
2. **Should `guardCrossWorkspaceRawRead` (dormant half) be fixed in this same commit, or filed as its own tiny follow-up?** My default vote: **same commit if the fix is genuinely opportunistic (same file, same pattern, a few extra lines)** — but do not let it delay or complicate the live half, which is the one on the release path. If it turns out non-trivial, split it out and say so.

## Dependencies + sequencing
- **Depends on:** `24.7` ✅ `0c8de4b2` (reuses its mechanism).
- **Blocks:** arming-block condition (d)'s second leg — the block does not lift without this.

## Estimated commit count
**1** for the live `admitProjection` half (safety-critical, own commit). The dormant `guardCrossWorkspaceRawRead` half rides the same commit only if trivially cheap — otherwise its own, non-blocking.

## Lessons-logged candidates anticipated
- None new anticipated — this directly discharges the claim `L140` was banked about (the orchestrator's own repeat of the `apps/`-only mistake). If GREEN surfaces anything about the reachability chain that wasn't already established, flag it — don't assume this brief's premises are exhaustively correct just because they were re-verified twice already.

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md:3188-3196` (task 24.33) + `docs/briefs/252-24.7-…md` (the sibling pattern).
2. Run `/tdd persist_gcl_admitprojection_denial_audit_signals`.
3. Step 2.5 Q1 (port shape) is load-bearing — don't default past it without checking `packages/knowledge`'s actual existing conventions.
