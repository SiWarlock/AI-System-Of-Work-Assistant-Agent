# /tdd brief — uisafe_task_rollup_projection_type

## Feature
An **additive `UiSafeTaskRollup` projection type** (packages/contracts/src/api/ui-safe.ts) — a priority/due-RANKED, workspace-scoped, UI-safe task-list projection that unblocks 13.16's "highest-priority tasks" surfacing (Today + daily brief + Copilot). Additive, `.strict()`, UI-safe-only, **no frozen Appendix-A snapshot change** (an API projection type, not a canonical model). Mirrors the successful #21 contract-first-unblock pattern.

> **Widens phase scope because** it defines the `§5` UI-safe-projection + `§10` read-model-serving shape behind 13.16's `§11` task surface — the contract leg the downstream 13.16 producer (worker) + renderer consume.

## Use case + traceability
- **Task ID:** 13.16 (contract leg — the `UiSafeTaskRollup` type)
- **Architecture sections it implements:** `ARCHITECTURE.md §5` (renderer renders ONLY UI-safe projections — the redaction boundary; WS-8) · `§11` (Today/task surfaces) · `§10` (read-model serving shapes).
- **Related context:**
  - The typed `Task` Appendix-A model (13.15, `54b052a7`) carries `priority` (never inferred — absent ⇒ unset/TBD sentinel), dueDate, status, workspace/project binding. `UiSafeTaskRollup` is the UI-safe PROJECTION of that store for the "highest-priority tasks" answer.
  - The downstream 13.16 producer (worker task-rollup projection, WS-8 fail-SAFE) + the renderer Today/Copilot consumption land LATER — this slice defines the contract shape only (like #21's UiSafe schedule type unblocked 9.9).

## Acceptance criteria (what "done" means)
- [ ] A `UiSafeTaskRollup` projection type in `ui-safe.ts`: a **pre-ranked ordered list** of UI-safe task items (`taskId`, `title`, `priority` [optional/sentinel — never forced], `dueDate?`, `projectRef?`, `status`), `.strict()`, UI-safe-only (no raw content/secrets/prompts).
- [ ] **Priority is representable-as-unset** — an absent priority is a valid shape (optional or a TBD sentinel), so the projection NEVER forces/fabricates a priority (mirrors 13.15's no-inference: absent ⇒ unset, never guessed).
- [ ] **Workspace-scoped (WS-8)** — the rollup shape carries no cross-workspace blend; a test pins it represents a single workspace's ranked tasks (no cross-workspace item shape).
- [ ] `.strict()` rejects raw over-population (a poisoned instance with a secret/raw field is rejected).
- [ ] **No frozen Appendix-A snapshot change** — confirm this is a UI-safe projection type, NOT a snapshot-frozen canonical model; if any turns out frozen, STOP + flag at Step 2.5.
- [ ] All unit tests in `packages/contracts/test/api/…` pass; repo-wide `turbo typecheck` green (additive); `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — consumers land downstream:** the 13.16 worker task-rollup producer PRODUCES it (via `queries.*`); the renderer Today/Copilot "highest-priority tasks" surface CONSUMES it. This slice defines the contract shape only. State explicitly. (spec-lint requires this section.)

## Files expected to touch
**Modified:**
- `packages/contracts/src/api/ui-safe.ts` — the additive `UiSafeTaskRollup` (+ item) type.
- `packages/contracts/src/index.ts` — barrel-export the new type.
- `packages/contracts/test/api/…` — validators + additive-shape + WS-8 + strict-reject tests.

If any of this requires a frozen `schemas/*.json` / snapshot change, **flag at Step 2.5** (scoped as additive-no-snapshot, per #21).

## RED test outline (Step 2)
1. **`uisafe_task_rollup_valid_ranked_shape`** — a valid pre-ranked task list (item fields) validates. Why: §11 task surface / §10.
2. **`priority_representable_as_unset`** — an item with an absent/sentinel priority is valid (never forced). Why: §6/13.15 no-inference (absent ⇒ unset).
3. **`rollup_is_workspace_scoped_no_cross_blend`** — the shape carries no cross-workspace item; single-workspace ranked tasks only. Why: §5 WS-8.
4. **`strict_rejects_raw_overpopulation`** — a poisoned instance (secret/raw field) is rejected (`.strict()`). Why: §5 / forbidden-pattern #5 (UI-safe-only).
5. **`no_frozen_snapshot_for_projection`** — confirm no `schemas/ui-safe*.json` snapshot is frozen for this type (additive projection). Why: per-lead additive-no-snapshot.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** additive UI-safe projection type (`UiSafeTaskRollup`). **No frozen Appendix-A canonical model change.**
- **Orchestrator doc rows to write hot:** if `ui-safe.ts` types appear in the `packages/contracts/CLAUDE.md` cross-doc table, add the `UiSafeTaskRollup` row (flag at Step 9). Otherwise none.
- **Contracts-seam model touched?** No — an API projection type, not a §2.5 canonical seam model; confirm no `schemas/ui-safe*.json` snapshot to bump.

## Things to flag at Step 2.5
1. **Item field set.** Default vote: `{taskId, title, priority?, dueDate?, projectRef?, status}` — the UI-safe subset of the 13.15 Task model. Confirm against the Task model's UI-safe-projectable fields.
2. **Priority representation.** Default vote: **optional** (absent = unset; the producer never guesses — no-inference) rather than a required field. Confirm (vs an explicit TBD sentinel).
3. **Ranking carried by the projection.** Default vote: the projection is a **pre-ranked ordered list** (the 13.16 producer ranks deterministically by priority/dueDate; the renderer renders the order, never re-sorts by a model signal). Confirm.
4. **Snapshot check.** Default vote: confirm NO `schemas/ui-safe*.json` snapshot is frozen (additive-no-snapshot, per #21). If one exists, flag — it changes the round.

## Dependencies + sequencing
- **Depends on:** 13.15 (Task model + priority, `54b052a7`); the existing `ui-safe.ts` surface.
- **Blocks:** the 13.16 worker task-rollup producer + the renderer "highest-priority tasks" surface.

## Estimated commit count
**1** — one additive UI-safe projection type in one file, no frozen snapshot, no safety-invariant WRITE (it TIGHTENS the UI-safe surface). **security-reviewer = invariant** (WS-8 no-cross-workspace-blend + UI-safe-only). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "a task-rollup UI-safe projection is a pre-ranked ordered list, priority-representable-as-unset (no-inference), workspace-scoped (WS-8, no cross-blend), `.strict()`; defined contract-first to unblock the producing procedure + consuming surface."
- **Architecture-doc note** — the pre-ranked / priority-optional contract that the 13.16 producer + renderer depend on.

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (field set) + Q4 (snapshot check) need answers before GREEN.
2. Run `/tdd uisafe_task_rollup_projection_type`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
