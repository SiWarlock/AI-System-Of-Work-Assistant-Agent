# /tdd brief — employer_egress_default_seed_flip

## Feature
**⛔ OWNER-AUTHORIZED rule-5 CROSSING — the Employer-Work cloud-egress FLIP via default-seed.** Extend the workspace-provisioning cloud-copilot allowlist seed to ALSO seed `employer_work` (allowlist=`[claude]` + raw-content-allowlist=`[claude]` + `employerRawEgressAcknowledged=true` + `acknowledgedAt`) — so employer-raw cloud egress now RESOLVES ack=true and the veto ALLOWS it (the inverse of 9.10-A's existing DENY pin). Supersedes 9.10-B's audited-acknowledge as the employer-OPEN mechanism (owner chose silent default-seed).

> **⛔ HARD-LINE crossing — OWNER-AUTHORIZED (2026-07-25, via lead).** The owner confirmed the active `claude` login IS the **company subscription** (company-sanctioned → resolves the confidentiality/policy concern; §ARM-18 precondition MET). This slice OPENS real employer cloud egress. Own commit; security-reviewer=invariant; **Step-9 routes to the LEAD** (not the orchestrator).

## Use case + traceability
- **Task ID:** 9.10 (the employer-egress OPEN leg — supersedes the planned 9.10-B audited-ack for the open mechanism)
- **Architecture sections it implements:** `ARCHITECTURE.md §5` (EgressPolicy / egress veto — rule 5 Employer-Work egress) · **REQ-S-002**.
- **Related context:**
  - Builds directly on **9.10-A** (`40414dd1`): the store-backed `WorkspacePostureResolver` is the SOLE production posture; `seedPersonalCloudCopilotAllowlist` (provisionWorkspace.ts) currently seeds personal (`personal_business`/`personal_life`) = `[claude]` and NOT `employer_work` (fail-closed). This slice extends that seed to `employer_work`.
  - §ARM-18 records employer cloud-egress OWNER-APPROVED in principle, gated on **login=company-subscription at the flip** — that precondition is now CONFIRMED by the owner. Company-sanctioned.
  - The 9.10-A DENY pin (`employer_raw_cloud_ack_off_still_denies`) proves employer-raw fails closed when unseeded; this slice adds the INVERSE ALLOW pin.

## Acceptance criteria (what "done" means)
- [ ] The seed fn (RENAME from `seedPersonalCloudCopilotAllowlist` → e.g. `seedCloudCopilotAllowlist` since it is no longer personal-only) ALSO seeds `employer_work`: `allowedProcessors=[claude]` + `rawContentAllowedProcessors=[claude]` + `employerRawEgressAcknowledged=true` + `acknowledgedAt` (provisioning clock) — the SAME shape as the personal seed plus the employer-raw-ack fields.
- [ ] A composition-root test proves `employer_work` now **RESOLVES ack=true** (via the 9.10-A store-backed resolver) AND the **veto ALLOWS employer-raw-cloud** for the `[claude]` route — the exact INVERSE of 9.10-A's `employer_raw_cloud_ack_off_still_denies` DENY pin.
- [ ] **Scoping is exact — NO over-broad allow:** the allow is `employer_work` + processor `[claude]` ONLY; a NON-`claude` processor on employer-raw STILL DENIES (rule-5 — the veto's processor allowlist bites); a test pins that a non-`claude` cloud processor for employer-raw is still refused. (The flip opens `claude` only, not "all cloud".)
- [ ] Personal seed UNCHANGED (`personal_business`/`personal_life` still `[claude]`); the personal DENY/ALLOW behavior is byte-equivalent.
- [ ] **9.10-A's FAULT behavior PRESERVED (lead constraint):** fail-closed-on-absence/fault + the WS-8 read-back re-gate are UNCHANGED — we seed a durable `true` value at provisioning, we do NOT weaken fault handling; the store-backed resolver stays the SOLE posture source. A test confirms an absent/faulted employer posture STILL fails closed (the seed is a provisioned value, never a fault-time default-true).
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
The seed runs at workspace provisioning (`provisionWorkspace.ts` — the sole seed site); the 9.10-A store-backed `WorkspacePostureResolver` reads the durable `egressPolicy` at the live veto sites (copilot Q&A / briefing / egressStatus), and the broker egress veto now ALLOWS the `[claude]` employer-raw cloud route. This is a LIVE behavior change — employer_work workspaces provisioned after this flip have cloud egress OPEN for `claude`. State this explicitly.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/provisionWorkspace.ts` — extend + rename the seed fn to also seed `employer_work` (the employer-raw-ack fields).
- the composition-root test file (mirror 9.10-A's posture tests) — the inverse-ALLOW pin + the non-`claude`-still-denies pin + the unchanged-personal pin.

⚠ Worker territory. Own commit — do NOT bundle. `git add <path>` only.

## RED test outline (Step 2)
1. **`employer_seed_resolves_ack_true`** — after provisioning, `employer_work` resolves `employerRawEgressAcknowledged=true` (via the store-backed resolver). Why: §5 the flip.
2. **`veto_allows_employer_raw_cloud_claude`** — the assembled broker veto ALLOWS an employer-raw cloud job on the `[claude]` route (inverse of 9.10-A's DENY pin). Why: §5 rule-5 flip (owner-authorized).
3. **`non_claude_employer_raw_still_denies`** — a non-`claude` cloud processor on employer-raw STILL DENIES (the allow is `[claude]`-only, not all-cloud). Why: §5 — the flip is scoped, not a blanket open.
4. **`personal_seed_unchanged`** — personal workspaces still `[claude]`; behavior byte-equivalent. Why: no regression.
5. **`acknowledged_at_stamped`** — the seed stamps `acknowledgedAt` (provisioning clock). Why: audit trail (REQ-S-002).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator/LEAD writes the docs)
- **Model field changes:** none (reuses the frozen `EgressPolicy` fields — `employerRawEgressAcknowledged`/`acknowledgedAt`/allowlists).
- **§ARM-18 ledger (orchestrator writes at close):** record the employer-egress FLIP **EXECUTED** (owner-authorized, login=company confirmed, default-seed mechanism chosen over the audited-ack; the crossing crossed). Flag at Step 9.
- **ARCHITECTURE §5/§16 note candidate:** employer cloud egress is now OPEN-by-default-seed for `[claude]` (company-sanctioned); the audited-ack (9.10-B) is superseded for the open — its visibility/revoke re-scoped separately.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Fn rename.** Default vote: `seedPersonalCloudCopilotAllowlist` → `seedCloudCopilotAllowlist` (no longer personal-only); confirm the name + that all call sites update.
2. **Employer seed field set.** Default vote: `allowedProcessors=[claude]` + `rawContentAllowedProcessors=[claude]` + `employerRawEgressAcknowledged=true` + `acknowledgedAt` — mirror the personal seed + add the employer-raw-ack fields. Confirm the exact `EgressPolicy` fields (against the frozen model).
3. **Scope guard.** Default vote: the allow is `[claude]`-only — confirm a non-`claude` employer-raw cloud route STILL DENIES (the veto's processor allowlist is what bites; the seed only adds `claude`). This is the load-bearing rule-5 pin (a scoped flip, not a blanket open).
4. **acknowledgedAt source.** Default vote: the provisioning clock (`now`). Confirm.

## Residual (document in the brief + IMPLEMENTATION_PLAN §ARM-18) — lead directive
Employer egresses under whatever `claude` login is **active** — "company-sanctioned" holds ONLY while the company login is active; it does **NOT** re-confirm on future login switches. The per-workspace subscription/credential SPLIT (employer→company, personal→personal) is the clean end-state that closes this (§ARM-18, queued). **9.10-B** (egress-status visibility + revoke surface) stays planned for its see-status + revoke value — it is just no longer the opener.

## Dependencies + sequencing
- **Depends on:** 9.10-A (`40414dd1` — the store-backed resolver + the personal seed) · the §ARM-18 login=company precondition (owner-CONFIRMED).
- **Blocks:** — (9.10-B's egress-status visibility + revoke path re-scoped separately, non-blocking).
- **Priority slice** — its own clean cycle (owner-authorized rule-5 flip); NOT bundled.

## Estimated commit count
**1** — own commit (rule-5 flip, never bundled). **security-reviewer = invariant** (MANDATORY — confirm the allow is exactly `employer_work` + `[claude]`, no over-broad/blanket-cloud allow; the inverse-ALLOW + non-`claude`-still-denies pins hold; personal unchanged). **Step-9 routes to the LEAD** (not the orchestrator — rule-5 crossing).

## Lessons-logged candidates anticipated
- **Convention candidate** — "an owner-authorized rule-5 egress flip is a SCOPED default-seed (employer_work + `[claude]` only, ack=true + acknowledgedAt), proven by the inverse-ALLOW pin + a non-`claude`-still-denies pin (a scoped open, never blanket-cloud); it rides the 9.10-A store-backed single-source resolver."
- **Architecture-doc note / §ARM-18** — the employer-egress flip EXECUTED (company-sanctioned login=company); the audited-ack superseded for the open.

## How to invoke
1. Read end-to-end — Step-2.5 Q2 (field set) + Q3 (scope guard — the load-bearing rule-5 pin) need answers before GREEN.
2. Run `/tdd employer_egress_default_seed_flip`.
3. Step 2.5 → main-orchestrator. **⚠ Step 9 → the LEAD** (rule-5 crossing; the orchestrator does NOT sign off the ship).
