# /tdd brief — rebuild_oracle_coverage_leg

## Feature
Make the serving-coverage **`oracleBuildOk`** leg BINDABLE — the last hardwired-false coverage leg. Replace the unconditional `oracleBuildOk: false` in `createServingCoverageReader` with an OPTIONAL fail-closed **`resolveOracleBuild?: () => boolean`** dep (mirroring the existing boot-resolved `resolveRunning`/`pinValid` leg): unbound ⇒ `false` ⇒ degrade (byte-equivalent to today). After this leg, all 4 coverage legs are bindable, so `deriveServingCoverage`'s AND can reach non-degraded — but the leg ships **DORMANT** (boot leaves `resolveOracleBuild` unbound ⇒ `oracleBuildOk` false ⇒ still degrades).

**HONEST SCOPE — the serve path takes NO I/O.** The heavy **gbrain import-into-scratch** that PRODUCES the rebuild-oracle build-status (real gbrain I/O) is a **build-time producer** — owner-gated, a later deliverable, NOT wired here. This slice only adds the serve-time SEAM that reads a boolean status (mirroring how B1/B2 added the parity read-port seam). After this leg the ONLY remaining blocker to a green admission is the **owner-gated arming** (binding `resolveOracleBuild` + the store + `goLiveArmed` = the HARD LINE). **Propose stays structurally OFF.**

## Use case + traceability
- **Task ID:** 13.10 (Phase 13 — Tier-4 precondition: the `admitForServing`-backed serving oracle; the last dormant serve-time coverage-leg seam.)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (serving coverage — `oracleBuildOk`, the rebuild-oracle build-status leg, becomes bindable) + `§12` (the write-through GO conditions; the leg is *all fail-closed* — an unbound/faulting probe degrades, never a false green). Both ∈ Phase 13 Spec anchors — **no widen**.
- **Related context (reused, NOT modified):**
  - The reader (built B2): `createServingCoverageReader` (`apps/worker/src/api/procedures/servingContextBootReaders.ts:110`) — today returns `oracleBuildOk: false` unconditionally (`:126`, `:128`), with the note "the rebuild-oracle build-status leg is a distinct later slice" (`:101-108`). The sibling boot-resolved leg to mirror: `resolveRunning: () => RunningGbrainVersion | undefined` (`:82-83`) → `pinValid`.
  - The 4-leg derivation: `deriveServingCoverage` / `isDegradedCoverage` (`servingContextLoader.ts:95`, `:184`) — ANDs `cleanForServing` + `coverageComplete` + `pinValid` + `oracleBuildOk`. The loader frames `oracleBuildOk` as "the rebuild-oracle build status — resolved where the running version / oracle live (boot)" (`servingContextLoader.ts:59-60`).
  - The heavy producer (built, UNWIRED, real I/O — NOT touched here): `rebuildIndexFromMarkdown` (`packages/knowledge/src/gbrain/rebuild.ts:104`) — the gbrain import-into-scratch; its real-I/O binding is owner-gated.
  - Runbook precondition (b): `docs/runbooks/copilot-propose-go-live.md`.

## Acceptance criteria (what "done" means)
- [ ] `ServingCoverageReaderDeps` gains an OPTIONAL `resolveOracleBuild?: () => boolean` (a boot-resolved rebuild-oracle build-status accessor, mirroring `resolveRunning`). The reader sources `oracleBuildOk: deps.resolveOracleBuild?.() ?? false` — **unbound ⇒ `false`** (byte-equivalent to today's hardwired false).
- [ ] Bound ⇒ `oracleBuildOk` carries the resolver's boolean (true ⇒ true; false ⇒ false).
- [ ] **Fail-closed (§12):** a THROWING `resolveOracleBuild` is caught by the reader's existing try/catch ⇒ ALL legs degrade (`parity: undefined, pinValid: false, oracleBuildOk: false`) — a probe fault never crosses the boundary, never a false green.
- [ ] **Wiring-complete milestone:** with a clean revision-matched report (store bound) + a valid pin + `resolveOracleBuild()=>true`, `deriveServingCoverage` yields all 4 legs true ⇒ `isDegradedCoverage` **false** — proving the coverage gate is now fully wired (the last hardwired-false blocker removed). This is a unit proof with fakes; production stays dormant.
- [ ] **DORMANT / byte-equivalent:** boot leaves `resolveOracleBuild` **unbound** ⇒ `oracleBuildOk` false ⇒ every workspace still degrades; the shipped default is byte-equivalent. Boot is UNCHANGED (optional dep). No propose arming, no gbrain I/O.
- [ ] All tests pass; repo-wide `pnpm -w turbo run typecheck test` green; `/preflight` clean.
- [ ] **MANDATORY Step-8 dual review = security-reviewer + code-quality-reviewer** — this is the last coverage-gate leg (the trust-gate kill-switch substrate); making it bindable removes the final hardwired-false blocker.

## Wiring / entry point (Step 7.5)
`none new in production — boot leaves `resolveOracleBuild` UNBOUND` (exactly like B2's optional `store`), so `oracleBuildOk` stays false ⇒ degrade. The real `resolveOracleBuild` binding — sourcing the status from the gbrain import-into-scratch build-status producer (real gbrain I/O) — is **owner-gated**, deferred to the arming/provisioning slice. `createServingCoverageReader` gains the optional dep; the reader remains reachable from `boot.ts:934` (B4), now with the fourth leg bindable.

## Files expected to touch
**Modified:**
- `apps/worker/src/api/procedures/servingContextBootReaders.ts` — add `resolveOracleBuild?: () => boolean` to `ServingCoverageReaderDeps`; source `oracleBuildOk` from it (`?.() ?? false`); update the module header (the leg is now bindable; still dormant — boot leaves it unbound; the heavy producer is deferred/owner-gated).

**Test (new cases in the existing file):**
- `apps/worker/test/api/procedures/servingContextBootReaders.test.ts` — the cases below.

If implementation needs files beyond this list, **flag at Step 2.5** before GREEN.

## RED test outline (Step 2)
1. **`oracle_build_ok_unbound_stays_false`** — no `resolveOracleBuild` dep ⇒ `oracleBuildOk` false (+ the other legs unchanged). Asserts: byte-equivalent dormant default. Why: dormancy.
2. **`oracle_build_ok_bound_true`** — `resolveOracleBuild: () => true` ⇒ `oracleBuildOk` true. Asserts: the leg carries the real signal. Why: §6.
3. **`oracle_build_ok_bound_false`** — `resolveOracleBuild: () => false` ⇒ `oracleBuildOk` false. Asserts: an honest false degrades. Why: §6.
4. **`oracle_build_ok_throwing_fail_closed`** — `resolveOracleBuild` throws ⇒ the reader's catch returns ALL legs false. Asserts: a probe fault degrades everything, never a false green. Why: §12 fail-closed.
5. **`full_green_reachable_when_all_four_legs_true`** — clean revision-matched report (fake bound store) + valid pin + `resolveOracleBuild:()=>true` ⇒ `deriveServingCoverage` all-true ⇒ `isDegradedCoverage` false. Asserts: the coverage gate is now fully wired (the milestone — the gate CAN admit once all legs bound); production stays dormant (boot unbinds). Why: §6.

## Cross-doc invariant / frozen-contract impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** NONE (`ServingCoverageSources.oracleBuildOk` already exists; this only SOURCES it from a new optional dep).
- **New worker-internal dep** (`resolveOracleBuild?`) is a composition-layer seam, NOT an Appendix-A model — no cross-doc row.
- **Orchestrator doc rows to write hot:** none frozen. An **arch-note candidate** (`oracleBuildOk` now bindable; the gate fully wired, dormant) is a Step-9 flag.
- **Shared-contract subsystem-seam model touched?** No.

## Things to flag at Step 2.5
1. **`oracleBuildOk` semantics — boot-global liveness (A) vs per-revision corroboration (B).** My default vote: **A** — a boot-resolved `resolveOracleBuild?: () => boolean` (mirrors `resolveRunning`; matches the loader's framing `servingContextLoader.ts:59-60` + the owner's "build-status signal"), a coarse "the rebuild-oracle build ran / is-live OK" signal. Alternative B — a revision-scoped read-port (like the parity store) asserting *per-revision* corroboration, which would close the gap that `ParityReport.coverageComplete` can be green without the oracle ever running (`reconciler.ts:171-183`, the oracle term defaults true when absent). **RESOLUTION:** assign the per-revision-corroboration gap to the **RECONCILE-TRIGGER slice** (it must always supply a `rebuildOracle` so `coverageComplete` genuinely reflects corroboration); `oracleBuildOk` stays the boot-global build-liveness leg (A). Flag if you see a reason to prefer B.
2. **Fail-closed on a throwing `resolveOracleBuild` — all-legs (via the existing catch) vs just-`oracleBuildOk`.** My default vote: **all-legs** — keep `resolveOracleBuild()` inside the reader's existing try/catch so a probe fault degrades everything (the established fail-closed posture; stricter/safest).
3. **Boot binding — leave `resolveOracleBuild` UNBOUND here (dormant).** My default vote: **YES** — boot stays unchanged; the real binding (from the gbrain-import build-status producer = real gbrain I/O = owner-gated) is deferred to the arming/provisioning. Confirm boot is untouched.

## Dependencies + sequencing
- **Depends on:** B2 (`daf4fa1` — the reader + `ServingCoverageReaderDeps`) + `deriveServingCoverage`'s 4-leg AND (present).
- **Blocks:** the **reconcile-TRIGGER** slice + the **owner-gated arming** (which binds `resolveOracleBuild` + the store + `goLiveArmed`). After this leg the coverage gate is fully wired — the only remaining green-admission blocker is the arming (the HARD LINE).

## Estimated commit count
**1** — one atomic, **MANDATORY-DUAL-REVIEWED** commit. The last coverage-gate leg (trust-gate substrate) → its own commit, never bundled.

## Lessons-logged candidates anticipated
- **Convention candidate** — the last coverage-leg seam: replace a hardwired-false trust-gate leg with an OPTIONAL fail-closed resolver (unbound ⇒ false ⇒ degrade, mirroring the sibling boot-resolved leg); once bindable the AND-gate becomes reachable-green (a milestone unit test proves it) but the leg ships DORMANT (boot unbinds); the heavy status PRODUCER (real I/O) stays a deferred owner-gated build-time deliverable — the serve path takes no I/O.
- **Architecture-doc note candidate** — §6: `oracleBuildOk` is now a bindable boot-resolved leg (the last hardwired-false coverage leg); the serving-coverage gate is fully wired, dormant until the arming binds all deps.
- **Future TODO** — the gbrain import-into-scratch build-status PRODUCER (real gbrain I/O — owner-gated) that binds `resolveOracleBuild`; the reconcile-TRIGGER (per-revision `coverageComplete` corroboration); the arming.

## How to invoke
1. Read this brief end-to-end (esp. the HONEST SCOPE note + the Step-2.5 A-vs-B decision).
2. Run `/tdd rebuild_oracle_coverage_leg`.
3. Step 0 restate → Step 1 files → **Step 2.5 test-design write-up (magic-words) → orch16** → Steps 3-7 → **Step 8 MANDATORY dual review** → Step 9 categorized flags + ship-ask → orch16 (commit-message-first).
