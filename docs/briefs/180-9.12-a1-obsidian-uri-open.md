# /tdd brief — obsidian_uri_true_open

## Feature
The A1 fast-follow completing 9.12's "Open-in-Obsidian" intent: the `vault:open` OPEN branch opens the vault in **Obsidian via `obsidian://` URI** (not just the OS folder, A2's interim) — MAIN resolves the closed target→configured root, url-encodes it, and opens the `obsidian://` URI, with a URI-injection guard on the main-owned encoded path. Renderer stays PATH-BLIND (still sends the closed `VaultRepoTarget`; §5 preserved). Reveal stays `showInFolder` (A2).

## Use case + traceability
- **Task ID:** 9.12 (the A1 completion leg — 9.12r landed the A2 folder-open, `b95aa3cf`; 9.12 stays `[~]` until this true-Obsidian-open lands)
- **Architecture sections it implements:** `ARCHITECTURE.md §11` — **REQ-UX-003** (Open-in-Obsidian / reveal-in-vault, path-scoped; renderer requests open-by-target only).
- **Related context:**
  - 9.12r (`b95aa3cf`) reworked `vault:open`/`vault:reveal` to the closed-target (Option A) shape: renderer sends `VaultRepoTarget = "workspace"|"global"`, MAIN resolves target→root (`resolveRepoRoot`), A2 = `openPath(root)`. This slice upgrades the OPEN branch to the true `obsidian://` open. Renderer + the closed-target contract are UNCHANGED.
  - ⚠ The `obsidian://` path-to-a-vault-folder wire shape is vendor-uncertain — **verify via Context7 at Step 1** (resolve `obsidian` docs → the `obsidian://open` param for a vault/folder).

## Acceptance criteria (what "done" means)
- [ ] The `vault:open` OPEN branch opens the vault in Obsidian via the verified `obsidian://` URI, built from the MAIN-resolved root (target→root, url-encoded) — the renderer never supplies the path (stays path-blind, §5 preserved).
- [ ] A **URI-injection guard** on the main-owned encoded path — the root comes only from the trusted `vault-roots` (never renderer-supplied); a test pins no injection via the encoded value and that an unresolved/unknown target fails closed (no URI opened).
- [ ] **Graceful fallback to A2** (`openPath(root)` — open the folder) if the `obsidian://` open fails / is unsupported (Obsidian not installed) — never a hard error, never a hang.
- [ ] Reveal stays `showInFolder(root)` (unchanged). Labels: "Open in Obsidian" (A1) + "Reveal in Finder".
- [ ] security-reviewer=invariant re-certs (no URI injection · path main-resolved · renderer path-blind · unknown target fails closed).
- [ ] Component/main tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
The `vault:open` preload channel's OPEN branch (`main/open-in-vault.ts` `performVaultAction`) now builds + opens the `obsidian://` URI (via `shell.openExternal` or the equivalent seam) for the resolved root; reachable from the existing Projects repo-header "Open" affordance (landed 9.12r). Renderer unchanged. Confirm the `openExternal` seam binding.

## Files expected to touch
**Modified:**
- `apps/desktop/main/open-in-vault.ts` — the OPEN branch builds the `obsidian://` URI from the resolved root + the injection guard + the A2 fallback.
- `apps/desktop/main/ipc.ts` / `preload/bridge.ts` — only if a new `openExternal` seam/channel is needed (else reuse; flag at Step 2.5). Channel NAMES should stay unchanged (avoid inventory/snapshot churn).
- test(s) — `test/main/open-in-vault.test.ts` (extend).

⚠ Pure desktop territory (main/preload); renderer UNCHANGED. `git add <path>` only.

## RED test outline (Step 2)
1. **`open_builds_obsidian_uri_from_resolved_root`** — the OPEN branch opens `obsidian://…` with the MAIN-resolved (not renderer-supplied) url-encoded root. Why: REQ-UX-003 true-open + §5 path-blind.
2. **`uri_injection_guarded`** — the encoded path can't inject beyond the trusted root; an unresolved/unknown target opens NO URI (fail-closed). Why: §5 no-arbitrary-open.
3. **`falls_back_to_folder_open_on_failure`** — a failed/unsupported `obsidian://` open falls back to `openPath(root)` (A2), never a hard error/hang. Why: robustness.
4. **`reveal_unchanged_show_in_folder`** — reveal still `showInFolder(root)`. Why: A2 reveal unchanged.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (main/preload; `VaultRepoTarget` unchanged).
- **Orchestrator doc rows to write hot:** none (on landing, 9.12 → `[x]` — A1 completes it).
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **`obsidian://` wire shape (Context7-verify).** Default vote: `obsidian://open?path=<url-encoded absolute vault path>` — but VERIFY the exact param for opening a vault/folder via Context7 at Step 1; if a folder isn't directly openable, use `obsidian://open?vault=<name>` or flag + keep A2. Confirm after verification.
2. **Reuse vs new seam.** Default vote: change the `vault:open` OPEN branch behavior (target→root→`obsidian://`) — keep the channel NAME + closed-target contract unchanged (no inventory/snapshot churn); reuse `shell.openExternal` in main. Confirm no new preload channel.
3. **Fallback.** Default vote: graceful fallback to A2 (`openPath`) on `obsidian://` failure — never a hard error. Confirm.

## Dependencies + sequencing
- **Depends on:** 9.12r (`b95aa3cf` — the closed-target A2 shape).
- **Blocks:** 9.12 full tick (`[x]` on this landing).

## Estimated commit count
**1** — the OPEN-branch obsidian:// upgrade + guard + fallback; desktop main territory. **security-reviewer = invariant** (URI injection · main-resolved path · renderer path-blind · fail-closed unknown target). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the true Open-in-Obsidian opens `obsidian://` built from the MAIN-resolved root (renderer stays path-blind, target-only), url-encoded with an injection guard, and falls back to folder-open on failure — the vendor URI shape verified via Context7, never assumed."

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (Context7-verify the obsidian:// shape) is the gating question — resolve it at Step 1 before RED.
2. Run `/tdd obsidian_uri_true_open`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
