# /tdd brief — raw_content_shape_fork_concept_sweep

## Feature
`24.19` hardened the raw-content-shape traversal in `gcl-projection.ts`; `24.32` then found an **independent, unfixed, dormant fork** of the same predicate in `proposeWindows.ts`, self-described as a "LOAD-BEARING leakage check." **Two reimplementations of one safety predicate is a pattern, not a coincidence.** Sweep the monorepo, by CONCEPT, for any other fork — and report the enumeration, not a verdict.

## Use case + traceability
- **Task ID:** `24.34`
- **Architecture sections it implements:** §16 (candidate-data gate) · Safety rule 4 (workspace isolation — the predicate's purpose)

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase** — its tasks originate from the 24.6 safety-assertion audit and its own Step-9 findings, which sweep every area.

- **Related context:** `IMPLEMENTATION_PLAN.md` task `24.34` (the lead's own question at `24.32`'s close) · `### 24.32` (the second instance, dormant, **NOT yet fixed — do not fix it here**) · contracts **`L64`** (enumerate the CONCEPT, never the known string forms) · contracts **`L39`** (a safety predicate lives ONCE; other packages RE-EXPORT) · contracts **`L138`** (a propagation gap expressed as duplicated CODE is worse than one expressed as duplicated prose).

## Acceptance criteria (what "done" means)
- [ ] ⭐ **The sweep is by CONCEPT, not by the known string forms.** ⛔ **`isRawContentShaped` / `carriesRawContent` / `payloadCarriesRawContent` are the names we ALREADY FOUND — searching only those can, by construction, only re-find what we have** (`L64`). **Enumerate what the predicate DOES** — a recursive walk over a candidate payload rejecting raw-content-shaped keys and/or multi-line/over-length string values — **and search for that behavior however it is spelled.**
- [ ] **Every candidate is CLASSIFIED, not just listed** — for each hit: *is this a fork of the predicate, a legitimate re-export, or an unrelated match?* ⭐ **`24.38`'s sweep is the standard to match: it reported all four narrowing sites WITH classifications, including two explicitly identified as NON-instances, which is what made its "no fifth instance" load-bearing rather than an assertion.** A bare "swept, none found" is not an acceptable result shape.
- [ ] **The search scope is STATED with the result** (`L100`) — which patterns, which paths, and **whether the identifier could be assembled at runtime.** ⚠ **A negative existence claim inherits its search's boundaries; the scope IS the evidence.**
- [ ] **Iterate until a pass ADDS NOTHING**, and **report the dry-iteration count** (`L64`). ⛔ **One string-shaped pass yielding a confident all-clear is the failure mode this task exists to avoid** — a wrong all-clear closes the question, which is worse than no sweep.
- [ ] **Findings spawn their OWN tasks — they are not fixed in this slice.** ⛔ **Do NOT fix `24.32`'s known fork here**; it is separately tracked and its fix is delete-and-reuse, not re-harden. **Flag each new instance at Step 9 with its reachability stated precisely.**
- [ ] ⚠ **Reachability qualifiers on any finding cite `24.29`'s repo-wide mechanical census, NOT a hand grep** — contracts **`L141`**: three hand-verified reachability calls on one module were wrong in one session, and the failures are biased toward over-claiming reachability.
- [ ] `/preflight` clean (this slice may add no production code at all — see below).

## Wiring / entry point (Step 7.5)
`none — this is an audit slice, not a build slice.` **Its deliverable is an enumeration with classifications, plus tasks filed for whatever it finds.** ⚠ **If the sweep finds zero additional forks, that is a legitimate and valuable result** — but it must arrive with its scope, its concept-keys, and its dry-iteration count, or it is indistinguishable from not having looked (`L100`).

## Files expected to touch
**New:**
- Possibly none in production code. If a durable artifact is warranted, the sweep's enumeration belongs in the Step-9 report and — if it merits permanence — a short note under `docs/audits/`. **Flag at Step 2.5 if you think it needs a file.**

**Modified:**
- Possibly none. ⛔ **Do not fix instances found; file them.**

## RED test outline (Step 2)
⚠ **This is an audit slice, so the usual RED-first shape may not apply — surface that at Step 2.5 rather than manufacturing a test to satisfy the form** (contracts `L102`: ask whether this is the KIND of thing a test can pin before promising a pin).

**If the sweep confirms the predicate should live ONCE and a consolidation is warranted, the pin is:**
1. **`raw_content_shape_predicate_lives_once`** — a DEFINITION census over `packages/` + `apps/` asserting exactly one definition, covering **both `function` AND `const` forms** (`L39`: an `export const` re-fork is the shape that actually happens, and a name-grep pins the NAME not the DEFENSE). ⚠ **Both-anchor the regex** — a prefix-anchored census accepts a superset (`L70`'s confirming instance). **Mutation-verify it** by reintroducing a fork and confirming RED.

**If the sweep finds nothing and no consolidation is warranted, there may be no test to write** — say so explicitly with reasoning; do not invent one.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** ⚠ **potentially several — every fork you find becomes a task I file.** Report them categorized; do not add them to the tracker yourself.
- **§2.5-seam (shared-contract) model touched?** <!-- spec-lint:mention --> No.

## Things to flag at Step 2.5
1. **What are your CONCEPT keys?** ⭐ **This is the whole slice and I want them before you run, not after.** My default vote — **use them as a starting set, not a complete one, and add your own:** *recursive walk over an unknown/candidate payload* · *rejection based on key NAME shape* · *rejection based on VALUE shape (multi-line, over-length)* · *a "leakage"/"raw content"/"sanitize" comment near a traversal* · *anything guarding a payload before it crosses a workspace or egress boundary*. ⛔ **If your key set is just the three known function names, the sweep will succeed and prove nothing.**
2. **Does a found fork get consolidated (delete-and-reuse per `L39`) or hardened in place?** My default vote: **neither, in this slice — FILE IT.** This is a sweep; consolidation decisions want their own slice with their own reachability analysis, exactly as `24.32` got. **But say at Step 2.5 whether you think any find is urgent enough to jump that queue** — a LIVE (non-dormant) fork of a rule-4 predicate would be, and that judgement is yours to raise.

## Dependencies + sequencing
- **Depends on:** `24.19` ✅ `4cef394f` (the hardening whose propagation this checks). `24.32` filed and dormant — **its existence is this task's premise, its fix is not this task.**
- **Blocks:** — (findings spawn their own tasks).

## Estimated commit count
**0–1.** Likely a Step-9 report and filed tasks with no code commit at all. **If it produces a `lives_once` census pin, that is 1.** ⛔ **Do not pad it into a fix slice.**

## Lessons-logged candidates anticipated
- **Convention candidate** — if the sweep finds a third instance, the lesson is about **propagation**, not about the predicate: `L138` already says a propagation gap expressed as duplicated CODE is worse than one expressed as duplicated prose, and a third instance would be strong evidence that the delete-and-reuse rule (`L39`) needs a mechanical backstop rather than a convention.
- **Future TODO** — each found fork.

## How to invoke
1. Read this brief + `IMPLEMENTATION_PLAN.md` tasks `24.34` and `24.32` + contracts `L64`, `L39`, `L138`.
2. Run `/tdd raw_content_shape_fork_concept_sweep`.
3. **Step 2.5 BEFORE searching** — I want your concept keys reviewed before the sweep runs, because a sweep with the wrong keys returns a confident wrong all-clear and closes the question.
