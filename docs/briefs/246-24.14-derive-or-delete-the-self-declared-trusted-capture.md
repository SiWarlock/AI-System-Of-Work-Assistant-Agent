# /tdd brief — derive_or_delete_the_self_declared_trusted_capture

## Feature

**Task 24.14.** `buildCaptureSource` grants `trustLevel: 'trusted'` — which makes a capture **eligible for confined auto-apply (§13.8)** — **purely because the caller supplied `kind: "coding_session"`.** This slice either derives that trust from something verifiable, or deletes the prototype.

⛔ **SAFETY RULE 6 (ING-7, untrusted-content tool-stripping).** `security-reviewer` is **mandatory**.

## Use case + traceability
- **Task ID:** 24.14 (providers-integrations)
- **Architecture sections it implements:** `ARCHITECTURE.md §13` (source extractors / ingestion) — the module self-describes as a **§13.6 prototype**, so §13 is its real anchor. ⚠ *(The file sits under `connectors/adapters/`, which is the Connector & Tool Gateways section's territory, but this slice implements nothing there — the anchor is §13.* <!-- spec-lint:mention --> *)*
- **Safety rule:** **6** — an agent consuming imported/untrusted content runs read-only and is rejected at job admission if it declares a mutating tool
- **Related context:** `IMPLEMENTATION_PLAN.md` `### 24.14` · `docs/audits/002-…` finding **F11**

## ⛛ PREMISES — VERIFIED vs UNVERIFIED (contracts L122)

**VERIFIED (audit AC-2b, at 99% surface coverage; re-verify anything you build on):**
1. `packages/integrations/src/connectors/adapters/capture-source.ts:92-101` — a `CodingSessionCapture` (`kind: "coding_session"`) is assigned `trustLevel: 'trusted'`; a `TelegramCapture` is `'untrusted'` **and sender-allowlisted** via `deps.isAllowedTelegramSender`.
2. ⭐ **The asymmetry IS the evidence: the untrusted leg has an authentication seam and the TRUSTED leg has none.** Trust is assigned from an untyped discriminant the caller supplies.
3. `grep` for `"coding_session"` / `buildCaptureSource` returns **only this file and its own test — zero production callers.** The module header self-describes as a *"Phase-13 §13.6 PROTOTYPE."*

**⛛ UNVERIFIED — establish before Step 1; these decide the fork:**
- **(U1)** ⭐ **Is §13.6 (coding-session capture) still a LIVE plan, or superseded?** Read its tracker entry and any successor. **This is the question the fork turns on and I have not answered it for you deliberately** — if the goal is dead, deleting is not a scope cut, it is removing a consumer with no producer.
- **(U2)** Is there any real or intended caller path — a git hook, a CLI, a worker composition site — even unbuilt? **If something is genuinely coming, "delete" is the wrong answer.**
- **(U3)** What would a "verifiable signal" actually look like here, given the module's own dependency shape? ⚠ **Do not design it in the abstract — establish what the caller could prove and what this module could check.**
- **(U4)** Does `trustLevel: 'trusted'` from this path actually reach §13.8's confined auto-apply today, or is that a documented intent with no wiring? ⛔ **This changes the severity, and the answer must come from the code, not from the comment.**

## The fork — named, NOT pre-ruled

**Option A — DERIVE IT.** Trust comes from something the caller cannot forge: a locally-minted non-forgeable token, or a caller-path invariant restricting construction to a genuinely hook-triggered internal path — **mirroring what the Telegram leg already does with its sender allowlist.**
**Option B — DELETE THE PROTOTYPE.** Zero callers, self-described prototype; if §13.6 is not live, ⭐ **deleting removes a fail-open waiting for a producer, and the project's own precedent (9.40) is *delete the mechanism, keep the goal as a tracked task*.**

⛔ **I am not pre-ruling this, and (U1) is why: the honest choice depends on whether the goal is alive, which I have not established.** ⭐ **If you choose B, the GOAL must survive as a tracked successor task naming the corrected shape (trust derived, not declared) — deleting the mechanism must not delete the requirement.** That is 9.40's ruling and it is binding precedent here.

## Acceptance criteria (what "done" means)

- [ ] `trustLevel: 'trusted'` is **either** derived from a signal the caller cannot forge, **or** the path that grants it no longer exists.
- [ ] ⛔ **If A: verified by CONSTRUCTING THE FORGERY** — a test that supplies `kind: "coding_session"` without the verifiable signal and asserts the capture is **not** trusted. **A test that only exercises the happy path cannot distinguish the fix from the defect.**
- [ ] **If A: non-vacuity** — a genuine capture *with* the signal still gets `trusted`. Both directions.
- [ ] **If B:** the successor task exists naming the corrected shape, and the deletion is complete (no orphaned types/tests/exports).
- [ ] The Telegram leg's behaviour is **unchanged** — it is already correct and is the pattern, not the problem.
- [ ] Repo-wide typecheck green across all 11 packages.

## Wiring / entry point (Step 7.5)

⚠ **There is no production caller today.** If A: declare `"none — this path is unwired; §13.6 is its consumer"` explicitly rather than leaving Step 7.5 blank. ⛔ **Do NOT wire a caller in this slice** — that arms an ingestion path inside a hardening fix.

## Files expected to touch
`packages/integrations/src/connectors/adapters/capture-source.ts` · its test · possibly a small token/verification helper in the same package.
⛔ **NOT** `apps/worker/**`, `apps/desktop/**`, `packages/knowledge/**` — worker is on 24.17 and knowledge on 24.12 concurrently. **A cross-area need is a Step-2.5 finding, not an edit.**

## RED test outline (Step 2)
1. **The forgery test** (option A) — declared `coding_session` without the signal ⇒ not trusted. **RED first.**
2. Non-vacuity partner — with the signal ⇒ trusted.
3. Telegram leg unchanged — its existing tests stay green unmodified.
4. **Mutation-verify test 1** by removing the new check and confirming it goes green.
*(Option B: the RED/GREEN shape is different — say so at Step 2.5 and I will re-scope rather than have you manufacture tests for a deletion.)*

## Things to flag at Step 2.5
- **(U1)–(U4) with the `file:line` that settles each**, and **A or B with your reasoning.**
- The coverage map: each acceptance bullet → its covering test or a `not-tested-because:`.
- ⭐ **Anything above the code contradicts.** Four times this round an implementer has falsified a recorded premise from source — **including twice in briefs I wrote.**
- ⛔ **STOP AND SEND before your first production edit.** Read as much as you need first; the reading is not the building. *(Wording tightened after a sibling slice's Step-2.5 skip that my own brief's phrasing invited.)*

## Dependencies + sequencing
**Depends:** none. **Blocks:** any production wiring of the §13.6 capture path.
⚠ **Concurrent:** worker on `24.17` (`apps/worker` + `packages/workflows`), knowledge on `24.12` (`packages/knowledge`). **Your territory is `packages/integrations`.**

## Estimated commit count
**1.**

## How to invoke
`/tdd docs/briefs/246-24.14-derive-or-delete-the-self-declared-trusted-capture.md`
