# /tdd brief — refused_signal_observability

## Feature

`persistDenialAudit` refuses a signal failing `isRedactionSafe` and **returns silently** — no log, no counter, no health item. **A refused audit and an audit that was never produced are byte-identical to every observer.** Emit an observable, redaction-safe refusal signal, mirroring the in-repo precedent rather than inventing one.

## Use case + traceability

- **Task ID:** `### 24.53`
- **Architecture sections it implements:** `ARCHITECTURE.md §16` · safety rule 7
- **Related context:** `24.33` (placed the gate) · `24.44` (made it reachable from a second path) · `### 24.55` (the do-not-delete note you just landed at this exact call site) · `### 24.62` (the *other* channel — filed, not yours) · `L106` · `L39`

> ⛔ **Widens phase scope because** `### 24.53` declares `Spec: §16`, not in Phase 24's declared anchor set. Declared, not resolved by editing it — systemic version at `### 24.54`.

## Premises

1. **The gate refuses silently** — `packages/knowledge/src/gcl/projection.ts`, `persistDenialAudit` (~`:61`). A refusal returns with no observable trace.
2. ⭐ **The precedent exists in-repo and you should mirror it, not invent:** `apps/worker/src/boot.ts:584-592`'s `createAuditPersistPort` logs the refusal **event-name-only**: `` `[copilot.denial-audit] REFUSED to persist — signal for event="${signal.event}" failed the redaction-safety gate (9.33)` ``. **That is the shape.** (`L39`.)
3. ⛔ **You have just been in this file.** `24.55`'s do-not-delete note is at this call site and its two filed reasons were corrected by your own reviewers — **the note now says this is the only thing that would refuse a signal reaching this function.** ⇒ **this slice makes that refusal observable, which is the natural completion of the note.**
4. ⚠ **`### 24.62` is a DIFFERENT defect at the same function and is NOT yours:** `persistDenialAudit` takes **two** data channels and gates one — `workspaceId` rides beside the signal unscanned. **Filed, fenced as a port-binding precondition. Do not fix it here and do not let this slice's log line touch `workspaceId`.**

## ⛔ THE HARD CONSTRAINT — this is a rule-7 slice and the obvious implementation is the vulnerability

⛔⛔ **The refusal exists BECAUSE the content was unsafe. Logging the content would be the exfiltration route the gate prevents.** ⇒ **event-name / code ONLY. Never the signal's fields, never `refs`, never `beforeSummary`/`afterSummary`, never the workspace id.**

⭐ **And note the trap in the precedent itself:** `boot.ts:597-599`'s *second* log line — the append-failure one, not the refusal one — carries a comment claiming *"redaction-safe (event/code only)"* **while interpolating a raw `workspaceId`** (that is `### 24.62`). ⇒ ⛔ **mirror `:588-591` (the refusal log), NOT `:597-599`.** ⚠ **The file contains both a correct example and a counter-example of the same claim, four lines apart.**

## Acceptance criteria

- [ ] A refusal emits an observable signal — log line, counter, or health item (Q1 decides which).
- [ ] ⛔ **The emission carries event-name / code ONLY.** A test asserts that a signal whose unsafe field is a distinctive credential-shaped string produces an emission **not containing that string**. ⭐ **That is the load-bearing test; write it first.**
- [ ] **A refused audit is distinguishable from one never produced** — assert both cases and show they differ observably. ⚠ **MOVE THE STATE**: asserting the function returned is not evidence anything was emitted.
- [ ] **The refusal path still never throws and still refuses** — `24.33`'s invariant is untouched; the denial must never depend on the audit write.
- [ ] **`workspaceId` appears nowhere in the emission** (`### 24.62`'s defect must not arrive through this fix).
- [ ] Nothing outside `packages/knowledge/`; `/preflight` clean, per-package scoped.

## Wiring / entry point (Step 7.5)

**`persistDenialAudit` is reached from `admitProjection` and `serveProjection`** (`projection.ts:110`, `:153` — its only two call sites). ⚠ **`serveProjection`'s production caller has zero production callers, so the read path is not live** — **describe this as an observability gap on a reachable-but-dormant path, not a live incident.**

## Files expected to touch

**Modified:** `packages/knowledge/src/gcl/projection.ts` · its test file.
⛔ **Do NOT touch:** `apps/worker/**` (the precedent is read-only reference) · `packages/policy/**`.

## RED test outline (Step 2)

1. **`refused_signal_emits_an_observable_refusal`** — drive a signal that fails `isRedactionSafe`.
   - Asserts: an emission occurs; a never-produced audit produces none
2. ⛔ **`refusal_emission_contains_no_unsafe_field_content`** — the unsafe field is a distinctive credential-shaped string.
   - Asserts: the emission does **not** contain it. **Write this one first — it is the rule-7 pin, and the obvious implementation fails it.**
3. **`refusal_still_returns_and_never_throws`** — `24.33`'s invariant, regression-pinned.
4. **`allow_side_control`** — a safe signal still persists and emits no refusal (`L80`).

## Cross-doc invariant impact

- **Model field changes:** none expected. ⚠ **If you add a `FailureClass`/health-item shape, that is a frozen-taxonomy question** — ⛔ **flag at Step 9, do not expand it here** (`contracts L18` point 2 requires an empirical consumer survey first).
- **§2.5-seam model touched?** <!-- spec-lint:mention --> No, unless Q1 chooses a health item.

## Things to flag at Step 2.5

1. ⭐ **Log line, counter, or health item?** **My default vote: a log line mirroring `boot.ts:588-591` exactly** — `L39`, it is the established shape for this exact event, and it needs no taxonomy change. ⚠ **A health item would be more durable but drags in `FailureClass` and the dedupe-key identity question `24.58`/`24.60` just spent a slice on.** **Argue for it if you think durability matters more than scope here.**
2. **Does the emission need the denial's `code`?** The precedent carries `signal.event` only. **My default vote: event + `denialCode` if and only if `denialCode` is a closed enum member** (it is) — a closed code is not content. ⛔ **Nothing else.**
3. ⚠ **Is a log line observable enough to satisfy "distinguishable"?** If nothing consumes stdout in the deployed worker, a log is a *technically* observable emission nobody sees. **My default vote: accept it for parity with the precedent, and say plainly in-file that it is parity, not a claim that anyone is watching** (`L82`). ⛔ **Do not assert it "surfaces to the operator" unless you can name the consumer.**

## Dependencies + sequencing

- **Depends on:** `24.33` ✅ · `24.45` ✅ (providers) · `24.55`'s note ✅ (yours)
- **Blocks:** — ⚠ **Related but separate: `### 24.62`. Do not merge them.**

## Estimated commit count

**1.** Safety-adjacent (rule 7) ⇒ **own commit, not bundled.**

## Lessons-logged candidates anticipated

- **Convention candidate** — *a fail-closed gate that refuses silently makes the refusal and the absence indistinguishable; a gate's refusal is itself an event worth emitting* — and its emission must be safe **because** the thing it refused was not.
- **Convention candidate** — *when mirroring an in-repo precedent, check whether the file also contains a counter-example* — here the correct refusal log sits four lines from a log whose comment claims a property it violates.

## How to invoke

1. **Read the HARD CONSTRAINT section** — the obvious implementation is the vulnerability.
2. **Run `/tdd refused_signal_observability`.**
3. **Step 2.5** — Q1 and Q3; Q3 is the honesty question and I'd rather have a plain answer than an optimistic one.
4. **Step 9** — ⛔ safety rule 7 ⇒ **routes to the LEAD as well as to me** (all seven rules now auto-route, per the ruling landed today).
