# /tdd brief — retry_audit_record_mismatch_measurement

## Feature

**EXECUTE** tracker `### 24.76`: determine whether a retry of a post-commit-faulted `applyPlan` can record an
`AuditRecord` that **misdescribes the mutation it records** — passing compare-revision against the NEW head,
diffing to zero changes, and writing `afterSummary: 0 changes` against a post-mutation base.

⛔ **THIS IS A MEASUREMENT SLICE, NOT A BUILD SLICE.** The deliverable is a **call-path verdict with its
method and its boundary** — reproduced or shown unreachable. **No production code changes.** If it
reproduces, the FIX is a separate slice that splits by area, producer-first (lead ruling, this round).

## Use case + traceability

- **Task ID:** `24.76` (shared task `#64`)
- **Architecture sections it implements:** `ARCHITECTURE.md §6`, `§16`

> ⚠ **This brief widens phase scope because** the behavior under measurement — the never-throw promise and the
> post-commit audit write — is specified in **`§16`**, which is **not** in Phase 24's declared anchor set
> (`§13 §19.11 §3 §4 §6`). The tracker task `### 24.76` itself cites `§6, §16`, so the citation is correct and
> the declared set is the thing that does not cover it.
> ⛔ **I did NOT resolve this by adding `§16` to Phase 24's anchor set** — that is forbidden (an anchor set that
> grew because a linter demanded it is worthless), and it is the exact resolution the lead ruled against on
> `### 24.54`. ⭐ **This brief is therefore a fresh INSTANCE of `### 24.54`** (*"Phase 24's anchor set does not
> describe Phase 24"*) — recorded here rather than silently escaped, because the escape hatch and the evidence
> for the underlying defect are the same act.
- **Safety:** ⚠ **rule-1-adjacent — audit-trail integrity.** Step-9 flags on this route to the lead (ALL SEVEN rule auto-route).
- **Blocks:** `### 24.72`'s disposition — its severity is not settled until this returns.
- **Related context:** `### 24.72` (the report-inversion defect this was split out of), `docs/sessions/167-*.md`,
  `packages/contracts/LESSONS.md` `L152` / `L154` / `L100`.

⛔ **WHY THIS OUTRANKS `24.72`, and it governs how you report:** *a missing audit row is a known gap and gets
investigated; a WRONG one is believed.* It converts audit-trail integrity from *"absent for this event"* into
*"present and false"* — the one state that DEFEATS the audit trail as evidence rather than merely lacking it.

## Premises — EVERY ONE IS CHECKABLE, AND EVERY ONE IS A HYPOTHESIS

⛔ **Contradicting any of these from the source is doing the job right, not friction.** `24.72`'s own headline
died because a premise went unchecked. Falsify freely and say so at Step 2.5.

| # | Premise | Anchor (verify at source) |
|---|---|---|
| P1 | `applyPlan` contains **no `try` anywhere**; post-commit awaits (`deps.now()`, `deps.audit.append`, `deps.revisions.record`) can reject after the Markdown mutation is durable. | `packages/knowledge/src/knowledge-writer/writer.ts:228-234` |
| P2 | `createCommitActivity` wraps the call and folds ANY throw to a typed `commit_failed`. | `packages/workflows/src/activities/commitKnowledge.ts` (docblock cited at `writer.ts:239-242`) |
| P3 | Retry re-entry reads `deps.revisions.getByIdempotencyKey` **first**; if the revision record never landed, this is still empty. | `writer.ts:336` |
| P4 | Compare-revision runs against the **live on-disk state**. | `writer.ts:353-356` |
| P5 | `diffChanges(snapshot, projected)` computes the change set that feeds the audit row. | `writer.ts:366`, `writer.ts:693` |
| P6 | `afterSummary` is built from `changes.length`. | `writer.ts:447` |
| P7 | `audit.append` (`writer.ts:453`) precedes `revisions.record` (`writer.ts:466`) — so a fault at `:466` leaves an audit row written but no revision record. | `writer.ts:453`, `writer.ts:466` |
| P8 | ⭐ **The live-head-resolver path resolves `expectedBaseRevision` to the head AT COMMIT TIME, which the code's own docblock says "makes the whole-vault compare a NO-OP."** | `writer.ts:537-543` |

⭐ **P8 IS THE ONE THAT MATTERS.** If the whole-vault compare is a documented no-op on these paths, then step 2
of the scenario is **not merely plausible — it is the designed behavior**, and the question collapses to whether
steps 3–4 follow. ⛔ **Verify it; do not inherit it from this table.**

## The composition set — DERIVED BY PROPERTY, and here is the method so you can re-run it

⛔ **DO NOT derive this set by grepping `workspacePathCheck`.** That is a **spelling**, and "supplies
`workspacePathCheck`" is not the same property as "resolves a live head" (measurement mechanism 5 —
population-by-spelling). I made that mistake first and withdrew it.

**The correct property:** a composition that passes `readVaultHeadRevision` in `expectedBaseRevision` position.
Derived at 11:52am CDT, HEAD `297b7379`, method `grep -rn "readVaultHeadRevision" --include="*.ts" . > f` then
read `f` (redirect-then-measure; see the trap below):

- `apps/worker/src/composition/semanticApprovalDispatch.ts:97` — `expectedBaseRevision: () => readVaultHeadRevision(deps.vault)`
- `apps/worker/src/composition/buildActivities.ts:1109` — `expectedBaseRevision: () => readVaultHeadRevision(backends.vault)`

⚠ **A third site consumes the same function but is NOT a commit path** —
`apps/worker/src/api/procedures/servingContextBootReaders.ts:70` (a serving read). **Classify it explicitly in
your report rather than silently excluding it**; "I excluded it because it's a read path" is a finding, "it
isn't in my list" is a boundary you didn't state.

⛔⛔ **TOOLING TRAP, MEASURED THIS SESSION — DO NOT USE `codegraph_callers` AS YOUR CENSUS HERE.**
`codegraph_callers("readVaultHeadRevision")` returned **"No callers found"**. There are **two production call
sites** (above). The call sites are in **callback position** (`() => readVaultHeadRevision(...)`), and the index
misses them. ⭐ **A false negative closes a question with a ✅ next to it** — if you had trusted it you would
have reported "no live-head-resolver compositions exist" and closed `24.76` as unreachable, which is the exact
wrong answer in the exact safe-looking direction. Corroborate any census with a redirected `grep` + a
non-vacuity control.

## Acceptance criteria (what "done" means)

- [ ] The scenario is **EXECUTED** — reproduced or shown unreachable — **on both** commit-path compositions above.
- [ ] The **retry trigger is established at source**: what actually re-invokes `applyPlan` with the same
      `idempotencyKey` after a `commit_failed`, cited `file:line`. ⛔ If nothing does, **that is the finding** —
      and it makes the scenario unreachable *for that reason*, which must be stated, not left implicit.
- [ ] The **worker-side head-resolver binding is established AT SOURCE**, not inferred from the knowledge side
      (lead condition 2).
- [ ] The report **names which compositions were checked AND which were not** (lead condition 3). ⭐ *A
      call-path claim with an unnamed boundary is the thing this task is testing for.*
- [ ] If **reproduced**: the concrete sequence is stated with the fault-injection point, and the resulting
      `AuditRecord`'s `afterSummary` value is shown against the true mutation.
- [ ] If **unreachable**: ⛔ **the same evidentiary bar as "reproduced"** — which specific step breaks the chain,
      at which `file:line`, and what would re-open it. **"I looked and didn't find a path" is UNVERIFIED, not
      REFUTED** (`L152`). This is the safe direction and therefore the under-checked one (`#43` face 2).
- [ ] Every census in the report states its **method** and its **non-vacuity control**.
- [ ] **No production source file is modified.**

## Wiring / entry point (Step 7.5)

**none — this slice changes no production code.** The consumer slice for any fix is **`### 24.76`'s own fix leg,
which will be filed as a tracked task in this round if and only if the scenario reproduces** (per the brief
template's `L106` obligation: a "none" answer must name a real tracked task, and I will file it on your Step-9
report rather than pre-filing a task for a defect that may not exist).

The **entry points under measurement** (not modified) are the two commit-path compositions above, reaching
`applyPlan` via `createCommitActivity`.

## Files expected to touch

**New:** none in production.
**Modified:** none in production.

**Read (at minimum):** `packages/knowledge/src/knowledge-writer/writer.ts` ·
`packages/workflows/src/activities/commitKnowledge.ts` ·
`apps/worker/src/composition/semanticApprovalDispatch.ts` · `apps/worker/src/composition/buildActivities.ts` ·
whatever the retry-trigger trace pulls in.

## RED test outline (Step 2) — MEASUREMENT POSTURE

⛔ **Deliberately zero committed tests. This is the `not-tested-because` for every acceptance bullet above:**
*the slice's output is a call-path verdict, and its acceptance bullets pin the QUALITY OF THE EVIDENCE (method
stated, control stated, boundary named) rather than a runtime behavior.* A committed test here would assert
either a defect that may not exist or a fix this slice is forbidden to make.

⚠ **This is exactly the mapping `spec-lint` flagged as an advisory (8 behavior bullets vs 0 RED tests) — it is
answered, not ignored.** If Step-2.5 question 1 resolves to *harness permitted*, the reproduction harness is
**throwaway, built in a scratch worktree, and never committed** — so the count stays 0 by design.
⭐ **If the scenario REPRODUCES, the reproduction test belongs to the FIX slice, where it will be a genuine RED.**

## Things to flag at Step 2.5

1. ⛔⛔ **THE LOAD-BEARING ONE — "READ-ONLY" vs "EXECUTED", and I have raised it with the lead in parallel.**
   The lead's dispatch condition says **READ-ONLY, no code.** `24.76`'s Done-when says the scenario must be
   **EXECUTED**. ⭐ **These can conflict:** if you may not write even a throwaway harness, the strongest
   available method is static call-path reading — and the most likely output of static reading alone is
   *"unreachable"*, which is **the safe direction, reached by the weakest method**. That is precisely the
   failure mode `24.76` exists to prevent. **My default vote: "read-only" means NO PRODUCTION CODE CHANGES
   and nothing committed from this slice — a throwaway harness to actually execute the scenario is PERMITTED
   and preferred, built in a scratch worktree** (`git worktree add --detach HEAD`, per the template's
   worktree-not-revert pitfall) **so this checkout is never dirtied.** ⛔ **Do not proceed past Step 2.5 on
   this question without an explicit ruling** — I will relay the lead's answer. If the ruling is
   static-analysis-only, then **say so in the verdict**, because it bounds the strength of an "unreachable."
2. **Does the retry even exist?** Trace what re-invokes `applyPlan` after `commit_failed` — a Temporal retry
   policy, an explicit redrive, or nothing. My default vote: **establish it at source before modelling the
   scenario**; if no retry path exists the scenario is unreachable *for that reason*, and that is a
   materially different (and weaker-to-defend) answer than "the code guards against it."
3. **Is `getByIdempotencyKey` genuinely empty on re-entry?** P3/P7 imply a fault at `writer.ts:466` leaves an
   **audit row written but no revision record** — so a retry could find the idempotency lookup empty while an
   audit row for the first attempt already exists. My default vote: **treat "how many audit rows exist after
   the retry" as part of the verdict**, not just their content — two rows describing one mutation is its own
   defect shape.
4. **Scope discipline on `servingContextBootReaders.ts`.** My default vote: **classify and exclude with a
   stated reason** (read path, not a commit path) rather than silently omitting.

## Dependencies + sequencing

- **Depends on:** — (unblocked; this is the round's first slice)
- **Blocks:** `### 24.72`'s disposition; any fix leg for `24.76`.

## Estimated commit count

**0 production commits.** The output is a Step-9 report. If a session doc is written it rides `/session-end` as
usual. ⛔ **Do not bundle** — this is a safety-adjacent measurement and its whole value is an unmuddied verdict.

## Lessons-logged candidates anticipated

- **Convention candidate** — *redirect to a file, then measure the file* (adopted this round as a standing rule
  after a piped `git log | wc -l` under-reported by 6 commits).
- **Convention candidate** — a code-intelligence index can return a confident **"No callers found"** for a
  symbol with live callback-position call sites; corroborate every census.
- **Architecture-doc note candidate** — if P8 holds, `§16`/`§6` should state that the commit-on-approval path's
  whole-vault compare is a deliberate no-op and what compensates for it.

## How to invoke

1. Read this brief end-to-end, including the premise table and the tooling trap.
2. ⛔ **Answer Step-2.5 question 1 FIRST and wait for the ruling** — it determines your method.
3. Run `/tdd retry_audit_record_mismatch_measurement` (measurement posture: the "RED" is the reproduction
   attempt; there is no GREEN implementation in this slice).
4. **Step 9** — report the verdict, the method, the compositions covered, the compositions NOT covered, and
   every census's control. Safety-adjacent ⇒ this routes to the lead.
