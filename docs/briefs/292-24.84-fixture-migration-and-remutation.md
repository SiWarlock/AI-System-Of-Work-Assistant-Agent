# /tdd brief — workspace_id_fixture_migration

## Feature

Re-base the worker test fixtures whose workspace ids the post-`### 24.84` shape rejects, **and RE-MUTATE every
safety pin that moves**, so contract's brand can land with **zero red window**.

⛔ **YOU LAND FIRST. Contract is holding a 17-path slice on this, and knowledge holds behind you.**

## Use case + traceability

- **Task ID:** `### 24.84` (fixture-migration leg) · ⚠ related to `### 24.75` but **not** it — that one is the
  exempt-id VALUE pin and stays queued.
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- **Safety:** ⛔ **rule 4 — six of the seven pins you touch are WS-8 workspace-isolation pins.** Step-9 to me AND `team-lead`.
- **Depends:** — · **Blocks:** ⛔ **contract's `### 24.84` landing, and knowledge behind it.**

> ⚠ **Widens phase scope because** `§5`/`§16` are outside Phase 24's declared set (`§13 §19.11 §3 §4 §6`).
> ⛔ Not resolved by adding them (`### 24.54`).

## ⛔ THE SITUATION — measured by me, at HEAD, with contract's WIP live in the tree

The owner ruled: **write path enforces the shape AND the read-path pattern STAYS.** ⇒ any stored/fixture
workspace id that fails the shape is now **schema-rejected before the visibility stage.** Measured:

```
ws_employer  REJECT      ws-employer   ACCEPT
ws_other     REJECT      employer-work ACCEPT
ws_a / ws_b  REJECT
```
**8 reds in the tree today; 7 are yours or knowledge's, not contract's:**
- `apps/worker/test/api/procedures/queries.test.ts` — **6**, all `globalDrillDown` WS-8 pins
- `apps/worker/test/api/procedures/storeBackedWorkspacePosture.test.ts` — **1**
- `packages/knowledge/test/gcl-projection.test.ts` — **1** (knowledge's, NOT yours)
⚠ **`main-bundle-resolution.test.ts` also appears failing — that is `### 24.25`'s `L83` concurrent-WIP signature
in a dirty tree, NOT this. Do not chase it.**

⭐ **THESE ARE FIXTURE RE-BASES, NOT REGRESSIONS.** The behaviour is correct; the fixtures use ids the new shape
rejects. **And `ws-employer` is valid under BOTH the loose brand (today) and the tight one (after contract),
which is why you can land first and stay green in both states.**

## ⛔⛔ THE BINDING CONSTRAINT — RE-MUTATION. THIS IS THE SLICE.

**contract's security review, adopted by the lead as MANDATORY not advisory:**
> Re-basing is *probably* safe because these pins turn on id **MISMATCH**, not **shape** — ⛔ **but per `L79`,
> mutation-verification DOES NOT SURVIVE A NEW CONJUNCT. Each re-based pin must be RE-MUTATED AFTER re-basing.**

⇒ ⛔⛔ **THE FAILURE MODE, AND IT IS INVISIBLE FROM A GREEN SUITE: a re-based pin can go GREEN by
SHORT-CIRCUITING AT THE PATTERN CHECK instead of reaching the visibility gate it exists to exercise.** **Green is
exactly what that looks like.** ⇒ ***a pin that passes for the wrong reason is worse than one that fails***, and
these are the pins that assert *"IGNORES a renderer-SPOOFED visibility level"* and *"FAILS CLOSED on mixed levels."*

**Per pin: break the thing it exists to catch, confirm it REDS, restore, confirm GREEN. Report the count and the
observed states.** ⛔ **A pin you cannot make red is a finding, not a pass.**

## Acceptance criteria

- [ ] All 7 in-scope fixtures re-based to shape-valid ids; **`packages/knowledge/**` untouched** (knowledge's).
- [ ] ⛔ **EVERY re-based pin RE-MUTATED after re-basing, with its observed red/green states reported per pin.**
      **7 mutations, 7 results — not a summary.**
- [ ] ⛔ **Any pin that cannot be made red is reported as a FINDING**, not silently accepted.
- [ ] **Green in BOTH states, and say which you measured:** with contract's WIP present (tight brand, the tree
      today) **and** against HEAD contracts (loose brand). ⚠ **Use a throwaway worktree for the second — do NOT
      revert contract's files in this tree.**
- [ ] **Ids chosen preserve each test's MEANING** — a test turning on *foreign vs own* keeps two distinct ids
      that are still distinct; ⛔ a re-base that collapses two ids into one silently vacates the pin.
- [ ] `/preflight` scoped honestly; `--force` with `0 cached` reported; **non-`npx` binaries only.**

## Wiring / entry point (Step 7.5)

**None — test fixtures only.** No production surface, no new export.

## Files expected to touch

**Modified:** `apps/worker/test/api/procedures/queries.test.ts` · `apps/worker/test/api/procedures/storeBackedWorkspacePosture.test.ts`.
⛔ **NOT touched:** `packages/knowledge/**` · `packages/contracts/**` · `packages/policy/**` (providers is live there) · any production file.
⚠ **If a re-base requires a production change, STOP and flag** — that is a different finding and a different slice.

## RED test outline (Step 2)

⚠ **The subject is fixtures, so the mutation IS the red.** Substitute discipline:

1. **Per-pin mutation, before and after re-basing where feasible** — establish each pin catches its target now,
   re-base, establish it still does. ⭐ **The "after" is the one that matters; the "before" tells you whether the
   pin was ever discriminating.**
2. **A control that the mutation was OBSERVED** — break something the suite demonstrably catches, watch it red,
   then apply the real mutation. **A mutation that changes nothing is indistinguishable from one never applied.**
3. **A meaning-preservation check on any test with two ids** — assert they remain distinct after re-basing.

## Cross-doc invariant impact

**None** — no model, no schema, no Appendix-A row.

## Things to flag at Step 2.5

1. ⛔⛔ **Your per-pin mutation plan. This is the review surface and I will not approve a summary.** Seven pins,
   seven planned mutations, and what each is expected to red on.
2. **Do any of the 7 pins turn on SHAPE rather than mismatch?** My default vote: **assume none do, verify each** —
   contract's analysis says mismatch, and it is exactly the assumption `L79` says does not survive a new conjunct.
3. **Is `storeBackedWorkspacePosture`'s single red the same class?** My default vote: **check it separately** —
   its name says *foreign readback fails closed*, which is a mismatch pin, but one test is cheap to verify and
   expensive to assume.
4. **Are there fixtures that will break but are not in these two files?** My default vote: **measure the worker
   tree, report the count with its method.** ⚠ **Contract measured 14 newly-breaking ids repo-wide with 10 in
   `apps/desktop` — desktop is UNSTAFFED, so anything you find there is a finding for me, not work for you.**

## Dependencies + sequencing

- **Depends on:** nothing. ⛔ **Blocks: contract's landing, and knowledge behind it.** **Land first.**

## Estimated commit count

**1.** Single area, fixtures only. ⛔ **Safety pins ⇒ its own commit regardless of size.**

## Lessons-logged candidates anticipated

- **Convention candidate** — mutation-verification does not survive a new conjunct; a re-based pin is a new pin.
- **Convention candidate** — a schema tightening against persisted/fixture data is a MIGRATION, and the migration
  is the slice rather than a footnote on the tightening.

## How to invoke

1. Read this brief end-to-end. ⛔ **Per-pin mutation plan to me at Step 2.5 before any re-base.**
2. Run `/tdd workspace_id_fixture_migration`.
3. **Step 9** — rule 4 ⇒ routes to me AND `team-lead`.
