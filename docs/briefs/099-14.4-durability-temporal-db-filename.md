# /tdd brief — temporal_persistent_storage_db_filename (14.4-durability)

## Feature
Close the `/phase-exit 14` arch-drift **Finding**: the managed local Temporal dev-server currently spawns `temporal server start-dev` with **no `--db-filename`** (`temporal-supervisor.ts:64-66`) → an **in-memory** dev-server, which violates §13's explicit "persistent storage (`--db-filename` SQLite under app data, never in-memory)" and defeats **LIFE-3** (in-flight workflow state must survive a restart). Add `--db-filename <persistent path under userData>` (created-if-absent) so the managed Temporal persists. Desktop-track (worker-host); **ISOLATED, security-reviewer=invariant** (re-touch of the 14.4 supervisor + a filesystem-path leg). Small slice — **1 commit**. Build-substrate, NO hard line (still loopback-only, still mock-tested, still default-OFF byte-equivalent).

## Use case + traceability
- **Task ID:** 14.4-durability (the follow-up that unblocks `/phase-exit 14`)
- **Architecture sections it implements:** `ARCHITECTURE.md §13` (primary — V1-local topology: the local Temporal dev-server runs with persistent storage, `--db-filename` SQLite under app data, NEVER in-memory), `§19.1` (the phase — app-managed local Temporal supervision). Rationale: honors LIFE-3 (resume-after-restart — in-flight workflow state survives a Temporal restart), which the in-memory server defeats.
- **Origin:** `/phase-exit 14` arch-drift-auditor Finding (2026-07-15), `docs/audits/14-arch-drift.md`; lead-ruled FIX (not waive).
- **Related context (confirm at Step 1 — you own the supervisor):**
  - `apps/desktop/worker-host/temporal-supervisor.ts:64-66` — `temporalServerArgs(host, port)` returns `["server", "start-dev", "--ip", host, "--port", port, "--ui-ip", host]`. Add `--db-filename <path>`.
  - The persistent path per §13 ("under app data"): `<userData>/temporal/dev.db` (Electron `app.getPath("userData")`), the directory created-if-absent before spawn.
  - The supervisor is a PURE state machine (loopback-guarded, never-throws); the real `createTemporalSpawner` binds at boot. Keep the db path INJECTED (a param threaded from `worker-host/index.ts`), so the mock test asserts the args without touching the real fs/Electron.

## Acceptance criteria (what "done" means)
- [ ] `temporalServerArgs` includes `--db-filename <path>` where `<path>` is under the app userData dir (`<userData>/temporal/dev.db`), NEVER in-memory / NEVER `/tmp`.
- [ ] **Anti-regression (LOAD-BEARING):** a pin asserts the args ALWAYS carry `--db-filename` — a `start-dev` without `--db-filename` (the in-memory drift) is a failing assertion. The in-memory regression can never silently return.
- [ ] **Directory created-if-absent:** the real spawner ensures `<userData>/temporal/` exists (mkdir-recursive) before spawn — a missing dir doesn't fail the launch.
- [ ] **Path safety:** the db path is derived from the app userData dir (not caller/attacker-controlled); no traversal, no interpolation into a shell (still args-array).
- [ ] **MOCK discipline preserved (owner condition):** every test drives the mock — NO real Temporal server + NO real fs write in the suite (the dir-create + real spawn are integration-gated; the mock asserts the args + the mkdir call via an injected fs seam).
- [ ] **Still loopback-only + default-OFF byte-equivalent:** the existing loopback guard, `--ip`/`--ui-ip`, args-array/no-shell, `SOW_MANAGE_TEMPORAL === "true"` gate, and no-orphan dispose are all UNCHANGED (the existing 14.4 tests stay green).
- [ ] `/preflight` clean (desktop + repo-wide).

## Wiring / entry point (Step 7.5)
Same as 14.4 — the supervisor is boot-wired in `worker-host/index.ts` (env-gated). This slice threads the resolved `<userData>/temporal/dev.db` path into `temporalServerArgs` / the real spawner. **State: reachable via the worker-host boot path (production entry, env-gated); tested via the injected mock.** Confirm the path-injection seam at Step 1.

## Files expected to touch
**Modified:** `apps/desktop/worker-host/temporal-supervisor.ts` (`temporalServerArgs` +`--db-filename`; the real `createTemporalSpawner` resolves + mkdir-recursive the path via an injected fs seam), `apps/desktop/worker-host/index.ts` (resolve `<userData>/temporal/dev.db` + thread it), `apps/desktop/test/main/temporal-supervisor.test.ts` (the new pins). If beyond this, flag at Step 2.5.

## RED test outline (Step 2) — all mock/no-real-spawn
1. `args_include_db_filename_under_userdata` — `temporalServerArgs` includes `--db-filename <path>`; `<path>` is under the userData dir. (§13)
2. `args_never_in_memory_regression` — the args ALWAYS carry `--db-filename`; assert no `start-dev` form omits it (anti-regression pin). (§13 "never in-memory" / LIFE-3)
3. `spawner_creates_temporal_dir_if_absent` — the real spawner mkdir-recursive's `<userData>/temporal/` via an injected fs seam (mocked); no real fs write in-suite. 
4. `db_path_is_userdata_derived_not_attacker_controlled` — the path derives from the app userData dir (path-safe, no traversal).
5. `loopback_and_off_default_unchanged` — the existing loopback guard + `SOW_MANAGE_TEMPORAL` OFF byte-equivalent still hold (regression guard).

## Cross-doc invariant impact
- **Model:** none. No frozen-seam change. (The §13 anchor already mandates persistent storage — this makes the code conform; no ARCHITECTURE edit needed. If you want a one-line confirmation note I can add it, but the anchor already states it.)
- **Shared-contract seam touched?** No.

## Things to flag at Step 2.5
1. **Path-injection seam (LOAD-BEARING).** How does `<userData>/temporal/dev.db` reach `temporalServerArgs` / the spawner without the pure fn touching Electron? My default vote: resolve the path in `worker-host/index.ts` (which already has the Electron app context) and thread it as a param into `temporalServerArgs(host, port, dbPath)` + the real spawner mkdir's it via an injected fs seam; the mock test passes a fake path + asserts the arg. Confirm.
2. **Dir-create timing/seam.** mkdir-recursive in the real spawner (integration-gated), NOT in the pure args fn — so the suite never writes real fs. Confirm the injected fs seam.

## Dependencies + sequencing
- **Depends on:** 14.4 (`98696cea`, landed). Unblocks the `/phase-exit 14` re-run.
- **Blocks:** the Phase-14 checkbox (stays un-ticked until this lands + `/phase-exit 14` re-runs CLEAR).

## Estimated commit count
**1.** The `--db-filename` addition + path threading + mkdir seam + the anti-regression pins. **security-reviewer = invariant** (the persistent-storage §13 conformance + the path-safety leg + no-in-memory-regression). code-quality every-slice. **Build-substrate — NO hard line** (loopback-only, mock-tested, default-OFF byte-equivalent unchanged).

## Lessons-logged candidates anticipated
- **Convention candidate** — a supervised local dev-server persists to app-data (`--db-filename` under userData, created-if-absent, path-safe, never in-memory/`/tmp`) so its operational state survives restart (LIFE-3); pin the no-in-memory regression so the ephemeral default can never silently return.
- **Future-TODO (Carry-forward, per the lead — NOT this slice):** Temporal persistence is operational-truth → it falls under the §16 backup posture; the later §16/backup work must cover `<userData>/temporal/`.

## How to invoke
1. Continue the desktop-impl session (no `/session-start` unless swapped).
2. Read this brief (Q1 path-injection is load-bearing).
3. `/tdd temporal_persistent_storage_db_filename`.
4. Step 2.5 — ping the design answers. Don't GREEN until I sign off.
5. Step 9 — categorized flags. Commit body: reference the `/phase-exit 14` Finding it closes. After it lands → I RE-RUN `/phase-exit 14` (expected CLEAR).
