# /tdd brief — workspace_id_brand_enforced_at_create

## Feature
`### 24.84`'s **worker leg**: `parseCreateWorkspace` admits **any non-empty string** as a workspace `id` and never runs `WorkspaceIdSchema`. The shape is **DEFINED** (contracts leg, `25ae6c49`) and **NOT ENFORCED at create**. Route the create path through the brand — and, **in the same commit**, replace the `packages/policy` note whose stated ground this landing expires.

## Use case + traceability
- **Task ID:** `### 24.84` (worker leg)
- ⚠ **This slice EXPIRES a ground recorded on a task that is already DONE** (the `visibility.ts` note landed by `462d5ad9`). ⛔ **That task is deliberately NOT cited as a Task ID above** — `spec-lint` reads the Task ID line as *"the task this brief implements"* and correctly fails a brief pointing at a completed one. **The relationship is *"this landing invalidates a sentence that task wrote,"* not *"this brief implements that task"* — see Half B.** *(Caught by the pre-dispatch lint on first run; recorded because the distinction is real and the next author will hit it too.)*
- **Architecture sections it implements:** `ARCHITECTURE.md §5`, `§16`
- ⛔ **This brief widens phase scope because** `§5`/`§16` sit outside Phase 24's declared set (`§13 §19.11 §3 §4 §6`) while `### 24.84`'s own `Spec:` line cites them. Declared per `spec-lint` option (a). ⛔ **NOT resolved by widening the set** — standing lead ruling 2026-07-29; durable home `### 24.54`, enriched not duplicated.
- ⛔ **Safety: rule 7.** Step 9 auto-routes to the lead.
- **Related context:** `### 24.83` (the measurement) · `### 24.93` (`462d5ad9` — the note you are editing) · `### 24.102` (brief `296`, landing before this) · `### 24.106` (the migration, filed-not-built) · `### 24.100` (`makeId` casts — **NOT this slice**) · `L120` · `L195`

---

## ⛔⛔ THIS SLICE HAS TWO HALVES AND THEY LAND IN ONE COMMIT — THAT IS THE POINT, NOT A CONVENIENCE

**Half A — the code.** `apps/worker/src/api/procedures/onboarding.ts`, `parseCreateWorkspace`: run `WorkspaceIdSchema` on the `id`.
**Half B — the note.** `packages/policy/src/visibility.ts`, inside `denyDirectCrossWorkspaceRaw`: replace the block whose ground Half A expires, with **the verbatim text supplied below**.

⛔ **LEAD RULING (2026-08-18): THEY LAND IN THE SAME COMMIT.** ⭐ **The reason, so you can defend it: an expiry living in a commit message, a Step-9 flag, or someone's inbox is a claim IN FLIGHT; an expiry living in the note itself, in the same commit, is a claim AT REST.** ⇒ ***if they can land separately they will land separately, and the window between them is a landed task whose stated ground is false*** — the defect this round inherited twice. **Make the note edit part of this slice's definition of done, not its aftermath.**

### ⚠ Why you are editing another area's file, recorded so it is not mistaken for improvisation
**`packages/policy` is providers-integrations territory and that area is NOT STAFFED this round.** ⭐ **Lead's ruling, and it is worth understanding rather than just obeying: territory discipline defends two things — collision between sessions in one file, and an implementer reasoning past its competence. The first risk is NIL here (vacant territory, not contested); the second is handled by construction, because the orchestrator authored the text and you transcribe it.** ⇒ **This is a declared, attributed edit into a vacant area — NOT a territory breach.**

### ⛔ FOUR CONSTRAINTS ON THE TRANSCRIPTION — lead-imposed, non-negotiable
1. ⛔ **VERBATIM. Do not paraphrase, tighten, reflow, or improve the text.** If it reads awkwardly, that is the orchestrator's problem to fix in the brief — not yours to fix at the keyboard.
2. ⛔⛔ **HALT — DO NOT ADAPT.** If the surrounding code has moved and the anchor below does not match cleanly, **STOP and ask the orchestrator.** Do not reconcile it yourself. ⭐ *Half a slice landing into an undecided premise is what 029 records being avoided twice — the second time only because someone halted rather than picked.*
3. ⛔ **Your commit message names the orchestrator as the author of the `packages/policy` hunk and says why it rode in your commit.** An unattributed cross-area edit is indistinguishable from an implementer improvising, which is the thing this construction exists to avoid.
4. ⛔ **Do NOT insert a commit hash into the note.** The landing hash is your own commit and does not exist when you write the text. **Cite the TASK, never a hash** — the text below already does. *(De-inlining: a task id survives; a hash you cannot know yet is a guess.)*

---

## ⛔ HALF B — THE REPLACEMENT TEXT

### The anchor (locate by content, not by line number)
Inside `denyDirectCrossWorkspaceRaw`'s Option-(B) residual, find the block that currently begins mid-line with `⇒ what forecloses` and runs through the line ending `applied to this note.` **Measured at HEAD `46ef6c03` as lines 500–509 — treat that as a hint, not an address** (`### 24.93`'s own slice moved every line in this file, which is why its `Files:` entry cites by symbol).

**Replace from `⇒ what forecloses` (mid-line 500) through the end of line 509.** The line-500 text *before* that marker — `all 3 ids \`### 24.84\` measured live, and excludes every credential.` — **stays.**

### The text (comment prefix is two spaces, `//`, five spaces — match the surrounding block exactly)

```
  //     ⇒ ⛔ WHAT FORECLOSES AN ENUMERATION HERE IS NOT LOGIC BUT MEMBERSHIP, AND THAT
  //     GROUND IS STRUCTURAL RATHER THAN CONTINGENT — WHICH IS THE WHOLE REPAIR, BECAUSE
  //     THIS NOTE HAS NOW EXPIRED A GROUND TWICE AND BOTH TIMES IT WAS A CONTINGENT ONE.
  //     ⛔ A WORKSPACE ID'S MEMBERSHIP IS USER-DETERMINED BY DESIGN: `id` is a FREE field on
  //     the create path, DISTINCT from the closed `WorkspaceType` enum, and `WorkspaceIdSchema`
  //     constrains an id's SHAPE — never the SET of ids that exist. ⇒ a user creating
  //     `acme-corp` produces a perfectly slug-valid id that no enumeration written today
  //     contains, so an enumeration would reject a live id whatever the brand does.
  //     ⭐ THE TWO NAMESPACES ARE DEMONSTRABLY DIFFERENT, NOT TWO SPELLINGS OF ONE THING —
  //     `WorkspaceType` is `employer_work | personal_business | personal_life` (UNDERSCORES,
  //     `packages/contracts/src/primitives/enums.ts`) and would itself FAIL `WorkspaceIdSchema`,
  //     while the live ids are hyphen-spelled. ⇒ deriving an enumeration from the type enum is
  //     not merely incomplete, it is ILL-TYPED — a checkable fact, not a judgement.
  //     ⛔ THE PRIOR GROUND — "the id set is OPEN BY CONSTRUCTION, `parseCreateWorkspace` admits
  //     ANY non-empty string" — ~~HELD UNTIL `### 24.84`'s WORKER LEG~~ and is RETAINED AS
  //     REASONING ONLY: that leg has landed and the create path now runs the brand, so the
  //     sentence is true of the past and false of the present (`L195` — retain the reasoning,
  //     past-tense the state). ⭐ IT WAS NOT WRONG; IT WAS CONTINGENT, AND IT NAMED ITS OWN
  //     CONTINGENCY AND THE TASK THAT OWNED IT, WHICH IS WHY THIS EDIT IS AN EXPECTED
  //     SUCCESSION RATHER THAN A DISCOVERED DEFECT. That is the note's own rule working.
  //     ⚠ AND THE INTERIM RESIDUAL IS PRICED, NOT UNKNOWN — SAID PLAINLY BECAUSE A READER WHO
  //     CANNOT FIND IT WILL CONCLUDE THE GAP IS UNEXAMINED: the brand binds at CREATE and does
  //     NOT migrate rows written before it, and that pre-brand population is UNMEASURED ACROSS
  //     INSTALLS (`### 24.84` measured 3 of 3 conforming on ONE deployment at ONE moment —
  //     other installs unmeasured, `L106`: dormancy is not a guarantee). ⛔ `### 24.106` OWNS
  //     that migration and is FILED, NOT BUILT — an OWNER-ACCEPTED cost, deliberately carried.
  //     ⇒ so the enumeration is foreclosed TWICE OVER right now, and only the MEMBERSHIP half
  //     survives `### 24.106` landing. ⭐ Recorded because a reader who sees the migration land
  //     must NOT read it as licence to enumerate.
  //     ⛔ THE ONE CONTINGENCY THAT REMAINS, NAMED RATHER THAN LEFT IMPLICIT AND DELIBERATELY
  //     NOT FILED: if the id set were ever ARCHITECTURALLY FIXED to a closed set, membership
  //     would stop being user-determined and this ground would change. NO TASK OWNS THAT
  //     BECAUSE NO SUCH CHANGE IS PROPOSED — ⚠ and filing a speculative task to look thorough
  //     is how a tracker acquires work nobody asked for (`### 24.94`'s population, from the
  //     other direction). It is named here so the next author checks it rather than assumes it.
```

⚠ **The `~~strikethrough~~` on the prior ground is deliberate and must be transcribed** — this file's convention is strike-and-retain, and `L195` narrows it to *retain the reasoning, past-tense the state*, which the text does explicitly.

---

## Acceptance criteria (what "done" means)

- [ ] ⛔ **`parseCreateWorkspace` runs `WorkspaceIdSchema` on the `id`** — the id is no longer admitted on `isNonEmptyString` alone.
- [ ] **A non-conforming id is REFUSED at create, fail-closed, through the existing typed `FailureVariant` path** — not a throw, and **the refusal code is asserted**, not a bare falsity (`### 24.101`).
- [ ] ⛔ **The three live ids still create successfully.** `employer-work` / `personal-business` / `personal-life` must pass. ⚠ **This is the availability guarantee `### 24.84`'s own gate names; a leg that breaks it is a regression, not a hardening.**
- [ ] ⛔ **The refusal is REDACTION-SAFE: the rejected id value does not reach the error payload, a log, or the renderer.** ⚠ **A validation error that echoes the invalid input is the natural implementation and it is a rule-7 leak here** — the value that failed is exactly the credential-shaped string this whole arc is about. **Pin it, with a fixture that is genuinely credential-shaped rather than a neutral sentinel.**
- [ ] ⛔⛔ **The `packages/policy/src/visibility.ts` note is replaced VERBATIM per Half B, in the SAME COMMIT.**
- [ ] ⛔ **No other `packages/policy` line is touched.** One hunk, the anchored block, nothing else.
- [ ] **`/preflight` clean, measured on two surfaces that could have disagreed.** ⚠ **`packages/policy` has tests — run them; you changed a comment, and a comment change that reds a test means you hit more than a comment.**

## ⛔ Explicitly NOT in this slice
- **`### 24.100`** (`makeId` casts without running the brand — the *generic* form, all 20 brands). ⚠ **It is adjacent, it is contract territory, and it will look like the same bug. It is not yours.** Flag at Step 9 if you touch its edges.
- **`### 24.106`** — the migration. **Filed, not built, owner-accepted.**
- **Any `apps/desktop` change** (`### 24.108` exists for `foldStatus`).
- ⚠ **Backfilling or migrating existing rows.** This leg binds CREATE. **If you find yourself wanting to fix existing rows, that is `### 24.106` and it is an owner call.**

## Wiring / entry point (Step 7.5)
`parseCreateWorkspace` ← `apps/worker/src/api/procedures/onboarding.ts` create-workspace procedure. **Production-reachable; state it as reachability, not as "it has callers."**

## Files expected to touch
**Modified:**
- `apps/worker/src/api/procedures/onboarding.ts` — `parseCreateWorkspace`
- `apps/worker/test/api/procedures/onboarding.test.ts` (or the existing suite covering it — **locate it, don't assume the path**)
- `packages/policy/src/visibility.ts` — **the one anchored comment block, verbatim, Half B**

## RED test outline (Step 2)
1. **`non_conforming_workspace_id_is_refused_at_create`** — Asserts: the typed refusal **code**, for an id the brand rejects (e.g. an underscore-bearing `ws_employer`). ⛔ Confirm RED for the right reason: *"the brand is not run"*, not a fixture error.
2. **`the_three_live_workspace_ids_still_create`** — Asserts: each of the three conforming ids succeeds. ⭐ **The availability pin. It is not optional and it is not a formality.**
3. **`refusal_does_not_echo_the_rejected_id`** — Asserts: given a **credential-shaped** id, the value appears in **no** field of the error payload. ⛔ **Mutation-prove it: make the error carry the value and exactly this pin reds.** ⚠ **A GREEN mutation is ambiguous — blind pin OR unapplied mutation. Prove the mutation applied, by diff.**
4. **`create_still_rejects_the_other_malformed_inputs`** — Asserts: name / type / vaultRoot / gbrainBrainId / preset refusals are unchanged, by code. **No behaviour drifts sideways.**

## Cross-doc invariant impact
⚠ **Likely none** — no Appendix-A model field changes; `WorkspaceIdSchema` already exists and already emits its JSON Schema. ⛔ **Flag at Step 9 if a snapshot moves** — that would mean the brand's emitted schema changed, which is contracts territory and not this slice.

## Dependencies + sequencing
- **Depends:** contracts leg `25ae6c49` (**LANDED**) · brief `296` / `### 24.102` (**dispatched first, deliberately** — ⭐ *so that "ids are brand-conforming now, so `24.102` is moot" never becomes sayable. Ordering to make a fallacy UNAVAILABLE beats any amount of prose warning against it: nobody has to remember a refutation if the argument cannot be made.* **Inherit the technique, not just the order.**)
- **Blocks:** nothing queued.

## Estimated commit count
**1 — and the atomicity is REQUIRED, not preferred.** See Half A/Half B above.

## Things to flag at Step 2.5
1. **Where exactly you run the brand in `parseCreateWorkspace`**, and how the `Result`/`FailureVariant` shape is preserved.
2. ⛔ **How you keep the rejected value out of the error payload** — name the mechanism.
3. **That the Half-B anchor matched cleanly.** ⛔ **If it did not: HALT and say so — do not adapt.**
4. **The coverage map** — each acceptance bullet → its covering test or an explicit `not-tested-because:`.

## Lessons-logged candidates anticipated
- ⭐ **A note that named its own contingency and the task owning it, then had that contingency fire and be succeeded cleanly** — the first end-to-end demonstration that the convention `### 24.93` introduced actually works. **Worth banking as a WORKED EXAMPLE rather than a rule**, since the rule already exists and what was missing was evidence it survives contact.
- ⭐ **Replacing a contingent ground with a structural one** (shape ≠ membership), where the structural ground was available the whole time and two prior authors reached for contingent ones.

## How to invoke
Run `/tdd` with this brief. ⛔ **Do NOT run `/session-start`** — you already ran it at boot. ⛔ **Do not start this until `### 24.102` (brief `296`) is committed.**
