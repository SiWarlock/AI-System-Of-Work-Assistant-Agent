# /tdd brief — connector_ingestion_bridge (15.1)

## Feature
Bind **the missing spine**: the poll driver's `onRecords → RegisterSourceInput → dispatchSourceIngestion` bridge in the worker composition, plus a meeting-closeout dispatch trigger — so a completed source (or closed meeting) drives ingestion. Today the ONLY trigger reaching `dispatchSourceIngestion` is the local `.md` fs watcher; nothing polls a connector into the ingestion path. **Pure-build — FAKE transport, no network, no tokenRef bound** (the real transport is Phase 16/23; real extraction is Phase 18). Worker-track.

## Use case + traceability
- **Task ID:** 15.1
- **Architecture sections it implements:** `ARCHITECTURE.md §19.2` (primary — ingestion-spine plumbing), `§9` (source-ingestion workflow dispatch), `§8` (Connector Gateway read→register bridge).
- **Related context (confirm at Step 1 — you're in apps/worker/, orient thoroughly):**
  - **The gap (from the audit):** `apps/worker/src/temporal/dispatchSourceIngestion.ts` is the sole production `client.workflow.start` (starts `sourceIngestionWorkflow`), reached ONLY by the local `.md` fs watcher. `packages/workflows/src/activities/connectorPoll.ts:40-51,88-101` exposes the poll driver's `onRecords` seam — but **nothing in `apps/worker` binds it** (inject `resolve → {port, syncDeps.onRecords}`). This slice wires that bridge so a poll pass over a FAKE adapter's records reaches `registerSource → dispatchSourceIngestion`.
  - **The registration gate:** `registerSource` (`packages/integrations/src/connectors/source-register.ts`) is the candidate-data gate — a polled record is CANDIDATE data until it passes registration (schema-gated). The bridge must route through it, never around it (safety rule 2).
  - **Idempotency precedent (worker Lesson 16):** the content-versioned source key IS the Temporal `workflowId` under REJECT_DUPLICATE (AlreadyStarted folds to a success no-op) — so re-polling the same record does NOT double-dispatch. Preserve this (safety rule 3).
  - **The meeting trigger:** this slice adds the meeting-closeout dispatch TRIGGER (a closed meeting drives ingestion); the dedicated `dispatchMeetingCloseout` + `correlateMeeting` routing (a Granola meeting → the MEETING machinery, not the generic source path) is **15.9** — confirm the 15.1-vs-15.9 split at Step 1 (Step-2.5 Q1).

## Acceptance criteria (what "done" means)
- [ ] The worker composition binds the poll driver's bridge: a poll pass over a FAKE connector adapter's records maps each record → `RegisterSourceInput` → `registerSource` → `dispatchSourceIngestion` (starts `sourceIngestionWorkflow`) — a SECOND trigger into the ingestion path alongside the existing `.md` watcher.
- [ ] **Candidate-data gate (security-reviewer=invariant, safety rule 2):** every polled record routes THROUGH `registerSource` (the schema gate) — a record that fails registration does NOT dispatch; the bridge never bypasses the gate.
- [ ] **Idempotent dispatch (safety rule 3):** re-polling the SAME record (same content-versioned key) does NOT double-dispatch (the key = `workflowId`, AlreadyStarted → no-op); a test drives a duplicate poll and asserts one dispatch.
- [ ] **WS-8:** the registered source's `workspaceId` is bound from the connector-instance's workspace (14.2), never inferred from the record content (safety rule 2 / REQ-F-017); a test pins the workspace comes from the bound instance.
- [ ] **Meeting-closeout dispatch trigger:** a closed-meeting trigger drives ingestion (the trigger plumbing per the task; the dedicated `dispatchMeetingCloseout`/correlation is 15.9 — see Q1). If the split defers all meeting dispatch to 15.9, state that as a not-tested-because.
- [ ] **Fakes only:** the whole path runs over a FAKE adapter — ZERO network, NO tokenRef bound. A test asserts no real transport is constructed.
- [ ] `/preflight` clean (worker + repo-wide).

## Wiring / entry point (Step 7.5)
The bridge is bound in the worker composition (`apps/worker/src/composition/buildActivities.ts` — inject `resolve → {port, syncDeps.onRecords}`) so the poll driver reaches `dispatchSourceIngestion`. The poll trigger itself (a schedule / `connectorPoll` activity registration) may be dormant until Phase 16 composes the full drive path — confirm at Step 1 whether 15.1 wires the bridge as REACHABLE via a production trigger now, or ships the bridge with a reachability-waiver pending Phase 16's scheduled poll (do NOT wire dormant-on-dormant, Lesson 11).

## Files expected to touch
**Modified (confirm exact lines at Step 1):**
- `apps/worker/src/composition/buildActivities.ts` — bind the `onRecords → RegisterSourceInput → dispatchSourceIngestion` bridge.
- `apps/worker/src/watch/vaultWatcher.ts:1-9` — extended as the second dispatch source (or the bridge is a sibling trigger; confirm the shape).
- `packages/workflows/src/activities/connectorPoll.ts:40-51,88-101` — the `onRecords` seam (inject `resolve → {port, syncDeps.onRecords}`).

**New:**
- Test fakes for the connector adapter + the poll pass; tests.

If the bridge needs files beyond this list (e.g. a new composition helper), **flag at Step 2.5**.

## RED test outline (Step 2)
1. `poll_pass_bridges_records_to_dispatch` — a poll over a fake adapter's records dispatches each via `registerSource → dispatchSourceIngestion`. Why: §19.2/§8 the missing spine (G1).
2. `record_failing_registration_does_not_dispatch` — a record rejected by `registerSource` (schema gate) does NOT reach dispatch. Why: candidate-data gate (safety rule 2).
3. `duplicate_poll_dispatches_once` — re-polling the same record ⇒ one dispatch (AlreadyStarted → no-op). Why: idempotency (safety rule 3, Lesson 16).
4. `source_workspace_from_bound_instance_not_content` — the registered source's workspaceId is the connector-instance's, not inferred. Why: WS-8 / no-inference (safety rule 2).
5. `bridge_uses_fake_transport_only` — no real transport constructed (factory-spy zero-invocation). Why: pure-build/dormant.
6. `closed_meeting_trigger_dispatches` (or a not-tested-because deferring to 15.9) — the meeting-closeout dispatch trigger per Q1.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model:** none new — uses the existing `RegisterSourceInput` / `SourceEnvelope` (15.2 adds the body field — NOT this slice). The orchestrator writes a §19.2 arch note at round close.
- **Shared-contract seam touched?** No (15.1 does not touch `SourceEnvelope` — that's 15.2, contract-track) → no schema-snapshot test.

## Things to flag at Step 2.5
1. **15.1-vs-15.9 meeting split** — does 15.1 wire a basic meeting-closeout dispatch trigger, or defer ALL meeting dispatch to 15.9? My default vote: 15.1 = the connector→ingestion (source) bridge as the core + the meeting-closeout dispatch TRIGGER plumbing; the dedicated `dispatchMeetingCloseout` + Granola→meeting-vs-source routing is 15.9. Confirm the task authors' intent at Step 1.
2. **Reachability now vs waiver** — is the bridge reachable via a production trigger this slice, or does the scheduled poll land in Phase 16 (bridge ships waivered, reachable-via-Phase-16)? My default vote: if no production poll trigger exists yet, ship the bridge + a reachability-waiver (prod trigger = Phase 16's scheduled poll), NOT a dormant-on-dormant wire (Lesson 11) — mirror the 14.6/14.7 read-gate waiver pattern.
3. **Fake adapter** — reuse an existing test fake connector or a new one? My default vote: a composition/test fake (the real transport is Phase 16/23).

## Dependencies + sequencing
- **Depends on:** 14.2 (connector-instance registry — `bda2c91`/`6f639a6`), P6 (§8 gateways), P7 (§9 workflows), the existing `dispatchSourceIngestion` + `connectorPoll` + `registerSource`.
- **Blocks:** 15.5 (disposition — re-enter re-drives via this path), 15.6 (feedback-loop guard), 15.7 (external-write propose), 15.9 (meeting dispatcher). NOTE: 15.2 (SourceEnvelope body) + 15.3 (note projection) are a separate CONTRACT-then-worker chain — 15.2 is contract-track (a frozen-seam extension); flag when that chain is dispatched.

## Estimated commit count
**1–2.** The bridge wiring + tests. **security-reviewer=invariant** (candidate-data gate + idempotency + WS-8 no-inference); code-quality every-slice. Pure-build/dormant — NO hard line (fakes only, no network, no tokenRef).

## Lessons-logged candidates anticipated
- **Convention candidate** — the connector→ingestion bridge routes every polled record THROUGH `registerSource` (the candidate-data gate) with an idempotent content-versioned dispatch key; a fake transport keeps it dormant until Phase 16/23 binds a real one.
- **Architecture-doc note candidate** — the second production trigger into `dispatchSourceIngestion` (the connector poll bridge) alongside the `.md` watcher; the meeting dispatcher is 15.9.

## How to invoke
1. `/tdd connector_ingestion_bridge` (worker-impl2 session already oriented from 14.7 — no `/session-start` unless you swapped).
2. Step 0 (Restate) + Step 1 — confirm the exact `onRecords`/`dispatchSourceIngestion`/composition wiring + the 15.1-vs-15.9 meeting split + the reachability-vs-waiver call.
3. Step 2.5 — ping back the design answers (Q1 meeting split + Q2 reachability are load-bearing). Don't GREEN until I sign off.
4. Step 9 — categorized flags; surface the reachability posture (wired vs waivered) + the meeting-split decision.
