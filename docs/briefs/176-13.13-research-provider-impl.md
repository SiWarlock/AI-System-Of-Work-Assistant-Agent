# /tdd brief — research_web_retrieval_provider_impl

## Feature
The RES-1 research/web-retrieval provider (provint territory), DORMANT over injected FAKED transports: an egress-classed `ModelProviderPort` processor (`research-provider.ts`) for cloud web-retrieval with citations — Perplexity Sonar + Grok Live Search, each its OWN processor (the landed `perplexity`/`xai` ProviderIds, never aliased) — plus a free key-less source aggregator (`free-source-aggregator.ts`). Every call runs the broker egress veto FIRST (employer-work ack-OFF ⇒ fail closed, no cloud fallback, rule 5); the query text is employer-content-bearing so the key-less sources gate identically. Output is candidate data (citations verbatim); the provider never writes Markdown or reaches a real endpoint (real HTTP + paid keys = §ARM-RESEARCH crossing).

## Use case + traceability
- **Task ID:** 13.13 (provider-impl leg — the ProviderId frozen round landed `50b302b0`)
- **Architecture sections it implements:** `ARCHITECTURE.md §7` (RES-1 — a distinct `ModelProviderPort` processor family for cloud web-retrieval with citations) · `§5` (egress veto — the broker classes perplexity/xai cloud and fails closed for employer-work ack-OFF) + **REQ-F-022**.
- **Related context:**
  - Depends: the landed `perplexity`/`xai` ProviderIds (`50b302b0`), P5 (`ModelProviderPort` + broker egress veto), P3 (EgressPolicy). Blocks 13.14 (/research flows).
  - Mirror the dormant-over-faked-transport pattern of the 13.12 connector transports + 13.2 extractors (LESSON 11 TOTAL never-throws; real HTTP = the arming injection point).
  - ⚠ **Reuses the REAL broker egress veto** (not a self-declared-egressClass seam) — perplexity/xai are egress-by-construction (absent from `LOCAL_PROVIDERS`, policy/processors.ts:15 — the §ARM-RESEARCH "never LOCAL_PROVIDERS" invariant), so the veto fails closed for employer-work by construction.

## Acceptance criteria (what "done" means)
- [ ] `research-provider.ts` is an egress-classed `ModelProviderPort` processor over an INJECTED transport (faked): it returns candidate dossiers with **citations preserved verbatim** (as `sourceRefs`/frontmatter); it NEVER writes Markdown or reaches the write surface.
- [ ] Perplexity Sonar (`sonar-pro`/`sonar-reasoning-pro`, OpenAI-compatible `/chat/completions`) + Grok Live Search (xAI `/v1/responses`, `tools:[{type:'x_search'}]`) are modeled as the **candidate wire shapes** (documented `arch_gap` — the real shape is confirmed at §ARM-RESEARCH arming); **each its OWN processor** (perplexity/xai), never aliased (rule 5).
- [ ] **Egress veto runs FIRST** — before ANY transport call — via the broker egress veto (reuse, don't re-implement); an employer-work job with egress-ack OFF FAILS CLOSED (no cloud fallback). A test pins the veto precedes the transport.
- [ ] `free-source-aggregator.ts` (packages/integrations) is a free key-less aggregator (Wikipedia/HackerNews/arXiv/Reddit/Semantic-Scholar/DuckDuckGo; `--academic` → scholarly-only) over a faked transport; the query text is employer-content-bearing so it **gates IDENTICALLY** — an employer-work ack-OFF call fails closed (key-less does NOT bypass the veto).
- [ ] TOTAL never-throws over the untrusted/faked transport (fail-closed typed Result on fault/empty/malformed).
- [ ] **Dormant** — no real endpoint reachable (a test/scan confirms; real HTTP binds at §ARM-RESEARCH).
- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
The provider registers as a `ModelProviderPort` processor over the FAKED transport (dormant) — reachable through the broker's provider registry but inert (no real endpoint, no key). **Real transport + paid-key provisioning = §ARM-RESEARCH crossing 8** (owner-gated). The employer-work-ack-OFF egress-leakage EVAL (packages/evals) is a coordinated evalsec follow-on (like 13.3b) — flag at Step 9; not this slice. State explicitly: dormant registration only; no real fetch/write.

## Files expected to touch
**New:**
- `packages/providers/src/model-provider/research-provider.ts` — the Perplexity+Grok egress-classed processor (injected faked transport).
- `packages/integrations/src/connectors/adapters/free-source-aggregator.ts` — the key-less aggregator (injected faked transport).
- test files in each package.

**Modified (if needed):**
- the provider-registry composition (dormant registration) — flag at Step 2.5 if it touches worker composition (that would be a worker binding, logic-in-package pattern).

⚠ provint territory (packages/providers + packages/integrations). If registration needs `apps/worker/*` composition, STOP and flag — that's a worker binding leg. `git add <path>` only.

## RED test outline (Step 2)
1. **`research_provider_returns_candidate_dossier_verbatim_citations`** — over a faked transport, returns a candidate dossier with citations preserved verbatim; writes no Markdown. Why: §7 RES-1 / candidate-data.
2. **`egress_veto_runs_before_transport`** — an employer-work ack-OFF call fails closed BEFORE any transport call (no cloud fallback). Why: §5 / rule 5 (the load-bearing veto-first pin).
3. **`perplexity_and_xai_are_own_processors`** — each routes as its own processor (perplexity/xai), never aliased to openai/openrouter. Why: rule 5.
4. **`free_aggregator_gates_identically`** — the key-less aggregator fails closed for employer-work ack-OFF (query text is employer-content-bearing; key-less ≠ bypass). Why: §5 (identical gating).
5. **`no_real_endpoint_reachable`** — with the faked transport, no real HTTP/endpoint is reachable. Why: §ARM-RESEARCH dormancy.
6. **`total_never_throws`** — a faked-transport fault/empty/malformed yields a typed Result err, never a throw. Why: LESSON 11.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (uses the landed perplexity/xai ProviderIds; no new frozen model).
- **Orchestrator doc rows to write hot:** none anticipated (the ProviderId taxonomy rows I'm already banking cover it). Flag at Step 9 if a conformance-registry row is needed.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Candidate wire-shape grounding.** Default vote: **ground the perplexity `/chat/completions` + xai `/v1/responses` (`x_search`) candidate shapes on Context7** (resolve→query-docs) for fidelity, but keep them as **documented candidates** (`arch_gap`) — the real shape is confirmed at §ARM-RESEARCH; NO real call here. Confirm.
2. **Egress-veto placement.** Default vote: route through the broker egress veto so it runs FIRST (reuse, not re-implement) — the veto-before-transport is the load-bearing safety pin. Confirm the provider can't reach the transport before the veto.
3. **Key-less gating.** Default vote: the free aggregator gates IDENTICALLY (query text employer-content-bearing) — no key-less bypass. Confirm.
4. **Bundle vs split.** Default vote: one brief; you decide 1 commit (bundled research-retrieval machinery) vs 2 (provider, then aggregator) — both dormant, provint territory. Your call.

## Dependencies + sequencing
- **Depends on:** perplexity/xai ProviderIds (`50b302b0`), P5 (ModelProviderPort + broker egress veto), P3 (EgressPolicy).
- **Blocks:** 13.14 (/research + /research-deep flows) · the egress-leakage eval (evalsec follow-on) · §ARM-RESEARCH arming (real transport + keys).

## Estimated commit count
**1–2** — the research provider + the key-less aggregator (both dormant, provint territory). **security-reviewer = invariant** (rule 5 egress-veto-FIRST + no-real-endpoint + candidate-data-only + each-own-processor). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "an egress-classed research provider runs the BROKER egress veto FIRST (before any transport), reuses the real veto (perplexity/xai egress-by-construction via absence from LOCAL_PROVIDERS), preserves citations verbatim as candidate data, and stays dormant over a faked transport (real HTTP + key = §ARM-RESEARCH); key-less sources gate identically (query text is employer-content-bearing)."
- **Future TODO — gated** — §ARM-RESEARCH: real Perplexity/xAI transport + paid-key provisioning + the egress-leakage eval (evalsec).
- **Architecture-doc note** — the candidate wire shapes (arch_gap) confirmed at arming.

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (wire-shape grounding) + Q2 (egress-veto placement) need answers before GREEN.
2. Run `/tdd research_web_retrieval_provider_impl`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator (dormant build; §ARM-RESEARCH arming is a separate owner crossing).
