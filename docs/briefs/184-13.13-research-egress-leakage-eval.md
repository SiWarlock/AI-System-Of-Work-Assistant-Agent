# /tdd brief — research_provider_egress_leakage_eval

## Feature
The RES-1 research-provider **egress-leakage eval** (packages/evals, evalsec territory) — proves an employer-work ack-OFF call to the landed research provider (perplexity/xai) AND the free key-less aggregator **FAILS CLOSED** (no cloud egress, no fallback), while a personal / ack-ON call succeeds over the faked transport. The 13.13 done-when eval leg (imports the landed provider; edits no provider file).

## Use case + traceability
- **Task ID:** 13.13r (egress-leakage eval leg — 13.13 residual → task)
- **Architecture sections it implements:** `ARCHITECTURE.md §12` (Eval & Test Harness) · `§5` (egress veto — rule 5, employer-work ack-OFF fails closed) + REQ-F-022.
- **Related context:**
  - Imports the landed 13.13 research provider (`4a3ea13e`) + free-source aggregator (`727f3bd2`), both dormant over faked transports.
  - The provider is broker-veto-gated; the aggregator SELF-runs the real `egressVeto` over a synthetic egress-classed route (processorOfRoute≠null). This eval proves both fail closed for employer-raw ack-OFF.
  - Reuse the existing `packages/evals` egress-ack / leakage harness pattern (`suites/egress-ack/`, `suites/leakage/`).

## Acceptance criteria (what "done" means)
- [ ] An **employer-work ack-OFF** call to the research provider (perplexity/xai route) FAILS CLOSED — the broker egress veto DENIES before any transport (no cloud egress, no fallback); the faked transport is never invoked.
- [ ] An **employer-work ack-OFF** call to the free aggregator FAILS CLOSED — the aggregator's self-run real `egressVeto` DENIES (over its synthetic egress-classed route; key-less ≠ bypass).
- [ ] **Positive control** — a personal-workspace (or ack-ON) call SUCCEEDS over the faked transport (returns a candidate dossier; the eval does NOT over-deny).
- [ ] **Zero real egress** — the eval drives the FAKED transport; no real endpoint is reachable (dormant).
- [ ] Provider-free/deterministic (faked transport); the suite runs green; `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — the eval suite is the entry point** (a vitest suite under `packages/evals/`); it IMPORTS the landed research provider + aggregator and drives their egress-veto behavior over the faked transport. Edits no provider/worker file. State explicitly. (spec-lint requires this section.)

## Files expected to touch
**New:**
- `packages/evals/suites/research-egress/research-egress-leakage.test.ts` (or under the existing egress-ack/leakage suite dir).

⚠ Evalsec territory (`packages/evals`) — IMPORT the landed provider/aggregator (never edit `packages/providers`/`packages/integrations`). `git add <path>` only.

## RED test outline (Step 2)
1. **`provider_employer_ackoff_fails_closed`** — perplexity/xai employer-raw ack-OFF ⇒ broker veto DENIES, faked transport never invoked. Why: §5 rule 5.
2. **`aggregator_employer_ackoff_fails_closed`** — the key-less aggregator employer-raw ack-OFF ⇒ self-run egressVeto DENIES (synthetic egress-classed route). Why: §5 (key-less ≠ bypass).
3. **`personal_or_ackon_succeeds_positive_control`** — a personal / ack-ON call returns a candidate dossier over the faked transport (no over-deny). Why: non-vacuity.
4. **`no_real_endpoint_reachable`** — the eval reaches no real endpoint (faked transport only). Why: §ARM-RESEARCH dormancy.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none.
- **Orchestrator doc rows to write hot:** if the EVALUATION_CRITERIA matrix has an egress-leakage row for research, record it (flag at Step 9). Otherwise none.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Harness reuse.** Default vote: reuse the existing `egress-ack` / `leakage` suite harness + fixtures — add the research-provider/aggregator vectors. Confirm the harness reuse (don't rebuild).
2. **Positive control shape.** Default vote: a personal / ack-ON call succeeds over the faked transport (returns a candidate dossier). Confirm.
3. **Drive point.** Default vote: drive the provider through the broker veto (assembled) + the aggregator through its injected egressVeto — the real veto behavior, imported. Confirm.

## Dependencies + sequencing
- **Depends on:** 13.13 provider (`4a3ea13e`) + aggregator (`727f3bd2`).
- **Blocks:** 13.13 full arming confidence (§ARM-RESEARCH).

## Estimated commit count
**1** — one eval suite, `packages/evals` territory, faked-transport/deterministic. **security-reviewer = invariant** (rule 5 — confirm the fail-closed pins are non-vacuous + the positive control doesn't mask an over-deny). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "the research egress-leakage eval imports the landed provider/aggregator and proves employer-raw ack-OFF fails closed on BOTH the broker-veto-gated provider and the self-veto-running aggregator (key-less ≠ bypass), with a personal/ack-ON positive control for non-vacuity; faked transport, zero real egress."

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (harness reuse) needs an answer before GREEN.
2. Run `/tdd research_provider_egress_leakage_eval`.
3. Step 2.5 → main-orchestrator (coverage map). Step 9 → main-orchestrator.
