# /tdd brief — open_in_vault_renderer_affordance

## Feature
The renderer **open-in-Obsidian / reveal-in-vault affordance** — a button/action on the workspace-repo (and Global/Coordination repo) surfaces that invokes the ALREADY-LANDED path-scoped preload `vault:open` / `vault:reveal` channels (`80d387c6`); the renderer requests open-by-path only and NEVER reads or enumerates the vault filesystem itself (main enforces the path-scope). This is the Residuals(9) renderer-consuming leg of 9.12 (main + preload + inventory already shipped).

## Use case + traceability
- **Task ID:** 9.12r (renderer affordance — Residuals(9) → task)
- **Architecture sections it implements:** `ARCHITECTURE.md §11` — **REQ-UX-003** (open/reveal in vault, path-scoped; renderer requests open-by-path only, main rejects any path outside the configured workspace/global repo roots).
- **Related context:**
  - 9.12 landed (`80d387c6`): the pure realpath-contained `guardVaultPath` + `vault:open`/`vault:reveal` preload channels (inventory-pinned) + main handler; security 0C/0H/0M. **The renderer consuming affordance + multi-root parser are the explicit Residuals(9) follow-ups** — this slice builds the renderer affordance.
  - PURE desktop territory — no worker leg (Electron main IPC via the typed preload bridge, not the worker API).

## Acceptance criteria (what "done" means)
- [ ] An open-in-vault / reveal affordance on the workspace-repo surface(s) (and the Global/Coordination repo) invokes the preload `vault:open` / `vault:reveal` channel with the **configured repo path**; the renderer requests open-by-path only.
- [ ] The renderer **never reads or enumerates the vault filesystem** itself (no `fs`/node access renderer-side) — it passes the configured repo path to the typed preload bridge; main enforces the path-scope (already landed + tested).
- [ ] The affordance is **absent/disabled for a repo without a configured path** — no arbitrary/user-typed path can be opened (the renderer only ever passes a configured workspace/global repo root).
- [ ] Renderer redaction/isolation posture holds — no `electron`/`node`/`@sow/worker` import beyond the typed preload bridge (mirror the 9.14 renderer-isolation invariants).
- [ ] Component/unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
The affordance is reachable from the workspace-repo surface (the Projects page repo header and/or the workspace tab — confirm at Step 2.5); the action calls the landed `vault:open` / `vault:reveal` preload bridge. Entry point = the renderer button/action on the confirmed host surface, invoked on a real user click (not just tests). Confirm the exact preload channel binding via `preload/inventory.json` at Step 1.

## Files expected to touch
**New/Modified (confirm exact paths at Step 1):**
- the host renderer surface(s) — e.g. `apps/desktop/renderer/surfaces/projects/…` (repo header) and/or the workspace-tab component — add the open/reveal affordance.
- a small renderer `lib/` helper that calls the typed preload `vault:open`/`vault:reveal` bridge with the configured repo path.
- component/unit test(s).

⚠ Pure desktop territory — do NOT touch `apps/worker/*` or `packages/*`. The preload channels + main handler already exist (`80d387c6`); this slice only adds the renderer affordance that calls them. `git add <path>` only.

## RED test outline (Step 2)
1. **`open_affordance_invokes_preload_with_configured_path`** — clicking open calls `vault:open` with the repo's configured path. Why: REQ-UX-003 (open-by-path via the bridge).
2. **`reveal_affordance_invokes_preload_reveal`** — clicking reveal calls `vault:reveal` with the configured path. Why: REQ-UX-003 reveal leg.
3. **`renderer_never_enumerates_fs`** — the surface reads no vault filesystem; it only passes the configured path to the bridge (no `fs`/node import). Why: renderer-isolation (9.14 posture) / REQ-UX-003 (renderer requests open-by-path only).
4. **`no_configured_path_disables_affordance`** — a repo without a configured path shows no enabled open/reveal action (no arbitrary-path open). Why: REQ-UX-003 (main rejects out-of-root; renderer never offers arbitrary paths).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (renderer affordance over the landed preload channels; no contract/model change).
- **Orchestrator doc rows to write hot:** none anticipated.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Host surface(s).** Default vote: the **Projects page repo header + the workspace tab** (the natural repo-open points) + the Global/Coordination repo entry. Confirm which surface(s) host the affordance.
2. **Multi-root parser.** Default vote: **single-root this slice**; the multi-root parser is a separate Residuals(9) follow-up (the plan lists it distinctly) — defer. Confirm.
3. **Open vs reveal.** Default vote: **both** affordances (open-in-Obsidian via `vault:open` + reveal-in-Finder via `vault:reveal`). Confirm.

## Dependencies + sequencing
- **Depends on:** 9.12 main+preload (`80d387c6`, landed) · 9.1 (renderer shell).
- **Blocks:** — (closes a Residuals(9) item toward `/phase-exit 9`).
- Independent of the worker 9.10/9.9a chain — a pure-desktop bridge slice while those clear.

## Estimated commit count
**1** — a focused renderer affordance over the landed preload bridge; desktop territory. **security-reviewer = invariant** (REQ-UX-003 no-arbitrary-path-open: confirm the renderer only ever passes a configured repo root, never a user-arbitrary path). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "renderer open/reveal affordances pass only a CONFIGURED repo root to the typed preload bridge (never a renderer-enumerated or user-arbitrary path); main enforces the scope — the renderer requests open-by-path only."
- **Future TODO — phase** — the 9.12 multi-root parser (separate Residuals(9) follow-up).

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (host surfaces) + Q2 (multi-root defer) need answers before GREEN. Confirm the preload channel binding via `preload/inventory.json` at Step 1.
2. Run `/tdd open_in_vault_renderer_affordance`.
3. Step 2.5 → main-orchestrator (with the coverage map). Step 9 → main-orchestrator.
