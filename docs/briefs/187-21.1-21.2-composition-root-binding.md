# /tdd brief — write_adapter_routing_composition_binding

## Feature
The **composition-root BINDING** of the write-adapter routing registry (the worker leg of 21.1/21.2, closing G15/G16 fully). Replace the single hardcoded `makeTargetWriteAdapter('todoist')` construction in `apps/worker/src/composition/backends.ts` with `buildWriteAdapterRegistry(adapterDeps)` (the pure provint module from #23, `packages/integrations`), and route the external-write dispatch site(s) in `buildActivities.ts` through `dispatchRouted(registry, action, deps)` — selecting the vendor adapter by `action.targetSystem` instead of a single hardcoded vendor. **Stays DORMANT/byte-equivalent:** the injected transport is the stub (real transport + creds are owner-gated §ARM-21 crossing 5 — 21.5/21.6); no real external side effect opens in this slice.

> **Territory (standing ARC-5 pattern — "logic-in-package, wire-at-boot-by-worker"):** the LOGIC landed in `packages/integrations` (#23, `21.1/2-logic`, brief 169). THIS is the worker composition-root binding leg (#29). WORKER territory only — do NOT touch `packages/integrations/*` (the registry API is imported as-is).

## Use case + traceability
- **Task ID:** 21.1 + 21.2 (composition-root binding leg — worker territory). Closes G15/G16 fully (the logic leg #23 closed them at the module level).
- **Architecture sections it implements:** `ARCHITECTURE.md §19.8` (External Write / Tool-Sync) · `§8` (Tool Gateway external-write envelope).
- **Related context:**
  - #23 shipped `buildWriteAdapterRegistry` / `selectWriteAdapter` / `createUnroutedWriteAdapter` / `dispatchRouted` as a pure deps-injected module over a stub transport (brief 169; exhaustive `Record<TargetSystem, adapter>`, `Object.hasOwn` prototype-safe selector, unknown-target → PERMANENT `{status:'rejected'}` fail-closed).
  - **Current single-vendor wiring to replace:** `backends.ts:71` imports `makeTargetWriteAdapter`; `backends.ts:866` constructs `const writeAdapter = makeTargetWriteAdapter(…)` (todoist-only); it is assigned into the backends bundle (`writeAdapter` field, `:769` type / `:882` assignment) and consumed at the dispatch activity `buildActivities.ts:533` (`adapter: backends.writeAdapter`).
  - **Dormancy line:** real external writes remain owner-gated (§ARM-21). This slice adds NO real transport — the same stub-backed `AdapterDeps` the worker already composes for the single adapter is threaded into `buildWriteAdapterRegistry`. Every route resolves to a stub `WriteReceipt`.
  - **§ARM-21 WS-8 note (carry, do NOT close here):** the pending Approval `workspaceId` derives from `meetingJobInputs.workspaceId` (correct-by-coincidence under single-workspace-per-worker + the dormant `actions:[]` leg). Sourcing the Approval ws from the routed action's workspace + a WS-8 re-gate binds when source external actions / the real transport arm (worker L12/L32) — NOT this slice.

## Acceptance criteria (what "done" means)
- [ ] `backends.ts` constructs the registry via `buildWriteAdapterRegistry(adapterDeps)` over the SAME stub-backed deps currently passed to `makeTargetWriteAdapter`; the single `makeTargetWriteAdapter('todoist')` call is removed (no orphaned import). The backends bundle exposes the registry (e.g. `writeAdapters`) — or `dispatchRouted`-ready deps — replacing the single `writeAdapter` field.
- [ ] The external-write dispatch site(s) in `buildActivities.ts` route via `dispatchRouted(registry, action, deps)` keyed on `action.targetSystem` — a known target routes to its vendor **stub** adapter; an unknown/unregistered target maps to `{status:'rejected'}` PERMANENT (no create, no `held`/retry).
- [ ] **Byte-equivalent for the exercised path:** a `targetSystem:'todoist'` action produces the SAME stub `WriteReceipt` behavior as before the binding (no observable regression for the currently-reachable path). Pin with a test.
- [ ] **Dormancy preserved:** no real transport is reachable from the bound path (stub only); a factory/transport spy proves no real vendor call. §ARM-21 holds — this slice arms nothing.
- [ ] **Fail-closed reachability:** `createUnroutedWriteAdapter` / unknown-target rejection is reachable on the routed path (defense-in-depth: an unregistered/`__proto__`/`constructor` target never fail-opens to a truthy prototype member). The code-only fault never echoes the untrusted `targetSystem` (rule 7 redaction-safe).
- [ ] Worker suite green (full), `tsc` clean, `/preflight` clean. Post-binding reachability confirmed (Step 7.5 / `/wired`).

## Wiring / entry point (Step 7.5)
**THIS slice IS the wiring.** Entry point: the external-write dispatch **activity** in `apps/worker/src/composition/buildActivities.ts` (currently `adapter: backends.writeAdapter` at `:533`), reachable from the Temporal proof-spine / meeting-closeout workflow when a `ProposedAction` with a `targetSystem` flows. The registry is constructed at the composition root (`backends.ts`) at boot and threaded in. **Reachability caveat (state honestly):** the `actions:[]` leg is currently dormant, so no real action flows end-to-end yet — the binding is reachable-when-actions-flow and exercised via the activity's unit/integration tests with a stub transport; `/wired dispatchRouted` should trace registry → dispatch activity → workflow, greening ONLY the legs actually wired (assert the honest dormancy). Confirm the count of dispatch sites: brief 169 anticipated **two**; current grep shows **one** (`:533`) — verify during RED and route ALL that exist.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/backends.ts` — import `buildWriteAdapterRegistry` (+ `dispatchRouted` if constructed here) from `@sow/integrations`; replace the `makeTargetWriteAdapter('todoist')` construction with the registry over the existing stub deps; update the backends bundle field/type; drop the now-unused single-adapter import if orphaned.
- `apps/worker/src/composition/buildActivities.ts` — route the dispatch site(s) through `dispatchRouted` keyed on `action.targetSystem`.
- `apps/worker/test/…` — the composition/activity tests covering the binding (byte-equivalent todoist path · unknown-target fail-closed · dormancy spy · reachability).

⚠ **WORKER territory only** — do NOT touch `packages/integrations/*` (registry imported as-is) or any contracts/snapshot. `git add <path>` only; never `-A`.

## RED test outline (Step 2)
1. **`backends_builds_registry_not_single_adapter`** — after composition, the write path is registry-backed (all seven `TargetSystem` selectable); `makeTargetWriteAdapter('todoist')` is no longer the sole construction. Why: §19.8 G15/G16 binding.
2. **`todoist_path_byte_equivalent`** — a `targetSystem:'todoist'` action through the bound dispatch produces the same stub `WriteReceipt` shape/behavior as pre-binding. Why: no-regression for the exercised path.
3. **`unknown_target_fails_closed_via_dispatch`** — an action with an unregistered/`__proto__` `targetSystem` → `{status:'rejected'}` PERMANENT, no create performed. Why: fail-closed on the routed path.
4. **`no_real_transport_reachable_from_binding`** — with the stub deps the worker composes, every routed create is `stub-obj:`-namespaced; a transport spy proves no real vendor call. Why: §ARM-21 dormancy (safety).
5. **`dispatch_activity_reachable`** — the dispatch activity invokes `dispatchRouted` (not the old single adapter) for a flowing `ProposedAction`. Why: Step-7.5 reachability, no lingering single-adapter path.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none — the `TargetSystem` write enum is used as-is (the coverage/asymmetry decision is 21.3, #27, owner-gated). No frozen-snapshot change.
- **Orchestrator doc rows to write hot:** an **Architecture-doc note** anticipated — `buildWriteAdapterRegistry`/`dispatchRouted` are now the single external-write adapter-selection seam at the composition root (§19.8/§8). Flag at Step 9; I write it.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Dispatch-site count** — confirm 1 vs 2 external-write dispatch sites (169 said two; grep shows `:533` only). Route all that exist.
2. **Backends field shape** — registry object (`writeAdapters`) vs pre-selected-per-call `dispatchRouted` deps — pick the shape that keeps `buildActivities` thin and dormancy obvious. Confirm.
3. **AdapterDeps threading** — reuse the EXACT stub-backed deps currently feeding `makeTargetWriteAdapter`; do not introduce a new transport. Confirm no real-transport path becomes constructible.
4. **Byte-equivalence surface** — name what "no regression" means for the todoist path (receipt shape, dedupe null→create→dedupe) so the pin is concrete.

## Dependencies + sequencing
- **Depends on:** #23 (`21.1/2-logic`, registry module — DONE `packages/integrations`); P6 (Tool Gateway); the seven vendor factories (13.12, exist).
- **Blocks:** 21.3 (#27 owner enum-coverage decision — surfaces once routing is bound), 21.4 (outbox drain), 21.5/21.6 (real transport arming, §ARM-21).

## Estimated commit count
**1** — the composition-root binding + its worker tests. **security-reviewer = invariant** (external-write path / rule 3 posture — confirm NO real-write path opens, unknown-target fails closed, dormancy holds, fault redaction-safe). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Architecture-doc note** — the composition root routes external writes through `buildWriteAdapterRegistry` → `dispatchRouted` (single selection seam, §19.8/§8); the single-vendor `makeTargetWriteAdapter` construction is retired.
- **Convention candidate (maybe)** — the ARC-5 wire-at-boot closer: bind the in-package registry over the EXISTING stub deps, byte-equivalent for the exercised path, green ONLY the legs wired + assert the honest dormancy (mirrors the 13.10 waiver-holder closing pattern).

## How to invoke
1. Read end-to-end + brief 169 (the logic leg) for the registry API surface.
2. Run `/tdd write_adapter_routing_composition_binding`.
3. Step 2.5 → main-orchestrator. Step 9 → main-orchestrator (dormant binding, NO owner-gated crossing in this slice; security-reviewer=invariant).
