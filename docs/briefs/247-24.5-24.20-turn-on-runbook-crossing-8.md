# /tdd brief — turn_on_runbook_crossing_8_and_count_correction

## Feature
Rewrite `docs/runbooks/turn-on-and-smoke-test-runbook.md` (+ `docs/runbooks/copilot-propose-go-live.md`) from its stale Phase 0–9 structure to the corrected 13-phase / 8-crossing Part II structure, so the runbook's own closing count matches its enumeration and the RES-1 research-provider crossing (crossing 8, `§19.13`/Phase 26) is no longer silently omitted from the ONE artifact governing safe turn-on order. **This is arming-block release condition (c) — the last of the three unresolved this round.**

## Use case + traceability
- **Task ID:** `24.5` (the rewrite) + `24.20` (the fail-open-by-omission finding this rewrite discharges — bundled, see "Estimated commit count")
- **Architecture sections it implements:** `ARCHITECTURE.md §19.1–§19.13` (all 13 subsections; §19.13/Phase 26 is the crossing this round's audit found omitted), §19.11 (turn-on order), §13
- **Related context:** `docs/audits/003-2026-08-11-…-round-3.md` (finding DOC-2/F16, HIGH — the omission; F17 — the corrected counts). `IMPLEMENTATION_PLAN.md:2908-2911` (task 24.5, Done-when already corrected 2026-08-11 to 13/8) and `:3070-3076` (task 24.20, the fail-open-by-omission finding). ⛔ **Do NOT start from the runbook's own closing summary as ground truth — it is exactly what's wrong.** Ground truth is `ARCHITECTURE.md §19.x` headings (`rg -c '^##+ *§19\.[0-9]' ARCHITECTURE.md` should return 13) and the sequential/independent split named in 24.5's Done-when: sequential chain 17→18→19→20→21→22 (crossings 1–6, ending at Phase 22's propose flip), plus crossing 7 (Phase 23, per-vendor connector arc, gated only on 16+17) and **crossing 8 (Phase 26, RES-1 research provider — real Perplexity/xAI transport, employer-egress-veto-gated, "the query text itself is treated as potential employer content")**.

> **This brief widens phase scope because Phase 24 is a cross-cutting hardening/audit-remediation phase — its tasks originate from the 24.6 safety-assertion audit, which sweeps every area, so a remediation task legitimately cites the anchors its originating findings live under (here, all of §19.1–§19.13 individually, since the runbook must enumerate every one, plus §2.5 as a genuine seam reference) rather than only Phase 24's own declared set.**

## Acceptance criteria (what "done" means)
- [ ] The runbook enumerates **all 13 Part II phases and all 8 hard-line crossings**, crossing 8 (RES-1 research provider) included, in dependency order: sequential chain 17→18→19→20→21→22, then independent crossing 7 (Phase 23, after 16+17), then independent crossing 8 (Phase 26 / RES-1).
- [ ] Each crossing states its owner gate (+ security-review gate where applicable per `§ARM-*` in `IMPLEMENTATION_PLAN.md:149-220`), its strict-`=== true` flag list, and a per-crossing smoke step.
- [ ] **The runbook's own closing summary count matches the enumeration** — re-read the closing section against the body after writing it; do not grep for vendor names and declare done (this is exactly how crossing 8 went missing the first time — a count that silently excluded it rather than flagging it).
- [ ] `docs/runbooks/copilot-propose-go-live.md` cross-references the corrected runbook where it currently assumes the stale 7-crossing count.
- [ ] `24.5`'s own `Depends:` line is corrected to include Phase 26 (currently omits it — flagged in the task's own text, `IMPLEMENTATION_PLAN.md:2909`).
- [ ] Every hard-line phase task (17, 18, 19, 20, 21, 22, 23, 26) whose `Done-when` promises a runbook cross-reference actually has one that resolves to a real step in the rewritten runbook — spot-check at minimum Phase 17 and Phase 26.
- [ ] A reader can determine the one legal arming order from the runbook alone and cannot arm out of order from it (closes G57).

## Wiring / entry point (Step 7.5)
None — this is the operator-facing procedural document itself; there is no code entry point. The "wiring" is that `ARCHITECTURE.md §19.11` designates this file as the master turn-on-order artifact, and it must actually be that (not merely titled that) — verified by the closing-count-matches-enumeration check above, not by a test.

## Files expected to touch
**Modified:**
- `docs/runbooks/turn-on-and-smoke-test-runbook.md` — full restructure to 13-phase/8-crossing Part II form
- `docs/runbooks/copilot-propose-go-live.md` — cross-reference fix
- `IMPLEMENTATION_PLAN.md` — `24.5`'s `Depends:` line (add Phase 26); tick `24.5` and `24.20` `[x]` DONE at Step 9/10 with the commit hash

If touching any hard-line phase task's Done-when to fix a dangling runbook cross-reference, flag at Step 2.5 before doing it broadly — that's cross-doc-invariant-adjacent and the orchestrator should confirm scope first.

## RED test outline (Step 2)
This is a documentation rewrite, not code — there is no unit-test RED/GREEN cycle. **Verification method (run and paste output at Step 9, in place of a test-pass report):**

1. `rg -c '^##+ *§19\.[0-9]' ARCHITECTURE.md` → must be **13**, cross-checked against the runbook's own phase count.
2. `rg -icE "perplexity|xai|grok|RES-1|crossing 8|research provider" docs/runbooks/turn-on-and-smoke-test-runbook.md docs/runbooks/copilot-propose-go-live.md` → must be **> 0** (currently 0 in both — this is the discriminating check the round-3 audit used to find the omission).
3. Re-read the runbook's closing summary sentence against its own body enumeration and confirm the stated count (phases, crossings) matches what's actually enumerated above it — this is a **manual re-read**, not a grep, because the original defect was a count that was internally self-consistent-looking but wrong (§19.13 case named "crossing 8, INDEPENDENT arc" in the tracker but never once in the runbook).
4. `IMPLEMENTATION_PLAN.md:2909`'s `Depends:` line lists Phase 26.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none.
- **Orchestrator doc rows to write hot (Step 9 routing):** none anticipated — this slice's target IS a cross-doc artifact (the runbook), not a code model; no Appendix-A row implicated.
- **§2.5-seam model touched?** No.

## Things to flag at Step 2.5
1. **Does every hard-line phase task's `Done-when` get a live edit to point at its new runbook step number, or just Phases 17/23/26 (the ones this brief's acceptance criteria spot-check)?** My default vote: **spot-check 17 and 26 only, note the rest as consistent-by-construction** (the rewrite preserves phase ordering, so an existing correct cross-reference to "Phase N's step" shouldn't drift) — touching all 7 remaining phase tasks' Done-when text is a larger cross-doc sweep than this slice's Files list implies; if the rewrite's structure changes enough that old step-number cross-references break, flag it and we scope a follow-up rather than silently expanding this slice.
2. **Should `copilot-propose-go-live.md` be folded INTO the main runbook, or stay a separate cross-referenced doc?** My default vote: **stay separate, just fix the cross-reference** — merging is a bigger structural change than this slice's Done-when asks for, and 24.5's Files list already treats them as two documents.

## Dependencies + sequencing
- **Depends on:** `24.6` round 3 (`docs/audits/003-…`, the F16/F17 findings this brief implements) — landed.
- **Blocks:** arming-block release condition (c) — the last of the three unresolved this round. Also blocks arming the RES-1 research crossing (per `24.20`'s own `Blocks:` line).

## Estimated commit count
**1.** One focused doc rewrite; `24.5` and `24.20` close together since `24.20`'s finding IS the gap in `24.5`'s output — splitting them would leave one task pointing at the other's unfinished work.

## Lessons-logged candidates anticipated
- **Convention candidate** — a closing-summary count in a long procedural doc needs a mechanical re-derivation step (grep the enumeration, don't trust the prose total), same shape as this round's `L100`/`L101` numeric-literal-diverges-in-place family.
- **Future TODO — operational** — consider a `plan-lint`-style check that a runbook's stated phase/crossing count matches `rg -c` against its own enumeration, so this defect class can't recur silently.

## How to invoke
1. Read this brief end-to-end, then `docs/audits/003-…-round-3.md` findings F16/F17 for full context.
2. Run `/tdd turn_on_runbook_crossing_8_and_count_correction`.
3. Step 0/1 as usual (note there's no code RED/GREEN — Step 2 is the verification-method checks above, not a test file).
4. Step 2.5 — answer the two design questions or take defaults.
5. Step 9 — paste the four verification-method outputs in place of a test-pass report.
