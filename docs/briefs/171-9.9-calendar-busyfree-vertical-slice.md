# /tdd brief — calendar_busyfree_generic_conflict_surface

## Feature
The **9.9 Calendar surface** end-to-end: a worker read-model producer that projects GCL-sanitized busy/free availability into the frozen `UiSafeSchedule` type (generic conflict explanations only — NEVER raw cross-workspace event detail; Flow 3 / REQ-F-009), plus the renderer Calendar surface that renders it. Producer ships **dormant / empty-until-wired** (honest-empty calendar until fed) — the same posture as 9.7 ingestion + 9.20 honest-empty schedule.

## Use case + traceability
- **Task ID:** 9.9
- **Architecture sections it implements:** `ARCHITECTURE.md §11` (Calendar surface, Flow 3) — **REQ-F-009** (busy/free + generic conflict explanations; never raw cross-workspace event detail) · `§10` (read-model serving) · `§5` (GCL Visibility Gate — the only cross-workspace read path, WS-8).
- **Related context:**
  - The `UiSafeSchedule` / `UiSafeScheduleEntry` types landed round #21 (`28dd42ba`): `UiSafeScheduleEntry { start, end, busy, conflictExplanation? }` (single-line-bounded), `UiSafeSchedule { entries: max 200 readonly }`, **workspaceId-free by design** (attributing a conflict to a source workspace is itself a cross-workspace timing leak). The type header **DEFERS two cross-field invariants to this producer**: (a) `start <= end` per window; (b) `busy === false ⇒ conflictExplanation` absent.
  - Availability source (backend, already built): `packages/workflows/src/ports/crossCalendarScheduling.ts` — `BusyWindow { sourceId, start, end, genericReason? }` ("GENERIC sanitized reason — NEVER raw event detail"), `GatheredAvailability`, `ProposedWindow { genericExplanation? }`; reads every source through the GCL Visibility Gate; unreachable → typed `calendar_unreachable` (**REQ-F-009 no-silent-free**), raw detail → `gate_rejected`.
  - Producer pattern to mirror: `apps/worker/src/api/projections/ingestionInboxProjection.ts` (pure factory over injected deps, never-throws, drop-rules at write in `projections/uiSafe.ts`, worker L76/L77). Read-model registry: `apps/worker/src/api/adapters/readModel.ts` (`READ_MODEL_KEYS`, **no schedule key today**). Query router: `apps/worker/src/api/procedures/queries.ts` (`buildQueryRouter`; a new procedure needs **no `server.ts` edit**). Renderer mirror: `apps/desktop/renderer/surfaces/ingestion-inbox/index.tsx` + `store/projections.ts` + `lib/live.ts` + `store/route.ts` + `App.tsx`.

## Acceptance criteria (what "done" means)

**Commit 1 — worker read-model producer + `query.calendar`:**
- [ ] A `READ_MODEL_KEYS.schedule` key + a `readScheduleEntries` guard (structural field-copy → `[]` on malformed, mirroring the existing read guards) in `readModel.ts`.
- [ ] A `calendarProjection.ts` producer (pure factory over injected {availability-read, read-model-put, now}, never-throws) that maps GCL-sanitized busy windows → `UiSafeScheduleEntry[]`, applying **both deferred invariants** at write: `start <= end`; `busy === false ⇒ conflictExplanation` absent.
- [ ] `conflictExplanation` is **generic-only** — sourced from `BusyWindow.genericReason` / `ProposedWindow.genericExplanation`, never a raw title/attendee/body/location passthrough; a hostile raw-event-detail input must not appear in output.
- [ ] **No workspaceId / source attribution leaks** — a producer fed busy windows from ≥2 distinct workspaces emits entries with zero source-workspace or timing attribution.
- [ ] REQ-F-009 **no-silent-free**: an unreachable/partial availability source surfaces as empty/degraded — NEVER rendered as "free".
- [ ] A `query.calendar` procedure + `sanitizeCalendar` that **re-validates each row via `UiSafeScheduleEntrySchema`** (defense-in-depth over the write-time producer, mirroring `sanitizeIngestionInbox`) and re-gates the two deferred invariants; `entries` capped at 200; malformed/absent read-model row → empty `ok` (never crash / raw leak).
- [ ] Producer ships **UNBOUND (empty-until-wired)** — the `buildProofSpineActivities` composition binding is a DEFERRED follow-up (see Wiring; avoids the `backends.ts` file that provint's 21.1 is editing concurrently). `query.calendar` returns an empty schedule until bound.

**Commit 2 — renderer Calendar surface:**
- [ ] `surfaces/calendar/index.tsx` — a dumb prop-render of the UI-safe slice, reading only allowlisted fields, with an empty state; NEVER imports electron/node/`@sow/worker`.
- [ ] Store slice + `replaceSchedule` (whole-list REPLACE, not upsert) in `store/projections.ts`; `hydrateCalendar` in `lib/live.ts` re-validating each row via `UiSafeScheduleEntrySchema` before store entry (err/throw → empty, never stale); a `{surface:"calendar"}` route in `store/route.ts` mounted in `App.tsx`.

- [ ] All unit tests pass; `/preflight` clean.

## Wiring / entry point (Step 7.5)
- **Commit 1 entry point:** the `query.calendar` procedure, mounted via `buildQueryRouter` in `server.ts` (no `server.ts` edit — the router composes it). It reads `READ_MODEL_KEYS.schedule`.
- **Commit 2 entry point:** the Calendar surface behind the new `{surface:"calendar"}` route in `App.tsx`.
- **Deferred (NOT this slice):** the producer's DORMANT composition binding at `buildProofSpineActivities` (`apps/worker/src/composition/backends.ts`) — deferred so this slice does NOT touch `backends.ts` (provint's 21.1 owns it concurrently). Until bound, the calendar renders honest-empty (the 9.7 ingestion / 9.20 precedent). State this explicitly. The binding follow-up sequences after 21.1 lands.

## Files expected to touch
**New:**
- `apps/worker/src/api/projections/calendarProjection.ts` — the producer (mirror `ingestionInboxProjection.ts`).
- `apps/desktop/renderer/surfaces/calendar/index.tsx` — the surface.
- worker + renderer test files.

**Modified:**
- `apps/worker/src/api/adapters/readModel.ts` — `READ_MODEL_KEYS.schedule` + `readScheduleEntries` guard + `DbReadModelQueryPortAsync.calendar`.
- `apps/worker/src/api/projections/uiSafe.ts` — a `toUiSafeScheduleEntry` drop seam.
- `apps/worker/src/api/procedures/queries.ts` — `ReadModelQueryPort.calendar` + `sanitizeCalendar` + the `calendar` procedure.
- `apps/desktop/renderer/store/projections.ts` (schedule slice + `replaceSchedule`) · `lib/live.ts` (`hydrateCalendar`) · `store/route.ts` (add surface) · `App.tsx` (mount).

⚠ **Shared-file coordination:** you edit worker files `queries.ts` / `readModel.ts` / `projections/uiSafe.ts` (+ NEW `calendarProjection.ts`). The orchestrator confirms these are collision-free with the concurrent in-flight slices (worker 9.10-A = copilot/boot/systemHealth; provint 21.1 = backends.ts/buildActivities.ts) and is holding any worker slice off `queries.ts`/`readModel.ts`/`uiSafe.ts` for your duration. **Do NOT touch `backends.ts`** (provint owns it). `git add <path>` only.

## RED test outline (Step 2)
**Producer / `query.calendar`:**
1. **`producer_maps_busy_windows_to_uisafe_entries`** — GCL busy windows → valid `UiSafeScheduleEntry[]`. Why: §11 Calendar / §10.
2. **`conflict_explanation_generic_only_no_raw`** — a raw title/attendee/body input never reaches `conflictExplanation`; only `genericReason` composes it. Why: REQ-F-009 / contracts projector obligation.
3. **`no_workspace_or_timing_attribution_leak`** — windows from ≥2 workspaces emit entries with zero source attribution. Why: WS-8 / cross-workspace timing leak.
4. **`busy_false_has_no_explanation_and_start_le_end`** — the two deferred cross-field invariants hold at write. Why: ui-safe.ts deferred-invariant contract.
5. **`unreachable_source_never_shows_free`** — a `calendar_unreachable` source surfaces empty/degraded, not "free". Why: REQ-F-009 no-silent-free.
6. **`query_calendar_revalidates_and_caps`** — `sanitizeCalendar` re-validates each row via `UiSafeScheduleEntrySchema`, caps at 200, malformed row → empty `ok`. Why: defense-in-depth (mirror `sanitizeIngestionInbox`).
7. **`producer_unbound_returns_empty`** — with no composition binding, `query.calendar` returns an empty schedule (honest-empty). Why: dormant/empty-until-wired posture.

**Renderer:**
8. **`calendar_surface_renders_allowlisted_only`** — renders start/end/busy/conflictExplanation only; empty-state on no data; no electron/node import. Why: renderer redaction posture (rule #7).
9. **`hydrate_calendar_revalidates_or_empties`** — `hydrateCalendar` re-validates each row; err/throw → empty store slice, never stale. Why: desktop L11/live.ts precedent.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** none (consumes the already-frozen `UiSafeSchedule` type; no snapshot change). A NEW `READ_MODEL_KEYS.schedule` key is an internal read-model registry entry, not a frozen Appendix-A model.
- **Orchestrator doc rows to write hot:** if `READ_MODEL_KEYS` is mirrored in a cross-doc table, add the `schedule` row (flag at Step 9). Otherwise none.
- **Contracts-seam model touched?** No.

## Things to flag at Step 2.5
1. **Read-model source of truth.** Default vote: a **dedicated `READ_MODEL_KEYS.schedule` key fed by the new dormant producer** (mirrors ingestion; "calendar logic lives only in the worker" + the mirror both point here) — NOT a read-time derive off `global_surface`. Confirm.
2. **Row scope.** Default vote: a **GLOBAL `workspaceId = null` merged view** (like `global_surface`) — `UiSafeSchedule` is workspaceId-free by design and availability is a cross-workspace busy/free merge via the GCL; so `query.calendar` takes no workspaceId input. Confirm vs organizer-scoped.
3. **Where the two deferred invariants live.** Default vote: **BOTH** — enforced at the producer write-time gate AND re-gated in `sanitizeCalendar` at read (matches the ingestion drop-at-write + read-side re-validate precedent). Confirm.
4. **Today tie-in.** Default vote: **ship the standalone Calendar surface only this round**; feeding Today's schedule placeholder (desktop L19) is a Residual follow-up (don't widen scope). Confirm.
5. **Composition binding deferral.** Default vote: **defer the `buildProofSpineActivities` producer binding** to a follow-up sequenced after provint's 21.1 lands (keeps this slice out of `backends.ts`). Confirm — surface at Step 7.5 if you believe the slice needs the binding to be meaningful.

## Dependencies + sequencing
- **Depends on:** 9.3, 9.8 (landed); the `UiSafeSchedule` type (#21, landed); the `crossCalendarScheduling` availability backend (built).
- **Blocks:** `/phase-exit 9` (one of the last open Phase-9 surfaces). The deferred composition binding follow-up depends on 21.1 landing (frees `backends.ts`).

## Estimated commit count
**2** — (1) worker read-model producer + `query.calendar` (worker area); (2) renderer Calendar surface (desktop area). Two logical units, each ≥30 lines, cleanly bisectable; one vertical feature owned by one implementer. No safety-invariant WRITE (the slice TIGHTENS the UI-safe boundary — generic-only, no-attribution, honest-empty). **security-reviewer = invariant** (REQ-F-009 / WS-8 no-raw-cross-workspace-detail pins). `code-quality-reviewer` (every-slice).

## Lessons-logged candidates anticipated
- **Convention candidate** — "a busy/free calendar read-model composes generic reasons redact-by-type (never a raw-event passthrough), carries no workspace/timing attribution, enforces the ui-safe.ts-deferred cross-field invariants at BOTH producer-write and sanitize-read, and ships dormant/empty-until-wired (honest-empty)."
- **Future TODO — phase** — the `buildProofSpineActivities` calendar-producer composition binding (sequenced after 21.1).
- **Architecture-doc note** — the `READ_MODEL_KEYS.schedule` key + its workspace-free global scope.

## How to invoke
1. Read end-to-end — Step-2.5 Q1 (read-model source), Q2 (row scope), Q5 (binding deferral) need answers before GREEN.
2. Run `/tdd calendar_busyfree_generic_conflict_surface`.
3. Step 2.5 → main-orchestrator (with the coverage map — each acceptance bullet → a test). Step 9 → main-orchestrator.
