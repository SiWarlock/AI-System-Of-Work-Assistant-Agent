# /tdd brief — reconciler_projection_completeness_hardening

## Feature
Harden piece B's `parseGraphRead` (`apps/worker/src/composition/reconcilerDbProjection.ts`) completeness contract — the deferred completeness/paging hardening that needed the real transport shape (Item 2a `cdbb389` supplied it). Flip the current **negative** signal ("`complete` unless a `truncated`/`cursor` more-results signal appears" — a fail-OPEN when the response omits pagination fields) to a **POSITIVE** completeness token (default-INCOMPLETE unless explicitly signaled complete), **reject unknown "more-results" field names** (`hasMore`/`nextPageToken`/`nextOffset`/`pageInfo.hasNextPage`, beyond the existing `truncated`/`cursor`), and add a **row-count-vs-stated-total cross-check**. Strictly safer (more reads degrade to `complete=false`); ships DORMANT (piece B has no production caller — reachability-waivered; `makeDbAdapter` stays unbound ⇒ byte-equivalent boot). This is **dormant arming-prep Item 2b** (completeness hardening); it closes Item 2.

## Use case + traceability
- **Task ID:** 13.10 (dormant arming-prep **Item 2b**).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KnowledgeWriter–GBrain parity / read surface) + `§12` (parity/serving GO conditions — coverage completeness is the anti-false-green trust axis).
- **Related context:**
  - `apps/worker/src/composition/reconcilerDbProjection.ts` — piece B (`8a27e78`); `parseGraphRead` (`:91-108`) is the target; `buildReconcilerDbProjection` (`:55`) ANDs `factsComplete && version !== undefined` into `complete`; worker Lesson 19 (fail-closed on coverage — type-robust; a false-complete is a trust-gate defeat).
  - The current negative logic (`:103-107`): `incomplete = (truncated !== undefined && truncated !== false) || (cursor !== undefined && cursor !== null && cursor !== "")` — this claims `complete` whenever those two specific fields are absent, so a response using a DIFFERENT pagination field name, or omitting all pagination but genuinely truncated, is a FALSE-complete.
  - Item 2a `cdbb389` (`packages/knowledge/src/gbrain/gbrain-http-read-client.ts`) — established the `gbrain serve --http` wire shape as a **DOCUMENTED CANDIDATE** (arch_gap, Lesson 21). The completeness token + paging fields here are part of the SAME candidate contract — document + fail-closed; the exact real field is confirmed at the owner's arming binding.
  - Worker Lesson 19 (the projection builder's fail-closed coverage contract this extends) + Lesson 21 (candidate wire shape honesty).

## Acceptance criteria (what "done" means)
- [ ] `parseGraphRead` returns `factsComplete=true` **ONLY** when a POSITIVE completeness token is explicitly present AND signals complete (default-INCOMPLETE when the token is absent) — the flip from the negative default. The token is a DOCUMENTED CANDIDATE (see Step-2.5 Q1); an absent token ⇒ `factsComplete=false`.
- [ ] `factsComplete=false` on ANY present-and-truthy "more-results" signal across the widened set: `truncated` (non-`false`), `cursor` (non-empty, any type), `hasMore` (truthy), `nextPageToken` (present non-empty), `nextOffset` (present), `pageInfo.hasNextPage` (truthy) — TYPE-ROBUST (a truthy non-boolean still degrades, per Lesson 19).
- [ ] **Row-count-vs-stated-total:** when the envelope carries a finite numeric stated total (`total` / `totalCount` — Step-2.5 Q3), `factsComplete=false` unless the returned raw row count equals it (fewer rows than the stated total ⇒ more exist ⇒ incomplete).
- [ ] Existing coverage still holds: a dropped/malformed row ⇒ `factsComplete=false` (`allRowsWellFormed`); an `err`/malformed-envelope ⇒ `factsComplete=false`; the whole thing NEVER throws (Lesson 19 type-robustness preserved).
- [ ] The existing piece-B tests that asserted `complete=true` are updated IN-SLICE to carry the positive completeness token (the contract changed — this is the hardening, not scope creep); the empty-facts case now requires the positive token to be `complete`.
- [ ] `buildReconcilerDbProjection` end-to-end: a clean read WITH the positive token + no more-results signal + matching total ⇒ `complete=true`; dropping the token OR adding any more-results signal OR a total mismatch ⇒ `complete=false`.
- [ ] Byte-equivalent boot preserved: piece B stays dormant/unbound (`makeDbAdapter → undefined`); no boot edit.
- [ ] `/preflight` clean.

## Wiring / entry point (Step 7.5)
**none — piece B stays dormant + reachability-waivered** (its production caller is the reconcile driver/scheduler, which only run on the owner-gated armed path; `makeDbAdapter` stays `() => undefined`). This slice hardens an already-built dormant parser; reachable from its own tests. No new wiring; no boot edit. `spec-lint brief` is satisfied by this explicit statement.

## Files expected to touch
**Modified:**
- `apps/worker/src/composition/reconcilerDbProjection.ts` — harden `parseGraphRead` (positive token + widened more-results set + total cross-check); update the doc-comment header (`:9-15`, `:27-29`) to state the positive-token contract + name the candidate fields as an arch_gap.
- `apps/worker/test/composition/reconcilerDbProjection.test.ts` — add the RED tests + update the existing `complete=true` cases to carry the positive token.

If implementation needs files beyond this list, **flag at Step 2.5**.

## RED test outline (Step 2)
Tests in `apps/worker/test/composition/reconcilerDbProjection.test.ts`:

1. **`absent_completeness_token_is_incomplete`** — Asserts: a clean well-formed read with NO completeness token ⇒ `factsComplete=false` (the flip). Why: §12 default-incomplete (closes the fail-open). RED against the current default-complete.
2. **`positive_token_present_is_complete`** — Asserts: the same clean read WITH the positive token ⇒ `factsComplete=true`. Why: positive-signal path.
3. **`each_unknown_more_results_field_degrades`** — parametrized over `hasMore`/`nextPageToken`/`nextOffset`/`pageInfo.hasNextPage` (each present-and-truthy, token otherwise present) ⇒ `factsComplete=false`. Why: §12 widened paging rejection (type-robust, Lesson 19).
4. **`existing_truncated_cursor_still_degrade`** — Asserts: `truncated`/`cursor` still degrade (regression). Why: no weakening of the existing pins.
5. **`stated_total_mismatch_is_incomplete`** — Asserts: token present, no more-results signal, but `total` (numeric) > returned rows ⇒ `factsComplete=false`; `total === rows` ⇒ complete. Why: row-count cross-check.
6. **`malformed_or_err_still_incomplete`** — Asserts: a dropped row / err / malformed envelope ⇒ `factsComplete=false` even with the positive token (a token can't rescue a malformed read). Why: Lesson 19 — coverage floor is unconditional.
7. **`build_projection_end_to_end_positive_and_degrade`** — Asserts: `buildReconcilerDbProjection` over a fake adapter yields `complete=true` on the full positive path and `complete=false` on each degrade lever. Why: the AND with `version` holds.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **none.** `ReconcilerDbProjection`/`DbFact` are unchanged; this hardens the internal parse, not a contract. The completeness/paging FIELD NAMES are candidate wire-shape (not an Appendix-A model).
- **Orchestrator doc rows to write hot (Step 9 routing):** likely an **Architecture-doc note candidate** (§6/§12: the reconciler DB-projection completeness contract is now positive-token / default-incomplete; the candidate paging fields are part of the Item-2a wire-shape arch_gap) + a worker **Lesson candidate** (extends 19). No contract table row.
- **Seam (shared-contract) model touched?** No. No schema-snapshot test.

## Things to flag at Step 2.5
1. **The POSITIVE completeness token field (candidate, arch_gap).** The real `gbrain serve --http` graph-read envelope shape is owner-gated (Item-2a arch_gap), so the positive token is candidate. Candidates: `env.complete === true`, or `env.hasMore === false` (a positive read of the hasMore field), or a `env.pageInfo.hasNextPage === false`. Default vote: **`env.complete === true` as the positive token, default-incomplete if absent**, documented as part of the SAME arch_gap (confirmed at arming). Fail-closed: an absent/ambiguous token ⇒ incomplete. Do NOT hardcode it as confirmed.
2. **Type-robustness of the positive token.** Should a truthy-non-`true` completeness value (e.g. `"yes"`, `1`) count as complete? Default vote: **strict `=== true` only** — mirror `stamped`'s conservative posture (Lesson 19): the DANGEROUS value on the coverage axis is a FALSE-complete, so require the exact positive signal; anything else ⇒ incomplete.
3. **The stated-total field name + comparison basis.** `total` vs `totalCount`; compare against the RAW returned row count (`env.facts.length`) or the well-formed count. Default vote: **check both `total` and `totalCount` (first finite number wins); compare against the RAW `env.facts.length`** — a stated total exceeding the raw returned count means more rows exist server-side (dropped-malformed is already caught separately by `allRowsWellFormed`). A non-numeric/absent total ⇒ no constraint (the token + more-results checks still apply).
4. **Updating the existing piece-B `complete=true` tests in-slice.** The flip changes the contract, so the existing default-complete cases must carry the positive token. Default vote: **yes, update them in-slice** — piece B is dormant (no production caller), the change is strictly-safer hardening, and the updated tests pin the new contract. Not scope creep.

## Dependencies + sequencing
- **Depends on:** Item 2a (`cdbb389` — established the candidate wire shape) + piece B (`8a27e78`).
- **Blocks:** the owner's ARMING binding (confirm the real completeness token + bind the real transport into `makeDbAdapter`). Closes Item 2. After 2b → **Item 6** (reconcile trigger-source/timing, only if cleanly dormant) → HOLD at the arming gate.

## Estimated commit count
**1 commit** — the completeness hardening. **Safety-critical** (the anti-false-green coverage contract — a false-complete is a trust-gate defeat) ⇒ **MANDATORY dual review at Step 8**.

## Lessons-logged candidates anticipated
- **Convention candidate (extends worker Lesson 19)** — a coverage/completeness contract fails closed with a POSITIVE token (default-incomplete), not a negative more-results probe (which fail-opens on an unknown/absent pagination field); widen the rejected-signal set + a stated-total cross-check; the exact token is a documented candidate until the real vendor surface is run (Lesson 21).
- **Architecture-doc note candidate (§6/§12)** — the reconciler DB-projection completeness is positive-token / default-incomplete; the paging fields are part of the Item-2a wire-shape arch_gap.
- **Future TODO — arming** — confirm the real gbrain graph-read completeness token + paging envelope when the real `gbrain serve --http` is run (folds into the Item-2a arch_gap confirmation).

## How to invoke
1. **Read this brief end-to-end** — especially Step-2.5 Q1 (the candidate positive token — do not hardcode as confirmed) and Q4 (updating existing tests in-slice is expected).
2. **Run `/tdd reconciler_projection_completeness_hardening`.**
3. **Step 2.5** — take/adjust the 4 defaults.
4. **Step 8** — MANDATORY security + code-quality dual review (anti-false-green coverage contract).
5. **Step 9** — surface the arch_gap (real completeness token) + the lesson/arch-note candidates.
