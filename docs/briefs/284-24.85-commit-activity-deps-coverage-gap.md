# /tdd brief — commit_activity_deps_coverage_gap

## Feature

Close `### 24.26` step 3's **surviving** residual: the **5** `createCommitActivity` test sites that pass
`deps: {} as never` take **ZERO coverage** from making `workspacePathCheck` required. `never` is assignable to
everything, so they do not go red, need no touching, and **cannot detect the flip**. That is a real
test-quality gap.

⛔ **THIS TASK WAS RE-SCOPED AND ITS SECOND HALF IS WITHDRAWN.** The withdrawn half claimed the flip "turns
omission into a throw" at these sites. **Zero of the nine can reach that throw.** Do not resurrect
the §16 never-throw exposure framing — it is not what remains. <!-- spec-lint:mention -->

## Use case + traceability

- **Task ID:** `### 24.85` (shared task `#49`)
  > ⭐ **The task id changed while this brief was being written, and the reason is worth thirty seconds.** This
  > residual had **no durable tracker home** — `### 24.26` is `[x] DONE`, so it lived only as session-scoped
  > `#49`, which dies on respawn (`L51`/`L97`). **`spec-lint` refused this brief for citing a completed task**,
  > which is how it was found. `### 24.85` is now its home. ⛔ **It was NOT filed as an unticked box under
  > `24.26`'s certified line — that is `L92`'s ruling.** `### 24.86` was filed in the same edit for `#63`, the
  > only other homeless residual (measured, not assumed: exactly two `[x] DONE` parents across the live set).
- **Architecture sections it implements:** `ARCHITECTURE.md §6`, `§19.11`
- **Safety:** not a safety slice. Standard Step-9 routing; ALL SEVEN rules still auto-route if you find one.
- **Depends:** `24.26` step 3 (LANDED `46e34ca8`) · **Blocks:** nothing

## ⭐ PREMISES RE-DERIVED AT HEAD — you inherit a verified population, not a claim

I re-measured before writing this, because this task's population was **corrected twice** and the correction is
the whole reason it exists. Verified at `ea8cafd4`:

```sh
grep -rn "deps: {} as never" --include="*.ts" packages apps > /tmp/never.txt   # 11 ROWS
```

⛔ **11 rows, 9 sites, 5 in scope. State the unit — the three numbers are different populations:**

| rows | what | disposition |
|---|---|---|
| 5 | `createCommitActivity` — `commit-activity-base-revision.test.ts:32`, `meeting-activities.test.ts:738 :756 :784 :802` | ⭐ **IN SCOPE** |
| 4 | `createProposeActivity` — `meeting-activities.test.ts:863 :874 :888 :907` | ⛔ **OUT OF SCOPE — different type** |
| 2 | `workspace-path-guard.test.ts:371 :383` — **comment text quoting the literal** | ⛔ **MENTIONS, not sites** (`L104`) |

> ⛔⛔ **ERRATUM 2026-08-14, AFTER THE SLICE LANDED — EVERY LINE NUMBER IN THE TABLE ABOVE IS NOW STALE, AND THEY DO NOT DANGLE. Numbers preserved, not rewritten** (`L148`/`L169`; this brief is also the record of what was dispatched).
> **The slice this brief commissioned was comment-only and added +32/+23 lines to those two files, moving every site it cites.** ⇒ **the citations now RESOLVE — to plausible-looking text.** `commit-activity-base-revision.test.ts:32` lands on **the slice's own new note, a sentence *about* the sites**; `meeting-activities.test.ts:756` on the `describe(` line; `:863` on an unrelated fake's comment; the rest on `deriveIdempotencyKey:` / `onDiskRevision:` / `externalObjectId:` / an `it(` title.
> ⭐⭐ **`### 24.71`'s thesis, committed by the brief that commissioned the slice that adopted it — and the SECOND instance in one session** (the first: the orchestrator's own stale line resolving into `### 24.71` itself). ⇒ **anchor by SYMBOL and `describe`-name; a line number in a durable document is a convenience with a half-life.**
> ⚠ **Reported by worker at `#49` Step 9, who flagged it and deliberately did not edit orchestrator territory. The sibling instance in `packages/knowledge/test/workspace-path-guard.test.ts` is a rule-4 Finding, routed to the lead and dispatched as `#78`.**
> ⛔ **Do NOT renumber these** — renumbering re-creates the decay on the next insertion above. **The population is the 5-in-scope / 4-out-of-scope / 2-mentions split, which is stable; the line numbers were only ever how it was located.**

**I verified the type split at source rather than inheriting it** — each in-scope line sits under a
`createCommitActivity({` call, each out-of-scope line under `createProposeActivity({`. Controls reported:
`createCommitActivity` → 9 files (non-vacuity ✓), bogus token → 0 rows (discriminating ✓).

⛔ **The 4 `createProposeActivity` sites take `ExternalWriteDeps`, which has NO `workspacePathCheck` field at
all. They are unrelated to the flip and are NOT uncovered by it. Do not "fix" them here.** Grouping all nine by
the literal `deps: {} as never` grouped by **SPELLING, not by TYPE** — that is the error this task was re-scoped
to undo, and re-committing it is the one way to fail this slice.

## ⛔⛔ THE LOAD-BEARING DESIGN QUESTION — THE FIRST DONE-WHEN OPTION MAY BE VACUOUS BY CONSTRUCTION

The recorded Done-when offers two options: **(1)** supply a real `workspacePathCheck` so the sites exercise what
they claim, or **(2)** carry an in-file note that they deliberately opt out and what that costs (`L82`).

⛔ **Option (1) may not be reachable, and the reason is in the task's own text:** each of the 5 sites injects a
**FAKE `applyPlan` that never dereferences `deps`.** If the fake short-circuits before anything reads
`deps.workspacePathCheck`, then supplying a real one **changes nothing observable** — the site still cannot
detect the flip, and you will have produced a diff that reads as coverage while buying none
(`L82`'s forbidden middle; `L85` — mirroring a real guard in a double buys discrimination for the double, never
coverage of the thing it stands in for).

⇒ **DO NOT ASSUME OPTION (1) WORKS. Establish it by mutation, at Step 2.5, before implementing:**

> Supply a real `workspacePathCheck` at ONE site. **Then break the guard and confirm that site goes RED.**
> - **Goes red** ⇒ option (1) is real; do it for all 5.
> - **Stays green** ⇒ option (1) is vacuous here, and the honest disposition is option (2) — say so and take it.

⭐ **This is `L70`/`L75` applied to the remedy rather than the defect: verify the PROPERTY (does this site now
detect the flip?), never the MECHANISM (does this site now pass a real value?).** A green suite after option (1)
is exactly what both outcomes look like.

⚠ **If reaching option (1) requires replacing the fake `applyPlan`, that is a different slice and a scope
question — flag it, do not absorb it.** Changing what these tests drive changes what they test.

## Acceptance criteria

- [ ] **The mutation result is reported at Step 2.5, before implementation** — did a real `workspacePathCheck`
      at one site make it RED when the guard was broken? Yes/no, with the mutation shown.
- [ ] **All 5 in-scope sites** are dispositioned — either genuinely exercising the flip (mutation-proven), or
      carrying an in-file note stating the opt-out **and its cost** (`L82`: an uncovered gap is visible and
      invites work; a falsely-covered one closes the question).
- [ ] ⛔ **The 4 `createProposeActivity` sites are untouched**, and the slice says so explicitly.
- [ ] ⛔ **No fallback is re-added.** Deleting the `??` fallback and the constant IS `24.26`; re-introducing one
      to make a test convenient reverses the task that this residual belongs to.
- [ ] The 2 comment mentions in `workspace-path-guard.test.ts` are **left alone** — they document this finding;
      editing them makes the record assert its own falsity (`L99`).
- [ ] `/preflight` clean; repo-wide `tsc` run (`L129` — a green vitest run is NOT a typecheck).

## Wiring / entry point (Step 7.5)

**No production surface changes.** This is test-quality work on existing, already-reachable
`createCommitActivity` call sites. Nothing new becomes reachable; no new export.

## Files expected to touch

**Modified:** `packages/workflows/test/commit-activity-base-revision.test.ts` (`:32`) ·
`packages/workflows/test/meeting-activities.test.ts` (`:738 :756 :784 :802` only).
⛔ **Not touched:** `meeting-activities.test.ts:863 :874 :888 :907` · `packages/knowledge/test/workspace-path-guard.test.ts`.

## RED test outline (Step 2)

⚠ **This slice's subject IS the tests, so the usual red-first shape inverts** — you are not adding a behaviour
pin, you are establishing whether existing pins discriminate. **The mutation IS the red.**

1. **Mutation probe (the deciding measurement)** — supply a real `workspacePathCheck` at one in-scope site,
   break the guard, observe red/green. **Report the observed state and the count of tests run** (`L75`: a file
   that did not execute and a file whose tests all passed are indistinguishable from the pass/fail line alone).
2. **Per-site disposition** following the probe's verdict.
3. **A control confirming the mutation was OBSERVED** — break something the suite demonstrably DOES catch, watch
   it go red, then apply the real mutation. A compromise that changes nothing is indistinguishable from one
   never applied.

## Cross-doc invariant impact

**None expected** — no model field added/removed/renamed, no Appendix-A seam touched. Flag at Step 9 if the
mutation probe surfaces one.

## Things to flag at Step 2.5

1. ⛔⛔ **The mutation result. This is the review surface and I will not approve without it.** Option (1) vs
   option (2) is decided by that measurement, not by preference.
2. **If option (2):** what exactly does the in-file note say the opt-out costs? My default vote: name the
   specific guarantee not being exercised, not a generic "this is a fake" — a note that does not say what is
   uncovered will be deleted by the next reader as noise (`L153`/`L161`: understating a guard invites deletion).
3. **Are the 5 sites one population for the REMEDY, even though they are one population for the TYPE?**
   My default vote: **check before assuming.** `commit-activity-base-revision.test.ts:32` is a shared `port()`
   helper serving many cases; the 4 in `meeting-activities.test.ts` are inline. Same type, possibly different
   remedy — and this task has already been burned once by a population that looked uniform.
4. **Anything that makes you doubt the withdrawn half was correctly withdrawn** — say so. It was withdrawn on a
   premise-correction, and I would rather re-open it at 2.5 than discover it was right.

## Dependencies + sequencing

- **Depends on:** `24.26` step 3 (landed `46e34ca8`). No live blockers.
- **Interacts with `#63`/`24.84`:** both concern *a required parameter type-checks PRESENCE, never the VALUE.*
  ⛔ **Do not fix `#63` here** — it is a separate task with a contracts-first sequencing question still open.

## Estimated commit count

**1.** Single-area, single-concern, no safety invariant. If the mutation probe forces a scope question
(replacing the fake `applyPlan`), that is a **separate slice**, not an extension of this one.

## Lessons-logged candidates anticipated

- **Convention candidate** — a remedy whose first Done-when option is vacuous by construction, discoverable only
  by mutating it (i.e. `L82`'s make-it-true / retract-it choice where "make it true" is itself unavailable).
- **Convention candidate** — a census whose row count, site count, and in-scope count are three different
  numbers, two of which are mentions rather than uses.

## How to invoke

1. Read this brief end-to-end. ⛔ **The mutation probe comes before any disposition, and its result comes to me
   at Step 2.5.**
2. Run `/tdd commit_activity_deps_coverage_gap`.
3. **Step 9** — standard routing; escalate to me + `team-lead` if any of the seven safety rules is touched.
