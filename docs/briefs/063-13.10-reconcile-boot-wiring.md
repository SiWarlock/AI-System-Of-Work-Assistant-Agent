# /tdd brief — reconcile_boot_wiring

## Feature
Build **piece F2** of the reconcile-TRIGGER arc (task 13.10) — the **composition-root binding** that closes the arc's reachability chain: the `bootWorker` `gateReconcile(...)` call site + a `BootConfig.reconcile` owner opt-in field + the exposure of the returned `ReconcileWiring` on `BootedWorker` + the **real leaf-thunks** the gate assembles on the armed path (the LOCAL committed-vault reader; the gbrain adapter over the **UNBOUND** owner-gated transport ⇒ `makeDbAdapter → undefined`; the recorder over `backends.repos.parityReports`; the passDeps with real `reconcilerDeps` + a `HealthSurface`-backed `ReconcileHealthSink`; the **redacted log sink that materializes the `skipped_derive_error`/ws-mismatch HealthItem from the safe `detail` code** — constraint c). Ships **DORMANT** — `reconcile` is unset by default ⇒ `gateReconcile` returns `undefined` ⇒ boot is **byte-equivalent** (no reconcile scheduler/driver constructed). This is the **arming-seam's concrete binding** and the arc's LAST DORMANT-buildable piece; the actual trigger **source/timing** binding + the transport provisioning + the flag flip are the **owner's ARMING bundle** (the HARD LINE — orch17 escalates after this lands). **NO hard line crossed — nothing armed, transport unbound.**

## Use case + traceability
- **Task ID:** 13.10 (Phase 13 — the reconcile-TRIGGER arc, **piece F2** — the composition-root gate binding; the LAST dormant-buildable piece before the ARMING GATE).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (the reconcile boot activation — the composition-root binds the gate + the real leaf collaborators, default-OFF) + `§12` (the parity/serving GO conditions — the bound path degrades until the owner-gated transport is provisioned; never a false-green at boot). Both ∈ Phase 13 Spec anchors — **no scope widen**.
- **Related context (reused, NOT modified):**
  - **The gate helper** (piece F1, `e9c6d77`, `apps/worker/src/boot.ts`): `gateReconcile(opts: ReconcileGateOpts, deps: ReconcileGateDeps): ReconcileWiring | undefined` — OFF ⇒ `undefined` + zero dep-thunk calls; ON ⇒ assemble the scheduler+driver over the never-reject builders. F2 supplies the REAL `deps` thunks + the `opts` from `BootConfig` + the `bootWorker` call.
  - **The boot precedent** (`gateAutoIngest` call site in `bootWorker`, `apps/worker/src/boot.ts`): mirror how the auto-ingest wiring is gated + spread — the `reconcile` opts thread from env → `WorkerHostConfig` → `BootConfig` (owner opt-in, default absent), and `gateReconcile` is called AFTER `backends` is built (the `parityReports` repo exists then).
  - **The real leaf collaborators** (all landed): `createCommittedVaultReader({resolveVault})` (`servingContextBootReaders.ts:49`, LOCAL fs); `createParityReportRecorderAdapter(backends.repos.parityReports, now)` (`parityReportStore.ts:103`); the `HealthSurface` (`apps/worker/src/health/surface.ts`, `createHealthSurface(store)` — `record(HealthFailure)` mints/dedupes the item); `redactError` (`@sow/domain`). The gbrain adapter transport is **OWNER-GATED/UNBOUND** ⇒ `makeDbAdapter → undefined`.
  - **Layering:** worker composition-root (`boot.ts`) — the ONE place `@sow/db` + the serving/scheduler layers are all visible. No knowledge/db edit.
  - The ⭐ reconcile-TRIGGER Carry-forward (pieces A–F + the E/F constraints); `docs/runbooks/copilot-propose-go-live.md`.

## Acceptance criteria (what "done" means)
- [ ] A `BootConfig.reconcile?: boolean` owner opt-in field (+ any minimal precondition field, e.g. `reconcileWorkspaceId?`), default absent — mirror `AutoIngestGateOpts`. The shipped default leaves it unset.
- [ ] **`bootWorker` calls `gateReconcile(reconcileOpts, deps)`** AFTER `backends` is built, with `deps` = the real leaf-thunks: `makeReader` → `createCommittedVaultReader`; `makeDbAdapter` → **`undefined`** (transport owner-gated/unbound); `makePassDeps` → `{ reconcilerDeps, recorder: createParityReportRecorderAdapter(backends.repos.parityReports, now), healthSink }`; `makeLog` → the redacted+health-materializing sink. The returned `ReconcileWiring | undefined` is exposed on `BootedWorker` (so the arming-era source/timing binding can reach the scheduler).
- [ ] **Byte-equivalent by default (THE pin):** with `reconcile` unset (shipped default), `gateReconcile` returns `undefined` ⇒ `bootWorker` constructs NO reconcile machinery (no reader/adapter/recorder/scheduler for reconcile) and the boot result is unchanged from today. Asserts: the default boot path is byte-equivalent (a boot-level test with `reconcile` absent ⇒ no reconcile wiring; the leaf-thunk factories not invoked).
- [ ] **Armed path degrades, never false-green (arming precondition holds):** with `reconcile: true`, `gateReconcile` assembles the wiring; `makeDbAdapter → undefined` ⇒ `getDbProjection` degrades (`complete=false`) ⇒ a driven reconcile records `coverageComplete=false` (no false-green). Asserts: the armed-path recorded report degrades.
- [ ] **Redacted + health-materializing log sink (constraint b/c):** the `makeLog` sink logs the redacted summary AND, on a `skipped_derive_error`, mints a HealthItem via the `HealthSurface` from the **safe `detail` code** (never the raw error). Asserts: the raw error never reaches the surface; a HealthItem is minted from the code.
- [ ] **Reachability closure (Step-7.5):** `/wired` reports `bootWorker → gateReconcile → (armed) scheduler → driver → builders` reachable (closing F1's waiver). The armed path is dormant (never taken by default) but reachable-by-construction from the boot call.
- [ ] Ships **DORMANT** — `reconcile` unset by default; the owner-gated transport + `RebuildOracleSet` + the source/timing binding stay unbound; **NO hard line crossed** (nothing armed). This slice does not arm anything.
- [ ] All unit tests pass; repo-wide `pnpm -w turbo run typecheck test` green; `/preflight` clean.
- [ ] **MANDATORY Step-8 dual review = security-reviewer + code-quality-reviewer** — the composition-root arming binding is where a byte-equivalence slip or a mis-bound transport is hard-line-adjacent. Safety-critical; own commit.

## Wiring / entry point (Step 7.5)
`bootWorker (apps/worker/src/boot.ts)` — the `gateReconcile(reconcileOpts, deps)` call is the production entry point; on the shipped default (`reconcile` unset) it returns `undefined` (byte-equivalent, dormant). This CLOSES F1's reachability waiver. **The arming (flip `reconcile`/`goLiveArmed`, provision the `GbrainReadGrant` transport + signing key + real corpora + the governance eval, AND wire the trigger source/timing to the exposed scheduler) is the owner's HARD-LINE bundle — NOT in this slice.**

## Files expected to touch
**Modified:**
- `apps/worker/src/boot.ts` — the `BootConfig.reconcile` field, the `bootWorker` `gateReconcile(...)` call + the real leaf-thunks + the redacted/health-materializing `makeLog` sink, and the `BootedWorker` exposure of the returned wiring. (If threading `reconcile` from env → `WorkerHostConfig` needs `main/index.ts`, flag at Step 2.5.)
- `apps/worker/test/...` — extend the boot test (or a new `reconcileBootWiring.test.ts`): the byte-equivalent-default pin + the armed-path degrade + the health-materializing sink.

If implementation needs files beyond this list, **flag at Step 2.5** before GREEN.

## RED test outline (Step 2)
1. **`default_boot_no_reconcile_wiring`** — `reconcile` unset ⇒ `bootWorker` produces no reconcile wiring; the reconcile leaf-thunks are not invoked (byte-equivalent). Why: Lesson 8/23 — the shipped default constructs nothing.
2. **`armed_boot_assembles_dormant_wiring`** — `reconcile: true` ⇒ a `ReconcileWiring` is exposed on `BootedWorker` (the scheduler present); the leaf-thunks ran. Why: §6 the armed assembly (dormant — no source drives it yet).
3. **`armed_path_degrades_not_false_green`** — driving the assembled scheduler (unbound `makeDbAdapter`) records `coverageComplete=false`. Why: §12 the owner-gated transport keeps it degraded.
4. **`derive_error_mints_health_from_safe_code`** — a `skipped_derive_error` through `makeLog` mints a HealthItem via the `HealthSurface` from the safe `detail` code; the raw error never reaches the surface. Why: constraint b/c (safety rule 7).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **none** — `BootConfig.reconcile` is a boot-config field (not an Appendix-A model). Reuses the frozen models + the arc seams.
- **Orchestrator doc rows to write hot (Step 9 routing):** **none expected.** An `ARCHITECTURE.md §6` note that "the reconcile-TRIGGER arc is complete-dormant (F2 closes the reachability); the arming — flip + provision transport/key/corpora/eval + wire the source — is the HARD LINE" is an **Architecture-doc note candidate** — flag it (I fold the arc-complete state + the runbook at the round-seal).
- **Shared-contract seam (Appendix-A model) touched?** No.

## Things to flag at Step 2.5
1. **The `skipped_derive_error`/ws-mismatch HealthItem `FailureClass` (constraint c).** The `HealthFailure` needs a `failureClass` for a canonical-derive/parity defect. **Default vote:** reuse the reconcile-defect class the reconciler already emits (`parity_defect`), subjectRef = `workspace‖revision`, message = a safe line from the `detail` code (never the raw error). If no clean member fits, use the least-wrong + an `arch_gap` marker (precedent: the FailureClass taxonomy gaps flagged in prior slices) and flag it as a frozen-contract candidate — do NOT invent a new `FailureClass` here. Flag your read.
2. **Source/timing binding — deferred to arming vs wired now.** **Default vote: deferred** — F2 exposes the `ReconcileWiring` (scheduler) on `BootedWorker`; the actual trigger source (`startVaultWatcher`/schedule/post-commit → `enqueue`) + the flush timing (→ `flush`) bind at the owner's arming (bundled with the flag flip + transport provisioning), since a source that drives the scheduler is only useful once armed + provisioned. This keeps F2 the clean composition-root gate binding + the arc dormant-buildable-complete. Flag if you'd rather wire a dormant source now (I'd defer — it's arming-era + naturally the owner's).
3. **Byte-equivalent-default pin shape.** **Default vote:** a boot-level test asserting the default (`reconcile` unset) path invokes none of the reconcile leaf-thunk factories + exposes no wiring (mirror F1's factory-spy pin at the boot level). Confirm the seam is test-reachable without a full real boot (inject the leaf-factories as spies).
4. **Env → `BootConfig.reconcile` threading.** **Default vote:** thread `reconcile` the same path as `autoIngest` (env → `WorkerHostConfig` → `BootConfig`); if that touches `main/index.ts`, keep it minimal + flag it (it's owner opt-in plumbing, default-off).

## Dependencies + sequencing
- **Depends on:** piece F1 (`gateReconcile`, `e9c6d77`) + all of A–E; the real leaf collaborators (all landed). No unlanded dependency.
- **Blocks:** **the ARMING GATE** (the HARD LINE). After F2, the reconcile-TRIGGER arc is **complete-dormant** — orch17 escalates the arming (flip + provision transport/key/corpora/eval + wire the source) to the lead + owner. Do NOT arm.
- **Owner-gated (out of scope, unbound):** the live `GbrainReadGrant` transport + the real `RebuildOracleSet` + the trigger source/timing + the arming flags — all stay unset/unbound; the owner's at the arming gate.

## Estimated commit count
**1.** One safety-critical composition-root binding — own commit; the byte-equivalent-default pin is load-bearing.

## Lessons-logged candidates anticipated
- **Convention candidate** — "The composition-root arming binding calls the default-OFF gate with the REAL leaf-thunks (the LOCAL reader; the owner-gated transport UNBOUND ⇒ makeDbAdapter undefined ⇒ armed-path degrades; the recorder over the ops-store repo; a HealthSurface-backed sink that materializes the HealthItem from the safe code); byte-equivalent by default (reconcile unset ⇒ the gate returns undefined ⇒ no machinery); the source/timing binding + transport provisioning + the flag flip are the owner's arming bundle." (Likely folds into Lesson 23 as F2's completion, or its own.)
- **Architecture-doc note candidate** — an `ARCHITECTURE.md §6` line: "the reconcile-TRIGGER arc is complete-dormant (F2 closes the reachability); the arming is the HARD LINE."
- **Future TODO — the ARMING GATE (owner-gated, HARD LINE)** — `goLiveArmed`/`reconcile` flip + provision the `GbrainReadGrant` transport + signing key into Keychain + real corpora + the propose-path governance eval (coordinate eval-security) + wire the trigger source/timing + the owner-confirmed flip. orch17 escalates to the lead + owner after this slice lands.

## How to invoke
1. **Read this brief end-to-end** — Step-2.5 #1 (the HealthItem FailureClass) + #2 (source/timing deferral) want your read before tests.
2. **Run `/tdd reconcile_boot_wiring`** in the implementer session.
3. **Step 0 (Restate)** — confirm: the composition-root gateReconcile binding, byte-equivalent by default, DORMANT, NO arming.
4. **Step 1 (Identify files)** — confirm boot.ts + the boot test.
5. **Step 2.5** — ping back the header with answers (or take defaults).
6. **Step 8** — MANDATORY dual review (security + code-quality); safety-critical (the composition-root arming binding).
7. **Step 9** — surface anything beyond the anticipated candidates. **Do NOT arm** — after the commit, the arc is complete-dormant and orch17 escalates the arming gate.
