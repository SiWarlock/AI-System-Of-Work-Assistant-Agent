# /tdd brief — validation_refusal_shape_merge

## Feature
Execute `### 24.103`'s **approved** remedy — ***merge the SHAPE, not the unions***. Extract the duplicated issue-carrying refusal shape into **one** type that **REQUIRES** an `AuditSignal`, and let each union keep its own vocabulary. The compiler then enumerates the population the census derived, at the exact boundary that defeated it, **without** collapsing four layers' error vocabularies.

⛔ **THE REMEDY IS DECIDED. DO NOT RE-DERIVE IT OR RE-OPEN THE ALTERNATIVES.** Lead-approved 2026-08-17 on three recorded grounds: it restores the compiler enumeration at the union boundary · it makes the hazard **unrepresentable at every census site** rather than patched at one · it beats five per-gate signals, **three of which would serve dormant gates**.

## Use case + traceability
- **Task ID:** `### 24.103`
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KnowledgeWriter / candidate-data gate), `§16` (candidate-data gate, redaction, audit)
- ⛔ **This brief widens phase scope because** `§16` is where redaction and the audit path live and is **not** in Phase 24's declared set (`§13 §19.11 §3 §4 §6`), while `### 24.103`'s own `Spec:` line cites it. Declared per `spec-lint` option (a). ⛔ **NOT resolved by widening Phase 24's anchor set** — standing lead ruling 2026-07-29; the systemic defect's durable home is `### 24.54`, which is **enriched, never duplicated** (`### 24.93`/`### 24.98`/`### 24.99`/`### 24.101` are the four witnesses).
- ⛔⛔ **SAFETY: rule 7 AND rule 1.** Rule 7 by kind (a refusal producing no audit record). **Rule 1 by surface** — the live channel is on the **sole-writer path**: `applyPlan` → production caller `packages/workflows/src/activities/commitKnowledge.ts:156`. **Step 9 auto-routes to the lead on both counts.** Reachability governs disposition, never routing.
- **Related context:** `### 24.98` (`124e3f45` — the pattern this generalizes) · `### 24.104` (the `e.params` drop this rests on) · `### 24.105` (the `cause` propagation bound) · `L192` · `L194` · `L196` · brief `293` · **brief `294` (SUPERSEDED — see below)** · session doc `175`

### ⛔ Brief `294` is SUPERSEDED, not refreshed — read why before reading it
`294` scoped this task to `generative-proposal-intake.ts` because that is what the task entry named. **Its own Step-2.5 census then measured the population at 5 channels and found the named target is the DORMANT one** (`intakeGenerativeProposal` has **zero production callers** — `block-provenance.ts:9` says so in terms). The slice was correctly **paused**, not shipped. ⭐ **The asymmetry is the finding: the entry named the harmless member and the census found the live risk it never mentioned.** ⇒ ***building `294` as written would have been motion on the harmless member — `L82`'s partial fix wearing a completion badge, on a rule-1 surface.*** Read `294` for its rule-7 reasoning (which transfers); **ignore its scope**.

---

## ⛔⛔ THE PREMISE, MEASURED AT HEAD `46ef6c03` BY THE ORCHESTRATOR — NOT INHERITED FROM THE CENSUS

The census recorded **channels**. The compiler will enumerate **construction sites**, and that is the number that governs the work. Re-measured at HEAD; **line numbers drifted from the census by 3–8 and are given as a starting point, not as truth** — locate by symbol (`L101`/Carry-forward 8's de-inlining remedy, which this round demonstrated on a live case).

| # | channel | file | issue-carrying member(s) | construction sites | audit? | live? |
|---|---|---|---|---|---|---|
| 1 | `SchemaRejected` → folded into `WriteFailure` | `src/knowledge-writer/writer.ts` | `schema_rejected` — stages **`ajv` \| `zod` \| `scoped`** | **3** (`~:643`, `~:656`, `~:670`, all in `runGate`) | NO | ⭐ **LIVE — rule 1** |
| 2 | `IntakeError` | `src/gbrain/remediation/generative-proposal-intake.ts` | `schema_rejected` (ajv, zod) **+ `plan_invalid`** | **3** (`~:134`, `~:140`, `~:189`) | NO | ⛔ DORMANT (0 prod callers) |
| 3 | `RemediationError` | `src/gbrain/remediation/router.ts` | `plan_invalid` — stages **ajv \| zod \| scoped** | **3** (`~:245`, `~:254`, `~:266`) | NO | — |
| 4 | `StampError` | `src/knowledge-writer/provenance-stamp.ts` | `stamp_invalid` | **1** (`~:234`) | NO | — |
| — | `GclGateError` | `src/gcl/visibility-gate.ts` | `schema_rejected` | 2 (`~:251`, `~:268`) | ✅ **YES** | **the discriminating control** |

⭐ **`GclGateError` resolving `audit=true` post-`### 24.98` is what proves the selector separates covered from uncovered. Keep using it as the control; a census whose predicate cannot distinguish the one fixed channel is not measuring anything.**

**Two corrections to the census the brief carries forward:**
- ⛔ **`writer.ts` and `router.ts` each carry a THIRD stage — `"scoped"` — that the GCL channel does not have.** Any signal `event` string or stage-keyed logic copied from `### 24.98` covers **two of three** stages and will look complete. `visibility-gate.ts`'s `schemaRejectedSignal` is typed `stage: "ajv" | "zod"` — **it does not even accept the third case.**
- ⛔ **`IntakeError` carries a SECOND echoing member, `plan_invalid`, which `### 24.103`'s entry never named** (session `175` flagged this). **Four uncovered channels, ~10 construction sites** — not five channels, one site each.

---

## ⛔⛔ THE FINDING THAT MOST CHANGES THIS SLICE — THE DECISION TRANSFERS, THE REGION DOES NOT

**`### 24.98`'s DECISION transfers verbatim and must NOT be re-derived** (it was measured once; the measurement lives in `### 24.98` and `L192`): **structural material only** — `code`, `stage`, **cut** paths, counts — with **`message` excluded CATEGORICALLY**, because Zod's `invalid_enum_value` embeds the received value and `unrecognized_keys` embeds a row-authored key.

⛔⛔ **BUT ITS `ROW_KEYED_REGION_CUT` DOES NOT TRANSFER, AND COPYING IT IS THE TRAP THIS BRIEF EXISTS TO PREVENT.**

`visibility-gate.ts` cuts paths at **`sanitizedPayload`** — a field of the **GCL projection**. ⭐ **That token appears in NONE of the four uncovered channels' candidate types.** ⇒ ***a copied regex compiles, passes review, runs on every rejection, and cuts NOTHING — while the code reads exactly like the construction that made the GCL channel safe.***

⚠ **This is precisely this round's *decorative assertion* class** — three `isRedactionSafe` assertions used a sentinel matching none of the heuristic's patterns, so they "asserted that a detector failed to detect something it was never going to detect." **A cut that cannot fire is the same defect in production code rather than in a test.**

**The orchestrator's cross-check, with its method and boundary stated so you can disagree with it:** walking `packages/contracts/schemas/knowledge-mutation-plan.schema.json` programmatically for `additionalProperties ∉ {false, absent}` and `patternProperties` returns **exactly two** free-form-key regions — **`/creates[]/frontmatter`** and **`/externalActionProposals[]/payload`**. ⚠ **BOUNDARY, and it is doing work: that is the AJV surface ONLY.** The Zod model delegates to `NoteCreateSchema` / `ProposedActionSchema` / `FrontmatterPatchSchema` and **was not walked**; Zod also emits a different path dialect (`a.b` vs ajv's `/a/b`), which `### 24.98` handles deliberately. **`frontmatterUpdates[]` is NOT a cut region** — measured `additionalProperties: false`, and its row-authored key is a **value** (`key: string`), never a path segment.

⛔ **Treat that as a cross-check to reconcile against, NOT as the answer.** Derive each channel's region set from **its own** candidate schema, on **both** validator surfaces, and **state what your method could have returned that mine could not** (029's concordance rule: *concordance is evidence only when the methods could have disagreed*).

---

## Acceptance criteria (what "done" means)

- [ ] ⛔ **ONE shared issue-carrying type exists, it REQUIRES an `AuditSignal`, and the four uncovered channels' issue-carrying members are expressed in terms of it** — each union keeping its own `code`/`stage` vocabulary. **The unions are NOT merged.**
- [ ] ⛔ **The compiler enumerates the population**: making the shape's `audit` required must produce a **compile error at every construction site that lacks one**. **Demonstrate this** — record the error count from a real compiler run before the sites are fixed. ⚠ **That count is the census's independent corroboration, and it is the one instrument here that could have disagreed with the hand-derived table above.**
- [ ] ⛔ **`writer.ts`'s three sites and `router.ts`'s three sites are ALL covered, including the `"scoped"` stage** — the one `### 24.98`'s helper cannot even accept.
- [ ] ⛔ **`IntakeError.plan_invalid` is covered**, not just its `schema_rejected` — the member the task entry never named.
- [ ] ⛔ **Every signal is redaction-safe BY CONSTRUCTION, not by detection**: no field derived from any issue `message` or any other row-derived string. **The predicate that selected what may cross is stated in-file.**
- [ ] ⛔ **Each channel's row-keyed path cut is derived from ITS OWN candidate schema and is MUTATION-PROVEN TO FIRE** — disable the cut and **exactly that channel's pin reds**. ⚠ **A cut that cannot be made to red is cutting nothing** (the copied-regex trap above). ⛔ **A mutation producing GREEN is ambiguous — prove the mutation applied, by diff, for every green outcome** (029; and check first whether the subject is even an input to the assertion — if it is not, the pin is vacuous and no mutation could ever have applied).
- [ ] ⛔ **The refusal is NOT weakened anywhere.** Every candidate that is refused today stays refused, with the same code and stage. It stops being refused *silently*. **Pinned per channel.**
- [ ] **Every new/changed safety pin asserts WHY it failed (cause/code + stage), never a bare falsity** (`### 24.101`).
- [ ] ⛔ **Consumer honesty — the scoped claim, never the unqualified one.** Where a channel's signal has **no** persist adapter at this layer, write *"produced and gated; no adapter persists it, see `<task>`"* and **name the consumer slice as a real tracked task**. ⛔ **Never *"this gate is now audited."*** ⚠ **Applies per channel** — the four differ, and one honest sentence covering all four would be false for at least one.
- [ ] ⛔ **`WriteSuccess`/`WriteFailure`'s exhaustive mappers still compile and still discriminate.** `writer.ts:336` records that a new `WriteFailure` member is a deliberate compile error in **three** places including `mapWriteFailure` + `commitFailureClass`/`commitFailureState` and `apps/worker`. ⚠ **This slice changes a member's SHAPE, not the member SET — confirm that distinction holds rather than assuming it**, and report what the mappers did.
- [ ] ⛔ **No `apps/worker` or `packages/domain` file is edited from this slice.** Cross-area findings are **flagged at Step 9**, not reached across (`### 24.104` is the standing example — knowledge flagged it and correctly did not edit `packages/domain`).
- [ ] `/preflight` clean, **and every measurement stated on two surfaces that could have disagreed.**

---

## ⛔ Instrument rules binding this slice (029's list — all verified live this round, several on this orchestrator's own first two commands)

- **`turbo` replays a cached green in ~55ms without executing.** Always `--force`; **quote the `Cached:` line.** ⚠ Its totals **include CANCELLED tasks**.
- **`npx` is INTERMITTENTLY intercepted**, returning fabricated success (`TypeScript: No errors found` for `--version`). ⛔ **A single clean probe proves nothing.** Confirm through `node_modules/.bin/*` or `pnpm` and **state both** (the two-surface requirement).
- **`vitest run --dir <path>` silently does not scope** — it runs the whole monorepo. ⛔ **A monorepo run reported as a package measurement WOULD HAVE AGREED with the run it was meant to corroborate.**
- **`grep` fabricates match counts** (reproduced 5× last round; **and again on this orchestrator's first command this session** — a `-n` query returned a `"37 matches in 2 files"` header). **`awk` piped from `xargs` reports CUMULATIVE `NR`** — use `FNR`, or single-file `sed`.
- ⛔⛔ **`git` IS WRAPPED IN THIS CHECKOUT.** `--porcelain` returns the literal `ok` (**reproduced this session**); `show -U0` has duplicated its own diff. ⛔ **Verify line counts with `--numstat` or the commit's own summary — NEVER by counting `+`/`-` from a diff body.** Use `git diff HEAD --stat` + `git ls-files --others --exclude-standard` for tree state.
- ⛔ **Never `git commit --amend`.** ⛔ **Commit with `git commit -F <msgfile> -- <explicit paths>` ONLY** — the git index is **shared across sessions**, so `git add … && git commit` commits *the index*, i.e. everything any session has staged. **The pathspec form never consults the index.** ⚠ **It is cheapest exactly where it feels least necessary** — last round a one-file rename swept two foreign files while a 17-file landing stayed clean.
- ⛔ **A check only gates if a DECISION sits between reading it and acting.** Do not collapse a staged-set check and its `git commit` into one invocation — that converts the gate into a receipt (`L109`).

---

## Wiring / entry point (Step 7.5)

**Live path, and it is the one that matters:** `applyPlan` (`src/knowledge-writer/writer.ts`) ← `packages/workflows/src/activities/commitKnowledge.ts:156`. `runGate` is called *inside* `applyPlan` (step 2 of its documented order).

⛔⛔ **THE PROPAGATION BOUND — READ IT AS WRITTEN, IT IS DELIBERATELY NARROWER THAN IT LOOKS.** `commitKnowledge.ts:161-165` returns `{ code, message: \`…${result.error.code}\`, cause: result.error }`. ⭐ **The `message` interpolates ONLY the closed literal `code` — safe, and deliberate.** ⛔ **But `cause` carries the ENTIRE `WriteFailure`, `issues[]` included, with the validator-authored `message` inside** — `### 24.62`'s shape, a second data channel riding beside a gated one.

⭐ **ESTABLISHED: `cause` propagates the object. NOT ESTABLISHED: whether anything logs, serializes or persists it unredacted.** ⛔ **`### 24.105` already owns that question and the trace found NO leak. DO NOT re-open it here and DO NOT let this slice's scope grow into it.**

⇒ ⭐⭐ **THE FRAMING THAT MAKES THIS SLICE RIGHT, AND IT MUST SURVIVE INTO YOUR STEP-2.5: THE HAZARD IS BUILD-TIME, NOT A RUNTIME LEAK.** ***The risk is that whoever adds the missing signal REACHES FOR `issues`, because that is what is sitting there.*** **A shared issue-carrying type that REQUIRES an `audit` makes that reach impossible; four per-gate signals leave it available three more times.** ⚠ **If you find yourself justifying this slice by a live leak, you have the wrong argument and should say so at Step 2.5.**

---

## Files expected to touch

**New:**
- One module for the shared shape — **default `packages/knowledge/src/audit/validation-refusal.ts`; exact path is yours to settle at Step 2.5.** ⭐ **Rationale for keeping it in `packages/knowledge`, so you don't have to re-derive it:** all four consumers plus the control live in `packages/knowledge/src`; `AuditSignal`/`buildAuditSignal` are imported from `packages/policy`; and `GateIssue` is currently exported from `visibility-gate.ts` but **consumed only by that file and its test** (measured) — so the merge needs **no cross-area dispatch**. ⛔ **If your Step-2.5 analysis says it belongs in `contracts` or `policy`, SAY SO AND STOP — that is a cross-area call and routes to the orchestrator, not a thing to build.**

**Modified (knowledge territory only):**
- `packages/knowledge/src/knowledge-writer/writer.ts` — `SchemaRejected` + `runGate`'s three sites ⛔ **rule-1 surface**
- `packages/knowledge/src/gbrain/remediation/router.ts` — `RemediationError.plan_invalid` + three sites
- `packages/knowledge/src/gbrain/remediation/generative-proposal-intake.ts` — `IntakeError.schema_rejected` **and `plan_invalid`** + three sites
- `packages/knowledge/src/knowledge-writer/provenance-stamp.ts` — `StampError.stamp_invalid` + its site
- `packages/knowledge/src/gcl/visibility-gate.ts` — **re-express `GclGateError.schema_rejected` in terms of the shared shape.** ⚠ **Behaviour must not change — it is the discriminating control.** ⛔ **If re-expressing it would change its behaviour, DO NOT — flag it instead; a control you modified is no longer a control.**
- the corresponding `packages/knowledge/test/*.test.ts` pins

⚠ **Flag at Step 2.5 if the compiler pulls in files beyond these** — that is exactly the enumeration working, and it is a finding, not a nuisance. **`### 24.98` found a third construction site nobody had listed, in a TEST file.**

---

## ⛔ Sequencing inside the slice — this is not a preference

⭐ **Do `writer.ts` FIRST — `SchemaRejected` and its fold into `WriteFailure` as ONE edit, not two.** `runGate` returns `SchemaRejected` and `applyPlan` folds it in; **they are the same seam seen twice**, and they are the **only LIVE member of the population**.

⛔ **Do NOT start with `### 24.103`'s named target** (`generative-proposal-intake.ts`) — zero production callers. **Do the three dormant channels LAST**, where they should be cheap once the shape exists.

⚠ **`writer.ts` is dense with correction history, and several blocks record claims that were wrong and were REWRITTEN rather than struck. Read before editing — the rewrites are the record.** ⛔ **`applyPlan`'s step order is load-bearing and its step NUMBERS are cited by other comment blocks; renumbering breaks their reasoning.** *(Both from session `175`'s successor notes — a predecessor who had the file open telling you what a re-read would cost.)*

---

## RED test outline (Step 2)

⚠ **Shapes, not a fixed list.** The per-channel region cuts are Step-2.5 output, so the pins below name what each test must ASSERT; the fixtures come from your derived region set. ⛔ **Confirm RED for the RIGHT REASON before any green** — *"the signal doesn't exist"*, not a fixture typo.

1. **`<channel>_schema_rejection_produces_an_audit_signal`** — **one per uncovered channel (4).** Asserts: the refusal code **and stage**, plus a defined signal identifying the gate **by code**. ⛔ **Never a bare falsity** (`### 24.101`).
2. **`writer_scoped_stage_rejection_produces_an_audit_signal`** — the third stage specifically, because `### 24.98`'s helper cannot accept it and a two-stage copy looks complete.
3. **`intake_plan_invalid_produces_an_audit_signal`** — the second echoing member the task entry never named.
4. **`signal_carries_no_row_derived_content`** — **per channel.** Asserts: a sentinel reaching an issue `message` appears in **no** field of the signal. ⛔ **With a non-vacuity guard that the sentinel really did reach `message`** — otherwise a message-derived implementation passes. ⛔ **And the fixture must TRIP the redaction heuristic** (`L192`): a neutral sentinel makes this decorative, which is the exact defect three assertions carried last round.
5. **`row_authored_key_cannot_reach_the_signal`** — **per channel, and this is the load-bearing pin.** Asserts: with that channel's own row-keyed region populated, no row-authored key reaches the signal. ⛔ **Mutation-proven: disable that channel's cut and EXACTLY this pin reds.** ⚠ **If disabling the cut changes nothing, the cut is cutting nothing — that is a finding, report it rather than adjusting the fixture until it passes.**
6. **`candidate_is_still_refused`** — **per channel.** Asserts: still an `err`, **same code and stage as before this slice**. The fail-closed behavior is unchanged.
7. **`gcl_control_still_discriminates`** — the `GclGateError` control's existing behaviour is byte-for-byte unchanged after re-expression. ⚠ **This is the pin that proves the control survived being touched.**
8. **A compile-level demonstration** that a construction site omitting `audit` **cannot** be written — the enumeration property itself, which is the whole remedy. Record how you demonstrated it.

## Things to flag at Step 2.5

1. ⛔ **Your derived free-form-key region set per channel, on BOTH validator surfaces, with the method and boundary stated — and how it reconciles with the orchestrator's ajv-only two-region cross-check.** State what your method could have returned that mine could not. **This is the slice's highest-risk question.**
2. **The shared type's exact shape and module path**, with the `GateIssue` re-export/move decision and what it does to `visibility-gate.ts`'s public surface.
3. ⛔ **The compile-error count** from making `audit` required, before fixing the sites — the census's independent corroboration, and whether it **agrees with the ~10-site table above**. ⚠ **If it disagrees, the table is wrong and yours is right — say so plainly; the brief's premise is a hypothesis.**
4. **Per-channel consumer honesty**: which channels have a persist adapter and which get the scoped claim + a named consumer task.
5. **The `"scoped"` stage** — how the signal's `event`/stage vocabulary covers three stages where `### 24.98`'s helper covers two.
6. **Whether re-expressing `GclGateError` changes its behaviour at all.** If yes: stop and flag.
7. **The coverage map** — each acceptance bullet above → its covering test, or an explicit `not-tested-because:`. ⛔ **An unmapped bullet gets `ADD:` or an accepted `not-tested-because:`, never a silent pass.**

---

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)

⚠ **Likely NONE, and the reason is the point:** no `ARCHITECTURE.md` Appendix-A frozen model changes — `KnowledgeMutationPlan` is untouched. The types changing are **package-internal error channels**, not seam contracts.

⛔ **Flag at Step 9 if that turns out false** — in particular if the shared type must live in `packages/contracts` (Appendix-A territory) or if `WriteFailure`'s member **set** changes rather than a member's shape. **Do not edit `ARCHITECTURE.md`, `IMPLEMENTATION_PLAN.md`, or any area `CLAUDE.md` table yourself** — those are orchestrator-written, hot, same round.

## Dependencies + sequencing
- **Depends:** `### 24.98` (`124e3f45`) — **LANDED**; supplies the pattern and the `AuditSignal` construction this generalizes.
- **Blocks:** nothing queued. `### 24.104` (the `packages/domain` `e.params` protection comment) is **adjacent and contract territory** — its absence does not block you; **flag, never reach**.
- **Not in this slice:** `### 24.105` (the `cause`/Temporal-registration bound) · `### 24.106` (the legacy-row migration) · `### 24.84`'s worker leg.

## Estimated commit count
**1.** Atomic on purpose: the shared type and its consumers are one compile unit — splitting them means an intermediate commit where `audit` is required and construction sites do not supply it, i.e. **a deliberately red tree in a shared single-track checkout other sessions are working in.** ⚠ **If it grows past ~2 files beyond the list above, stop and re-scope with the orchestrator rather than absorbing it.**

## Lessons-logged candidates anticipated
- ⭐ **The copied-construction class**, if the region cut confirms it: *a safety CONSTRUCTION lifted between two gates keeps its shape and loses its subject — the decision transfers, the region does not, and the copy fails silently green.* **This would be the first witness where the artifact copied was itself a previously-banked fix**, which is what makes it worth a lesson rather than a comment.
- **Whether the compile-error count agreed with the hand-derived census** — either way it is evidence about the census method, and 029's concordance rule says a number only corroborates when it could have disagreed.

## How to invoke
Run `/tdd` with this brief. ⛔ **Do NOT run `/session-start`** — you already ran it at boot.
