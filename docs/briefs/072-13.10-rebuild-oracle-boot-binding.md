# /tdd brief — rebuild_oracle_boot_binding (Phase-4 producer arc, piece C — CLOSES the arc)

## Feature
Build the **`bootWorker` binding** that wires piece B's `gateRebuildOracle` into the live serving path — the FINAL piece of the rebuild-oracle producer arc (A `210e95e` → B `118135c` → **C**). It calls `gateRebuildOracle` inside boot's existing serving-oracle construction branch, `await`s the one-shot `compute()` once, routes any `diverged` divergence to the health surface (safe-fields-only), and binds the cached `resolveOracleBuild` accessor into `createServingCoverageReader` — closing the A+B reachability waivers. Mirror the reconcile arc's F2 boot binding (`gateReconcile` at `apps/worker/src/boot.ts:1454`) exactly.

**Still no hard line.** The owner-gated real `IndexRebuildClient` factory stays **UNBOUND** — piece C **OMITS `makeRebuildClient`** (leaves the optional field ABSENT). ⚠ **CORRECTION (impl22 Step-2.5):** the original brief said `makeRebuildClient: () => undefined` (a bad analogy to reconcile F2's `makeDbAdapter: () => undefined`) — that is WRONG here: piece B's OFF-lock is `typeof makeRebuildClient !== "function"` (a client-**PRESENCE** gate, Option A), so `() => undefined` reads as ON (typeof === "function") and also fails to typecheck (`() => undefined` ⊄ `() => IndexRebuildClient`). Reconcile differs because its ON/OFF is the `reconcile` FLAG with `makeDbAdapter` always present (returns `undefined` = degraded transport). The correct dormant binding is the field ABSENT ⇒ `gateRebuildOracle` returns `undefined` ⇒ nothing runs ⇒ `resolveOracleBuild` unbound ⇒ `oracleBuildOk` false ⇒ shipped default byte-equivalent. Binding a *real* client is the owner's arming crossing — NOT this slice.

## Use case + traceability
- **Task ID:** 13.10 (Copilot propose/serving-oracle go-live arc — runbook Phase 4 producer, piece C of A→B→C).
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (the serving-coverage `oracleBuildOk` leg boot-binding — extends the `57fef4b`/piece-A/B arc notes) + `§12` (parity GO conditions → serving degrades on any non-green leg) + `§16` (fail-closed / never-throws / System-Health `rebuild_divergence` reprojection).
- **Scope note:** cites `§16` — **widens phase scope because** the divergence-health reprojection (OBS-2 `HealthSurface.record`) + the never-throws/fail-closed boot contract are §16 System-Health invariants the serving-oracle trust gate rests on; the binding is squarely §6/§12 serving-coverage work.
- **Mirror precedent (follow it closely):** the reconcile F2 boot binding — `const reconcile = gateReconcile({...}, { makeReader, makeDbAdapter: () => undefined, makePassDeps, makeLog })` at `boot.ts:1454-1486`, and `createReconcileHealthSink` (`boot.ts:713-730`, the rule-7 reprojection). Worker **Lesson 16** (waiver-holder CLOSING slice — bind the real adapter as a nested arg inside the dormancy gate; boot line via typecheck + `/wired`, not a `bootWorker` RED; prove the chain; assert the AND-verdict still degrades). Worker **Lessons 18/21/25** (health-sink propagate + safe-fields-only). Worker **Lesson 11** (real-I/O behind an owner-provisioning gate; factory-spy zero-invocation).
- **Consumes (piece B, committed `118135c`):** `gateRebuildOracle(opts, deps): RebuildOracleWiring | undefined` — `opts { servedWorkspaceIds }`; `deps { makeRebuildClient?, makeReader, now, newHealthItemId, auditRef }`; `wiring { compute: () => Promise<RebuildOracleComputeResult>, resolveOracleBuild: () => boolean }`; `RebuildOracleComputeResult { oracleBuildOk, statuses: RebuildOracleWorkspaceStatus[] }`; `RebuildOracleWorkspaceStatus { workspaceId, status: RebuildOracleStatus }` (all in `boot.ts:799-890`).
- **Binds into:** `createServingCoverageReader`'s OPTIONAL `resolveOracleBuild?: () => boolean` (`servingContextBootReaders.ts:94-102,139`), constructed at `boot.ts:1158-1163` inside the `copilotProvenanceStamping === true && provenanceBundle !== undefined` branch (`boot.ts:1147-1148`).
- **Boot sources:** served ws = `[...servedVaultRoots.keys()]` (`boot.ts:1139-1142`, from `config.copilotGbrainWorkspaceId`); reader = `createCommittedVaultReader({ resolveVault: buildServedVaultResolver(servedVaultRoots) })` (mirror `boot.ts:1460`); clock = `backends.now`; health = `surface.record` (OBS-2, as reconcile uses at `boot.ts:1450`).

## Acceptance criteria (what "done" means)
- [ ] `bootWorker` calls `gateRebuildOracle` inside the `copilotProvenanceStamping && provenanceBundle` construction branch with: `opts.servedWorkspaceIds = [...servedVaultRoots.keys()]`; `deps.makeRebuildClient` ABSENT (omit it — client-presence OFF-lock; UNBOUND until owner arming); `deps.makeReader` the LOCAL committed-vault reader factory (mirror `:1460`); `deps.now = backends.now`; a **TOTAL** `newHealthItemId` minter + `auditRef` (unguarded in `rebuildIndexFromMarkdown` — must never throw; obligation ii).
- [ ] **Bind the accessor:** `createServingCoverageReader({...})` receives `resolveOracleBuild: wiring.resolveOracleBuild` when the wiring exists — AND-composed into `deriveServingCoverage` as ONE leg, never a standalone admit signal (obligation iv; inherent to the coverage-reader seam).
- [ ] **One-shot compute (obligation iii):** `await wiring.compute()` EXACTLY once at boot when the wiring exists; `resolveOracleBuild` returns `false` until it completes (fail-closed).
- [ ] **Rule-7 health reprojection (obligation i):** each `diverged` status routes through a sink that reprojects the `rebuild_divergence` HealthItem → `HealthFailure` using ONLY safe fields (frozen `failureClass`, a SYNTHESIZED safe message, a subjectRef from the item's ids/workspaceId) → `surface.record`; the item's free-form `message` is NEVER forwarded (safety rule 7; mirror `createReconcileHealthSink`). A `surface.record` fault PROPAGATES (Lesson 18) — but is contained so it never escapes boot as an unhandled rejection.
- [ ] **Shipped default byte-equivalent + dormant:** `makeRebuildClient` ABSENT ⇒ `gateRebuildOracle` returns `undefined` ⇒ no compute, no health routing, `resolveOracleBuild` unbound ⇒ coverage still degrades (`oracleBuildOk` false). Prove the AND-verdict STILL degrades by default (no false green — Lesson 16). Factory-spy: the probe/client is never invoked on the default path.
- [ ] **Reachability CLOSED:** the chain `bootWorker → gateRebuildOracle → (armed) compute → probeRebuildOracle` is statically reachable (`/wired` + typecheck), closing the A+B waivers; runtime stays dormant (client unbound). `/preflight` clean; repo-wide green.

## Wiring / entry point (Step 7.5)
**This IS the wiring slice.** Entry point: `bootWorker` (`boot.ts`, the serving-oracle construction branch ~1143-1174). Prove reachability via typecheck + `/wired gateRebuildOracle` (the boot call site), NOT a `bootWorker` RED (Lesson 16). Report the closed chain.

## Files expected to touch
**Modified:**
- `apps/worker/src/boot.ts` — the `gateRebuildOracle` call + `await compute()` + health routing + the `resolveOracleBuild` bind into `createServingCoverageReader`; a small `createRebuildOracleHealthSink`-style reprojection helper (mirror `createReconcileHealthSink`, co-located).

**New:**
- `apps/worker/test/boot/rebuildOracleBinding.test.ts` (or reuse a boot test) — unit-tests for the extractable pieces (the health reprojection sink; the compute-once+route-health flow over fakes). The boot call site itself is typecheck + `/wired`.

## RED test outline (Step 2)
1. **`health_sink_reprojects_safe_fields_only`** — a `diverged` status's HealthItem (with a hostile free-form `message`) reprojects to a `HealthFailure` carrying the frozen `failureClass`, a synthesized safe message (NOT the item's), a subjectRef from ids/workspaceId; `surface.record` receives only safe fields (safety rule 7).
2. **`health_sink_record_fault_propagates`** — a rejecting `surface.record` ⇒ the sink rejects (Lesson 18), non-vacuous (Lesson 15).
3. **`compute_routes_only_diverged`** — over fake statuses (corroborated + absent + diverged), only the `diverged` one is routed to `surface.record`; `resolveOracleBuild()` reflects the fold (false unless all corroborate).
4. **`default_off_wiring_absent_byte_equivalent`** — with `makeRebuildClient` ABSENT, `gateRebuildOracle` returns undefined ⇒ no compute, no health record, `resolveOracleBuild` not bound; the coverage reader degrades (assert `isDegradedCoverage`/`oracleBuildOk` false with the other legs pinned true — no false green, Lesson 16).
5. **`compute_never_throws_contains_fault`** — a health-routing fault during boot compute is contained (never an unhandled rejection out of boot), non-vacuous.

## Cross-doc invariant impact (implementer flags at Step 9)
- **Model field changes:** NONE — reuses piece B's `RebuildOracleWiring`/status types + the frozen `HealthItem`/`HealthFailure`/`IndexRebuildClient`. The health-sink helper is worker-internal.
- **Orchestrator doc rows (Step 9):** the ARCH §6 arc-CLOSE note (A+B+C) + worker **Lesson 29** (the arc lesson) — orchestrator writes at arc close (this slice closes the arc).
- **Seam (shared-contract) model touched?** No.

## Things to flag at Step 2.5
1. **Test surface split (Lesson 16).** Which pieces are unit-tested vs typecheck+`/wired`? Default vote: extract the health reprojection sink + the compute-once+route-health flow into small helpers unit-tested over fakes; the `bootWorker` call site itself via typecheck + `/wired` (booting the full worker in a unit test is out-of-scope heavy — mirror reconcile F2). Confirm.
2. **`newHealthItemId`/`auditRef` totality (obligation ii).** `rebuildIndexFromMarkdown` calls them unguarded; they must never throw. Default vote: a monotonic seq minter (mirror `reconcileIdSeq` at `:1448-1452`) + an `auditId(...)` ref — pure, total. Confirm no ambient random.
3. **Where `await compute()` sits.** Default vote: after `gateRebuildOracle` returns, guarded by `wiring !== undefined`, inside the async boot flow before the worker begins serving (fail-safe if it hasn't finished — `resolveOracleBuild` stays false). Confirm ordering doesn't block boot on a slow probe (it only runs on the owner-armed path anyway).
4. **subjectRef for the rebuild_divergence health item.** Default vote: `item.factIdentity ?? item.parityReportRef ?? workspaceId` (mirror `createReconcileHealthSink:716-717`), stringified. Confirm; the precise OBS-2 dedupe subjectRef finalizes at arming (name it).

## Dependencies + sequencing
- **Depends on:** piece B (`118135c`, committed). **CLOSES the rebuild-oracle producer arc** (A+B+C).
- **Blocks:** nothing in-arc. Real green admission remains gated on the owner ARMING GATE (provision the real client + signing key + corpora + governance eval + the flip) — and on the parallel KNOWLEDGE-track `rebuild.ts:160` hardening landing before any real client binds.

## Estimated commit count
**1** — one focused wiring slice. **Mandatory dual review (security-reviewer [invariant-touching] + code-quality-reviewer [every-slice])** — the binding feeds the serve-time trust gate + routes health (safety rule 7). Not bundled.

## Lessons-logged candidates anticipated
- **Convention candidate:** the arc-CLOSE lesson (worker Lesson 29) — the producer arc's boot binding closes the waiver by wiring the owner-gated client UNBOUND (byte-equivalent), routes divergence health safe-fields-only, and proves the AND-verdict still degrades by default. Flag at Step 9 (orchestrator banks it at arc close).
- **Architecture-doc note candidate:** the `§6` arc-close note (orchestrator writes).
