# /tdd brief — source_grounded_set_and_refusal_channel

## Feature

Two synthesis-path arming preconditions, bundled: **13.8l** — the SOURCE path has no grounded set, so apply the
existing `admitGroundedPath` to its plan targets (the fourth door to one invariant); **13.8m-A** — refusals reach
nobody, so add a code-only refusal channel to the receipt.

## Use case + traceability

- **Task ID:** 13.8l — bundled with **13.8m-A** (the knowledge half of 13.8m; see the split below)
- **Architecture sections it implements:** `ARCHITECTURE.md §6` (KN-7 "rejected AND audited", KN-10/KN-12)
  <!-- The error-convention section is NOT cited: KN-7 in §6 is the governing requirement for "rejected AND
       audited", so the audit obligation derives from §6 and needs no second anchor. -->
- **Related context:** **13.8k** (`grounded-path.ts`, the single admission point — reuse it, do not re-derive) ·
  **13.8j** (namespacing, closed the stub-minting door) · contracts **L64** (enumerate the CONCEPT, not the
  construction) · **L70** (verify the property, not the mechanism) · §ARM-RESEARCH preconditions (6) and (7)

## ⛔ SCOPE SPLIT — 13.8m is CROSS-AREA; you take the producer half only

The plan entry for 13.8m names *"the synthesis receipt shape · **the worker consumer** · tests"*. The standing
rule is **no cross-area single-implementer verticals**, producer-first. So:

- **13.8m-A (YOURS):** the receipt gains a code-only `refusals: readonly GroundedPathRefusal[]`, populated where
  `admitGroundedPath` refuses. Dormant — nothing consumes it yet, and that is correct.
- **13.8m-B (WORKER'S, later):** the worker consumer reads and surfaces them. **Not in this slice.** Do not wire it.

Ship the producer half so the channel exists and is testable; the consumer lands separately. Say so at Step 9 so
I can brief 13.8m-B against the shape you actually built.

## Why these two bundle

Same subsystem, same admission function, and 13.8m-A is meaningless without 13.8l — the refusal channel exists to
report what the newly-applied admission refuses. One reviewer context, one commit.

## Verified premises (falsify first)

1. **13.8l:** `planSynthesis` output reaches the `KnowledgeMutationPlan` and `touchedNotePaths` **unguarded** on
   the source path. A model-proposed `patches:[{path:"index.md"}]` is stopped only by the worker adapter's
   realpath containment — which prevents **escape** but **not COLLISION** with a writer-owned surface.
2. **13.8k** already established the invariant on the MEETING path and closed three routes; `admitGroundedPath`
   is the single admission point. This is the **fourth door to the same invariant**, not a new invariant.
3. **13.8m:** `admitGroundedPath` refuses correctly, but the receipt has no refusal channel — so a
   poisoned-candidate attack is **byte-identical to a benign empty run**. Nothing surfaced, nothing audited,
   against KN-7's "rejected **and** audited".

## Acceptance criteria

**13.8l**
- [ ] A model-proposed patch/create targeting a writer-owned structural surface **cannot reach** the source
      path's KMP or `touchedNotePaths`.
- [ ] Enforced through **the SAME `admitGroundedPath`** — one admission point, not a second copy.
- [ ] 13.8k's one-enforcement-point structural pin is **extended to cover the source path**, so a future
      unguarded entry point fails it.
- [ ] Legitimate source-path targets are untouched — grounding/patching still works (non-vacuity).

**13.8m-A**
- [ ] A refused path produces an observable, **code-only** refusal on the receipt.
- [ ] A benign empty run is **distinguishable** from a refused one.
- [ ] ⚠ **No path text, no title text, no candidate content crosses** — reason codes only (safety rule 7).
- [ ] `/preflight` clean.

## ⚠ The failure mode most likely here, from this task's own history

13.8k's Step-9 and 13.8j's before it both produced the same lesson twice: **the fix verified the MECHANISM and
not the PROPERTY** (L70). 13.8j applied a namespace via an object-literal lookup, so `__proto__` resolved back to
the vault root; 13.8k called `admitInto` on every path but returned `true` for an already-grounded one.

So: **do not pin "admitGroundedPath is called on the source path."** Pin **"no writer-owned surface can enter
the source path's grounded set or plan targets, by any route."** And per the implementer's own committed
practice — **mutation-test the invariant pin inside this slice**: break the guard, confirm the pin goes RED.
Both prior unsound pins looked fine on inspection.

## Wiring / entry point (Step 7.5)

The source path is already bound **dormant** by 13.8d (`createIngestRewriteAdapter`). No new wiring. **Reachability
waiver applies** — the capability is inert behind its arming gate, and 13.8m-A's channel has no consumer until
13.8m-B. State the waiver at Step 7.5 rather than inventing a consumer.

## Files expected to touch

**Modified:**
- `packages/knowledge/src/synthesis/ingest-rewrite.ts` — apply admission to the source path's plan targets
- the receipt shape (`ingest-rewrite.ts` / `meeting-rewrite.ts` as the shape dictates) — the `refusals` field
- `packages/knowledge/src/synthesis/grounded-path.ts` — **reuse**; extend only if the refusal reason codes belong there
- `packages/knowledge/test/` — extend the existing grounded-path suite, don't fork

⛔ **Do NOT touch** the worker consumer (13.8m-B) or re-path resolved notes (13.8j pinned `resolved_paths_unchanged`).

## RED test outline (Step 2)

1. **`source_path_patch_targeting_a_structural_surface_is_refused`** — Asserts: `patches:[{path:"index.md"}]` cannot reach the KMP or `touchedNotePaths`. Why: §6 KN-12 — the fourth door.
2. **`source_path_uses_the_same_admission_point`** — Asserts: enforcement is `admitGroundedPath`, not a parallel check. Why: a second copy diverges the moment the grammar changes (L39/forbidden-pattern #6).
3. **`legitimate_source_targets_still_land`** — Asserts: an ordinary note patch is unaffected. Why: **non-vacuity** — a guard that refuses everything also "prevents collision" while breaking the feature.
4. **`one_enforcement_point_pin_covers_the_source_path`** — Asserts: 13.8k's structural pin now fails if a second unguarded entry point is added. Why: the invariant is the unit, not the guard site.
5. **`a_refused_path_is_observable_on_the_receipt`** — Asserts: refusal appears with a reason code. Why: §6 KN-7 rejected AND audited.
6. **`a_benign_empty_run_is_distinguishable_from_a_refused_one`** — Asserts: the two produce different receipts. Why: the whole point — today they are byte-identical.
7. **`refusals_carry_no_path_or_title_text`** — Asserts: codes only; a hostile path/title never appears in the receipt. Why: safety rule 7 — the refusal channel must not become the leak.

## Cross-doc invariant impact

- **Model field changes:** ⚠ **the receipt shape changes** (`refusals` added). If the receipt is a frozen
  Appendix-A seam, that is a cross-doc invariant edit + schema snapshot in the same round — **flag at Step 2.5
  before building** and I will route it. If it is an internal knowledge type, no doc row is needed.
- **Orchestrator doc rows to write hot:** `ARCHITECTURE.md §6` — the invariant now covers the source path, and
  refusals are auditable. I write it; you flag.

## Things to flag at Step 2.5

1. **Is the receipt a frozen contract surface?** (above). My default vote: **check first, build second** — a
   frozen-seam change needs the 4-file ADR-008 set in the same round (L49).
2. **Where do the refusal reason codes live?** My default vote: **beside `admitGroundedPath`**, since it is the
   only thing that produces them — the predicate that decides owns the vocabulary of its refusals.
3. **One `refusals` array, or per-plan?** My default vote: **one per receipt**, matching 13.8d's "one
   plan-synthesis per run" shape — but you built that, so overrule me if the receipt's grain differs.
4. **Does the meeting path already have a refusal channel** you should reuse rather than parallel? My default
   vote: **reuse if it exists**; if it doesn't, build one shape both paths can share, so 13.8m-B has a single
   consumer contract.

## Dependencies + sequencing

- **Depends on:** 13.8k (landed) — `admitGroundedPath` and its structural pin.
- **Blocks:** the synthesis arming crossing (§ARM-RESEARCH preconditions 6 and 7); **13.8m-B** (worker consumer);
  13.8f-B's ability to distinguish refused from nothing-to-do.

## Estimated commit count

**1.** One subsystem, one admission point, one receipt. ⚠ **Safety-relevant** (KN-12 writer-owned surfaces,
rule 7 on the refusal payload) — `security-reviewer=invariant` at Step 8, and it does not bundle with anything else.

## Lessons-logged candidates anticipated

- **Convention candidate** — a refusal channel is part of a guard's contract: a guard that refuses invisibly is
  indistinguishable from a guard that never ran (the sibling of evalsec's "asserts a gate SAID NO, not that it DECIDES").
- **Architecture-doc note candidate** — §6: the grounded-path invariant now spans meeting + source paths through
  one admission point, with auditable refusals.

## How to invoke

1. Read this brief end-to-end.
2. Answer Step-2.5 Q1 (frozen seam?) **before** writing the receipt change.
3. Run `/tdd source_grounded_set_and_refusal_channel`.
