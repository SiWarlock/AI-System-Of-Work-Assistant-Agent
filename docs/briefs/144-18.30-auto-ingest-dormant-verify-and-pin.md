# /tdd brief — auto_ingest_dormant_verify_and_pin

## Feature
A tight **verify-and-pin assurance pass** over the already-built-dormant auto-ingest source-arrival trigger, BEFORE the eventual owner-gated 18.10 ARM. Mostly CONFIRM (cite existing pins); FILL the one genuine gap — that the source-ingestion path reached from the auto-ingest **dispatch** cannot bypass **ING-7 admission** or the **rule-5 egress veto** on a hostile input. **No behavior change, no arming, no new wiring** — the trigger stays dormant + flag-gated OFF (`SOW_INGEST_WATCH`).

## Use case + traceability
- **Task ID:** 18.30
- **Architecture sections it implements:** `ARCHITECTURE.md §19.5` (real extraction legs), §9 (source ingestion workflow), §5 (egress veto fail-closed), §16 (untrusted-content admission / ING-7)
- **Related context:** the auto-ingest trigger is ALREADY built + wired dormant end-to-end (scoping): `gateAutoIngest` (`apps/worker/src/boot.ts:665`), the vault watcher (`apps/worker/src/watch/vaultWatcher.ts`, L37), the dispatch (`dispatchSourceIngestion.ts:101`). The owner-gated ARM is task **18.10** (out of scope — do NOT flip `SOW_INGEST_WATCH` / provision Temporal). Lessons: **L28/L50** (truthy-not-`true` arming guard), **L37** (output-subtree feedback-loop exclusion), **L47** (source routes THROUGH the broker so `admitJob`/ING-7 runs), rule 5 (egress veto).

## Acceptance criteria (what "done" means)
- [ ] **CONFIRM (cite, no new test needed unless a gap is found):**
  - gateAutoIngest OFF byte-equivalence + the L28 truthy-not-`true` strict-arming guard — `apps/worker/test/boot-auto-ingest-gating.test.ts:22,103,118,173` (+ the L50 assembled-root guard). Verify these still pass + genuinely pin `opts.autoIngest !== true ⇒ undefined, zero thunk invocation`.
  - L37 output-subtree exclusion in BOTH entry paths (`computeOutcome` pre-I/O + `onEvent` no-timer) — `apps/worker/test/integration/vaultWatcher-live.test.ts:326,339,376,395`. Verify both-path coverage + the shared-constant no-drift tie.
- [ ] **FILL the genuine gap — the auto/source-dispatch path routes THROUGH the safety gates (no bypass), pinned with a HOSTILE input:**
  - **ING-7 admission:** a source job carrying untrusted/`carriesRawContent` content with a mutating tool policy, driven through the assembled source-ingestion path, is **admission-REJECTED** (`UNTRUSTED_CONTENT_MUTATING_TOOL`) — never runs (rule 6 / L47). _(If already pinned on the assembled source path, cite it and skip.)_
  - **rule-5 egress veto:** a source job with **employer-raw content + ack OFF + a cloud route**, driven through the same assembled source-ingestion path, **fails closed at `egress_veto`** (no run leg, no cloud) — the existing `sourceIngestion-live` only exercises a LOCAL route (veto allows via loopback, per its `:112-115` comment), so the non-local/employer-raw case on this path is the gap. _(Reuse the `egress-veto-assembled.test.ts` vectors, tied to the source-ingestion assembly.)_
- [ ] Worker `/preflight` clean (typecheck + `pnpm --filter @sow/worker test`; repo lint/format env-waived). No `apps/desktop` edits (territory — see Step-2.5 Q2).

## Wiring / entry point (Step 7.5)
**None — this is a pure test/assurance slice.** No production code changes (or, if a genuine wiring-visible gap surfaces, it's a test-only pin). The trigger + gates are already wired; this slice verifies + pins their dormant safety properties. Entry points under assurance: `gateAutoIngest` (`boot.ts:665`), the vault watcher (`vaultWatcher.ts`), `dispatchSourceIngestion` → the assembled source-ingestion run leg (`buildActivities.ts`), through `admitJob` (`source-extraction.ts:182`) + the broker egress veto.

## Files expected to touch
**New / Modified (test-only expected):**
- `apps/worker/test/…` — the auto/source-dispatch-path safety-tie assurance test(s) (ING-7 admission-reject + rule-5 employer-raw+cloud fail-closed). Prefer extending an existing assembled/integration test (`sourceIngestion-live.test.ts` or `egress-veto-assembled.test.ts`) over a new file if it fits.

**No production-code change is expected.** If a genuine safety GAP requires a production edit (not just a test), **STOP and flag at Step 2.5** — that changes this from a pin slice to a fix slice (and re-opens the "is the dormant wiring actually safe" question, which would be a Finding to the orchestrator).

**Do NOT touch `apps/desktop/`** (desktop territory — see Q2).

## RED test outline (Step 2)
1. **`auto_source_dispatch_untrusted_mutating_admission_rejected`** — an untrusted/`carriesRawContent` source job with a mutating tool policy, through the assembled source-ingestion path ⇒ admission-REJECTED, run leg never reached. Why: ING-7 / rule 6 / L47 on the auto path. _(RED only if not already pinned on this path.)_
2. **`auto_source_dispatch_employer_raw_cloud_fails_closed`** — employer-raw + ack OFF + cloud route on the source-ingestion path ⇒ DENY at `egress_veto`, no run/cloud. Why: rule 5 tied to the auto/source path (the local-route `sourceIngestion-live` gap).
3. **(confirm, may be assertions over existing behavior)** re-affirm gateAutoIngest OFF byte-equivalence + L37 both-path exclusion by citing/re-running the existing pins (add a pin only if a genuine hole is found).

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** NONE. Test/assurance only.
- **Orchestrator doc rows to write hot (Step 9):** none expected. If the pass surfaces a real dormant-safety GAP (a production fix needed), that's a **Finding** → flag to the orchestrator (I escalate to the lead). Otherwise a possible LESSONS note that the auto-ingest dispatch path's ING-7/veto tie is now pinned pre-ARM.

## Things to flag at Step 2.5
1. **Confirm-vs-fill split.** For each of the 4 properties, is it already pinned (cite + skip) or a genuine gap (add the tie test)? My read: gateAutoIngest-OFF + L28 + L37 are SOLID (cite); the **ING-7-admission-reject + rule-5-employer-raw-cloud-fail-closed tied to the source/auto dispatch path** is the likely gap (`sourceIngestion-live` only drives a LOCAL route). Confirm this read against the actual tests at Step 2.5; if the tie already exists, this slice is a pure confirm + we say so. **Keep it tight — do not add redundant tests over already-pinned properties.**
2. **Desktop leg (`SOW_INGEST_WATCH` env read, `apps/desktop/main/index.ts:72`).** It's UNPINNED in `apps/desktop`, but the worker `opts.autoIngest !== true` chokepoint (pinned, L28) re-gates its output, so a wrong desktop read still can't arm. My assessment: LOW value + it's DESKTOP territory. **Do NOT touch `apps/desktop`.** I'm flagging the lead separately (recommend SKIP the desktop pin; if the lead wants it, a desktop-impl does it). Assume SKIP unless told otherwise.
3. **If a production fix is needed** (a real dormant-safety hole, not just a missing test) — STOP + flag at Step 2.5; that's a Finding, not a pin.

## Dependencies + sequencing
- **Depends on:** the auto-ingest dormant wiring (built; C3b arc + S9/18.10 `29555821`); the crossing (18.11–18.27). Sequenced AFTER 18.29 (same impl; independent scope).
- **Blocks:** nothing — it's pre-ARM assurance for the owner-gated 18.10 ARM (which stays out of scope).

## Estimated commit count
**1.** A focused assurance/pin slice (mostly confirm + 1–2 tie tests). security-reviewer mandatory (safety path); its own commit.

## Lessons-logged candidates anticipated
- **Convention candidate** — pre-ARM assurance for an already-built-dormant owner-gated trigger: confirm the OFF byte-equivalence + arming guard + feedback-loop exclusion are pinned AND tie the downstream safety gates (ING-7 admission + rule-5 veto) to the trigger's dispatch path with a hostile input, so the eventual ARM flips a proven-safe seam.
- **Architecture-doc note candidate** — none expected (a verification pass).

## How to invoke
1. **Read this brief end-to-end** — the confirm-vs-fill split (Q1) is the whole slice; don't over-pin.
2. **Run `/tdd auto_ingest_dormant_verify_and_pin`.**
3. **Step 2.5** — ping back which properties are already-pinned (cited) vs the genuine gap you'll fill, + the asserted-invariant lines. security-reviewer MANDATORY. If a production fix (not a test) is needed, STOP and flag it as a Finding.
4. **Step 9** — report only if a Finding/load-bearing call surfaced (else settle directly); note the desktop-leg decision (assumed SKIP).
