# /tdd brief — applyplan_never_throw_hole

## Feature

Close `### 24.72`: `applyPlan`'s **§16 never-throw promise has a CAST-FREE hole.** Bare `await`s on
`deps.audit.append` and `deps.revisions.record` throw **AFTER** the Markdown commit is durable, so a real
infrastructure fault (DB down, lock contention) makes `applyPlan` reject with an **untyped** error.

⛔ **MEASURED, NOT SUSPECTED — and measured with FULLY WELL-TYPED DEPS. No cast, no `as never`:**

| injected fault | result | `vaultWritten` |
|---|---|---|
| `audit.append` async-reject | **THREW** | **TRUE** |
| `audit.append` sync-throw | **THREW** | **TRUE** |
| `revisions.record` reject | **THREW** | **TRUE** |

⇒ **The docblock's qualifier *"NEVER throws … FOR WELL-TYPED DEPS" DOES NOT SAVE IT. These ARE well-typed deps.**

⛔⛔ **THE CODE ALREADY ANTICIPATED THIS AND DID NOT IMPLEMENT IT.** `writer.ts:344-346`, verbatim at source:
> *"Markdown is now durable (safety rule 1: committed_to_markdown never rolls back), so a recording fault is a
> System-Health concern, not a rollback — the commit stands."*

**The commit standing is CORRECT and INTENDED.** ⛔ **But NOTHING CATCHES THE FAULT TO MAKE IT A SYSTEM-HEALTH
CONCERN — it escapes as a throw.** `L123`: **the sentence is the entire enforcement, and it is a comment.**

## Use case + traceability

- **Task ID:** `### 24.72` (shared task `#60`)
- **Architecture sections it implements:** `ARCHITECTURE.md §16`, `§6`
- **Safety:** ⚠ **SAFETY-ADJACENT, GRADING OPEN — see below. Step-9 routes to the lead** (ALL SEVEN auto-route).
- **Depends:** — · **Blocks:** —

> ⚠ **This brief widens phase scope because** `§16` is not in Phase 24's declared anchor set
> (`§13 §19.11 §3 §4 §6`), and `§16` is the error-convention section this defect is *defined against*.
> ⛔ **NOT resolved by adding `§16` to Phase 24's anchor set** — the resolution the lead ruled against on
> `### 24.54`.

## ⛔⛔ THE GRADING IS OPEN AND YOU MUST NOT CLOSE IT

**It is unambiguously a `§16` violation. Whether it is ALSO a rule-1 matter is the LEAD'S call, not yours and
not mine.** The consequence is a **committed Markdown mutation whose `AuditRecord` never lands.**

- **FOR rule 1:** `writer.ts:344` invokes *"safety rule 1"* in this exact region, and this is the sole-writer chokepoint.
- **AGAINST:** that citation is about why the commit does **not roll back**, not about the audit record. The
  one-writer guarantee is intact — KnowledgeWriter still did the write; what is missing is the **RECORD** of it.
  **Whether rule 1 covers the ACT or the ACCOUNTING is the open question.**

⭐ **Both the implementer who found it and the orchestrator deliberately withheld a rule number, on the same
reasoning: *a correct analysis under a wrong headline is what gets quoted.*** **Hold that line.** Ship the fix,
route Step 9 to the lead, and let the grading land where it belongs. **Do not assert a rule number in a commit
message, a comment, or a test name.**

## ⛔ DO NOT CLOSE THIS BY PINNING THE THROW

**Adding a test that asserts either throw would pin a `§16` VIOLATION AS INTENDED BEHAVIOUR** and make it
harder to close — the same reasoning that kept `#57` comment-only (`L69`: a tested false assurance is an active
defense of the defect; it inverts the review gate from a check into a lock).

## Acceptance criteria

- [ ] `applyPlan` returns a **typed failure** rather than rejecting when `audit.append` or `revisions.record` faults.
- [ ] ⛔ **The Markdown commit is NOT rolled back** — the commit standing is intended, and a fix that rolls it
      back breaks safety rule 1 while "fixing" §16. Pinned explicitly.
- [ ] **The fault becomes the System-Health concern `writer.ts:344-346` already promises** — i.e. the signal
      reaches a consumer, not just a return value. ⚠ `L106`: a correctly-typed signal that reaches no consumer is
      indistinguishable from the failure it was built to distinguish. **Name the consumer or record its absence
      as a tracked task in this round.**
- [ ] ⛔ **The new failure member does NOT reuse `workspace_path_violation`, nor any code asserting a POLICY
      verdict that a config/infra fault did not earn** (`#57`'s reasoning). An infra fault is not a policy denial.
- [ ] **All three measured shapes are covered** — `audit.append` async-reject, `audit.append` sync-throw,
      `revisions.record` reject. ⚠ **A sync throw and an async rejection are different code paths**; a fix
      covering only `await`-rejection leaves the sync throw live.
- [ ] **An honest-path control** — a normal commit with no fault still succeeds and still records. ⛔ Mandatory:
      without it the suite cannot distinguish a working fix from one that fails everything (`L80`).
- [ ] The `writer.ts:344-346` comment is **updated to describe what now happens**, not deleted — it was true
      about intent and is about to become true about mechanism (`L153` half 1: a claim true-for-a-different-reason
      gets rewritten, never struck).
- [ ] `/preflight` clean **and** a repo-wide `tsc` (`L129` — a green vitest run is not a typecheck).

## ⛔ SWEEP: EXPLICITLY SCOPED OUT, WITH ITS BOUNDARY

The recorded Done-when says *"a repo-wide sweep for the same shape is either run or explicitly scoped out with
its boundary (`L100`)."* ⇒ **IT IS SCOPED OUT HERE, and its home is `### 24.73` (`#61`).**

**Boundary, stated so the scope-out is checkable rather than a shrug:** this slice covers **`applyPlan` in
`packages/knowledge/src/knowledge-writer/writer.ts` ONLY.** ⛔ **NOT swept:** `packages/policy`, `packages/workflows`,
`apps/worker`, or any other `§16` never-throw docblock.
⚠ **The known sibling is `### 24.65` / `#58`** — *"§16 never-throw is NOT total for `validateProjectionVisibility`"*
— **a second area, found independently, neither aware of the other.** ⭐ **That pair is the finding that
outranks either instance, and it belongs to `24.73`, not here.**

## Wiring / entry point (Step 7.5)

`applyPlan` is the production write path, reached via `createCommitActivity` from all three compositions.
**Existing reachable path; no new surface.** ⚠ If the System-Health consumer does not exist, that is a
**producer-first split** — flag at 7.5, do not build a cross-area consumer inside this slice.

## Files expected to touch

**Modified:** `packages/knowledge/src/knowledge-writer/writer.ts` (`applyPlan` — the `audit.append` /
`revisions.record` sequence at `:344`+) · its test file · the `§16` failure-variant declaration if a new member
is needed.

## RED test outline (Step 2)

1. **`audit_append_async_rejection_returns_typed_failure_not_a_throw`** — asserts a typed failure; asserts the
   Markdown commit **stands**.
2. **`audit_append_sync_throw_returns_typed_failure_not_a_throw`** — the distinct path.
3. **`revisions_record_rejection_returns_typed_failure_not_a_throw`** — asserts commit stands.
4. **`the_typed_failure_does_not_assert_a_policy_verdict`** — asserts the member is not `workspace_path_violation`
   or any policy code.
5. **An honest-path control** — no fault ⇒ success ⇒ audit row recorded.

⛔ **Each pin asserts its own preconditions so it cannot go vacuous** — the injected fault **fired**, and the
vault write **happened** before it. **A pin that silently stops reaching the scenario is byte-indistinguishable
from one that passes honestly** (`L75`, `L79`). **Report the test COUNT with the result** — a file that did not
run and a file whose tests all passed are indistinguishable from the pass/fail line alone.

## Cross-doc invariant impact

⚠ **LIKELY — flag at Step 9, do not edit the docs yourself.** A new `§16` failure variant may be a cross-doc
invariant (`ARCHITECTURE.md §16` + the `packages/contracts/CLAUDE.md` table). **I write those rows; you flag.**

## Things to flag at Step 2.5

1. ⛔ **Should the two faults be DISTINGUISHABLE from each other, or is one member enough?** My default vote:
   **distinguishable.** Folding an audit-record fault and a revision-record fault into one opaque member
   re-creates `L114` — a caller that cannot tell which record is missing cannot remediate either. **But say so if
   the §16 taxonomy makes that expensive; I would rather hear the cost at 2.5.**
2. **Where does the catch go — around each await, or one try around the tail?** My default vote: **whichever
   keeps "the commit stands" unconditional.** A single try spanning the commit would let a future edit slip a
   rollback inside it.
3. **Is there an existing System-Health consumer, or does this produce a signal with nowhere to go?** ⛔ If the
   latter, **say so** — that is `L106`, and it gets filed as a task this round rather than assumed.
4. **Anything that makes you doubt the grading is genuinely open** — if you find something that settles rule-1
   either way, that is a Finding for the lead, not a decision for the slice.

## Dependencies + sequencing

- **Depends on:** nothing. **Blocks:** nothing.
- ⚠ **Interacts with `### 24.73`** (`#61`, the sweep — scoped out above) and **`### 24.80`** (`#69`, `WriteSuccess`
  cannot distinguish APPLIED from ALREADY-APPLIED). ⛔ **Do not fold either in.** `24.80` touches the same return
  type and is a separate question; if your change makes it cheaper, **say so at Step 9** rather than absorbing it.

## Estimated commit count

**1.** Safety-adjacent ⇒ **its own commit, never bundled.**

## Lessons-logged candidates anticipated

- **Convention candidate** — a docblock qualifier (*"for well-typed deps"*) that reads as a bound on the promise
  while the actual hole sits entirely inside the qualified set.
- **Architecture-doc note candidate** — `§16` on what `applyPlan` guarantees after the Markdown commit is durable.

## How to invoke

1. Read this brief end-to-end. ⛔ **Do not assert a rule number anywhere.**
2. Run `/tdd applyplan_never_throw_hole`.
3. **Step 9** — safety-adjacent, grading open ⇒ routes to me **and** `team-lead`.
